# A lawful common gauge realizes the 48 primitive `K4` buckets

**Status: FINITE-EXACT COMMON-ANCESTRY SUPPORT REALIZATION + FIVE-PRIME
SPECTRAL CERTIFICATE; INDEPENDENTLY AUDITED AND ACCEPTED AT THIS SCOPE.**  The actual 39 guard atoms
of THM-3514 are inserted as Boolean partitions on the linked THM-2471 arrival
and source nodes before transfer, root lifting, integration, or Fourier
contraction.  The common torsor gauge realizes exactly the 48 owner-active
nonzero-drift buckets.  Retaining its absolute offset restores full
`K4 x F_13` spectral support at every nonzero owner character.  This is not
an identity with the frozen U_full endpoint weights, a physical current, a
grouped coefficient, a row exclusion, or LRC(14).

## 1. Inheritance and the missing object

Three exact results now meet at one typed boundary:

- THM-2471 equation (31) realizes its weighted root service as a finite
  Boolean fibre product on linked nodes `X_(u,a)` and `Y_(q,e')` over one
  outer base;
- THM-2594 proves that this `r=5` fibre supports a nonzero refined
  contraction, with collision-root drift `s=u-q` and the whole `s=0` slice
  identically zero;
- THM-3514 factors the frozen U_full endpoint bank into 39 guard atoms and
  117 chamber/drift types, with 52 owner-active types forming a full
  `K4 x F_13` Walsh spectrum.

The corrected root-difference probe showed that `s` is the right drift type,
but every linear map after marginalization was annihilator-only.  The missing
object was therefore a Boolean address relation before the sheet and root
sums.  THM-2471 already licenses precisely that object: its partition
`{P_omega}` may be any rational Boolean partition on each linked node.

Use the actual U_full guard partition

```text
I_(a,C),
a in F_13,
C in {L=[0,1), M=[1,6), R=[6,7)}                    (1)
```

as `P_omega`.  This does not identify `X` and `Y` as one point.  It evaluates
the same rational partition separately at the two typed linked nodes inside
the one THM-2471 Boolean fibre.

## 2. The exact joint table

Write `a,b` for the guard-sheet labels of the atoms containing `X` and `Y`,
and retain

```text
d=b-a,                    address drift,
s=u-q,                    collision-root drift.       (2)
```

The companion computes

```text
M(C,D,d,s)
 =sum_(a,b,u,q: b-a=d, u-q=s)
   integral A_(a,C)(y,u) F_(b,D)(y,q) dy,             (3)
```

where `A_(a,C)` and `F_(b,D)` are obtained by multiplying the original
Boolean functions by the atom indicators *before* applying `P_(13^5)`.
Thus (3) is a regrouping of literal THM-2471 summands, not a coupling of
already integrated marginals.

The 39 arrival atom inputs occupy 21 atoms and the 39 source inputs occupy
20.  After transfer, the exact atom-pair support is `420/1521`.  Summing all
atom labels reproduces every root colour, the structural zero

```text
M(-,-,-,0)=0,                                         (4)
```

and the anchor

```text
sum M = 169 I_5
      =48602521488933856/1996669193081744181.          (5)
```

Equations (4)--(5) are positive and hostile controls: the refinement neither
creates diagonal service nor loses any mass.

## 3. The common torsor gauge gives exactly `48/52`

The common root/sheet torsor gauge is

```text
c_L=a-u,                 c_R=b-q.
```

Therefore

```text
c_L=c_R
  iff a-u=b-q
  iff s=-d.                                                (6)
```

Predicate (6) is character-independent and is imposed while all four labels
are still present.  Its complete support is:

```text
72/117 chamber/drift types;

for each of
(L,L),(L,R),(M,L),(M,R),(R,L),(R,R):
  every d!=0 survives and d=0 vanishes;

every type with right chamber M vanishes.              (7)
```

THM-3514's owner factor removes the middle chamber on both endpoint legs.
Restricting (7) to its active carrier leaves

```text
{L,R}^2 x F_13^*,
support 4*12=48 of the 52 U_full buckets.               (8)
```

The four missing buckets are exactly `d=0`.  By (6) they would require
`s=0`, so their absence is not a numerical accident: it is the proved
first-collision diagonal law (4).  The opposite convention `s=d` has the
same support count, so support alone cannot choose the orientation; the
typed root convention and owner phase remain load-bearing.

## 4. Rank four survives, but two zero modes reveal the marginal debt

Let the four rows of (8) be ordered

```text
LL, LR, RL, RR.                                        (9)
```

Their `4 x 13` rational matrix has rank four, independently certified modulo
547 and 911.  Every row is supported at exactly the twelve nonzero drifts.
Its four Walsh rows have exact drift-Fourier support

```text
W_0 : 13/13,
W_L : 12/13,
W_R : 12/13,
W_X : 13/13.                                          (10)
```

The only missing coordinates in (10) are the zero frequencies of the two
one-sided Haar rows.  Direct integer sums give

```text
sum_d LL(d)=sum_d RR(d)=586161995511426137294400,
sum_d LR(d)=sum_d RL(d)=593595775077439052580480.      (11)
```

Consequently `W_L(0)=W_R(0)=0`, while `W_0(0)` and `W_X(0)` are nonzero.
For every primitive thirteenth root, nonvanishing is exact rather than only
modular: a rational polynomial of degree at most 12 vanishing at a primitive
root must be a scalar multiple of `Phi_13`; its constant coefficient here is
zero while another coefficient is nonzero.  Hence all twelve primitive
frequencies of every Walsh row survive.

This is the minimum-joint-coordinate theorem in a realized form.  The
zero-frequency sector retains the uniform and checkerboard coordinates but
loses the two one-sided marginals.

## 5. The absolute common offset restores spectral closure

On (6), retain rather than sum the common value

```text
c=a-u=b-q in F_13.                                    (12)
```

For owner frequency `k`, twist by `zeta^(-kc)` before summing.  This is the
absolute phase predicted by the translated-guard identity in THM-3518; it is
not a new marginal manufactured after the fact.

At `k=0`, equations (10)--(11) remain.  At every

```text
k in F_13^*,                                           (13)
```

the four chamber rows have rank four and all four Walsh rows have all
thirteen drift-Fourier modes nonzero:

```text
support(k; W_0,W_L,W_R,W_X)=(13,13,13,13).             (14)
```

Each coordinate in (14) is certified nonzero in at least one of the exact
split fields at primes

```text
547, 911, 1093, 2003, 2549.                            (15)
```

A nonzero reduction proves the corresponding cyclotomic integer is nonzero.
All thirteen owner-frequency matrices have rank four at one or more of the
same good primes.  Thus the common offset supplies exactly the two spectral
coordinates erased by its own zero-character marginal.

This is a support-level spectral closure theorem candidate, not the LRC
spectral closure crux itself.  The values in (3) are ancestry-service masses;
they have not been identified with THM-3514's frozen endpoint bucket weights.

## 6. What this rules in and out

The earlier linear no-go is now explained rather than contradicted:

```text
after summing c:
  two one-sided zero modes are lost;

before summing c, at every k!=0:
  the full K4 x F_13 spectrum is present.              (16)
```

No fixed channel matrix acting on the marginalized root bank can recreate
the missing `c` coordinate.  Conversely, the exact address lift shows that
the rank obstruction was not a lack of lawful ancestry capacity.

The next decisive test is not another support census.  It is a typed
pullback of the actual THM-3514 atom weights along (1), followed by the
common-gauge/owner-phase contraction inside (3).  The required comparison is
with the frozen `q_H-q_q5` bridge and its four Walsh rows.  A mismatch must be
reported as a weight/connection obstruction; a match would still require the
temporal-current and grouped-coefficient gates.

## 7. Connection contract

| field | value |
|---|---|
| source | THM-2471 Boolean stalk for the proved `r=5` row |
| target labels | THM-3514's 39 guard atoms and 117 chamber/drift types |
| map | evaluate the same atom partition separately on `X_(u,a)` and `Y_(q,e')` |
| common support | `a-u=b-q`, equivalently `s=-d` |
| preserved | root drift, guard sheet/chamber, Boolean provenance, service mass |
| destroyed by marginal | absolute common offset `c` and its owner phase |
| hostile | `s=d` has the same support; orientation needs the typed phase convention |
| exact boundary | full spectral capacity, but no endpoint-weight equality or current |

## 8. Reproduction

```text
python -B 04-computation/lrc_r5_common_ancestry_guard_atom_root_drift_probe_20260816.py
python -B -O 04-computation/lrc_r5_common_ancestry_guard_atom_root_drift_probe_20260816.py
```

Normal and optimized transcripts are byte-identical.  The semantic digest is

```text
3d8c88fb7b9762f41ef35c00d980b99fc435c8352baf5dddb9fe412d1baeace0.
```

The script and output SHA-256 hashes are

```text
83f1fa49ac4d02e21a1d76fed169d101715a6620342714ed05b9172ae967a730
7727571722f69a9c59af0183c50c8bae0b7944d6acb7bbb1b3c0e9bd02d54565.
```

The disjoint audit imports only the pinned THM-2594 stage constructor and
rebuilds the atomization, integration, gauge tensor, exact cyclotomic tests,
and five-prime certificates.  Its script/output/semantic hashes are

```text
ec16f5124c5b83a10337fba6046d08572251d0776285b7e250a86539a26ddf97
ebe5acbe2e19465ed10f1380eaa2bd19975849d72be64151514cb0087e645d4f
ad81e945207703956f5d6ec300430d562c9f98a9ec8788011119a4857ab34e01.
```
