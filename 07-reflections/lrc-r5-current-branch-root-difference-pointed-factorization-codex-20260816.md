# The four-way crossing factors exactly through the six pointed carriers

**Status: FINITE-EXACT POSITIVE FOUR-COORDINATE CANDIDATE ON ONE `r=5`
OWNER BASE; INDEPENDENTLY HOSTILE-AUDITED; LRC(14) remains OPEN.**  Retaining the
current-leg inverse digit `r_owner=a mod 13` and the ordered source-root
difference `s=u-q` together produces a nonzero irreducible

```text
V_4(state) x F_13(r_owner) x F_13(s) x F_13(relation)
```

interaction.  All `3*12^3=5184` possible four-way Fourier modes survive.
More importantly, an exact row-space comparison identifies the repeated
rank-six response with the six already audited pointed source-tail carriers.

This is a finite common-base statistic.  It is not THM-2334's exact address,
a word/deep physical current, U_clock chronology, a row exclusion, or a proof
of LRC(14).

The independent audit rebuilt the stronger
`point x branch x difference x relation` object without importing this
implementation, reproduced both segmentwise/gamma/inverse marginals and every
pinned tensor digest, and explicitly solved every branch row in its statewise
pointed basis.  It also separates branch amplitude rank four, pointed carrier
rank six, and whole-tensor bipartition ranks `40/42`.  See
`lrc-r5-current-branch-root-difference-independent-hostile-audit-codex-20260816.md`.

## Inheritance pass

Three independently controlled parents meet here:

1. the audited owner-visible Boolean square with four physical states;
2. the independently audited root-difference tensor with centered ranks
   `(3,12,12)`; and
3. the independently audited pointed-six refinement
   `(state,u)=((0,0),(1,0),(1,6),(3,6),(3,12),(2,12))`.

The independently audited current-leg parent is the inverse digit
`r_owner=a mod 13` on THM-2471's sheet `X_(u,a)`.  Its three-coordinate
response has centered ranks `(3,4,6)`.  MISTAKE-417 remains the canonical
hostile: complete character support without a rank gate can be a delta-cell
artifact.

The least-used operation is not another marginal.  It is the fibre product
that retains both lawful `F_13` coordinates before multiplication and
integration.

## Double-marginal construction

At every exact endpoint segment the probe records

```text
state,
current branch r_owner,
selected source tail u,
selected complementary root q,
difference s=u-q.
```

The weighted contribution in branch `r` and difference `s` is

```text
U_(u,r)(y) V_q(y) Qjump(y),
```

with the literal endpoint mask and guard order inherited unchanged.  The
pointwise identities

```text
sum_r U_(u,r)=U_u,
sum_s 1_(s=u-q)=1
```

give two independent marginal gates:

```text
sum_r joint = audited root-difference bank,
sum_s joint = current-leg inverse-owner bank.
```

Both identities pass at the gamma-bank level and after relation inversion.
The same event sweep also reconstructs the pointed-six gamma bank and tensor,
matching their pinned digests.  Weighted and support-normalized same-root
slices vanish exactly, and three literal delete-before-transfer controls pass.

## The irreducible four-way signal

For the actual tensor, the four single-axis flattening ranks are

```text
(state, branch, difference, relation) = (4,4,12,13).
```

The three `2+2` bipartition ranks are

```text
(state,branch) | (difference,relation) = 6,
(state,difference) | (branch,relation) = 40,
(state,relation) | (branch,difference) = 42.
```

After subtracting every marginal missing any one of the four coordinates,
the pure four-way ANOVA tensor has

```text
axis ranks          = (3,4,12,12),
2+2 ranks           = (6,36,34).
```

The middle bipartition reaches its centered maximum `3*12=36`.  The last has
a genuine two-dimensional deficit.  The first remains exactly six; the next
section explains why.

The complete actual Fourier tensor has all `4*13^3=8788` coefficients
nonzero.  After four-way centering, precisely the `5184` full-support modes
remain and every lower-support mode is zero.  This is interpreted only after
the double marginal and rank gates; character fullness by itself would not
be enough.

## Exact pointed-carrier factorization

For a fixed Boolean state `i`, form two row spaces over the common split
field:

```text
M_i = span of the 13 current-branch rows over (difference,relation),
P_i = span of the audited pointed-tail rows (i,u) over the same columns.
```

The exact rank certificate records

```text
(number of pointed tails, rank M_i, rank P_i, rank(M_i+P_i))
```

and gives

```text
state 0: (1,1,1,1)
state 1: (2,2,2,2)
state 2: (1,1,1,1)
state 3: (2,2,2,2).
```

Therefore

```text
M_i = P_i
```

for every state, not merely `dim M_i=dim P_i`.  The global
`(state,branch)|(difference,relation)` rank is

```text
1+2+1+2 = 6,
```

exactly the number of realized pointed pairs.  This resolves the repeated
rank-six phenomenon: the current branch does not create a seventh output
carrier; it gives an amplitude-resolved coordinate system inside the six
pointed source-tail carriers.

This is stronger and more precise than the provisional idea of a mysterious
six-dimensional endpoint ceiling.  The ceiling is channel-specific and has
an explicit carrier.  Other bipartitions reach ranks `40` and `42`, so the
whole four-way tensor is not six-dimensional.

## Incidence is flat; amplitudes carry the new information

A support-normalized control replaces the nonzero branch weights above each
tail by equal mass while preserving the root-difference support parent.  Its
ranks are

```text
axis ranks = (4,1,12,13),
2+2 ranks  = (4,13,12),
four-way ANOVA = 0.
```

It has no nonzero branch-frequency coefficient.  Thus current-branch support
incidence alone is completely flat: all thirteen branches are available on
each active fibre.  The four-way signal is carried by the exact profile
amplitudes `U_(u,r)`, not by a branch-presence pattern.

This sharply contrasts with the root-difference parent, where support-only
incidence retained maximal centered ranks `(3,12,12)`.  The two sidecars are
load-bearing for different reasons:

```text
root difference: directed cut incidence,
current branch:  amplitude distribution within a pointed-tail carrier.
```

Two further hostiles isolate the axes.  A branch-flat lift with the same
root-difference parent and a difference-flat lift with the same current-branch
parent both have identically zero four-way interaction.

At fixed relation `(1,0,6)`, the actual `V_4 x F_13 x F_13` slice has ranks
`(4,4,6)`, centered ranks `(3,4,6)`, `572/676` nonzero entries, and all `432`
triple modes.  Its support-normalized and branch-flat controls have branch
rank one and zero triple interaction.

## Tournament interpretation with the missing edge kept honest

For each nontrivial cut `S` of the three-root spine `{0,6,12}`, orient every
crossing pair from `S` to its complement.  This gives two directed edges and
one missing within-side edge: exactly a three-vertex tournament-with-a-missing
edge.  The six abstract cuts are the six such oriented cut objects; the
physical Gray path realizes five, and OWNER retains four.

The six pointed pairs are incidences `(retained cut state, marked tail u)`,
not six tournament vertices.  The current branch is an amplitude coordinate
over those incidences, and `s=u-q` colours the oriented crossing arc.  This
typing explains why a forced `C_6` Fourier transform would be wrong while the
genuine `F_13` transforms in branch, difference, and relation are lawful.

No intrinsic pairwise orientation has been constructed between the four
owner states themselves.  Calling them a size-four tournament would forget
the cut, marked-tail, and missing-edge sidecars that make the rank-six
factorization true.

## Tree and recurrence reformulation

The current-leg digit is one level of a `13`-ary inverse tree.  The row-space
identity changes the right next question.  Rather than treating `13^k`
address words as an exploding set of unrelated vertices, retain the six
pointed carriers as a rank-six bundle and ask whether each additional inverse
digit acts by a finite transition matrix on that bundle.

The concrete two-level target is

```text
(a mod 13, floor(a/13) mod 13)
    -> transition on the six pointed response rows.
```

If the transition closes, inverse ancestry becomes a matrix recurrence or
graded monoid.  If it fails, the first extra row-space direction gives the
missing address sidecar.  This is the precise analogue needed before invoking
Stern--Brocot, Berggren, Fibonacci, or ternary-tree recurrences: an address
map, a preserved carrier, and a checked transition law.

An arbitrary subset of the natural numbers can be encoded by an infinite
indicator word and weighted as a harmonic subseries.  But the scalar sum
forgets the word and is not generally an injective address.  Here the same
lesson is exact: branch marginalization preserves the coarse response but
destroys the four-way amplitude coordinate.  Any harmonic or subset analogy
must retain the cylinder word, not only its scalar weight.

## Connection contract

| field | exact answer |
|---|---|
| source | audited owner square, source-root pairs, pointed tails, and THM-2471 current-sheet branches |
| target | `V_4 x F_13(r_owner) x F_13(u-q) x F_13(relation)` |
| map | retain branch and ordered root pair before endpoint multiplication/integration, then invert the relation bank |
| preserved | common base, literal guard order, state, tail, cut orientation, exact source amplitudes, endpoint factors |
| destroyed | four older current digits, source-time high digit, deep label, exact relation-lattice address, chronology |
| marginal gates | sum branch gives root-difference parents; sum difference gives inverse-owner parent; pointed bank is independently recovered |
| positive gate | nonzero four-way ANOVA with ranks `(3,4,12,12)`, `2+2` ranks `(6,36,34)`, and `5184/5184` modes |
| mechanism | statewise current-branch row spaces equal the six pointed-tail row spaces exactly |
| hostile boundary | support-normalized, branch-flat, and difference-flat lifts all kill the four-way interaction |
| scope | finite candidate only; no exact address/current, chronology, row exclusion, or LRC(14) conclusion |

## Reproduction

```text
python -B 04-computation/lrc_r5_owner_node_inverse_branch_root_difference_four_way_probe_20260816.py
python -B -O 04-computation/lrc_r5_owner_node_inverse_branch_root_difference_four_way_probe_20260816.py
```

The semantic digest is
`0e527913f22fc7d165f795df42e25692fba9289ccaf85aee4a3a2402613bdb41`.
The LF script/output digests are
`d66378cc8db99c4de087fa78413721de6aa7be3960f51ee39f29feae9313eeba`
and
`25122a1344c2677a6444a98f21beb7e4b7f27f80acc708263050916409ebea1b`.
