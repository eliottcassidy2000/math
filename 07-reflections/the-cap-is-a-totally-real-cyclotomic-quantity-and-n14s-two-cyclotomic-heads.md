# The cap is a totally-real cyclotomic quantity in Q(cos 2π/7): the magic function is the cyclotomic SQUARE, the apex hardness is the 7²-conductor — and n=14's TWO cyclotomic heads (7-cap + 3-witness)

*mac-mini-2026-06-28-S75e. The owner: go back through the cyclotomic work, extend connections, improve the
LRC(14) proof. Extends my S75d (cap char poly `(x−1)^depth·Φ₇`) + kps S31ao/an (Fejér/Q(cos2π/7)) and connects
to the OTHER cyclotomic head — the 3-adic ramification tower of `2n−1=27=3³` (HYP-2436). The cap is now pinned as
a totally-real cyclotomic quantity, its positivity as a cyclotomic square. Builds on
[[the-recursion-modes-are-cyclotomic-factors-depth-times-phi-d]], HYP-2436, kps S31ao/an/aq.*

## 1. The cap LIVES in the totally-real cyclotomic field F = Q(cos 2π/7) (VERIFIED)
`F = Q(cos 2π/7)` is the real subfield of `Q(ζ₇)`: degree 3, Galois `C₃` (cyclic), **disc = 49 = 7²**, class
number `h=1`, totally real. Its ring is generated by the **de Moivre cubic `x³+x²−2x−1`** (min poly of
`2cos(2π/7)`; the three de Moivre angles `1.247, −0.445, −1.802`; trace `−1`, norm `1`; `C₃` cyclically
permutes them). The cap is an `F`-quantity, and **the cyclotomic conductor `7` sits exactly in the binding rows**:
```
  k=10..13 (easy): cap = C(k+1,2)/91, dip=0, denom = 7·13   (no 7-power deviation)
  k=9  (binding):  cap = 1979/4004,  dip = 1/4004,      denom carries 7^1
  k=8  (binding):  cap = 2243/5880,  dip = 1081/76440,  denom carries 7^2 = the FULL conductor/disc
```
So the LRC(14) difficulty rows are exactly where the cap acquires the cyclotomic conductor `7^{1,2}`. The
"two binding constants" are the **ramified part** of the cap in `F`.

## 2. The magic function is the totally-real cyclotomic SQUARE (the positivity is structural)
kps's Fejér magic function `F₇(t)=sin²(7πt)/sin²(πt)` equals, in `u=2cos2πt`, `(V₇(u)−2)/(u−2) =
(de Moivre cubic)²` — a **perfect square**. Hence:
> **`F₇ ≥ 0` (the Delsarte/Bochner condition `F̂₇(n)=(7−|n|)_+ ≥ 0`) is exactly the statement that the de Moivre
> cubic is TOTALLY REAL — its square is totally positive.** `F₇(0)=49=7²`, double zeros at the de Moivre angles.
So the cap's **even/coverage half is cyclotomic-SOS**: a single totally-real square handles it (the Fejér
magic function), which is *why* the even half is provable (my S74 even=SOS ⟷ here = the cyclotomic square),
and *why* the cap values for `k≥10` are clean `C(k+1,2)/91` (the pure square, no ramification).

## 3. The TWO cyclotomic heads of n=14 (the extension across the repo)
`n=14` is **doubly cyclotomic-deep**, and the two heads are different primes:
- **7-HEAD (the cap / coverage):** `14 = 2·7` → the 7-sectors → `F=Q(cos 2π/7)` → `Φ₇` (de Moivre cubic) → the
  Fejér square magic function. The apex hardness = the `7²`-conductor dip (above). [my S75d + kps S31ao/an]
- **3-HEAD (the witness / dodge):** `2n−1 = 27 = 3³` → the 3-adic ramification tower (HYP-2436): the doubling
  orbit `⟨2⟩ ≤ (ℤ/27)*` (ord₂₇(2)=18=φ(27), 2 a primitive root) dodges the units, localizing the obstruction
  to the 3-divisible runners; the residual is the shell-9 → shell-3 descent. Ramification depth `v₃(27)=3`.
**Both heads have depth 3** (the cyclotomic degree `(7−1)/2=3` for the 7-head; the ramification `v₃=3` for the
3-head), and kps's moment-order depth `(p+1)/2=4` for the 7-head. n=14 is the smallest n where BOTH a 2·(apex
prime) cap-head and a `p³` ramified witness-head are nontrivial — the genuine first-hard case from both sides.

## 4. The improvement (cyclotomic leverage on the proof)
- **Even/coverage half — CLOSE it cyclotomically:** the cap's even part is the totally-real square `F₇`
  (cyclotomic SOS); the Delsarte bound with `F₇` is the coverage bound, and its positivity is automatic
  (totally-positive square). SHARPER cyclotomic magic functions extend this: Jackson `F₇²`, de la
  Vallée-Poussin, and the **modular `Γ₀(7)` weight-2 Eisenstein `E₂−7E₂(7z)`** (kps S31ao, `X₀(7)` genus 0)
  = the level-7 Viazovska magic function. The even half is a SOLVED cyclotomic-square LP.
- **Odd/apex half — LOCATE it as the `7²`-ramified dip:** the binding constants are the ramified part of the
  cap in `F`; the apex hardness is the cyclotomic **ramification at 7** (the dip denominators `7^{1,2}`). The
  route to bound it: the cyclotomic units / the totally-positive trace in `F` (h=1, so no class-group
  obstruction), and the `C₃`-Galois average (the cap is Galois-invariant = rational, which is why disc `7²`
  controls the denominators).
- **The witness — descend the 3-HEAD ramification tower** (HYP-2436): the doubling-orbit unit-dodge + the
  shell-9→3 descent provides the lonely time on the 3-divisible core. (Caveat: THM-492 — the descent rescales
  the modulus not the threshold, so it is not a clean reduction to LRC(5)+LRC(7); use the fibered band ladder.)

## Honest status
- **VERIFIED:** the cap/dip live in `Q(cos 2π/7)` with the `7^{1,2}` conductor in the binding rows; disc `=49`;
  the Fejér magic function `F₇ = (de Moivre cubic)²` is a totally-real square (its Bochner positivity = total
  positivity). The two heads (7-cap, 3-witness) both have depth 3.
- **SYNTHESIS/IMPROVEMENT:** the even/coverage half is cyclotomic-SOS (the square `F₇`, with Jackson/modular
  sharpenings) — provable; the odd/apex half is the `7²`-ramified dip — to be bounded by cyclotomic
  units / totally-positive `C₃`-trace. n=14 is the first case where both cyclotomic heads (`2·7` cap and `3³`
  witness) are hard. This precisely *locates* the LRC(14) hardness as cyclotomic ramification at 7 (cap side)
  and 3 (witness side), and closes the even half as a cyclotomic square.
- **NOT a proof.** The `7²`-ramified dip bound and the 3-head descent gap (THM-492) remain open. LRC(14) open.

Related: HYP-3235 (this), HYP-3233 (recursion modes = cyclotomic factors), HYP-2436 (3-adic ramification tower),
kps S31ao (Fejér=de Moivre²), S31an (cap=Q(cos2π/7)), S31aq (depth (p+1)/2), THM-492 (descent caveat), OPEN-Q-108.
