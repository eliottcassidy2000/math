# The regularizable/Eisenstein BULK vs the un-regularizable/cusp RESIDUAL has a SHARP GEOMETRIC CRITERION: the covering-min margin is a classical (2-fold) Dedekind sum — hence carries the −1/12 = ζ(−1) eta anomaly — EXACTLY when the speeds form a SCALED INTERVAL (arithmetic progression / cyclic orbit) at the witness rotation; the construction does (a scaled interval-core {1,…,n−2} + antipode killer −1, MODULAR), the beaters n=7,8,9,10 do NOT (generic non-consecutive subsets, NON-MODULAR); so the beater margin is NOT a Zagier cotangent sum (a concrete NEGATIVE that refutes my own HYP-3773 "beaters = higher-dim Dedekind" hope) — the covering-min's no-closed-form irregularity (a(n)) IS genuine non-modularity, the genus-1 cusp form f₁₄ at n=14; this gives mac-mini-S67's "regularizable bulk + residual" (HYP-3774) and klein-S56's "ι-even Eisenstein / ι-odd cusp" (HYP-3768) split a checkable geometric form: **regularizable ⟺ AP/interval; residual ⟺ generic**

*opus-2026-06-30. Owner: work the concrete next step (the beater's higher-dim Dedekind form) + relate the
abstract pattern to the repo + synthesize/extend. The concrete step yielded a clean NEGATIVE that sharpens the
whole bulk/residual synthesis into a geometric criterion, and a repo search wove it to the modular arithmetic.*

## 1. The concrete step: the beater margin is NOT a cotangent (Zagier) sum
HYP-3773 hoped the general-rung margin was a Zagier *higher-dimensional* Dedekind sum
`d(D;a₁,…,a_r)=(1/D)Σ_j ∏_i cot(πj a_i/D)`. Computed — it is **not**:
- The naive full-product sum over a beater's speeds **diverges / is ill-defined** (beater speeds are not all
  coprime to the binding `D` — e.g. the n=8 beater has `16≡1 mod 15`, a residue collision), and where defined
  (n=7 construction, D=43 prime) it gives ≈ −2328, nothing like the margin.
- The single 2-fold Dedekind at the witness rotation `s(a,D)` does not match either (rechecked).
So the modular (Dedekind) structure does **not** extend to beaters. This is a genuine negative — the covering-min
residual is not a cotangent sum at all.

## 2. WHY: the sharp criterion — MODULAR ⟺ scaled interval (AP) at the witness
At the binding witness rotation `a`, look at the residues `{v·a mod D : v∈S}`:
> **The margin is a classical 2-fold Dedekind sum ⟺ the residues are a SCALED CONSECUTIVE INTERVAL** (an AP
> `c·{1,2,…,m}`), optionally plus the antipode `−1`.
Verified exactly:
| set | residues at witness (descaled) | scaled interval? | ⇒ |
|---|---|---|---|
| construction n=7 | `{1,2,3,4,5, −1}` | **YES** (c=7) | MODULAR (Dedekind `s(n,Φ₆)`) |
| construction n=9,14 | `{1,…,n−2, −1}` | **YES** | MODULAR |
| beater n=7 | `{1,2, 5,6,7,8}` (gap 3) | **NO** | NON-MODULAR |
| beater n=8,9,10 | generic | **NO** | NON-MODULAR |
The construction descaled is the **interval-core `{1,…,n−2}` plus the antipode killer `−1`** — a single
arithmetic progression (cyclic orbit generated by `n`, since `v·n mod Φ₆` runs the AP and `killer·n ≡ (n−1)²≡−n`).
The lonely measure of an AP/interval is *classically* a Dedekind sum (the sawtooth sum of an AP telescopes) —
that is *why* the construction margin `= −12 s(n,Φ₆)/n²`. Beaters are non-AP (a hole, or a residue collision
from lifts), so no telescoping, no Dedekind sum.

## 3. This IS the bulk/residual = Eisenstein/cusp split, made geometric
mac-mini-S67 (HYP-3774): the covering-min floor = **regularizable bulk** (ζ(−1), ζ(2), E₂) **+ un-regularizable
residual** (cusp form f₁₄). klein-S56 (HYP-3768): **ι-even Eisenstein/E₂ bulk + ι-odd genus-1 cusp form**. My
criterion gives both a **checkable geometric form**:
> **regularizable / Eisenstein / bulk ⟺ the speeds are a scaled INTERVAL (AP) at the witness** — this is the
> CONSTRUCTION, whose Dedekind sum → −1/12 = ζ(−1) is the E₂/eta anomaly (the "−1/12" carrier, HYP-3774).
> **un-regularizable / cusp / residual ⟺ GENERIC (non-AP) speeds** — these are the BEATERS, the true
> covering-min `a(n)`, with **no closed form** because they are genuinely non-modular (no Dedekind/eta cocycle).
The **genus jump 0→1 at n=14** (`genus X₀(2p)=0,0,1,2,2` for p=3,5,7,11,13; HYP-3586) is exactly the appearance
of this non-modular residual: below the jump the covering is the flat hexagonal A₂ (interval/Eisenstein, genus 0);
at n=14 the genus-1 cusp form f₁₄ = curve 14a is the residual — the beaters. So **the covering-min's irregularity
is the cusp form**, and my "beaters are generic non-AP" is its down-to-earth witness.

## 4. The repo web (search-integrated) — one arithmetic
The −1/12/Dedekind/eta pattern threads the whole repo:
- **Φ₆ three guises** (HYP-3705): Eisenstein norm `N(n−ζ₆)`; point count of `PG(2,n−1)`; and `= 2·Σ(speeds)+1`.
  The construction's interval-at-the-witness is the **cyclic/projective (Singer-type) structure** — the ordered
  object; beaters break it.
- **B₂ = 1/6 unifies** (HYP-3768): `ζ(2)=π²B₂` (Farey floor `3/π²=1/2ζ(2)`, HYP-2856), `E₂=1−(4/B₂)Σσ₁qⁿ`,
  Dedekind reciprocity const `=B₂/2=1/12`. The functional-equation partners `ζ(−1)=−1/12` and `ζ(2)=π²/6` sit
  at the two ends of the covering story.
- **ψ(14)=24=2·12=η exponent** (`Δ=η²⁴`, `1/24=−ζ(−1)/2`): the modular index of the covering modulus IS the
  regularization denominator.
- **The (2,3,n) spine / geometry** (HYP-3771): n≤6 spherical (Platonic, genus 0), n=6 Euclidean (hexagonal A₂ =
  the covering-min), **n=7 hyperbolic (Klein quartic, first non-crystallographic) = the frontier** where the
  cusp/residual is born. The interval/AP is the flat (Euclidean/Eisenstein) object; the residual is the
  hyperbolic/cusp one.
- **Constants**: the eta anomaly `−1/12` joins the triangle's constants (fiber fraction `~1/√(πn)` = Wallis/π,
  Γ/Stirling = e, Euler–Mascheroni γ) — now the *modular/Casimir* constant of the covering-min.

## 5. Extension: the modular locus = the ordered (AP/interval) sets
The synthesis extends cleanly: **the "regularizable" covering sets are exactly the AP/interval configurations**
— the maximally *ordered* speed sets (minimal ordering complexity `O=Φ(n−1)`, the three-distance/Steinhaus
extreme). Their lonely measure is a Dedekind sum (modular), their margin carries `−1/12` (the E₂ anomaly), they
are the Eisenstein bulk. **The covering-min extremum sits OFF this ordered locus** — among the *disordered*
(generic, non-AP) sets — which is why it is irregular, cuspidal, and has no closed form. The `−1/12` is the
signature of order; the residual `a(n)` is the price of disorder. (So the covering-min question "how far below the
Eisenstein construction can a disordered set reach" = "how big is the cusp form" = OPEN-Q-108's ι-odd index.)

## Status
- **Verified (opus):** the beater margin is NOT a cotangent/Zagier sum (full product diverges/mismatches;
  witness-Dedekind mismatches); the MODULAR criterion "scaled interval (AP) at the witness" holds — construction
  YES (n=7,8,9,14), beaters NO (n=7,8,9,10).
- **Synthesis (opus):** the bulk/residual (HYP-3774) = Eisenstein/cusp (HYP-3768) split IS the AP/interval vs
  generic split; the construction is the ordered/modular/−1/12 object, beaters the disordered/cusp residual;
  the genus jump at n=14 is the residual's birth.
- **Correction (opus, honest):** HYP-3773's "beaters = Zagier higher-dim Dedekind (reciprocity extends)" is
  **withdrawn** — beaters are non-modular; there is no cotangent-sum form. The lever does not extend past the
  ordered (interval) locus.
- **Open:** quantifying the residual (the cusp form f₁₄ / a(n)) — the genuinely non-modular, no-closed-form core
  (OPEN-Q-108, H3/H4).

Related: HYP-3774/mac-mini-S67 (bulk/residual, ζ-regularization carrier), HYP-3768/klein-S56 (Eisenstein/cusp,
E₂, B₂), HYP-3773/opus-S6 (Φ₆=2Σk+1, −1/12=ζ(−1); its Zagier hope corrected here), HYP-3705 (Φ₆ = Eisenstein
norm = PG(2,n−1) = Singer), HYP-3586 (genus jump 0→1 at n=14), HYP-3771/3772 ((2,3,n) spine, φ=6 quasicrystal),
HYP-2856 (Farey floor 3/π²), OPEN-Q-108. HYP-3775 (this).
Scripts: 04-computation/lrc_modular_criterion_interval_vs_generic_opus_20260630.py.
