# Fixed-half-twist ZMC rank nine, XOR loss, and the CRT current transplant

**Research reflection / provenance, not a truth source.**  The exact claims
below are routed to THM-3453/3455 and the independently audited THM-3461.

## Inheritance pass

| lane | closest proved mechanism | hostile or corrected near miss | sidecar used here |
|---|---|---|---|
| fixed physical lift | THM-3398 modes and THM-3405's two-point gcd gauge | MISTAKE-389: a physical time need not be a selected-mode centre | exact mode residue `h`, width `w`, and active gcd `d` |
| cap-seven support | THM-3453's fifteen literal atoms | MISTAKE-404: the fixed half-twist centre was discarded with the arbitrary-centre problem | the literal identity `B=D(1/(2q))` |
| current | THM-3410's integral wedge | all fixed-half owners lie on one ray and give zero tariff | endpoint radius / Fourier support |
| mixed fibre | THM-3429's `q=51` affine character | owners `1,35` agree modulo `34` but not in their `k mod 3` lifts | affine lift character |
| overlap compiler | THM-3459's ternary intersections | identical partition masks can have incompatible labelled stars | owner labels before orientation/current |

## Live concept board

1. literal half-twist masks;
2. selected THM-3398 modes and zero cochains;
3. XOR/OR/ternary overlap data;
4. Berggren/Fibonacci branch labels and harmonic subsets;
5. projective wedge and endpoint current;
6. `7 x 13` CRT decorations;
7. turnpike/tournament orientation sidecars.

The decisive map is now typed:

```text
literal mask
  -> labelled selected mode
  -> fixed common centre and P=0
  -> open local physical cell.
```

It preserves support, owner labels, strictness, widths, and a physical clock.
It destroys no data until the first arrow is compressed back to a rank word.
The next desired arrow, from that packet to a relation-residue endpoint
current, is absent because all columns `(u_i,A_i)` are proportional.

## The first four parabolic labels are now `6,4,7,9`

THM-3455 isolated

```text
q_t=(2t+1)^2+2,
(q_1,q_2,q_3,q_4)=(11,27,51,83),
```

with cap-seven word `(6,4,7,>7)`.  THM-3461 sharpens the fourth entry to `9`
with an independent meet-in-the-middle audit.  This does not
alter THM-3455's period-`1683` cap-seven density or its Fibonacci harmonic
coefficient: replacing one `>7` label by an exact larger rank changes no
membership in the cap-seven subset of the natural numbers.

This is the useful way to apply the user's “every subset of the naturals is a
subset of the harmonic series” observation.  A branch property gives an
indicator word `a_t`; its logarithmic mass is studied by

```text
sum a_t/t,
```

while the values themselves have the different series

```text
sum 1/q_t.
```

THM-3455 already proves that the first diverges on its periodic positive
index set and the second converges.  Exact rank nine refines the label on the
complement; it does not change either convergence statement.

## Why this is not “essentially a tournament of size four”

The four branch labels can be made vertices, but there is no intrinsic
pairwise orientation between them.  Their exact observable is a unary rank
grade plus owner-sheet incidence.  For `q=11,27` the witness is an XOR
partition; for `q=51,83` it is a multiply covered OR clutter.  THM-3459's
ternary intersection compiler can recover pair-overlap defects, but it still
does not orient a pair or recover a current.

The turnpike comparison from THM-3457 is informative precisely because its
orientation comes from strict distance comparison.  XOR there recovers K4
edge incidence but not order.  XOR here recovers parity of sheet occupancy but
not owner ancestry, mode width, or relation address.  In both settings the
binary algebra is a defect detector, not the tournament itself.

## The CRT transplant has a precise first gate

For `q=11,27,51`, `gcd(2q,91)=1`.  Therefore one may preserve an owner modulo
`2q` while setting its `7`- and `13`-adic decorations independently.  Six
exact shallow/deep examples survive every literal mask and mode-centre gate.
This is real progress: valuation decoration and support compatibility are no
longer competing constraints.

But the zero wedge makes a direct current transplant impossible.  The next
candidate must begin with a relation vector `w`, choose residues `r_i`, and
pass

```text
sum_i w_i r_i == 0 (mod 2q).                           (T)
```

Only then should CRT impose the `7/13` data.  Endpoint coefficients and all
`169` twists must be constructed after `(T)`, retaining the `q=51` affine
character.  A target-variance or all-`91`-unit projector is the cheapest
decisive spectral test.  Repeating literal coverage cannot answer it.

## Boundary

The fixed physical clock is now established for the named packets; the
projective current is established to vanish.  Neither statement produces an
LRC row, a decrement, a `7 tensor 13` bispectrum, or a Jacobian map.  LRC(14)
remains open.
