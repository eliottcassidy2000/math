# The current-branch/root-difference crossing survives a clean-room pointed-carrier audit

**Status: FINITE-EXACT INDEPENDENT HOSTILE AUDIT ACCEPTS THE SCOPED
FOUR-COORDINATE INTERACTION AND ITS POINTED-SIX FACTORIZATION; LRC(14)
remains OPEN.**  This audit did not import the submitted four-way candidate.
It fixed conventions from the independently audited current-digit and
pointed-root parents, rebuilt a stronger point-resolved object, and only then
compared digests and conclusions with the candidate.

The accepted statement is finite and static.  On the one audited `r=5`
OWNER base, retain

```text
Boolean state x current digit r=a mod 13 x source-root difference s=u-q
              x inverse relation (1,0,t).
```

The pure four-way interaction is nonzero with axis ranks `(3,4,12,12)`,
the three `2+2` ranks `(6,36,34)`, and all `5184=3*12^3` full-support
Fourier modes.  This is not an exact-address, chronology, physical-current,
row-exclusion, or LRC(14) result.

## Independent construction

The inheritance was fixed before reading the submitted implementation:

- `r=a mod 13` is the current-leg inverse digit from the independently
  audited current-digit parent;
- `s=u-q` is the ordered selected/complementary source-root difference from
  the independently audited root-difference parent;
- the carrier is the six realized pointed incidences
  `((0,0),(1,0),(1,6),(3,6),(3,12),(2,12))`, not an abstract `C_6`;
- the inverse convention is literally `(1,0,t)` in the pinned Lucas split
  field.

The audit retains the point label before forming the state tensor.  It
rebuilds ordered root pairs and literal endpoint events, expands the exact
current-digit profiles, applies the endpoint phase, and only then performs
the relation inverse.  The submitted state tensor is the point marginal of
this stronger object.

The exact work counts are

```text
actual/support/pointed/branch-flat/difference-flat
= 7107008/7108460/1200462/186244/186244.
```

All active source-tail fibres independently have all thirteen current
branches.  Thus the support-normalized branch hostile really is uniform mass
`1/13`, rather than an inferred normalization.

## Marginal and typing gates

Both required marginal chains pass segmentwise, at the gamma bank, and after
the inverse transform:

```text
sum_r  -> audited weighted and support root-difference tensors,
sum_s  -> independently audited current-digit tensor.
```

The point marginal also reproduces the independently audited pointed-root
tensor.  The ordered-pair phase, common coordinate, literal guard order,
pointwise same-root zero, and three boundary controls all pass exactly.

No arrival/source-time digit has been substituted for `r`: it remains the
current-sheet branch `a mod 13`.  No Fubini swap is used to define the
object—the segmentwise identities are checked before aggregation.  The
normalization hostile is rebuilt from the actual branch-support census.

## Exact interaction and hostiles

For actual, support-normalized, branch-flat, and difference-flat tensors, the
single-axis ranks are

```text
(4,4,12,13), (4,1,12,13), (4,1,12,13), (4,4,1,6),
```

and the three `2+2` ranks are

```text
(6,40,42), (4,13,12), (4,13,12), (6,4,4).
```

Sequential centering gives `(3,4,12,12)` and `(6,36,34)` only for the
actual tensor.  Every support-normalized, branch-flat, and difference-flat
four-way interaction vanishes identically.  At fixed `(1,0,6)`, the actual
state/branch/difference slice has ranks `(4,4,6)`, centered ranks `(3,4,6)`,
and all `432` pure modes; the hostile slices again have zero pure
interaction.

## Pointed equality, not a rank coincidence

For each Boolean state, the audit compares the current-branch rows and the
corresponding pointed rows over the same `(difference,relation)` columns.
The exact records

```text
(point count, branch rank, point rank, union rank)
= (1,1,1,1), (2,2,2,2), (1,1,1,1), (2,2,2,2)
```

prove equality of the statewise row spaces.  In addition, every branch row
is explicitly solved in the pointed basis; the resulting coordinate bank has
digest

```text
9cb4071131018679cb9cebf6c9e7a1ea34562e18a59a9a750d99c3bc2e76efc7.
```

Retaining all six point labels gives base, one-current-digit, and union ranks
`(6,6,6)` globally.  Therefore the first bipartition rank six is an exact
pointed relation carrier.  It is not the dimension of the whole four-way
tensor: the other bipartitions have ranks `40` and `42`.

The incoming representation sidecar identifies these six point labels with
the directed arcs of the bidirected path

```text
0 <-> 1 <-> 3 <-> 2.
```

Thus the equality proved here lands more precisely in that six-arc module:
the branch digit changes amplitudes on the arcs but creates no seventh arc
carrier.  The alternating `P7` refinement and the contracted `P4` both have
`H1=0`.  Consequently this factorization is not yet a cohomology class or
flux; the missing Boolean closure edge `2--0`, a lawful clocked cochain, and
its temporal typing remain necessary sidecars.

## Amplitude versus carrier

The incoming source/current and two-current computations sharpen the scope:

```text
source x current: profile rank 17 -> state/relation amplitude rank 4
                  -> pointed carrier rank 6;
one current digit: 13 labels -> amplitude rank 4 -> pointed carrier rank 6;
two current digits: raw/contrast amplitude ranks 13/12
                    -> pointed carrier and union ranks 6.
```

These are different invariants.  The present four-way result says that the
one-digit amplitude coordinates lie inside the same six pointed response
carriers.  It does not say that amplitude rank equals carrier rank.

The two-current package is also a warning against an unproved recurrence:
there is no stationary `K_r1` valid across all `r0`; only the exact
address-conditioned diagonal bundle `K_(r0,r1)` survives, with fixed-`r0`
sum `I_6`.  Consequently the one-digit row-space equality does not imply a
stationary address transition, chronology, or autonomous inverse-ancestry
recurrence.

## Reproduction and scope

```text
python -B 04-computation/lrc_r5_owner_node_inverse_branch_root_difference_four_way_independent_audit_20260816.py
python -B -O 04-computation/lrc_r5_owner_node_inverse_branch_root_difference_four_way_independent_audit_20260816.py
```

Normal and optimized replays are identical.  Script/output/semantic
LF-normalized SHA-256:

```text
cf2efd6317dd6ba2a05865ad11d115f1ab239e8ebafb2b8b3764447d1e0e67a3
8b5e5101e9df0da25899d5b39c3604dae7d2706200208f153b65d37fd2f63640
57bb908c381608415091feb14035354e7cef6dd3b11a4e9ce78c363fc703f2a0
```

Accepted scope: one finite common-owner-base response tensor, its exact
marginals, irreducible four-way interaction, and pointed-six row-space
factorization.  Still open: complete or THM-2334 exact address, `U_clock`
chronology, arrival typing, a physical current, row exclusion, and LRC(14).
