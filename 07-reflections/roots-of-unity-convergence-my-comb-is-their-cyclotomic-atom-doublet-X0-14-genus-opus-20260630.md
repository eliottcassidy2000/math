# Roots-of-unity convergence: my "extremality of the comb" IS klein/mac-mini's proved cyclotomic atom 4cos²(3π/7) = the doublet = the odd 7-cycle = genus-1 cusp form f₁₄ of X₀(14); my Dirac comb is its geometry, the cusps d|14 are my 2·7, and I add the cap-side Eisenstein Φ₆=Q(√−3) to their apex Q(√−7)

*opus-2026-06-30. Owner: explore past work on roots of unity and related threads. Pulled origin/main: in
the same hours, klein and mac-mini reached the SAME truth I synthesized (independently), but PRECISE and
PARTLY PROVED. This records the convergence, maps my pieces to theirs, and adds what's mine.*

## The convergence (three independent agents, one object)
The owner asked each of us "the truth across all frames." Three answers, one object:
- **opus (me):** LRC = the extremality of the **roots-of-unity comb** Ш₁₄; the AP tiles, the observer is
  the one empty tooth; the conjecture is the positivity of the deviation.
- **klein (HYP-3593):** the truth = **one cyclotomic atom `4cos²(3π/7)=0.198`** at the apex-7 cusp, above
  the bulk floor `3/π²=1/(2ζ(2))`; the frames are a monotone zoom-in.
- **mac-mini (HYP-3594):** the truth = **a single ODD CYCLE** `C₇` (apex 7-cycle); VALUE `4cos²(3π/7)=
  2+λ_min(C₇)`, IDENTITY an odd cycle in the R-odd space, MULTIPLICITY the genus `(0,0,1,2,2)`; the OCF
  full circle (the project began with odd cycles, the obstruction collapsed to one).
**Same object, three faces: geometric (my comb), spectral (klein's atom), topological (mac-mini's cycle).**

## My conjectures are now THEIR theorems (the gift back)
> **THM-590 (klein, PROVED) IS the positivity my `Z₇` cyclotomic-SOS-floor reflection conjectured.** For
> a core `O⊊Z₇`, `g(O)=min_{k≠0}|Σ_{x∈O}ζ^{kx}|² ≥ 4cos²(3π/7) > 0`, `=0` only at `∅,Z₇` (by `Φ₇`
> irreducibility) — exactly my "ρ_j positivity, set-independent because `Z₇` is transitive." My
> `(0,1,1,1,1,1,1)` was the `|O|=1` slice; THM-590 is the full core landscape, with the explicit minimum.
> **And the minimizer is the DOUBLET `|O|=2` — which IS my binding pair `{±1},{±3},{±5} mod 14`.** The
> runners that hold my razor's edge are the cores that minimize the cyclotomic gap. Peak-binding (mine)
> and density-gap (theirs) are the SAME doublet.

## The dictionary (my frames ↔ theirs)
| mine | theirs | shared object |
|---|---|---|
| the comb Ш₁₄, observer = empty tooth | the cyclotomic atom `4cos²(3π/7)` | the apex-7 obstruction |
| binding pairs `{±1},{±3},{±5}` (φ(14)=6) | the **doublet** (THM-590 equality; THM-578 proved tail) | the 2-element core / the atom |
| `14 = 2·7` (mirror × apex) | cusps of `X₀(14)`: `d=1,2,7,14` (Klein group) | the divisors of 14 = the cusps |
| Galois `(Z/14)*≅Z/6`, the razor's edge | `Atkin–Lehner W(14)=(Z/2)²` on the cusps | the modular symmetry |
| the razor-thin is in the MEASURE not peak | bulk `3/π²` (Eisenstein, constant) vs atom (cusp) | density floor vs obstruction |
| L=0 perfect cancellation; the gap | genus 1 = ONE cusp form `f₁₄` (curve 14a) | the single global mode |
| ζ(2) floor (my resonance-killing) | `3/π²=1/(2ζ(2))` bulk (Eisenstein) | the local-global density |
**Their deepest layer I had NOT reached: the modular curve `X₀(14)`.** LRC(2p) hardness `=` genus`(X₀(2p))
= 0,0,1,2,2`, jumping `0→1` at 14; the obstruction is the genus-1 weight-2 cusp form `f₁₄ =` the rank-0
elliptic curve `14a`; bulk `= ` Eisenstein (dim `#cusps−1=3`, constant). My "comb's irreducible gap" is,
precisely, this one cusp form. The cusps `d=2` (doubling) and `d=7` (apex) ARE my mirror-2 and apex-7.

## What I add to their picture (the cap side + the geometry)
1. **The Dirac-comb geometric realization.** Their atom `4cos²(3π/7)` is the doublet's Fourier gap; MY Ш₁₄
   with the observer's one empty tooth is its real-space picture (Poisson-dual). The killer (mult-of-14)
   is the runner that lands ON the empty tooth — the geometric form of "the doublet/cusp-form obstruction."
2. **The cap-side Eisenstein, complementing their apex Q(√−7).** Their floor/atom lives at the apex-7 cusp
   `= Q(√−7)` (Heegner, Mersenne∩Heegner∩3mod4={3,7}). The covering-MIN PEAK `M=n/(n²−n+1)=n/Φ₆(n)=14/183`
   is the **Eisenstein norm `N(n−ζ₆)` in `Q(√−3)`** — the OTHER Heegner field (class number 1, hexagonal).
   So the conjecture is framed by **two class-number-one fields: `Q(√−7)` (apex/floor/density) and `Q(√−3)`
   (Eisenstein/cap/peak)**, meeting at `7=Φ₆(3)`. (My peak `14/183` and their density `0.198/3π²` are
   different quantities — peak vs measure — but both cyclotomic-apex; the doublet binds both.)
3. **The two pyramids** give the covering-min construction (killer `=` pronic `(n−1)n`, the first pyramid's
   split; denominator `=Φ₆(n)`, the central polygonal / Eisenstein norm).
4. **The corrected empirical covering-min** `14/183 < 7/89` (drop the binding-unit/doublet member, add its
   killer).

## The unified truth (all three syntheses + the modular layer)
> **LRC(14) is the positivity of ONE object at the apex-7 cusp of `X₀(14)`: the doublet = the binding pair
> = the odd 7-cycle `C₇` = the genus-1 cusp form `f₁₄` (curve 14a) = the cyclotomic atom `4cos²(3π/7)` =
> the empty tooth of the roots-of-unity comb Ш₁₄.** Above it sits the constant bulk `3/π²=1/(2ζ(2))`
> (Eisenstein/density). The conjecture is that the single genus-1 mode never sinks the bulk — a
> local-global / cusp-form rigidity, walled between the two Heegner fields `Q(√−7)` (apex/floor) and
> `Q(√−3)` (Eisenstein/cap). My comb is its geometry; klein's atom its spectrum; mac-mini's odd cycle its
> topology; `X₀(14)` its modular home; the OCF its origin (the project began and ends with one odd cycle).

## Status
- **Convergence recorded:** my comb ≡ klein's atom ≡ mac-mini's odd cycle ≡ the genus-1 cusp form of X₀(14);
  THM-590 (PROVED) IS my `Z₇` SOS floor's positivity; the doublet IS my binding pair.
- **Their proved/verified core (credit):** THM-590 (cyclotomic gap), THM-578 (doublet tail bound), the
  X₀(14) genus/cusp data, the `3/π²` bulk.
- **My additions:** the Dirac-comb geometry; the cap-side Eisenstein `Φ₆=Q(√−3)` (two-Heegner framing);
  the two-pyramids construction; the corrected covering-min `14/183`.
- **The wall (shared):** the genus-1 cusp-form positivity = LRC(14); still open (the leading apex-cusp
  coefficient of `f₁₄`, floor owners).

Related: my truth-beneath-the-frames + Z₇-SOS + Dirac-comb + Eisenstein-Φ₆ reflections; klein THM-590,
HYP-3593, HYP-3586/3587 (X₀(14), genus, cusp form); mac-mini HYP-3594, HYP-3586 (even-graph dual,
4cos²=2+λ_min(C₇)); THM-578 (doublet); HYP-3547 (Mersenne∩Heegner∩3mod4={3,7}); OPEN-Q-108.
