---
source: mac-mini-2026-07-23-S169 (Opus 4.8)
status: DECODE CONSOLIDATION + DISCRIMINATORS. The eq (27) reconstruction is CERTAIN (independently
  reproduced to all 51 digits, agreeing with mac-mini-S168 / opus-S2,S3 / klein-S402,3). What is NEW here:
  four discriminators that constrain the SEMANTICS, a corrected two-family taxonomy of the source problem,
  and an explicit threshold mismatch (1/25 vs the repo LRC(14) target 1/14) that the fleet's LRC mapping
  had not confronted. The source problem remains UNIDENTIFIED; this narrows the field and kills two guesses.
tags: [snippet, artanh, log-energy, certified-log, decode, discriminators, lrc, irrationality-measure, riesz, meta]
related: [THM-2000, THM-518, THM-252, HYP-9023]
---

# The artanh certificate: a rate-comparison / log-energy positivity, two live families, and a threshold mismatch

**mac-mini-2026-07-23-S169.** Owner made the external fragment a cluster priority. Independent pass.
This consolidates what is CERTAIN, adds discriminators, and corrects two things the fleet drifted on.

## 0. Certain (independently reproduced, all 51 digits)

    RHS(27) = (2457/6592)·log(8847357/2974400) − log(1285/896),   proved  > 1/25
    integer form:  2457·log_B − 6592·log_A > 6592/25 = 263.68

certified by the snippet's own two-sided artanh bounds (UPPER on log_A via t⁵/(5(1−t²)), LOWER on log_B
via truncation at t⁵/5). The certified surplus over 1/25 is the snippet's 51/53-digit fraction, EXACT.
`t = (S_n−S_{n−1})/(S_n+S_{n−1})`, `2·artanh(t)=log(S_n/S_{n−1})`; pairs (896,1285), (2974400,8847357).
This layer is settled — do not re-derive it.

## 1. Four discriminators (the new content)

**(D1) It is a RATE COMPARISON, not a small linear form.**  X := (2457/6592)log_B − log_A = 0.045725 is a
genuinely POSITIVE, moderate quantity, cleared above 1/25 by 0.0057. Since log_A/log_B = 0.3308 ≠ the
coefficient 0.3727, log_A is NOT ≈ c·log_B: there is **no near-cancellation**. This RULES OUT the
"|b log_B − a log_A| is tiny" application (Baker lower bounds for solving Diophantine equations —
klein-S402's original Baker framing). It does **NOT** rule out an irrationality/transcendence **MEASURE**,
which is itself a rate comparison C_analytic − C_arith > 0 with both rates clean logs. (I over-stated this
in my first fleet message by lumping measure with Baker; correction: measure stays alive.)

**(D2) The integers are not web-indexed and not in OEIS.**  Zero real hits for 5872957 / 11821757 /
8847357 / (8847357−2974400); OEIS has no (896,1285) or (2974400,8847357) sequence. Apéry-type irrationality
numerators ARE in OEIS; these are not. ⇒ the numbers were **generated by the source's own computation**,
not lifted from a paper. Hunting for "the paper with eq (27)" is likely futile. (arXiv 2511.16636, Bedert,
"Riesz products & LRC: a wider gap of loneliness", Nov 2025, is REAL and the right METHOD-family, but is an
ASYMPTOTIC theorem ML ≥ 1/(2n)+1/n^{5/3+o(1)} — it would not carry a 51-digit rational certificate.)

**(D3) A, B are bespoke, at different scales, not products over small velocity sets.**  A = (5·257)/(2⁷·7),
B = (3·2949119)/(2⁶·5²·11·13²) with 2949119 PRIME. A ~ 10³, B ~ 10⁶ scale ⇒ **two different quantities
compared**, not two edges of one telescoping series (this kills the single-Abel-Dini-series sub-reading).
The lone huge primes (2949119, 727, 381347, 3001) are the fingerprint of **convergents / resultants /
evaluated sums** in a Diophantine or optimization computation — consistent with BOTH families below.

**(D4) THRESHOLD MISMATCH with the repo's LRC.**  The repo flagship is LRC(14): target 1/14 = 0.0714,
prime route reaches 2/29 = 0.069, and THM-518's explicit finding is **"the Riesz product is the WRONG tool
for AP-cores" (it stalls)**. The snippet's threshold is 1/25 = 0.04 — a DIFFERENT scale. And the n-reading
of 1/25 is internally ambiguous: if 1/25 = "beat trivial 1/(2n)", the proven value 0.0457 ≈ 1/21.9 ⇒ n≈11,
whereas klein's Ostrowski unit 1/(2·12+1) ⇒ n=13. So the LRC(14) mapping is threshold-inconsistent; **if**
it is lonely-runner at all it is a DIFFERENT n and/or a NON-AP configuration (where Riesz does not stall),
i.e. the wider-gap method, not the repo's AP-core thread. @klein: which n does the R=∏(1−p cos) optimum give?

## 2. Mechanistic framing (unifies the three repo readings)

`2·artanh(t) = Σ_{k odd} t^k/k` is the closed form of a **harmonic×geometric** sum (weight 1/k on odd k).
That is a per-mode **log-energy / entropy** term. "Rapidity" (klein, THM-252), "Abel–Dini log-edge"
(mac-mini, THM-2000 §3.1), and "logit/entropy" (opus) are ALL this one object — a genuine unity. The
certificate says: a configuration-specific **log-energy quantity** built from two parameters
`p_A = (1+t_A)/2 = 1285/2181 ≈ 0.589` and `p_B = 8847357/11821757 ≈ 0.748 (≈ 3/4, log_B ≈ log 3)`, weighted
2457/6592, exceeds 1/25. Odd-only harmonics ↔ the distance-to-nearest-integer (tent) function's odd Fourier
series — suggestive of a lattice/loneliness origin, but not decisive.

## 3. Two live families (the fragment alone cannot separate them)

**(A) Analytic extremal / log-energy beats a rational floor** — loneliness (Riesz/entropy), Chowla
minimum-cosine-sum, merit-factor / flat-polynomial, or an LP/energy margin. Favoured by: moderate-positive
value; ugly optimized coefficient 2457/6592; odd-harmonic hint; Bedert method is live and real.

**(B) Irrationality / transcendence MEASURE via a clean-denominator (Padé/convergent) construction** — the
rate condition C_analytic − C_arith > margin, both clean logs. Favoured by: the convergent/resultant
big-prime fingerprint (D3); log_B ≈ log 3 could be a saddle value; not-OEIS is fine (a bespoke/AI-found
construction). Weakly disfavoured by: the arithmetic rate is usually messier than a single clean log.

Off-repo candidates in family (A) worth a glance before committing (owner: "may be ANY problem"):
Cohn–Elkies sphere-packing LP margins; autocorrelation/merit-factor; large-deviation/Chernoff rate
separations; AlphaEvolve/FunSearch construction-beats-bound certificates (the artanh rigorous-rational
verification style is exactly how those are checked).

## 4. Decisive open sub-task

Reconstruct the CONSTRUCTION that outputs A = 1285/896 and B = 8847357/2974400 (two different quantities).
Concretely: (i) @klein — from R=∏(1−p cos) loneliness optimization, do x_A, x_B fall out, and at which n?
(ii) @mac-mini/@codex — is (1285/896, 8847357/2974400) an EXACT ratio of two THM-2000 masses / mass-formula
coefficients (not merely figurate-flavoured)? (iii) test A,B against Padé/convergent tables for a log/ζ/π
construction. The one thing that closes it instantly is the CONTEXT of eq (27) from the original message —
owner reports that is no longer accessible, so we work from the fragment.

Files: inline scripts this session; builds on snippet_eq27_exact_linear_form_macmini_S168.py,
certified_logratio_abeldini_opus_S2.py, snippet_padic_decode_opus_S3.py.
