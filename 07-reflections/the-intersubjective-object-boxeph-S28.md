# The intersubjective object — relation as the thing itself

*boxeph-2026-07-16-S28. Owner prompt (verbatim frame): "The lonely runner problem recurses
into its own second moment... for the same reason that velocity is relative in physics,
but acceleration isn't — both are about frames of reference, and the correct realization
in both cases is that attempts at a universal objective singular frame try to do too
much; instead all perspectives need to be held simultaneously, and the structure of that
intersubjective object studied. Luckily tournaments are the perfect simplest-case
(binary) example: tournaments are not a set of things with some relations among them,
tournaments are a relation on a set of things. What structure can relation have?"*

## 1. The frame reading of the last 48 hours is not a metaphor — it is the data

Five times this week the fleet conjectured a universal single-frame law, and five times
the mathematics refused, repairing itself into a per-frame vector law that then held
EXACTLY:

| universal-frame conjecture (died) | the intersubjective repair (survived, exact) |
|---|---|
| HYP-6994: one constant C with max\|S\|² ≤ CM for ALL clusters | per-cluster decidable scans + the classifier VECTOR (‖ℓw/e‖)_{e∈E} (THM-881/886/887) |
| the k-uniform Möbius–sinc O(1) (all scales, one bound) | o(k/d) per scale; k = 13 instance with its own constants (THM-877/879) |
| absolute flatness of compact cores (one slope 0) | exact per-regime slopes; A\* = 3B\* balanced teeth (death-star THM-889) |
| the per-owner dipole factorization (one owner's frame explains its arcs) | arcs close CROSS-owner (137/188); the multi-comb profile (klein cont.3, THM-887) |
| my own /2-run-constant (one instance's frame generalized) | the per-owner honest constant; corrected in place (THM-887(II)) |

The repair is the same move every time: stop seeking the view from nowhere; carry the
tuple of views; prove the law OF THE TUPLE. The resonance classifier is the cleanest
instance — Q_s(w) is not a function of any absolute property of w; it is a function of
w's simultaneous Diophantine position against every owner's comb: a vector over the
cluster's OWN speeds. The problem measures the observer with its own rulers.

## 2. Velocity, acceleration, and which of our quantities is which

- **Frame-dependent (velocity-like):** a witness time t (dilation renormalizes it); a
  single clearance ‖vt‖; a single owner's spectrum value S_e(m); the value Q_s(w) at one
  w; a tournament vertex's score.
- **Frame-invariant (acceleration-like):** M(E) (dilation-invariant: M(λE) = M(E) —
  the whole problem is projective in the speeds); the covering property; klein's w-free
  sup max_{n≠0}|S(n)| (invariant under the coprime frame group); my comb diagonal's
  dependence on w mod 7 ONLY (THM-888(A): six frame-classes, then invariance within
  each); Rédei's parity of H (invariant of the relation under every relabeling);
  Aut-weighted counts (Burnside: literally averaging over frames).
- The physics grammar transfers precisely because the frame group is explicit in both:
  boosts there; the unit group (Z/P)^* acting on spectra, and S_n acting on labelings,
  here. Invariants = functions on the quotient; the quotient IS the intersubjective
  object. Burnside/orbit-counting is "hold all frames simultaneously" as a formula.
- Second moments are where invariance begins for the same reason acceleration is where
  objectivity begins: first-order data sits in a torsor (no canonical origin), second
  differences kill the torsor ambiguity. Q_s — literally our second moment — is the
  first quantity in the chain with a frame-decomposable exact law (comb diagonal
  invariant per class + covariant tail with explicit transformation via w̄).

**A provable little statement this frame demands (named here, HYP-tracked):** the
UNIT-AVERAGE ⟨Q_s⟩ = (1/φ(P))Σ_{gcd(w,P)=1} Q_s(w) is the invariant "objective part";
by the csc²-collapse machinery (THM-888) it should have a CLOSED FORM as a gcd-class
pairing Σ_{g|P} (k̂-mass of class g)·(S-energy of class g), with each instance's
deviation Q_s(w) − ⟨Q_s⟩ carrying exactly the Diophantine data of w. The relativity
program in one identity: mean = invariant, fluctuation = frame.

## 3. Tournaments: what structure CAN a relation have?

"A relation on a set, not a set with relations" — the repo has been discovering exactly
this for months without saying it this way. A tournament is a TOTAL, ANTISYMMETRIC,
BINARY relation: the minimal relation that forces a choice between every pair — the
simplest object that is PURE relation (no slack, no symmetry to hide in). The project's
inventory of "structure a relation can have", assembled:

1. **Geometric structure:** the relation IS a binary tiling of the staircase triangle
   (everything-is-the-triangle). Not a picture OF the relation — the relation itself,
   coordinatized. The vertices are an afterthought; the tiling is the thing.
2. **Cohomological structure:** cut ⊕ cycle (base-path = cut space, wiggly = cycle
   space): the relation decomposes into "hierarchy" (gradient part) and "circulation"
   (curl part) — Helmholtz for relations. Scores are the gradient's potential; the
   cycle part is what no single-vertex frame can see.
3. **Parity structure:** Rédei (H odd always): the one scalar every frame agrees on;
   the locker/negation involutions are its faces.
4. **Spectral/resonance structure (this week's addition):** when a relation meets a
   torsion grid (the LRC bridge), it acquires mode combs — every element broadcasts a
   7-periodic comb from its own frame, and the joint object is the comb SYSTEM with CRT
   transversality. Resonance = two frames sharing structure (gcd).
5. **Quotient structure:** the metagraph — the space of all relations modulo
   frame-change — with its spine/ribs/sea anatomy. The intersubjective object is not
   one relation but the GRAPH OF ALL OF THEM with frame-preserving moves as edges; the
   flow laws (C3 OU-linear, E4 mod-8 constant, H fiber-constant) are its dynamics, and
   the coordinate-atlas principle (kps cont.29: "every proved law rode a coordinate
   that made it linear") is this reflection's methodological twin.

The answer the repo keeps giving to "what structure can relation have?": EVERY structure
it has appears as an invariant of a frame action — and the productive move is never to
pick the right frame but to name the frame GROUP and compute its invariant theory.
(Burnside for S_n; the unit group for dilations; the deck maps for 2-adic doubling —
THM-882's ×2-on-points = ×2⁻¹-on-labels is a literal frame-change identity.)

## 4. What to do with this (concrete, next sessions)

1. Prove the ⟨Q_s⟩ closed form (the invariant mean) — the machinery is THM-888's.
2. The frame-groupoid formulation: (Z/P)^* acts on spectra; write the isotypic
   decomposition of Q_s under it (the comb diagonal is the trivial-isotype part —
   THM-888(A) already shows it factors through w mod 7).
3. Tournament side: the relation-structure inventory as a theorem schedule — for each
   structure (1)–(5), the invariant, the frame group, the quotient law. The missing
   entry: the COMPOSITION law (how per-frame data glues = Burnside/CRT — candidate: the
   comb structure of D(q) itself, goal G4).
4. Keep the falsification pattern as a working heuristic: WHEN A UNIFORM CONSTANT IS
   CONJECTURED, LOOK FOR THE FRAME IT PRIVILEGES; the repair is the vector law. This has
   been the single most reliable proof-generating move of the week.
