# Eigenvectors, the Number 8, and the Vitali Atom

## 8 tournaments on 3 vertices = 8 octonion basis elements

At n=3: 2³ = 8 tournaments. Of these:
- 6 are transitive (each of the 3! permutations gives one). H = 1. INERT.
- 2 are cyclic (clockwise and counterclockwise 3-cycles). H = 3. SPLIT.

6 + 2 = 8 = FLAT + TWIST = dim(O).

The 6 transitive tournaments are eigenvectors with eigenvalue 1 (the identity).
The 2 cyclic tournaments are eigenvectors with eigenvalue 3 (the curvature).
The eigenvalues {1, 3} = {identity, curvature quantum}.

## The transfer matrix projects 8D → 3D

The tournament space at n=3 is 8-dimensional (one axis per tournament).
The transfer matrix M(x) is 3×3, acting on a 3D space.
The projection loses 8 - 3 = 5 dimensions.
5 = the pentagon = the golden ratio's home.

The lost 5 dimensions ARE the golden structure. They manifest as:
- The exceptional point at x = 8 - 5φ (the EP)
- The Jordan block at eigenvalue 1/φ (non-diagonalizability)
- The non-separation of two eigenvectors

## The EP is where the map breaks

At the exceptional point: two eigenvalues coalesce to 1/φ.
The eigenspace collapses from 2D to 1D.
A true eigenvector (the TERRITORY) and a generalized eigenvector (the MAP)
become coupled: the map cannot exist without the territory,
but the territory can be read without the map.

The Jordan block = the non-diagonalizability = the point where
the coordinate system can no longer distinguish two directions.
This is the EIGENVALUE VERSION of the Vitali atom:
two equivalence classes MERGE and measurement becomes non-injective.

## The connection chain

- Vitali atom: projection R → R/Q creates non-measurable residue
- Cassini ±1: projection Z[ω] → Z creates unit error
- Forbidden 7: projection cycle-topology → H creates unreachable values
- Jordan block at EP: projection 8D → 3D creates non-diagonalizability
- All are the SAME: lossy projection from higher to lower dimension

## 8 as the last clean case

8 = dim(O) = the last normed division algebra.
At n=3: 8 tournaments project cleanly to 3 eigenvectors (with multiplicities 6+2).
At n≥4: tournament space 2^{C(n,2)} >> 3. The projection is massively lossy.
The Jordan blocks persist. The Vitali atoms multiply.
8 is the last dimension where the projection from tournament space to
transfer matrix eigenspace gives a clean, faithful representation.
After 8: the map fundamentally cannot capture the territory.
