# LRC(14): endpoint-636 exchange, zero response, and recursive ideals

**Session status:** THM-4295 is `PROVED RELATIVE TO THM-4287 + FINITE-EXACT
+ DETACHED LITERAL-WALL AUDITS PASS`. LRC(14) remains `OPEN`.

Canonical result:
[THM-4295 -- endpoint-636 minimum-fourteen exchange and recursive signature
ideals](../01-canon/theorems/THM-4295-lrc14-endpoint-636-minimum-fourteen-exchange-and-recursive-signature-ideals.md).

## Inheritance pass

- **Closest proved mechanism:** THM-4287's exact missed-body response
  quotient and complete common-activity intersection over a signature ideal.
- **Canonical hostile:** the full 101-body failure layer of the 9,006-mask
  carrier on `(100,636)` and `(256,636)`.
- **Corrected near miss:** THM-4286's type firewall. A classifier, deletion
  ideal, response quotient, common deck, carrier, and proof union cannot be
  exchanged merely because their row sets overlap.
- **Least-used sidecar:** recursively enlarge from a target signature to the
  full current subset-signature ideal, then intersect activity over every row
  before choosing replacement masks.

## Live concept board

| concept | represented object | preserved predicate | main loss or hostile |
|---|---|---|---|
| row-aware response | one bit per failed `(pair,body)` | lawful carrier response at that obligation's row | forgets performance on the next endpoint |
| consumer-aware witness selection | 176-bit response vector | minimum endpoint-636 cardinality plus endpoint-632 performance | still restricted to rank-eight append-only masks |
| zero-response body | `(256,632,1d106401)` | exact emptiness of the response set | says nothing about physical entry or other certificate operations |
| recursive signature ideal | `H_D={p:I_E(p) subset D}` | one rebuilt deck active on every ideal row | may be too large for a cheap common-activity intersection |
| typed proof union | carrier node plus three common-deck nodes | exact row deletion with overlaps | destroys the existence of one common certificate |

Every meaningful pull was compared against these five objects. The most
important change was caused by the zero-response body: it converts “try a
larger carrier repair” from a plausible continuation into a refuted operation
on this fixed pool, while increasing the value of the recursive-ideal lane.

## Anchor: exact exchange on the 101 failures

For a failed body, the natural observable is not common activity across both
hostile rows. It is the row-specific predicate

```text
mask active at the body's row AND mask disjoint from the body.
```

The complete quotient has 43,660 responsive masks, 1,835 response classes,
and 259 maximal classes. Exhaustive search proves the minimum is 14; an
independent HiGHS model gives the same optimum.

The first candidate witness was optimized only for the 101-bit consumer. The
new signal was to preserve the next scale before breaking ties: attach the 75
inherited endpoint-632 failures, then minimize their misses subject to the
exact 14-mask endpoint-636 cover. The 176-bit quotient proves a sharp optimum
of 37 misses. This is a reusable move:

```text
source: cardinality-minimal response class
target: next-boundary performance
map: extend each response vector by the next consumer's obligations
preserved: current exact cover and cardinality
destroyed: individual-mask geometry outside the frozen consumers
sidecar: direct replay of the selected representatives
test: independent integer optimum plus literal selected-witness scan.
```

The selected 14-mask witness closes every live row at endpoints 636 through
633. It first fails at 632.

## Niche: recursive signature ideals

The target signatures at `(338,636)`, `(294,636)`, and `(372,636)` generate
three manageable subset ideals. Their exact row counts are 36, 21, and 54;
their exposed-body counts are 8, 34, and 24. Complete common-activity
intersections yield replacement minima 2, 4, and 4.

This connection is not a carrier improvement. Its typed map is:

```text
source: target inactive signature D
target: complete current ideal H_D
map: delete the indexed joint masks, enumerate exposed bodies,
     intersect activity over H_D, solve the response cover
preserved: one labelled body-covering deck common to all rows of H_D
destroyed: the old joint-deck identity and target-locality
sidecar: ideal membership, deleted indices, full activity intersection
test: exhaustive rebuilt body scan plus detached literal activity audit.
```

The larger top signatures were censused but not fully intersected. That is an
honest stopping reason, not a negative result.

## Wildcard: the endpoint-632 response death

The curiosity lane asked whether the first lower-layer failure was merely a
bad selected witness. One body gave a much stronger answer. At
`(256,632)`, body `1d106401` is disjoint from 203,490 rank-eight masks, but
every one is inactive. The maximum exact margin is strictly negative.

For this false continuation claim:

- **Minimal witness:** `(256,632,1d106401)`.
- **First failed implication:** a carrier failure can be repaired by adding
  another rank-eight mask.
- **Strongest survivor:** the selected carrier closes all ten live rows in
  layers 636 through 633 and is downstream-optimal at cardinality 14.
- **Repaired form:** append-only descent is valid through endpoint 633 and
  terminates at this endpoint-632 response obstruction.
- **Missing coordinate:** a different operation or certificate type, not a
  larger append budget.
- **New question:** which deletion ideal or higher/lower-rank body-cover
  operation first responds to `1d106401` while retaining a lawful common
  consumer?

The body occurred in earlier hostile work, but its complete response death at
this lower endpoint is a scale boundary rather than mere recurrence.

## Typed consequence and updated frontier

The carrier closes ten rows. The three ideals are pairwise disjoint, but each
overlaps the carrier in one top row. Therefore the proof union has
`10+36+21+54-3=118` rows. Exact independent consumers leave 22,529 rows,
maximum endpoint 632.

The portfolio interaction is the real output:

- the anchor supplied a strictly better fixed-pool boundary;
- the wildcard proved why the same operation must stop;
- the niche supplied three lawful orthogonal certificate nodes after the
  stop;
- the proof graph kept these gains without pretending that they form one
  common deck.

## Next hostile probes

1. Generate the complete inactive signature of the zero-response row and
   census its recursive ideal before attempting a common-activity
   intersection.
2. Change the repair rank and first ask whether `1d106401` has any lawful
   response; an empty response gives another cheap stopping theorem.
3. Search for a small deletion set whose exposed-body obligations include
   the endpoint-632 witness and whose full ideal remains computationally
   manageable.
4. Keep physical entry separate. No fixed-pool body-cover failure is evidence
   of a counterexample to LRC(14).
