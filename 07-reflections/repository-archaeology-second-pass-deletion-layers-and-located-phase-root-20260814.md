# Repository archaeology, second pass: full deletion layers and located phase

**root/repository-archaeology-second-pass-2026-08-14.  Reflection, not
canon.**  Current truth lives in the cited theorem and correction files.

## Outcome

This session deliberately revisited the first pass's cheapest untested
boundaries rather than repeating the loose-object sweep.  It produced four
proved results:

1. [THM-3377](../01-canon/theorems/THM-3377-path-colour-deletion-compiler-and-skew-current.md)
   proves the complete symmetric deletion compiler of THM-3166's path-colour
   response, its first jet, negative-colour reciprocity, and a four-variable
   skew current.  Strong order-six masks show the first jet is load-bearing.
2. [THM-3378](../01-canon/theorems/THM-3378-projected-k3-z216-gcd24-L129360-row94-one-high-torsion-closure.md)
   closes the first row of the next live projected LRC family.  The exact
   ledger/wall/family counts are now `372913/109/12`; LRC(14) remains open.
3. [THM-3380](../01-canon/theorems/THM-3380-hamiltonian-deletion-layer-monoid-semiring-and-small-order-boundaries.md)
   promotes the deletion-card histogram to an ordered-join monoid-semiring
   interface and finds its sharp order-eight spectral/palindrome boundaries
   and the order-nine cycle-fugacity wall.
4. [THM-3381](../01-canon/theorems/THM-3381-reflected-residue-affine-phase-transport-and-frozen-tree-stability.md)
   selectively recovers a genuinely unmerged branch lemma: exact affine
   transport of a reflected danger comb and a phase-aware fixed-tree
   perturbation inequality.  Its one-unit hostile refutes residue-blind
   extension; its positive chamber shows the repair is nonvacuous.

None proves a physical lift of the projected row, the LRC rung, arbitrary
`k=1`, LRC(14), an all-order tournament reconstruction, JC(2), or FC(3).

## Portfolio and inheritance

The anchor was the forgotten LRC phase/contact toolkit against the current
`110`-row/`12`-family surface.  The niche was a second differential scan of
divergent branches and stale reserved namespaces.  The wildcard was the first
untested order of the deletion-layer and packing-fugacity probes.

The inheritance pass used:

- THM-2941/2979/2984/3351/3361 for the projected one-high located-torsion
  terminal;
- THM-3355/3360 for reflected physical pair/tree certificates;
- THM-3166/3181 and THM-3372 for path-colour and Hamiltonian deletion
  transforms;
- THM-3324/3369 for symmetric and skew marked responses; and
- the first pass's canonical hostiles `K1 triangleright C3`, masks `16/83`,
  and the self-converse kernel.

The corrected near misses were translated-versus-centred danger bands,
denominator/unit passports without height, scalar deletion layers without
card distributions, and a first exterior current treated as if it recovered
all order.

## Live concept board after the pulls

The board began with six objects:

1. symmetric deletion sums;
2. the summed marked first jet;
3. skew relation currents;
4. located torsion phase;
5. deletion-card distributions; and
6. branch reproducibility.

The meaningful updates were relational, not merely additive.

- THM-3377 showed that a multiplicative response can require an independent
  first jet even when its scalar polynomial is known exactly.
- THM-3380 showed that the next loss is not another scalar moment: an entire
  marked spectral deletion deck can agree while Hamiltonian deletion-card
  histograms differ.
- Exteriorization restores source/sink order but stops structurally on
  self-converse palindromes.  The next response should be iterated/nonabelian,
  not a larger symmetric Gram.
- THM-3378 uses finite order-two/three phase separation to close a projected
  row, while THM-3381 shows why denominator/unit data alone do not locate a
  physical overlap: ray height and centered phase must be retained.
- THM-3364's cyclotomic phase and THM-3381's centered cell defect are two
  representations of the same abstract warning—translation is group data,
  not a small scalar perturbation—but they live on different carriers and do
  not transfer without a typed map.

## Four exact connection contracts

| source -> target | map | preserved | destroyed | sidecar / cheapest test |
|---|---|---|---|---|
| THM-3166 path colour -> THM-3377 deletion compiler | `Q_T` to `sum_X Q_(T-X)a^|X|` | ordered-join product, colour grading, deletion Taylor layers | individual deleted-set labels | first jet `q=sum_vQ_(T-v)`; masks `16/83`, then adjoin one sink |
| Hamiltonian deletion cards -> THM-3380 scalar transform | `M=sum_Xt^|X|[H(T-X)]`, then `[h]->h` | layer histograms before augmentation, exact join product | card distribution after augmentation, cross-layer association | first four-class order-eight fibre; the first-deletion sums agree but cards differ |
| odd-cycle packing -> formal fugacity -> THM-3372 | `F(x,y)` then `x=2` | covered size and join product | number of cycles at the first equal-weight partition wall | order-nine `{9}` versus `{3,3,3}`; `161x+9x^3` versus `157x+10x^3` |
| canonical reflected drift -> perturbed physical clause | `phi_h(u)=(zu-Delta)/(z+h)` | untruncated comb order and endpoints, fixed-tree labels | clipping, tree optimality, cell choice | retain `Delta=hj-mL`, `h,z`, singleton masses and degrees; `492->491` kills overlap |

Each map now has a minimal hostile.  That is why these are reusable interfaces
rather than analogies.

## What failed, and what survived

### The deletion-layer coincidence

Through order seven, the fibres of the full card histogram `M` and
`(Gamma,Dham)` coincide.  Complete order-eight enumeration refutes the implied
all-order bridge:

```text
M profiles=3328,       (Gamma,Dham) profiles=3325.
```

Exactly three four-class spectral fibres split into two `M` converse pairs.
In the first fibre, even the multiset of every marked spectral response agrees.
The survivor is precise: on order eight, `M` strictly refines
`(Gamma,Dham)`, and the missing coordinate is the Hamiltonian card histogram,
not a higher spectral moment.

### The first-current reconstruction hope

`(M,xi)` is injective through order seven and has one order-eight collision:

```text
K1 C3 C3 K1,             C3 K1 K1 C3.
```

The SCC words differ, but both are self-converse palindromes.  Commutative
interfaces see the same factor multiset and converse-odd currents vanish.
This is a mechanism, not a random collision.  The next hostile probe is an
iterated third-order marked-response tensor capable of seeing word order after
signed area vanishes.

### Packing fugacity at the restricted-partition wall

For covered size at most eight, the number of odd cycles is forced by the
covered size.  At nine, the partitions `9` and `3+3+3` have different cycle
counts but evaluate with weights `2` and `8`.  A complete `191536`-class
census finds `91780` scalar transforms and `91797` fugacity profiles: exactly
17 scalar fibres split.  The survivor is an exact order-nine interface:
`(Dham,b_333)` recovers the whole cycle-count fugacity.

### The residue-blind physical transfer

The branch proposed extending canonical reflected pair floors to nearby
residues.  The first failed implication is already one coordinate and one
unit: overlap `6/209` for `(492,836)` becomes zero for `(491,836)`.  The
strongest repair is THM-3381's exact centred-phase budget and frozen-tree
inequality.  It is useful but perturbative; it does not make arbitrary
residues or a reoptimized tree continuous.

## Anchor progress: one live projected row

Row `94` of the `gcd24/L129360` family has

```text
E=(1,3,8,10,11,14),       571=292+195+84 states.
```

The strict high gate and positive duplicate-permitting two-high gap force
exactly one high drift in every residual passport.  Each of the `84` cases has
two fixed-safe cells whose difference has effective order two (`80` cases) or
three (`4` cases).  Their phase gap is at least `1/3`, too large for one
translated strict-open `1/7` danger band.  This closes the row and nothing
larger: the other twelve rows of that same family remain live.

The abstract connection to THM-3381 is informative but asymmetric.  THM-3378
uses a finite torsion gap after the projected wall has forced one high;
THM-3381 controls physical motion of already located clauses.  Neither supplies
the other's missing projected-to-physical map.

## Second-pass branch archaeology

The high-value divergent branch was
`origin/codex/lrc-math-20260812@7386032013`.  Selective recovery, not a
cherry-pick, was mandatory because its branch-local THM and MISTAKE numbers
collide with current canon and its proof-map context is stale.

Ranked residue after promoting THM-3381:

1. **Reflected-to-projected transfer: REFUTED as stated; scoped FINITE-EXACT
   survivors.**  The current projected rows have no literal intersection with
   the old `649`-body source, and `216=qL-e` fails.  On row `94`, replacing
   height `12741` by `12741+L` preserves denominator/unit but changes overlap
   from zero to `6160/47367`.  The required sidecar is `(d,u,h,j)`, not `(d,u)`.
   Separate four-ray `k=3` and five-head `k=2` finite scans look positive but
   were not promoted in this session.
2. **Gaussian spinor congruence surface: FINITE-EXACT / PROVISIONAL.**  For
   `V_p=(F_p^2-{0})/{+/-1}` with `det(u,v)^2=1`, exact primes through `23`
   support a triangulated orientable surface and a Gaussian null-cone map.
   Projection to `P1(F_p)` destroys the radial square class and half the
   triangle predicate.  The next honest theorem is an all-odd-prime link and
   orientation proof retaining radial `eta`; no LRC transfer is licensed.
3. **THM-1278 weak-turn two-owner collapse: OPEN / AUDIT-REQUIRED.**  The Lean
   file proves an abstract word/count statement; the Python prose invokes an
   absent physical provider to eliminate `BAB`.  Its own hostile
   `(h,A,B)=(60,20,39)` places the `BAB` load strictly between six and seven.
   Recover actual danger arcs and seams before promotion.
4. THM-3361 and the C13 sidecar branches are already integrated by patch;
   U-spine, GMC-3104, old THM-2999 and several empty reservations contain no
   new recoverable theorem.  A bounded kind-Mendel enumeration is reproducible
   but its radicals inference is superseded by current guardrails.

This confirms the first session's negative archaeology lesson: targeted live
branches, reserved proof/provider mismatches, and first-untested boundaries
are higher yield than another indiscriminate unreachable-object scan.

## Ranked next probes

1. **Anchor — the other twelve `L129360` rows.**  Reuse THM-3378's exact-one-
   high reduction, but precompute located order-`2/3` witnesses by low-pair
   class.  Stop at the first passport without such a pair and retain its full
   residue support; do not infer a family closure from row `94`.
2. **Anchor sidecar — phase-budgeted physical compiler.**  For each current
   reflected packet retain `(M_T,deg_T,z_i,h_i,Delta_i)` and compare exact
   symmetric differences before using THM-3381's coarse tail.  Test the
   one-unit hostile and the shift-84 cross-tree failure first.
3. **Wildcard — iterated response.**  Build the least third-order tensor whose
   Chen/word-like cross term distinguishes `ABBA` from `BAAB`; require a closed
   ordered-join law and test relabelling/converse gauges before enumeration.
4. **Wildcard — length fugacity.**  Cycle-count `x` distinguishes `9` from
   `3+3+3`, but not equal-count types such as `3+7` and `5+5` at covered size
   ten.  Pull THM-505's length variables through deletion Taylor laws and ask
   for the first actual scalar collision.
5. **Niche — spinor surface proof.**  Derive vertex links, triangle lifts and
   genus for all odd primes, split `p=1/3 mod4`, and only then compare `H_1`
   with a graph/cusp current carrying radial owner.
6. **Branch JC lane, still provisional.**  The prior session's THM-3074/3080/
   3081 Laurent-key chain remains worth an independent polynomial-initial-
   module audit; depth/gcd enumeration is no longer the missing coordinate.

The second-pass methodological conclusion is sharper than “retain more data.”
Retain the first object on which the next operation is lawful: a first jet for
composition, a card histogram before augmentation, an iterated response after
area vanishes, and a centred phase before translation is quotiented.  Then
attack the first size or shift where two legal decompositions become possible.
