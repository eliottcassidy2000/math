# The repo's concept space in one view — three surveys, three tests, one convergence phenomenon

**death-star-2026-07-19-S59l** (HYP-8045; owner: run the generators, pull and synthesize,
become aware of as many of the repo's concepts as possible, test ideas from past threads).
Method: three parallel Explore surveys (tournament half / engineering half / LRC
side-threads), ~10-minute pull cadence over a hot fleet day, and three tests executed
from what surfaced. The three full survey reports are preserved in this session's record;
this reflection is the weave.

## 1. The shape of the whole

The repo is ONE project wearing three coats:

- **The tournament/parity half** (THM-001..~900): Rédei with four in-house routes, the
  OCF/Claim-A core (proved, partly externally), the conflict-graph Ω world with its
  real-rootedness boundary at n=9 (THM-025) and the H=7/H=21 permanent gaps, the
  metagraph triad **G_n / G_n/Z₂ / E_n** (spine-ribs-sea, the even-graph dual), the
  tiling model with its wiggly/waggly/blue-black stratification, the triangle frame
  (three sides, four constants, Mode A/B), the Cayley-Dickson/Paley/Hadamard tower,
  and the enumeration backbone (A000568 to n=200, ΣH, tribonacci).
- **The LRC half**: the proof-map arc plus — the side-survey counted them — **53 named
  conceptual frames**, from the covering-min deep well (14/183, Φ₆-Eisenstein) through
  three-gap/Ostrowski/Stern-Brocot, Kakeya-as-adaptive-graphic-rank, the fill profile,
  Goddyn-Wong ties, the K- and 12m/13s-ladders, the D-graded gate tower, to the newest
  synthesis layer: **rung theory** (stack/duty/exclusion/θ-flow).
- **The engineering half**: TWO eras. The March homology stack (12 domains; mod_rank,
  TDA, circulant homology shipped-as-modules; GPU and toolkit-class items spec-only;
  the T₁₉ barrier engineered-against but NOT verifiably broken, with a FLAGGED sparse
  eigenspace bug — the P₇ palindrome mismatch in omega6_sparse.out). And the LIVE era
  nobody had named as engineering: the LRC exact-computation + Lean-certificate
  pipeline (exact evaluator → parallel scanner → Python-validated Lean generator →
  601 LRC Lean modules with kernel-exact spectrum values). It is one packaging session
  from being the repo's flagship product.

## 2. The convergence phenomenon is the story

The deepest pattern across all three surveys: **the repo keeps discovering the same
objects under different names**, at a measurable cost (two vocabularies of one spectrum
theory built 13 days apart; "seven names, one object" for the residual core; my own
MISTAKE-187). The extended identification table now includes a dozen entries — binder
gate ≡ determinant slack ≡ GW acceleration; forced-cover ≡ ghost channel ≡ ghost loop ≡
CF active-leg; attained ≡ certificate ≡ gridmax ("one ladder, three roles"); fill-profile
dichotomy ≡ binder gate one Farey level up; the θ=1/2 dominance tournament ≡ the (D,s)
rung lattice rediscovered. The countermeasures are now infrastructure: CONSTANTS-INDEX
(constants are vocabulary-invariant), the statement-grep discipline with synonym
families, and opus-S410's attempt to NAME the unified field (rung/duty/ghost/gate/θ) so
future work lands in one vocabulary. The honest kernel count after all identifications:
**three genuinely distinct walls** — the inverse/rigidity (Freiman/near-AP) core, the
six-comb phase-transport wall, and the bound-D shell.

## 3. The bridges, graded

The halves are genuinely one project at the level of *concepts* and provably distinct at
the level of *proof steps*: klein-S322's ±-fibration makes this precise — tournaments
are SECTIONS of (Z/q)*→U_q, LRC is the BASE, and the decisive disanalogy is that the
tournament Z₂ (T↔T^op) has fixed points (SC classes, the spine) while the LRC Z₂
(s↔−s) is free for every q ≥ 3 — no spine, so SC-keyed transfers fail a priori. The
bridges that DO carry weight: the observer principle (Rédei = LRC + 1: inshat's
1 + 2·#3-cycles is the same atom as the Farey escape), the CV² ~ 2/n concentration
identity (metagraph variance = covering concentration), parity-as-obstruction on both
halves, the Φ₆ = 2·T_{n−1}+1 triangle extension, and the c3(t*) regularity bridge —
now correctly weakened by mac-mini's trajectory profile (excursions, not argmax).

## 4. Three tests run this session (the "ideas you test" directive)

- **K-ladder (from kind-pasteur S128c86's filed target)** → THM-1295: the floor of
  M(K_c(N)) = c/(cN+1) proved in three lines (witness a = c; x ≡ −1 makes it literal);
  the ceiling's seals proved (complete-prefix Dirichlet — the deleted-element sub-cases
  VANISH; packing; e=1 channel killed by u = q mod N via m ≤ c−1 < cr; m=1 channel
  empty by self-consistency); verification extended to (N,c) ≤ (60,12), 696 pairs, zero
  violations; and the residual identified as THE SAME e≥2/m≥2 ghost-channel corner as
  the F_D tower's — one future proof closes both ladder laws.
- **Dyadic descent of tower members (from mac-mini S127's HYP-8015)**: all four tested
  gate-tower members collapse in ONE σ-step onto the 1/2ʲ ceiling ladder — the D-gate
  structure lives entirely at the finest 2-adic layer (consistent with the gates'
  N-odd requirement), giving mac-mini's invariant a sharp new separation datum:
  K-threaded descent (their F₃ data) vs immediate collapse (the tower).
- **Octal H-spectrum sieve at n=7 (dormant tournament testable, T832/mac-mini-S1)**:
  250k-sample exact Held-Karp — 35 and 39 ARE realized at n=7 (~1190× each); missing
  odd values ≤ 60 are EXACTLY {7, 21}. The n≤6 gaps {35, 39} were small-n artifacts;
  the permanent-gap conjecture now reads clean: only the two proved/impossible values.

## 5. The generator runs (lead xxiv, in flight at writing)

4/367 (91 speeds, 271 moduli) and 6/1271 (211 speeds, 631 moduli, worst S = 2520)
validated Python-side in seconds and emitted; the Lean builds are hours-scale and may
finish after this session's close (interim-close precedent applies). When they land,
the kernel-exact ledger reads 3/23, 4/127, 4/247, 4/367, 6/1271 — five machine-checked
exact spectrum values spanning two gate levels.

## 6. What the surveys hand the fleet (a portfolio, priced)

Sub-hour testables surfaced and NOT consumed today: the slack-2 multi-far rung search
(opus-S407's named open row — the live form of "first off-single-far gap member");
the θ-flow separation thetas + ranking-invariance falsification; the c(p) seven-tile
excess growth law past p = 269; moment-blindness decay at degrees 8-10; SC-blowup's
Ω-operation (n ≤ 5, exact); root-ratio SC-minimization at n = 7; E₈ perfectness
(ω = χ?); the invisible cospectral pairs as n=8 double-cones. Engineering: ship
mod_rank to PyPI (highest reuse-per-effort); NAME and package the live LRC toolkit;
root-cause the P₇ sparse-eigenspace mismatch before any T₁₉ claim.

## 7. The meta-lesson

Today's fleet (seven+ concurrent sessions, seven claim collisions, the CONSTANTS-INDEX
born mid-day) is the repo hitting the scale where **epistemics is the bottleneck, not
mathematics**: the marginal theorem is cheap; knowing whether it exists already is
expensive. The instruments that worked — constants as keys, validation gates,
statement-grep with synonym families, typed residuals, the Lean ledger as the only
monotone meter — are the project's real accumulated technology, and they are exactly
what let one session absorb three surveys, close three tests, and add a theorem
without duplicating a line of the fleet's concurrent work.
