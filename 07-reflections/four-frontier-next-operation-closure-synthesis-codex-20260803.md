# Four frontiers after the next operation: mixed faces, reset forks, base ideals, and local circuits

**Status:** procedural synthesis.  The single-edge sequence statement is
canonized as
[THM-3305](../01-canon/theorems/THM-3305-single-edge-visible-zero-mode-and-rank-two-resolvent-update.md),
with a separate exact audit.  The lawful HFC--JC boundary bridge in
[THM-3303](../01-canon/theorems/THM-3303-keller-simplex-null-moments-force-a-boundary-collision.md)
and the Fourier/sign-cell statements in
[THM-3304](../01-canon/theorems/THM-3304-fourier-dirichlet-kernel-and-alternating-quintic-hfc3-exclusion.md)
are proved at their literal scopes.  The affine-`c` square discriminant is
canonized with a separate exact audit as
[THM-3306](../01-canon/theorems/THM-3306-affine-c-critical-section-square-discriminant-and-transverse-base-locus.md).
The LRC first-normal lift and factorial all-choice closure are **FINITE-EXACT
PARTIAL SCOUTS**.  This reflection is not a proof source and proves none of
`LRC(14)`, `FC(3)`, `SFC(3)`, `HFC(3)`, `JC(2)`, or `DC(2)`.

This note continues
[the compatibility-object and jet-budget synthesis](four-frontier-compatibility-objects-and-jet-budget-synthesis-codex-20260803.md).
That note proposed four cheap next tests.  All four have now been carried out.
The common lesson sharpened from “restore a lost coordinate” to:

```text
close a proposed section under the next native operation before trusting its
scalar shadow.                                                       (1)
```

The objects exposed by `(1)` are not interchangeable.  They are a missing
mixed face, a terminal reset fork, a transverse base ideal, and an
observer-visible kernel circuit.

## Inheritance pass and live board

| lane | closest proved mechanism | canonical hostile / corrected near miss | least-used sidecar restored here |
|---|---|---|---|
| LRC(14) | THM-3285's literal `R-M-R` horn and THM-2813's lawful label successor | the `tau=12` target-only simplex; MISTAKE-281/300/310/313 | a common raw source/target square before covariance or current |
| factorial / three faces | THM-3286's binary Helly colouring and selected-successor section | the full-domain `(16,17)` failure and incompatible reset state | active face, exact reset distance, and all row-labelled choices |
| planar Jacobian | THM-3289's affine `C,E` clutch no-go and its linear-subresultant control | treating `[-b:a]` as a chart switch when `a=b=0`; MISTAKE-360 | the ideal `(a,b)`, its blow-up, and the surviving quadratic row |
| static response / sequences | THM-3288's rational witness walks and selector half-transfer | the path `2-17-16`, selector-only lumping, and chronology confusion | active-start births, zero-mode head, and the two edge endpoints |

The five live concepts were therefore:

```text
raw mixed atom; reset-distance cone; transverse base ideal;
localized kernel circuit; boundary/multiplicity passport;
target-preserving chronological action.                               (2)
```

Every new object was compared with the final item in `(2)`.  None of the
first four is itself a chronological action.

## Exact answers to the four proposed tests

| test | exact finite or proved answer | positive survivor | first unresolved operation |
|---|---|---|---|
| [13-label LRC first-normal lift](lrc14-horn-first-normal-lift-partial-scout-codex-20260803.md) | all `13` lawful labels were tested on the canonical `(clock,sigma,tau)=(1,0,3)` square; only `(t,q)=(0,0),(6,3)` are middle-complete, but every label has empty outer typed co-support and no candidate reaches the `169 x 2` covariance scan | `t=6,q=3` recovers the target-only THM-2820 complete-section signal | build a lawful two-axis physical square with independent source and target increments and a common raw atom |
| [three-face all-choice closure](three-support-face-all-choice-reset-fork-closure-codex-20260803.md) | the smallest lane-wise closure has `854` typed nodes, `760` physical states, `30,853` unrestricted row-labelled edges, and exactly four blocked typed nodes | `Phi=sum` active reset distances drops by the active-face count on every edge; every `F12` path resets in `1..6` steps | construct a lawful origin rebind/copy at the four shared blockers; separate lane-wise inevitability from a shared interleaving |
| [affine-`c` critical section](../01-canon/theorems/THM-3306-affine-c-critical-section-square-discriminant-and-transverse-base-locus.md) | in each accessory field, `a` is irreducible squarefree of degree `36`; for `c_*=-b_0/b_1` in `K[x]/(a)`, `H=H_2(c-c_*)^2` and `b=b_1(c-c_*)`; hence `Res_x(a,H)=unit*D(c)^2` and `Res_x(a,b)=unit*D(c)`, with `D` squarefree irreducible of degree `36` | the base ideal `(a,b)` is transverse; the preceding quadratic subresultant survives, so the common `y`-fibre has degree exactly two, while the distinguished `H` root has exact `x`-multiplicity two | pull the quadratic row to the exceptional divisor of `Bl_(a,b)` and determine splitting or monodromy |
| [single-edge relation compiler](../01-canon/theorems/THM-3305-single-edge-visible-zero-mode-and-rank-two-resolvent-update.md) | all `11` missing core edges were exhausted; `(7,18)` is uniquely observer-visible at zero, with mass `3/5`, order `15`, half-orders `8/7`, and an exact `2 x 2` Woodbury update | the local circuit `3e_(18,Q+1)-sum_(six siblings)e_leaf` gives the entire finite head and rational series | compose several bordered/rank-two updates while retaining node births and the zero-mode projection |

These are stronger stopping reasons than four failed scalar searches.  The
LRC lift never produces a typed domain on which covariance is meaningful.
The factorial colouring survives every local response test but its all-choice
closure ends at four physical incompatibilities.  The Jacobian graph section
fails at a geometrically clean base locus rather than at an uncontrolled PRS
row.  The sequence update succeeds because its defect is a local linear
circuit that can be retained exactly.

## A lawful beach crossing arrived while the four probes closed

The concurrent
[fibre-passport synthesis](cross-frontier-fibre-passports-after-coprime-completion-root-20260803.md)
contains two proved components that materially sharpen this board.

First, THM-3303 gives a literal, predicate-preserving map from a narrow
homogeneous factorial sector to the planar Jacobian frontier.  For a triangle
polynomial `g=p+iq` with all positive area moments zero, assume

```text
det D(p,q)=constant != 0.                                      (3)
```

If `(p,q)` were injective on the triangle boundary, degree and change of
variables would turn the image into a one-node quadrature domain.  Its
Bergman kernel forces a centred disk, while a nonconstant polynomial edge
cannot trace a circle.  Hence the boundary collides.  Therefore any HFC(3)
counterexample satisfying `(3)` is already a noninjective planar Keller map,
and

```text
JC(2) implies HFC(3) in the constant-nonzero-real-Jacobian sector.      (4)
```

This is a one-way sector bridge, not an equivalence.  A boundary collision
does not force multiplicity two: cuts, holes, and boundary identifications
remain possible.  The preserved sidecar is the joint boundary-incidence and
sheet-multiplicity passport.

Second, THM-3304 turns the previously vague invariant-Gaussian sieve into an
exact Fourier moment kernel.  In barycentric Fourier coordinates `a,b`,

```text
sum N_(r,s) u^r v^s/(r!s!) = 1/(1-u^3-v^3-3uv).             (5)
```

Thus `<a^r b^s>` vanishes exactly off the balanced congruence and is strictly
positive on it.  A degree-four cyclic null candidate must have shortest closed
coefficient-phase covering width at least `pi/3`; THM-3310 later makes this
strict and THM-3321 closes support `<=4`, while full support five remains open.
Its five moment forms through `M15` are frozen modulo `103`.  Orthogonally, every
alternating homogeneous quintic is

```text
(x-y)(y-z)(z-x)(A e1^2+B e2),                               (6)
```

and its second/fourth moment numerators have nonzero resultant `846709600`,
including the infinity chart.  That representation cell is completely
excluded.

These results meet the present probes at clean type boundaries.  THM-3306
stops **before** a Keller map, at a critical base
ideal; THM-3303 starts **after** the Keller condition and forces a boundary
passport.  The three-face closure concerns a finite response controller for
full `FC(3)`; THM-3304 concerns homogeneous triangle moments for `HFC(3)`.
Neither implication may be reversed.

## Three concurrent advances that change the next queue

### The direct LRC ledger, not only the horn, moved

The independently audited
[costly gcd-eight closure](../05-knowledge/results/lrc14-z216-costly-gcd8-closure-opus-20260803.md)
empties the two `z1=216` rows omitted only by the earlier work cutoff.  Rows
`238` and `370` have invoices `19,059,040`; exact screening leaves `115` and
`19` residuals, and the complete one-high terminal banks close all of them
with minimum slack one.  Consequently

```text
projected ledger 373186 -> 373184,
z1=216 wall rows    382 -> 380,
gcd-eight stratum  complete.                                  (7)
```

This is necessary projected-atlas progress, not a physical-cover theorem.
The direct economical target is now an intrinsic ruler family in the
remaining gcd `24/36/72` walls, ranked by `sum L*r`.

An orthogonal finite-exact lane also became sharp.  The
[micro-staircase audit](lrc14-microstaircase-binary-audit-microstaircase-audit-agent.md)
proves that at `n=14` the full blockers in its residue/open-cell model are
exactly the `14` scalar ramps; normalized `n=15` is also unsatisfiable.  This
does not use speeds, endpoints, gcd data, or physical covers.  The suggested
all-`n` rigidity has a deliberate hole at `n=13`.  A boundary finite-module
shadow is insufficient: one nonscalar vector passes all `14^2` boundary tests
but misses `336` interior-cell candidates.  The viable human route must join
rank-one affine-cover rigidity to Farey-cell curvature.

### The Jacobian critical divisor now has a typed inverse graph

The finite-exact
[critical inverse-graph sidecar](jc-critical-inverse-cover-cofactor-jacobian-probe-agent-20260803.md)
retains two PRS pivots after scalar elimination.  If its quadratic pivot `a`
and later linear pivot `ell_1` are units modulo a saturated critical factor
`H`, the critical scheme is the unique graph

```text
y=-ell_0/ell_1 in K[x]/(H).                                (8)
```

The associated elimination cofactors satisfy

```text
U-(V'/4)W=a^2,                                             (9)
```

so they are unimodular wherever that pivot is a unit.  Both named degree-53
controls pass `gcd(H,H')=gcd(H,ST)=gcd(H,a)=gcd(H,ell_1)=1`.

This does not supply a Keller mate.  The quadratic pivot called `a` in this
inverse-graph note has degree `32`; it is **not** the degree-`36` leading
coefficient `a` in the affine-`c` linear-subresultant scout.  The useful
connection is a pivot passport, not a symbol identification:

```text
scalar critical factor -> quadratic pivot -> linear pivot -> inverse graph
                       -> primitive Keller cofactor -> integrable mate.  (10)
```

The first four stages are now exact on named controls; the last two remain
open.  If a gradient ideal is already the unit ideal, the same note gives a
canonical next obstruction

```text
mu(P)=[A_x+B_z] in coker(P_x partial_z-P_z partial_x),       (11)
```

independent of the chosen Bezout row.  A polynomial mate exists exactly when
this class vanishes.  Thus the blow-up quadratic on the affine-`c` base locus,
the two inverse-graph pivots, and `mu(P)` form three successive sidecars rather
than three names for one obstruction.

### Reynolds averaging fails the operation-closure test exactly at power two

The
[factorial/Gaussian quotient no-go](factorial-gaussian-power-compatible-quotient-no-go-bridge-probe-agent.md)
confirms that the forward radial embedding is an algebra map and hence
preserves powers.  The tempting reverse Reynolds projection is only linear.
On THM-3290's explicit Gaussian polynomial,

```text
R(P^2)-R(P)^2
 = -4 q^2 t^2 (q+t^2)^8 (3q-2t^2),                       (12)
```

and the hostile Gaussian moment has magnitude `3,212,537,328,000`.
Therefore a Gaussian null sequence cannot be pushed into FC by averaging
`P` first and then taking powers.  The entire character grading or the whole
sequence `R(P^m)` is required.  This is an exact operation-intertwining
failure and direct evidence for rule `(1)`.

## One operation earlier than the scalar symptom

The four calculations fit the following typed ladder:

| lane | next native operation | pre-scalar defect | later scalar symptom |
|---|---|---|---|
| LRC | take the mixed source/target first-normal face | empty typed co-support and `144` joint-absent mixed aggregate cells | no affine covariance or positive current to test |
| factorial | allow every legal row-labelled successor until closure | four terminal shared-face blockers `C union U`, with `C=(3,4,5,6,7,8)` and `U subset {1,2}` | failure of a fixed chronological controller |
| Jacobian | vary the affine parameter through the linear-section degeneracy | the transverse ideal `(a,b)` and its degree-two fibre | the square norm factor `D(c)^2` in `Res_x(a,H)` |
| sequence | add one missing static relation edge | a three-versus-six adjacency-kernel circuit | a `-108` recurrence head and zero spectral mass `3/5` |

Thus the useful commonality is not “all four have a kernel.”  The LRC defect
is an empty intersection, the factorial defect is a terminal transition-cone
intersection, the Jacobian defect is a nonempty scheme-theoretic base locus,
and the sequence defect is an actual vector-space kernel.  Identifying them
would destroy exactly the typing that made the probes decisive.

What does transfer is where to look.  A scalar statistic usually reports the
defect one operation late.  The raw mixed square precedes covariance, the
closed successor cone precedes scheduling, the coefficient ideal precedes a
resultant discriminant, and the incidence circuit precedes recurrence
elimination.

## The positive survivors are more informative than the no-go statements

### LRC: the missing coordinate is genuinely two-axis

The lawful THM-2813 successor changes the target address by
`q_alloc=7t mod 13`; the source address is fixed by the tested operator.  The
middle signal at `t=6,q=3` is therefore real but one-sided.  Even the unique
raw co-support at `t=0` has a flat `K4=(1,1,1,1)` and no mixed face.  Another
one-parameter normal scan cannot create the independent source increment
that the failed test names.  The next object must expose two physical axes
before `q_alloc` is compared with any endpoint quantity such as `L-R`.

### Factorial: scheduling slack points to origin recycling

The closure potential proves termination rather than success.  On every one
of the `94` starts, the inevitable shared-face fork occurs no earlier than
the `F12` reset; the difference histogram is

```text
0:7, 1:24, 2:16, 3:16, 4:31.                              (13)
```

Equality occurs on nonempty subsets of `(3,4,5)`.  This is a precise signal
for recycling the origin released by the `F12` reset.  It is not yet a copy
map: a lawful rebind must preserve the face ancestry and actual response.
The two-pole macro is a hostile control, not the repair.  Its only jointly
typed choices are `(+1,+2)` and `(+1,+4)`; both land on universal blockers
without a common first one-pole step.

### Jacobian: the failure locus is clean enough to blow up

At `D(c)=0`, the homogeneous selector is `[0:0]`, but the determinant of the
coefficient map is `a'(x)b_1(x)`, a unit modulo `a`.  Hence the base locus is
transverse.  This makes the blow-up a concrete next carrier rather than a
metaphor.  Moreover the degeneracy is interior to the live section: `D` is
coprime to the degree-six owner-boundary wall and degree-five finite-clutch
wall.  The `183` rational values of height at most `12` hit neither `D` nor a
repeated-`H` failure; the only wall values, `-2` and `-3/2`, are the already
known finite-clutch failures.  This bounded census does not classify the
degree-119 residual repeated-`H` locus.

### Sequences: locality makes exact recompilation possible

For `(7,18)`, the update is literally

```text
e_(7,Q-7)e_(18,Q+1)^T + e_(18,Q+1)e_(7,Q-7)^T.             (14)
```

The old resolvent therefore needs only its two endpoint Green functions and
a `2 x 2` inverse.  The same exhaustive census also shows what a compiler
must not assume: three edges birth active nodes, and `(19,22)` lowers observer
order from `14` to `12`.  Recurrence order is neither monotone under edge
addition nor determined by vertex and arrow counts.  A robust incremental
compiler must carry the start vector, births, endpoint resolvent block, and
kernel projection.

## Tournament structure: a negative and a positive boundary

The relation graph now has an exact operation law under one edge addition,
but this does not make it a tournament.  Its adjacency is symmetric, ties and
multiplicities are meaningful, and the observable counts static
witness-decorated walks.  Forcing an orientation would erase the local
three-versus-six circuit that creates the head.

The positive tournament lesson is procedural: operation-response structure
can be computed before inventing a new scalar invariant.  Here the endpoint
resolvent block is the sufficient local state for an edge update.  In a
genuine tournament lane, the analogous question is which score, SCC,
switching, or substitution sidecar makes its native directed operation
closed.  Symmetric compatibility remains a control, not an orientation.

## Ranked frontier after closure

1. **Direct LRC gcd `24/36/72` ruler family.**  Rank the remaining projected
   walls by `sum L*r`, build one complete intrinsic ruler family, and retain
   the exact terminal semantics used by the audited gcd-eight closure.  This
   attacks the `373184` ledger directly, while remaining only a necessary
   projected atlas.

2. **LRC two-axis common-atom square.**  Choose independent lawful source and
   target increments, reconstruct the common raw atom, and require all outer
   semantic factors plus a nonzero mixed face before any `169`-origin
   covariance scan.  The present one-axis atlas is the zero-control.  In the
   orthogonal micro-staircase lane, isolate the `n=13` case by combining
   affine-cover equality cases with interior-cell curvature.

3. **Jacobian exceptional-divisor and pivot passport.**  Blow up the degree-36
   linear-row ideal `(a,b)`, restrict the
   surviving degree-two subresultant to the exceptional divisor, and compute
   its discriminant and monodromy.  Then audit the distinct degree-32
   quadratic pivot and `ell_1`; only after gradient unimodularity compute
   `mu(P)`.  Splitting gives local branches, not yet a Keller mate.

4. **Factorial origin-rebind gate.**  At each of the four blockers, test
   whether the no-later `F12` reset supplies a lawful new origin for the
   shared pair.  Preserve face ancestry, row choice, active-face change, and
   the reset potential.  Reject any repair that silently interleaves the two
   lanes or turns the response-positive two-pole diagonal into chronology.

5. **Bordered low-rank sequence compiler.**  Add two or more core edges in
   every order, update the resolvent by its endpoint block, and independently
   rebuild the Krylov observer.  Track node births and the zero projection;
   test whether update order commutes after the carrier is enlarged.

6. **Cyclic quartic coamoeba/projective elimination.**  Use the positive
   support of the five THM-3304 moment forms to enumerate only phase chambers
   where cancellation is possible, enforce the `pi/3` diameter barrier, then
   eliminate each projective chart including infinity.  The mod-`103` forms
   are a sidecar, not a characteristic-zero certificate.

7. **Constant-J boundary/multiplicity passport.**  Eliminate same-edge,
   cross-edge, and vertex collisions among the three polynomial edge images,
   while retaining winding cells, holes, cuts, and sheet multiplicity.  In
   parallel, the alternating-sextic lane `V` times a symmetric cubic gives a
   small independent `P2` moment elimination.

The first four tests target exact pre-scalar defects.  The fifth converts a
successful static compatibility law into an efficient closed-form algorithm.
The last two exploit a proved HFC--JC bridge and positive Fourier kernel;
neither is the three-face controller.  No reverse Gaussian-to-factorial
averaging is licensed by the forward embedding.

## Scope boundary

No finite closure above supplies positivity, a shared chronological
interleaving, or a global physical action.  The LRC calculation has no
endpoint current; the factorial calculation has no controller beyond its
declared closure; THM-3306 has no Keller cofactor or inverse cover;
and THM-3305 counts static symmetric relation walks.  THM-3303 proves only a
constant-Jacobian HFC sector implication, while THM-3304 leaves its cyclic
quartic cell open.  The comprehensive gain is a set of smaller, typed
frontier problems whose first decisive tests are now explicit.
