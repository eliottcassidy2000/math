# LRC(14) / JC(2): response rank, cubic first faces, and the transfer firewall

**SESSION SYNTHESIS. Current proved truth is THM-4301--4304 plus the
navigation files that cite them; this reflection records the alternating
research moves and does not enlarge those theorems.**

## Portfolio and inheritance

The anchor alternated after every meaningful exact pull.

- **LRC anchor:** THM-4300's 24 carrier failures at `(210,596)`. Closest
  mechanism: its size-preserving inactive exchange. Hostile: response
  deactivation under endpoint motion. Near miss: the index-297 cover number
  two need not persist for a different consumer. Least-used sidecar: the
  complete response quotient on the exposed bodies.
- **JC anchor:** THM-4299's `D=Lambda=0`, `UZ!=0` cubic corner. Closest
  mechanism: THM-4297's good-differential valuation, rederived on the wall
  from THM-4103/4230 rather than specialized illegally. Hostile: the literal
  cubic with three pairwise order-six branches. Near miss: scalar Morse
  critical value after `q=r-1` ceases to be a unit. Least-used sidecar: the
  `q`-linear response row together with the `q^2` translation row.
- **Wildcard:** test whether the two lanes share an operation, or only a
  controlled-forgetting pattern.

The six-item board was

```text
{(210,596) exposed bodies, inactive exchange, cubic q-face,
 good-form order, source-row cokernel, physical consumer}.
```

## Alternating exact pulls

### Pull A: LRC response rank rises

**THM-4302 / FINITE-EXACT.** The complete rank-eight/rank-nine active response
atlas of the 24 exposed bodies has 718 realized types and 82 maximal types.
A half-integral dual of value `7/2` and a four-mask cover prove exact mixed
minimum four. Restricting to rank eight gives a dual value `11/2` and a
six-mask cover, hence exact minimum six.

This refutes the session's cheapest cross-consumer extrapolation:

```text
index-297 common deck minimum 2  -/->  endpoint-596 carrier minimum 2.
```

The response number is consumer-, pair-, rank-, and predicate-relative.

### Pull B: the cubic has a uniform separable bound

**THM-4301 / PROVED RELATIVE + VERIFIED-EXACT.** For

```text
s=v(sigma), beta=v(z), tau=s+beta, gamma=v(q),
```

the literal corner equation retains both `Uq^3` and `-t^12/2`. If `N` is a
first-face weight and the face component is generically `q`-separable, then

```text
v(F_q)=N-gamma
       <=min(2gamma,12tau-gamma)
       <=8tau.
```

Therefore the good form has order at least

```text
9s+11beta-8(s+beta)=s+3beta>0.
```

Every such component is Keller-constant. A `q`-independent factor after
torus saturation can only come from the binomial coefficient cancellation
`t^ell-z^12`, and its normalization is rational. Only genuine repeated
`q`-factors survive to a later refinement.

The weakest bound is real, not slack: a literal five-term first face at
weights `(1,2,12)` is an irreducible genus-four trigonal curve and has exact
good-form order seven.

### Pull C: carrier size is preserved, but only inside the finite consumer

**THM-4302 / FINITE-EXACT.** Four rank-nine responses close the 24 failures.
Across the complete 363-row endpoint-`>=596` prefix, 75 augmented-carrier
masks are strictly inactive; deleting the first 73 and adding the four
responses restores carrier size `9,019`. The typed row consequence adds nine
rows, giving union `1,633`, residual `21,014`, and maximum endpoint `595` on
28 rows.

This remains a fixed-pool carrier certificate. It supplies no physical
entry, owner trajectory, or arbitrary-pair theorem.

### Pull D: the endpoint-595 layer collapses to three rows

**THM-4303 / FINITE-EXACT.** A raw replay of all `28*C(30,9)=400,600,200`
body-row tests shows that the same 9,019-mask carrier already closes 25 of the
28 endpoint-595 rows. The only carrier-failing rows are

```text
(96,595):116 bodies,  (100,595):13 bodies,  (210,595):16 bodies.
```

An independent literal-wall implementation reproduces the active families,
joint-exposed bodies, and failure identities on every row. The typed union is
therefore `1,658`; the residual is `20,989`, with precisely those three rows
at its top endpoint. This is a stronger row consequence, not evidence that a
common three-row repair or physical entry exists.

### Pull E: repeated cubic first faces have rational reduced carriers

**THM-4304 / PROVED RELATIVE + VERIFIED-EXACT.** For a saturated first face
`f`, `deg_q(f)<=3`. If a `q`-dependent irreducible factor `K` is
derivative-null, characteristic zero forces `K^2|f`, hence `deg_q(K)=1`.
The reduced component is the graph

```text
q=-b(sigma,z)/a(sigma,z)
```

over a weighted projective line and is rational. Exact support analysis shows
that literal multiplicity is always two. Besides three quadratic-section
regimes, the balanced cubic repeats only at

```text
U_+/-=-315392/3645 +/- (217088/18225)*sqrt(265).
```

Weights `(1,3,16)` realize the repeated cubic literally. The first face is
rational and Keller-constant; translating its double section exposes two
prepared `T^1` coordinates whose later discriminant refinements remain open.

## Typed connection ledger

### Internal LRC map

```text
source:      active rank-8/9 masks at the fixed pair (210,596)
target:      subsets of the 24 exposed labelled bodies
map:         mask |-> bodies disjoint from that mask
preserves:   integral body coverage for that fixed activity predicate
destroys:    margins, endpoint history, owner/phase, physical motion
sidecar:     pair grid, rank, active sign, carrier and consumer identity
test:        exact primal cover plus fractional dual on all 718 types
```

### Internal JC map

```text
source:      exact local germ F(sigma,z,q)
target:      weighted initial face and its q-derivative
map:         take the valuation initial form
preserves:   first weight and generic q-separability
destroys:    higher faces and repeated-root splitting data
sidecar:     full depressed-cubic pair, q^2 translation, discriminant
test:        genus-four sharp face and repeated-factor hostile
```

### Cross-lane firewall

There is no target-preserving map between the two ledgers. THM-4300/4302 may
delete a mask only after family-wide inactivity is proved. Off the JC corner,
THM-4299 similarly divides by `r-1` only when it is a unit. At the corner,

```text
q=r-1 is nonunit,                     F mod q=-t^12/2.
```

Division by `q` creates a pole. The cubic deformation quotient
`C[[q]]/(q^2)` has rank two, but its `q^2` translation feeds the prepared
pair nonlinearly. No map from masks/bodies to cubic coefficients preserves
either coverage or Keller constancy. The shared word "response" and the
occasional shared rank are scheduling signals only.

The source-row tangent cokernel is likewise rank one per row, not canonically
the rank-two cubic base. At least two addressed rows and the lower-weight
horizontal sidecars would be needed even to formulate a candidate map.

## Board update and next decisive probes

1. **LRC:** construct pair-tagged response quotients on the three remaining
   rows and their 145 bodies. Test shared additions only with all three
   activity predicates and audit inactivity on the full consumer before any
   exchange. Do not extrapolate the endpoint-596 minimum four.
2. **JC:** translate each exact double section, retain both constant and
   linear prepared `T^1` coordinates plus the discriminant valuation, and
   classify the first refinement splitter. Reduced rationality alone does not
   control exceptional tails.
3. **Source rows:** derive the actual bracket/Hasse equations through
   `G[t^8]`; THM-4298 visibility remains an observer theorem, not row
   existence or seam entry.
4. **Bridge:** keep the negative result. Revisit only if a lawful functor
   preserves a named consumer predicate on both sides; rank equality alone
   is not evidence.

The three LRC top rows and the cubic `q`-degree three are a numerical
coincidence only. Their source, target, predicates, and destroyed information
remain different, and no cross-lane map was produced.

Neither LRC(14) nor JC(2) is solved.
