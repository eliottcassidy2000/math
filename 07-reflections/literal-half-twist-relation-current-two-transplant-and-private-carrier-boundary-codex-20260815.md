# Two exact transplants meet a thirteen-edge carrier only at an obstruction

**Research reflection / provenance and route synthesis, not a truth source.**
The audited exact claims are now in promoted THM-3479 and its deterministic
package.  The private-support graph is inherited from proved THM-3473 and
its frozen FINITE-EXACT incidence sidecar.  Nothing here proves a physical
current, a bispectrum, a scalar-row exclusion, or LRC(14).

## Inheritance pass

The closest proved mechanism is THM-2334: exact relation-address orbit sums
push forward to a finite residue current, and the two-dimensional target
quotient is controlled by 169 coordinate twists.  The canonical hostile is
THM-2334's positive-factor example whose six nonzero terms cancel in one
exact address orbit.  Term support is therefore not grouped noncancellation.

The corrected near miss is MISTAKE-404.  A literal half twist already occurs
at the fixed common centre `1/(2q)`; forgetting that centre in a compressed
rank word does not show that no common time exists.  MISTAKE-406 separately
forbids promoting one literal-layer rank to the full zero-cochain rank without
an explicit transport.

The least-used relevant sidecar is THM-3429's q=51 affine lift character.
Owners `1` and `35` agree modulo `34` but differ in `k mod 3`, and their active
sections separate off the fixed fibre.  THM-3479 preserves this datum rather
than flattening it into quotient order.

## Concept board

| object / representation | live predicate | retained invariant | operation | lost coordinate | cheapest test |
|---|---|---|---|---|---|
| `U_full` relation tuple | all unrestricted target aggregates nonzero | full 169 twist/target banks | inverse `F_13^2` DFT | same-clock centre | test the centre/delayed word on this exact tuple |
| `U_clock` relation tuple | named target `A(e_c2)` nonzero | centre `1/22`, margins, two twist values | complete its twist bank | uncomputed 167 twists | compute one exact inverse-DFT target numerator |
| grouped address `C(a;X,m)` | one orbit coefficient nonzero | explicit atomic term at `(13,1,742599)` | sum the full gauge orbit | phase cancellation | exact grouped coefficient at the displayed address |
| q=51 lift | preserve global literal cover | `k mod 3` affine character | CRT lift/descent | off-fibre sheet geometry | owners `1` versus `35` on `y=0,1,2` |
| private-support graph | weighted determinant or nonzero cycle flux | two `K4`s plus bridge, `7x13` incidence | assign current weights | relation/owner dictionary and phase | bridge weight, then both 16-term tree sums |

The two-transplant result changes the board in one important way.  Relation
compatibility, literal masks, positive delayed mass, and endpoint
nonconstancy are no longer separately speculative.  They coexist in explicit
objects.  What remains is a same-object quantifier: `U_full` carries the full
spectrum; `U_clock` carries the physical same-clock witness.

## The positive two-transplant signal

Both tuples satisfy the same primitive exact relation

```text
(-27,-27,-27,20110798,-41,-27,-27,-27,38).U=0.
```

Both realize exact q=11 literal covers and positive delayed intersections.
For `U_full`, all 168 nontrivial endpoint twists differ from the zero twist,
and every one of the 169 unrestricted target aggregates survives.  For
`U_clock`, source and delayed patterns occur at the same centre `1/22`, with
strict positive margins; two independent finite-field embeddings prove that
its endpoint bank is nonconstant, and one exact all-septimal-unit atomic term
has the displayed relation address.

This rules out a broad class of pairwise incompatibility explanations.  The
current obstruction is not “literal covers cannot carry relation data” or
“same clocks force constant endpoints.”  It is the conjunction on one tuple,
followed by grouped cancellation and the all-91-unit projector.

## Attempted map to the private-support carrier

The incoming graph invited a seductive `13=13` identification.  Applying the
connection contract prevents that coincidence from becoming a false bridge.

| field | typed answer |
|---|---|
| source | nine-coordinate U_full/U_clock relation tuples with 169 coordinate twists indexed by `F_13^2` |
| target | thirteen coactivity edges among eight private-support owners |
| candidate map | select a translation-stable affine character line of size thirteen and biject it with the edge set |
| predicate intended to survive | endpoint nonvanishing should become nonzero bridge weight and nonzero weighted tree polynomial in each tetrahedron |
| information destroyed | exact address, q=11 mask, clock, endpoint phase, private-sheet counts, and q=51 `k mod 3` state |
| needed sidecar | explicit relation-coordinate to owner/edge labels, plus a phase/holonomy assignment |
| decisive hostiles | source `C13` translation, unique bridge `(u5,u7)`, and the two sixteen-term `K4` tree sums |

The exact automorphism computation gives

```text
|Aut(G)|=72,
orders={1,2,3,4,6},
edge orbits=1+6+6.
```

A native affine character line has a regular translation of order thirteen.
If a bijection were equivariant, conjugation would produce a graph
automorphism of order thirteen.  None exists.  Thus the natural
`C13`-equivariant map is impossible.

This is a sharper obstruction than a dimension mismatch.  It identifies the
first failed implication:

```text
same cardinality
  does not imply
symmetry-compatible labelled carrier.
```

An explicitly labelled non-equivariant map is not ruled out.  It would be new
data, not a canonical consequence of the two existing objects.  Moreover the
static owner-potential edge currents are graph coboundaries, so even such a
labelling still needs phase/holonomy to reach nonzero cycle flux.

## Preserved and destroyed information

The relation-current reduction preserves the exact relation lattice, target
quotient, endpoint factors, and character phases.  It forgets private-support
owner colours and coactivity packets.  The private-support two-section
preserves pairwise coactivity existence.  It forgets sheet locations, counts,
singleton atoms, and the ternary `k mod 3` state.

Their intersection currently contains only abstract finite indexing and the
integer thirteen.  The intersection does not contain a label map.  The
required sidecar has two logically separate jobs:

1. identify which current coefficient owns each graph edge; and
2. retain a phase around graph cycles so the resulting edge response is not a
   coboundary.

The unique bridge edge is the first hostile for job one.  The six cycle
pairings and two weighted tree polynomials are the first hostiles for job two.

## Cheapest next computation

The cheapest decisive continuation is the named U_clock target, not the
all-91-unit projector and not an invented carrier labelling.  Compute the
complete U_clock 169-twist bank only far enough to form the exact inverse-DFT
numerator for `A(e_c2)` under both certified embeddings.

- If both images are nonzero, the same tuple gains a named unrestricted
  target while retaining the common-centre delayed word.
- If one image is zero, that image is inconclusive for exact vanishing and a
  second prime or exact cyclotomic reduction is required.
- If the exact target vanishes, retain the minimal witness and inspect which
  twist orbit cancels; do not replace the target by U_full's result.

Only after a lawful owner/edge dictionary arrives should the carrier test be
run.  Then evaluate, in order:

```text
weight(u5,u7),
Tau_K4(first six weights),
Tau_K4(second six weights).
```

All three must be nonzero for the weighted reduced determinant to survive.
Nonzero individual edge weights are not enough.

## Honest frontier

The package closes an explicit relation-residue existence and endpoint
nonconstancy problem on two complementary tuples.  It does not close the
grouped address coefficient, the U_clock named target, the all-unit CRT
projector, ancestry, the THM-2512 Boolean-current typing, or the spectral
closure.  The 7 by 13 carrier remains useful precisely because the failed map
is now typed: label and holonomy, not another rank count, are missing.

LRC(14) remains OPEN.
