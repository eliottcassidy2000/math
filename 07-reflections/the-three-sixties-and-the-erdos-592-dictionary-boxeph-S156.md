# The Three Sixties (two are one theorem) and the Erdos-592 <-> Kernel Dictionary

**boxeph-2026-07-20-S156 (HYP-8255).**

## 1. Three sixties, resolved (T1534 attack step executed)

The owner's key: 60 = the period of Fibonacci final digits base 10 = pi(10).
Mechanism (verified exactly, script S156): pi(m) = ord of M = [[1,1],[1,0]] in
SL2(Z/m); pi(10) = lcm(pi(2), pi(5)) = lcm(3, 20). The 20 is STRUCTURAL:
x^2 - x - 1 ramifies mod 5 (disc = 5; sqrt5 = 0, phi = 3 double eigenvalue), so
M mod 5 lies in a BOREL of SL2(F5) and pi(5) = |unipotent| x |semisimple| =
5 x 4 = 20, a Borel order. And A5 = PSL(2,5) — the icosahedral group IS the
golden-prime group. So pi(10) = 60 and |A5| = 60 are two faces of ONE
PSL(2,5)/golden-ratio theorem. The third sixty, ord_1001(2) = lcm(3,10,12)
(1001 = 7*11*13 divides 2^60 - 1), shares only the lcm-target 2^2*3*5:
"forced equality" REFUTED for it; 60's role there is divisor-richness
(the lcm attractor). Verdict: 2/3 theorem, 1/3 numerology — knowing WHICH
third is which is the content.

## 2. Erdos 592 <-> the kernel's three parts

Erdos #592 asks alpha -> (alpha, 3)^2: every pair-coloring yields a full-size
ordered copy (alpha) or a 3-set. The kernel K's anatomy has exactly this
dichotomy shape, at three levels we have already measured:
- THE LITERAL 3-SET: K's defining witness is the 3-collision
  {(0,0,-1/4), (1,-3/2,13/2), (-1,3/2,13/2)} — the "triangle" found when
  automorphy (the alpha side: an order-isomorphic copy of C^3) fails.
- THE THREE CHEBOTAREV PARTS: fibers split by monodromy class — identity
  (density 1/6, all 3 preimages rational: the visible triangle),
  transposition (1/2, one preimage), 3-cycle (1/3, zero preimages). The mod-p
  decision theorem IS the partition-calculus dichotomy in arithmetic form:
  bijective mod p for large p (alpha side) XOR positive-density collision/
  deficiency classes (3-set side) — Jordan supplies the fixed-point-free
  element exactly as Ramsey supplies the homogeneous set.
- KERNEL / ORBIT / HALO (S141 anatomy): the rigid classified core, its
  equivariant orbit, and the halo — the three "parts" of counterexample
  space; R(n,2) = 2n+1 (the repo's observer ray, T831/HYP-2396) is the tame
  axis on which Erdos 592's finite shadow sits, and R(3,2) = 7 lands on the
  forbidden h-value — logged as resonance, not claim.
DICTIONARY CLAIM (test next): the Chebotarev census (1/6, 1/2, 1/3) plays the
role of the positive-density color class; a partition-calculus proof pattern
(pigeonhole on ordered blocks) should reprove the mod-p decision theorem's
deficiency lower bound without Lang-Weil for GALOIS covers — a concrete,
checkable bridge. Effort M; filed.
