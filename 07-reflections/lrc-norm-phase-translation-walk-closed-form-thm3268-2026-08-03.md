# The norm-phase translation census is a two-mode walk

**Status: PROVED + VERIFIED-EXACT + INDEPENDENTLY HOSTILE-AUDITED; promoted as
THM-3268.** This note supplies no physical LRC current, row exclusion, safety
certificate, or `LRC(14)` decrement.

## Inheritance pass and bounded question

The closest proved mechanism is **THM-3267, norm-phase factorization ladder
and projective-determinant blindness**. In its chosen model

```text
F_169=F_13[u]/(u^2-2),  alpha=1+2u,
phi(alpha^j)=j mod 12,
```

the aggregate over all nonzero translations has `2,184` phase-preserving
transitions and `2,352` transitions for each fixed nonzero phase difference.
The nearest representation-theoretic sidecar is **THM-3255, twelve-balance
multiplicative Singer rank defect and phase-marker boundary**: its missing
phase sector is the eleven-dimensional augmentation representation of
`C_12`, generated by the centered markers `12e_r-1`.

The hostile example is already latent in THM-3267: phase is not invariant
under even one affine translation. The bounded question here is different.
If the nonzero translation may be chosen afresh at every step, can all
length-`n` phase-difference counts be computed without enumerating
exponentially many paths?

## Exact walk and quotient

Let

```text
X=F_169^*,   |X|=168,
```

An allowed step chooses `x,t,x+t` all nonzero and sets `y=x+t`. Equivalently,
for nonzero `x,y`, there is an edge `x -> y` exactly when `x!=y`; its
translation label is uniquely `t=y-x`. The translation graph is therefore
the loopless complete digraph on 168 points, with outdegree 167.

For `n>=0` and `d in C_12`, define the indexed multiplicity

```text
C_n(d)=#{(x_0,...,x_n) in X^(n+1):
          x_(i+1)!=x_i for every i,
          phi(x_n)-phi(x_0)=d}.                         (1)
```

Every phase fibre has fourteen points. From any fixed point there are
thirteen targets in its own phase and fourteen in each other phase. Hence
the phase partition is equitable, with quotient

```text
Q=14 J_12-I_12,                                        (2)
```

where `J_12` is the `12 by 12` all-ones matrix.

The constant line has eigenvalue `14*12-1=167`; the eleven-dimensional
augmentation space has eigenvalue `-1`. Thus

```text
Q^n = (167^n/12) J_12 + (-1)^n (I_12-J_12/12).         (3)
```

Multiplying the appropriate entry by all 168 possible starting points gives

```text
C_n(0)=14(167^n+11(-1)^n),                             (4)
C_n(d)=14(167^n-(-1)^n),       d!=0.                   (5)
```

The total is `168*167^n`, as it must be. More sharply,

```text
C_n(0)-C_n(d)=168(-1)^n.                               (6)
```

The counts alternate on opposite sides of exact phase uniformity with a
constant absolute gap, although their total grows exponentially.

## Recurrence, generating functions, and first terms

Both profiles have characteristic roots `167,-1`, hence satisfy

```text
C_(n+2)=166 C_(n+1)+167 C_n.                           (7)
```

Their ordinary generating functions are

```text
sum_(n>=0) C_n(0) z^n
  =168(1-153z)/((1-167z)(1+z)),                        (8)

sum_(n>=0) C_n(d) z^n
  =2352z/((1-167z)(1+z)),       d!=0.                  (9)
```

The first independently enumerated terms are

```text
C_n(0):
168, 2184, 390600, 65204328, 10889148648, 1818487798344;

C_n(d), d!=0:
0, 2352, 390432, 65204496, 10889148480, 1818487798512.
```

The `n=1` entries reproduce THM-3267's frozen `2,184/2,352` census. The
companion also performs a point-level dynamic program through `n=5`: it
retains the initial phase and traverses every one of the 168 point vertices,
without using the quotient matrix in the update. All twelve difference
profiles match (4)--(5). The closed forms compute the same exponentially
large path sequence with one integer exponentiation, in `O(log n)` integer
multiplications by repeated squaring.

## Uniform finite-field generalization

The mechanism does not depend on the special polynomial `u^2-2`. For every
prime power `q`, the norm map

```text
Norm:F_(q^2)^* -> F_q^*
```

is onto and each fibre has size `q+1`. The same nonzero-translation graph is
the loopless complete graph on `q^2-1` points. Its norm-phase quotient is

```text
Q_q=(q+1)J_(q-1)-I_(q-1),                              (10)
```

with eigenvalues `q^2-2` on constants and `-1` on the augmentation space of
dimension `q-2`. If `C_(q,n)(eta)` counts paths whose terminal/source norm
ratio is `eta in F_q^*`, then

```text
C_(q,n)(1)
 =(q+1)((q^2-2)^n+(q-2)(-1)^n),                       (11)

C_(q,n)(eta)
 =(q+1)((q^2-2)^n-(-1)^n),       eta!=1.              (12)
```

For `q=2`, (12) is vacuous. Both sequences obey

```text
C_(n+2)=(q^2-3)C_(n+1)+(q^2-2)C_n.                    (13)
```

This is a proof for every prime power, not an interpolation claim: norm
surjectivity gives the equal fibres, and the loopless complete graph gives
(10). The companion checks the initial values, totals, and recurrence for
all integer parameters `2<=q<=30` as hostile algebraic controls.

## Why the rank-eleven mode appears

For the THM-3255 centered phase marker

```text
M_r=12e_r-1,
```

equation (2) gives

```text
Q M_r=-M_r.                                             (14)
```

The twelve markers span the same eleven-dimensional augmentation sector
that THM-3255 identifies as the missing multiplicative phase coordinate.
After normalizing the walk by its outdegree, that entire sector has
multiplier

```text
-1/167                                                   (15)
```

per free-translation step. This explains the exact alternating error in
(6). It does not construct the missing marker physically: it says how a
marker transforms after averaging over every admissible next translation.

The connection contract is therefore

```text
source:      nonzero field point with freely chosen nonzero increment;
quotient:    retain only the C_12 norm phase;
preserved:   endpoint phase-difference path multiplicities;
destroyed:   point, increment word, intermediate geometry and ancestry;
sidecar:     the full point/increment path for deterministic or physical use;
hostile:     repeated use of one fixed translation.                   (16)
```

## Fixed-translation hostile

The one-step histogram for the fixed translation `t=(1,0)` is also

```text
(13,14,14,...,14),                                      (17)
```

but this numerical match does not authorize iteration by `Q`. Reuse that
same `t` and require every intermediate point to remain nonzero. Exact
enumeration gives

```text
n=2:  (12,14,14,...,14),       only 166 valid paths;
n=13: (156,0,0,...,0),         only 156 valid paths.    (18)
```

At step thirteen, characteristic 13 returns every surviving point to itself;
the twelve nonzero points on the `F_13 t` line have encountered zero, while
the other 156 points survive. Thus the closed form counts a walk whose
translation may vary at each step. It does not count powers of one affine
translation, a constrained `H_13` word, or a path on one literal ancestry
sheet.

This hostile is the sharp boundary: a one-step aggregate can be an equitable
quotient for a free walk and still be false as a deterministic transition
law.

## Reproduction and scope

Run

```text
python3 04-computation/lrc_norm_phase_translation_walk_closed_form_thm3268.py
python3 -O 04-computation/lrc_norm_phase_translation_walk_closed_form_thm3268.py
```

and compare LF-normalized bytes with

```text
05-knowledge/results/lrc_norm_phase_translation_walk_closed_form_thm3268.out.
```

The normal and optimized executions byte-match. The companion contains no
`assert` statement, floating literal, randomness, or imported candidate
computation. It independently reconstructs the field and point graph while
hash-pinning THM-3255, THM-3267, and THM-3267's exact script/output.

```text
script SHA-256:
fd26af6cba1cad5019556fd2235b319f7d089273b64d7f0dc01d2877ca0a4985

output SHA-256:
a9d8f74c6f9a101b81fd1c6a652c8f9379d945d2883c502452252fdbfc1fe844
```

The result is an exact sequence theorem and representation-theoretic
explanation of THM-3267's translation census. It does not realize the norm
phase on THM-2791's physical rail sheet, preserve a deterministic affine
action, construct an endpoint current, exclude an LRC row, or prove any part
of `LRC(14)`.
