# Fixed-first-newcomer activation and source-before-projection

**Session synthesis / provenance, 2026-08-26.** This is not a proof source.
Current claims are owned by THM-4211 and THM-4209; their theorem files and
frozen artifacts outrank this reflection.

## Portfolio outcome

| lane | inherited mechanism | hostile inherited or found | outcome |
|---|---|---|---|
| Anchor: LRC(14) | THM-4191 one-newcomer repair decks; THM-4170 component discrepancy; THM-4207 fixed `(50,51)` joint deck | depth-eight strict reserve has a nine-cover whose body still has positive limiting margin | **PROVED RELATIVE / VERIFIED-EXACT:** THM-4211 gives every full-pool nine-body with `{50,r}` for `r>=5682`, every nine-body in an 18-label chart for every outsider, and the fixed `(49,50)` checkpoint |
| Niche: planar Jacobian | THM-4147 packet/carrier response; THM-4192 rational source pair and degree-drop firewall | projected critical roots may collide; anti tangent repeats at `Theta=-Delta` | **PROVED RELATIVE / VERIFIED-EXACT:** THM-4209 removes the critical-open hypothesis on the mixed `K=0` face except the repeated-tangent anti wall |
| Wildcard: moving-pool entry | conditional failure atoms under one-label deletion | total mass averaging forgets which deleted label owns the reserve | **PROVED ANALYTIC IDENTITY / OPEN APPLICATION:** the deletion sunflower is exactly empty atom plus one labelled singleton petal; arbitrary entry is still open |

LRC(14), JC(2), and DC(2) remain open.

## What actually moved

### 1. Fixed-first-newcomer activation

The useful discarded object was not another literal two-newcomer wall. It
was the entire one-newcomer safe set before the second comb was imposed. For
each deletion `R`, two integers survive:

```text
base mass m_R,        component count c_R.
```

THM-4170 turns these into a lawful cofinal activation time. The full
depth-nine activation deck changes transversal status between `5681` and
`5682`. This proves a uniform tail but also exposes its own quotient loss:
the activating edge is already literally lawful at `5681`. Component count
forgets the component addresses and their phase alignment with the incoming
comb.

That hostile determines the next operation. Do not merely optimize the
discrepancy constant. Refine only the few edges and bodies controlling the
transversal transition with phase-resolved component data.

### 2. Restricted chart before arbitrary entry

The full thirty-label pool is too ambitious for every second newcomer, while
one fixed body is too narrow to expose structure. Optimizing the intermediate
object produced the chart

```text
{8,16,40,42,80,84,85,88,95,120,126,143,145,168,193,240,252,286}.
```

Every one of its `48,620` nine-bodies survives every outsider. This is a
lower bound `chi_50>=18`, not a maximality result. It is useful precisely
because it makes chart growth a finite hostile-driven optimization problem:
an attempted nineteenth label fails on identifiable bodies and outsider
rows, rather than on an opaque global statistic.

### 3. Source ideal before critical projection

On the Jacobian side, the analogous discarded object was the two-generator
source critical ideal. Projecting it to one resultant coordinate introduced
a squarefreeness hypothesis that Keller-Morse reducedness never needed. The
pair `(A,B)`, together with

```text
p det D(A,B)=2t^2 det Hess(G) mod (A,B),
```

counts the complete reduced critical scheme even when projected roots
collide. Endpoint units then give lengths `25` and `23` across whole
coefficient strata. The anti wall `Theta=-Delta` is not a projection
artifact: its tangent cone genuinely repeats, and the residual degrees fall
to `17/15`. That is a geometric boundary which must be normalized anew.

## Connection contracts

### Failure atoms to one-label entry

```text
source:       phases safe for both outsiders, labelled by failed chart subset
target:       the m one-label deletions of an m-body
map:          failure subset F -> every deletion whose retained labels avoid F
preserved:    outsider safety, exact Haar mass, deleted-label identity
destroyed:    phase address inside each atom
sidecar:      empty and singleton atom masses
decisive test: w(empty)+max_p w({p})>=4/63.
```

The exact sunflower formula shows why an average can fail: it scalarizes the
label which owns the singleton petal. The next entry theorem must either
lower-bound the maximum singleton mass or produce a body-dependent winning
label.

### One-newcomer base to cofinal pair

```text
source:       U_R=G_((P union {50})\R)
target:       lawful joint repair for all r above a cutoff
map:          U_R -> (mass,components) -> activation time
preserved:    deletion label, threshold, base reserve
destroyed:    component addresses and exact comb alignment
sidecar:      literal boundary mass for controlling edges
decisive test: transversal number of the activated deck.
```

### Projected resultant back to the Jacobian source

```text
source:       polynomial critical pair (A,B) on pt!=0
target:       affine critical length and monodromy response budget
map:          source ideal -> saturated resultant -> restored universal fibres
preserved:    intersection multiplicity, Keller-Morse reducedness, residue row
destroyed:    s-coordinate after elimination
sidecar:      source-infinity rows, p=0/t=0 exclusions, four universal points
decisive test: endpoint units plus the Hessian bridge.
```

There is a methodological analogy between the first and third contracts:
both recover labelled data before a lossy scalar projection. There is no
claimed theorem transferring LRC atoms to Jacobian monodromy.

## Next sharp experiments

1. **Literal cofinal threshold (Anchor, OPEN).** Starting from the `5681`
   activation cover, build the literal E9 deck only on complements of the
   surviving covers. Descend in `r`, retaining component addresses. The first
   goal is an exact smaller upper bound, not an assertion of monotonicity in
   the newcomer label.
2. **Chart growth (Anchor/Niche, OPEN).** Add each of the twelve omitted pool
   labels to the 18-chart, enumerate only newly created nine-bodies, and record
   the minimal hostile `(body,r)`. Use those hostiles in a branch-and-bound
   search for `chi_50>=19` or an exact maximality certificate.
3. **Singleton-reserve entry (Wildcard, OPEN).** On the canonical hard
   ten-bodies, compute the conditional empty and singleton atom vector for
   `(q,r)`, rather than ten separate masses. Test whether component
   discrepancy can lower-bound `max_p w({p})` uniformly in one outsider.
4. **Anti repeated tangent (Niche, OPEN).** At `Theta=-Delta`, separately
   normalize the `Phi!=0` degree-17 row and the terminal degree-15 row. Derive
   the new local branches and differential orders before using either degree
   in a response budget.
5. **Other mixed critical walls (Niche, OPEN).** Re-run the source-ideal
   endpoint method on the remaining THM-4147 coefficient walls. A positive
   signal requires unit endpoints and a complete boundary packet; a projected
   resultant degree alone is not enough.

No new META-PATTERNS card is promoted here. The successful move is another
instance of the existing “certify before projection / retain the missing
sidecar” pattern; the new evidence strengthens that card but does not yet add
a distinct trigger or counterindication.
