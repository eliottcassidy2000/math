# LRC(14): component erosion, divisor combs, and the next owner-word frontier

**Session status:** LRC(14) remains **OPEN**. This reflection routes the proved
THM-4003/4004 packet, the finite exact controls, the negative method results,
and the next procedurally generated tasks. Canonical truth is in the theorem
files, not in this narrative.

## Inheritance pass

- **Closest proved mechanism:** THM-3995's four correctly oriented parity
  collars inside the two components of the scale-two `(2,1,9)` obstruction.
- **Canonical hostile:** AP11 and the signed-phase collisions of THM-4002;
  for raw scale-one endpoint erosion, the common congruence-alignment family
  with `t=91^6+6` makes every forced gap vanish.
- **Corrected near miss:** total support and scalar variance do not retain
  which obstruction component carries a connected body image. Pairwise
  tournament/Gram data similarly forget cross-phase.
- **Least-used sidecar:** THM-3818's same-component pair-height bound
  `height<=91^6`. It had been used as a scale decoder/crossing ban, not as a
  quantitative separation law between the two largest body speeds.

## Concept board

| live concept | retained coordinate | strongest interaction this session | present loss |
|---|---|---|---|
| body-safe component `J` | connected source, two boundary owners | two-flank and top-balance lower bounds | full endpoint word |
| sheet count `N_t` | integer occupancy and component support | each image of `J` must fit one trimmed core | selected sheet/arrival |
| four directed event gaps | endpoint, side, orientation, multiplier | 77 exact cells after two-flank erosion | joint owner realizability |
| divisor branch `a mod d` | common lift label for all detuned owners | prime incidence profile | which pack phase to choose |
| decoder graph | connected labelled ratio edges, pair height | separates top two body scales | circular wall order |
| signed cross-phase | complete relative endpoint phase | fixed `[1,21]` closure in THM-4002 | arbitrary-height uniformity |

Every meaningful pull changed at least two lanes. The two-flank lemma made the
directed residues stronger; the divisor profile now constrains which endpoint
owners may coexist; the decoder pair-height cap strengthened a metric body
component estimate; incoming cross-phase work clarified exactly what the
component quotient still destroys.

## Anchor: arbitrary-body scale-two response

### 1. Total support was the wrong granularity

THM-3995 bounds total positive support by `4(U-1)/(63U)`, but it actually
proves two separate closed cores, each of width

```text
2(U-1)/(63U).
```

A cited deep body interval maps to one connected arc. Comparing that demand
with one component's capacity, rather than the sum of both capacities, first
gave `3t<=4(U-1)`.

### 2. The second boundary has a price

At a `1/12`-deep body time, each direction remains safe for at least
`1/(84w)` when owner `w` supplies the first wall. The two wall owners either
differ or coincide.

- If they differ, integer distinctness supplies denominators `U,U-1`.
- In the exact `W=V_dec` branch, the pair-height cap improves `U-1` to the
  projective separation `U/V>=Q/(Q-1)` once `U>Q=91^6`.
- If they coincide, the body component is a whole owner-safe tooth of length
  `6/(7w)`, much larger than either lower bound.

Thus, with `H=min(U,Q)`,

```text
U lambda_+(u)>=1/42+1/(84(H-1)).
```

This forgotten pair-height coordinate converts directly into the necessary
scale-two gate

```text
3t(2H-1)<=8(H-1)(U-1).
```

For `U<=Q`, odd `t` gives the exact threshold

```text
U>=floor(3t/4)+2+1_(t=1 mod 4).
```

The extra `t=1 mod 4` layer is an infinite symbolic gain, not a finite-search
pattern.

### 3. Top balance retains both pieces after the maximum tooth

Removing the maximum body speed leaves ten speeds. Their cited `1/11` time
has a `1/14`-safe interval of length `3/(77V)`, where `V` is the second
largest speed. In `U`-scaled coordinates, remove at most one maximum-owner
danger tooth and retain the longer of the two pieces. For `R=U/V<22`,

```text
U lambda_+(u)>=max(0,(3R-11)/154),
```

and for `R>=22` the bound is `5/14`. This is stronger for the component
consumer than passing through THM-1002's scalar margin. It closes every one
of the 17 conditional response types when `R>=11`, and scale two already at
`R>=1001/189`. Equality closes because THM-3910's compact-to-open failure
inequality is strict.

### 4. Exact event residues and genuine synergy

For one hypothetical owner `u`, `d=gcd(t,u)`, the four correctly oriented
inward distances are the least positive residues of

```text
3t-4u mod 42d,       9t+16u mod 126d,
9t-110u mod 126d,   3t+38u mod 42d,
```

divided by `42u,126u,126u,42u`. Reflection makes the outer pair equal and
the inner pair equal. Literal Fraction grids and an independent integer-wall
path confirm every sign and modulus.

On the 62,989 old-strip `(t,U)` cells through `t<=1001`, the new body width
closes 742 symbolically and exact residues close 77 more. Nineteen are
genuine composition rows: neither the older residue gate nor the two-flank
gate closes them alone. This is the clearest procedural signal from the
session: after two partial ideas are typed by the same source interval and
target component, recompute the inequality rather than unioning their output
sets.

## Niche: the untouched `t<U` branch

### 1. Literal role swapping is dead

The maximum body owner has safe teeth of length `6/(7U)`, so

```text
lambda(u)<=6/(7U),
t<U ==> t lambda(u)<6/7<1.
```

The symmetric one-component grid criterion can never fire. This is a
stopping theorem for one operation, not evidence that a shifted `t`-grid
misses the union of body-safe components.

### 2. Divisor combs retain a common branch

If `d|t` divides eight body coordinates, those eight plus the two pair owners
form a ten-speed harmonic pack after division. At a cited `1/11` phase, the
three remaining owners are danger combs on the same branch label `a mod d`.
For actual detuned speed `delta`, `g=gcd(d,delta)`, `m=d/g`, its exact bad
branch count is

```text
b_d(delta)=g ceil(m/7),
```

with the same value `g(m/7)` when `7|m` because the danger arc is open. The
strict union gate `sum b_d<d` gives the prime profile

```text
ell>=5: at most 7 divisible body coordinates;
ell=3:  at most 8;
ell=2:  at most 9, and equality needs scale one plus
        reduced odd exception sum >7.
```

The `ell=3` control saturates all three selected branches at `y=1/11`, but
the row itself wins at `4/33`. The object defeated is the phase-free selector,
not LRC(14).

### 3. Crossing height starts the branch far out

The primitive relation between `s u_i` and `tp` is a crossing support-two
row. THM-3818 forbids height at most `Q`; with `t<U`, `p<=177`, `s<=2`, this
forces

```text
U>Q/177,
U>=3,208,300,859.
```

Small branch controls therefore demonstrate mechanisms only; they cannot be
mistaken for realizations of the inherited high-height residual.

## Wildcard: decoder and tournament archaeology

The ratio-labelled decoder graph is connected on the eleven body vertices,
but exact controls show that graph density is not monotone for the needed
circular geometry.

- Among all 31,434 primitive connected eleven-subsets of `[1,18]`, the unique
  minimizer of `U lambda_+` is
  `(1,2,3,4,8,9,10,11,12,13,14)`, with `U lambda_+=7/110`.
- A connected exact hostile has `r_+=432>U=355`; hence no graph-only
  inference `r_+<=U` is available.
- More decoder edges can coexist with shorter safe components and more wall
  components.

Earlier tournament work therefore returns as a typing rule, not a black-box
certificate. The intrinsic directed objects here are first-enter/last-exit
events and their owner-transfer order. Divisibility is a labelled hypergraph,
and decoder ratios form an undirected labelled graph. Forcing runners into a
cosmetic tournament would erase ties, common branch labels, and the circular
wall word. If Tournament Analysis is retried, use proof obligations or
oriented wall events as vertices and declare the observable relation first.

## Typed connection ledger

### Component demand to component capacity

```text
source:     one closed body-safe component
target:     one THM-3995 retained obstruction core
map:        y -> ty
preserved:  connectedness, exact length, multiplier
destroyed:  selected sheet and endpoint owner
sidecar:    two boundary owners / pair-height cap
test:       compare t L_(U,Q) with the exact directed-residue core
```

### Divisor pack to simultaneous arrival

```text
source:     one cited harmonic-pack phase
target:     a full-row safe lift
map:        y -> {(y+a)/d : a mod d}
preserved:  every pack clearance and common branch label
destroyed:  freedom to choose a different pack phase
sidecar:    three owner-specific danger combs
test:       strict union count; then classify equality at ell=2,3
```

### Decoder graph to body component

```text
source:     connected ratio-labelled body decoder
target:     a quantitative body-safe component
map:        largest pair -> gcd/reduced-height separation -> two flanks
preserved:  top two owner labels and Q-height
destroyed:  remaining circular wall word
sidecar:    endpoint owner sets
test:       joint decoder-edge / wall-word feasibility
```

### Incoming signed phase to trimmed cores

```text
source:     THM-4002 signed endpoint word
target:     noncontainment in the scale-two obstruction
map:        exact cross-covariance / sheet occupancy
preserved:  relative endpoint phase
destroyed:  nothing at the endpoint-word level; arbitrary-height control absent
sidecar:    THM-4003 parity-trimmed core rather than untrimmed C
test:       recompute the Cauchy/integer invoice on K_1 union K_2
```

## Corrections and hostile boundaries

- The phrase “`(3,7)` is the minimal two-lift hostile” was false. `(3,5)` at
  `3/16` is already hostile. The correct theorem is the reduced-sum `>7`
  survivor condition.
- Scale-one endpoint incidences were already present in the earlier repair
  reflection; only the least-positive gap/tariff audit was new. A common CRT
  alignment makes the uniform shrink exactly zero on all sixteen types.
- Even `t` makes the scale-two outer residue vanish at `(t,u)=(12,9)`; oddness
  is essential.
- Equality in component containment is never silently discarded: source
  arcs and trimmed cores are closed, while the failure obstruction is open.

## Procedurally generated next tasks

1. **Joint endpoint-owner optimization (highest value).** Replace four
   independent minima over `1<=u<=U` by an eleven-owner set satisfying
   primitivity, pair-height `<=Q`, decoder connectedness, and THM-4004's
   deletion-prime profile. Decide whether the 62,170 owner-relaxed cells
   shrink symbolically.
2. **Prime-three equality classification.** At `ell=3`, characterize exactly
   when three exceptional combs cover all branches for every cited ten-pack
   phase. Add difference-speed constraints only if their runner budget is
   legal.
3. **Prime-two multi-component selector.** Retain the reduced odd exception
   pair, all body-safe components, and the branch parity. The scalar
   `lambda` route is already refuted.
4. **Trimmed-core cross-phase.** Feed `K_1 union K_2`, not `C`, into
   THM-4002's signed endpoint and integer-occupancy formulas. Test the 19
   synergy cells first as positive controls.
5. **Decoder-edge / circular-word compatibility.** Enumerate small exact
   bodies with both representations and learn necessary local transitions;
   do not infer monotonicity from edge count.
6. **Top-balanced residual.** After `U/V<11` (or `<1001/189` in scale two),
   combine the Q-height separation with finite ratio intervals and endpoint
   owners. This is narrower than the classic `U<=13V` peeler residual.
7. **Natural-number scheduling without quotient loss.** Serialize each typed
   task by a triangular pairing of `(lane,t,U)` and then owner/residue tuples;
   regard the resulting ordinal as a work-queue rank only. As with reading the
   nth odd square as `n`, the encoding may overlap across representations, but
   it must retain a side table for lane, owners, symmetry gauge and destroyed
   coordinates. The natural number is an address, not the mathematical object.

The promising meta-move is “compare connected demand with each target
component before summing capacities.” It is not promoted to
`META-PATTERNS.md` yet: this session supplies strong LRC evidence but not the
required distinct-thread evidence.
