# The recovered `r=5` theta contraction is real, but it is only one linked-node candidate realization

**Status: VERIFIED-EXACT INDEPENDENT RECOVERY/HOSTILE AUDIT of PROVED
THM-2594.**  The exact contraction survives.  The strongest honest reading is
one realized nonzero auxiliary contraction on one canonical typed row and one
finite linked-node ancestry base.  It is not a generic THM-2512 bridge, not a
THM-2449 one-point response table, not a physical current, and not an LRC(14)
row exclusion.

## 1. Recovery and correction lineage

The two files named in the task are present.  Their current parent-worktree
bytes use CRLF, while the isolated-worktree copies use LF; after normalizing
line endings they agree exactly with current `origin/main`:

```text
primary script  09c43af0a0a56c7a0833bbfd13ed6a96bc5a7a3718aa1bc6b77a144bde101a06
primary output  bef4ee9a18ff3e2f455bad66a95252dd9989b2f60953e26e8ea0c2dc6ae7f5df
```

The earlier task notification named different LF hashes.  Those hashes are
not missing; they identify the immutable pre-repair snapshot at commit
`c04aca388`:

```text
historical script aa03adf52f72379ede708e2040925b088548a7cc05fa999e54078e1a8e06e9e1
historical output 6cf260afdfc41e0e560adf294b2cf31b50148a67bbb4b4085195bce9c43dd149
```

Both historical normal/optimized runs reproduce that historical output after
masking elapsed fields.  Its exact nonvanishing arithmetic is sound, but its
claim that the signal is specifically carried by affine slaving is
superseded by MISTAKE-295.  Commit `1f140d4a5` added the genuine
fixed-absolute-root hostile and repaired the wording.  The audit therefore
integrates the repaired package, not the stale causal interpretation.

## 2. Inheritance pass and concept board

The closest proved algebraic mechanism is
THM-2512-lawful-interaction-cut-bundle-transplant-and-replica-dichotomy: a
rational nonzero doubly centred `7 by 13` table fires all `5,184` primitive
toothpick coefficients.  The closest geometric mechanism is
THM-2471-owner-first-collision-weighted-root-service-and-temporal-atom-boundary:
its equation (31) realizes rational weights as finite Boolean ancestry
marginals, while (44)--(47) gives `theta=t-2u` exactly at `r=5`.

The corrected near misses are:

- MISTAKE-286: `r=5` and `theta` belong to this particular THM-2471-sidecar
  realization, not to THM-2512's generic question;
- MISTAKE-293: common ancestry means linked nodes over one base, not one
  circle point, and `k=6` is checked directly rather than licensed by an
  unknown eventual threshold;
- MISTAKE-295: constant-column erasure is not the fixed-absolute-root
  experiment.

The least-used live sidecar is the marginal-kernel viewpoint from THM-2538
and the newer U_full endpoint audit.  It distinguishes a genuine joint lift
before integration from a product of already separated endpoint marginals.

The resulting board is:

| object | preserved fact | missing coordinate / cheapest test |
|---|---|---|
| THM-2594 linked-node table | one exact common-base Boolean expansion and nonzero cut bundle | target-active semantic arrival; build it before marginalization |
| THM-2512 cut transform | non-replica iff every primitive mode fires for a rational table | does not create a physical table |
| fixed-root hostile | the same table remains nonzero without causal slaving attribution | identify what geometric predicate, if any, distinguishes the theta chart |
| U_full endpoint bank | all 72 graph packets are nonzero after marginalization | shared atom key `omega`; first recover only the `H-q5` bridge |
| 165 THM-2349 rows | common delayed-current frontier | uniform typed map; one row is not a row exclusion |

## 3. Why the table really is on one finite ancestry base

Put `d=13^5`, `R=13^2`, and use THM-2471's linked nodes

```text
w_u=(y+u)/13,
X_(u,a)=(w_u+a)/d,
Z_(u,a,b)=(X_(u,a)+b)/R,
Y_(q,e')=(w_q+e')/d.
```

Expanding the two transfer operators gives, before any integral or Fourier
sum,

```text
A(y,u)=1/(dR) sum_(a,b) 1_Q(X_(u,a)) 1_E(Z_(u,a,b)),
F(y,q)=1/d    sum_(e')  1_E(Y_(q,e')).
```

Thus their product has normalization `1/(R d^2)` and one common outer
variable `y`.  Inserting the response factors into each summand is lawful
because the three relevant descents are exact:

```text
13 w_u-y                         = u,
c_3 X_(u,a)-2(y+u)/13            = 2a,
13^6 X_(u,a)-y                   = u+13a.
```

The right sides are integers.  Therefore the owner cell at `w_u` descends to
`y`, the deep probe at `X_(u,a)` depends on
`theta=t-2u`, and the `k=6` word at `X_(u,a)` returns to `y`, independently
of the sheet.  The source remains at the linked node `Y_(q,e')`.  This proves
one finite Boolean fibre over one base; it does **not** identify `w`, `X`, and
`Y` as one point.

An exact breakpoint audit independently recovers

```text
DEEP_0=[0,13/28),
DEEP_1=(1/28,27/28),
DEEP_2=(15/28,1),
DEEP_theta=empty for theta=3,...,12,
sum_theta DEEP_theta = 2-d(2y)  almost everywhere.
```

The companion performs `3,380` direct Fraction checks of the linked-node
identities and `65` deep-window cell checks.  The common denominator is

```text
R d^2 T_DEN = 6939029398456584394954868880,
```

and the decisive contraction denominator is its product with `91`:

```text
631451675259549179940893068080.
```

## 4. Independent arithmetic audit

Normal, optimized, and stored current-primary transcripts agree after
masking only elapsed fields; their normalized transcript hash is

```text
184c159dd4aedb02b38b2029846b9e88f301fcc63dd163682bbabddc438b825e.
```

The audit uses the primary interval engine only to materialize its exact
`N(u,q,ell,theta)` table.  From that point onward it independently implements
double centring, direct toothpick summation, construction and exact division
for `Phi_91`, cyclotomic reduction, cyclic convolution, and the THM-2512
factorization.  This makes the independence boundary explicit: it is a fresh
consequence-object and typing audit, not a second constructor for the large
`E_1/Q_{1,{b}}` interval table.

The results are:

```text
Psi_(1,1)(1,1) != 0 exactly;
nonzero reduced coordinates: 59/72;
split-prime values: 295, 213, 380, 1405, 883;
primitive coefficients: 5184/5184 nonzero;
minimum nonzero reduced coordinates in that bundle: 36/72;
nonzero fibrewise interactions: 34/169, with none at shift s=0.
```

The direct ordered primitive-bundle digest is

```text
c695826d15bfec0867bf17ebc77e2fe3c8ba87cbab841f6d0752d63b6a25f75a.
```

This exhaustive check is stronger than merely invoking Galois transitivity,
although it agrees with the theorem: rationality conjugates every primitive
mixed character, and the geometric factor `K_(u,beta)` is nonzero for
`u,beta` primitive.

## 5. Hostile controls and the first failed causal implication

Three controls have different meanings.

1. `beta=0` vanishes exactly.  This is the row-zero law.
2. Copying the same deep marginal into every column makes the interaction
   identically zero.  This tests deep-label erasure only.
3. Fixing an absolute root `t` before choosing `u`, so that the `u` fibre
   reads column `theta=t-2u`, does **not** vanish:

```text
raw nonzero cells       18/91,
centred nonzero cells   91/91,
primitive coefficients 5184/5184 nonzero.
```

The third bundle has digest

```text
fba3895e684d14285e448556904621c9357e6b72ac6f84104ed228fc745e2217.
```

So the first historical causal implication fails:

```text
constant-column control vanishes
  does not imply
nonvanishing is uniquely caused by theta=t-2u slaving.
```

What survives is stronger and cleaner: the theta-indexed linked-node table is
non-replica, hence its entire primitive cut bundle fires.  No claim about the
unique mechanism of that non-replica interaction survives.

## 6. Relation to the newer endpoint work

THM-2594 and the U_full endpoint bank differ exactly at the marginal-kernel
boundary.  THM-2594 inserts all factors into one summand before the finite
sheet sum and integral.  The newer endpoint engine computes left and right
endpoint sums separately and only then multiplies them.  The two-atom
aligned/crossed hostile shows that equal marginals can have joint mass `1/2`
or `0`; the missing space is

```text
ker(epsilon_L) tensor ker(epsilon_R).
```

Connection contract:

| field | exact content |
|---|---|
| source | THM-2594 finite linked-node table |
| attempted target | U_full target-active endpoint/current table |
| available map | marginalize the THM-2594 sheets to a rational `7 by 13` response |
| preserved | rationality, zero margins after ANOVA, primitive cut nonvanishing |
| destroyed | target address, semantic source-arrival edge, endpoint role, uniform row identity |
| needed sidecar | a U_full joint ancestry key before either endpoint sum |
| cheapest decisive test | recover the single `H-q5` bridge atomwise before either `K4` factor |

Promoted structural THM-3479 supplies nonzero post-marginal endpoint
characters and all `72` labelled role charts, but its independent audit
explicitly retains the same common-ancestry boundary.  Consequently neither
newer endpoint package upgrades THM-2594 into a physical current.

## 7. Final status and remaining frontier

The accepted statement is:

> On the canonical THM-2309 (25) row at `(j,sigma,K,r,k)=(1,{b},2,5,6)`,
> one explicitly defined rational linked-node response table on one finite
> ancestry base has a nonzero THM-2512-shaped primitive contraction; all
> `5,184` primitive coefficients of that table are nonzero.

Not proved:

- that this table is THM-2449's one-point lawful response;
- that this one instance solves THM-2512's generic realization problem;
- target-active `B(q)` support or a semantic source-arrival map;
- a physical current or transplant theorem;
- exclusion of any of the `165` THM-2349 first-depth-one rows;
- LRC(14).

The global projected frontier's `12` families and the older target-landing
ledger's `165` rows are different ledgers and must not be conflated.

## 8. Reproduction

Run from the repository root:

```text
python -B 04-computation/lrc14_stage2_theta_contraction_r5_independent_audit_20260816.py
python -B -O 04-computation/lrc14_stage2_theta_contraction_r5_independent_audit_20260816.py
```

The companion itself replays both the historical and repaired primary
scripts in normal and optimized modes.  Its LF hashes are

```text
script 8be9c1b69b33ab51ac16ce2c2a7f836aae4b811e2817b90e25921c234578c568
output 087bb018408d3438494fb4ade461a4371e26bec60e31421540fea929316dd679
```
