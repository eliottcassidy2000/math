# LRC14 disconnected-low: the physical pair-floor frontier after two-axis compression

**kps, 2026-08-12.  PROVED reductions + FINITE-EXACT evidence + OPEN final
horns.**  This is not a proof of the universal pair floor, the reflected
branch, or LRC(14).

## Inheritance and correction

The closest proved mechanisms are THM-2941's sharp projective high-channel
floor `1/105`, THM-3350's all-channel midpoint/shear transport, and
THM-3352's exact arbitrary-ratio physical mass engine.  The canonical hostile
is a low channel with zero physical overlap; high/low typing is indispensable.

An earlier numerical observation was almost conflated with an unrelated
`1/294` statistic in the repo.  They are different objects.  Here `1/294` is
only a strategically sufficient target for the **physical reflected pair
mass**.

## 1. Why one pair floor would close every disconnected-low word

For distinct levels, let the low graph join a pair whose reduced levels
`P,Q` satisfy `P+Q<=7`.  If this graph is disconnected, its high complement
is connected and therefore has a five-edge spanning tree.  If every high
physical edge had mass at least `1/294`, Hunter credit would be at least

```text
5/294.
```

THM-3350's exact universal singleton debt is

```text
D_max=186636088362/11773143757375,
5/294-D_max=570672686921/494472037809750>0.              (1)
```

Thus the pair floor would close independently scaled disconnected low
components on all 649 upper-median bodies.  This implication is **PROVED**;
its antecedent remains open.

## 2. Exact finite signal and its arithmetic form

Across all `4,044` feasible contexts `(L,j,e,f)` and all high physical pairs
`1<=p<q<=80`, the exact minimum is

```text
(L,j,e,f;p,q)=(336,174,12,3;3,5),
I=158/46397=1/294+55/13640718.                            (2)
```

Writing `z=Lp-e`, `w=Lq-f`, the engine's numerator is `N=5688`; equivalently

```text
294N-zw=1980>0.                                          (3)
```

No endpoint correction is active in (2): the near-extremum is a bulk phase
alignment, not clipped-tooth loss.  The low hostile

```text
(L,j,e,f;p,q)=(168,90,1,3;10,2)
```

has exact mass zero and reduced channel `5:1`, whose primitive sum is six.
This prevents removal of the high-channel hypothesis.

An independent hostile audit extended different exact banks and found the
same minimum.  Those computations are evidence, not the infinite proof.

## 3. Proved large-ratio gate

Let `A_e(p)` be the first reflected comb and let `J` be an interval.  The
centered periodic primitive of the second radius-`1/14` comb has oscillation
`6/49`, so

```text
|mu(J intersect A_f(q))-|J|/7| <= 6L/(49w).              (4)
```

The first comb has at most `p+1` physical components, and

```text
mu(A_e(p))=1/7+e/(7z).
```

Summing (4) componentwise gives

```text
I >= 1/49+e/(49z)-6L(p+1)/(49w).                         (5)
```

Hence

```text
5w >= 36L(p+1)  ==>  I>=1/294.                           (6)
```

For `p=dP,q=dQ`, failure of (6) confines the projective ratio to

```text
Q/P < 36/5+(36L+5f)/(5LdP).                              (7)
```

Thus every sufficiently wide ratio is already closed.

## 4. Proved common-gcd gate inside the bounded-ratio band

THM-3350 gives, for `C=Qe-Pf`,

```text
I >= 1/105-B_e(d),
B_e(d)=gamma_P eP/(dLP-e)+gamma_Q fQ/(dLQ-f)
       +|C|(floor(|C|/L)+1)/(2d^2LPQ),                   (8)
```

where `gamma_k=1/2` for even `k` and `(k^2+1)/(2k^2)` for odd `k`.
Failure of (6), `e/L,f/L<=1/14`, and `d>=19` give

```text
Q < 36P/5+509/1330.
```

For `P>=8` this is less than `29P/4`; direct inspection of `P=1,...,7`
(retaining coprimality) gives the same conclusion.  Thus `Q/P<=29/4`.

Put

```text
a_d(k)=gamma_k k/(14dk-1).
```

Each shear term in (8) is at most `a_d` of its channel coordinate.  The odd
subsequence of `a_19(k)` decreases for `k>=1`, and the even subsequence is
below `a_19(3)` for `k>=2`.  If `P=1`, highness and the ratio bound force
`Q=7`; if `P>=2`, both summands are at most `a_19(3)`, whose doubled value is
smaller.  Therefore

```text
shear <= a_19(1)+a_19(7)=19652/3452155.                  (9)
```

For the determinant term, put `r=Q/P`.  Since

```text
|C|/L <= (P+Q)/14,
1/P+1/Q <= 8/7,
```

and `r+2+1/r` increases for `r>=1`,

```text
determinant <= (2945/45472)/d^2.
```

At `d=19`, (9) and this determinant envelope sum to

```text
B_e(d) <= 2501969023/426078778720
       = 3/490-106676561/426078778720 < 3/490.            (10)
```

All terms decrease with `d`.  Since

```text
1/105-3/490=1/294,
```

equations (8)--(10) prove

```text
d>=19 and failure of (6)  ==>  I>1/294.                  (11)
```

The actual contextual error on the apparent worst small channel
`(L,e,f;P,Q;d)=(168,12,6;1,7;19)` is smaller still,
`673605911/139615031640`.  It is a control, not a dependency of (10).

## 5. Honest remaining frontier

Any counterexample to the desired pair floor is confined to

```text
1<=d<=18,
5(LdQ-f)<36L(dP+1),                                     (12)
```

with `(P,Q)=1`, `P+Q>=8`.  Four asymptotically exceptional orientations are

```text
(L,j,e,f)=(168,90,12,f),   f in {1,2,3,4}.              (13)
```

For every other context, a centered-grid bound becomes positive beyond a
finite primitive `P`; the worst exploratory threshold is `P=733`.  For
`d>=2` it drops below `35`.  The four horns (12) require the stronger
Dirichlet multiblock method of THM-3171 or an aggregate-tree argument; the
naive centered-grid asymptote is too weak there.

This leaves two crisp routes:

1. generalize THM-3171's `d<=8` multiblock compiler to target `1/294`, ratios
   up to (7), and the four lanes (13); or
2. abandon edgewise minimization on (13) and prove that a five-edge
   complete-multipartite tree cannot contain too many exceptional
   orientations simultaneously.

The second route is attractive: the obstruction is concentrated on label 12
pointing to four small labels at the unique least ruler.  A tree has only one
label-12 degree budget, so the four bad lane types are strongly correlated.

## Reproduction

```bash
python3 04-computation/lrc14_disconnected_low_physical_pair_floor_frontier_kps_s170.py --cap 80 --workers 10
```

The run uses exact rational arithmetic, checks the `4,044`-context semantic
digest, freezes (2), includes the zero hostile, and prints (1), (6), and (9).
The LF-normalized script/output SHA-256 values are respectively
`e910a0f84b31cd68fd0f79b6aefe64df02f467a7ea90d8221b2778f73590cd40`
and `6e5890afbc676f58bccbcb3c4942056baddf9effcfec9e4a76be63e4a7e16740`.
