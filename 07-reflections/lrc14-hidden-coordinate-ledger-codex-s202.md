# LRC14 Hidden-Coordinate Ledger - S202

The useful hidden detail in the latest LRC stack is not a new scalar.  It is
the order in which scalars become legal.

The recent chain reads like this:

```text
status gate
-> owner-essential path lift
-> residual certificate tooth
-> first-tooth owner strip
-> primitive period deck
-> residual capacitor cut
-> q=23 drop/add zeta plus endpoint owner strip
```

That is a controlled-forgetting ledger.  Each step says which coordinate may be
forgotten only after a replacement has been exposed, a dual has killed it, or a
named residual debt has been emitted.

This connects several older warnings.  The S78 address-retention lesson was
not just conservative bookkeeping; it is the operating rule.  The S134
visible/hidden fold warning shows up verbatim in the q=23 repair: exact
`M=2/23` and endpoint word `B18Z6` look equal until owner strips split them.
The S549 anti-wedge rule suggests a new check on capacitor cuts: once a cut
`X -> Y` is accepted, the remaining ledger should not contain `Y -> Z -> X`
unless the wedge is named residual debt.  And the S24 cochannel lesson says
primitive decks, capacitor IDs, zeta classes, and owner strips are channels
that must be declared before analytic estimates are allowed to scalarize them.

The pair-good decoy story gives the count discipline.  Pair-good decoys become
manageable once they are classified as

```text
good source pair + blocker residue tooth.
```

The LRC14 residuals should be treated the same way: a residual count is a
symptom, while the generator/cut class is the theorem object.

The next useful packet sidecar bundle is therefore:

```text
hidden_coordinate_stage
visible_hidden_relation_type
primitive_safe_deck_2_13
first_primitive_safe_q_2_13
residual_capacitor_id
first_cut_stage
drop_add_square_id
exact_M_zeta
endpoint_owner_strip
anti_wedge_debt_count
```

Tournament Analysis: vertices are hidden-coordinate ledgers, not runners.
The pairwise observable records boundary/open preservation, route purity,
hidden-coordinate exposure, cut/dual availability, family transfer, and proof
cost.  The chosen path is

```text
addressed_packet
> owner_essential_path_lift
> residual_certificate_tooth
> primitive_period_deck
> residual_capacitor_cut
> drop_add_haar_square
> visible_hidden_relation_signature
> gK8_boundary_moment_channel
> raw_scalar_shadow
```

Assumption challenge: I considered runners, gaps, fixed sections, endpoint
owners, wall events, residues, cover arcs, Fourier modes, Haar tiles, matroid
circuits, pair-good blocker teeth, capacitors, and proof obligations.  The
preserved predicate is boundary/open status plus theorem-route schedulability,
so the vertex set should be the ledger stages that control when quotienting is
allowed.
