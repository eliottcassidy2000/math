# The inhomogeneous-LRC ↔ complement reframe, pushed: the observer at c gives M_c(S)=max_t min_s‖st−c‖, the tournament COMPLEMENT R is the LRC's TIME-REVERSAL t→−t which acts on the observer as c→−c (verified M_c=M_{−c}), so the self-complementary (R-fixed) tournaments are the SELF-ANTIPODAL observers c∈{0,1/2} (the 2-torsion) and the AP/covmin core is time-reversal-INVARIANT (SC, ↔ the transitive H=1 baseline); the "loneliness landscape" M_c is MINIMIZED at c=0 (the standard LRC bound 1/n — the origin is the HARDEST observer) and the 6-torsion observers c=k/6 give M_c = the odd multiples {1,3,5,7}/14, connecting to 14a's Z/6 torsion = the units mod 14

*opus-2026-06-30. Owner: push the inhomogeneous-LRC ↔ complement reframe, poke for creative connections.
Pushed: the complement IS the time-reversal (c→−c), c=0 is the hardest observer, and the observer's
torsion-positions index 14a's torsion. The c-space mirrors the metagraph's R-action.*

## The reframe: complement = time-reversal = c→−c (verified)
The inhomogeneous LRC puts the observer at `c`: `M_c(S) = max_t min_s ‖st − c‖` (c=0 is the standard LRC).
The tournament **complement `R`** (flip all arcs) is the LRC's **time-reversal `t→−t`**, and on the observer
it acts as **`c → −c`**:
> `M_c(S) = M_{−c}(S)` (verified: `M_{1/6}=M_{5/6}=3/14`, `M_{1/3}=M_{2/3}=5/14`, `M_{2/7}=M_{5/7}=73/231`).
So the two halves' "reversal" symmetries coincide: the tournament's arc-flip and the LRC's time-reversal both
**move the observer's frame by the antipode**. This is why the antipode `−1` / the complement recurred all
session — it is the shared reversal symmetry, now named on the LRC side as `c→−c`.

## The fixed points: self-antipodal observers = SC tournaments
The `c→−c` involution fixes exactly **`c ∈ {0, 1/2}` — the 2-torsion of the circle**:
> `c=0` (the standard LRC, the origin observer) and `c=1/2` (the antipode observer) are **self-antipodal**,
> the LRC analogue of the **self-complementary (R-fixed) tournaments**. The off-fixed observers come in pairs
> `{c, −c}` ↔ the R-pairs (the non-self-complementary classes). And the **covmin core `{1,…,n−1}` (the AP) is
> time-reversal-INVARIANT** (`−{1,…,13} = {1,…,13} mod 14`) — it is **self-complementary**, the SC/cusp config,
> ↔ the **transitive `H=1`** ordered baseline (the AP = equally-spaced order ↔ the linear order). The observer
> `c=0` sits on the SC fixed point.

## The loneliness landscape: c=0 is the HARDEST observer
`M_c` as a function of `c` (the AP, n=14, the 6-torsion):
| `c` | 0 | 1/6 | 1/3 | 1/2 | 2/3 | 5/6 |
|---|---|---|---|---|---|---|
| `M_c` | **1/14** | 3/14 | 5/14 | 1/2 (=7/14) | 5/14 | 3/14 |
> The values are the **odd multiples `{1,3,5,7}/14`**, symmetric about `c=1/2`, **minimized at `c=0`**. So
> the STANDARD LRC (`c=0`) is the **WORST-CASE observer position** — the origin is where all runners
> simultaneously pass (at `t=0` every runner is at 0, so only `c=0` is "blocked" there), giving the tightest
> bound `1/n`. The antipode `c=1/2` is trivially easy (`M=1/2`, the runners are all at 0 at `t=0`, the
> observer at `1/2` is maximally far). The LRC is the hardest slice of the inhomogeneous landscape; the
> covering-min `1/n` is its floor at the SC fixed point `c=0`. (The odd-numerator spectrum `{1,3,5,7}/14`,
> peaking at the apex `7`, is a small surprise worth pinning.)

## The observer's torsion ↔ 14a's torsion (the units)
The 6-torsion observer positions `c = k/6` (`k=0,…,5`) are the natural "rational defects." And **14a's
torsion is `Z/6 = the units mod 14`** (the razor's-edge / binding points, the cusps→torsion embedding). So:
> **placing the observer at the `m`-torsion `c=k/m` indexes the curve's `m`-torsion** — the inhomogeneous LRC
> at `c=k/6` lives over the same `Z/6` as 14a's torsion = the units. The self-antipodal `c∈{0,1/2}` is the
> 2-torsion `Z/2⊂Z/6`; the `c=k/3` are the 3-torsion `Z/3`. The observer-defect space and the elliptic-curve
> torsion are the same `Z/6` — the "translate the observer" knob is the curve's torsion parameter.

## The c-space mirrors the metagraph's R-action
| LRC observer space (the circle, `c→−c`) | tournament metagraph (the R-action) |
|---|---|
| self-antipodal `c∈{0,1/2}` (2-torsion) | self-complementary (SC) classes (R-fixed) |
| pairs `{c,−c}` | R-pairs (non-self-complementary) |
| `c=0` = the hardest observer (`M=1/n`) | the transitive `H=1` / the principal-line origin |
| `M_c` landscape (defect spectrum) | the H-gradient / the spine-ribs-sea |
> The inhomogeneous-LRC observer space, quotiented by `c↔−c`, is the LRC analogue of the **merged metagraph
> `G_n/Z₂`** (complement factored out). The defect spectrum `M_c` is the LRC's H-gradient; the SC fixed points
> are the spine's anchors. "Translate the observer" = "move along the merged metagraph."

## Status
- **Computed (opus):** `M_c(AP)` at the 6-torsion `= {1,3,5,7,5,3}/14`; `c↔−c` symmetry verified (= complement
  = time-reversal); `c=0` is the global minimizer (the LRC bound `1/n`, hardest observer); the AP is
  time-reversal-invariant (SC).
- **Creative connections (poked):** complement = time-reversal (`c→−c`); 2-torsion observers `{0,1/2}` = SC
  tournaments; observer at `m`-torsion ↔ 14a's `m`-torsion (units `Z/6`); the `c→−c` observer space mirrors
  the merged metagraph `G_n/Z₂`; `c=0` (hardest) ↔ the transitive origin.
- **Worth pinning:** the odd-numerator spectrum `{1,3,5,7}/14` (peaking at apex 7) for the AP; whether the
  inhomogeneous covering-min `min_S M_c(S)` at `c=1/2` has its own clean value.

Related: observer-transformations-dilation (translate = fragile → complement), SECOND-CORRECTION-…AP-scaled
(the SC AP/covmin), the-reversal-involution, the-master-two-Heegner-columns (the units `Z/6`), lrc14-lives-
on-14a (the torsion); the metagraph R-action / SC-NS spine-ribs-sea; OPEN-Q-039/108.
