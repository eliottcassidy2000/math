# The r5 common child core is a dual response plane, while the second formal H1 class is endpoint debt

**Status: FINITE-EXACT STATIC REPRESENTATION SYNTHESIS AROUND RESERVED
THM-3534.  THE MATRIX CLAIMS HAVE A CUSTOM-RREF REPLAY AND AN INDEPENDENT
FLINT REPLAY FOR BOTH THE ORIGINAL RESPONSE COSPAN AND THE NEW COMMON-CORE,
ENDPOINT-EXTENSION, AND 127-WINDOW PACKAGE.  EVERY FINITE MAP IS NOW
INDEPENDENTLY REPRODUCED; THE NETWORK HOSTILE AUDIT OF VARIANCE AND TYPING IS
PENDING.  NO PHYSICAL CURRENT, D5 FLUX, ROW EXCLUSION, OR LRC(14) CONCLUSION.**

The frozen r5 tent and common ten-dimensional quotient do contain a lawful
rank-two object.  It is more rigid, and less directly useful, than the scalar
transport intuition suggested:

~~~text
all-child common quotient U
        = orientation-forgetting kernel inside Q10
        = dual of the endpoint-relative response plane V_rel.
~~~

The word “dual” is load-bearing.  The missing endpoint direction is also
load-bearing.  Once those two facts are kept separate, the cohomology puzzle
has an exact classification:

~~~text
two-dimensional coefficients V_rel:  formal twisted H1 has dimension 1;
three-dimensional coefficients V_ext: formal twisted H1 has dimension 2;
the extra class in V_ext:              exactly the r0=6 endpoint line.
~~~

Neither calculation is physical H1 because the same-copy owner edge closing
the state path is still absent.

## 1. Inheritance pass and concept board

The closest proved mechanism is THM-2716's C4 arm transporter: it teaches that
a relative object can be genuine even when absolute lifts disagree.  The
canonical hostile is the exact tent-location cospan

~~~text
h=(12,12,9,3,0,0),       h_odd=(0,0,3,-3,0,0),
~~~

whose normalized seam is formal and whose literal arc realization has the
factor-two defect.  The corrected near miss is the r1-blind scalar
location-to-response map: the actual middle response has two independent rows
and eight amplitudes on each exceptional row.  The least-used relevant sidecar
is quotient duality, together with the endpoint line that the first relative
quotient erased.

The live board was:

| lane | object | decisive question |
|---|---|---|
| Anchor | frozen tent plus Q10 | does a genuine rank-two marked response carrier exist? |
| Niche | all-child intersection inside Q10 | is its rank two the same orientation defect, or another dimension coincidence? |
| Wildcard | THM-3536 signed Berggren C4 | what distinguishes a real edge cocycle from a response-shaped four-state analogy? |
| D5 | THM-3496 graph-to-Kummer line | can any coefficient-compatible additive map receive the r5 carrier? |
| XOR/tournament | bidirected P4 and chamber involution | is there an intrinsic antisymmetric relation, and what does Booleanization erase? |

The niche overtook the anchor: it supplied the missing canonical source for
the rank-two dual pairing.

## 2. The exact common-core identity

Let W be the 78-dimensional arc-by-digit space, R the rank-68 parent rowspace,
and C_t the thirteen fixed-third-digit child rowspaces.  Write

~~~text
Q=W/R,                     q:W -> Q,
C_common=intersection_t C_t,
K=C_common intersect R,
U=q(C_common)=C_common/K.
~~~

Exact reduction gives

~~~text
dim C_common=14,          dim K=12,          dim U=2.
~~~

Let A reverse each directed P4 arc.  The old quotient audit proved only

~~~text
dim(R+A R)=70,            dim W/(R+A R)=8.
~~~

The new basis comparison proves the exact identity

~~~text
U = (R+A R)/R = ker(Q -> W/(R+A R)).                    (1)
~~~

The canonical RREF digest of both sides is

~~~text
584eb2a4022ffed65be254f73eda96facaaaa87b2cc1b80ff8f7714dd74bd65a.
~~~

Dually,

~~~text
U^perp=(W/(R+A R))^vee,                                 (2)
~~~

and both eight-spaces have canonical digest

~~~text
b5143e9ea23f3f981c1b6b334799e7924df0a4a544b69d6dcd18defc01d0f238.
~~~

This resolves an ambiguity in the frozen Q10 input.  The common rank-two
child image is not a transferable state shared by every child.  It is
precisely the information killed when arc orientation is forgotten.

## 3. The perfect middle-only pairing

The quotient dual Q^vee splits by the six directed P4 arcs with dimensions

~~~text
(2,2,1,1,2,2).
~~~

Pair U against those six blocks using the canonical evaluation
Q times Q^vee -> k.  The rank profile is

~~~text
(0,0,1,1,0,0).                                          (3)
~~~

Thus U is invisible to all four outer blocks and pairs perfectly with the two
middle dual lines

~~~text
L=L_(1->3) direct sum L_(3->1).
~~~

In chamber-linked deterministic bases, the pairing matrix is the scalar
matrix

~~~text
636675481197456361648540 * I_2,
~~~

with nonzero determinant

~~~text
149750845022728455688979.
~~~

That scalar is tied to the frozen deterministic free-variable/RREF chart.
An extra basis reduction in the independent implementation changed the
displayed nonzero scalar while preserving the perfect pairing; restoring the
pinned basis recovered the number above.  Thus perfectness, middle-only
support, and chamber equivariance are invariant, while the scalar itself is
marked chart data.

The middle response difference D contracts L injectively to a response plane
I.  The source plane S and I are distinct, but

~~~text
(S+I) intersect E = k D_(6,*),
V_rel=(S+I)/kD_(6,*),
L -> V_rel is an isomorphism.
~~~

Dualizing this isomorphism and composing with (3) gives the lawful static map

~~~text
U -> L^vee -> V_rel^vee.                                (4)
~~~

This is the strongest map furnished canonically by the frozen child quotient.
A direct map U -> V_rel requires a marked self-duality.  Such a marking is
allowed as bookkeeping, but it is new gauge data and cannot be renamed a
chain map.

## 4. Why reversal produces scalar holonomy, not an action

The common realization is not stable under pure arc reversal:

~~~text
dim(C_common+A C_common)=16,
C_common intersect A C_common=K.                        (5)
~~~

The kernel K itself is A-stable.  Therefore one can reverse a common lift,
project to Q, and reinflate through C_common/K.  This defines a canonical
compressed endomorphism T_A of U.  It is not the restriction of the
involution A, because A C_common is not C_common.

In the chamber-linked basis,

~~~text
T_A=lambda C,
lambda=145859431184888028092125,
lambda^2=17748677861075734903229 !=1.                   (6)
~~~

Here C is the genuine coupled chamber swap.  Equation (6) is the exact
holonomy invoice for “reverse, forget the parent gauge, and realize again.”
It also explains why applying A^2=1 after compression is invalid.  The
realization step between the two applications is not A-equivariant.

Pure digit reflection is not even a compressed endomorphism of U: it moves
K outside R, and the image of the reflected common child has rank ten in Q.
The coupled chamber reflection is the only genuine involution in this
two-plane.

## 5. Four different meanings of “two-dimensional”

The phrase “a genuinely two-dimensional class” was hiding four dimensions:

1. The exceptional location support has two marked chambers r0=3,9.
2. Their response span S has dimension two.
3. The all-child quotient core U has dimension two.
4. A cohomology group may itself have dimension two.

The first three now have explicit typed maps.  The fourth behaves differently.
On V_rel, chamber monodromy is a swap, so

~~~text
rank(C-I)=1,
dim H0(C4;V_rel,C)=dim H1(C4;V_rel,C)=1.                (7)
~~~

The anti-invariant common line is exact.  The invariant line survives.

The minimal endpoint-retaining ambient is

~~~text
V_ext=S+I=I direct sum kD_6=S direct sum kD_6,
dim V_ext=3.                                             (8)
~~~

Its chamber matrix swaps the two I channels and fixes D_6.  Hence

~~~text
rank(C_ext-I)=1,
dim H0(C4;V_ext,C_ext)=dim H1(C4;V_ext,C_ext)=2.         (9)
~~~

The two invariant classes are represented by

~~~text
i_+ + i_-                    relative response,
D_(6,*)                      pure endpoint.
~~~

MISTAKE-420 records why the first draft's broader no-go was false.  The
correct classification is:

> A chamber-faithful two-dimensional coefficient plane has only one formal
> twisted class.  Two formal classes require the three-dimensional extension,
> and the second class is exactly endpoint debt.

Both S and I split the short exact sequence

~~~text
0 -> kD_6 -> V_ext -> V_rel -> 0.                       (10)
~~~

Their anti-invariant lifts agree.  Their invariant lifts differ by
2cD_6.  Thus the endpoint term is not noise: it measures the discrepancy
between two natural splittings.

## 6. Berggren is the positive C4 control

THM-3536 supplies a real signed C4:

~~~text
vertices: four inverse-wall phases,
edges:    lawful successor transitions,
cochain:  branch orientation signs (-,+,-,-),
gauge:    vertex sign switching,
class:    the nonzero element of H1(C4;F2).
~~~

The raw Fibonacci projective cycle has a switching-equivalent sign word.
This is a lawful comparison because both sides possess actual successor
edges and their sign products are -1.

The r5 response object differs at every load-bearing coordinate:

| feature | Berggren wall C4 | r5 response cospan |
|---|---|---|
| base edges | four literal successor transitions | three owner adjacencies; same-copy closure absent |
| coefficients | signs in F2 after a lawful plus/minus reduction | amplitudes in a large odd finite field |
| nonzero class | odd edge-sign seam | digit seams all zero; relative chamber class is formal |
| gauge | vertex sign switching | parent-row gauge plus endpoint D_6 |
| holonomy | sign product -1 | compressed scalar lambda^2 not equal to 1 |
| H1 status | proved graph class | no physical graph class |

This is the meta-pattern “same representation, not same carrier.”  The
four-state shape is useful because it names the missing structure; it is not
itself a bridge.

## 7. Tournament and XOR verdict

The intrinsic state relation in r5 is the bidirected path

~~~text
0 <-> 1 <-> 3 <-> 2,
~~~

with census

~~~text
(both-way,one-way,missing)=(3,0,3).
~~~

It is not a tournament.  The chamber map exchanges two marked orientations
both ways and is likewise not an antisymmetric dominance relation.

XOR is lawful for the Berggren sign class because multiplication of plus/minus
signs descends to addition in F2 modulo vertex switching.  It is not lawful
for (6).  Booleanization would:

- identify invariant and anti-invariant amplitude channels;
- erase the nonzero endpoint mismatch 2cD_6;
- replace lambda C by the bare swap even though lambda is not plus or minus
  one; and
- pretend there is a coefficient map from the split prime field to F2.

There is no such unital coefficient map.  XOR therefore records a shape but
destroys the obstruction.

## 8. The D5 connection contract

THM-3496 gives the proved marked correspondence

~~~text
H1_graph(C7;F13) -> H1_et(K((lambda));mu_13),
~~~

after fixing source orientation, deck generator, target uniformizer,
meridian orientation, and an exponent-one Kummer torsor.  Its seam map is
genuine because C7 is a literal graph and the coefficient group is F13.

For the r5 candidate, the full contract is:

| field | exact value |
|---|---|
| source | U=q(intersection_t C_t), or after marking its self-duality, the relative response plane V_rel |
| intermediate map | perfect evaluation U -> L^vee and dual contraction L^vee -> V_rel^vee |
| endpoint extension | V_ext with exact sequence (10) |
| desired target | THM-3496 Kummer line or a characteristic-zero additive Hamiltonian flux module |
| preserved so far | middle orientation, all-child common quotient class, chamber involution, relative zero/nonzero |
| destroyed so far | endpoint dual, literal child lift, digit chronology, source ancestry, closure edge, additive flux, Keller predicate |
| mandatory sidecars | a physical current-to-response chain map, same-copy closure edge, endpoint dual carrier, coefficient realization, and filtered target predicate |

At the pinned finite coefficient level, the additive group of U, V_rel, and
V_ext has exponent p, while the Kummer line has exponent thirteen.  Since
p and 13 are coprime,

~~~text
Hom_Add(V_ext,F13)=Hom_Add(F13,V_ext)=0.                 (11)
~~~

Every additive map from these finite p-groups to a characteristic-zero vector
space is also zero.  A characteristic-zero reconstruction would be genuinely
new data; it would still face THM-3354's site/predicate obstruction,
THM-3450's margin and gauge obstruction, and THM-3496's Kummer-versus-additive
flux hostile.

Thus (4) is a lawful static LRC-side duality map, not an LRC word-current to
JC flux map.

## 9. Strongest survivor and next decisive operations

The strongest survivor is a two-stage object:

~~~text
U  --perfect duality-->  V_rel^vee
                         |
                         | needs endpoint dual
                         v
                       V_ext^vee.
~~~

Four next tests are now sharply typed.

1. **Endpoint-dual search.**  Search the frozen parent/child bank for a
   canonical one-dimensional functional pairing nontrivially with D_6 and
   vanishing on I.  A positive result would extend U to a dual of V_ext; a
   zero result would prove that Q10 cannot see the second formal class.
2. **Physical closure search.**  Construct an actual same-copy 2--0 address
   edge before any further C4 cohomology.  Its boundary and compatibility with
   the source current must be checked before evaluating a seam.
3. **Characteristic-zero reconstruction.**  Recompute the response cospan
   over an integral or characteristic-zero coefficient object.  Merely
   choosing integer representatives of finite-field entries does not lift the
   equations.
4. **Derived/nonadditive route.**  If a future map is nonlinear or derived,
   state the functor and target predicate explicitly.  Equation (11) excludes
   ordinary additive shortcuts but not every correspondence imaginable.

The first endpoint-dual test is now complete.  Exhaust all 127 nonempty
third-digit subsets stable under `r2 -> 12-r2`, intersect their child
rowspaces, and project to Q.  Their complete chamber-character histogram is

~~~text
dimension 2, split 1+1: 119 windows;
dimension 6, split 3+3:   4 windows;
dimension 10, split 5+5:  4 windows.
~~~

There is no three-dimensional window and no unbalanced chamber character.
Thus relaxing “all thirteen children” to any chamber-stable child window
cannot append one endpoint-invariant dual line by itself.  The quotient jumps
from 2 to at least 6, adding a balanced 2+2 complement.  A smaller selected
subquotient inside that complement is not excluded, but it requires an
additional selector not supplied by child intersection alone.

## 10. Scope

The exact companion is
`04-computation/lrc_r5_common_child_middle_duality_thm3534_20260816.py`.
Normal and optimized transcripts agree with the stored output.  Its semantic
hash is

~~~text
4261ca08016b2de90bc8f609ebf283d915f8613410a5dbc15229b02baeed8803.
~~~

The disjoint audit is
`04-computation/lrc_r5_common_child_middle_duality_flint_audit_thm3534_20260816.py`.
It imports no primary computation, delegates the algebra to FLINT, and uses a
cached seven-orbit window lattice.  Normal and optimized transcripts match
the stored output.  Script, output, and semantic hashes are

~~~text
50496905b49dce05ba7ac76bcd7714a646c1e5821b04300ec7f93069d52d5904
c808cfe4e5e8847b355671ac703ee185da6428dca1e449a3c3837f25193eb474
036823a7ba481528a3d02f9b36cecc08c2a76b796d69b437ac644ea62251c53e.
~~~

THM-3534 remains reserved pending the external hostile audit of quotient
variance, map typing, and scope.  The finite-reproduction gate is now clean,
but that does not by itself promote a bridge-shaped statement.  This synthesis
constructs no chronology, physical word-current, C4 closure edge, D5 flux,
Jacobian counterexample, row exclusion, or LRC(14) proof.  LRC(14) remains
**OPEN**.
