# The deepest-source present wall is a one-target projection artifact

> **STATUS: PROVED-ELEMENTARY + VERIFIED-EXACT; independent audit pending.**
> On the canonical typed row, restoring the second lawful target coordinate
> turns the global `E3`/present disjointness into `936` positive semantic
> sections, and their rational target profile has every nonzero deepest-target
> character.  This is a target-active semantic mass current.  It is not yet an
> affine half/`C_221` cycle, private-unit current, row exclusion, or proof of
> LRC(14).

## 1. Inheritance and the missing coordinate

The closest proved positive mechanism is the full two-target action of
THM-2365/2407.  In the canonical target basis it is

```text
eta    = e_c2-e_q1,
lambda = e_c3-e_q2,                                      (1)
```

with coordinates `(s,t) in F_13^2`.  The hostile mechanism is THM-2720:
the later present packet `F_(ell,s)` is actually the slice `t=0`, so it
contains unshifted `c3`-safe while the exclusive deepest source `E3`
contains unshifted `c3`-danger.  Their intersection is definitionally empty.

The least-used sidecar is therefore not another root or affine phase.  It is
the discarded target coordinate `t`.  Write `d_v(theta)` for the ordinary
width-`1/14` danger comb of speed `v` centred at `theta`,
`g_v(theta)=1-d_v(theta)`, and `C_H` for the distinct width-`1/7`
guard-safe set.  The full source-one present section is

```text
F_(ell,s,t)
 = d_c1(ell/7)
   * 1_C_H * product_(i=3)^5 g_qi(0)
   * g_q1(-s/13) * g_c2(s/13)
   * g_q2(-t/13) * g_c3(t/13).                           (2)
```

The signs in `(2)` are exactly the two graft differences in `(1)`.  This is
the inherited source-deleted `U_(s,t)` packet with the shifted owner probe
`d_c1(ell/7)` reinserted.  At `t=0`, its last factor is the wall in THM-2720.
At `t!=0`, that factor is shifted lawfully; restoring `t` deletes or relabels
no additional factor relative to this inherited source-deleted packet.

## 2. The complete source/present compatibility matrix

Let

```text
A0 = guard-safe intersect all five ordinary-unit safeties,

E_j = A0 intersect d_cj(0)
             intersect product_(i!=j) g_ci(0).             (3)
```

The old one-target section has the exact global compatibility law

```text
E1 intersect F_(ell,s,0) != empty  iff ell=0;
E2 intersect F_(ell,s,0) != empty  iff ell!=0 and s!=0;
E3 intersect F_(ell,s,0)  = empty  for every ell,s.        (4)
```

Thus its positive-section counts are respectively

```text
13, 72, 0.                                                (5)
```

The asymmetry is structural.  The visible clock `ell` shifts the deleted
owner `c1`; the first target coordinate shifts the `q1/c2` graft; and the
second target coordinate shifts the `q2/c3` graft.  Freezing the last
coordinate at zero singles out `E3` for annihilation.

Restoring `t` gives the sharp law

```text
E3 intersect F_(ell,s,t) != empty
       iff ell!=0 and t!=0,                               (6)
```

with **no restriction on `s`**.  Hence exactly

```text
6*13*12=936                                               (7)
```

of the `7*13^2=1183` sections are positive.  On the integer `T`-grid their
unnormalized masses range from

```text
242609986080 at (ell,s,t)=(1,9,1)

to

1362887826300 at (ell,s,t)=(3,0,2).                       (8)
```

This simultaneously supplies the hostile `t=0` slice and `936` changed-
coordinate positive controls.

## 3. Every repaired section reaches the genuine terminal fork

Source compatibility alone would not reconstruct the THM-2305 handoff.  Let

```text
Q=Q_(3,{1,2})
 =A0 intersect d_c1(0) intersect d_c2(0) intersect g_c3(0). (9)
```

For every triple in `(6)`, the exact prefix-correlation identity computes

```text
H_(ell,s,t)
 =measure(E3 intersect F_(ell,s,t) intersect D^(-6)Q).    (10)
```

All `936` values are positive.  The exact range is

```text
114819491/12545122758259
 <= H_(ell,s,t) <=
672887730/12545122758259,                                 (11)
```

with the same minimizing and maximizing labels as `(8)`.  A strict rational
witness in the first positive section is

```text
(ell,s,t)=(1,0,1),
x=15991693680925/100360982066072,
D^6 x=3179229/20792408 in Q.                              (12)
```

Thus the full two-target present object attaches to the actual ordinary
`E3 -> D^6 -> Q_(3,{1,2})` semantic cospan, not merely to an isolated source
point.

## 4. The repaired mass is target-active

Fix `ell!=0` and `s`.  The rational thirteen-vector

```text
t |-> H_(ell,s,t)                                        (13)
```

is zero at `t=0` and strictly positive at every `t!=0`.  It is therefore
nonconstant.  If one primitive Fourier coefficient vanished, its rational
coefficient polynomial of degree at most twelve would be divisible by
`Phi_13=1+X+...+X^12`; all thirteen entries would then be equal, contradicting
the sharp zero.  Galois conjugacy consequently gives

```text
sum_t H_(ell,s,t) zeta_13^(tau t) !=0
       for every tau in F_13^*.                           (14)
```

There are `6*13*12=936` nonzero marked coefficients in `(14)`.  Summing
`H_(ell,s,t)` over every `(ell,s)` preserves the same zero/positive profile,
so all twelve primitive deepest-target characters also survive after that
aggregation.  This is exactly the character direction erased by the
one-target `t=0` projector.

The printed aggregate profile also obeys the exact reflection law

```text
A_t=A_(-t).                                               (15)
```

Consequently its primitive coefficients are real and occur in equal
`tau,-tau` pairs.  The current is target-active but does not orient the two
conjugate target directions.  A separately oriented physical deck character
cannot simply be identified with `tau`: the all-depth physical/target
transversality obstruction still applies.

The resulting object is a nonnegative semantic mass table before Fourier
transform and a nonzero complex deepest-target current after transform.  It
does not rely on treating a packet address as a Fourier relation index.

## 5. What changed in the live problem

The previous live board contained:

```text
positive E3 scalar cospan;
canonical present packet disjoint from E3;
physical atom macro with no target action;
spectral target dipole with no lawful left carrier.       (16)
```

The full target sheet connects the first two entries and supplies a lawful
target direction:

```text
E3 semantic source/word
  + full F_(ell,s,t) present sheet
  -> positive two-target mass H_(ell,s,t)
  -> every nonzero deepest-target character.              (17)
```

What remains is narrower.  The table `(10)` has not been intersected with
the half/`C_221` affine-cycle states, the THM-2712 inner physical triangle,
or a common rail/carry/private-root/primitive-unit cospan.  Nor has its target
character been paired with the left relation residue required by THM-2334.
The next test should retain `(ell,s,t)` while rebuilding those sidecars;
projecting to `t=0` first is now proved to destroy the desired source.

## 6. Exact reproduction

Run normally and with optimization:

```bash
python3 04-computation/lrc14_two_target_present_semantic_attachment_probe_20260728.py
python3 -O 04-computation/lrc14_two_target_present_semantic_attachment_probe_20260728.py
```

Both modes byte-match the stored output

```text
05-knowledge/results/lrc14_two_target_present_semantic_attachment_probe_20260728.out.
```

The LF-normalized SHA-256 values are

```text
script  d64a9e52db49c4ce4e391119d45e593453cbb3081c36ca2ae24d81bd7ab56b8f
output  d7159343e91b593d4be670cd7a53b89b5423ea077f78fc038eb7766c43939c03
```

The companion pins the audited canonical interval implementation, rebuilds
all three sources and the terminal fork, scans the complete `1183`-section
target sheet, applies the exact `D^6` prefix identity, constructs a strict
rational witness, and reduces every primitive target coefficient in the
power basis of `Q(zeta_13)`.  It uses no truth-bearing Python `assert`.

No affine-cycle attachment, private-unit current, all-row statement, row
exclusion, or LRC(14) conclusion is proved.
