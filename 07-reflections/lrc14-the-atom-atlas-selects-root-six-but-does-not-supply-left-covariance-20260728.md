# The atom atlas selects root six, but does not supply left covariance

**Status: FINITE-EXACT + VERIFIED in one exact cyclotomic field, with normal
and optimized replays byte-matching.**  The
seven-clock/five-tooth semantic address atlas has a unique maximal first-inner
Fourier-energy pair `h=+/-6`, and the physical orientation can select `h=6`.
At the matching marked triangle, the canonical THM-2625 current retains all
`169` endpoint-target marginals and the `3042`-midpoint atom macro contributes
a nonzero coherent cospan multiplier.  This is a tensor-product
nonannihilation theorem, not a semantic-current intertwiner, a packet/target
identification, chronology, a covering-row result, or LRC(14).

## Inheritance pass and typing target

The closest proved physical mechanism is THM-2712: the full frozen THM-2680
following atom occurs at exactly the `304` THM-2707 addresses `n=0 mod13`, and
its root-neutral physical dynamics descends through `n=13m`.  The based-macro
probe sharpens these `304` reduced addresses into thirty-five consecutive
toothpicks, five near each of seven reflected clock centres, and proves that
every ordered pair of atom endpoints has all `3042` transit packets as
two-step physical midpoints.

The closest spectral mechanism is THM-2625.  On the same typed speed row

```text
W=(1,14,27,40,53,66,13,13^3,2*13^5),
```

its separately descended left and right endpoint factors give a full
`13^2 x 13^2` endpoint current at one marked triangle.  The canonical hostile
is the physical/target transversality lemma: a physical diagonal deck
translation is not a THM-2350 target anti-diagonal dipole at any 13-adic
depth.  The corrected near miss is the full-packet pivot scout: moving right
characters exist everywhere, but pivot profiles fail to transport on more
than 96 percent of atom-to-transit edges, and a one-sided row cannot determine
left covariance.

The precise question here is therefore narrower than “does the atlas provide
the missing endpoint index?”  It does not.  The useful question is:

```text
Can its tooth/clock geometry choose a physical Fourier mode at which an
already well-typed, separately covariant fixed-X spectral current survives,
and can the two-step macro carry that mode without coherent cancellation?
```

The answer is yes, with an exact boundary.

## 1. Full address spectrum is the opposite of covariance

Write the semantic addresses as `n=13m` and let

```text
A subset Z/(13^5),                         |A|=304.       (1)
```

There are two immediate all-depth consequences of

```text
304=5 mod13.                                               (2)
```

First, `A` has no nontrivial translation stabilizer.  Every orbit of a
nonzero translation in a cyclic 13-group has cardinality divisible by
thirteen.  If `A+d=A`, its cardinality would therefore be divisible by
thirteen, contrary to (2).  The companion also checks all `304` possible
candidates `d` (zero belongs to `A`) and recovers exactly

```text
Stab_trans(A)={0}.                                         (3)
```

Second, the indicator of `A` has **full 13-power Fourier support**.  Let
`chi` be any nontrivial character, of order `13^r`, and let `P_chi(T)` be the
integer polynomial obtained by collecting the address exponents modulo that
order.  If `P_chi(zeta_(13^r))=0`, then

```text
Phi_(13^r)(T) divides P_chi(T),
```

so evaluation at one forces

```text
13=Phi_(13^r)(1) divides P_chi(1)=304,                    (4)
```

again impossible.  Thus every address character survives.

Equations (3)--(4) are complementary, not contradictory.  Full spectral
support measures the atlas's complete lack of translational regularity.  It
does **not** make the atlas a cyclic endpoint orbit.  In particular, the
atlas cannot itself supply the missing left `C_13` covariance: there is no
nontrivial translation action preserving its vertex bank.

## 2. The first descended histogram canonically selects the root-six pair

The first inner quotient has exact residue counts

```text
c=(23,27,22,27,22,23,22,23,23,23,23,23,23).              (5)
```

Its `AGL(1,13)` stabilizer is the identity.  For a nontrivial thirteenth root
`z`, the constant baseline `23` cancels and the Fourier coefficient becomes

```text
D(z)=4z-z^2+4z^3-z^4-z^6.                                (6)
```

Put `x=z+z^-1`.  An exact Laurent multiplication gives

```text
|D(z)|^2=g(x),
g(x)=-4x^5+x^4+12x^3+14x^2-8x+1.                        (7)
```

The six real conjugates `x_h=2 cos(2 pi h/13)`, `h=1,...,6`, are the roots of

```text
f(x)=x^6+x^5-5x^4-4x^3+6x^2+3x-1.                       (8)
```

This follows directly by pairing the terms in `1+z+...+z^12=0`.  The
companion gives six disjoint rational sign-changing intervals for (8), so
they contain all six roots.  Exact interval evaluation of (7) proves

```text
|D(zeta^6)|^2 > 16511942539/156250000 > 105,

|D(zeta^h)|^2 < 31577/800 < 40,
                       h=1,...,5.                         (9)
```

Therefore the unique maximal Fourier-energy pair is

```text
{h,-h}={6,7}.                                             (10)
```

This is not numerology extracted from a floating plot.  It is a rational
root-isolation certificate.  The selected pair agrees, up to orientation,
with the frozen following atom's deep root `r=6`.  Energy alone cannot choose
between conjugates, but the oriented physical lift does.

## 3. The oriented selector frequency

An address `n=13m` moves the packet coordinate by `7n/13^6`.  Choose

```text
X=12*13^4=342732.                                        (11)
```

Then the Fourier translation phase is

```text
7X(13m)/13^6 = 6m/13                    (mod 1),          (12)
```

because `7*12=84=6 mod13`.  Thus (11) selects the oriented character `h=6`,
not only its energy pair.

Retain THM-2625's deepest multiplier `m_deep=1` and deepest target speed
`c3=2*13^5`.  The marked triangle is

```text
(X,m_deep,Y)
 =(12*13^4,1,(12+26)*13^4)
 =(342732,1,1085318).                                    (13)
```

The right endpoint has the same address phase:

```text
7Y(13m)/13^6 = 6m/13                    (mod 1).          (14)
```

Equations (12)--(14) are the phase-matching mechanism.  The deepest-speed
offset changes the exponent by an integer on the congruence-locked atom
bank.  Notice the scale shift: the original THM-2625 control used valuation
one frequencies, while the atlas's first-inner selector naturally lives at
valuation four.

## 4. The fixed-X spectral current survives at the selector

The companion rebuilds THM-2625's complete separately allocated current at
(13), rather than assuming survival from the old frequency.  It uses the
same exact Boolean interval algebra, target quotient representatives, signs,
and normalizations.  In the certified field

```text
p=352341050142921841,
```

with the already Lucas-certified exact cyclotomic embedding, it obtains

```text
support(P_ell)=169,                 support(Q_ell)=169,
support(Lstar)=169,                 support(Rstar)=169,
support(Anum)=169.                                        (15)
```

The separate `P/Q` quotient-gauge descent is rechecked on hostile
representatives at the new frequency.  A nonzero finite-field image proves
the corresponding characteristic-zero cyclotomic integer nonzero, so one
certified embedding is sufficient for every assertion in (15).  This is not
a probabilistic modular test.

Thus all `169` coarse target marginals of the canonical marked current
survive at the physical root-six selector frequency.

## 5. The two-step macro gives a coherent nonzero cospan multiplier

Let

```text
S_6=sum_(m in A) zeta_13^(6m).                            (16)
```

Equation (4) already proves `S_6` and `S_-6` nonzero.  Every ordered pair of
atom endpoints has exactly `3042` physical transit midpoints.  The two edge
gains telescope, so their phase depends only on the endpoints and not on the
chosen midpoint.  Summing the complete physical cospan therefore contributes

```text
M_atom=3042 S_6 S_-6 =3042 |S_6|^2 >0.                  (17)
```

In the exact field, the probe finds

```text
S_6   =305773570255157360,
S_-6  =177741545403661698,
M_atom=152256481181880920 !=0.                           (18)
```

Multiplying each of the `169` surviving target marginals in (15) by (17)
kills none of them.  This is the promised fixed-marked-`X` spectral cospan:

```text
(atom endpoint) <- (3042 physical two-step paths) -> (atom endpoint)
       tensor
(separately descended THM-2625 left/right marked current at fixed X). (19)
```

The atlas is therefore not a source of cancellation.  More strongly, its
first inner residue geometry picks exactly the deep-root pair, and the
physical macro transports that character coherently while an actual target
current remains fully supported.

## 6. Exact boundary and next decisive attachment test

The positive bridge in (19) is a tensor product of two proved objects sharing
the same typed speed row and a phase-matched frequency.  What is still absent
is a theorem identifying their **geometric endpoint layers**.  THM-2625 uses
the canonical non-cover word `{a}` and clock two; THM-2712 uses the frozen
THM-2680 semantic following atom on the common cylinder `I`.  The calculation
does not prove that the THM-2625 endpoint functions restrict nontrivially to
that cylinder, that a physical macro edge intertwines their separate target
twists, or that the resulting current belongs to a covering row.

Nor has a packet address become a target residue.  The root-six character in
(12) is a physical deck character.  THM-2350's target character remains a
paired blocker--graft Fourier-index difference, and the all-depth
physical/target transversality no-go remains intact.  The left covariance in
(15) comes from THM-2625's factorwise spectral construction; equation (3)
proves the atlas cannot supply it.

### 6.1 Type map to the restored lawful two-target semantic sheet

A concurrent exact probe repairs the old deepest-source present wall by
restoring the full lawful target sheet `F_(ell,s,t)`.  For every
`ell!=0`, `t!=0`, and every `s`, its semantic mass

```text
H_(ell,s,t)
 =measure(E3 intersect F_(ell,s,t) intersect D^(-6)Q_(3,{1,2}))       (20)
```

is positive.  Consequently every primitive `t`-character survives.  This is
highly compatible with the selector result, but the compatibility is a
fibre-product statement, not an equality of the two cyclic labels.

The exact dictionary is:

| object | primal variable | dual label | mathematical type |
|---|---|---|---|
| atom atlas | `m mod13` | `h_phys` | descended physical deck character |
| lawful sheet | target twist `t` | `tau` | endpoint dipole residue `lambda.(u-v)` |
| THM-2625 | target twist coordinate | `q_b` | full target residue `m_deep+lambda.(u-v)` |
| marked current | circle variable | `X` | physical Fourier frequency |
| following atom | packet tooth/root | `r=6` | semantic packet label |

Here

```text
lambda=e_c3-e_q2
```

is exactly THM-2625's second target representative `V2`; that part of the
typing agrees canonically.  With `m_deep=1`, THM-2365's target law gives

```text
q_b=1+tau.                                               (21)
```

Thus the new sheet's `tau=6` coefficient and the selector-frequency current's
`q_b=7` cell lie on the same lawful target axis, and both are separately
nonzero.  If one instead wants the full target label `q_b=6`, use `tau=5`;
the repaired sheet has every primitive `tau`, and (15) has every `q_b`.
Fourier-sign conventions can replace both labels by their negatives, without
changing the statement.

By contrast, `h_phys=6` is not `tau=6`.  It is a character of the descended
deck coordinate, while `tau` is a character of the blocker--graft twist.
The physical/target determinant-unit lemma proves that these two cyclic
directions intersect only at the identity at every 13-adic depth.  Their
common numeral records a possible **bidegree** `(h_phys,tau)=(6,6)`, not a
diagonal identification.  The semantic root `r=6` is a third type again.

There is also a fixed-frequency mismatch.  Equation (20) is an integrated
physical mass.  Its nonzero `t`-Fourier coefficient does not retain the
physical frequency `X`; it can be a sum of many fixed-`X` endpoint
coefficients.  The present probe proves survival at the single
`X=12*13^4`, but has not attached that coefficient to (20).  Nonzero
marginals on the two sides do not prove their joint fibre nonempty or their
joint coefficient nonzero.

The exact missing fibre product can now be named.  One needs separately
target-covariant endpoint factors on the **actual** semantic section,

```text
P_(a,ell,s,t)(X,m_deep),
Q_(c,ell,s,t)(Y),                    Y=X+m_deep*c3,       (22)
```

with `a,c` semantic atom addresses connected by the physical two-step macro.
They must retain, simultaneously,

```text
X=12*13^4,
h_phys=6 on (a,c),
tau=lambda.(u-v) on the t sheet,
q_b=m_deep+tau,
the E3 -> D^6 -> Q_(3,{1,2}) semantic attachment.         (23)
```

Only then may one form the joint coefficient

```text
sum_(a,c) chi_6(c-a)
  sum_t zeta_13^(tau t)
    P_(a,ell,s,t)(X,1) conjugate(Q_(c,ell,s,t)(Y)),        (24)
```

with the `3042` midpoint multiplicity inserted after the phase telescopes.
The lawful sheet proves the `t` marginal of (24) is not structurally empty;
the present probe proves its fixed-`X` address/spectral marginal is not
structurally zero.  Neither proves the fibre-product coefficient (24).

The cheapest next test is now sharply typed:

1. fix one semantic address and the common open cylinder `I`;
2. attach the actual THM-2625 left and right Boolean endpoint layers at the
   selector triangle (13), rather than tensoring independent objects;
3. test a single separately descended target marginal for nonzero overlap;
4. only after that, test whether one physical two-step macro transports both
   endpoint twists with the phase law (12)--(14).

A positive result would construct the missing semantic-current intertwiner.
A failure would locate it at endpoint attachment or covariance, rather than
at Fourier cancellation, pivot selection, recurrence, or physical
connectivity.

## Reproduction

Run

```bash
python3 04-computation/lrc14_atom_atlas_root_six_spectral_cospan_probe_20260728.py
python3 -O 04-computation/lrc14_atom_atlas_root_six_spectral_cospan_probe_20260728.py
```

Both modes byte-match

```text
05-knowledge/results/lrc14_atom_atlas_root_six_spectral_cospan_probe_20260728.out
```

The companion uses `require()` throughout; optimized mode cannot erase the
validity gate.  LF-byte SHA-256 values are

```text
script  fdbd62c3b96c695ebefd84871396860979ae1f7134b4e181383cbc42c7e191ac
output  07b5ba7cace0156b1ef547f819f1c126ebbf34c12ebe38e6bf5f14f999a169c2
```
