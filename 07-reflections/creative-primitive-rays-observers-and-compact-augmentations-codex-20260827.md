# Primitive rays, complete observers, and compact augmentation — 2026-08-27

**Status:** CURRENT SYNTHESIS OF PROVED AND FINITE-EXACT RESULTS. This is a
research map, not an additional theorem. LRC(14), the planar Jacobian
conjecture, Dixmier(2), Rule-30 prizes, and all external conjectures remain
open unless a cited theorem says otherwise.

## Portfolio, inheritance, and concept board

The session kept the required Anchor / Niche / Wildcard portfolio.

- **Anchor — LRC(14).** Closest proved mechanism: THM-4266's learned
  rank-eight carrier and THM-4270's primitive-ray common decks. Canonical
  hostile: the former unique top pair `(520,688)`, where the frozen carrier
  misses two exact nine-bodies. Corrected near miss: carrier failure means
  only failure of that certificate, never danger for the underlying set.
  Least-used sidecar: the full missed-body incidence hypergraph, rather than
  just the first failed body or a scalar activity count.
- **Niche — planar JC(2).** Closest mechanism: THM-4264's visible two-edge
  attachment observer and THM-4272's rank-twelve fat contact. Canonical
  hostiles: a same-parity observer, coalesced point values, and arbitrarily
  deep transverse perturbations with simple special roots. Corrected near
  miss: density or generic injectivity does not recognize a zero predicate
  after specialization. Least-used sidecar: complete fibre partitions and
  normal Hasse coefficients.
- **Wildcard — finite observer and transport calculus.** Closest mechanism:
  THM-4263's moving-graph jets and normalized fibre weights. Canonical
  hostiles: `u-f` after `u=f`, nonmonic recurrences, bad resultant primes,
  modular wraparound, and heavy fibres. Least-used sidecar: the pair-difference
  set `W-W`, which identifies exactly what a quotient must separate.

The live concept board was:

```text
primitive scale; missed-body incidence; response quotient; set-cover dual;
complete observer; confluent/Hasse sample; normalized fibre weight.
```

Each new object was compared with every board coordinate. Connections were
promoted only where a source, target, map, preserved predicate, lost
information, and decisive hostile could all be named.

## Exact advances

### 1. Four primitive outsider rays

[THM-4270](../01-canon/theorems/THM-4270-uniform-four-primitive-outsider-rays-common-deck-closure.md)
constructs one frozen 8,192-mask carrier and ratio-specific common decks for
the primitive rays `3:5`, `7:8`, `8:9`, and `11:12`. Literal joint-wall audits
cover every one of the `14,307,150` bodies at every declared scale. Exact
overlap accounting removes 146 then-current edges and, at that stage, leaves
residual `177,323` with unique top `(520,688)`.

The useful abstraction is not that a primitive ratio is an ordinal label.
It is that quotienting `(q,r)=g(a,b)` preserves the primitive wall geometry
only when the discarded scale `g` is retained as an activation sidecar. A
ratio name alone is not a common deck.

### 2. Historical one-atom control

[THM-4273](../01-canon/theorems/THM-4273-endpoint-542-732-minimum-one-atom-carrier-augmentation.md)
classifies the historical `(542,732)` boundary. Its frozen active deck misses
two bodies; all `125,970` common-disjoint candidates are checked, and exactly
`2,172` active masks complete the deck. Zero additions fail and one succeeds.

This control supplied two lessons used later. First, “minimum” must be typed:
it is minimum augmentation relative to a frozen deck and repair language, not
a globally minimum proof. Second, the whole completion family is more useful
than a canonical least witness because its support, transversals, exchange
graph, and transfer failures are new mathematical coordinates.

### 3. Complete observer calculus

[THM-4274](../01-canon/theorems/THM-4274-confined-confluent-observer-fibre-and-density-transport.md)
separates several operations that had repeatedly been conflated. For a finite
candidate set with complete observer image `W`, a linear quotient preserves
the complete fibre partition exactly when

```text
ker(lambda) intersect (W-W)={0}.
```

Only when zero belongs to the complete image does the quotient's zero test
have the intended meaning. Monic recurrence prefixes, cleared Bezout
identities, rectangular normal-Hasse samples, fixed-basis modular boxes,
total codomain capacity, and normalized fibre weights each receive their own
sharp gate. Uniform integrability is the exact sequential condition for
transporting every negligible target family.

The negative boundaries matter as much as the theorem:

- specialization vanishing is not coefficientwise vanishing;
- a bad resultant prime permits degeneration but does not force it;
- `q^r` is a capacity only when the full codomain really is the declared
  `r`-fold alphabet;
- bounded average fibre size does not replace uniform integrability;
- an observer with equal fibre sizes may still merge zero with nonzero data.

### 4. Opposite-parity JC attachment observers

[THM-4275](../01-canon/theorems/THM-4275-opposite-parity-attachment-observers-and-confluent-sample-matroids.md)
proves that, on all 1,512 inherited visible JC incidences, every pair of
attachment samples of opposite parity has exactly the same fibres as the full
twelve-edge word. All 36 unordered pairs work, including gaps three and nine;
parity orbits, not generation of `Z/12` by the gap, drive the proof.

The ambient `F4^3` sample matroid is controlled by represented residue classes
modulo four, while the visible `F4^2` matroid is controlled by represented
parities. A one-edge visible observer has four equal fibres but its zero fibre
contains three nonzero words, an exact warning that measure transport does not
imply predicate transport.

At THM-4272's fat contact, twelve generic Fourier point evaluations fall from
rank 12 to rank 1 when their sample points collide. Twelve Hasse coefficients
retain rank 12. The family

```text
D_N=t-t0,    E_N=t-t0-W^N
```

shows that simple roots on the special fibre impose no uniform transverse jet
depth. This identifies the next JC obligation: an effective normal
contact-order bound or an off-fibre dictionary, not another point-value
sample. No new incidence is excluded; JC(2) and DC(2) remain open.

### 5. Compact one-atom classification at `(520,688)`

[THM-4278](../01-canon/theorems/THM-4278-endpoint-520-688-minimum-one-atom-carrier-augmentation.md)
turns THM-4266's former stopping row into a complete response classification.
The 8,319-mask carrier has 5,934 active masks and misses exactly two bodies.
Their union has eleven labels, leaving `binom(19,8)=75,582` common-disjoint
candidate repairs. Exactly 72 are active, with FNV `ed1bfbaf6eaa06a3`; these
are precisely every possible one-atom completion. The least is

```text
{8,40,42,63,80,84,120,143},
mu=1559831620541/24511557965895>4/63.
```

The 72-mask exchange graph is connected with 318 edges and label-transversal
number two. Neither gcd, parity count, nor sum modulo fourteen classifies the
family. At `(416,704)`, however, the analogous `5,872`-mask family splits
`5,871+1`; the isolated active mask's best one-swap neighbor is already below
threshold. Thus exchange connectivity is a local empirical coordinate, not a
theorem schema.

All 72 masks are absent from THM-4271's full 5,398-mask discovery prefix and
therefore from its 199 novel masks. The compact witness and learned prefix are
genuinely distinct certificates for the same seed. THM-4271 already owns the
seed and its full 2,419-edge descent, so THM-4278 contributes zero current
deletions.

### 6. Incoming minimum-six theorem as signal

During the session,
[THM-4271](../01-canon/theorems/THM-4271-fourth-round-learned-carrier-endpoint-descent.md)
arrived with the 2,419-edge descent to the two endpoint-671 hostiles. Then
[THM-4276](../01-canon/theorems/THM-4276-six-atom-endpoint-671-augmentation-and-one-layer-descent.md)
promoted the next step: its 27 row-labelled failure obligations admit 330
nonempty response patterns across all 5,852,925 repairs. A six-obligation
packing proves that no repair can pay for two selected obligations, while a
matching six-pattern cover proves sufficiency. The resulting carrier closes
both endpoint-671 rows and 161 of 163 endpoint-670 rows. The current exact
residual is `174,741`, topped by `(256,670),(384,670)`.

The important extension is conceptual and exact. Given a frozen active deck,
let `F` be its uncovered bodies and map every eligible repair `R` to the subset
`C(R)` of `F` that it covers. Minimum augmentation is exactly set cover on the
response quotient. THM-4278 is the case where 72 repairs have response `F`
and the optimum is one; THM-4276 is the case where a packing/cover dual pair
certifies optimum six. This quotient preserves the closure predicate but
forgets margins and pairwise transfer, so every selected mask must be rescanned
on every new arithmetic fibre.

### 7. Incoming four-channel signal and the integral three-channel sharpening

Late in the session, [THM-4279](../01-canon/theorems/THM-4279-four-channel-formal-log-hasse-observer-for-e0-hom-at-fat-contact.md)
arrived with a clean formal-log reformulation of THM-4272's special-fibre jet
obstruction. After translating the target value, coefficients in degrees
`1,2,4,7` recover the complete chosen-embedding complexified Hom class. The
first response was to audit the explicit lattice rather than treating the
complex dimension count as the last word.

That audit produced
[THM-4280](../01-canon/theorems/THM-4280-integral-three-channel-fat-contact-observer-and-sharp-five-jet-bound.md).
On the exact basis `M=O<u,f,g,h>` with
`2h=v+omega^2 f+g`, the normalized rows are

```text
       [ 0  1  -kappa  (omega^2-kappa)/2 ]
       [ 1  0     0               0       ]
       [ 0  0     0              1/2      ]
       [ 0  1   kappa  (omega^2+kappa)/2 ],
```

whose determinant is `kappa`. This confirms the four-channel theorem after
complexification. The unexpected extra structure appears before base change.
For

```text
A=p+omega^2 d/2,                 B=q+d/2,
c_1=A-kappa B,                   c_7=A+kappa B,
```

the field separation `kappa notin Q(omega)` forces either hidden channel to
vanish only on `O u direct-sum O v`. Channels `2,4` are the two visible
coordinates there. Consequently `{1,2,4}` and `{2,4,7}` are exactly the two
minimal inherited arithmetic observers; every pair has an explicit hostile.

This is a useful non-generic phenomenon: one exact algebraic coefficient can
carry two coordinates over a smaller field. It is also deliberately fragile.
After adjoining `kappa`, the vector `kappa f+g` becomes a pure degree-seven
hostile to `{1,2,4}`, so THM-4279's four-channel complex-linear minimum remains
correct. The source, target, base field, and lattice sidecar are all essential.

The same calculation improves the actual ramification spectrum from the
complexified possibility set `{1,2,4,7}` to the exact integral set
`{1,2,4}`, attained by `f,u,v`. Therefore global morphisms are faithfully
determined by restriction to `5Q`, and `v` proves that `4Q` is insufficient.
On the special degree shells `34,42`, channel one cannot vanish: its kernel is
visible and hence has degree divisible by four. Those maps are therefore
unramified at `Q` and nonconstant already on `2Q`. This shortens the honest-map
contact obligation but does not supply the missing raw Keller descent.

Two exact implementations audit the theorem. The primary symbolic route and
an independent standard-library field implementation agree in normal,
optimized, and fixed-hash modes. The latter checks all sixteen active subsets,
all 4,096 length-twelve subsets, and every Eisenstein residue tuple modulo
four. Three independent proof audits found the same kernel, minimal triples,
base-change hostile, and degree-shell corollary.

## Cross-frontier synthesis

The strongest shared pattern is **complete incidence before quotient**:

| Source | Complete object | Quotient | Preserved target | Lost sidecar |
|---|---|---|---|---|
| LRC frozen deck | body-by-repair incidence | response pattern | deck closure | exact margin and pair identity |
| JC attachment word | twelve sample values | selected samples | full word fibre | parity orbit if chosen badly |
| fat contact | normal Hasse coefficients | coalesced point values | none at collision | multiplicity/jet order |
| actual `E_0` Hom lattice | four formal-log channels | arithmetic triple `1,2,4` | complete Hom class | target value, global-Hom membership, and base field |
| finite map | candidate fibre partition | scalar density | negligible-family transport only with UI | zero predicate |

The table gives a procedural research rule:

1. build the complete finite observer or incidence object;
2. define the exact target equivalence on it;
3. quotient only after computing the pair-difference kernel or response
   fibres;
4. retain whatever margin, jet, weight, or arithmetic-fibre label the
   quotient destroys;
5. attack any apparent convexity or transfer with the cheapest hostile.

This is stronger than an analogy. Each row names the map and the preserved
predicate. It also says where the analogy stops: there is no known
predicate-preserving map from LRC repair incidence to the planar-JC normal
collar, so one cannot transport a theorem between them merely because both
use fibres.

## Negative signals that changed the search

- A frozen carrier's first failure is an address, not evidence of danger.
- A uniform fibre distribution can preserve density while destroying the
  zero/collapse predicate.
- A resultant identifies the only primes where rank may fall; it does not say
  that rank actually falls there.
- Generic point samples are the wrong observer at a collision; confluent
  Hasse data is the lawful replacement.
- Connected active-repair graphs need not remain connected under a change of
  arithmetic fibre.
- A canonical least witness is not a canonical mechanism; the complete
  response family and its set-cover dual expose the reusable proof.
- Tournament language is useful only when an intrinsic pairwise relation
  survives. These repair problems are naturally hypergraphs; forcing their
  multi-body obligations into cosmetic edge orientations would discard the
  very incidence needed for minimality.

## Generated next tasks

### Anchor: LRC(14)

1. At `(256,670),(384,670)`, build the exact row-labelled failure-response
   quotient and search first for a packing lower bound, not a large learned
   prefix. Record which of THM-4276's six repairs survive each fibre.
2. Explain the jumps in active-family structure—transversal numbers two at
   `(520,688)`, three at historical `(542,732)`, and four in the `(416,704)`
   hostile—using exact wall-margin exchange cuts rather than gcd/parity
   summaries.
3. Test whether response-pattern dominance yields a safe antichain reduction.
   Any pruning must preserve pair identity and exact activity margins.
4. Keep the principal open obligation visible: no endpoint carrier currently
   supplies the physical entry from an arbitrary 14-runner counterexample.

### Niche: planar JC(2)

1. Recast the missing raw Keller descent at the degree-`34/42` contact as a
   first-channel preservation problem: THM-4280 shows that an honest map
   cannot lose `c_1`. Any proof must use a saturated raw graph, not merely the
   resolved nodes or cleared equations.
2. Extend the exact channel matrix over a bounded algebraic collar and compute
   the first transverse rank/field-drop stratum. Keep the arithmetic base
   field explicit; the three-channel theorem is not stable after adjoining
   `kappa`.
3. Seek an effective bound on transverse contact order from the actual Keller
   and degree hypotheses. The `E_N-D_N=-W^N` hostile forbids deriving such a
   bound from simple special roots alone.
4. Separate target translation, global-Hom membership, normal lifting, wall
   passage, and off-fibre exclusion. None follows from the special-fibre
   observer by itself.

### Wildcard

1. Search other finite certificate systems in the repo for an explicit
   response quotient plus packing/cover dual certificate. Promote the pattern
   only when a second non-LRC theorem supplies the same mechanism.
2. In p-adic density work, replace specialization-only coefficient tests by a
   finite normal-Hasse observer and audit the complete fibre partition before
   invoking Cartier or density.
3. In Rule 30 finite fibres, distinguish uniform-output density transport from
   recognition of a prize predicate; compute the corresponding pair-difference
   obstruction on bounded block images.

## Close-out truth

The session produced proved abstract observer machinery, three exact JC
observer/matroid refinements, and a complete LRC one-atom classification, and
integrated incoming minimum-six progress. Its sharpest late result is the
arithmetic three-channel theorem: faithful `5Q` restriction for every global
special-fibre morphism and a one-channel `2Q` obstruction on degrees `34,42`.
It did **not** prove LRC(14), the planar Jacobian conjecture, Dixmier(2), any
Rule-30 prize, or any external p-adic-density theorem. The most actionable
frontiers are the response hypergraph at endpoint 670 and a saturated
first-channel descent for the raw `A_23` contact.
