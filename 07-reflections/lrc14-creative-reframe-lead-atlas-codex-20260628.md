# LRC14 Creative Reframe Lead Atlas

This pass deliberately forced the old weird lenses back into the newest exact
packet data.  The useful anchor is the actual-packet sheaf instantiation:
the curated bank has exactly one mixed coarse fiber, and nonunit residue word
kills it while `v2` alone does not.
The incoming HYP-3401 AP-collar exactness scout sharpened the same lesson:
nonunit height is still not enough, because AP and `13->27` share that packet
while `13->27` is strict-open.  Unit-height/flex is the next missing sidecar in
that collar.
Incoming HYP-3402 sharpened the first failure test again: owner-current and
tropical-wall words already split the known mixed fiber, so any new
residue-word failure should be checked against current leakage and
height-wall crossing before being promoted to a new kernel.
Incoming HYP-3403 then turned the same lesson into a shadow-charge gluing
table: index/C3 and `Q(sqrt(-7))` describe the bank, covering residue is the
low-cost separator, `v2` alone is weak, and height is debt for same-residue
moves.

That changes the next proof question.  Instead of asking for a new scalar, ask
where residue-word exactness first breaks when the bank is enlarged.  If it
never breaks on the relevant families, that is a theorem.  If it breaks once,
the first failure names the missing sidecar.

The best new leads after the extra sweep are:

```text
1. residue-word breakpoint theorem;
2. colored-resonance half-boundary sieve;
3. endpoint deletion-cut Menger theorem;
4. rank-one covering-flex Hessian;
5. shadow-charge Farkas ledger.
```

The creative reframes all point at the same missing mechanism.  HYP-3301 says
the first forgotten payload is a cocycle.  HYP-3400 says a scalar shadow must
preserve, transfer, or debt proof charge.  The p-curvature note says a scalar
denominator condition is useless unless the transport operator is retained.
The Haar/Baire note says positive mass should be detected by labelled owner
coefficients, not raw measure.  The colored-discrepancy notes say CRT witness
counts should be bounded by half-boundary resonance rather than raw component
count.  The endpoint/deletion work says a quotient may forget an owner only
after a cut/flow certificate resurrects it.  The actual-packet bank says the
first real ambiguity is a covering-layer residue word.  HYP-3401 says the
next AP-collar ambiguity is a unit-height lift.

The synthesis:

```text
residue word exact locally
  -> enlarge until first failure
  -> first failure is curvature / height flex / endpoint owner / exact period
  -> first try owner-current, tropical-wall, shadow-charge, colored half-boundary, or endpoint cut resurrection
  -> prove any survivor opens positive mass or routes to named debt
```

That feels closer to a rigorous route than the more decorative analogies,
because it is falsifiable.  A next script can either find the first residue
failure or certify that a whole sampled family is residue-exact.

One namespace note: incoming mainline claimed `HYP-3401/T1362/LTI-362/LTT-262`
for the three-coordinate obstruction exactness scout and
`HYP-3402/T1363/LTI-363/LTT-263` for the owner-current / tropical-wall scout.
`HYP-3403/T1364/LTI-364/LTT-264` was then claimed by the shadow-charge
packet-gluing scout.  I left those intact and placed this follow-on atlas
under `HYP-3404/T1365/LTI-365/LTT-265`.
