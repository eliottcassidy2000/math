# Sextic JC as a filtered lifting problem, not a larger arm calculation

**Status: RESEARCH SCOUT + VERIFIED-EXACT CROSS-FRONTIER PROBE.**  This note
records a task-generating mechanism.  It proves no sextic normal-strip
classification, constructs no Russell Darboux pair or finite completion, and
has no consequence for `JC(2)`, which remains open.

The exact companion is
`04-computation/jc2_filtered_kernel_principal_part_connection_scout_20260823.py`
with frozen output
`05-knowledge/results/jc2_filtered_kernel_principal_part_connection_scout_20260823.out`.

## 1. Inheritance and live concepts

The closest proved mechanism is
`THM-3412-hamiltonian-principal-part-differential-and-prufer-torsion-arms.md`:
for a filtered Hamiltonian complex, the natural map

```text
gr(ker D) -> ker(gr D)
```

can have a sharp positive deficit.  Its smallest hostile has one true
length-four arm but graded blocks of lengths three and one.

The canonical target hostile is the now proved
`THM-3871-quintic-normal-strip-keller-pairs-are-automorphisms.md`.  In the
genuine `(5,4)` row, simultaneous cancellation of both original arms leaves a
smooth one-dimensional leading family.  Two lower non-arm buckets integrate
to conserved polynomials and kill that whole family.

The least-used relevant sidecar is
`THM-3404-factorized-danielewski-principal-parts-and-finite-cover-obstruction.md`:
oriented CRT principal parts, including truncated coefficient jets, rather
than unsigned pole orders or divisor classes.

The concept board is:

1. the twelve sextic coefficient buckets;
2. the filtered Hamiltonian deformation kernel;
3. the Rees/associated-graded lifting ideal;
4. branchwise CRT principal-part arms;
5. the alternative-seed recovery packet;
6. the all-`A1` common-infinity completion passport.

## 2. The direct typed mechanism

Fix a rational normal profile `(A,C)` and linearize its Keller equation:

```text
L_(A,C)(dot A,dot C)={dot A,C}+{A,dot C}.                (1)
```

Filter the deformation space in two different ways:

- by transverse normal degree; and
- at each boundary prime `pi`, by pole order in the local parameter.

The proposed target symbol map is

```text
gr ker L_(A,C) -> ker gr L_(A,C).                        (2)
```

For the nonlinear problem, replace `(2)` by the map from the Rees ideal of
the complete bucket equations to the ideal generated by their leading
forms.  A component of the leading ideal is a candidate symbol family, not a
lift, until the lower extension equations have been solved.

| field | exact contract |
|---|---|
| source | filtered Hamiltonian deformations or the nonlinear Rees lifting ideal |
| target | leading normal/pole symbols and arm principal residues |
| map | take the highest normal coefficient and each boundary-prime leading principal part |
| preserved | top Wronskians, Kummer valuation ratios, target `SL_2` gauge, and leading arm cancellation |
| destroyed | lower buckets, integration constants, conserved rows, liftability, and allocation among collided boundary arms |
| required sidecar | filtered kernel with transition maps; nonlinearly, the full Rees ideal plus CRT principal-part packets |
| cheapest decisive test | compute `dim ker(gr)` versus the image of `gr(ker)`, then add the next bucket and saturate every apparent component |

This is a direct mechanism because the target operator is itself
Hamiltonian.  No numerical or thematic identification is being made.

## 3. Exact two-ended replay

The companion independently reconstructs the local THM-3412 control
`f=y,g=y^3,q=2` on

```text
k[y,t]/(y^3,t)^2.
```

It obtains

```text
actual finite kernel:    dimension 4, P-blocks [4],
graded kernel:           dimension 4, P-blocks [3,1],
liftable symbol space:   dimension 2,
graded lifting deficit:  2=(q-1) deg gcd(g,g').           (3)
```

At the target end, put

```text
C_arm=1+X+Y+Z,
A_arm=5X^2+10XY-8.                                      (4)
```

The point

```text
(X,Y,Z)=(1,3/10,-23/10)                                 (5)
```

annihilates both equations in `(4)`.  Their Jacobian has rank two there, so
this is a smooth point of a genuine local one-fold, not an isolated
coincidence.  The two conserved leading rows omitted by the arm observer are

```text
X^3-8XZ-4Y^2=476/25,
Y(3X^2-8Z)=321/50.                                      (6)
```

They reject `(5)`.  On the only remaining balanced leading channel, the
terminal eliminants have resultant

```text
3171942400000=2^23 5^5 11^2 !=0.                        (7)
```

Thus the source deficit and the target false arm component have the same
failure anatomy: taking the visible kernel first deletes the equations that
decide whether a symbol lifts.

The companion also freezes a sextic shallow-observer control:

```text
A=z^6+s,                    C=z^5,
{A,C}=-5z^4.                                             (8)
```

All upper buckets `z^5,...,z^11` vanish; the next extension bucket does not.
This is not a nonliftable-symbol theorem by itself, but it is an exact
warning that an upper-bucket observer is not the Keller predicate.

## 4. What THM-3404 transfers, and what remains proposed

For the literal suspension

```text
R=k[v,X,Y]/(XY-F(v)),
```

THM-3404 proves the direct principal-part model

```text
R_X/R = direct_sum_(q>=1) X^(-q) k[v]/(F^q),             (9)
```

with a CRT jet arm for each irreducible factor of `F`.  Here the transfer is
literal: a sum of Laurent terms is regular exactly when its residue packet in
`(9)` is zero.

The companion replays the sharp cancellation for `F=v(v-1)`:

```text
[1]+[F-1]=0 in k[v]/(F),
CRT([1])=(1,1),                CRT([F-1])=(-1,-1).       (10)
```

Both summands have the same negative orders on the two boundary arms, but
their signed principal parts cancel.  Orders alone cannot decide `(10)`.

Repeated factors require jets, not only values.  For

```text
F=v^2(v-1)^2,                  h=v(v-1),                 (11)
```

the value observer at `0,1` is zero, while

```text
[h]!=0 in k[v]/(F),
(h(0),h'(0),h(1),h'(1))=(0,-1,0,1).                     (12)
```

This exactly justifies a joint coefficient-jet sidecar whenever several
terms are expected to cancel at one boundary divisor.

The scope boundary is important.  No canonical isomorphism has been proved
between the sextic depressed coefficient ring and the suspension in `(9)`.
Nor has an intertwiner been proved between its CRT transitions and all twelve
sextic Hamiltonian buckets.  Therefore:

```text
CRT principal parts: DIRECT on an identified suspension/DVR chart;
CRT for sextic normal strips: PROPOSED SIDECAR pending a chart and intertwiner.
                                                               (13)
```

## 5. Three generated planar-JC tasks

### Anchor: the sextic twelve-bucket lifting atlas

For

```text
A=sum_(i=0)^6 a_i(s)z^i,       C=sum_(j=0)^6 c_j(s)z^j,
```

the twelve exact buckets are

```text
E_m=sum_(i+j=m+1)(i a_i c_j'-j a_i'c_j),   0<=m<=11.    (14)
```

After constant target normalization, Kummer extraction, scaling, and
depression:

1. form the full differential ideal of all surviving buckets;
2. introduce a Rees parameter for normal degree and, separately, for every
   prime of the Kummer scale;
3. compute the leading arm ideal and its components;
4. add conserved rows one at a time and record the first failed implication;
5. compare `gr(ker)` with `ker(gr)` for the linearized complex on every
   surviving component; and
6. retain a transition matrix and CRT residue vector rather than only a
   resultant or pole order.

The desired output is a lifting-deficit table, not another arms-only
classification.

### Niche: alternative seed recovery as a joint module problem

The current artifacts

```text
04-computation/jc2_russell_alternative_quadratic_seed_recovery_scout_20260823.py
04-computation/jc2_russell_alternative_quadratic_seed_recovery_independent_audit_20260823.py
```

are **FINITE-EXACT SCOUTS**, not theorems.  They find no Keller composite:
the hidden-`u` exceptional branch has a nonintegral `u`, while the `x`-only
branch can make `u,w` integral but retains an `x` pole and nonconstant
boundary Jacobian.

The next task should replace “is this one recovered coordinate integral?” by
the joint map

```text
(u,x,w,D,...) -> direct_sum_(boundary pi, pole q)
                 principal_part_(pi,q)(u,x,w,D,...).      (15)
```

Coordinatewise zero in `(15)` is necessary for simultaneous regularity;
cancellation must occur inside each coordinate's CRT jet arm.  If a finite
completion enters, THM-3862 adds a separate global sidecar: the index
form/different of the finite flat algebra.  Its proved nonmonogenicity says a
single recovered generator cannot be the final object.  It does not turn the
FINITE-EXACT seed into a completion or prove existence.

Cheapest test: replay the hidden-`u` and `x`-only exceptional strata as vectors
in `(15)` and check whether multiplying or adjoining one additional candidate
changes the residue span without making the boundary Jacobian constant.

### Wildcard: collided infinity support versus distinct branch arms

THM-3862 proves conditionally that every finite-completion branch has affine
normalization `A1` and that all such components share one target point at
infinity.  THM-3412 supplies the exact warning: distinct roots with the same
observable value remain separate Prüfer arms even though a diagonal
annihilator merges their support.

The target analogue is to retain, for every branch component `Gamma_i`, its
normalization place `p_i` above the common infinity point and the full Puiseux
principal-part arm there.  The common point is only the shared support.

An exact hostile is supplied by the polynomial graph curves

```text
Gamma_d: y=x^d,                   d=2,3,4.               (16)
```

Each has affine normalization `A1` and projective infinity point `[0:1:0]`.
In the local chart `Y=1`, with `u=X/Y,v=Z/Y`, its equation and normalization
are

```text
v^(d-1)-u^d=0,
(u,v)=(t^(d-1),t^d).                                    (17)
```

They have the same tangent ray `v=0`, but

```text
I_infinity(Gamma_2,Gamma_3)=3,
I_infinity(Gamma_2,Gamma_4)=4.                           (18)
```

Thus even common point plus tangent-cochain data loses contact.  The required
sidecar is the branchwise Puiseux/valuation arm and pairwise intersection
matrix.  These graph curves are geometry hostiles only; existing JC graph
no-go theorems prevent treating them as candidate completion branches.

## 6. Other requested signals: typed survivors and rejections

| source and current status | possible map | preserved predicate | destroyed information | required sidecar / cheapest test | verdict |
|---|---|---|---|---|---|
| THM-3410 projective LRC cochain — **PROVED analytic + PROVED-ELEMENTARY + VERIFIED-EXACT; independent audit requested** | send branch first jets at common infinity to projective rays and wedge them | equality/distinctness of first tangent directions | higher Puiseux contact, branch multiplicity, Keller data | full Puiseux arms; hostile `(16)--(18)` | useful first-jet compiler only |
| THM-3756 odd-square ordinal Berggren chart — **PROVED + VERIFIED-EXACT + independently hostile-audited** | use the shared ambient address `T(r-2)+s=T(n-2)+j` to enumerate Pythagorean nodes or JC degree buckets | chosen shell/order and the integer label | native admissibility: at address `8`, Pythagorean `(r,s)=(5,2)` is a primitive hole because `gcd(9,3)=3`, while JC `(n,j)=(5,2)` is genuine and dies by the independent arm residue `3R/8` | retain the decoded pair plus the native gcd/filter and valuation/conserved-row sidecars; address-eight comparison is decisive | scheduler, no JC invariant |
| THM-3413 Hadamard strength filtration — **PROVED + VERIFIED-EXACT + independently audited** | compare a triangular high-interaction transform with bucket elimination | invertible-transform discipline | its subset lattice and repeated-minor zero law do not match anti-diagonal coefficient convolution | prove an explicit intertwiner; same-index Jacobian wedge `i(s( s^2)'-s's^2)=is^2!=0` refutes literal minor transfer | method survives, literal map refuted |
| THM-3511 / THM-3824 Rule 30 — **PROVED + VERIFIED-EXACT + independently audited** | treat increasing normal depth as a moving observer | need for operation closure and honest overflow | no Hamiltonian algebra or branch geometry transfers | next-bucket congruence test; `(8)` is the JC control | research protocol, not reduction |
| THM-2176 knot continuation — **PROVED abstract metric-monoid theorem + cited knot application** | let allowed formal seed changes act as a monoid and take the continuation profile of pole/Jacobian observables | largest operation congruence inside current equality | a finite query dictionary can miss later changes | full principal-part response profile; split equal quadratic jets at the first cubic residual | promising formal wrapper, no finite completeness yet |
| THM-3303 factorial/GMC boundary collision — **PROVED + independently audited** | push a Keller map to its multiplicity-weighted image measure | null moments plus a boundary collision | boundary incidence, sheet multiplicity, branch allocation | boundary/multiplicity passport | supports the need for branch sidecars; no sextic-bucket map |

The Pythagorean address-eight witness is stronger than a mere rank collision:
the *same formula and integer* name an absent primitive node on one side and a
real obstruction bucket on the other.  Thus even a bijective ambient
enumeration does not transport the selected subobject without its native gcd
or filtered-lifting sidecar.  The Pythagorean address and Hadamard transform
are the clearest stopping certificates: attractive discrete encodings do not
preserve the native Hamiltonian operation.  The Rule 30 and knot results
contribute operation discipline, not equations.

## 7. Frontier boundary

What is exact here:

- the THM-3412 finite kernel/graded-kernel replay;
- the THM-3871 smooth arms-only false component and conserved-row rejection;
- the twelve-bucket sextic observer and its shallow hostile;
- simple and repeated-factor THM-3404 CRT cancellation packets;
- the common-infinity graph contact hostile; and
- the ordinal and Hadamard nontransfer witnesses.

What remains open:

- the sextic normal-strip classification;
- a sextic Rees/filtered-kernel deficit theorem;
- a boundary chart intertwining sextic buckets with THM-3404 CRT arms;
- an alternative Keller seed or finite completion;
- the all-`A1` branch Puiseux/contact classification; and
- `JC(2)` itself.

The operative meta-pattern is **test filtration--observer commutation before
scalarizing**.  The counterindication is equally explicit: only a proved
split/semisimple square or target-preserving section allows the leading
observer to replace the filtered lifting problem.
