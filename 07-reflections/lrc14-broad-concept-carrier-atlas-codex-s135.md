# LRC14 Broad Concept Carrier Atlas

This session did the requested wide repo archaeology and then compressed it
back into the current LRC14 spine.  The surprise is that the old niche threads
are not random from the proof's point of view.  They mostly repeat one lesson:
scalar shadows become useful only after the right binary relation has retained
the missing label.

The recent S130-S134 line already says this locally.  `q` is the theorem scale,
`p+q` is the additive side ledger, `p*q` is the incidence/product side ledger,
`2/27` belongs to the `C=27` shell, `3/41` is the first K33 wall, and S134's
visible/hidden relation signature prevents AP, shifted AP, GW, petals, and
near-misses from falling into the same additive-energy bucket.

The older repo concepts now slot around that:

```text
coimage/Yoneda:        keep a conservative probe family, not runner identity
Vitali/Helly:          finite moments can miss p0 collapse
unit distance:         Hamiltonian paths can be geometric or order artifacts
cauldrons/Collatz:     density is blind to correlated residue streams
deck derivatives:      repair projections by boundary labels, not scalars
OCF/forbidden H:       state-lift endpoint must be a realized packet category
octahedron/Clebsch:    support-six and folded masks are Hodge/covariance carriers
totient/Mobius:        exact denominators live on labelled divisor packets
```

The executable atlas ranks the proof carriers by branch retention, typed
visibility, state-lift fit, finite certifiability, anti-scalar guard, and
cross-problem signal.  The resulting tournament is transitive:

```text
bigraded summand/multiplicand signature
> C=27 shell and Yoneda coimage
> Farey/q binding scale
> Vitali wall / anti-Poisson coimage
> Kpq/K33 incidence wall
> Forbidden-H7 state lift
> exact-period Mobius/totient packet ledger
> octahedron L(K4) support-six current
> depth-corrected Bonferroni parity
> OCF as hard-core partition function
```

The role order is not the same as old-repo maturity; an old-maturity-only gauge
would flip `96` carrier pairs.  That is good.  It means the atlas is not just
reciting the classics.  It is asking which old mechanisms preserve the current
LRC14 predicate.

The proof route I would push next is:

```text
q/Farey branch first,
C27 shell or K33 owner incidence second,
visible/hidden relation sign third,
octahedral/Clebsch packet if support-six mass survives,
HYP-2908 state lift or exact-period analytic branch last.
```

The sharpest lemma shape coming out of the scan is:

```text
Every top-balanced non-AP/GW LRC14 residual that survives the peeler has either
a C=27 p=2 shell defect with finite petal/lift discharge, or a sign-visible
three-owner incidence packet whose connected OCF packet evaluates to the
forbidden activity-two value 7.
```

Still not a proof.  But the rooms are better labelled now, and the next door to
try is much less foggy.
