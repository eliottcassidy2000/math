# Independent hostile audit accepts the two-current pointed diagonal bundle

**Status: FINITE-EXACT INDEPENDENT AUDIT ACCEPTS THE SCOPED FIVE-COORDINATE
DIAGONAL-BUNDLE CANDIDATE; STATIC ADDRESS-CONDITIONED PARTITION OF IDENTITY
ONLY; LRC(14) remains OPEN.**  The audit reconstructs the complete
pre-integration tensor without importing
`lrc_r5_two_current_digits_pointed_root_difference_carrier_transition_probe_20260816.py`.
Every requested marginal, rank, obstruction, diagonal bundle, hostile, and
exceptional source fibre agrees exactly.

The accepted object is

```text
T(point=(state,u), r0, r1, s=u-q, relation=t)
```

over the split field of characteristic
`755373809845391722745761`.  It is a finite response table on one common
OWNER base.  Neither the labels nor the coordinate matrices have been typed
as a clock, complete address, arrival ancestry, physical current, row
exclusion, or LRC(14) mechanism.

## Independent construction

The checker hash-pins and imports only two independently audited parents:

1. the exact mod-169 two-current-cylinder source audit; and
2. the pointed-six ordered-root-difference audit.

For each endpoint character `(alpha,beta,tau)`, it independently sweeps the
endpoint segments and records literal ordered root pairs `(u,q)` before any
address expansion.  Contributions are first aggregated by

```text
(exact source cell, u, q),
```

then expanded through the 169 exact `(r0,r1)` half-open source weights.  This
is a different Fubini ordering from the submitted candidate and never reads
its coefficient buckets, tensor, or matrix bank.

One typing detail proved important during the hostile run.  The independent
two-digit parent labels source supports outside the OWNER window, whereas the
candidate's source-profile comparison concerns only OWNER-lawful cells.  The
audit reconstructs that restriction directly:

```text
left  iff 7(left+right) < 2J,
right iff 7(left+right) >= 12J,
```

with the middle chamber discarded and the four lawful support/chamber states
checked against the parent labels.  Without this restriction one obtains a
larger, incorrectly typed exception universe.  With it, the exact reported
60-fibre census follows.

## All three parent marginals pass

At gamma level:

- summing pointed tails within state and summing `s` reproduces all 13 pinned
  alpha blocks of the independently audited two-digit tensor;
- summing pointed tails within state and summing `r1` gives the complete
  one-digit/current-root gamma bank, digest
  `843fe780896a23471f9e844383cc03cbdc70836a138a6addc218e87e319649de`;
- summing `r0,r1` gives the complete pointed/root gamma bank, digest
  `416f03a90894e526bc767a34839c2489aa4cac7051ac04687e0feacfa36d58d2`.

The same three quotient maps pass after exact Fourier inversion.  Three
literal delete-before-transfer controls and four direct 2,197-term inversion
probes independently check the compressed route.  The same-root slice
`s=0` vanishes identically.

The final actual/support/flat/point-difference-flat tensor digests are

```text

```

and match the submitted candidate exactly.

## Carrier ranks and the stationary obstruction

For the actual tensor, the exact global record

```text
(base, one, base union one, two, one union two, (r1 raw, r1 contrast))
= (6,6,6,6,6,(13,12))
```

is reproduced.  Statewise carrier dimensions are `1,2,1,2`, and every fixed
`r0` has parent/child/union ranks `(6,6,6)`.  The support-normalized and flat
hostiles retain the same six-dimensional carrier while changing the `r1`
amplitude ranks to `(4,3)` and `(1,0)`.  Thus the rank-six carrier does not
identify a digit coordinate or a temporal evolution.

No stationary matrix `K_r1` acts uniformly over all `r0`.  At
`r1 in {0,6,12}`, child/union ranks are `(4,10)`; at the other ten residues
they are `(6,12)`.  The first exact residual is

```text
(r1,row,column,predicted,actual)
=(0,2,2041,0,148404725567356333796897).
```

This rejects only the stated static coordinate ansatz.  No temporal process
has been defined, so it is not evidence for or against Markovianity or a
current recurrence.

## The exact survivor: 169 diagonal partitions

For every fixed `(r0,r1)`, the six fixed-`r0` parent rows have rank six and
the child lies in their row space.  Hence a unique matrix

```text
K_(r0,r1) in Mat_6(F)
```

exists.  All 169 matrices are diagonal in the pointed basis, and for each of
the 13 values of `r0`,

```text
sum_r1 K_(r0,r1) = I_6.
```

There are 121 distinct actual matrices.  Their rank and support histograms
both equal

```text
0:35, 2:2, 4:2, 6:130.
```

The 35 scalar matrices are precisely the zero matrices.  Distinct-matrix
counts as `r0` varies at fixed `r1` are

```text
(2,13,13,13,13,13,3,13,13,13,13,13,2).
```

The incoming representation sidecar identifies the six pointed lines as the
six directed arcs of the bidirected path

```text
0 <-> 1 <-> 3 <-> 2.
```

Accordingly, the accepted matrices are six static address-conditioned arc
amplitude splitters.  The path and its alternating `P7` refinement have
`H1=0`; no flux or cohomology class follows from this partition.

## The 60 exceptional source-profile fibres

Before endpoint integration, 954 of the 1,014 actual
`(point,r0,r1)` source profiles are proportional to their one-digit parent.
The other 60 are exactly ten nontrivial `r1` values

```text
{1,2,3,4,5,7,8,9,10,11}
```

in each of six `(point,r0)` fibres:

```text
((0,0),  r0=12),
((1,0),  r0=12),
((1,6),  r0=9),
((3,6),  r0=3),
((3,12), r0=0),
((2,12), r0=0).
```

All 60 output rows nevertheless satisfy the exact diagonal factorization.
Thus the common endpoint operator annihilates their nonproportional source
remainders.  The support-normalized hostile makes all 1,014 source fibres
proportional and still gives the same one-digit pointed parent, while the
point/difference-flat hostile preserves the two-digit marginal but lowers
the base carrier rank to four.  These controls separate source support,
source amplitude, parent marginals, and output carrier containment.

## Checksum repair is metadata only

The tracked LF SHA-256 of the submitted script at `b1baa781a` is

```text
9d1671e0f823fdbaa9ab79915ba05dbb4dda4c6eabb97fe4484baf4c2e3205f2,
```

not the stale `bc872773...` value originally printed in its reflection and
index.  Upstream commit `7f88e61af` repaired that documentation before this
audit landed.  The submitted output digest
`c4d7e859cce2b2e37dfaf506fd722c2f9f97c12ee21352b37d580802c645a555`
and semantic digest
`38725dc1d7129b326634c99bd70e1eb414590dc24fb83bd9522e2095e41f204c`
are unchanged.  This was documentation hash drift, not a semantic or result
failure.

## Reproduction and boundary

```text
python -B 04-computation/lrc_r5_two_current_digits_pointed_root_difference_carrier_transition_independent_audit_20260816.py
python -B -O 04-computation/lrc_r5_two_current_digits_pointed_root_difference_carrier_transition_independent_audit_20260816.py
```

Normal and optimized outputs are byte-identical.  Independent
script/output/semantic LF SHA-256:

```text
d57c6c38207216054e8bc8e191fa20338aa18cab3fce216ff19d593bcc6a80e2
2c39c9cbc6a0acad45f4591724a5a84f09568078575dffe7e7c72b90b588240f
b58d98c283a2dc42111365a3d61af0948757feda3b1ff11ca65cd7d562b15a56
```

Accepted scope: one finite exact five-coordinate response tensor, its three
parent quotient maps, six-dimensional pointed-arc carrier, failure of a
stationary `K_r1`, and the 169-member address-conditioned diagonal partition
of identity.  Still absent: complete or THM-2334 exact address, `U_clock`
chronology, arrival typing, a one-integral temporal current, a nonzero `H1`
class, row exclusion, and LRC(14).
