# The inverse branch transports the endpoint sheet that the clock erases

**Status: FINITE-EXACT SOURCE-TIME ONE-BASE SIMPLE-KERNEL TRANSPLANT;
independent audit pending.**  Refining the left packet by
`f_omega^src=1_Q P^K(e P_omega)` rather than by the arrival-time product
`f P_omega` puts both 39-atom labels at endpoint/source time before they meet
on the one THM-2471 first-collision base.  The source-aligned common gauge
again realizes all 48 owner-active chamber/drift types, rank four at every
owner character, and complete nonzero-owner Walsh--drift spectrum.  Pulling
THM-3514's actual endpoint pair function through this measure is nonzero at
all thirteen owner characters; its doubly centred interaction part is also
nonzero at all thirteen.  This closes the finite temporal **atom-label**
mismatch.  The `AX/BY` values remain preintegrated scalars, however, so this
is not yet a physical relation current, grouped coefficient, row exclusion,
or LRC(14).

## 1. Inheritance: the temporal copy was the real missing coordinate

The relevant proved mechanisms are now unusually specific.

- THM-2471 (31) realizes the first-collision service as a finite Boolean
  fibre product, while its equations (33)--(35) distinguish an arrival atom
  `P_omega(T^Kx)` from a source atom `P_omega(x)`.
- THM-3514 gives the actual 39 U_full guard atoms and an independently
  audited endpoint pair function `E_(omega,nu)` with complete
  `K4 x F_13` spectrum.
- The arrival-labelled common-gauge probe realizes the correct 48 support
  types and full nonzero-owner spectrum on one ancestry base.  Its
  independent audit accepts that scoped statement, but it deliberately does
  not identify its left atom with the endpoint-time atom.
- The first endpoint-weight pullback is nonzero at all owner characters, but
  its labels are still those arrival/source temporal copies.

Thus the cheapest honest transplant is not a new post-marginal channel map.
It is THM-2471's already stated source refinement

```text
e_omega=e P_omega,
f_omega^src=1_Q P^K(e P_omega).                       (1)
```

The label on `f_omega^src` is inherited before the clock, even though the
packet itself is evaluated after it.  Expanding `P^K` keeps the inverse
branch that witnesses this inheritance.

## 2. Hostile theorem: the unlabelled clock has rank three

Before using (1), compute the raw temporal transition between the guard
partition and its `K=2` arrival copy.  The atoms are

```text
I_(a,C)={t:91t in 7a+C},
a in F_13,
C in {[0,1),[1,6),[6,7)}.                            (2)
```

On the common grid of `91*169=15379` half-open cells, let

```text
N_(omega,eta)
 =15379 * measure(I_omega intersect T^(-2)I_eta).      (3)
```

Exact enumeration gives

```text
support(N)=1521/1521,
rank_Q(N)=3.                                          (4)
```

More sharply, for each chamber `C`, all thirteen rows

```text
N_((a,C),-),       a in F_13,                         (5)
```

are identical.  Aggregating over the three chambers gives the constant
`13 x 13` sheet matrix whose every entry is `91`.  Hence every nontrivial
source-sheet character is killed exactly.  Full support is therefore the
wrong statistic: the clock mixes every atom with every atom while erasing
the whole twelve-dimensional nonzero `F_13` sector.

This is a sharp hostile to any direct temporal intertwiner on the 39
unlabelled marginals.  It also explains why increasing a post-marginal rank
never repaired the earlier connection systems.

## 3. The inverse branch restores the erased sheet exactly

Write the `K=2` inverse branch as

```text
t=(x+n)/169,       0<=x<1,       0<=n<169.            (6)
```

Then

```text
91t/7=(x+n)/13.                                      (7)
```

Because `0<=x<1`, the source sheet in (2) is

```text
a_source=floor((x+n)/13)=floor(n/13).                 (8)
```

The high base-13 digit of the retained inverse branch is precisely the
endpoint guard sheet.  The companion checks (8) on all 15379 exact cells.
So the rank-three marginal is not invertible, but THM-2471's full Boolean
stalk never discarded the coordinate needed to lift it.

This is the concrete `7 tensor 13` mechanism in this lane: the 91-grid atom
has a septimal chamber coordinate and a thirteenth sheet coordinate; the
forward `13^2` clock erases the sheet marginal, while its 169-valued branch
sidecar carries that sheet as its high digit.

## 4. Source-time common ancestry retains the full 48 carrier

Apply (1) to the same 39 guard atoms on the left and use `e_nu=eP_nu` on the
right.  Of the 39 labels, 20 are nonempty on each endpoint leg.  Their
first-collision packets satisfy exact pointwise recombination, reproduce

```text
sum M=169 I_5
     =48602521488933856/1996669193081744181,           (9)
```

and retain the atomwise same-root zero.  The complete pair ledgers are

```text
400/1521 atom pairs before the common gauge,
362/1521 atom pairs on the common gauge.              (10)
```

Use endpoint-sheet drift `d=b-a`, collision-root drift `s=u-q`, and the
same common torsor offset

```text
c=a-u=b-q,
s=-d.                                                 (11)
```

Source-time owner support now removes the middle chamber on **both** legs
before the gauge marginal.  Consequently the entire surviving carrier is
already

```text
{L,R}^2 x F_13^*,
48/117 full chamber/drift types,
48/52 owner-active types.                             (12)
```

The four rows have rank four at all five split primes.  At owner character
zero their Walsh--drift supports are

```text
(13,12,12,13),                                        (13)
```

with the two one-sided zero modes absent.  For every nonzero owner
character, retaining `c` restores

```text
(13,13,13,13),                                        (14)
```

and rank four.  Thus source alignment pays no spectral price.

## 5. The actual endpoint pair function survives as a one-base kernel

Reconstruct THM-3514's actual endpoint pair contribution in its certified
split field

```text
p=572252886246508880869.                              (15)
```

It has `676/1521` nonzero atom pairs and total bridge

```text
sum_(omega,nu)E_(omega,nu)
 =389266878372286537904.                              (16)
```

All 362 source-aligned common-gauge pairs lie in that nonzero support.  For
each owner character define

```text
P_k=(1/D) sum_(omega,nu,c)
        M_(omega,nu,c) E_(omega,nu) zeta^(-kc).        (17)
```

Because (1) labels literal summands before integration, finite linearity
rewrites (17) as one simple-kernel integral on the same THM-2471 outer base:
the coefficient of a Boolean summand is the finite lookup
`E_(omega,nu)zeta^(-kc)`.  No equality between arrival and endpoint atoms is
used.

The exact profile is

```text
(250941903326683426761,
 271159198555082027237,
 220352936335239463001,
 501574842116604792361,
 239782533927131609483,
 291441097101636924479,
 178462009264486618414,
 243496486572856498854,
 437482997473219292858,
 199633020257945740472,
 497778147385972258730,
 404483861453645261110,
  43772789859398821019) mod p.                        (18)
```

Every entry is nonzero.

## 6. The positive is genuinely a two-address interaction

Double-centre `E` over its full `39 x 39` address universe:

```text
E^circ_(omega,nu)
 =E_(omega,nu)-rowmean_omega-colmean_nu+grandmean.     (19)
```

Its row and column sums vanish exactly.  Pulling the constant, left-only,
right-only, and doubly centred pieces through the same measure gives support

```text
flat                    13/13,
left-only ANOVA         13/13,
right-only ANOVA        13/13,
doubly centred E^circ   13/13.                        (20)
```

In particular, the endpoint positive is not explained by normalization or
a one-leg atom bias.  The genuinely joint endpoint interaction survives the
source-time ancestry pullback at every owner character.

## 7. Exact boundary and next theorem

This calculation closes one precise debt:

```text
arrival atom versus endpoint/source atom
  -> repaired by source refinement (1) plus branch sidecar (8). (21)
```

It does **not** close the semantic-current debt.  The values
`E_(omega,nu)` were obtained by the independently audited `AX/BY` endpoint
integrations and then used as finite coefficients in (17).  The next theorem
must show either that:

1. the `AX/BY` factors themselves are values of a lawful THM-2334 relation
   current on this cospan; or
2. the simple kernel in (17) is an allowed grouped exact-address coefficient
   with the correct target, owner, word, and chronology.

Without that theorem, nonvanishing of (17) removes no scalar row.  With it,
the temporal label alignment, one-base integral, full spectrum, and nonzero
doubly centred interaction are already in place.

## 8. Connection contract

| field | value |
|---|---|
| source | THM-2471 source refinement `1_Q P^K(eP_omega)` and `eP_nu` |
| target function | actual THM-3514 endpoint pair contribution `E_(omega,nu)` |
| temporal map | retain inverse branch `n`; source sheet is `floor(n/13)` |
| hostile marginal | raw 39-atom `T^2` transition has support 1521 but rank 3 |
| common gauge | `a-u=b-q`, equivalently `s=-d`, with absolute offset retained |
| preserved | endpoint-time atom pair, collision roots, owner phase, common base |
| positive | full and doubly centred endpoint kernels are nonzero at all 13 owner modes |
| remaining sidecar | physical relation-current/grouped-coefficient typing of `AX/BY` |

## 9. Reproduction

```text
python -B 04-computation/lrc_r5_source_aligned_guard_atom_branch_sidecar_probe_20260816.py
python -B -O 04-computation/lrc_r5_source_aligned_guard_atom_branch_sidecar_probe_20260816.py
python -B 04-computation/lrc_r5_source_aligned_actual_endpoint_simple_kernel_probe_20260816.py
python -B -O 04-computation/lrc_r5_source_aligned_actual_endpoint_simple_kernel_probe_20260816.py
```

The semantic digests are

```text
31e9e90c63053944b590195555be07ccbf84fd4c7abc2101de6a2a3562202de6
430aa2201ee8f624de66f034bdf5e31b11ec6ecbe1f3f0b9cef1342b2b1bc2ff.
```

Script/output SHA-256 pairs are

```text
.
```
