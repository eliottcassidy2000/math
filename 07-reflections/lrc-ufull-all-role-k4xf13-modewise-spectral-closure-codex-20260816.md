# Every lawful endpoint address and every drift mode carries both tetrahedra

**Status: VERIFIED-EXACT endpoint-response theorem candidate, pending an
independent audit.**  The exact source is
`04-computation/lrc_ufull_guard_bucket_all_role_spectral_probe_20260816.py`.
This is maximal spectral support inside the inherited endpoint `B^1` bank;
it is not ancestry, a physical current, absolute graph `H^1`, a grouped
coefficient, a bispectrum theorem, a scalar exclusion, or LRC(14).

## 1. Inheritance and the sharper question

The audited U_full guard-bucket theorem gives the exact active address space

```text
(C_L,C_R,d) in {left,right}^2 x F_13,                 (1)
```

with all 52 buckets nonzero for the distinguished `H` and `q5` responses and
their bridge.  It also proves that the middle chamber is empty by the owner
speed-13 gate, and that the four chamber pairs form an honest `F_2^2=K_4`
carrier at every drift.

That result does not answer whether the *other* role classes survive the
same atomization, or whether the two private-support `K_4` tree factors can
cancel after localization to one address or one drift character.  Those are
the cheapest remaining spectral hostiles before ancestry is even discussed.

## 2. The five-class response tensor

The eight retained relation roles occupy five distinct refined classes:

```text
c1                 -> (0, 0,0),
c3                 -> (0, 1,0),
c2,q3,q4,q5        -> (1, 0,0),
H                  -> (1, 0,1),
q2                 -> (1,12,0).                       (2)
```

For `q=(q_a,q_b,q_t)`, expand the two endpoint factors before restoring the
guard and define, schematically,

```text
B_q(C,D,d)
 =13^-3 sum_(alpha,beta) zeta^(beta-alpha*q_a-beta*q_b)
   sum_(a_R-a_L=d) AX_(alpha,beta)(a_L,C)
                      BY_(alpha,beta)(a_R,D)
   sum_tau g_C(a_L+tau)g_D(a_R+tau)zeta^(-tau*q_t).   (3)
```

Only `q_t=0,1` occurs in (2), so the two exact guard kernels already frozen
by the parent theorem suffice.  Summing (3) over all 52 addresses reconstructs
the five inherited inverse-transform values exactly.  The full tensor has
shape

```text
5 x 4 x 13.                                           (4)
```

Every one of its `5*52` entries is nonzero in the certified split field, and
the five response rows have rank five.  Thus no refined role is missing and
no linear combination of the five class rows vanishes globally on the
address carrier.

## 3. All 72 role charts close at every point

For a lawful role chart `chi`, assign the eight role values at one address to
the eight vertices of the private-support graph.  Let

```text
w_e=p_(head(e))-p_(tail(e))
```

in the fixed owner-order orientation.  The reduced weighted determinant
factors as

```text
Delta_chi=(H-q5) Tau_left(w) Tau_right(w),             (5)
```

where the two `Tau` terms are the sixteen-tree polynomials of the two `K_4`
blocks.  There are 72 charts, one graph-automorphism orbit.

The exact census is maximal:

```text
52 addresses x 72 charts x 4 factors:
  zero bridge factors       =0,
  zero left K4 factors      =0,
  zero right K4 factors     =0,
  zero products             =0.                       (6)
```

Nonzero reduction in the certified primitive cyclotomic image proves exact
nonvanishing of every displayed algebraic coefficient.  Equation (6) is
strictly stronger than global factor nonvanishing: no cancellation is being
hidden by summing different guard addresses.

The controls are sharp.  A flat role response kills every edge and all four
factors.  Replacing `q5` by `H` kills the forced bridge and hence every
product, regardless of the two tetrahedra.

## 4. Drift marginal and Fourier closure

First sum the four chamber-pair entries at fixed drift.  All 13 drift
marginals still have all four factors nonzero in all 72 charts.  Next take
the discrete Fourier transform

```text
Bhat_q(k)=sum_(d in F_13) B_q(d) zeta^(-kd).           (7)
```

Again, for every `k in F_13` and every one of the 72 charts, the bridge,
left tree factor, right tree factor, and product are nonzero.  The zero mode
recovers the global five role values exactly.

The stronger chamber-resolved transform also passes:

```text
4 chamber pairs x 13 frequencies x 72 charts:
  every bridge and both K4 tree factors are nonzero.   (8)
```

At each of the 13 drift slices, the `5 x 4` role/chamber matrix has rank four;
the same is true at every Fourier frequency.  Thus neither physical address
space nor character space has a rank-deficient lane.

The frozen digests are

```text
pointwise chart bank:       a1d5183f37cc39deed976876ed91132f662f91137f41a10a79fd3e974ced2dfc
drift-marginal chart bank:  6bcdd9fa616ba2dc4883d8334dca7358d06375d7342f43c4aac8dea45deb5027
drift-Fourier chart bank:   e96a3859e6838fa91d04a55ca55fa4e44cafa45bd86b93c7f6b612b1e65f4dc1
chamber-Fourier chart bank: c9ca8c9b7ed7000b00e39b496523ad75a44379d84ea35e49163d6b64e4196e73. (9)
```

## 5. Why this is still `B^1`, not `H^1`

For each address or Fourier mode, the thirteen edge weights in (5) are the
coboundary of eight vertex potentials.  Hence every graph-cycle pairing is
zero.  The weighted determinant does not descend to absolute `H^1`, so (6)--
(8) can be nonzero simultaneously with a zero absolute flux class; proved
THM-3482 is the general warning.

The ancestry audit sharpens the missing map.  Guard sheets and THM-2471 root
labels are both regular `F_13` torsors under `+tau`, and every equivariant
label bijection is

```text
u=a+c.                                                 (10)
```

One common `c` preserves the drift `u_R-u_L=a_R-a_L`; changing `c` only
multiplies a nonzero Fourier coefficient by a root of unity.  Thus absolute
root-label origin is no longer an obstruction.  Equation (10) remains only
an isomorphism of label torsors: it supplies no physical root, common base,
owner/word/source/arrival sheet, horizon, chronology, or endpoint pairing.

The named equality shortcuts fail exactly.  Same sheet, same chamber, same
guard atom, same maximal component, and same circle point all miss the
frozen bridge.  None is a lawful proxy for THM-2471 ancestry.

The next required object is therefore one character-independent relation

```text
R subset Omega_L x Omega_R                             (11)
```

on the `26^2=676` active endpoint-atom pairs, carrying both factors on one
Boolean ancestry base and one common root gauge before marginalization.  Its
first test is only bridge recovery.  The two `K_4` factors should not be
tested until the inverse transform of (11) reproduces the frozen bridge.

## 6. Tournament, recurrence, and tree boundaries

Three tetrahedral objects now coexist in the session:

1. THM-3509's four recurrence-window entries form a labelled `K_4`; strict
   numeric comparison can orient them as `T_4`, and their six products as
   `T_6`, away from the classified root tie.
2. The present four chamber-pair states form `F_2^2=K_4`; no pairwise
   comparison observable or orientation is inherited, so this is not a
   tournament of size four.
3. The private-support carrier is two `K_4` graphs plus one forced bridge;
   its tree polynomials supply (5), not an ordering of its vertices.

The common grammar is tetrahedral representation plus a lost sidecar.  It is
not a common genealogy.  In the Fibonacci lane the missing sidecar is the
oriented Cassini choice inside one antipodal edge pair; here it is the
common-stalk ancestry and phase/holonomy data.  Similar carrier sizes do not
construct the cross-domain map.

## 7. Reproduction and scope

Run

```text
python -B 04-computation/lrc_ufull_guard_bucket_all_role_spectral_probe_20260816.py
python -B -O 04-computation/lrc_ufull_guard_bucket_all_role_spectral_probe_20260816.py
```

The semantic digest is

```text
2c9495fb8bcb731361ba331d9ca4b84a60f21551dc49b16e6519c1fc4f2e9f97.
```

Independent audit must still reconstruct the five character factors and the
pointwise/Fourier chart banks before promotion.  Even after that audit, the
result is an unrestricted mod-13 endpoint theorem component.  It provides
no grouped exact-address current, all-unit projector, physical order or sign,
nonzero absolute graph flux, common ancestry, scalar-row exclusion,
U_clock statement, bispectrum theorem, or LRC(14).
