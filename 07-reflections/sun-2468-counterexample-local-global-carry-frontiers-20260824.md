# Sun 2-4-6-8: a locally universal, barely supercritical exact hole

**Session synthesis, 2026-08-24.** Current truth is owned by THM-4026--4028;
this reflection records why the result matters and which searches it opens.

## Portfolio and inheritance pass

**Anchor -- LRC(14).** The nearest proved mechanism is THM-2059's CRT
fibre-product packet join. The canonical hostile is THM-2043/2050: complete
fixed local data can forget the global exit/height clock. The corrected near
miss is MISTAKE-222: Pascal syntax is not a cross-problem map. The least-used
relevant sidecar is HYP-1953's triple

```text
(representation count, local obstruction product, normal-form carry debt).
```

**Niche -- mixed binomial additive bases.** The user supplied
`N=896315812331399` as a counterexample candidate. Independent exact Python
and C++ audits prove it is a counterexample, while a separate algebraic audit
proves local solubility modulo every modulus.

**Wildcard -- reciprocal-degree criticality.** The numerical coincidence

```text
1/2+1/4+1/6+1/8=25/24
```

is not merely heuristic. It is the exponent of the exact summatory lattice
count. The system is only `1/24` above the volume threshold, so its mean grows
very slowly and cannot control a lower tail.

## Live concept board

1. **Bounded square certificate.** Fix `(x,y,z)` and recover the triangular
   coordinate from `8R+1=(2w-1)^2`.
2. **Universal local periods.** The least period of `C(n,k) mod m` carries an
   exact factorial correction; the four-coordinate map is locally onto.
3. **Local density rather than obstruction.** The candidate is rare in small
   prime-power histograms, especially `20 mod 33`, but never locally absent.
4. **Quadratic norm compatibility.** The low pair is the constrained fibre
   `A^2+3B^2=24(N-C(y,6)-C(z,8))+4`; inert-prime valuation parity gives a
   cheap necessary row sieve, while separate low-pair character holes force
   a nonzero high-pair translation at `17` and `23`.
5. **Combinadic carry state.** The unique rank-eight normal form uses every
   rank from eight down to one; an even-rank four-atom source must normalize
   to it through labelled Pascal carries.
6. **Height/global termination.** Reduction modulo any fixed clock loses the
   bounded lift whose absence is the theorem.

The decisive interaction is `2 + 6`: universal modular solubility and a zero
bounded modular cover are not contradictory because the latter is a cover of
one finite index box. Concept `3` selects promising boxes; it cannot certify a
hole. Concepts `4` and `5` offer structural sidecars for replacing a brute
box by a proof. Concept `1` remains the cheapest terminal oracle against which
every proposed compression must be tested.

## What the session actually established

- **PROVED + VERIFIED-EXACT:** `N` has no legal four-binomial representation.
  The admissible canonical universe contains `2,755,643,831` triples. Two
  disjoint C++ residue banks end in exact square tests with no solution. An
  independent Python route leaves `324` terminal candidates: `31` fail the
  height test and exact `isqrt` rejects `293`; a redundant bounded prime cover
  leaves zero candidates.
- **PROVED:** every residue modulo every positive modulus is represented. The
  prime-power proof uses a triangular permutation at two, degree-`p+1`
  permutations at `p=3,5,7`, regular Cauchy--Davenport coverage above eight,
  Hensel lifting, exact binomial periods, and CRT.
- **PROVED:** for any fixed degree tuple, the summatory ordered tuple count is
  an anisotropic Dirichlet volume plus an explicit boundary error. Here it is
  `V X^(25/24)+O(X^(11/12))`; each fixed residue class has the same error,
  and only intervals much longer than `X^(7/8)` inherit the shell mean.
- **PROVED structural sidecars:** the constrained `Q(sqrt(-3))` norm identity;
  an infinite family of low-pair holes for primes `17,23 mod 30`; both
  immediate-neighbor representations; and the exact rank-eight combinadic
  normal form.
- **FINITE-EXACT norm control:** odd valuations at all `87` inert primes
  through `1,000` reduce the `248,160` high-pair rows to `61,188`
  (`5099/20680`). This is a necessary sieve only. At the target, the low-pair
  holes force `H=C(y,6)+C(z,8)` to obey `gcd(H,391)=1`, but the high-pair
  support is complete modulo both `17` and `23`, so there is no local
  contradiction.

## Objects x representations x invariants x operations x scales

| View | Concrete object | Preserved invariant | Operation / scale | Main loss |
|---|---|---|---|---|
| exact support | bipartite intersection `L_(2,4) cap (N-H_(6,8))` | equality and witnesses | bounded meet-in-the-middle | pair multiplicity if Booleanized |
| discriminant | `8(N-C4-C6-C8)+1` | triangular equivalence | odd-prime masks, then `isqrt` | sign and height modulo primes |
| local packet | cyclic histograms over exact periods | residue multiplicity | convolution and CRT | common integral lift |
| norm fibre | `A^2+3B^2=24(N-H)+4` | exact low/high compatibility | characters and norm factorization | thin quadratic image of `A`; odd-positive `B` |
| normal form | decreasing binomial expansion | integer value | Pascal split/merge, finite difference | path, labels, atom budget |
| asymptotic body | `sum u_d^d/d!<=1` | tuple volume | anisotropic dilation | target-by-target distribution |

The native combinatorial carrier is a symmetric compatibility graph (or a
four-partite representation hypergraph). It has ties and multiplicities but
no intrinsic binary orientation, so forcing Tournament Analysis would discard
the structure needed by the question.

## Cross-frontier consequences, carefully typed

### LRC(14)

The reusable move is not a theorem transfer. It is a hostile test for local
packet programs:

```text
source: finite exact object with a height bound
map:    reduction to periodic residue packets
keeps:  necessary local compatibility
loses:  magnitude, common lift, first exit
repair: labelled height/termination sidecar
test:   an exact terminal oracle after CRT joining.
```

For the `165` LRC rows, residue packets may rank or prune bounded lifts, but a
zero packet at one clock cannot kill the row and a full packet cannot prove
arrival. Owner, phase, endpoint, and first-exit data remain mandatory. No LRC
row is closed here.

### Pollock and figurate defect descent

Let `D_246` be the complement of the three-summand `2/4/6` support and
`r_z=N-C(z,8)`. THM-4026 gives `r_z in D_246` for all `z=7,...,281`, while

```text
r_(z-1)=r_z+C(z-1,7).
```

This produces a genuine `274`-edge defect trajectory and supports HYP-2491/
2497's rule that adjacent-shell carry pairs, not single residues, are the
right object. The atom system and rank differ from Pollock, so this is evidence
for the carrier only, not for Pollock's conjecture.

### Integral observers and automatic structure

The exact period formula is the integer-valued-polynomial analogue of the
THM-4000/4010 Hasse/Newton observer lane. The next legitimate object is the
mixed-rank evaluation/jet lattice in the binomial basis, with its factorial
index explicitly retained. Its modular state machine is finite; the global
representability sequence is not thereby automatic or C-finite.

### GMC, Rule 30, and JC

Coefficient extraction, Pascal evolution, and local-global language generate
method probes only. GMC needs charge and Wick weights; Rule 30 has a nonlinear
physical transition; JC needs a Keller map. None is present. These lanes are
typed non-bridges, not research claims.

## Ranked new frontiers and cheapest decisive tests

1. **Least exception.** Build a monotone prefix certificate below `N`, with
   exact support bounds and independently replayable chunk hashes. A search
   miss without full prefix coverage is not minimality.
2. **More holes in rare CRT classes.** Combine exact prime-power histograms,
   start with `20 mod 33`, and search height-bounded arithmetic progressions.
   Positive signal: a second exact zero with a reproducible terminal stream.
3. **Lower-tail analytic theorem.** Measure first and second moments of
   `a(n)` after conditioning on the full local product. Seek an exceptional-set
   bound; the global mean alone is already known to be insufficient.
4. **Norm intersection sieve.** Meet the constrained `A^2+3B^2` values with
   reflected `6/8` pair values. Retain the quadratic-image/odd-positive
   sidecars. Cheapest hostile: a norm solution that violates those sidecars.
5. **Per-shell defect grammar.** For each of the `275` eighth-binomial shells,
   minimize the primes or norm clauses proving its `2/4/6` residual fails;
   compare adjacent certificates under the seventh-rank carry.
6. **Pascal confluence.** Define legal split/merge rules with rank, label, and
   atom-budget state. Prove normalization complete before treating a
   reachability invariant as an exact obstruction.
7. **Critical degree tuples.** At `sum 1/d_i=1`, compute the Dirichlet volume
   and local support; below one, density zero is proved, while above one the
   Sun system is the canonical insufficiency hostile.
8. **Formal certificate.** Minimize the bounded prime cover and formalize the
   range bounds, bit-mask implication, and zero-survivor conclusion. Universal
   local solubility is the required negative control.

## Research-move audit

The successful sequence was:

```text
search the literal statement and domain
-> exactify one coordinate by a discriminant identity
-> inherit sharp monotone height bounds
-> run independent necessary filters with positive controls
-> separate local support from bounded-lift compatibility
-> derive norm, carry, and average-order mechanisms
-> state the strongest survivors and the first failed implication.
```

This instantiates existing cards “search the statement,” “controlled
forgetting needs a sidecar,” and “complete local coordinates can be globally
blind.” The bounded-cover/local-surjectivity distinction also has independent
LRC evidence, so it is promoted as a scoped card in `META-PATTERNS.md`.
