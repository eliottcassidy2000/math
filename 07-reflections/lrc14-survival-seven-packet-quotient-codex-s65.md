# Survival middle mass is the tail quotient of the seven-packet recursion

**codex-2026-06-20-S65.**  The useful connection from this pass is more
precise than "the inequalities look alike."  The HYP-2701 survival currency is
not the `A+B+C-D-E-F+G` pair-tax character.  It is a different quotient of the
same packet architecture.

The exact survival coordinate is

```text
C = p1+p2+p3+p4-4p6 = G1 - G5 - 4G6,
U4 = 1-C = 1 - G1 + G5 + 4G6.
```

So the live depth cuts are `G1`, `G5`, and `G6`.  The transfer boundary is the
negative discrete derivative:

```text
C(t-1)-C(t), t=1..6 = -1,0,0,0,+1,+4.
```

This is exactly THM-558 in survival currency.  Closing the last missed sector
spends currency, while escaping five- and six-missed tails earns it back, with
the fully missed apex weighted by `4`.

The exact death-chain table is the strongest new signal.  With two
decorrelated far runners, a state with all six inner sectors missed moves from
coefficient `-4` to expected coefficient `26/49`; the gain is `222/49`.  That
explains why HYP-2701's two-far boundary margins are positive.  The boundary
model is not barely lucky; it has already converted the apex tail debt into
middle survival mass.  The remaining proof has to bound the signed resonant
deviation away from that boundary value.

This also corrects how to use the tiling analogy.  The full and half tiling
recurrences keep labelled `3+3+1` ownership.  HYP-2681's pair-tax shadow keeps
the singleton/pair/triple rank character.  The survival gate forgets the labels
and keeps only depth cuts whose coefficients survive the Bonferroni collapse.
That is why it resembles a seven-term recurrence while exposing only three
visible terms.

The practical proof target is now sharper:

```text
actual survival currency
  = exact death-chain boundary
    + signed low-relation deviation.
```

For two-far rows, the death-chain boundary already has the margin; low
relation-distance far pairs are the only visible way to spend it.  For
`far_count>=3`, the same table gives `K3(6)=296/343`, so the high-tail debt is
almost gone before any fine phase analysis.  That branch should be attacked as
a signed multi-far deviation problem, not as a raw middle-mass shortage.

The recurring rule is unchanged: choose the basis first, then scalarize.
HYP-2703 says the seven slope bands only work in signed aggregate.  HYP-2704
says the true-wide cap gate only works after the survival cut quotient, and
near equality must then be lifted back to sector labels, cube-root phase,
relation-lattice rank, and Abel/BV decorrelation.

The incoming mac-mini S6 batch sharpens that last sentence.  Coverage-count
majorization fails at the intermediate thresholds, Bonferroni prefix/block
certificates fail in the middle, and the Fourier mod-7 relation lattice has the
same raw size for every shape.  What varies is the signed weight profile.  So
the two-far deviation lemma should not be phrased as "small rank implies small
error"; it should be boundary margin versus signed phase/support profile.
