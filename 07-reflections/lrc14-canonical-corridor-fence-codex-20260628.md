# LRC14 Canonical Corridor-Fence Reflection

Anchors: HYP-3431, HYP-3430, HYP-3429, HYP-3428, HYP-3427, HYP-3426, HYP-3425,
HYP-3424, HYP-3422,
HYP-3421, HYP-3418, HYP-3415, HYP-3129, THM-523.

The useful new proof angle is endpoint ownership, not more positive-measure
sampling.  HYP-3425 found surviving windows for the canonical `84m` tower; the
new calculation explains them.

After splitting `S_m=O union 2E` and using `u=2t`, the low core
`O={1,3,5,7,9,11,13}` and `E_low={1,2,3,4,5}` leaves two fixed corridors:

```text
[8/49,6/35]      B1 odd 7 wall -> B1 odd 5 wall
[29/35,41/49]    B0 odd 5 wall -> B0 odd 7 wall
```

The high even half-speed `42m` only removes disjoint grid intervals of width
`1/(294m)`.  Each corridor has length `2/245`, so one high-grid component is
too short to cover a corridor, while multiple components leave grid gaps.  This
is an all-`m` proof of the canonical tower's two-branch relocation certificate.

The important reframing:

```text
HYP-3425 global Helly target
  -> HYP-3426 one-branch mirror / endpoint-owner support
  -> HYP-3427 finite wall-signature atlas
  -> HYP-3428 two-adic loss ledger / controlled-forgetting audit
  -> HYP-3429 endpoint-spine certificate
  -> HYP-3430 harmonic-intercept scalar firewall
  -> HYP-3431 canonical base case
     = fixed odd/even corridor + high-speed disjoint grid fence.
```

This should become the template for the next search.  For a general covering
row, strip the moving high/flex speeds and ask whether the low packet leaves a
branch corridor longer than every moving bad component.  If yes, the row exits
by a corridor-fence lemma and supplies the HYP-3429 endpoint spine.  If not,
the row goes back to HYP-3430's scalar firewall, HYP-3429's rank-2 spine
target, HYP-3428's loss classes, HYP-3425's component-gap Helly certificate,
HYP-3427's wall-word classifier, HYP-3424's duality router, or
HYP-3420/HYP-3417 owner-current exception labels.

The incoming HYP-3425 additive-energy sidecar changes how to run that fallback:
do not let a failed corridor row collapse to raw `fullE`, `RE`, or `oddE`.
Keep a sheet coordinate such as `q_zero_mass` or `q_range_hi` with the energy
coordinate, then ask whether the packet feeds signed SPEC, phase-cover debt, or
a terminal owner-current exception.  In this language the corridor-fence lemma
is the clean canonical base case of the HYP-3428 ledger and HYP-3429 endpoint
spine, while HYP-3430 explains why harmonic tail scalars cannot replace it:
the even child, odd blockers, and endpoint owners reduce to one high-grid width
comparison.  Energy-plus-sheet is the non-canonical classifier guardrail.

Do not overstate the result.  This closes the most visible canonical resonant
one-tail family, not the full LRC14 covering floor.  It does, however, remove a
major family from the finite Helly search and shows how to turn off-grid
transparency into a structural proof instead of a table of witness times.
