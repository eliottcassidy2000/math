# Observer-Extension Exact Duodecimal Audit - S230

The useful hidden detail is that the tempting count statement has to be
corrected before it becomes mathematical.

Literally:

```text
48 + 12 = 60, not 56.
```

But the corrected ledger is very suggestive:

```text
56 = 48 + 12 - 4.
```

Here the pieces are all live tournament carriers:

```text
48 = P(5), rooted/node perspectives on 5-tournament classes
12 = SC(6), self-converse six-tournament classes
4  = U(4), the four unrooted four-tournament classes
56 = U(6), all six-tournament classes
```

So the clue is not "add a dozen."  It is "a dozen-sized sidecar overlaps the
rooted cache along a four-class kernel."  In duodecimal terms, the first
failure is a two-thirds-dozen correction with a one-third-dozen overlap.

The same dozen also appears as S217's local rectangle redundancy:

```text
K_{4,5}: 20 lines, rank 8, redundancy 12.
```

That is probably the better proof object.  The dozen is a reservoir of
rectangle-cycle duplication laws, not independent class mass.  It can affect
`U(6)` only through a quotient/overlap law, a cycle residue, or a named
sidecar that says what controlled forgetting destroyed.

The exact audit strengthened the earlier S213/S214 edge-sector result.  Sector
sizes and internal sector tournaments separate `55/56` six-classes, failing
only on converse masks `344/345`; adding cross-sector orientation separates
all `56`.  That makes `cross_sector_orientation_word` the compact payload
after rooted memory.  The deletion split is also useful: the 12 self-converse
classes have rooted multiplicity sum `60`, while the 44 chiral classes sum to
`236`, for the full rooted `296`.

The LRC abstraction I want to keep is:

```text
observer-extension creates a cut payload;
controlled forgetting is legal only if the payload is retained, reconstructed,
annihilated by a dual/cycle equation, descended familywise, or named as
residual debt.
```

This unifies several older repairs.  HYP-3057 first types the small value by
origin.  HYP-3056 then records the observer-cut payload modulo visible
automorphisms.  HYP-3038's q=23 drop/add square keeps exact-M zeta and endpoint
owners.  HYP-3037's capacitors keep first-cut stage.  HYP-3041's AP-tail repair
keeps the hidden q=13 clock.  HYP-3045 keeps external endpoint owners inside
the `B18Z6` shadow.  HYP-3021/HYP-3022 keep blocker-generator/barcode/normal-fan
data instead of raw decoy count.  HYP-3049 keeps cross-sector orientation.
HYP-3053 keeps rectangle/hourglass residues.

Assumption challenge: I did not treat tournament vertices as runners by
default.  I considered runners, arcs, rooted vertices, ordered pairs, edge
sectors, diagonal tiles, line sectors, rectangle and hourglass cycles,
deletion fibers, self-converse branch points, cover arcs, barcode bars,
endpoint owners, residues, Fourier modes, matroid cocircuits, and proof
obligations.  The useful vertices here are observer-extension proof carriers.

Next theorem target: build or refute a real inclusion-exclusion object behind

```text
U(6) = P(5) + SC(6) - U(4).
```

The dangerous overclaim is that this identity is already a bijection.  It is
not.  It is a sharply marked overlap target.
