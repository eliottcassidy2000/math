# Perspective Groupoid Controlled Forgetting - S261

Identifier: HYP-3106 / T1183 / LTI-244 / LTT-142.  HYP-3103 remains the
incoming miss-count PGF root signal imported here as an analytic sidecar.
HYP-3104 remains the incoming maximizer signal atlas imported here as a
transfer-risk sidecar.  HYP-3105 remains the incoming obstruction-transfer
atlas imported here as a terminal-use sidecar.

The A000568 perspective curiosity is now better phrased as a functor problem.
The equality is not:

```text
node perspective = next tournament class.
```

It is:

```text
this quotient is legal for this next operation only if its lost coordinate has
a named sidecar.
```

The S261 scout confirms the old first failure and extends it one row:

```text
P_node(5)=48 < U(6)=56
P_node(6)=296 < U(7)=456
```

Node depth does its job: it recovers exact rooted memory.  At `m=5` the depth
row is `[36,47,48,48]`, and at `m=6` it is `[196,280,294,296]`.  But rooted
memory is not the add-observer payload.  That payload lives in incident words,
edge-sector cross orientation, deletion fibers, component topology,
obstruction cochains, endpoint owners, and now PGF root signatures.

The new `m=6` carrier table is the main fresh signal:

```text
arc=704
triple=928
transitive=688
cyclic=240
conflict_pair=32
```

Conflict/Omega-style perspectives become visible exactly when the shifted
rooted-node gap stops being a one-step toy.  That points back to HYP-3100:
H/Omega contradictions must be fed by complete, pulled-back carriers, not
loose graph shadows.

The dihedral/converse side stays concrete.  U(6) sector sizes and internal
sector decks separate `55/56` classes, and the single collision is the
converse pair `344,345`; cross-sector orientation repairs it to `56/56`.
This is the tournament version of the LRC dihedral warning: reflection is
legal, but chirality/owner data cannot be silently quotiented.

Incoming HYP-3101 and HYP-3102 clarify what the LRC stress tests are.
HYP-3101 asks whether the quotient survives a component-bound operation.
HYP-3102 asks whether it survives first-obstruction chart gluing.  S66 adds a
third stress test: whether a moment/extremality quotient survives after the
miss-count PGF root signature is forgotten.

So the working proof-interface field is:

```text
perspective_functor
root_object
automorphism_action
next_operation
forgotten_coordinate
required_sidecar
dihedral_reflection_status
component_bound_stress
first_obstruction_stress
pgf_root_signature
legal_discharge
```

Assumption challenge: I considered runners, tournament nodes, directed edges,
ordered pairs, cycles, cliques, conflict pairs, dihedral orbits, safe
components, obstruction cochains, miss-count roots, endpoint owners, matrix
sidecar columns, and proof obligations.  The selected vertices were
perspective functors.  They preserve the quotient's legality for the next LRC
operation and destroy labels, endpoint roles, cross-sector orientation,
chirality, topology, obstruction, and analytic root structure unless a named
sidecar carries them.
