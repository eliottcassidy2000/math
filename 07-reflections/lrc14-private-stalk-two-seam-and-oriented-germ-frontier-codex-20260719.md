# LRC(14): the frontier is a private stalk with two seams and an oriented address germ

**Session:** codex-2026-07-19-S78 frontier synthesis

**Status:** research map and sharply stated next lemma; not a proof of LRC(14),
Cover14 crown collapse, or uniform emptiness of the `n=12` sporadic branch.

**Same-session landing addendum.**  Two pieces described below as targets are
now theorems.  THM-1248 proves the `1,174`-symbol relative blocker word,
contracting affine transport, and a phase-located gcd-quantized wall seam.
THM-1250 proves that all six private needles force a five-edge tree of actual
interior lcm-quantized handoffs, strengthens it to the multiplicity debt

```text
H_fast>=1/c+(7/36)sum_(u<v)m_uv/lcm(u,v),
```

and extracts a literal private interval stalk for every owner.  The phrase
“two-seam target” below should therefore be read as historical motivation:
the live target is now the cover-preserving oriented-germ lift across the
affine shift from `d_i` to `c+d_i`, or a proof that its first failure creates
a new disjoint handoff counted by the multiplicity debt.  The exact mixed
identity and a sign-reversal guardrail are recorded in THM-1250 and the
companion owner-reuse memo.

## 1. What the newest theorems changed

The S82 crossing and THM-1243/1244/1246/1247 do more than add several
independent inequalities.  Together they separate the last credible local
obstruction pictures from the one object that still has enough information to
carry a proof.

The global seven-wall margin is now positive:

```text
mu(Safe_7) >= 15/154,
15/154-2/21 = 1/462.
```

THM-1226 and THM-1237 show why that margin cannot simply be restricted to a
chosen interval by summing independent gcd-period errors.  The disconnected
strict-high branch is closed, while the connected branch needs the exact wall
address, full kernel/gcd sheet, and a positioned forest.  THM-1233/1236/1241
simultaneously compactify the six-comb ratio packet, force one of five
macroscopic cuts, and identify the tilted upper-median functional

```text
H(y)=sum_i |d_i-y|-y/14,
argmin_(y>=7c/6) H(y)=max(7c/6,d4).
```

Thus neither projective escape nor complete coalescence is the residual.

THM-1243 and THM-1246 then remove the most seductive local picture.  A
`14a` blocker can reproduce and erase all seven `O(a^-2)` curvature cracks
in one selected gap, but the same packet has an explicit transverse clock of
depth greater than `3/28`.  Even the whole resonant bank
`14a,28a,...,14Ha`, `H<=12`, leaves the sharp corridor

```text
[15/196,(14H+13)/(196H)]/a,
width = (13-H)/(196Ha).
```

The toothpick ladder is real, but it is an endpoint-descent law, not a trap.
Local replication has one clock; global escape has another.

THM-1247 removes the other seductive picture.  The `q=15` sunflower is a
contracted Fano stalk, but its negative line degenerates after the unique
`P3~P6` contraction.  Address `chi_7` and speed `chi_7` are independent by
CRT.  More decisively, one explicit packet simultaneously has the full clock,
a legal Kakeya cut, blockers at every centered carrier spoke, blockers at all
fifteen positive fast-fast sum beats, and a nontransitive cross-clearance
tournament—and is nevertheless lonely at `t=1/12`.  Incidence, sampled
blocker completeness, and tournament cycling are therefore not cover truth.

The positive result is THM-1244.  On the canonical slowest spoke it constructs
a connected component

```text
K=G intersect S1,
|K| >= (6d1-c)/[7d1(c+d1)] > 432/(1729c),
```

whose cover needs at least three blocker labels.  An irredundant chronological
tooth chain consequently supplies a rank-two forest `F0`, two *located*
gcd-quantized overlaps, and a private owner with mass

```text
Q0 >= gB/(7d6^2),
mu(private owner in K) > 44/(3705c).
```

This is the first current object that is simultaneously macroscopic,
positioned, owner-labelled, and attached to exact arithmetic seams.  The
remaining problem is to transport it through the positive-holonomy blocker
cycle without discarding which side of each tooth it occupies.

## 2. Preservation ledger at the present frontier

| Viewpoint | Natural carrier | What it preserves | What it destroys when used alone | Present verdict |
|---|---|---|---|---|
| Global seven-wall Hunter crossing | weighted pair graph and deletion quartet | the `15/154` mass floor and strict/weak pair spectrum | interval phase, wall address, and which complementary killer acts | A positive reservoir, not a positioned witness by itself. |
| Functional `H`-drift | ordered ratio packet and auxiliary lap scale `y` | compact separation, the exact median cut, and the top-minus-bottom imbalance | tooth phase, blocker identity, gcd sheet, and owner chronology | It chooses the macroscopic cut; it cannot consume that cut. |
| Weighted six-bin drift | endpoint-labelled nerve edges and the twelve-piece load envelope | ratio-sensitive load, actual endpoint owners, and a seam budget | the chronological locations of all handoffs if projected to the scalar functional | A useful inequality sidecar, not a transition state. |
| Kakeya slow-gap needle | one complete `c`-safe gap with moving danger arcs | the literal local cover predicate, offsets, and boundary walls | other gap/address cells if one selected needle is privileged | The proof may localize here only if it retains an alternate-gap exit. |
| Curvature/toothpick cracks | active adjacent-pair defects in one near-tiling gap | exact `O(a^-2)` crack geometry and its resonant replication | global clocks and macroscopic transverse components | THM-1243 refutes local crack survival as a closing invariant. |
| Resonant needle-bank corridor | nested component with boundary owners `(14a,14Ha)` | the global transverse interval, projective band, and reciprocal endpoint decrement | arbitrary external blocker words and owner handoffs outside the harmonic bank | Toothpick self-similarity is boundary descent, not blocker-degree growth. |
| Positioned slowest-spoke chain | `K`, an irredundant labelled tooth word, `F0`, and private regions | connected cover truth on a canonical component, two exact seams, and private ownership | global cycle holonomy unless the spoke/address data is attached | This is the positive local stalk that must be transported. |
| Beat masks and the `q=15` sunflower | lifted quotient-clock masks and cyclic address word | exact sampled beat consumption and the unique sign-address contraction | off-grid tooth orientation, lift height, intervening teeth, and owner reuse | A sharp exceptional chart and guardrail, not an obstruction. |
| Fano/`chi_7` | labelled lines, seam colour, and third-support incidence | the zero-seam bipartition and useful organization of seven obligations | metric overlap, address contraction, and the independent speed-colour bundle | Valid only after a metric transport law couples its labels. |
| Blocker cycle and relation holonomy | centered spokes with numerator/tooth-address equations | nontransitivity and `prod P_i>prod N_i` in the canonical lift | private mass and which oriented germ can legally traverse the cycle | The first exact descent potential, still missing its transport arrow. |
| Covering-min / safe measure | literal union of danger combs | the terminal LRC predicate | most finite structural shadows | The current route remains viable precisely because `K`, seams, and germs are metric, not only combinatorial. |

This ledger reconciles the S82 “structure is severed from metric residue”
warning with the present proof route.  The proposed object is not another
structural shadow of safe measure.  It contains literal interval mass,
rational endpoints, strict sides, and cover-preserving continuation.

## 3. Tournament analysis after challenging the vertices

There is no single LRC tournament.  Different vertex choices expose
different two-skeletons of the actual carrier.

* **Runners.**  With observable `d_j-d_i`, sign gauge, and increasing-speed
  tie path, the tournament is transitive.  Its score histogram is
  `(0,1,2,3,4,5)`, it has no directed cycles, singleton SCCs, and one
  Hamiltonian path.  It remembers order and loses every current target.
* **Centered carrier-spoke obligations.**  With cross-clearance
  `c_ij=||i t_j||-||j t_i||`, sign gauge, and speed tie-break, THM-1247's
  packet has score multiset `(1,1,2,3,3,5)`, three directed triangles, SCC
  sizes `(5,1)`, and nine Hamiltonian paths.  This is genuine nontransitive
  telemetry, but the same packet is lonely.  Blocker cycles are necessary
  data, not sufficient verdicts.
* **Individual teeth or wall-crossing events.**  Chronological order is
  canonical, but the useful relation is a labelled overlap chain, not a
  tournament: some pairs never meet, labels recur, and triple support matters.
  Its invariant is connected graphic rank.  THM-1244 extracts rank two
  without inventing absent pair relations.
* **Proof obligations.**  A directed blocker functional graph does force a
  cycle, and attaching `(P_i,N_i,Q_i,r_i)` gives that cycle numerical
  holonomy.  Forget those weights and canonical lifts and it again becomes a
  nontransitive shadow.

Gaps, fixed circle sections, section boundaries, residues, cover arcs,
Fourier modes, and matroid circuits were also challenged as vertices.  Gaps
carry components but lose owners; residues carry clocks but lose off-grid
continuation; Fourier modes carry mass averages but lose isolated germs;
matroid circuits carry rank but lose phase and length.  The best present
“tournament” is therefore a decorated obligation cycle sitting over a
continuous handoff forest.  The cycle supplies holonomy; the forest supplies
located metric credit.  Neither may replace the other.

## 4. What remains of the older probes

### The `j=4` flood and continuum tail

The continuum four-comb problem is no longer an open tail.  THM-1203 proves
the exact ceiling `mu(BAD_4)<=2/21`; the sheared analytic tail starts at
`p+q>=26`, the remaining `99` reduced pairs have `p+q<=25`, and equality is
exactly the four-term AP direction.  Its live use is as a deletion-circuit
supplier inside the seven-wall margin, not as a new six-cover invariant.

The older `j=4` flood atlas is a different recursive operation.  Fano flags
organize its root charts and Heawood currents reconstruct scalar edge fields,
but rerooting and future insertion do not factor through that atlas.  The
closed completed-family bases and the remaining pure root-edge tails should
not be confused with the now-closed continuum tail.  The lesson imported into
LRC(14) is operation-congruence: an incidence atlas is useful only when the
next tooth insertion and terminal lonely predicate descend through it.

### Fano and `chi_7`

The Fano probe survives in two narrow roles: it organizes seven pair/line
debts, and `chi_7` orients exact zero seams.  THM-1247 proves that it cannot
serve as the whole quotient.  The `q=15` sign-address plane contracts two
points, its negative line becomes `C5C`, and every speed-colour word is
independently realizable.  Any future use must attach the Fano label to an
actual owner germ and show that colour is transported by the same metric
operation.  A line count or Paley tournament alone is exhausted.

### Toothpick self-similarity and Kakeya needles

The local crack ladder, the harmonic corridor, and the older Farey renewal
laws now give a consistent recursive picture.  What recurs is not the number
of cracks.  It is a labelled boundary operation with a source term and a new
address.  On a Kakeya needle, direction without offset is meaningless;
offset without owners cannot identify a handoff; owners without chronology
cannot distinguish continuation from replacement.  The correct recursive
atom is an oriented endpoint germ with its owner, address, exact width, and
legal next cell.

## 5. The smallest currently justified state

The following packet is not claimed categorically minimal.  It is the
smallest state for which every newest theorem supplies a field and none of the
known guardrails gives a liar fibre:

```text
X = (
  c; normalized ratios d1/c,...,d6/c; macroscopic cut index;
  canonical slow-gap lift k and centered spokes t_i=P_i/Q_i;
  K=G intersect S1 and its literal interval endpoints;
  irredundant chronological tooth word on K;
  rank-two forest F0 with both speed/address handoffs and exact overlaps;
  private owner b, its private components, and their one-sided endpoint germs;
  blocker cycle C with (P_i,N_i,Q_i,r_i) in the canonical lift;
  full exact kernel/gcd sheet and primitive detuning data;
  cyclic quotient-address word, with q=15 contraction retained only as a chart;
  adjacent gap/address cells and the terminal cover/lonely predicate
).
```

The oriented germ coordinate is elementary but is exactly what every current
quotient loses.  At a rational spoke `t=P/Q`, let speed `d` be blocked by the
tooth centred at `N/d`, and write

```text
r=dP-NQ,                         |r|<Q/14.
```

The distances from `t` to the left and right tooth walls are

```text
ell^- = 1/(14d)+r/(dQ),
ell^+ = 1/(14d)-r/(dQ).                              (1)
```

Thus the signed germ imbalance is `2r/(dQ)`.  At `q=15`, `r=+1` gives the
THM-1247 asymmetry `29/(210d)` versus `1/(210d)`, and `r=-1` reverses it.
Masks remember only that the grid point is blocked; (1) remembers which side
can continue into the next address cell.

On a selected blocker cycle the canonical equations are

```text
P_i d_(i+1)-N_i Q_i=r_i,
P_i Q_(i+1)-N_i Q_i=P_i c+r_i>0,
prod_i P_i > prod_i N_i.                              (2)
```

Equations (1)--(2), the private stalk, and the two seam addresses are the
three coordinates not yet combined by any theorem.  The full exact kernel
sheet is essential: MISTAKE-184/185 show that presentation height and a
low-height inverse cannot replace the high exact relation.

## 6. Sharply stated next lemma

The highest-leverage target is the following transport theorem.

> **Private-stalk two-seam oriented-holonomy descent.**  Let six faster
> danger combs cover a complete `c`-safe gap.  Form the canonical slowest-
> spoke component `K`, choose THM-1244's canonical rank-two handoff forest
> `F0` and a private owner `b`, and choose a directed THM-1240 blocker cycle
> in the canonical lift.  Then at least one of the following proof exits is
> available:
>
> 1. an oriented endpoint germ of a private `b`-component continues through
>    the adjacent address cell to a point outside all six danger combs; or
> 2. the germ and both located seams transport, with the exact kernel/gcd
>    sheet and owner labels intact, to a new covered component whose cycle
>    address word is the tooth-address word `(N_i)` rather than the spoke-
>    numerator word `(P_i)`.
>
> In the second case the positive-holonomy law gives the strict integer
> descent
>
> ```text
> A_new = product_i N_i < product_i P_i = A_old.       (3)
> ```
>
> The transport may change the selected slow gap, but it must preserve the
> literal cover predicate and at least one private oriented germ; it may not
> replace the full kernel sheet by a bounded relation-height truncation.

This is sharper than “compress the address” in three ways.  It names the
input component, the two phase-bearing credits that certify continuity, and
the integer potential that decreases.  It also passes every new guardrail.
The THM-1243 rays and THM-1246 banks should take exit 1 by their transverse
clock/corridor.  The THM-1247 `q=15` packet cannot be declared contradictory
by its masks; its asymmetric germs must either expose its known lonely phase
or be legally transported.  A sampled external blocker may dominate all six
spokes, but it cannot erase the continuous three-label handoff rank on `K`.

If (3) can be iterated, termination is automatic because `A` is a positive
integer.  The terminal packet is then a bounded primitive address chart or a
common-clock chart already covered by exact methods.  If the proposed
transport fails, the failure must have a first endpoint.  That endpoint is
itself a located off-grid owner-reuse obstruction and should enter the
positioned Hunter inequality as additional actual overlap credit.  This is
the useful fallback: failure of descent is not anonymous error; it is a new
metric seam.

## 7. Immediate proof program

1. **Localize the private mass to an oriented germ.**  Use the compact ratio
   box and the finite number of tooth walls meeting `K` to extract a private
   component with an explicit `const/c` length, retaining its owner and both
   wall labels.
2. **Write the transport square.**  For each cycle edge, compare (1) with the
   relation equation in (2).  Determine exactly when the outgoing side of one
   blocker tooth enters the incoming side of the next.  Do this on the full
   exact kernel sheet.
3. **Charge the first failed square.**  If an intermediate comb interrupts
   continuation, record its owner and actual overlap interval.  Test whether
   the two already located `F0` edges plus this interruption force a third
   positioned forest credit strong enough to close the compact ratio cell.
4. **Use Fano only after placement.**  If seven obligations remain, attach
   Fano lines and `chi_7` signs to these actual seams; do not infer signs from
   the contracted address plane.
5. **Enumerate only after descent bounds height.**  A finite exact atlas and
   Lean arithmetic core are appropriate consumers of a proved address bound,
   not substitutes for it.

The frontier is therefore no longer “find a stronger global inequality,”
“prove a blocker cycle,” “solve the Fano quotient,” or “show a selected gap
has an unblocked crack.”  It is one operation-level statement: **a
macroscopic private owner and two located seams must either cross the
off-grid blocker cycle or make its positive holonomy into a well-founded
address descent.**

The uniform `n=12` sporadic-emptiness problem remains a separate open branch.
Its AP-centred and several bounded/common-sheet strata are closed, but no
result above supplies the missing all-height extraction.  It should not be
silently counted as a consequence of the LRC(14) six-comb frontier.

## 8. S82 update — the oriented germ is reconstructed, not additional state

THM-1248 sharpens Sections 5--7 after this reflection was first written.  For
a blocker edge `i->j`, use the carrier-gap gauge

```text
X_i=cP_i-kQ_i,
ell_i=X_i+r_i,
delta_i=P_j-(k+N_i).
```

The raw tooth lift `N_i` is not an independent unbounded coordinate.  The
relative word is uniformly finite, `-586<=delta_i<=587`, and `ell_i` is the
least positive determinant residue, with `gcd(Q_i,Q_j)|ell_i`.  More
importantly for (1), the common centered orbit supplies

```text
X_i=Q_i/2+c epsilon_i=(Q_i+E_i)/2,
```

and hence

```text
ell_i^- - ell_i^+
 =2r_i/(d_jQ_i)=2(ell_i-X_i)/(d_jQ_i),
delta_i<=0 selects the left wall,
delta_i>=1 selects the right wall.                    (4)
```

Thus the oriented germ side and imbalance are already encoded by the
translation-invariant relative edge.  Integral lift height is gauge; the
common residue orbit and exact gcd sheet remain visible sidecars.

THM-1248 now advances the continuous bridge further.  The proper tail of the
slowest-rooted lasso has length at most four, contracts by less than `13/19`,
and satisfies an exact centered-residue telescope.  The full six-state
quotient reconstructs not only the blocker wall but every candidate owner's
wall residue, tooth integer, and signed forward/backward margins.  Sampled
ownership is therefore derived data, not an external oracle.

Coverage fills the interval between samples via the expiry tournament on
active tooth instances: orient by later forward endpoint in the current
travel direction and take the Hamiltonian maximum.  The greedy endpoints
advance until a tooth contains the next centered spoke.  After appending the
prescribed next blocker tooth and crossing its wall through the strict owner,
these local walks stitch the whole tail and cycle.  Existential
component-level transport is now proved.

THM-1250 is also proved.  It supplies a fully located five-edge tree containing
any prescribed interior seam forest, charges repeated chronological handoffs
by multiplicity, and extracts a literal private stalk for every label.  The
protected/tree scalar tariff closes new lcm cells, but an exact coprime tangent
packet shows that an undirected MST scalar still cannot finish the problem.

The decisive guardrail is now retracing.  For

```text
(1;2,3,4,5,12,13),       2->3->12->13->4->5->12,
```

two consecutive opposite-direction transports use the identical seam
`(13/28,85/182)` of length `1/364`.  Greedy continuation need not create a
new Hunter occurrence.  The remaining operation-level theorem is therefore:

> at every travel-sign reversal or same-label/different-tooth return, prove a
> genuinely new disjoint chronological handoff, a contradiction with the
> positive shifted-clock holonomy, or strict alternate-gap/address descent.

The missing object is a signed turn/reuse gain on the finite boundary-event
walk—not raw address compression, owner discovery, component transport, or
another runner tournament/Fano completion.

## 9. Coherent-word correction — the missing object is an aligned typed cell

The final paragraph of Section 8 is now historical.  THM-1252--1256 remove
the free greedy choice that made the retracing packet decisive: every
centered blocker is selected from one deletion-minimal chronological tooth
word, and all raw handoffs in that word are pairwise disjoint and fully paid.
The retracing example remains the reason this common-word choice is
necessary; it is not the live residual obstruction.

The target's own centered spoke gives the further missing observation.  If a
`d_j`-tooth contains the source phase `t_i`, then `d_j` is more than
`1/4`-safe at `t_j`, so `t_j` lies outside that tooth.  The relative digit
chooses which side.  Since both endpoint sequences increase along the minimal
word, every actual blocker edge is automatically phase/word aligned.  The
old alignment-or-mismatch dichotomy was too weak: mismatch survives only for
arbitrary marked pairs, never for a coherent blocker adjacency.

THM-1248's sharp wall clearance turns alignment into metric geometry.  A
binary edge has a target-free corridor

```text
5/[14(c+d_j)]<|I_j|<23/[28(c+d_j)]<6/(7d_j),
sum_(h!=j)1/h>(5/[6(c+d_j)]).
```

At the minimum-speed cycle vertex the next marked tooth cannot be adjacent;
the local word therefore exports a canonical three-handoff/rank-two window.
Using both walls on every blocker tooth gives the exact recursive trichotomy:

1. a wall is owned by a lasso label;
2. an outside owner label repeats;
3. the lasso is the slowest-rooted two-cycle.

Indeed, absence of the first two branches injects `2v` wall occurrences into
`6-v` outside labels, forcing `v=2`.  In that residual the four outside labels
own the four walls distinctly.  The two marked target teeth are separated by
at least two intermediate teeth, so their four wall seams form two disjoint
stars and an internal protected bridge joins them.  This is a forced located
spanning tree on all six labels, while the two ascent-target seams plus the
bridge form a rank-three forest in the slowest-safe component:

```text
H_B>=(6d_1-c)/[3d_1(c+d_1)]+7g_B/(4d_6^2).
```

Every full internal bridge seam lies in the descent target-free corridor.
If `C>5d_1` is its exact quotient clearance numerator, the combined
quotient/word tariff is

```text
sum_(h!=1)1/h
 >=C/[6d_1(c+d_1)]
   +(7/12)sum_internal 1/lcm(s_r,s_(r+1)).
```

This is the first carrier here that truly couples the centered residue
quotient to chronological seam mass.  It still does not close LRC(14): exact
lonely packets realize the four-distinct-owner two-cycle, including one with
a nonbinary ascent digit.  A structurally binary six-cycle is nevertheless
pruned further—each digit occurs at least twice—showing that the factor-two
clearance already excludes finite typed words.

The missing object is now narrower: a well-founded consumer of one **aligned
typed cell**.  Incidence must become torsion/address descent, repeated outside
ownership must become strict tooth-displacement descent, or the explicit
tariffed two-cycle must create metric excess or a smaller complete gap.  This
is not another tournament on runners; its vertices are marked tooth cells and
its sidecars are the two wall owners, internal bridge, clearance numerator,
and complete-gap address.
