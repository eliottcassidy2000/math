# R5 nonlinear common-gauge endpoint connection: independent hostile audit

**Status:** FINITE-EXACT INDEPENDENT HOSTILE AUDIT ACCEPTS THE SCOPED PACKAGE.  This is a finite address pullback, not a physical current or an LRC(14) theorem.

## Verdict

The two submitted common-gauge probes are reproducible and correctly scoped.  A clean-room implementation imported neither candidate script and reconstructed both load-bearing tensors directly from the hash-pinned THM-2594 interval engine and the independently audited THM-3514 endpoint-atom engine.

The exact source and target comparison digests agree:

```text
source 39 x 39 x 13 common-gauge tensor:
e4d0d4fa674e1f54496e613f7a3e1f057af033fa8992322a5f414ea176e1c3d4

target 39 x 39 endpoint pair function:
c2d5911b287510335edc6aefa6d3b865c982568f678bb89ee9b82ee211962df1
```

Normal and optimized runs have identical transcripts.

## Independent construction

The source leg was rebuilt before transfer.  The exact weighted source

```text
f = 1_Q P^2 1_E
```

was split by the 39 intervals `I_(a,C)` first, and only then was each piece folded by `P_(13^5)`.  Summing the atom profiles recovers every unsplit arrival and source profile at all 13 roots; multiplying the recovered profiles reproduces every THM-2594 pair mass.  This is the decisive guard against a post-integration bucket decoration.

The source middle chamber does not vanish on the arrival leg: nine middle atom profiles survive.  The `52`-type carrier is therefore correctly understood as THM-3514's target owner-boundary restriction, not as a source support theorem.  On that four-corner carrier, one common torsor offset gives

```text
a=u+c,
b=q+c,
d=b-a=q-u=-s.
```

Independent left and right offsets realize every defect and destroy this identity.  With one offset, the full source common-gauge support is `72/117`; target-active support is `48/52`, with exactly the four `d=0` types killed by first-collision same-root disjointness.

At owner frequency zero the four Walsh drift spectra have exact supports

```text
(13,12,12,13).
```

The two missing entries are exactly the zero modes of the one-sided Haar rows.  Retaining `c` before owner Fourier contraction gives `(13,13,13,13)` at every nonzero owner character.

## Actual endpoint weights

The audit reconstructed AX and BY atom tables, applied both guard kernels at pair level, and performed the `q_H-q_q5` inverse contraction before drift grouping.  It recovers

```text
full endpoint bridge       = 389266878372286537904,
d=0 contribution          = 324498447313453607031,
d!=0 common-support bridge = 64768431058832930873.
```

Both the full and `d!=0` four-row endpoint tables have all `52` Walsh-by-drift Fourier coordinates nonzero.

Pairing the endpoint pair function with the source `39 x 39 x 13` address measure is nonzero at all 13 owner characters.  An initial clean-room transcript retained the common THM-2594 denominator; multiplying by its inverse in the certified split field gives exact entry-by-entry agreement with the submitted normalized pullback.  This was a normalization difference only, not a support or rank discrepancy.

For each owner character, the augmented system retains a `4 x 4` channel mixer and all 13 projective frequency multipliers.  Every full-target and `d!=0`-target system has

```text
rank = 29,
nullity = 0.
```

Thus the nonlinear scalar address pullback is genuine, while a fixed linear channel map followed by one common drift convolution remains impossible.

## Type boundary

The source is an exact finite common-ancestry measure and the endpoint object is a preintegrated scalar pair function.  Their pairing is a lawful finite address contraction.  It is not one integral in which the source, owner, word, endpoint AX/BY observables, and temporal nodes are jointly evaluated before integration.  Therefore it supplies no physical current, absolute `H^1` class, grouped coefficient, row exclusion, or LRC(14) consequence.

## Reproduction

```text
python -B 04-computation/lrc_r5_nonlinear_common_gauge_endpoint_connection_independent_audit_20260816.py
python -B -O 04-computation/lrc_r5_nonlinear_common_gauge_endpoint_connection_independent_audit_20260816.py
```

Artifacts:

- `04-computation/lrc_r5_nonlinear_common_gauge_endpoint_connection_independent_audit_20260816.py`
- `05-knowledge/results/lrc_r5_nonlinear_common_gauge_endpoint_connection_independent_audit_20260816.out`

Script LF SHA-256: `886ef5ae18c97676acf6a6bde8f698468e2197ba22cd7cd2db5d27569e511c51`.

Semantic SHA-256: `1cf457b221107326b2cd7a5bb56a0cb1626934a2e5d8df57dcb04f04eab56d02`.
