# LRC14 Cube-Root Phase/Support Atlas

codex-2026-06-20-S54

## Summary

The cube-root lens survives the next stress test, but in a more modest and more
useful form.  It is not a direct cap-risk scalar.  It is a phase/support address
for the finite resonant atlas.

S52 showed that the user's recursion

```text
A+B+C-D-E-F+G
```

is the pair-tax shadow `H(1)-2(D+E+F)`, not the actual correction.  S54 then
held the relation rank fixed by scanning only consecutive far triples
`(m,m+1,m+2)`, all satisfying

```text
-u + 2v - w = 0.
```

The result: rank one is not enough information.  Across `63063` exact rows,
the sign ledger and risk leaders still move with the mod-7 far phase, the
bounded-core support address, and the `A_2` chamber of the Eisenstein imbalance.

## Exact Takeaway

The direct-risk leader, actual-residual leader, pair-tax leader, and
Eisenstein-norm leader are different rows.  The top-12 overlap matrix is:

```text
           direct actual pairtax eisenstein
direct       12      6      2        2
actual        6     12      0        0
pairtax       2      0     12        5
eisenstein    2      0      5       12
```

So pair-tax and cube-root imbalance see each other, but both see direct cap
risk only weakly.  This clarifies what the cube-root coordinate is doing: it
separates cyclic phase inside the signed multi-far correction, while direct
risk also depends on the bounded-core support/state word.

## Proof Route

For low-height resonant tuples, the next proof shape should be:

```text
finite phase/support atlas
  -> signed Abel/Koksma estimate
```

not:

```text
relation rank
  -> scalar bound
```

The atlas key should retain at least:

```text
far residues mod 7,
actual/pair-tax/pair/triple signs,
A2 chamber of S_omega-P_omega,
bounded-core support histogram or full state word.
```

This meshes with HYP-2680: the signed relation-lattice bound is still the open
analytic gap, but it should be applied after the finite low-height resonant
phase keys are routed.  Otherwise the proof spends cancellation in the wrong
currency.

KPS S19 sharpens the surrounding map: a bounded `C(k)=sup w|Delta_w|` is false,
and the wide branch should run through decorrelation/coverage to the plateau
`Q(k-1)`.  That makes this cube-root atlas more local, not less useful: it is
the low-height resonant address book that should be consulted before invoking
the decorrelation glue or a signed Abel/Koksma tail.

No LRC(14) proof is claimed.
