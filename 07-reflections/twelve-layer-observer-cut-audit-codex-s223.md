# Observer-extension cut payload - S223

The useful correction is that the user's `48 + 12 = 56` should not be read
literally.  The literal sum is `60`.  The exact nearby identity is

```text
56 = 48 + 12 - 4.
```

This extends HYP-3056's observer-cut orbit ledger and HYP-3055's duodecimal
observer-extension bridge rather than replacing either: HYP-3055 gives the
inclusion-exclusion lens, HYP-3056 gives the orbit-ledger format, and S223
pushes the source/sink, self-converse, deletion-fiber, and sector-crossing
payloads into an executable audit.

After rebasing over the incoming S218 observer-extension payload ledger, the
clean handoff is: S218 lists the broad payload fields for HYP-3054, while S223
tests the first A000568/rooted-perspective defect where those fields become
necessary.

That is worth keeping because all three terms have real tournament meaning:
`48=P(5)`, the rooted five-vertex perspective cache; `12=U(5)`, the
source/sink extension slice; and `4=U(4)`, the previous parent/source layer.
The first defect is still `U(6)-P(5)=8`, with fraction `1/7` of the six-class
surface.

S223 adds a finite audit around the `12` layer.  The recurring twelves are:
`P(4)=12`, `U(5)=12`, source-word children in `5 -> 6` equal `12`, sink-word
children in `5 -> 6` equal `12`, `SC(6)=12`, and the S217 `K_{4,5}`
rectangle-cycle redundancy is `12`.  These are not the same object, but they
are the same kind of signal: a quotient boundary where an observer/cut
coordinate is either retained or silently lost.

Two finite details are more valuable than the numerology.  First, source and
sink six-child sets each have `12` classes and overlap in `4`; that gives a
concrete place where the `-4` in the splice can be inspected.  Second, the
`12` self-converse six-classes have deletion decks touching all `12`
five-parent classes.  The self-converse branch is not just a diagonal symmetry
count; it spreads over the whole parent layer.

The cross-sector lesson from HYP-3049 survives unchanged.  Ordered-pair sector
decks by size or internal sector type separate only `55/56` six-classes, while
adding cross-sector orientation separates `56/56`.  This is the small
tournament version of the LRC controlled-forgetting rule: the coarse quotient
was not wrong, but it needed the mixed cut payload as a sidecar.

The abstraction is:

```text
base quotient Q
observer/cut word sigma
stabilizer Aut(Q)
sidecar C(sigma)
sink map Phi(Q,sigma)
legality exit
```

Controlled forgetting should use the recurrence

```text
retained_{r+1} = (retained_r, C_r) / Aut(retained_r)
```

and name the debt

```text
debt_r = kernel(C_r -> LRC predicate).
```

The same form now covers rooted tournament perspectives, residual capacitor
cuts, Haar zeta squares, S217 rectangle/hourglass residues, matrix
observability columns, Moser/fibbinary automata, and perfect-number divisor
lanes.  This is the reusable protocol: do not promote A000568, a half-tiling
count, a matrix scalar, or an automatic language state as a proof object until
its observer/cut payload is retained, reconstructed, annihilated, descended,
or routed to named residual debt.

Assumption challenge: I considered runners, rooted nodes, directed edges,
ordered pairs, incident words, deletion fibers, source/sink slices,
self-converse branch loci, rectangle/hourglass cycles, endpoint owners,
automaton states, divisor lanes, matrix columns, and proof obligations.  The
working vertices are proof carriers.  The preserved predicate is LRC
safe-box route/status information and tournament extension recoverability.
The destroyed information is labelled runner identity, raw word order, full
half-tiling presentation, and scalar-only counts unless a sidecar carries it.
