---
source: kind-pasteur-2026-07-07-S66
status: CONFIRMED (4th independent engine, exact n=3..7) + one NET-NEW conjecture (the
  pure-blue count closed form) + the fibration synthesis. Owner directive: the complete
  blue/black line + node-type structure of the merged metagraph, and the tiling->iso-class
  fibration solved via the line-pair partner.
tags:
  - tournaments
  - merged-metagraph
  - blue-black-lines
  - even-odd-duality
  - tiling-fibration
  - self-complementary
  - pure-blue
---

# The pure-blue count, and the fibration solved

**kind-pasteur-2026-07-07-S66 (HYP-4997).** The owner asked for the complete structural
theory: formulas for blue/black lines and pure-blue / pure-black / mixed nodes at each `n`,
how the even/odd duality organizes them, and the tiling→iso-class fibration solved via the
blue/black line-pair partner. I built a fourth independent engine (canonical form, `|Aut|`,
Hamiltonian-path count all from scratch), confirmed the entire structure exactly, and found
the one piece the concurrent triple all flagged as open.

**Heavy convergence, stated up front.** Three agents derived this same structure in the same
burst — mac-mini THM-643, klein HYP-4851, monad-explorer HYP-4967 (and opus-S139 is on the
fiber allocation). My census agrees with all of them at every checked value; treat the
blue/black line theory as now **quadruply** machine-verified. My contribution is (a) that
independent robustness, and (b) the **pure-blue count closed form**, which all three named as
"the remaining census mystery" without a formula.

## The even/odd duality is the whole skeleton

Everything organizes around one parity, generated by a single involution (the grid reflection
`g` = the tournament complement, THM-280/opus-S18):

| object | on SC classes | on NS classes |
|---|---|---|
| tiling count `tc = H/\|Aut\|` (LEM-003) | **odd** | even (merged `2·tc`) |
| blue (grid-sym) multiplicity `μ♦` | **odd** ≥ 1 | 0 |
| blue line-degree (the T-join) | **odd** | 0 |
| black line-degree (the even graph) | even | even |

This is precisely the owner's *"blues contribute odd amounts, blacks even."* Grid-symmetric =
transpose-fixed = self-complementary, so **the SC/NS split IS the blue/black allocation**:
BLUE = a T-join with boundary the SC nodes (odd everywhere on SC), BLACK = an even graph
(Eulerian, even everywhere). Confirmed 0 exceptions n=3..6, and by the two-involution proofs
(mac-mini/klein) for all `n`.

## The formulas (all confirmed, 4th engine)

With `m = C(n−1,2)` and `e(n) = ⌊(n−1)²/4⌋` (`= k²` for odd `n=2k+1`, `= k(k−1)` for even
`n=2k` — the square/pronic alternation, klein-S13):

- grid-symmetric (blue) tilings `= 2^{e(n)}` = the explicit sub-cube `Fix(g) ≅ Q_{e(n)}`;
- **blue lines `= 2^{e(n)−1}`** = `1, 2, 8, 32` (n=3..6);
- **black lines `= 2^{m−1} − 2^{e(n)−1}`** = `0, 2, 24, 480`;
- pure-black nodes `= NS = (A000568−SC)/2` (merged) = `0,1,2,22` merged / `0,1,2,44` unmerged;
- fibration `#tilings(C)·|Aut(C)| = H(C)`, every class, exactly.

## The line–node-type incidence (exact counts)

Which node types does each color connect? (t and its complement-tiling partner flip(t)):

| n | blue lines | black lines |
|---|---|---|
| 3 | (pureB,pureB):1 *(degenerate: no mixed yet)* | — |
| 4 | (mixed,mixed):1, (mixed,pureB):1 | (mixed,pureK):2 |
| 5 | (mixed,mixed):3, (mixed,pureB):5 | (mixed,mixed):12, (mixed,pureK):12 |
| 6 | (mixed,mixed):30, (mixed,pureB):2 | (mixed,mixed):10, (mixed,pureK):156, (pureK,pureK):314 |

So the **combination law** (matching klein's n=7 exactly): blue lines live only on `{mixed,
pureB}`, black only on `{mixed, pureK}`; **mixed is the sole interface carrying both**;
pure-blue nodes are blue **pendants** and pure-black nodes are black bulk. The `(mixed,pureB)`
count `= Σ_{pure-blue C} tc(C)` (n=5: `1+1+3=5`; n=6: `1+1=2`), so each pure-blue node hangs
off the mixed interface with multiplicity `tc(C)` and **never touches another pure-blue node**
(n≥4). As `n` grows the blue T-join becomes almost entirely internal to mixed (blue
(mixed,mixed) share `3/8 → 30/32`) and the black even-graph becomes the pure-black "sea"
(`(pureK,pureK)` share `0 → 314/480`).

## The net-new: the pure-blue count closed form

The counts `pure-blue(n) = 2, 1, 3, 2, 4` (n=3..7) were flagged everywhere as formula-less and
non-monotone. They fit exactly:

> **CONJECTURE (verified n=3..7).**
> `pure-blue(n) = ⌊(n+1)/2⌋ − [n even]` = `(n+1)/2` for odd `n`, `n/2 − 1` for even `n`
> — the interleaved sequence `2, 1, 3, 2, 4, 3, 5, 4, …` (predicts `n=8 → 3`).
> Hence `mixed(n) = SC(n) − (⌊(n+1)/2⌋ − [n even])` (`0,1,5,10,84`, matching klein n=7).

I verified `n=7 = 4` directly (only the 512-tiling blue sub-cube needs canonicalizing, not all
`2^{15}`). The mechanism (confirming mac-mini's C3): pure-blue = SC classes whose **entire**
fiber is grid-symmetric (`blue-mult = tc`), which forces `tc ∈ {1,3}`, `H ∈ {|Aut|, 3|Aut|}` —
"transitive + circulant-like." At `n=7` the four are `H=1,3,9` (`tc=1`, with `|Aut| = 3^0,
3^1, 3^2` up to `3^{⌊(n−2)/2⌋} = 9` — mac-mini's `H_sym` cap) plus `H=15, |Aut|=5` (`tc=3`,
the Paley `T_5`-type circulant lifted in). So the pure-blue backbone is exactly the
**arithmetically special** classes — the transitive spine plus the Paley/circulant cores — the
same objects that anchor the LRC Paley bridge (THM-640). Honest caveat: a 5-point conjecture
with a subtle "which `tc>1` class is all-blue" mechanism; `n=8` is the real test (`2^{21}`
tilings, or the `2^{12}` blue sub-cube by my cheap method).

## The fibration solved — how many, and *which* tilings

The owner's grand goal: know how many tilings map to each class, and *which particular ones*.
The line-pair partner makes it explicit:

1. **How many:** `tc(C) = H(C)/|Aut(C)|`, always odd (LEM-003 + Rédei odd `H` + odd `|Aut|`).
   SC classes split their odd `tc` into an odd blue part `μ♦` and an even black part `tc−μ♦`;
   NS classes are all black.
2. **Which ones are blue:** the blue tilings are *exactly* the coordinate sub-cube `Fix(g) ≅
   Q_{e(n)}` — constant on each of the `e(n)` grid-orbits of tiles. You can write them down:
   pick any value on each of the `⌊(n−1)²/4⌋` free orbits. These `2^{e}` tilings distribute
   over the SC classes by `μ♦(C)`.
3. **Who each pairs with:** flip(t) = the complement TILING (flip all `m` tiles). Blue lines
   stay inside `{mixed, pureB}`; black inside `{mixed, pureK}`. The partner is a *genuinely
   different tournament* — flip does **not** preserve `H` (only `7/32`, `53/512` of lines at
   n=5,6 are H-preserving; self-loop lines `0,1,4,8`) — which quantifies MISTAKE-033: the
   line pairing is emphatically not the tournament-complement pairing.

So the fibration is: the `2^m` tilings partition into odd-size class fibers (`H/|Aut|`); an
explicit `2^{e}` of them are blue (the grid sub-cube), landing only on SC; the flip involution
pairs them into monochromatic lines whose endpoint-type is forced by the SC/NS type of the two
classes. The **parity skeleton is fully determined** (types, degrees, blue support); the
residual "flesh" is the exact black even-graph matching (near fiber-proportional / quasi-random
at the class level, klein C5) and the `H`-metric that parity cannot see.

## Niche conjectures (the coalescing picture)

- **PB1 (headline):** `pure-blue(n) = ⌊(n+1)/2⌋ − [n even]`; equivalently the interleave
  `2,1,3,2,4,…`. Test `n=8` (predict 3).
- **PB2:** pure-blue nodes are blue pendants of multiplicity `tc` on the mixed interface,
  mutually non-adjacent for `n≥4`; `(mixed,pureB) = Σ_{pure-blue} tc`.
- **PB3 (arithmetic backbone):** pure-blue = transitive spine (`tc=1`, `|Aut|=3^j`,
  `j ≤ ⌊(n−2)/2⌋`) + the `tc=3` Paley/circulant cores — the same arithmetically special
  classes that carry THM-640's Paley bridge. A bridge between the metagraph spine and the
  LRC/Paley extremal objects.
- **PB4:** the three worlds asymptotics — blue T-join `~2^{e}` (`e~n²/4`) on the vanishing SC
  spine, black even-graph `~2^{C(n−1,2)}` on the NS sea, mixed the shrinking O(SC) interface;
  `blue/total → 2^{e−m} → 0`.
- **PB5:** flip is H-shattering (self-loop lines `0,1,4,8`; is that `A?` — 0,1,4,8 doubles then
  ...); the H-preserving fraction `→ 0`, so the line pairing decorrelates `H` — the complement
  tiling is a "random-looking" different tournament, the class-level quasi-randomness (klein C5)
  seen at the tiling level.

## Ledger

- Confirms: mac-mini THM-643 (HYP-4977), klein HYP-4851, monad HYP-4967 (all same burst);
  THM-280/281/282/477, LEM-003, MISTAKE-033, klein-S13 (blue=SC), THM-640 (Paley bridge = the
  pure-blue arithmetic backbone).
- Files: `metagraph_line_census_kps_S66.py`, `metagraph_pureblue_n7_kps_S66.py` (+outs).
- Net-new: the pure-blue count formula (PB1) + the fibration synthesis + PB2–PB5.
- Honest: a 4th-engine robustness confirmation of a heavily-converged result, plus one
  conjecture (PB1) from 5 exact points. Does not prove anything about LRC(14); it completes the
  even/odd tournament-metagraph structure the owner asked to map.
