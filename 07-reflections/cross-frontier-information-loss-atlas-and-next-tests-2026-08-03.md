# Cross-frontier information-loss atlas and next exact tests

**Status: CURRENT SYNTHESIS of proved canon through THM-3277.** The theorem
files named below are the proof sources. This note connects mechanisms and
open contracts; it does not assert a reduction among `LRC(14)`, `FC(3)`,
`JC(2)`, tournament problems, or sequence problems.

## 1. Portfolio and inheritance

The present portfolio has one anchor, one niche cluster, and one wildcard.

### Anchor: LRC(14)

- Closest proved mechanism: THM-3255 isolates the twelve-valued norm phase and
  its eleven-dimensional centered marker sector.
- Canonical hostile: THM-3234's Singer compactification loses literal `q`,
  interval geometry, endpoints and ancestry.
- Corrected near miss: THM-2806 retains a literal sheet but its allocation
  square is flat and does not create an endpoint current.
- Least-used physical sidecar: THM-2791 retains a same-ancestry partial
  rail-sheet germ, stopping just before endpoint allocation.
- New exact boundary: THM-3267 says precisely which abstract endpoint data
  retains the norm phase; THM-3268 and THM-3274 classify its free and three
  constrained varying-increment responses. THM-3269 supplies an intrinsic
  full-critical generator, rooted rank phase and six-edge unimodular sampler;
  THM-3277 extracts the seven-edge forest carrying every minimum simple-path
  target in the genuine critical quotient.

The anchor remains open because the new generator, sampler and backbone are
internal response-graph objects. None constructs the required same-ancestry
physical `(q,R)` record, owner-to-endpoint intertwiner, composable positive
current, row exclusion or LRC decrement.

### Niche: FC(3), response graphs, tournament boundaries and sequences

- Closest proved mechanism: THM-3249/3254/3256 give two promoted response
  faces, covering pairs, a compact selector and a factored distance sequence.
- Canonical hostile: the same count vector can demand opposite members of one
  row pair after the face tag is forgotten.
- New exact boundary: THM-3262 prices this loss for rows `{2,10}`; THM-3266
  proves it for every one of the 24 row pairs covering both promoted faces;
  THM-3275 permits all 22 rows and localizes the remaining obstruction to two
  shared count vectors joined by one reset-order edge.
- Least-used graph sidecar: THM-3260 turns the first-link-blocked pair graph
  into a complete bispanning exchange atlas. THM-3269 selects one weighted
  chart and root, while THM-3277 proves that the phase-preserving 36-chart
  exchange subatlas is connected of diameter five.

This niche remains short of `FC(3)`: the unrestricted memoryless selector is
now refuted for the two bank-I2 faces, but their availability relation is not
Gaussian-moment positivity. Other faces, history-dependent controllers and a
positive functional remain open.

### Wildcard: planar Jacobian infinity walls

- Closest proved mechanism: THM-3237/3257/3263 use saturated critical
  resultants and reciprocal strict transforms to expose square, cubic and
  quartic infinity edges.
- Canonical hostile: local resultant inertia loses branch labels, Keller
  cofactors, inverse-cover sheets and Jelonek data.
- New exact boundary: THM-3265 activates the degree-six coefficient, retunes
  twice, and exposes a quintic edge while 50 finite critical points remain.
  THM-3276 closes the whole `B`-only polynomial clutch family through degree
  eight: every member has a critical point, and the owner-coprime lane retains
  at least 43 off-owner resultant multiplicity units.

The wildcard remains a controlled local no-go atlas, not progress closing
`JC(2)`. Its value is procedural: further retuning of `B` below degree nine is
exhausted, so the next deformation must change coordinates or add the missing
Keller/cofactor data.

## 2. Exact new-result ledger

| theorem | exact gain | mechanism | hard boundary |
|---|---|---|---|
| THM-3255 | whole/single-phase multiplicative ranks `157/168`; whole additive rank `13`, slice rank at most `7`; centered/signed marker rank `11`; every nonzero nonnegative filling marker has rank at least `12`, sharply | disintegrate by the `C_12` norm phase before changing representation | no physical endpoint marker or LRC exit |
| THM-3260 | reset-link core: `12` vertices, `22` edges, `beta_1=11`, nonplanar, `Aut=C_2`; `4,960` complementary tree pairs in one exchange component; delayed relative rank `6` | integral tree/cotree polarizations and symmetric exchange | no faithful `C_12`/`C_7` graph action or vertex torsor; later weights and critical data select internal sidecars only |
| THM-3273 | `Jac(G_0)=C_74748` has a canonical order-12 quotient; vertices occupy six classes with multiplicities `3,3,3,1,1,1`; all 12 pair differences occur, while core edges miss `4,8`; delayed `Z^6` has involution signature `(5,1)` | reduced-Laplacian Smith form, Abel--Jacobi differences and saturated relative homology | no vertex torsor, row-to-owner phase map, positive response or LRC carrier; THM-3269 later fixes an internal full-group generator |
| THM-3269 | exact scale-free edge weights select one of `9,920` ordered charts, root `17`, the primitive generator `[16-17]`, a 12-vertex rank phase and six edges whose eleven directed increments have determinant `-1` | analytic clutch strengths break the graph symmetry; full critical coordinates repair the abstract polarization sidecars | rank phase is nonlinear, the genuine quotient has six vertex fibres, and neither is a physical owner/ancestry intertwiner |
| THM-3277 | all 12 minimum nonempty vertex-simple-path phase targets lie in one seven-edge forest; `205` trees and `36` ordered bispanning charts contain it; their exchange graph has `120` edges and diameter `5` | quotient endpoint pairs by critical phase, then preserve one exact geodesic per target under symmetric exchange | arbitrary walks fail by a cheaper backtrack; endpoint identities, response composition and all physical owner data are lost |
| THM-3262 | rows `{2,10}` need one face bit; threshold-tree Pareto points `(6,23)` and `(8,22)`; collision taxes raw `239`, signed-Parikh `80`, controller-ordered `22` | retain the origin face as a static/catalytic label | tree optimum is only in the declared threshold class; no FC(3) |
| THM-3266 | exactly `24` common fixed row pairs; every pair is obstructed at singleton `(5)`; conflict union `47`, common intersection `6`; one static face bit is exact for all `109,344` tagged cases | the singleton availability sets form a seven-by-seven bipartition and every common pair crosses it | pair identity is supplied; THM-3275 later closes the unrestricted 22-row question for these two faces negatively |
| THM-3275 | among 238 shared nonreset vectors, exactly `(3,4,5)` and `(1,3,4,5)` have disjoint availability even over all 22 rows; they form one directed edge; one origin bit is needed exactly there | compute the complete availability intersection, not a fixed-pair shadow | local two-face response theorem only; other faces, history and Gaussian-moment positivity remain open |
| THM-3263 | degree seven reopens and retunes the old infinity wall, leaving a nonzero quartic edge and degree-`51` residual | divide the wall multiplicity, retain the transverse parameter, then retune | four critical-resultant roots escape; this is not cover inertia |
| THM-3265 | degree six successively retunes `s`, `r`, then the unique `q_*`; a nonzero 58-term quintic-edge invariant leaves degree `50` and one even five-cycle | a lower coefficient activates one layer early after each strict transform | 50 critical points and all Keller/cofactor/cover debts remain |
| THM-3276 | every polynomial clutch `B` of degree at most eight in either THM-3212 accessory field gives a critical point; when `B` is owner-coprime, `deg H_B=52`, at most 9 units lie over `S`, none over `T`, so at least 43 remain off the owner divisor | a triangular eight-jet boundary invoice followed by two coprime untunable Laurent numerators | excludes one deformation family only; no Keller cofactor, inverse cover, Jelonek inertia or `JC(2)` consequence |
| THM-3267 | factorization ladder `q:C_12`, `[q]:C_2`, `det(q,R):trivial`, `([q],det):C_2`; affine translation destroys parity; the negative seam is one-per-phase and direction-transverse | exact norm fibres and quotient censuses in the chosen Singer model | the pinned physical sources do not supply a same-ancestry owner-to-endpoint square |
| THM-3268 | `Q=14J_12-I_12`, spectrum `167,(-1)^11`; two closed forms, order-two recurrence and rational OGFs; uniform `F_(q^2)/F_q` theorem | equitable quotient of the loopless complete nonzero-point walk | increments vary freely; repeating one translation and physical ancestry do not obey `Q` |
| THM-3274 | norm fibre: `12` states and irreducible minimal order `12`; projective line: coarsest `48`-state normalized squared-transverse refinement but modes `12,11,-1`; abstract seam: all `168` states and matrix order `168` with `chi_A=chi_B'/169` | refine the observable until equitable; recover a punctured Cayley spectrum by the principal-minor derivative identity | varying-increment semantics only; no physical ancestry, endpoint current, row exclusion or LRC decrement |

Every row above has an exact companion, frozen output and normal/optimized
replay. THM-3260, THM-3265--3269 and THM-3273--3277 also passed independent
hostile reconstruction.

## 3. One real meta-mechanism: price information loss

The strongest common structure is not a conjecture reduction. It is a typed
workflow:

```text
rich object -> quotient/shadow -> exact kernel or collision census
            -> minimal sidecar -> hostile test on the first consumer.
```

The current instances are:

| source and map | preserved predicate | destroyed information | exact sidecar / next test |
|---|---|---|---|
| parallel maps `(q,R)->q->[q]`, `(q,R)->v=det(q,R)`, and `(q,R)->([q],v)` in THM-3267 | full `q` retains phase; `[q]` and `([q],v)` retain parity; `v` retains none | projectivization loses the internal `C_6`; determinant alone loses all phase | retain full `q` plus a chosen norm gauge; allocate `(q,R)` on one THM-2791 ancestry sheet |
| `(face,n,row-bank) -> (n,row-bank)` in THM-3262/3266/3275 | availability in a fixed pair or the full 22-row bank | which face makes a row lawful | one face-origin bit; for the full bank its dependency locus is exactly the two vertices of `H(12,13)` |
| `(v,w)->[v-w]` from `V(G_0)^2` to the genuine critical `C_12` quotient in THM-3273/3269 | all twelve pair-difference classes and an intrinsic generator | vertex injectivity and physical ownership; the vertex image has six fibres | supply a physical row-to-owner/ancestry map; do not confuse the nonlinear 12-rank label with a group intertwiner |
| full point walk -> norm phase in THM-3268 | endpoint phase-difference multiplicities | point, increment word, intermediate geometry and ancestry | retain the increment automaton; the repeated-fixed-translation walk is the hostile |
| constrained point walks -> phase or its coarsest equitable refinement in THM-3274 | exact transfer counts and scalar recurrences | point identity; phase alone loses `b^2` on a line and all 14 within-phase distinctions on the seam | invoice the minimal state space (`12/48/168` here); keep varying-set and fixed-increment semantics separate |
| nonempty simple paths -> critical target in THM-3277 | one exact minimum clutch product for each of 12 targets | endpoints, intermediate vertices, response signs and composability | retain the seven-edge backbone; test actual response composition and use the cheap backtrack as the arbitrary-walk hostile |
| critical divisor -> saturated reciprocal resultant in THM-3263/3265/3276 | local intersection multiplicity, Newton edge and a uniform off-owner lower bound | root identity, cofactor unit, normalization sheet and affine second coordinate | stop retuning `B` through degree eight; change clutch coordinates or rebuild a branchwise Keller/cofactor record |

This explains why the same small numbers recur without proving the carriers
equal. In particular:

- THM-3255's centered phase sector and THM-3268's `-1` eigenspace are the same
  abstract phase-coordinate space in the chosen model.
- THM-3260's `beta_1=11` is only dimensionally resonant with that space. Its
  graph has no faithful `C_12` action; a vertex label and tree chart were
  load-bearing external sidecars in THM-3260, and THM-3269 later internalizes
  canonical abstract versions without making them physical.
- THM-3273 supplies a genuine intrinsic order-twelve quotient for relative
  divisor classes, not an identification with the THM-3255 norm phase.
  THM-3269 fixes its generator and repairs the edge sampler, but its six-class
  vertex image and nonlinear rank compression still leave the physical
  row-to-owner map open.
- THM-3277 shows that quotient loss can be useful: forgetting endpoints makes
  one tree carry all twelve target minima even though it carries only `48/132`
  endpoint geodesics. It does not make edgewise trap witnesses composable.
- THM-3262/3266/3275's one face bit is an origin tag, not a norm phase or graph
  polarization. THM-3275 refines its cost from “one bit globally” to “one bit
  whose raw dependency locus is one two-vertex edge.”

## 4. The closed-form sequence lane

THM-3268 supplies an actual efficiently computable sequence, not a fitted
recurrence. For `d in C_12`, let `C_n(d)` count length-`n` paths on
`F_169^*` with consecutive points distinct and terminal/source phase
difference `d`. Then

```text
C_n(0)    =14(167^n+11(-1)^n),
C_n(d!=0) =14(167^n-(-1)^n),
C_(n+2)   =166 C_(n+1)+167 C_n.
```

The two OGFs have denominator `(1-167z)(1+z)`. The centered phase-marker
space is exactly the `-1` mode, so the normalized walk multiplies it by
`-1/167` per free step. This turns the one-step `2,184/2,352` census from
THM-3267 into a structural explanation.

The first hostile matters: one fixed translation has the same one-step row,
but at lengths `2` and `13` its profiles are respectively

```text
(12,14,...,14),        (156,0,...,0),
```

not powers of `Q`. The recurrence belongs to the variable-increment operation
response. It is not evidence that a deterministic physical LRC trajectory
mixes by `Q`.

THM-3274 carries out that constrained experiment and shows that alphabet size
does not predict recurrence complexity:

```text
varying norm fibre       -> 12 phase states, irreducible scalar order 12;
varying projective line  -> 48 (phase,b^2) states, only modes 12,11,-1;
varying THM-3246 seam    -> 168 singleton states, matrix order 168.
```

For the projective line every relative-phase count satisfies

```text
C_(n+3)=22C_(n+2)-109C_(n+1)-132C_n,
```

and the three parity classes have explicit combinations of `12^n`, `11^n`
and `(-1)^n`. For the seam, restoring the missing zero vertex gives an
additive Cayley matrix `B`; deleting zero gives `A`, and translation symmetry
recovers its characteristic polynomial exactly by

```text
chi_A(lambda)=chi_B'(lambda)/169.
```

The phase-refining state invoice is therefore `12/48/168`. Every norm-fibre
relative-phase sequence has exact scalar order `12`; the projective family has
at most three modes (and the displayed zero/even classes use all three); seam
scalar projections have order at most `168`, while the full matrix has exact
order `168`. This distinction prevents the common mistake of equating
automaton size with one chosen observable's minimal recurrence.

The next sequence problem has moved outward: classify other structured
increment alphabets by additive stabilizer and coarsest phase-refining
equitable partition, and build the actual same-ancestry increment automaton
from THM-2791. Fixed-increment walks remain mandatory hostiles; they do not
iterate any of the three varying-set quotients.

## 5. Tournament structure: use it only where the relation is intrinsic

None of the newest cross-frontier carriers is naturally a tournament.

- The FC pair atlas is an undirected covering graph. On a covering edge and
  declared face, ties and both-lawful states occur; joint failure occurs for
  non-covering pairs or outside that face universe. Forcing an orientation
  would erase the availability predicate.
- THM-3260/3269/3273/3277's useful structures are bipartite cycle/cut duality,
  exact clutch weights, tree exchange, relative divisor classes and
  vertex-simple paths. THM-3277 gives symmetric exchange a genuine preserved
  predicate--retain a minimum path in every critical phase--and the resulting
  36-chart graph is already undirected and connected. Its six phase fibres,
  reversal ties and cheaper arbitrary-walk backtrack are information, not
  defects to hide with a total orientation.
- THM-3268's point graph contains both directed arcs between distinct points;
  choosing one arc would halve the operation and destroy `Q=14J-I`.
- No intrinsic pairwise observable preserving the Keller target has been
  supplied or proved for the critical-resultant roots in these JC charts.

The legitimate tournament lane remains operation-response: joins,
substitutions, SCC cores, duality, switching and observer coordinates. A new
tournament connection should first declare vertices, a binary observable,
orientation gauge, ties, preserved target and lost data. THM-3277 is the
positive control for the alternative: when symmetric exchange already
preserves the target, analyze that native relation instead of inventing arcs.

## 6. The Jacobian staircase and its stopping reason

The reciprocal wall now has a four-step exact pattern:

```text
degree 9: primitive square-root edge,
degree 8: one tuning -> cubic edge,
degree 7: reopen and retune -> quartic edge,
degree 6: reopen and retune twice -> quintic edge.
```

The degree-six chart gives the exact tuning chain

```text
s=(8r-a*Kappa)/(4r1),
r(q)=(32q r1-a*Rho)/(8Lambda),
q_*=-a*Sigma/(64Theta),
c94=-a^12*Psi/(16Theta^2),
```

with nonzero `Psi` in both accessory fields. The local cycle signs alternate
as the edge length grows, but the physical debt does not: 50 critical points
remain, and no cofactor, inverse cover or Jelonek component is created.

THM-3276 now gives the stopping reason uniformly. For every polynomial `B` of
degree at most eight in either accessory field, the clutch first coordinate
has a critical point. If `B` meets the owner divisor the point is explicit. If
it is coprime, the saturated residual has degree `52`; none escapes through
`T`, at most nine multiplicity units can remain over `S`, and at least `43`
remain off the owner boundary. Eight triangular boundary jets can be tuned,
but the next two Laurent numerators are coprime in both fields.

Therefore further `B`-only retuning below degree nine is exhausted, including
a blind degree-five scout. The next lawful test must either vary the other
clutch coordinates (`C_0`/`E_0` in the inherited notation) or attach a typed
Keller cofactor/inverse-cover record and follow a branch through saturation.
Success is a cover statement or a genuine obstruction, not another local
cycle statistic.

## 7. Ranked next frontier tests

1. **LRC physical handoff:** on the two THM-2791 cylinders, allocate one full
   `(q,R)` while retaining literal contributor labels and a chosen Singer norm
   gauge. Derive its actual increment automaton and test the germ/current
   square against THM-3274's state invoices and THM-3269's genuine phase
   sampler. Failure should name the first field that changes or becomes
   undefined.
2. **FC3 third-face hypergraph:** add one genuinely new support/bank face and
   compute the empty simultaneous-availability hypergraph extending
   `H(12,13)`, now known to be one two-vertex edge. Determine whether one
   origin bit remains enough or a higher-valued/history sidecar first appears.
   Only then test the selected response against the Gaussian-moment consumer.
3. **Next constrained sequences:** classify other structured increment sets by
   their additive stabilizer and coarsest phase-refining equitable partition;
   compile fast recurrences from the resulting minimal polynomial. Keep a
   fixed increment as a hostile and the THM-2791 physical automaton as the
   first non-abstract target.
4. **JC changed-coordinate/cofactor test:** do not retune `B` again through
   degree eight. Vary `C_0` or `E_0`, or attach branch labels and a Keller
   cofactor to the strict transform. Count success only if the target is an
   inverse cover or a genuine obstruction, not another resultant statistic.
5. **Backbone composition test:** on THM-3277's seven edges, ask whether the
   exact trap witnesses can be chosen compatibly along the twelve minimizing
   simple paths. In parallel, test whether full `C_74748` coordinates or a
   bottleneck rather than product objective change the backbone. Use native
   symmetric exchange; orient only an intrinsic target-preserving observable.
6. **Catalytic sequence consumption:** when combining the two FC faces, keep
   separate variables for origin face and pair identity. Specializing the face
   variable to one preserves the distance shadow but is known to destroy the
   selector.

## 8. Procedural conclusion

The session produced progress on all requested lanes without blending their
truth types:

- an internal LRC-adjacent generator, unimodular phase sampler, constrained
  transfer atlas and geodesic backbone, with the physical ancestry debt still
  explicit;
- a complete all-22-row two-face face-blind memoryless selector no-go whose
  entire origin-dependence locus is one two-vertex edge, sharpening the FC3
  response program without claiming moment positivity;
- a uniform planar-JC no-go for every `B`-only polynomial clutch through
  degree eight, with at least `43` off-owner units in the coprime lane and a
  changed-coordinate/cofactor next test;
- a target-preserving symmetric-exchange subatlas that shows where native
  graph structure succeeds and tournament orientation would lose data; and
- exact free and constrained finite-field recurrences, with state invoices
  `12/48/168`, including a cubic three-mode closed form and a punctured-Cayley
  derivative formula.

The unifying research move is now reusable: quotient only after naming the
consumer, compute the exact information tax, attach the smallest lawful
sidecar, and attack the first deterministic or physical hostile before
transporting the result to another frontier.
