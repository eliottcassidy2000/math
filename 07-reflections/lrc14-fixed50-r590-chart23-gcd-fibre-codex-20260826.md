# Fixed-fifty r590 bridge, chart 23, and gcd-fibre tariffs

**Session synthesis / provenance, 2026-08-26.** This is not a proof source.
Current claims are owned by
[THM-4242](../01-canon/theorems/THM-4242-fixed-fifty-direct-r590-tail-and-twenty-three-label-chart.md),
its dependencies, and its frozen exact artifacts. Those sources outrank this
reflection. LRC(14) remains open.

## Portfolio outcome

| lane | inherited mechanism | hostile control | outcome |
|:---|:---|:---|:---|
| Anchor: direct fixed-`50` tail | THM-4170 component discrepancy; THM-4234/4240 split core/petal atlases | the analytic worst cutoff `626` is only sufficient and is already literally safe below it | **PROVED RELATIVE / FINITE-EXACT:** all pool nine-bodies with `{50,r}` are safe for `r>=590` |
| Niche: universal chart growth | THM-4211/4229/4234/4240 face inheritance | safe marginals do not compose; every newly exposed face needs its own head | **PROVED RELATIVE / FINITE-EXACT:** `S=C union {10,15,20,30,264}` gives `chi_50>=23` |
| Wildcard: arithmetic fibres | multiplication-map disintegration and visible factor-5 resonances | a nonunit repeats phases; an arc can meet more than one distinct phase | **PROVED:** a gcd/ceiling fibre tariff for every modulus `h>=2`; the `p=5` census is only a sufficient diagnostic |

The closest mechanism was the fixed-fifty component-discrepancy chart. The
canonical hostile was THM-4207's noncomposition of safe marginals. The
corrected near miss was MISTAKE-520's misuse of a cardinality certificate for
the zero-newcomer layer. The least-used sidecar was the circular component
count, followed by the gcd multiplicity of a modulus fibre.

The concept board was

```text
fixed-fifty body atlas | exceptional literal bridge | face hypergraph
component tariff | gcd fibre | small-label owner ray.  (1)
```

## What moved

### 1. A complete fixed-fifty atlas became a 590 theorem

The split `P=C union O`, with `|C|=18` and `|O|=12`, turns every pool
nine-body into a layer indexed by `k=|B intersect O|`. The ten layers contain

```text
48,620, 525,096, 2,100,384, 4,084,080, 4,241,160,
2,423,520, 753,984, 121,176, 8,910, 220
```

bodies, summing to `binom(30,9)=14,307,150`. Every limiting base has positive
surplus. The raw component-discrepancy maxima are

```text
448, 502, 563, 589, 608, 626, 596, 517, 433, 336.      (2)
```

The useful operation was not to improve the global discrepancy inequality.
It was to literalize only the bodies whose sufficient cutoff exceeds the
desired interface. Among `7,418,664` bodies in layers four through six, just
`24` have `kappa>590`; their finite gaps contain only `258` rows. Every row is
strictly safe. Two independently generated exceptional ledgers agree on all
body masses, component counts, surpluses, and cutoffs, and the referee uses
two exact literal integrators.

The closest new row is still separated from threshold by

```text
51,577,155,731,993 / 562,435,748,674,800>0.            (3)
```

This is the mechanism behind the boundary reduction `626 -> 590`: analytic
cofinal control for the bulk, a complete literal invoice for the exceptions.
It aligns fixed `q=50` with THM-4238's `r>=590` boundary for genuine
`q<=49`. It does not interpolate in the newcomer label and does not close the
first-owner rays `q=51..1289`.

### 2. Face completion raised the chart to 23 labels

The selected petal set is

```text
F={10,15,20,30,264}.                                   (4)
```

The zero-, one-, and two-petal layers were inherited. The exact new invoice
contains every higher face:

```text
10 triples + 5 quadruples + 1 quintuple,
231,540 limiting profiles,
5,946 finite outsider rows,
88,007,232 literal body comparisons,
0 failures, 0 equalities.                              (5)
```

This proves the universal property for every one of the
`binom(23,9)=817,190` internal bodies, hence `chi_50>=23`. The key change from
the previous four-petal session is quantifier closure: all sixteen higher
faces were audited, not merely the maximal quintuple or a promising collection
of marginals.

### 3. Prime fibres repaired to a general gcd tariff

On the fibre of multiplication by `h`, a constraint `e` does not generally
visit `h` distinct phases. With `g=gcd(e,h)`, it visits `h/g` equally spaced
phases, each repeated `g` times. The open unsafe arc has length `1/7`, so the
exact worst fibre loss is

```text
w_h(e)=g ceil(h/(7g)).                                 (6)
```

Union-bounding these exact losses proves the general tariff in THM-4242. It
recovers the one-phase prime lemma when `p<=7` and `p` does not divide `e`.
The open endpoint is essential when `7` divides `h/g`.

Two hostiles locate the boundary:

```text
h=8,e=2:  naive coefficient 7/8 is false since mu(G_2)=6/7;
h=14,e=2: the repaired gcd tariff is attained at 6/7.  (7)
```

For the eleven new chart faces containing `264`, the `p=5` specialization
certifies `12,446,627` of `59,630,016` literal comparisons. The other
`47,183,389` are uncertified, not unsafe. The closest rows use outsiders
`65,70,110`, exposing a common factor `5` as a genuine phase address rather than
a scalar anomaly.

## Connection contracts

### Analytic atlas to literal boundary

```text
source:        fixed body V_B with exact (mass,components)
target:        all newcomers r at a chosen uniform boundary R
map:           (m_B,c_B) -> kappa(B); literalize only R<=r<kappa(B)
preserved:     body labels, threshold convention, exact Haar margin
destroyed:     phase addresses outside the exceptional rows
sidecar:       complete exceptional-body ledger and equality count
decisive test: two exact integrators on every exceptional (B,r).
```

### Petal faces to a universal chart

```text
source:        lower-face theorems plus a proposed petal set F
target:        every K in binom(C union F,9), every permitted outsider
map:           K -> T=K intersect F and core L=K intersect C
preserved:     exact face label, core size, finite-head cutoff
destroyed:     nothing until Pascal aggregation; aggregation forgets the face
sidecar:       one ledger for every newly exposed T
decisive test: sum of audited face layers equals binom(|C union F|,9).
```

### Modulus fibres to finite-head certificates

```text
source:        labelled speed set and modulus h
target:        a certified lower bound for its safe-set measure
map:           e -> (gcd(e,h), w_h(e)); d divisible by h -> d/h
preserved:     fibre multiplicity, unsafe-arc openness, divided system
destroyed:     overlaps among unsafe phase sets
sidecar:       per-constraint gcd weights and divided-system mass
decisive test: compare tariff to exact literal mass on closest resonances.
```

The third map is deliberately one-way. Overlap loss explains why its failed
certificate cannot be read as a counterexample.

## Next sharp fronts

1. **First-owner rays `51..1289` (Anchor, OPEN).** Order the two newcomers
   and fix the smaller one. Build a body atlas at selected owners, then apply
   the same exceptional-gap bridge to a common large-`r` interface. Start at
   gcd-rich owners suggested by the fibre weights, not by consecutive labels.
2. **Full fixed-`50` finite head (Anchor, OPEN).** THM-4242 closes all pool
   bodies only for `r>=590`; its 23-chart closes all `r` only on a subset.
   Construct the joint failure hypergraph for `r<590` and retain literal
   failure atoms rather than separate body marginals.
3. **A 24-label chart (Niche, OPEN).** For a sixth petal `e`, exactly
   `10+10+5+1=26` new faces contain `e` at sizes three through six. Rank
   candidates first by minimum limiting surplus, finite-row cost, and
   multi-modulus fibre coverage; then audit every one of the 26 faces with an
   independent engine. No six-petal chart is claimed here.
4. **Multi-modulus fibre cover (Wildcard, OPEN).** Treat each useful modulus
   tariff as a sufficient certificate edge on the finite literal universe.
   Test whether a small set of moduli covers most or all remaining rows. Keep
   exact literal integration as the hostile control: a cover miss is not
   danger.
5. **Recover phase overlap (Wildcard, OPEN).** The gcd tariff union-bounds
   per-constraint losses. On the closest `65/70/110` rows, record the actual
   unsafe residue subsets and compute their intersections. A positive theorem
   would replace `sum w_h(e)` by an exact residue-union size without returning
   to the full circular wall grid.
6. **Physical entry (Anchor, OPEN).** Direct Haar charts generate structured
   odd-tail families through THM-4150. They do not select a chart or common
   scaling from an arbitrary thirteen-speed row. Any entry map must retain
   the owner label and the scaling/gcd sidecar.

No new META-PATTERNS card is promoted. The successful moves instantiate two
existing patterns—literalize only the analytic exceptions, and restore the
coordinate lost by a quotient. The gcd repair is new mathematics, but one
session does not yet supply evidence from distinct problem threads for a new
meta-level rule.
