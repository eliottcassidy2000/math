# Sporadic at the core, familial by propagation: the deepest available reading of the Jacobian counterexample

**kind-pasteur-2026-07-19-S128c98** (HYP-8090, THM-1310) · owner: *"counterexamples
must be either sporadic or families... deepest possible understanding... as many
explicit examples as you can... for each of the rationals, search back through all
our previous threads... boldly look for patterns... be free in your thinking and
wild in your hypothesis proposal."*

## 1. The answer to sporadic-vs-families, as this session can prove it

The question has a two-sided answer, and both sides are now theorems-or-data:

**RIGID CORE.** The counterexample is generated by ONE plane function and ONE
residue — the design equations (THM-1310 §7):

    B₁ = uW,  B₂ = y + 3xW,  x³B₁ + u³B₃ = R,   W = y²(4+3xy),  R = xu(u+1).

Solving the design system in its natural box rediscovers the owner's map (the
instrument gate passed) and finds it **UNIQUE modulo the trivial z-translation**;
the same architecture at k = 2, 4, 5 returns EMPTY (bounded boxes — detection
floors named in the .out). Three independent rigidity forces converge on it:
Campbell's theorem forces field degree ≥ 3 with non-Galois (S₃) closure — the map
realizes the minimum; the ℂ*-weight arithmetic forces k = 3 among equivariant
templates ("why cubic," THM-1310 §8); and the design box has no moduli. Wild
sharpening (the SEED CONJECTURE, open): *F is the unique minimal counterexample
of ℂ³ up to Aut×Aut and iteration/composition.*

**FAMILIAL PROPAGATION.** From the seed, infinite families follow mechanically,
and they are genuinely inequivalent, not decoration: field degree is an Aut×Aut
invariant, and deg(F^{∘m}) = 3^m, so **the iteration ladder F, F∘F, F∘F∘F, … is
an infinite sequence of pairwise INEQUIVALENT explicit counterexamples** (det
(−2)^m, degree 7^m, fiber size 9 attained mod 11 for F∘F — verified). Add the
propagation monoid: Aut×Aut orbits (infinite-dimensional, trivially), ℂ*-flow
conjugates (the collision triple sweeps the a-axis), dimension padding F × id,
products F × F. Non-injectivity is OPEN under Keller deformation (IFT at a
collision pair), so the entire Keller-moduli component through F consists of
counterexamples — "sporadic" can only ever mean *discrete modulo propagation*,
never *isolated as a map*.

So: **counterexamples = the monoid generated by rigid seeds.** The remaining
mathematical content of "sporadic or families?" is exactly: how many seeds per
field degree, and which field degrees occur? Our data: degree 3 has (at least,
and in-box exactly) one seed; degrees realizable by iteration are 3^m; whether a
PRIME degree-4 or degree-5 seed exists is the sharpest open question this session
leaves, with the instrument built (below).

## 2. What the fiber geometry revealed (THM-1310, compressed)

- The fiber over (a,b,g) is a **conic pair**: 3ax² = u(bx−s) and
  ax³ + gu³ = xu(u+1) — the second is a Fermat-like ternary form in (x, u)
  weighted by the target.
- The fiber cubic is **depressed**: x₁+x₂+x₃ = 0 for EVERY target — a
  conservation law nobody ordered. (The repo reflex "look for sum-zero/balanced
  structure" — the cut-space orthogonality instinct — lands exactly.)
- Δ = −4Q²L: the **resolvent field is ℚ(√−L)** where L is simultaneously the
  cubic's leading coefficient and the **Jelonek set {L=0}** (first explicit
  asymptotic variety of a JC counterexample, an irreducible quartic). The same
  polynomial controls where fibers escape AND which quadratic field measures
  their splitting — degeneration and arithmetic are one object. χ(−L) then
  predicts every single mod-p fiber: 0 violations in 8316 targets. The c97
  statistics are now pointwise-derived, not just distributionally matched.
- {Q=0} is a FOLD of the x-projection (two fiber points sharing an x), not
  escape — my own first guess corrected within the hour: *the discriminant of one
  coordinate's minimal polynomial conflates map degeneracy with projection
  folds.* Filed as a transferable lesson (it is the exact analog of MISTAKE-116's
  "instrument sees its own coordinates" genus).

## 3. The rationals, decoded then archived honestly

Internal decode (mechanistic, complete): s* = −3/2 is the root of 4s+6 — the
collapse of the fiber system at the symmetric target; u* = −1/2; c* = u*/x* =
∓1/2; **13/2 = 2 − 3s* = (4+9)/2, so 13 = 2² + 3²** — the two structural primes
of the map, squared and summed; −1/4 = −1/(4x₀²) with x₀ = ±1, and the whole
collision family sweeps a ↦ −1/(4x₀²) along the ℂ*-flow; det = −2 is visible at
the origin as linear part (z, y, 2x) — a transposition times a doubling.

Repo archaeology (graded per the klein-E8 discipline; death-star and mac-mini
run the comprehensive sweeps — my verdicts on the sharpest hits):
- **13/2**: appears as 1/M of the n=7 covering-min (opus's residual law
  1/M = 6 + 1/2). Same rational, disjoint mechanism — NUMEROLOGY, archived.
- **−1/4**: THM-059's central-factorial variable u = r² − 1/4 = (r−1/2)(r+1/2);
  with the collision fiber living at coordinates built from ±1/2 products, both
  are faces of the "halves lattice" — mac-mini's HYP-8085 2-adic-depth-≤-2
  observation independently says the same. MECHANISM-ADJACENT, shared with
  mac-mini's lane.
- **3/2**: E[H] at n=3 (3!/2²) — NUMEROLOGY.
- REAL bridges (mechanism-level): (i) the map's constants factor entirely over
  {2,3} — the repo's mod-2/mod-3 universality pair is the right lens, and the
  boundary-prime sweeps confirm both primes are special (p=2: det ≡ 0 yet image
  6/8 with a 3-fiber; p=3: Frobenius degeneration, F ≡ (u³z + y²u, y, −x(1+x²z))
  with u³ additive — y is PRESERVED mod 3: the map fibers into plane Keller maps;
  singles fraction 2/3 > 1/2, the predicted anomaly direction). (ii) **S₃ =
  W(A₂)** and the direction cone b³ + 27a²c = 0 is the cone over a cuspidal
  cubic — A₂-discriminant geometry: the monodromy group and the ruling cone carry
  the SAME root-system datum. (iii) the trace law = sum-zero balance. (iv) the
  doubling det −2 / ladder 3^m ↔ Mode-B and n-vs-2n.

## 4. Wild hypotheses (bold, labeled, each with its arbiter)

- **W-ADE (the ladder conjecture):** counterexample seeds are ADE-indexed — the
  degree-d seed has ruling cone = the A_{d−1} discriminant cone and monodromy
  W(A_{d−1}) = S_d. Evidence: d=3 realized (cusp/A₂/S₃). Arbiter: the k=4
  refinement below (swallowtail). Counter-evidence: the k=4/5 boxes came back
  empty, and the equivariance argument kills k≠3 equivariantly — if W-ADE fails,
  the truth is "A₂ is alone at z-degree 1" and higher seeds need z-degree ≥ 2
  (z-quadratic congruences of PLANES, not lines — name: the 2-jet architecture).
- **W-BASE-MULT (the refinement that keeps the hunt alive):** my Bezout
  accounting at k=4 gives intersection 8 − (base multiplicity 3) = 5, not 4 —
  the empty boxes may simply have mis-tuned base points. Choosing R with
  x-order 2 at the base (R ∈ x²u·ℂ[x,u]) re-tunes the spurious multiplicity;
  the k=4 hunt should re-run over the R-stratification by base order. One
  session, instrument ready.
- **W-CHAR3 (design-origin):** the counterexample is the minimal char-0 lift of
  its own mod-3 Frobenius-triangular shadow (Tsuchimoto/HYP-8020 philosophy run
  backwards). Arbiter: attempt the lift construction from F₃[x,y,z] directly;
  if the lift equations reproduce W = y²(4+3s) from char-3 data, the discovery
  route is explained.
- **W-SEED (stated in §1):** uniqueness of the degree-3 seed. Arbiter: opus's
  coefficient-moduli lane + death-star's v(t)-family absorption question (is
  their A-side family = precomposition by plane tame maps? my B-side box was
  rigid mod shift; suspect yes — flagged to them, not asserted).

## 5. What this changes for the fleet

The Lean headline (c97 lead) now has a SECOND, richer target: beyond det+2=0 and
three evaluations, the identities Res_s(C1,C2) = ax³N, Δ = −4Q²L, and the design
equations are pure polynomial identities — a Lean file could certify the ENTIRE
fiber geometry, S₃ pin included (disc-non-square is UFD arithmetic), making it
plausibly the first formally verified structural analysis of any JC
counterexample. The resolvent law gives per-prime decidable instances shaped
exactly like death-star's certificate-module generator.
