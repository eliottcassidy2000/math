# Selectors, rooted sidecars, and operation congruence across open frontiers

**Session date:** 2026-08-22
**Status:** synthesis of PROVED THM-3728--3731, FINITE-EXACT THM-3732,
incoming independently audited PROVED THM-3733--3736/3739/3740, and incoming
PROVED + VERIFIED-EXACT THM-3688/3738/3741 pending independent audits,
together with canonical PROVED implications and named hostile controls. Every
cross-frontier comparison is methodological; no object-level reduction is
claimed.  LRC(14), JC(2), FC(3)/HFC(3), the strong tournament inequality, and
the Rule 30 prizes remain open.

## Outcome first

The session separated two kinds of quotient loss that had repeatedly been
called by the same words (selector, chart, cocycle, or gauge).

1. A quotient may forget a **covariant coordinate while retaining the native
   operation**.  Restoring that coordinate can repair the specified
   equivariance square; downstream operations still require their own audit.
   The LRC valuation quotient forgets the relative coefficient order needed
   by one canonical choice among two admissible equal-depth owners;
   pairwise-distinct positive coefficients restore an equivariant key
   (THM-3731).  A
   tournament quotient retaining only `[K]` discards how a fixed incident root
   transforms; retaining `u -> Du` restores rooted Pfaffian energy exactly
   (THM-3729).
2. A statistic may instead fail to determine **operation incidence or
   chronology**.  Then no static sidecar has yet been proved sufficient:
   retain the full relation/chronology, or an adaptive overflow state, until
   an operation-congruence theorem is established.  On one FC hostile seam
   the inherited named-row transition is empty and the positive fibres change
   size at the endpoint.  The complete THM-3732 census also finds one lawful
   identity-row corridor, so the failure is not universal.  For Rule 30,
   bounded-gap adaptive depth provides a lawful repair; it is not claimed minimal.

The THM-3466 HFC-null-candidate implication is a positive control: its local
boundary primitives differ by invertible additive translations, and zero
total current kills the holonomy once orientation and a basepoint are
retained.  This does not assert existence of an HFC example.  The W004 Jacobian
charge normalization (THM-3728) is a different positive control: it is lawful only after
the upper rows derive every denominator repayment.  It is a weighted
fraction-field row gauge, not a source automorphism.  The two-cube divisor map
is stronger still: it is a proved bijection, so support, multiplicity, and
Dirichlet questions may genuinely be transported along its fibres.

## Incoming extension pass

The late JC arrivals sharpened the operation audit rather than supplying a
cross-frontier reduction.  THM-3733 closes two persistent-arm placements on
the same W004 scalar fibre `12+20` as THM-3728, but its active address is `12`
whereas THM-3728 anchors `20`.  Endpoint arm transport and row-derived charge
normalization are therefore complementary proofs, not interchangeable gauges.
THM-3735 closes two more persistent-arm placements on fibre `02+11`.

An inherited THM-3613 ledger probe found the THM-3739 scalar-01 pair at
exactly the affine keys

```text
(1,(-1,1),(0,-2)),     (1,(0,-2),(-1,1)),
```

both with even/odd arm exponents `(2,1)`.  Incoming THM-3739 confirms the
parity exit, handles `n=1` by the singleton gate, and closes odd `n>=3` by
Mobius first integrals and incompatible cube degrees, including the merged
`n=3` placement.  Shared output weights with THM-3728 or THM-3735 still do
not preserve the other output, scalar address, or normalized row operator.
Together with audited THM-3728, this closes the stabilized-tail ledger, not
exceptional W004 placements or W005/6.

THM-3736 supplies a genuine but JC-internal transport: a Jacobian-one source
shear carries each triangular constant-right closure to a diagonal THM-3734
tower while preserving polynomial-mate existence.  THM-3740 now proves the
next one-variable right-shear gate: its connection integrates to a source
translation, and every survivor stays in the same no-mate tower.  A live Cohn
escape needs two interacting polynomial right factors or a different core.
None of this maps to THM-3732's reset relation, THM-3466's boundary primitive,
or THM-3730's divisor fibres.

THM-3688 uses a different kind of exact compiler: an Apéry gap quotient turns
the exceptional quartic actual-ring `J_1,J_2` lift into a finite square solve,
with the omitted coordinate checked separately.  It is PROVED + VERIFIED-EXACT
but still pending independent hostile audit; `J_3+`, globalization, and JC(2)
remain open.  Pending-audit THM-3741 extends THM-3738 across the axis boundary:
it classifies every nonsingular two-charge radial component and proves that
only the linear class has a polynomial mate.  Third-charge and nonradial
sectors remain.  Its `XT` charge has no typed map to the W004 collision-row
charge, despite the shared word “charge.”  THM-3737/3742/3743 are only
reserved namespaces.

## Euclidean address versus coefficient transport on the 182 skeleton

A new exact companion on THM-3710's thirty-chart non-cover control separates
an address from an operation cocycle.  Canonical continued-fraction words and
THM-778 midpoint traces distinguish all 26 skeleton phases.  On each of the
60 signed chart fibres,

```text
omega(j)=c(j+1)-c(j),
```

together with one integer basepoint reconstructs every coefficient, corner
deletion, characteristic-13 Hasse jet, and exact-182 residual.  The thirty
charts collapse to 13 complete periodic-profile classes.

This is a Euclidean-addressed phase-grid coboundary, not coefficient descent.
The first literal stripping steps `43/182->10/43` and `55/182->17/55` land
outside every maintained chart counter.  Even at the leaves, the same grid
edge has increments `-721` and `-677` in two actual charts, and the same
word at `1/182` carries coefficients `1280` and `1192`.  A basepoint-free
shift changes an exceptional corner-deleted radical order from two to zero.
Absolute clock lifts, owners, semantic roots, and arrival have already been
aggregated away.  Thus the ordered word is a lawful phase address but still
does not pay THM-3731's owner-conditioned drift/arrival interface.

Exact control: [Euclidean phase-grid companion](../04-computation/lrc182_euclidean_phase_grid_coboundary_probe_20260823.py)
and [frozen output](../05-knowledge/results/lrc182_euclidean_phase_grid_coboundary_probe_20260823.out).

## A three-gate selector audit

### Gate 1: stabilizer-fixed section

For a `G`-set `X`, a `G`-set of choices `J`, and a `G`-equivariant
admissible-choice field `A(x) subset J`, an equivariant deterministic selector
on the orbit of `x` exists exactly when `A(x)` contains a point fixed by
`Stab_G(x)`.  The proof is the usual orbit definition `f(gx)=g j` and its
well-definedness condition.

On the THM-3731 labelled valuation lift indexed by `165=150+15` orbit representatives, all 150 strict profiles
pass.  Each repeated `(1,1,c)` profile fails because its stabilizer swaps the
two admissible depth-one owners.  Full positive coefficient data repairs the
failure through

```text
argmin_i (nu_13(c_i),c_i),
```

which is `S_3` blocker-relabeling equivariant and invariant under common
positive-rational dilation.  This repairs only the owner label.  The first
unpaid interface is positive aggregate/selected-atom drift to positive drift
on the canonical owner atom; temporal-word and owner/deep-root transports are
later interfaces.  The abstract-interface THM-2478 hostile has owner drift zero and
total drift `21/742586`.

### Gate 2: predicate-preserving seam

For local witness fibres `F_a(x)`, declare the actual overlap relation before
using torsor or Cech language:

```text
R_ab(x,x')={(p,q): p in F_a(x->x'), q in F_b(x->x'),
                    and the named transition preserves the certificate}.
```

Test nonemptiness, cardinality, invertibility, composition, and the preserved
predicate.  On the named THM-3732 FC bank-I2 charts `F12,F13`, the common physical edge

```text
(3,4,5) -> (1,3,4,5)
```

has disjoint ten-element positive named-row sets, no equal positive response
magnitude in the inherited raw 22-row normalization, and no common next reset
edge.  The availability sizes then change from `10/10` to `9/10`.  This
excludes the inherited fixed-fibre
named-row/common-edge Cech torsor model.  It does not exclude arbitrary
relabelled torsor actions on separate finite sets.

The complete graph changes the strongest lawful conclusion.  Among all 568
edges, the inherited identity relation has five full bijections and a unique
three-edge constant 12-row corridor.  Thus “no identity subatlas” is false.
The raw equal-positive-magnitude relation is instead empty on 567 edges and a
non-full singleton on the last; all 1,132 length-two intersections are empty.
This yields a full finite-exact chronological obstruction in the inherited
raw gauge while preserving the positive identity corridor.

The native bank sidecars explain both outcomes.  Upset size is injective on
all 22 named rows.  Retaining it while transporting root-1 multiplicity by
`4->3->2->1` preserves the corridor and its two full length-two windows;
requiring a frozen source/target root tag would falsely kill them.  On the
magnitude singleton, rows 15 and 1 share degree five but have upset sizes
five and one.  The physical root continues lawfully, yet coefficient equality
dies on the next edge; equal upset size kills it already on the first.
THM-3278's core restriction leaves two five-row `S`-half corridors, but no
`F` row or canonical root 17.  This is a typed subatlas, not an HFC carrier.

The HFC primitive contrast is exact under THM-3466's HFC-null-candidate
premise.  Local primitives of `g^2 d(conjugate g)` differ by additive
constants, so the transition is an invertible affine translation.  Zero
boundary current makes the cyclic sum of constants zero; orientation labels
the transitions and a basepoint fixes the remaining additive gauge.  Even
here the slit-annulus hostile shows that a primitive can fail to distinguish
sheets: basepoint and sheet are different sidecars.

### Gate 3: next-operation congruence

Even an injective observer on a frozen bank need not commute with the next
operation.  The decisive square is not state separation but whether the
observer of the successor is determined from the observer of the state and a
declared finite sidecar.

The Rule 30 selected three-ray bank is injective on 23 physical signalizers,
yet ambient words with the same selected output have different first-return
gaps and successor outputs.  The repaired theorem charges depth `D+B`: it
recovers the gap and depth-`D` successor when the gap is at most `B`, and
otherwise returns the honest overflow `gap>B`.  Thus chronology is required
by this first-return observer, not a cosmetic chart label.

## Rooted Pfaffians: an exact sidecar and deletion law

By THM-3729, for a real skew matrix `K` and root `u`, the odd rooted Pfaffian energy is

```text
sum_(S odd) Pf([[K[S],u[S]],[-u[S]^T,0]])^2
 =det(I+K) u^T(I+K)^(-1)u.
```

The general switching law is `(K,u)->(DKD,Du)`.  Resetting `u` to the all-one
vector changes a five-vertex control from odd numerator `128` to `32`.
Averaging instead over every sign root kills cross terms and yields the new
deletion identity

```text
2^(-n) sum_(u in {+/-1}^n) E_odd(u)
 =1/2 sum_(v in V(T)) disc(T-v),             n>=2.
```

The older switching-class transversal sharpens this operation law.  Every
vertex ordering imposes path-graph sign equations that select exactly one
switched representative in which it is a Hamiltonian path.  Consequently

```text
avg_(T' in S(T)) H(T')=n!/2^(n-1),
avg_(T' in S(T)) E_odd(T';1)=1/2 sum_v disc(T-v).
```

Hadamard plus `m!>=m^(m/2)` makes the Hamiltonian mean dominate both
the constant even-energy mean and the odd-energy mean separately.  This is
a genuine class-level reservoir, but it does not control a fixed all-one root
in a fixed representative or the average of the maximum.  A raw
induced-subtournament-square route already fails at five vertices
(`272>16*15`) even though the signed rooted numerator is only `128`.

## Genuine versus cosmetic transports

The session produced four useful controls for future bridge claims.

| source | map | preserved predicate | destroyed unless retained | verdict |
|---|---|---|---|---|
| LRC full coefficients | labelled valuation tuple / sorted `S_3` orbit | admissible depths | relative magnitude in a valuation tie / blocker labels | repair by labelled coefficient sidecar |
| tournament `(K,u)` | switching class of `K` | even determinant energy | transformation of the incident-root response | repair by covariant root sidecar |
| THM-3728 anchor-20 W004 scalar-12 odd branch | `U=HK^2`, `Pi=K^R f1` | weighted zero/scalar Wronskians | polynomial divisibility and support word | lawful only after upper-row repayment; even scales use THM-3613 |
| `(m,x,y)`, `0<x<y` | `(m,x,y)->(m,d=x+y)` | `m` and the exact good-divisor representation | none for this bijection; projection to `m` later loses fibre count | genuine bijection; the later collision tax is exact |

THM-3730 makes the last distinction quantitative.  Indexed positive
distinct representations have asymptotic

```text
kappa X^(2/3)+O(X^(1/3)),
kappa=Gamma(4/3)^2/(2 Gamma(5/3)),
```

and both indexed and deduplicated support series have abscissa `2/3` and
diverge at the boundary.  The indexed series has a computed real-axis
critical coefficient.  The support divergence instead comes from a uniform
inert-prime additive-split singleton-fibre construction.  Its row mass is
`2kappa/p+O(p^-2)` with explicit one-sided errors, and elementary
Euler-product/Chebyshev estimates now give

```text
H(X)>=kappa log log X-6.3745826010...  (X>=e^3).
```

The repeated `kappa` is the angular integral of the same superellipse.  This
is a lower bound for a disjoint subfamily, so no support asymptotic or critical
coefficient follows.  It is a lawful divisor-fibre transport from THM-463,
unlike the retired tournament/1729 numerical resemblance.

## Ranked next tests

1. **LRC:** on actual scalar-cover rows, test owner-conditioned drift and a
   same-ancestry source/target coupling before attempting word or root gluing.
2. **Tournament:** the switching means are now exact and individually ordered;
   seek a transport or concentration statement coupling the same
   representative's `H` and fixed-root response.  Separate mean inequalities
   are insufficient.
3. **FC:** retain the full relation-valued reset nerve and chronology.  Seek a
   lawful subrelation with constant fibre and composable named transitions,
   rather than imposing an arbitrary torsor.
4. **JC:** move beyond the now-closed stabilized W004 tail without exporting
   its row gauges to exceptional W004 or W005/6.  In the Cohn lane test two
   interacting right factors; in component design add a third charge or
   genuinely nonradial coupling.
5. **Two cubes:** the inert-prime family now proves logarithmic lower growth
   with coefficient `kappa`; determine whether the rest of the support changes
   that order, or obtain a genuine asymptotic for `C(X)`.  Neither a full
   support coefficient nor a collision-tax asymptotic is known.

Apply the gates demanded by the proposed consumer: the stabilizer gate for an
equivariant single-valued selector, the seam/invertibility gate for an atlas or
cocycle, and the congruence gate when an observer is used across a native
operation.  Passing one gate never substitutes for another; failure narrows
the lawful output to a marked/set-valued, relation-valued, or adaptive object.
