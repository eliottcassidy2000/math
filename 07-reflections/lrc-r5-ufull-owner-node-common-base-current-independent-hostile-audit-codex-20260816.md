# Independent hostile audit: the owner-node bridge survives only as a one-dimensional residue signal

**Status:** FINITE-EXACT INDEPENDENT HOSTILE AUDIT.  The narrow one-common-base
owner-node integrand and its nonzero thirteen-residue role bridge are accepted.
The proposed genuine `7 x 13` cell/residue spectral closure is rejected: the
table is the rank-one tensor `delta_0(ell) R(t)`.  No theorem is reserved.

## Inheritance and independent route

The closest proved source mechanism is THM-2471 together with the realized
`r=5` profiles in THM-2594.  The closest endpoint mechanism is THM-3514 and
its audited THM-3479 `U_full` interval engine.  The canonical hostile is now
MISTAKE-417: full multidimensional Fourier support can come from a separable
boundary delta.  The least-used sidecar is the joint split field needed because
the source and endpoint grids do not embed into one another.

The audit fixed the intended integral before reading the candidate:

```text
Gamma_(alpha,beta,tau)
 = zeta_13^beta int Q(13y) e(57122y)
     [sum_u U_u(y) E_(alpha,beta,tau)(w_u)]
     [sum_q V_q(y) E_(alpha,beta,tau)(w_q)] dy,

w_u=(y+u)/13.
```

It imports only the canonical THM-2594 and THM-3514 dependencies.  Its
constructor is disjoint from the candidate: an independent transfer fold
rebuilds `U,V`; endpoint intervals are split directly at the thirteen sheet
boundaries; `Q(13y)` is materialized as an explicit pullback interval set; and
a prefix harmonic primitive replaces the candidate's atom-bucket/x-sweep
integrator.  Five guarded and guard-deleted rows are also rebuilt by the
THM-3479 reference boundary engine and compared pointwise after desheeting.

## Exact agreements

The common coordinate and field order are

```text
C = lcm(T_source,13 T_endpoint)
  = 9684279613402457983920,

L = lcm(T_source,13^2 T_endpoint)
  = 125895634974231953790960 = 13 C.
```

For

```text
p = 755373809845391722745761 = 6L+1,
g = 23,
rho = g^6 = 148035889,
```

the audit verifies the complete factorization of `p-1`, every Lucas gcd gate,
and exact order `L` for `rho`.

The source reconstruction has profile digest
`2de29f9be7fd16ceb4be5d15f7a71aa3d09f2907ec39f7af0b2017eadf3c18d2`.
Its service numerator is `168908463464745122312762880`, which gives exactly
`169 I_5`; the same-root term is zero on every source profile segment.  The
only source support types are `(1,12)` and `(2,11)`, of respective measures
`42548128262640` and `255288769575840` on the source grid.

The sheet arithmetic is exact:

```text
Q(169 w_u) = Q(13y),
-13 w_u + 742599 w_q = 57122 y + (-u+57123q),
```

so the branch phase is one and the remaining external endpoint phase is
`zeta_13^beta`.  The independent bank reproduces the candidate hashes for the
complete coupled gamma table, erased gamma table, inverse tables, coupled
spectrum, and output-ANOVA spectrum.  It also reproduces

```text
q_H       = 125385278409587426725290,
q_q5      = 657486478079327229022863,
bridge    = 223272610175651920448188,

erased bridge = 632148865111268231500111.
```

At relation class `(1,0,6)`, the seven-entry profile is

```text
(317699132065964946247468,0,0,0,0,0,0),
```

and all seven formal `F_7` transforms equal its first coordinate.

## Decisive hostile and corrected interpretation

The endpoint owner factor has speed thirteen and zero shift.  Therefore

```text
||13 w_u|| < 1/14
 <=> ||y+u|| < 1/14
 <=> ||y|| < 1/14.
```

This is exactly source cell zero almost everywhere.  Consequently all `2197`
character rows have support `(2197,0,0,0,0,0,0)`, the inverse table has rank
one, and

```text
T(ell,t) = delta_0(ell) R(t),

ANOVA(T)
 = (delta_0-1/7) tensor (R-mean(R)).
```

The support ledgers `(91,1,6,12,72)` and `(72,0,0,0,72)` are numerically
correct but do not certify two-coordinate mixing.  A delta vector has all
seven Fourier modes.  This independently confirms MISTAKE-417 and rejects the
old spectral-closure interpretation.

The source-erasure hostile replaces both source profiles by one and produces
the endpoint root-count square.  It remains nonzero and has the same delta-cell
factorization.  The coupled and erased residue vectors differ, so the source
weights genuinely change the one-dimensional signal; they do not create a
second cell coordinate.

## Typing audit

- **Common base / Fubini:** accepted.  Both finite root sums are multiplied
  before the single `y`-integration.  No product of separately integrated
  endpoint marginals occurs.
- **Normalization:** accepted for the scoped finite-field comparison.  The
  independent numerator profiles carry the canonical `169*13^10`
  denominator, inverse Fourier normalization is `13^-3`, and all omitted
  harmonic antiderivative constants are common nonzero units.
- **Sheets and signs:** accepted.  `Q` is sheet-independent, the branch phase
  is integral, the surviving harmonic has positive frequency `57122`, the
  endpoint phase is `zeta_13^beta`, and the inverse convention is
  `zeta_13^(-alpha-tau*t)` for `(1,0,t)`.
- **Arrival/source time:** not supplied.  Multiplying `E(w_u)` and `E(w_q)`
  gives a legitimate linked-node observable on one expanded Boolean fibre,
  but it does not identify THM-2471 arrival atoms with source atoms or transport
  the `U_full` row chronologically.
- **Meaning of “current”:** acceptable only with the explicit word
  **candidate** and the one-common-base formula displayed.  “Owner-node
  common-base integrand candidate” is safer.  It is not a physical or lawful
  THM-2512 current.

## Reproduction and scope

Run

```text
python -B 04-computation/lrc_r5_ufull_owner_node_common_base_current_independent_audit_20260816.py
python -B -O 04-computation/lrc_r5_ufull_owner_node_common_base_current_independent_audit_20260816.py
```

Both outputs are byte-identical and reproduce the stored transcript.  LF
SHA-256 values are

```text
script   72e819f1cae92e8969516dc79215e683c538e80da64c9e989e2c5115aebb5304
output   01738d82f1e0895ec294d3e716deca245567cd3e568b51d9e6669d67684a47b2
semantic 88d4be52bcb16a52ab2656ff0c0b6bf70e33a2174652ee7e1df62376426f24e6
```

Accepted: one common base, actual source root-service profiles, actual
`U_full` endpoint indicators, literal guard restoration, pointwise-zero
same-root sector, and a nonzero thirteen-residue role bridge.

Not obtained: exact `C(a;X,m)`, source/arrival transport, `U_clock` chronology,
a physical current, a two-coordinate seven-cell closure, row exclusion, or
LRC(14).
