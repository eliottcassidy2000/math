# The first-forgotten-coordinate transgression

**Instance:** root-sic-jc-lrc-2026-07-28.
**Type:** synthesis built on proved THM-2801/2803/2806/2807/2808,
THM-2811/2812/2815/2816/2817/2820/2824/2828/2839/2840, and the
THM-2796/2799/2800/2805 response atlas.  This note introduces no new
dependency.  Its purpose is to keep four superficially similar cancellation
mechanisms typed.

## 1. Concept board

The useful live objects are:

1. an annihilating functional;
2. the multiplication with respect to which powers are taken;
3. a quotient or averaging operation;
4. the first coordinate destroyed by that quotient;
5. a marked descendant that restores visibility; and
6. a positivity/metric structure, if one exists.

Every claimed transfer must specify all six.  Matching only the vanishing
formula is not enough.

## 2. SIC: a moving endpoint jet

For the THM-2801 two-pair witness, the Hopf reduction turns the pure
contraction into

```text
2^(-m)[u^m](1+u)^(m-1) A_m(1+u),
A_m'(1+u)=(-u(2+u))^m.
```

The order-`m` endpoint flatness kills the requested coefficient.  Multiplying
by `Z^s` shifts the extraction to `u^(m-s)` and exposes

```text
binom(m-1,s-1) A_m(1).
```

The quotient is angular averaging/constant-term extraction; the forgotten
coordinate is the endpoint Taylor order; the marker is `z_2^s`; and the
power `m` creates a new stencil at every level.  This is a Weyl-contraction
statement.

The surviving coefficient also carries a radial factorial factor.  THM-2810
proves that the characteristic-zero factorial functional has zero algebra
annihilator and unbounded Hankel rank.  THM-2815 now gives the sharp finite
positive boundary: through horizon `H`, the smallest time-homogeneous or
pointed multiplicative carrier has dimension

```text
floor(H/2)+1.
```

At even horizon `2D-2` its relation is the affine Laguerre family
`ell_D+c ell_(D-1)`; the next odd moment fixes `c=0`; degree `2D` is the
first readout alias.  This does not create a fixed all-degree carrier.
Moreover, the inverse-Hankel selectors require the inserted observations
`E[(ZW)^iP^m]`, whereas scalar nullity supplies only `i=0`.  Thus a transfer
of the whole SIC observer ladder must retain growing radial height and an
inserted-observer bank, or use a genuinely prime-local whole-layer
mechanism.

## 3. JC response: one fixed barycentric stencil

For a balanced response, THM-2796 gives

```text
F'/F=C/M.
```

At the roots `a` of the squarefree degree-`r+1` polynomial `M`, the signed
orders are

```text
w_a=C/M'(a).
```

Lagrange interpolation therefore gives

```text
sum_a w_a H(a)=C [x^r](H mod M).
```

This one fixed node set kills degrees below `r` and forces the next defect.
THM-2811 identifies the SIC binomial weights as its equispaced cousin:

```text
(-1)^(m-j) binom(m,j)=m!/M_m'(j),
M_m(X)=product_(j=0)^m(X-j).
```

The resemblance is exact, but the grading is not.  A response has one
fixed geometric stencil in one degree.  SIC requires a coherent
multiplicative tower whose node set grows with every power.  The missing
sidecar is an inter-degree multiplication law compatible with chart entry
and Faber flux.

THM-2808 removes the last apparent modulus in the `e=2` response layer.
For three poles,

```text
D=x^a(x-1)^b(x-lambda)^c
```

has exactly two non-pole critical points, and their equal-value condition is
a squarefree degree-`N-3` Maxwell polynomial.  The same number counts marked
noncrossing two-chord dessins.  Thus the open JC debt is not an accessory
parameter in the abstract response: it is entry of an arbitrary Keller map
into the response chart and compatibility with the independent flux
equations.

The same picture now extends in two directions.  THM-2816 replaces the
two-chord count by a ribbon-tree law: at maximal pole count `h=e+1`, every
ordered positive pole partition has

```text
(e-1)! binom(N-e-1,e-1)
```

clean Nielsen/response classes.  THM-2817 solves the first algebraic
`e=3` chamber rather than merely counting it.  For `N=6,h=4`, all ten
ordered accessory ideals are radical of length two, and their twenty marked
charts reduce to the two unmarked carriers

```text
x^3(x^3-1),                         E=x^3-1/2,
(y^2-1/4)^2(y^2-1),                E=T_3(y)/4.
```

So the first ternary accessory problem is also closed.  THM-2840 then solves
the highest-symmetry heptic passport `(2,2,2,1)`: its six marked charts are
the affine normalizations of one unmarked `T_7` carrier.  The linear factor
in `D-v=S E^2` is therefore the affine pullback of the simple Chebyshev
endpoint, not a new modulus.  The neighboring `(4,1,1,1)` and `(3,2,1,1)`
passports remain open.  Neither the all-degree ribbon count nor these
accessory classifications pay Keller-chart entry or Faber flux.

## 4. LRC: two multiplications on one coefficient vector

THM-2803 puts every canonical thirteen-cycle endpoint profile in two
different algebras:

```text
pointwise functions K^(C_13),
cyclic convolution K[C_13].
```

Every profile is a unit in both, but THM-2791's two-point semantic chain is
only a convolution unit.  The full circulant transition groupoid is flat;
all projective minors prove that it cannot be reduced to scalar times cyclic
translation.  Every two determinant fibres instead form a `[13,2,12]`
coefficient code.

The finite-function marked-power law uses pointwise multiplication:

```text
Lambda(f^m)=0,
Lambda(H f^m)=(H(x_0)-H(x_1))a(y)^m.
```

This is the same first-forgotten-coordinate shape as SIC, but not its
operator.  The quotient forgets the high address digit, the marker is that
digit, and the missing sidecar is a physical allocation of the semantic
cylinder to one endpoint-origin atom.

THM-2807 reduces the next physical test to three positive cylinders
`n_0,n_+,n_a`.  Modulo `169`, their endpoint coordinates are `(85,98,98)`.
THM-2803 makes any two distinct coefficient positions a determinant-fibre
decoder.  Hence a common-scalar allocation of the pure `Z1` pair already
determines the fibre; the third vertex is the exact
`Z2^4079` high-digit holonomy test.  Independent scalars or separately
integrated marginals destroy this decoder.

The literal test is now negative in the precise physical sense.  On the
audited `tau=12` simplex, the common allocation support `M` is empty and the
survivor is target-only.  On the separate 81-label common/right bank,
nonemptiness agrees cellwise in all `567` cells (with `193` positive cells),
yet

```text
M intersect R=empty
```

in all `567`.  Marginal presence therefore cannot be upgraded to a common
current by coefficient transport.

THM-2813 supplies a conditional affine normal-jet decoder, while THM-2814
separates two coefficient branches: a physically normalized Segre square
may have nonzero additive face with projective cross-ratio one; four
untrivialized vertex lines first interact through nontrivial square
holonomy.  THM-2820 explains why the existing filtered top residue does not
supply the missing jet: pointwise Boolean
idempotents have no infinitesimal lift, while the cyclic norm
`N=(g-1)^(p-1)` is invariant under `g->g^u` even though the cotangent
generator scales by `u`.  The missing sidecar is now the pair

```text
(oriented augmentation generator, common physical off-sheet atom).
```

The rooted half-step calculation sharpens the same boundary.  The three
exceptional target-twelve live chains have regular `C_13` translation
orbits and normalized first jets `(7,1,9)`, as THM-2839's prime-power unit
criterion predicts.  Yet root shifts `(6,12,4)` kill those absolute jets,
and multiplication by one half-step flips every interior tooth, leaving
only the finite endpoint defects.  The scalar chain also forgets the second
carrier axis and cannot intertwine a target-preserving physical motion with
regular target convolution.  Thus the smallest surviving object is not a
scalar tooth word but a decorated birooted chain cospan carrying the
half-step, target, carrier/address, native-mask, endpoint, and ancestry
coordinates separately.

## 5. What the KLS theorem can and cannot see

[Letwin's 2026 quadratic-form theorem](https://arxiv.org/abs/2607.24164)
starts with an isotropic log-concave probability measure on a real vector
space and controls the variance of a quadratic form by its real gradient
energy.  That is a positivity-and-metric statement.

The current endpoint objects are cyclotomic coefficients and finite-field
specializations.  Nonzero minors prove support, not an Archimedean lower
bound.  Their complex signs need not define a probability measure, and the
address walls do not form a convex log-concave state space.  KLS therefore
does not supply the missing LRC allocation.

There is a useful positive control.  On the uniform `C_13` cycle, the
two-point semantic coefficient word

```text
f=c(1_6+1_7)
```

has

```text
Var(f)=22c^2/169,
E_cycle(f)=c^2/13,
Var(f)/E_cycle(f)=22/13<2,
```

when `E_cycle=(1/(2*13))sum_j(f_(j+1)-f_j)^2`.  This is a
KLS-shaped numerical inequality, not an application of KLS and not uniform
over the endpoint bank.  It becomes potentially relevant only after a
lawful positive physical state graph and measure have been constructed.

## 6. The chord/ribbon relation is the right tournament replacement

In the three-pole response, the intrinsic binary relation is not on the
roots themselves.  It is on four cyclic endpoints: two endpoints are
related when they form one of the two zero transpositions.  Crossing chords
produce one pole cycle; noncrossing chords produce three.  If the four
positive cyclic gaps are `g_1,...,g_4`, a noncrossing pairing has cycle
lengths

```text
g_1, g_3, g_2+g_4.
```

Choosing which pole cycle is split gives

```text
sum_j(p_j-1)=N-3
```

marked charts.  Burnside rotation then gives the orientation-preserving
unmarked sequence

```text
1,2,6,10,19,28,44,60,85,110,...
```

with exact term

```text
(N-1)(N-2)(N-3)/12 + 1_(2|N)(N-2)/4.
```

This is a genuine relation/chord model: vertices, cyclic order, crossings,
and the preserved passport are explicit.  Forcing an orientation on
unrelated roots would add no information.

For general `e`, the planar dual of a noncrossing `e`-chord matching is a
labelled ribbon tree on the `e+1` pole regions.  A vertex of degree `d_i`
retains a cyclic order and a positive composition of its pole length into
`d_i` corners.  Prüfer degree multiplicities cancel the ribbon-order
factorials, and multivariate Vandermonde gives the THM-2816 count above.
This is the correct generalization of the chord relation: crossing creates
a handle and destroys two pole cycles; abstracting to a bare tree forgets
the corner-composition sidecar.

## 7. Highest-leverage next tests

1. **LRC anchor:** search THM-2818's off-sheet right-cofiber copies for the
   first literal common atom, but retain the oriented augmentation generator
   separately.  The stopping condition is now exact: marginal nonemptiness,
   a top norm, or projective coefficient holonomy without within-cell
   ancestry is insufficient.
2. **JC anchor:** for each response root, reduce the independent Faber-flux
   functional modulo `M`.  The barycentric identity shows that only its top
   remainder coefficient can survive.  Solve the two remaining
   `N=7,e=3` pole multisets and retain the monodromy block lattice of every
   decomposable carrier as a possible flux sidecar.
3. **SIC/GMC niche:** decide whether two pairs admit a balanced degree-two
   or degree-three SIC witness.  THM-2824 now proves arbitrary three-slot
   factorial detection, and THM-2828 extends one direction to the full lower
   prefix cone.  The live enlargement is THM-2830's two-positive-cone
   problem, reduced to a symmetric four-index factorial matching inequality;
   its universal sign, general SFC(3), and the Furter transfer remain open.
   THM-2801 proves degree one safe and exhibits degree four, so the SIC
   bidegree gap remains exact.
4. **KLS wildcard:** do not import a variance constant until the LRC carrier
   has a positive real measure and Dirichlet form.  At that point test the
   two-point word above as the positive control and a canonical endpoint
   profile as the hostile.

The meta-pattern is:

> When a scalar quotient annihilates an entire power tower, search first
> for the smallest marked descendant in the first forgotten coordinate.
> Then identify the multiplication and the physical sidecar before using
> the resulting transgression elsewhere.
