# Higher-order truth, observer congruences, and the open-frontier audit

**Session:** root/arxiv-2608-26874-session, 2026-08-29
**External status:** Ye--Xu is a **CITED VERY-RECENT PREPRINT v1, MANUALLY
AUDITED WITH TWO REPAIRABLE TYPOS**.
**Repo status:** the LRC experiment is **FINITE-EXACT / PROVED RELATIVE TO
THM-4282/THM-4283** in [THM-4286](../01-canon/theorems/THM-4286-signature-response-nonfactorization-and-two-deck-surgeries.md).
Every other transfer below is **OPEN**, **CONDITIONAL**, or a proposed test.

## Session verdict

The paper is most useful here as a discipline for deciding when a finite
observer supports a closure argument:

```text
finite observations are not enough;
the quotient must respect the successor and the target predicate.
```

That lens paid for itself immediately.  THM-4282's old 421-bit inactive
signature is not a congruence for any of four current certificate-response
predicates.  Refining it by labelled exposed bodies and appended-mask
activity produced two explicit 421-mask common decks on disjoint 36-row sets.
A concurrent strengthening of THM-4283 then subsumed all 72 rows.  Thus the
decks are independently audited alternate proof nodes, not a current ledger
decrement; the live residual remains 22,682 with top
`(100,637),(294,637),(520,637)`.

This is a real finite theorem, not a proof of LRC(14).  The paper itself does
not map `F_2` into an LRC object, and no topos obstruction proves a row safe.
Its contribution is the proof-design gate that led to the right experiment.

## The imported mechanism

Ye--Xu, [*Failure of Higher-Order Truth within Intuitionistic Propositional
Logic*, arXiv:2608.26874v1](https://arxiv.org/abs/2608.26874v1), claim that
`F_2` cannot be the global-truth Heyting algebra of an elementary topos.  The
mechanism is:

```text
recursive orbit in F_2
  -> infinite antichain in Bellissima's K_2
  -> orbit-union A exists in the profinite completion but not in F_2
  -> higher-order least-invariant Reach defines A as a truth value
  -> contradiction.
```

The decisive finite step is not merely that every finite layer can see only
finitely much.  Its equivalence relation is proved stable under the successor
`S`, and coprincipal predicates separate points outside the target union.
Without those two facts the argument would not force
`theta=exists u (Reach(u) and Z(u))` to equal the external orbit-observable
union `A=union_n Z(u_n)`.

The exact source audit, typo repairs, free-rank boundary, and the positive
etale-finite boundary of Abbadini--Almeida--Arrieta are recorded in
[the paper intake](../05-knowledge/reference/CORE-PAPERS-HEYTING-HIGHER-ORDER-TRUTH-2026-08-29.md).

## Inheritance pass

The closest proved repo mechanism is THM-4282's deletion-response hypergraph:
deleting masks exposes labelled bodies, and active appended masks solve an
exact set-cover problem.  Current THM-4283 combines a carrier band with a
complete 640-row top-signature fibre, leaving three rows at endpoint 637.

The canonical hostile is
[THM-2050](../01-canon/theorems/THM-2050-period14-top-germs-do-not-determine-global-loneliness.md):
identical period-14 germs can have global maxima `1/14` and `1/12`.  A local
observer needs a magnitude/first-exit sidecar.

The corrected near miss is MISTAKE-397: identical finite stages do not
determine an inverse limit when the transition arrows differ.  Ye--Xu avoid
that exact mistake by proving successor-congruence at every finite observer.

The least-used relevant sidecar is the embedded constructive address in
[HYP-2248](../05-knowledge/hypotheses/HYP-2248-borel-constructive-embedded-maximality.md),
still **OPEN**.  An orbit or diagonal escape becomes a usable proof object
only when the address that constructs the next state survives the quotient.

## Live concept board

| concept | object / representation | invariant or target | operation | information lost by the tempting quotient |
|---|---|---|---|---|
| orbit closure | `u_0,S(u_0),...` plus finite observers | least invariant `Reach` and observable `Z` | successor / saturation | iteration count and cross-depth arrows |
| LRC surgery state | actual deck, residual rows, exposed-body hypergraph | common activity and body coverage | delete / append / shrink | deck identity, margins, response validity at another pair |
| Rule 30 current | physical Cartier state and two right languages | infinite 2-kernel / center output | `T_0,T_1` | transverse odd current, boundary, quadratic admissibility |
| Mahler reset state | exact carry/reset/native fibre product | reset-Buechi plus native co-Buechi liveness | unary successor / reset | unbounded liveness rank and oriented interval geometry |
| compatible horizon | finite extractor trees under restriction | one coherent global section | restrict / extend | donor ancestry and deficit crossing the horizon |
| local lift tower | modular solution fibres plus height | bounded compatible integral lift | reduction modulo nested moduli | Archimedean height, positivity, role labels |

Every new object was compared against this board.  The common theme is not
"use category theory"; it is "retain the arrows and the coordinate on which
the target depends."

## Anchor: LRC(14)

### Exact result from this session

Source and target:

```text
source:  THM-4282's exact rows and frozen 421-mask deck E;
target:  body-covering decks common on residual rows;
map:     row -> old inactive signature -> exposed bodies ->
         appended activity/response word;
preserved after refinement: exact commonness for the fixed surgery menu;
destroyed by old signature alone: appended activity and response pattern;
sidecars: deck identity, pair, labelled body, strict margin, node type;
test: exhaustive body scan plus detached literal-wall margins.
```

The old signature has 17,604 fibres.  The four current response predicates
have `6,15,34,48` mixed fibres.  The most expressive hostile is signature
`{107}`, whose rows realize four joint response words.  Thus a closure search
on signatures alone would merge states on which its generators act
differently.

Two CEGAR probes refined exactly the missing coordinate.

1. The singleton signature `{396}` has 36 rows.  Deleting index 396 exposes
   eight bodies.  Exactly two of 495 body responders are active across the
   whole fibre.  The least gives a 421-mask common deck.
2. The signature `{107,374}` has five rows.  Deleting those indices exposes
   twenty bodies, and the exact fibre-wide response minimum is three.  After
   appending a minimum cover, old index 318 is the unique additionally
   removable retained mask.  The resulting 421-mask deck is common on 106
   signature-subset rows and proves 36 post-THM4282 residual rows.

Both branches have independent literal margins and global all-body scans.
Their 72 rows are disjoint, but all lie in current THM-4283's 691-row proof
union.  Their current incremental gains are therefore zero and the exact
residual stays 22,682.  This exact reconciliation was performed after the
concurrent theorem landed; physical entry is still absent.

### Best continuation: deck-state Nerode/CEGAR saturation -- OPEN

The paper suggests a controlled next object:

```text
state       = (actual body-covering deck D, exact residual U_D);
transition  = certified delete/append/shrink move;
observable  = exposed obligations + exact active-response pattern;
target      = rows certified common by D.
```

Hash-cons exact deck states and refine any observer class for which either the
transition or the common-row predicate is mixed.  Stop only at a stable
Nerode-style quotient or a minimal split witness.  This turns the current
sequence of local surgeries into a term-defined orbit with an honest
transition congruence.

The cheapest next hostile is the current endpoint-637 triple
`(100,637),(294,637),(520,637)`: compute their full response-aware deck states
and test every proposed transition on adjacent lower layers.  THM-4287 is
only a reserved empty stub, so it supplies no descent.  The crucial firewall
is that unioning row sets from several certificate nodes is not a single
reachable common deck.

Even a finite saturated state graph would remain a fixed-pool theorem.  An
unbounded LRC transfer still needs physical arrival or a semantic successor
retaining owner, phase, ancestry, and coefficient-two guard debt.  THM-2707's
support SCC and THM-3731's valuation quotient do not supply those coordinates.

## Niche: compatible-horizon AMM extractors -- OPEN

This is the cleanest compactness application.

```text
source:      exact fair extractor trees E_M(C,D) through horizon M;
target:      inverse system under horizon restriction;
map:         extractor -> restricted tree plus residual deficit polynomial;
preserved:   deadline and fairness only with the residual attached;
destroyed:   donor ancestry and deficit crossing the boundary;
sidecar:     full Bernstein residual and bounded routing window;
test:        exact SAT/ILP at M=8,16,32 and restriction-image stabilization.
```

THM-3340 supplies pointwise sections; THM-3342 blocks sublinear deadline
excess.  MISTAKE-361 is the hostile: an earlier finite argument illegally
routed mass arbitrarily far past the truncation edge.  If every restriction
image stabilizes and the exact tree is finitely branching, Koenig compactness
could produce a coherent extractor.  A first empty image would instead be a
sharp finite obstruction.  Pointwise nonemptiness alone is not enough.

## Wildcard: Rule 30 future-theory antichain -- OPEN

THM-4210 already supplies the rare structure the paper needs: a lossless
infinite-state physical tree with two exact successors.

```text
source:      physical dyadic current states Xi_(e,r);
target:      future center-output right language under T_0,T_1;
map:         state -> its theory over all successor words;
preserved:   physical successor and output observations;
destroyed:   odd transverse current, boundary data, D_t=N(B_t);
sidecar:     those three coordinates plus physical quadratic admissibility;
test:        minimize future theories on the 25,830 depth-16 profiles,
             then seek a symbolic recursive incomparable family.
```

`Reach` generalizes to closure under both children.  A symbolic infinite
family of pairwise distinct physical future theories would prove an infinite
2-kernel.  Bounded collisions or opposite *ambient* continuations do not:
the latter may violate the physical quadratic law.  This lane has the closest
shape to Ye--Xu's recursive antichain, but the physical sidecar is nonoptional.

## Mahler `3/2`: higher-order liveness rather than terminal flags -- OPEN

THM-4072's reset/native fibre product is structurally closer than a generic
finite automaton.

```text
source:      exact carry states with reset/native successor;
target:      infinitely many safe resets and eventual absence of digit 1;
map:         prefix -> reachable reset/native subtree;
preserved:   transition, reset recurrence, native acceptance;
destroyed:   unbounded liveness rank and interval orientation;
sidecar:     q_m, u_m/r_m, h_m, and oriented interval;
test:        Buechi/co-Buechi progress measures on the 2,290 depth-18
             skeletons, followed by a symbolic rank or recursive antichain.
```

The maintained `A=8`/`A=13` pair is the hostile: identical chosen terminal
flags make opposite next decisions.  The denominator-19 tower survives each
fixed horizon before failing later.  A finite-observer obstruction would
explain the nonregularity boundary; a genuine symbolic progress rank could be
positive structure toward the open problem.

## Sun 2468: local solvability with a height sidecar -- OPEN control

THM-4027 proves universal modular solubility while THM-4026 supplies an
integral hole.  This gives a theorem-shaped formulation that cleanly exposes
what the profinite limit forgets.

For nested moduli `M_r | M_(r+1)` tending to infinity, let `H_r(N)` be the
least `max(w,x,y,z)` among role-labelled admissible tuples representing `N`
modulo `M_r`.  Then the candidate equivalence is

```text
N is integrally represented  <=>  sup_r H_r(N) < infinity.              (24)
```

The forward direction is immediate.  For the reverse, a bounded finite set of
tuples contains one recurring along infinitely many moduli; its fixed integer
error is divisible by unbounded moduli and must vanish.  The remaining audit
is to align the exact admissibility/positivity convention and nested modulus
tower before canonization.

This is the completion/base mismatch in its simplest form.  The cheapest
experiment computes `H_r` for the known hole and represented neighbors:
represented controls should stabilize, while the hole's height must escape.

## Lower-priority transfers and one correction

### JC(2) gluing -- OPEN

THM-4067's exact defect `S_Gamma/A` and THM-4279/4280's faithful Hasse
observers suggest a site of **actual global Hom maps**, not an abstract
Heyting lattice.  Restriction to contact charts preserves response jets but
forgets global-Hom membership, integral base change, and effectiveness of
descent.  The cheapest test is the Cech equalizer over every `A_23` chart,
including nonincident parallel contacts, followed by essential-image equations
on the degree-34/42 shells.  The paper supplies no JC theorem; observer
faithfulness on the proved global domain actually blocks the most naive
completion-escape route.

### Matching logic -- CONDITIONAL

THM-4090's basic fragment is fixpoint-free and has no variables ranging over
predicates.  Ye--Xu's `forall Q subset U` therefore cannot be silently
translated.  Encoding finite `K_(2,d)` and asking whether `Reach` eliminates
into the maintained fragment is a useful expressivity boundary test, not a
new completeness counterexample.

### Tournament topoi -- historical claims corrected

The session found that HYP-1556/HYP-1557 had confused the integer OCF activity
`2` with `|Omega_Set|` and invoked Lawvere without a weakly point-surjective
map.  It also found a carrier switch between a Boolean duality groupoid and a
non-Boolean embedding category.  [MISTAKE-529](../01-canon/MISTAKES.md)
records the minimal witnesses and repair.  THM-002's OCF identity survives
unchanged: its `2` comes from the Grinberg--Stanley weight `2^psi`.

There is still a legitimate niche: subterminals of the embedding presheaf
topos are embedding-hereditary tournament predicates.  That language may
organize deletion-stable subclasses.  It gives no direct `H>=disc` or OS+
consequence until the target predicate and functors are explicitly stable.

## Prioritized next experiments

1. **LRC deck-state CEGAR:** start from the endpoint-637 triple, make each
   delete/append/shrink move an actual transition, refine every mixed response
   fibre, and retain deck identity/margins.  This is the highest-value
   continuation because this session produced two exact alternate nodes and
   a clean subsumption test.
2. **AMM restriction images:** build `E_M(C,D)` for three dyadic horizons and
   measure extension failure, not merely stage nonemptiness.
3. **Rule 30 physical future theories:** minimize the depth-16 physical
   profiles with every THM-4210 sidecar and search for a recursive family.
4. **Mahler liveness ranks:** run exact Buechi/co-Buechi progress measures on
   the reset skeleton and demand a symbolic survivor.
5. **Sun height escape:** formalize `(24)` with exact role/positivity
   conventions and use the known hole as the hostile control.

The stopping rule for all five is the same: a finite observation becomes a
proof mechanism only when the transition and the claimed target factor
through it.  Otherwise record the first mixed fibre and the missing
coordinate, as THM-4286 does, rather than calling the quotient a model of the
open problem.
