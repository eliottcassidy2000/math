# The last inverse-owner digit survives the owner-node Boolean square

**Status: FINITE-EXACT POSITIVE SIDECAR ON ONE `r=5` OWNER BASE;
INDEPENDENTLY AUDITED AT THE SCOPED SURVIVOR; LRC(14) remains OPEN.**
The disjoint audit
`lrc-r5-current-leg-inverse-owner-digit-independent-hostile-audit-codex-20260816.md`
reconstructs the object without importing this implementation and agrees on
the source, response, rank, spectrum, and fixed-slice digests.  Retaining the canonical
last digit `r_owner = a mod 13` of the THM-2471 current sheet produces a
genuine `V_4 x F_13 x F_13` interaction.  Its pure three-way component has
flattening ranks `(3,4,6)` and all `432=3*12*12` allowable triple Fourier
modes are nonzero.  A branch-flat lift with the identical Boolean-square
marginal has zero three-way interaction.

This is a finite ancestry statistic.  It is not THM-2334's exact relation
address, a temporal current, a chronology theorem, a row exclusion, or a
proof of LRC(14).

## Inheritance pass

The closest proved mechanism is THM-2471
`owner-first-collision-weighted-root-service-and-temporal-atom-boundary`.
Its finite Boolean stalk writes, at depth `d=13^5`,

```text
w_u=(y+u)/13,
X_(u,a)=(w_u+a)/d,       a in Z/dZ.
```

The closest audited realization is the four-state owner-node source square.
It retains the four physical cut states

```text
{0}, {0,6}, {12}, {6,12}
```

inside the six abstract nontrivial cuts of the spine `{0,6,12}`.  Its
response has rank four and centered rank three.  The corrected near miss is
MISTAKE-417: a delta-cell lift can have complete formal Fourier support while
remaining rank one.  Hence the decisive gate here is exact marginal recovery
plus a rank change against a branch-flat hostile, not Fourier support alone.

The least-used lawful sidecar is the last current-leg inverse digit

```text
r_owner = a mod 13.
```

It is distinct from the collision root `u`, the source-root difference
`u-q`, THM-2461's deepest-probe label `r_deep`, the source-time high digit
`b=floor(n/13)` in the left `P_169 e` packet, and THM-2334's grouped exact
address `C(a;X,m)`.  Equal cardinality does not identify any of these
coordinates.

## Exact radix construction

The program does not enumerate `13^5` sheets and does not label a
post-integrated table.  It factors the Perron fold as

```text
13^5 = 13 * 13^4.
```

It first folds the current packet by `13^4`, takes the thirteen
`r_owner` windows, and only then takes the thirteen collision-root windows.
For every common-base interval and every root `u`, the resulting nonnegative
profiles satisfy the pointwise identity

```text
sum_(r_owner in F_13) U_(u,r_owner)(y) = U_u(y).
```

Summing `r_owner` after endpoint coupling therefore recovers every entry of
the hash-pinned Boolean-square gamma bank and inverse table.  The check is
rowwise, before any rank or spectrum is inspected.

All thirteen current-leg branches survive.  At the fixed relation class
`(1,0,6)`, every one of the `4*13=52` state-branch entries is nonzero.  This
already separates the coordinate geometrically from the incoming
source-time high digit `b`: after its literal `Q` restriction, the `b=0` and
`b=12` rows vanish and only `44=4*11` entries survive.

## A stalk reflection, not a cosmetic tournament

The source packet is reflection invariant, and the sheet formula gives the
exact identity

```text
1-X_(u,a)(y) = X_(12-u,13^5-1-a)(1-y).
```

Consequently the retained stalk has the involution

```text
y -> 1-y,
u -> 12-u,
r_owner -> 12-r_owner,
state -> state XOR 2.
```

The last rule reverses the five-state physical Gray path: singleton `{0}`
goes to singleton `{12}`, and doubleton `{0,6}` goes to doubleton `{6,12}`.
It is not the cut-complement involution `state XOR 3`; reflection preserves
cut cardinality.  The script checks the profile identity on every joint
interval and obtains the palindromic branch-mass vector

```text
(44560361824598469720,
 40510923881424796800,
 40504424654832678540,
 40515633830289749040,
 40509264504422553840,
 40513136384094454080,
 40479588474296582520,
 40513136384094454080,
 40509264504422553840,
 40515633830289749040,
 40504424654832678540,
 40510923881424796800,
 44560361824598469720).
```

This involution is useful tournament discipline: it orients an actual path
reversal and pairs branch characters `k` with `-k`.  It does not supply a
pairwise orientation between arbitrary branch labels, so the thirteen
branches should not be called a tournament.

## Nonredundancy and hostile controls

Let `T(state,r,t)` be the exact inverse table for the endpoint relation bank
`(1,0,t)`.  Its three flattening ranks, in the order
`(state,r_owner,relation)`, are

```text
actual tensor:             (4,4,6)
branch-flat hostile:       (4,1,4)
```

After subtracting every zero-, one-, and two-coordinate marginal, the pure
three-way interactions have ranks

```text
actual tensor:             (3,4,6)
branch-flat hostile:       (0,0,0).
```

Thus the actual digit adds two relation-response directions beyond the
rank-four Boolean-square marginal.  The branch axis itself has rank four,
which is a real limitation: thirteen labels do not create thirteen response
directions.

The full actual Fourier tensor has all `4*13*13=676` coefficients nonzero.
The pure three-way tensor has exactly its `3*12*12=432` triple coefficients
nonzero and every lower-support coefficient zero.  The flat hostile retains
the same parent but has only `52` Fourier coefficients and no branch-bearing
or triple coefficient.  At `t=6`, the actual `4 x 13` slice has rank four and
its pure three-way slice has rank three; the flat slices have ranks one and
zero.

Same-root terms vanish exactly, three literal delete-before-transfer controls
pass, normal and optimized runs agree, and all source, table, rank, census,
and semantic digests are pinned.

## What the concurrent source-sheet result changes

The independently produced source-time digit `b=floor(n/13)` also raises the
relation rank from four to six and has a rank-six pure three-way interaction
with `432/432` triple modes.  The shared signature is striking, but it is not
an intertwiner:

| coordinate | stage | geometric support at `t=6` | proved relation |
|---|---|---:|---|
| `b=floor(n/13)` | left `P_169 e` source packet before transfer | `44/52` | source guard-sheet certificate |
| `r_owner=a mod 13` | THM-2471 current sheet after the depth-`13^5` fold | `52/52` | exact radix decomposition and base reflection |

The rank-six coincidence suggests a new hypothesis: the one-owner endpoint
response may have a six-dimensional reflection-compatible ceiling that
several distinct ancestry digits reach.  The cheapest decisive test is the
joint table

```text
(source-time b, current-leg r_owner, Boolean state, relation residue).
```

If conditioning on one digit removes the other's rank gain, both are
different presentations of one response sector.  If a new rank survives,
the two stages carry independent ancestry information.  Until that joint
table or an explicit stage map is built, neither conclusion is licensed.

## Tree, recurrence, and subset boundary

One digit is the first cylinder level of a `13`-ary inverse tree.  Retaining
two consecutive digits would turn the present branch set into `F_13^2` and
make a genuine transition/recurrence test possible.  This is structurally
parallel to address words in Stern--Brocot, Berggren, or ternary trees, but no
map preserving their recurrences has been constructed.

Likewise, the four cut states form a finite Boolean algebra fragment, while
an arbitrary subset of the natural numbers is an infinite indicator word
and hence a subseries of the harmonic series.  Passing from the word to the
scalar subseries sum destroys address information and is not injective in
general.  The LRC lesson is the same as here: retain cylinder/address digits
before taking a scalar marginal.  This is a research heuristic, not an LRC
implication.

## Connection contract

| field | exact answer |
|---|---|
| source | THM-2471 current-sheet coordinate `a` above the audited owner-node base |
| target | `V_4 x F_13(r_owner) x F_13(relation)` inverse tensor |
| map | split the `13^5` fold by `13^4`, retain `a mod 13`, then apply collision-root window, endpoint factors, integration, and relation inversion |
| preserved | common base, collision root, literal guard order, Boolean state, source weights, endpoint factors, branch reflection |
| destroyed | four older current digits, source-time high digit, absolute source-pair tail, root difference, deep label, exact relation-lattice address, chronology |
| positive gate | exact branch marginal plus rank jump `4 -> 6` and nonzero pure three-way rank `(3,4,6)` |
| hostile | branch-flat lift with the same parent and identically zero three-way interaction |
| boundary | finite candidate only; no current, row theorem, exclusion, or LRC(14) conclusion |

## Reproduction

```text
python -B 04-computation/lrc_r5_ufull_owner_node_boolean_square_inverse_owner_branch_probe_20260816.py
python -B -O 04-computation/lrc_r5_ufull_owner_node_boolean_square_inverse_owner_branch_probe_20260816.py
```

The semantic digest is
`f43e9fb0937ae93c54c2ac0aeb1257dfd8741f9cef32ef4bf2a5f3dd2cf06388`.
The LF script digest is
`ae1cf021ea23f325eeded42ff1dea8df903d837b1d7ab551289d62f7ab7a0348`.
