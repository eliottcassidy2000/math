# The endpoint bank is spectrally full precisely where it is cohomologically trivial

**Status: independent audit reflection for proved THM-3515.**  The proof
sources are the theorem and its exact companion; this reflection records the
research interpretation and the next lawful bridge test.

## Verdict

The incoming all-role result is sound in its strongest carefully typed form.
The five distinct refined role classes all occupy every one of the 52 active
owner-boundary addresses, and their response rows have rank five.  Every one
of the 72 lawful role charts has nonzero forced bridge, both weighted `K4`
tree factors, and nonzero product:

- pointwise on each chamber/drift address;
- after chamber marginalization at each of 13 drifts;
- at each of 13 drift-Fourier modes; and
- in every one of the 4 by 13 chamber-frequency fibres.

The independent reconstruction reproduced all four incoming bank digests.
It imported only the separately audited THM-3514 endpoint atom engine, then
wrote its own character contraction, chart generator, reduced-Laplacian
determinants, transforms, rank tests, and hostiles.

## What actually closes

The exact object is a family of weighted tree polynomials on the fixed
two-tetrahedra-plus-bridge graph.  At every endpoint address or transformed
mode, the thirteen edge weights are differences of eight role potentials.
The full graph determinant splits because every spanning tree must use the
unique bridge and independently choose a spanning tree in each tetrahedron.

This gives a useful three-layer closure:

```text
five role characters
      -> all 52 owner-boundary addresses, rank 5
      -> all 72 labelled graph charts
      -> bridge x left-tree x right-tree nonzero in every tested mode.
```

Nothing is being rescued only by a global sum: every physical address and
every Fourier fibre already carries the complete factorization.

## The productive paradox

The bank is maximally nondegenerate as a weighted-tree object and trivial as
an absolute graph-cohomology object.  If `p` is the vector of endpoint role
potentials, the edge response is exactly `delta p`.  Hence it lies in `B^1`,
pairs to zero with every graph cycle, and represents zero in absolute `H^1`.
The tree determinant sees a representative in the cut space; absolute
cohomology deliberately forgets that information.

This is not a defect in the computation.  It is the mathematical diagnosis:
spectral closure of weighted cuts is not flux.  A future D5 map cannot send
this endpoint object directly to a nonzero JC or LRC `H^1` class without an
additional support/holonomy sidecar.

## Carrier is not tournament

There are two appearances of four vertices:

1. the chamber-pair carrier `{L,R}^2`, an undirected `K4`; and
2. each private-support tetrahedron in the role graph.

Neither comes with an intrinsic pairwise comparison observable.  Orienting
edges by a storage order is a gauge for incidence signs, not a tournament.
The surviving structure is instead the cut-space/Walsh grammar: constant,
left, right, and mixed chamber channels over all thirteen drifts.

## The next decisive transplant

The owner-boundary factorization has now passed all endpoint-only hostiles.
Repeating more marginals is unlikely to cross the remaining type boundary.
The cheapest genuinely new object is a character-independent Boolean
relation

```text
R subset Omega_L x Omega_R
```

on the `26 x 26` active endpoint atoms, constructed on one physical ancestry
base before any character transform.  It must retain a common root gauge and
enough owner/word/source/horizon data to define the pairing.  Its first gate
is modest but decisive: its inverse transform must reconstruct the frozen
`H-q5` bridge.  Only after that passes should the two `K4` tree factors be
tested on the same support.

The regular-torsor isomorphism `u=a+c` removes a label-cardinality objection,
but it does not supply `R`.  One common `c` preserves relative drift; it still
does not identify guard atoms with physical collision roots.

## Exact audit record

The independent artifacts are

```text
04-computation/lrc_ufull_all_role_weighted_tree_spectral_closure_independent_audit_20260816.py
05-knowledge/results/lrc_ufull_all_role_weighted_tree_spectral_closure_independent_audit_20260816.out
01-canon/theorems/THM-3515-ufull-all-role-endpoint-weighted-tree-spectral-closure-in-b1.md
```

Their LF-normalized script/output hashes are respectively

```text
7b2a477845062bf2f40cc6570063edc9b4220ba1945ce71f4f1714a6ea471dba
3d7eabca4ab7e175eca9118a0e8dd0eabf3d867dcd8e188477b14c5c9934004e
```

and the independent semantic digest is

```text
61db05458acb26cd8d6a9a89991d3bbeed91f95f6bdf035720a3a93a5b4f886b.
```

The scope stays exact: endpoint `B^1`, no ancestry, no physical current, no
absolute `H^1`, no grouped coefficient, no scalar-row exclusion, and no
LRC(14).
