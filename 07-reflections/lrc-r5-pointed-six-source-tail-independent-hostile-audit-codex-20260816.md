# Independent hostile audit: the six pointed source tails add two real directions

**Status: FINITE-EXACT INDEPENDENT AUDIT ACCEPTS THE SCOPED SIX-POINTED-STATE
REFINEMENT.  LRC(14) remains OPEN.**  The submitted pointed-state
implementation was not imported.  A new event sweep starts from the
hash-pinned independent root-difference audit and retains the actual `U`-tail
root on every selected ordered source pair before integration.

## The physical six-state carrier

The audited owner square has one active `U` root in each singleton state and
two in each doubleton.  Marking that tail gives exactly

```text
(0,0) -> (1,0) -> (1,6) -> (3,6) -> (3,12) -> (2,12).
```

The six objects are pointed directed-cut states, not the six abstract
nontrivial cuts and not vertices of a `C_6` tournament.  The middle path edge
bridges the owner-inactive physical state `{6}`; it is not a direct Gray edge.

The independent construction bins each selected ordered pair by

```text
point=(state,u),
difference=u-q mod 13,
```

then applies the exact source weight or its support-only indicator, the
endpoint harmonic jump, response phase, and relation inversion.  Summing each
pointed fibre recovers every row and every inverse entry of the audited
four-state root-difference parent for both weighted and support-only banks.
The same-root slice remains pointwise zero and three literal guard controls
pass.

The logged point-segment census has a separate, now explicit convention.  It
counts each source-supported tail on every `Q`-active weighted segment before
endpoint guard selection:

```text
(41935,51187,51187,51187,51187,41935).
```

The tensor itself includes only endpoint-selected `(u,q)` pairs.  Conflating
those layers produces a different harmless census but would be a typing error
if used as a tensor multiplicity.

## Rank witness and equal-tail hostile

The raw and three-axis-centered flattening ranks are

```text
                  raw          centered
weighted       (6,12,13)      (5,12,12)
support-only   (6,12,13)      (5,12,12)
flat-tail      (4,12,13)      (3,12,12).
```

The flat-tail bank divides every four-state parent value equally over the one
or two allowed tails.  It has exactly the same parent marginal.  Therefore the
rank gain `4 -> 6`, and centered gain `3 -> 5`, cannot be a formal consequence
of splitting labels.  The two added directions are precisely the two
within-doubleton tail contrasts.

At fixed relation `(1,0,6)`, weighted and support-only matrices have rank `6`,
centered rank `5`, and both tail contrasts fire on all twelve nonzero root
differences.  Flat-tail has ranks `4/3` and kills both contrasts identically.
Numerical source magnitudes are again unnecessary: typed support incidence is
the load-bearing datum.

## Why path contrasts replace a six-point DFT

There is no intrinsic cyclic law on the six states.  The lawful nonconstant
observer is the five-edge incidence map of their geometric path.  After
centering the two genuine `F_13` axes, weighted and support-only edge supports
are

```text
(120,144,120,144,120).
```

The outer edges miss relation frequencies `+/-1`, the owner-gap bridge misses
`+/-6`, and the two marked-tail switches are full `144/144`.  Flat-tail kills
exactly those two switch spectra and leaves the three state-changing edges
full.  This separates support-state transport from marked-tail transport.

The palindromic path, counts, and support word are exact reflection data.  They
do not establish a Fibonacci, ternary, or substitution recurrence; that would
require the same typed edge operator at a second lawful scale.

Normal and optimized runs are byte-identical, and every submitted
gamma/tensor/ANOVA/path-spectrum digest agrees.  The independent semantic
digest is
`66db2301f88db1ced7784868095e198e3e12f1fb79175ebd902d0f569a5decef`.

## Connection contract

| field | exact answer |
|---|---|
| source | audited source-square/root-difference tensor and absolute `U` tail |
| target | six pointed states x source difference x endpoint relation |
| map | `(state,u,q) -> ((state,u),u-q)` before weighting and integration |
| preserved | common base, absolute source tail, directed cut, source support/weights, endpoint masks/guards, phases |
| destroyed | inverse-branch digit, deep root, horizons, THM-2334 gauge orbit, chronology |
| mandatory marginal | summing pointed fibres gives the audited four-state parent exactly |
| decisive hostile | equal-tail lift with the same parent marginal |
| survivor | maximal `(5,12,12)` centered ranks and two full tail-switch spectra |

This source tail is distinct from both incoming source-time inverse-branch
sheet data and the current-leg `r_owner=a mod 13`.  The three labels have
different maps and temporal copies.  A lawful next crossing must retain them
on one Boolean stalk, not identify them by cardinality.

No cyclic `C_6`, exact `C(a;X,m)`, source/arrival intertwiner, `U_clock`
chronology, physical current, uniform-row theorem, row exclusion, or LRC(14)
claim follows.

## Reproduction

```text
python -B 04-computation/lrc_r5_ufull_owner_node_pointed_six_state_root_difference_independent_audit_20260816.py
python -B -O 04-computation/lrc_r5_ufull_owner_node_pointed_six_state_root_difference_independent_audit_20260816.py
```
