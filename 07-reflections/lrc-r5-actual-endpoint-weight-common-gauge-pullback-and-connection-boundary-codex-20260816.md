# Actual U_full endpoint weight survives the common-gauge pullback

**Status: FINITE-EXACT ACTUAL-ENDPOINT-WEIGHT ADDRESS PULLBACK + SHARP
PROJECTIVE CONNECTION NO-GO; independent audit pending.**  The frozen U_full
`AX/BY` atom-pair contribution is reconstructed from the independently
audited THM-3514 engine and pulled back as a scalar function along the lawful
THM-2471 common-gauge address measure.  Every common-offset owner character
gives a nonzero auxiliary contraction.  Nevertheless, no owner character
admits a fixed four-channel mixer followed by one common drift convolution,
for either the full or common-support-restricted endpoint curve.  This is not
a one-integral endpoint transplant, physical current, grouped coefficient,
row exclusion, or LRC(14).

## 1. Two objects that must not be conflated

The preceding common-gauge probe constructs an exact ancestry measure

```text
M_(omega,nu,c),                                        (1)
```

where `omega=(a,C)` and `nu=(b,D)` are actual U_full guard atoms evaluated on
the linked THM-2471 nodes, and

```text
c=a-u=b-q                                              (2)
```

is retained before any owner-character sum.  This is one common-base Boolean
object.

Independently, THM-3514's endpoint engine gives atom tables

```text
AX_(alpha,beta)(omega),
BY_(alpha,beta)(nu)                                    (3)
```

and exact guard-pair kernels for `q_H=(1,0,1)` and `q_q5=(1,0,0)`.  Contracting
(3) with the `alpha,beta,tau` characters defines an actual endpoint
pair-contribution function

```text
E_(omega,nu) in F_p,                                   (4)
```

in the certified split field

```text
p=572252886246508880869.                               (5)
```

The sum of (4) is exactly the frozen bridge

```text
sum_(omega,nu) E_(omega,nu)
 =389266878372286537904.                               (6)
```

Equation (4) is a scalar function on the finite atom-pair address set.  It is
not yet an endpoint factor evaluated inside the outer integral defining (1).
That distinction is the remaining physical-current debt.

## 2. The same 48 types carry a nonzero target bridge

The common gauge has support

```text
{L,R}^2 x F_13^*.                                     (7)
```

On the target side, the four same-sheet buckets `d=0` contribute

```text
324498447313453607031.                                 (8)
```

Deleting them from (6) leaves

```text
B_48
 =64768431058832930873 !=0.                            (9)
```

Thus the exact 48 types realized by the ancestry gauge also carry a nonzero
piece of the actual U_full bridge.  The restricted four-by-thirteen target
table still has rank four and all four Walsh rows retain every one of their
thirteen drift-Fourier modes:

```text
support(B_48;W_0,W_L,W_R,W_X)=(13,13,13,13).           (10)
```

The missing four raw buckets are therefore not needed for endpoint spectral
nonvanishing, even though they are needed to reconstruct the full numerical
bridge (6).

## 3. Pulling endpoint weight through the ancestry address

For each owner character `k in F_13`, define

```text
P_k
 =sum_(omega,nu,c)
   M_(omega,nu,c) E_(omega,nu) zeta^(-kc).             (11)
```

The common denominator of `M` is inverted in the split field.  Formula (11)
is a lawful pullback of the finite scalar function (4) along the address map
of (1): the atom pair and common offset are retained before their sum.  It is
not a claim that the two original real-variable integrals have already been
identified.

The exact values are

```text
(P_0,...,P_12)=
( 89020872861375921416,
 236085603545857215793,
 323683959657851125455,
  54173676827327624142,
 249656651420909028337,
 103356827118739902928,
 384696996346514331447,
  29597935718057174234,
 387428575013009974450,
 249995533066198665075,
 318311674730929824069,
 138773773920322904500,
 532141509419833569170 ) mod p.                        (12)
```

All thirteen are nonzero.  A flat pair-function control gives the nonzero
common-gauge mass

```text
211784182424725384807 mod p,                            (13)
```

so (11) is not passing only because of a zero normalization.  The pair-level
endpoint function has digest

```text
c2d5911b287510335edc6aefa6d3b865c982568f678bb89ee9b82ee211962df1.
                                                               (14)
```

This is the first exact positive in this lane that simultaneously uses:

```text
one THM-2471 common ancestry base,
the actual U_full guard-atom address,
the common root/sheet torsor gauge,
the absolute owner phase,
and the actual q_H-q_q5 endpoint pair weight.           (15)
```

Its honest status is an auxiliary address-weighted contraction.

## 4. Why this positive is genuinely nonlinear

For each owner frequency, form the four common-gauge chamber rows and their
drift spectra `X_j`, and let `Y_j` be either the full endpoint spectrum or
the restricted spectrum from (10).  A post-marginal fixed channel mixer and
one common circulant would require

```text
M X_j=lambda_j Y_j,              j in F_13.             (16)
```

Keep all sixteen entries of `M` and all thirteen multipliers `lambda_j`.
For every one of the thirteen owner frequencies, both choices of `Y` give

```text
29 unknowns,
augmented rank 29,
nullity 0,
multiplier-projection rank 0.                           (17)
```

Thus not even the zero map plus arbitrary multipliers survives: (16) has
only the zero vector in the augmented unknown space.  This is stronger than
the earlier annihilator-only systems because the source chamber curve itself
has rank four at every owner frequency.

There is no contradiction between (11) and (17).  Formula (11) pairs a
two-address scalar function with a joint ancestry measure.  Formula (16)
tries to factor that interaction through one linear map on already
marginalized four-vectors.  The former retains the incidence that the latter
forgets.

## 5. The frontier has moved again

The sequence of obstructions is now:

```text
rank-three folded source
  -> excluded by THM-3524;

rank-four/six/eight/nine post-marginal enrichments
  -> excluded by the independent common-connection audit;

unmarginalized common-gauge address measure
  -> exists and has full nonzero-owner spectral capacity;

actual endpoint pair function pulled through that measure
  -> nonzero at every owner character;

factorization through one fixed linear connection
  -> excluded by (17).                                 (18)
```

The next missing theorem is therefore a temporal/Fubini transplant, not
another spectral-support or linear-algebra computation.  It must either:

1. lift the endpoint factors producing (3) onto the same linked-node fibre
   before their endpoint integration; or
2. prove that the finite address pullback (11) is the pushforward of a lawful
   relation current with the required chronology and grouped coefficient.

Without one of those statements, (11) cannot remove a scalar row.  With one,
the nonzero calculation is already present.

## 6. Connection contract

| field | value |
|---|---|
| source measure | `M_(omega,nu,c)` on one THM-2471 Boolean fibre |
| target function | actual endpoint pair contribution `E_(omega,nu)` |
| map | common guard-atom labels plus `c=a-u=b-q` |
| preserved | 48 common types, owner phase, actual endpoint weight, nonvanishing |
| lost by linear marginal | atom-pair incidence used in (11) |
| positive | all thirteen `P_k` are nonzero |
| hostile | all 26 projective systems in (17) have nullity zero |
| needed sidecar | temporal/Fubini current realizing the address pullback before integration |

## 7. Reproduction

```text
python -B 04-computation/lrc_r5_common_gauge_endpoint_weight_connection_probe_20260816.py
python -B -O 04-computation/lrc_r5_common_gauge_endpoint_weight_connection_probe_20260816.py
```

The semantic digest is

```text
6d4f3389f50c98a103c5c6cb2daedea0ed2dd86e267aaa5ab0f9a4585433693d.
```

The script and output SHA-256 hashes are

```text
3d25572a84c48ccc8c4550b7d4f66180fab97e028094982a2b5a58dba9e534fc
86427a2a296db87f74fe0abed508745bcdb02780752e5eb0670e604f1a6337a8.
```
