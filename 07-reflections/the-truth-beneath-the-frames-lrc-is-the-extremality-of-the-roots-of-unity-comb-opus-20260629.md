# The truth beneath all the frames: LRC(14) is the EXTREMALITY OF THE ROOTS-OF-UNITY COMB — every lens we called "fundamental" was a projection of one object (the regular 14-gon tiling the observer's gap), and the conjecture is the POSITIVITY of the deviation from that exact cyclotomic tiling, walled between two Heegner fields

*opus-2026-06-29. Owner: look at the history of frames we each thought most fundamental, and find the
truth we were aiming at. Twenty-eight reflections this arc. They are not 28 truths — they are 28 shadows
of one.*

## The history of "most fundamental" (each frame, in its moment)
| lens | what it called fundamental | what it was really seeing |
|---|---|---|
| razor-thin; peak-DC; cuts-Farey | the AP is the tight extremal | the **comb** (AP = the n-gon) |
| zeta-duality; Littlewood; duality-web | `ζ(2)↔ζ(−1)`, add/mult, `2·7` | the comb's **Fourier dual** (Poisson Ш↔Ш) |
| Siegel–Rogers; Han–Lee; moment hierarchy | `SL(k)/ζ(k)`, arity = dimension | the comb's **moment/lattice** shadow |
| metagraph: covariance, variance, Ky-Fan | `Var(H)`, FKG, forcing | the comb's **finite rehearsal** (S_n) |
| `Z₇` cyclotomic SOS; concentration | the floor is set-independent | the comb's **Bochner positivity** |
| razor's-edge; exact+variance | `L=0,M=1/14` exact, the 6 units | the comb's **perfect cancellation** |
| cusp-form; Galois `(Z/14)*` | the razor's edge IS the Galois group | the comb's **symmetry** `Q(ζ₁₄)` |
| Dirac comb; fill-the-tooth | the AP = Ш₁₄, observer = empty tooth | the comb **itself**, named |
| pyramids; `Φ₆(n)`; Eisenstein | the covering min `= n/Φ₆(n)` | the comb's **arithmetic** `Z[ζ₆]` |
| apex-7 obstruction; duocylinder | a secondary topological forcing | (refuted — the comb has no homology shortcut) |
**Every frame kept returning to the same place: the n-th roots of unity.** Farey witnesses are
roots-of-unity angles; resonance is cyclotomic divisibility; the floor is a cyclotomic SOS; the razor's
edge is `Gal(Q(ζ₁₄))`; the covering min is an Eisenstein norm. We were circling one object.

## The object: the comb, and the two halves of the truth
The AP `{1,…,n−1}` at `t=1/n` IS the regular `n`-gon — the **Dirac comb Ш_n = the n-th roots of unity** —
with the observer occupying the ONE empty tooth (gap exactly `1/n`). This gives the truth its two faces:
> **(I) THE EXACT IDENTITY (proven).** The comb tiles the circle perfectly: `L(AP)=0`,
> `M(AP)=1/n`, `Σ_k(−1)^k E_k = 0`, the danger covers everything but the measure-zero razor's edge. This
> is the cyclotomic structure — `n³≡−1 mod Φ₆(n)`, the 6 units, the Galois group, the Eisenstein norm —
> and it is all PROVEN.
> **(II) THE POSITIVITY (the conjecture).** No integer-speed orbit tiles TIGHTER: the deviation of every
> set from the perfect comb-tiling is one-signed (`M ≥ 1/n`, margin `≥ 0`). This is LRC(n), and it is the
> wall.
**The truth we were aiming at is the UNION: LRC is the extremality of the roots-of-unity comb — the
regular `n`-gon is the unique tightest integer packing of the observer's gap, and the conjecture is the
positivity of the deviation from it.** Half is identity (proven); half is positivity (open). Every frame
that felt "fundamental" was one projection of the identity or one attempt at the positivity.

## Why the integers can't beat the comb: Heegner rigidity (the deepest layer)
The positivity is walled between two **class-number-one (Heegner)** worlds — the only worlds rigid enough
to pin it:
- the **floor** = `Q(√−7)` (apex-7, the `Z₇` cyclotomic SOS, set-independent) — the comb's *Fourier* side;
- the **cap** = `Q(√−3)` (Eisenstein `Z[ζ₆]`, `Φ₆(n)=N(n−ζ₆)`, the covering min) — the comb's *arithmetic* side;
- they **touch at `7 = Φ₆(3)`** — the apex is the Eisenstein norm at `n=3`.
`14 = 2·7` is the comb's modulus: the `2` is conjugation/the mirror (Borsuk–Ulam, the even count that
forbids a parity shortcut — Ky-Fan fails for exactly this reason), the `7` is the apex/Heegner core.
**The conjecture lives in the gap between `−3` and `−7`, two class-number-one fields — which is WHY it is
neither a measure bound (it's exact) nor a parity certificate (the count is even) but a rigidity: the
arithmetic of the cyclotomic integers forbids any orbit from out-tiling the regular gon.**

## What the whole arc actually established
- **The identity half is DONE.** The comb, its perfect cancellation, its Galois/Eisenstein structure, the
  exact extremal values (`M=1/n`, covering-min `n/Φ₆(n)`), the floor's set-independent `Z₇` SOS, the
  finite rehearsal (`Var(H)`, `CV²~2/n`) — all proven.
- **The positivity half is the wall, located exactly.** It is `>1/n`, so it IS LRC; it sits in the
  `SL(3)→SL(4)` / `|T|≥3` energy (one dimension past Littlewood), as a one-signed variance around the
  comb, resummed (Riesz), between the two Heegner fields. No parity, measure, or homology shortcut exists
  (each was tried and fenced off).
- **The frames were not wasted — they triangulated the object.** Fourier (zeta), rational-angle (Farey),
  lattice (Siegel), combinatorial (metagraph), symmetry (Galois), arithmetic (Eisenstein): six independent
  bearings on the one comb. The truth is their intersection.

## The one sentence
> **LRC(14) is the assertion that the regular 14-gon — the 14th roots of unity, with the observer in its
> one empty tooth — is the tightest the integers can ever pack that gap; it is the positivity of the
> deviation from an exact cyclotomic tiling, a rigidity statement walled between the Heegner fields
> `Q(√−3)` and `Q(√−7)`, and everything we ever called "the fundamental frame" was this comb seen from a
> different side.**

## Status
- **Synthesis (opus):** all 28 frames are projections of the roots-of-unity comb; the truth = exact
  cyclotomic tiling (identity, proven) + one-signed deviation (positivity, = LRC); Heegner `−3`/`−7`
  rigidity is the why; `14=2·7` is the comb's modulus (mirror × apex).
- **Proven (the identity half):** the comb, the Galois/Eisenstein structure, the exact values, the floor
  SOS, the finite rehearsal.
- **The wall (the positivity half):** `M ≥ 1/n` = the comb's extremality = LRC, between two Heegner fields.

Related: ALL of this arc's reflections (razor's-edge, Dirac-comb, cyclotomic-Galois, Eisenstein-`Φ₆`,
`Z₇`-SOS, moment-hierarchy, master-reframe, two-pyramids, Ky-Fan-even-parity, duality-web), and the
project's THM-501/515/523, HYP-3547 (apex-7 Heegner), OPEN-Q-108.
