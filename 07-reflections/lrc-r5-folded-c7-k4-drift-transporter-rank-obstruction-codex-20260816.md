# The septimal fold is a `K4`, but three deep windows cannot carry four Walsh rows

**Status: PROVED as THM-3524 + FINITE-EXACT + INDEPENDENTLY AUDITED.**  The
exact companion is
`04-computation/lrc_r5_folded_c7_to_ufull_k4_drift_transporter_probe_20260816.py`.
It compares proved THM-2594 with the THM-3514 endpoint bank, whose all-role
weighted-tree strengthening has now been independently promoted as THM-3515.
The disjoint audit is recorded in
`lrc-r5-folded-c7-k4-transporter-independent-audit-codex-20260816.md`; normal
and optimized replays of both implementations match their stored outputs.
The comparison is over one certified split field.  It constructs no U_full
ancestry relation, physical current, nonzero absolute `H^1`, scalar-row
exclusion, or LRC(14) conclusion.

## 1. Verdict and inheritance

The closest positive mechanism is THM-2594: its `r=5` response is formed on
one finite linked-node ancestry base before marginalization, and all `5184`
primitive mixed cut coefficients fire.  The closest endpoint mechanism is
THM-3514/3515: the U_full bridge has complete `K4 x F_13` Walsh/Fourier
support, and all role-chart tree factors fire in every tested address and
drift mode.  The canonical hostile is the common-ancestry marginal kernel;
the corrected near miss is that full Fourier support does not supply a
common-base lift.  The least-used sidecar is the inversion fold of the six
nontrivial septimal characters.

The new verdict has a positive half and a negative half.

1. THM-2594's **raw** `7 x 13` table has all `91` Fourier coordinates
   nonzero in the certified split field.  Every one of its seven septimal
   profiles is therefore a cyclic vector for `F_P[C_13]`.
2. The four U_full Walsh drift rows are also cyclic vectors, so each target
   row is carried from a chosen source row by a unique invertible circulant.
3. Those four circulants are pairwise inequivalent even after independent
   nonzero amplitude and cyclic-gauge changes.  No allocation of source
   modes produces one shared scalar kernel.
4. More fundamentally, the whole THM-2594 raw profile bank has row rank
   three because only `theta=0,1,2` are nonempty, while the U_full Walsh bank
   has row rank four.  No fixed channel map followed by one common drift
   convolution can increase rank from three to four.

Thus there is a formal channel-dependent spectral transplant, but the
character-independent linear transplant fails for an exact structural
reason: it owes one independent deep channel.

## 2. The Paley heptagon really folds to a four-vertex bidirected carrier

Let `T_7` be the Paley tournament on `F_7`, oriented by the quadratic
residues `{1,2,4}`.  Fold vertices under negation:

```text
O_0={0},       O_1={1,6},       O_2={2,5},       O_3={3,4}.       (1)
```

The exact arc-count matrix between orbit blocks is

```text
      O0 O1 O2 O3
O0     0  1  1  1
O1     1  1  2  2
O2     1  2  1  2
O3     1  2  2  1.                                      (2)
```

The diagonal entries in the last three rows record the one internal arc
collapsed inside each two-point orbit.  Every off-diagonal entry is symmetric.
This is forced because `-1` is a quadratic nonresidue: simultaneous negation
reverses every Paley arc.  After folding, each distinct orbit pair is therefore
both-way, not singly oriented.  The quotient support is an undirected or
bidirected `K4`, not a tournament on four vertices.

Multiplication by two cycles the three nonzero orbit classes:

```text
O_1 -> O_2 -> O_3 -> O_1.                              (3)
```

This is the familiar

```text
F_7^*/{+/-1} ~= C_3                                    (4)
```

ternary/Galois carrier.  On the endpoint side, the three nontrivial Walsh
characters of `F_2^2` cut the four chamber states into the three perfect
matchings

```text
01|23,             02|13,             03|12.           (5)
```

Equations `(3)--(5)` explain the recurring “four vertices / six edges /
three matchings” grammar.  They do not choose a map: preserving the trivial
channel leaves `3!=6` folded identifications, and choosing one member of each
antipodal source pair raises this to

```text
3! 2^3=48                                               (6)
```

marked lifts.

## 3. The rational algebras retain different descent data

The four folded labels do not make the two rational convolution algebras
equal.  The inversion-fixed septimal algebra is

```text
Q[C_7]^(b -> -b)
  ~= Q direct_sum Q(zeta_7+zeta_7^(-1)).                (7)
```

The cubic factor is generated by a root of

```text
x^3+x^2-2x-1,
disc=49.                                                (8)
```

It is the cyclic real cubic.  In contrast,

```text
Q[F_2^2] ~= Q^4.                                       (9)
```

The algebra in `(7)` has two rational simple factors, while `(9)` has four;
there is no `Q`-algebra isomorphism.  After extending to a field splitting
the cyclic cubic, both become four copies of that field and a marked
identification becomes possible.  That base extension is load-bearing.

This also blocks a tempting Jacobian identification.  THM-2546's three
coordinate cubics for the fixed sporadic map have

```text
disc=-4 (square)^2 L,                                  (10)
```

with the variable branch class carrying generic `S_3` monodromy.  The cyclic
cubic `(8)` has square discriminant and `C_3` Galois group.  Both split into
three labelled roots over a large field, but matching those roots discards
exactly the discriminant/monodromy descent datum.  The common `disc=-4`
coordinate anatomy and the folded septimal `C_3` are therefore different
cubic carriers, not an unmarked D5 map.

## 4. Why the raw `r=5` table has all 91 modes

Write THM-2594's raw table as

```text
A(ell,theta),          ell in F_7, theta in F_13.       (11)
```

Only the columns `theta=0,1,2` can be nonzero.  For `b in F_7`, define the
septimal profile

```text
x_b(theta)=sum_ell A(ell,theta) zeta_7^(-b ell).        (12)
```

The exact probe finds

```text
xhat_b(a) != 0       for all (b,a) in F_7 x F_13.      (13)
```

There is also a conceptual proof of why no omitted boundary frequency is
accidental.

- For `b!=0` and `a!=0`, `(13)` is THM-2594's primitive mixed
  nonvanishing, since raw and doubly centred arrays have the same mixed
  Fourier coordinates.
- For `b!=0,a=0`, one Fourier-transforms the seven rational row sums.  They
  are not all equal.  A rational polynomial of degree at most six that
  vanishes at one primitive seventh root is a scalar multiple of `Phi_7`,
  so all seven coefficients would have to be equal.  They are not.
- For `b=0,a!=0`, the theta profile is a nonzero rational polynomial
  supported in degrees zero through two.  It cannot vanish at a primitive
  thirteenth root, whose minimal polynomial has degree twelve.
- The `(0,0)` coordinate is the positive total mass.

Nonzero reduction at the certified prime independently certifies all `91`
coordinates.  Hence the sparse three-column table is a unit in the split
group algebra even though its row rank is only three.  This distinction—full
entrywise spectral support versus cross-channel rank—is the central hinge.

Double centring removes exactly

```text
13 septimal-trivial modes + 6 remaining root-trivial modes = 19,   (14)
```

leaving the `6*12=72` primitive interaction coordinates.  For each
`b!=0`, the centred raw profile equals the THM-2594 interaction profile; its
zero-frequency coordinate is the one deleted row margin.

## 5. The U_full endpoint target has four independent cyclic rows

Let `D_ij(d)` be the THM-3514 bridge bucket at chamber pair `(i,j)` and drift
`d`.  Its Walsh rows are

```text
W_0=D_00+D_01+D_10+D_11,
W_L=D_00+D_01-D_10-D_11,
W_R=D_00-D_01+D_10-D_11,
W_X=D_00-D_01-D_10+D_11.                               (15)
```

THM-3514 proves that every one of the `4*13=52` Walsh/Fourier coordinates is
nonzero and that the four physical drift rows have rank four.  THM-3515
independently strengthens the same endpoint bank to all five role classes
and every weighted-tree factor.  The present probe independently reconstructs
the bridge rows from the pinned atom engine and reproduces both properties.

## 6. The common-kernel no-go is a rank theorem

Suppose a linear folded branch transplant were built from

1. any fixed channel map `M` from all seven THM-2594 septimal profiles to
   the four Walsh channels; and
2. one scalar circulant drift operator `C`, applied identically in every
   channel.

Writing the source and target banks as matrices gives

```text
Y=M X C.                                                (16)
```

But the three nonempty theta columns give

```text
rank(X)=3,                 rank(Y)=4.                   (17)
```

Therefore

```text
rank(MXC)<=rank(X)=3<4=rank(Y),                         (18)
```

whether or not `C` is invertible.  No such transplant exists.

The exact finite referee makes the boundary hostile in several redundant
ways.

- None of the `48` trivial-channel-preserving marked allocations gives one
  common scalar kernel.
- None of all `192` folded allocations does.
- Even all `7^4=2401` unrestricted choices of four septimal modes require
  four inequivalent kernels.
- Independent amplitude and cyclic root-gauge changes do not merge any pair
  of the four kernels.
- For each of the eight antipodal sign lifts, the `78 x 16` projective
  channel-mixing system has rank twelve.  Its four-dimensional nullspace is
  exactly the space of matrices annihilating the rank-three source; it
  contains no admissible transport.

This is the branch-transplant obstruction in its cheapest form.  The fourth
Walsh direction cannot be manufactured by relabelling three deep windows.

## 7. The formal survivor and the missing fourth sidecar

For a single source cyclic vector `x` and target cyclic vector `y`, there is
always one invertible circulant with Fourier multiplier

```text
muhat(a)=yhat(a)/xhat(a).                               (19)
```

Using the calibration

```text
b=0,1,2,3  ->  W_0,W_L,W_R,W_X,                       (20)
```

the probe constructs four such operators.  Every kernel has support `13/13`;
the four kernel rows have rank four.  Their exact digests are

```text
full:
9f03166d1b8d730673aeccf7bff9196d2de42e0ff9fa7c08680d38a772dc041f,

augmentation:
351976b6d1ac51915d686b32e1cc0f3f3dfa53d1f33f7c8831f7a74a495e3025. (21)
```

For each channel, full transport minus augmentation transport is a constant
kernel, so the trivial root mode is an explicit margin sidecar.  For
`b=1,2,3`, the augmentation source is the actual THM-2594 interaction.
For `b=0`, double centring deletes the whole septimal-trivial source profile.

Equation `(19)` is a formal split-field equivalence, not a discovered
physical map: any pair of cyclic vectors admits it.  The fact that four
inequivalent multipliers are required is the useful content.  A shared linear
drift transplant needs a fourth independent source response—such as a new
deep window or another typed sidecar.  A future lawful ancestry relation
could instead be nonlinear, nonconvolutional, channel-dependent, or
frequency-dependent; `(18)` does not exclude those broader constructions.

The ternary/Boolean slogan can now be stated exactly.  The three active deep
labels are a ternary alphabet, and the antipodal sign restores the six labels
`C_3 x C_2 ~= F_7^*` at the set level.  But a Boolean sign does not add a
fourth independent column to a rank-three response table.  The U_full `K4`
needs that fourth linear coordinate, not merely six renamed symbols.

## 8. Connection and loss ledger

| field | exact content |
|---|---|
| source | THM-2594 raw common-base `7 x 13` response |
| target | THM-3514 U_full bridge on `F_2^2 x F_13` |
| folded labels | `{0},{+/-1},{+/-2},{+/-3}` versus four Walsh characters |
| positive map | four marked channel-dependent invertible `C_13` circulants |
| failed stronger map | any fixed channel map followed by one common scalar circulant |
| preserved by the positive map | split-field character labels and zero/nonzero spectrum |
| destroyed | rational descent, positivity, atoms, common ancestry, owner/word/source/horizon data, chronology, physical current |
| exact obstruction | source rank three from three deep windows; target Walsh rank four |
| required linear repair | a fourth independent typed source channel |
| broader live repair | a nonconvolutional or nonlinear common-base relation before endpoint marginalization |

This adds a precise gate to the ancestry API.  It does not replace the
required relation on the `26^2` U_full endpoint-atom pairs, and it does not
turn the endpoint `B^1` bank into absolute graph flux.

## 9. Reproduction

Run

```text
python -B 04-computation/lrc_r5_folded_c7_to_ufull_k4_drift_transporter_probe_20260816.py
python -B -O 04-computation/lrc_r5_folded_c7_to_ufull_k4_drift_transporter_probe_20260816.py
```

The pinned semantic digest is

```text
bae3749e01361ebbaf6c9cc0e7160d2e4d22d6df420d4512ba94f778718af3a1.
```

The exact result is promoted as THM-3524, a split-field transporter
classification and rank obstruction.  No Boolean coupling, physical current, grouped coefficient,
all-unit projector, nonzero absolute `H^1`, D5 word-current/JC-flux map,
scalar-row exclusion, or LRC(14) follows.
