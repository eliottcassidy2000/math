# The transverse packet cycle has an ordinary `D^2` shadow, but only after a relative Segal completion

**Status: VERIFIED-EXACT packet-support theorem candidate and stopping
certificate.**  This note connects the transverse `C_221` design target to
THM-2697/2698 and the semantic no-go beside THM-2701.  It proves an exact
ordinary-lattice macro cycle and the minimal midpoint completion needed to
subdivide it.  It also proves that this completion is not an inherited
THM-2305 endpoint/current cospan.  No scalar row, row exclusion, or LRC(14)
conclusion follows.

## 1. Inheritance and the object being tested

Put

```text
p=13,                 R=13^6=4826809,          L=17R=82055753.
```

The transverse scout found the two strict packet points

```text
x0=39123022/L,                    x1=41305508/L.          (1)
```

Their delayed phases and intrinsic clock edges are

```text
pi(x0)=4/17,          clock(x0)=(1,4),
pi(x1)=13/17,         clock(x1)=(4,1),                    (2)
```

where `pi(x)={Rx}`.  The two displayed `C_221` arrows exchange them by
transverse phases `12/17,5/17`.  The question here is different: after
forgetting that transverse action, does an exact arrow remain in the ordinary
`C_R` affine category generated by

```text
D(x)={13x},                    A_k(x)={x+k/R}?            (3)
```

The closest proved positive mechanism is THM-2698's parity-cancelled `D^2`
endpoint loop.  Its canonical hostile is also the warning: an endpoint macro
need not have a lawful packet midpoint.  The least-used sidecar is the
reciprocal clock labeling in (2), which determines the missing midpoint more
sharply than delayed support alone.

## 2. Exact nonconstant ordinary-lattice macro cycle

Since `169=-1 mod17`, ordinary `B0^2`, with `B0(y)={13y}`, exchanges the two
delayed phases.  At the lifted level define

```text
K0=4472399,                    K1=1954775.                 (4)
```

Exact integer arithmetic gives

```text
169*39123022+17*K0 = 41305508+81L,
169*41305508+17*K1 = 39123022+85L.                        (5)
```

Hence

```text
A_(K0)D^2(x0)=x1,              A_(K1)D^2(x1)=x0.          (6)
```

The root increments `2K_i mod13` are `(5,8)`.  Equation (6) is a genuine
nonconstant endpoint cycle in the ordinary translation lattice.  It is not
the composite of the two transverse arrows: it is the unique ordinary
interpolant modulo `R` between the already selected endpoints.  Thus (6) is
structural evidence, but endpoint interpolation itself supplies no semantic
operation.

The strict endpoint packet radius `eta` from the transverse scout consequently
gives positive endpoint-sampled macro cylinders at every finite horizon, of
initial radius `eta/169^(H-1)`.  No midpoint factor is included in that
statement.

## 3. The forced four-phase orbit and its `Z/2` grading

The elementary ordinary base orbit is

```text
4/17 -> 1/17 -> 13/17 -> 16/17 -> 4/17.                  (7)
```

It has a canonical grading on this orbit:

```text
grade 0: endpoint phases {4/17,13/17},
grade 1: ghost phases    {1/17,16/17}.                    (8)
```

`B0` toggles the grading; `B0^2` is multiplication by `-1` on the displayed
`C_17` orbit and exchanges the two points within each grade.  This is the
ordinary-lattice shadow of the reflection-symmetric `C_221` stalk
`55 <-> -55`.

The midpoint statement is universal.  Every factorization of a macro arrow
has the form

```text
(A_b D) after (A_a D)=A_(b+13a)D^2,                     (9)
```

with `a in C_R` arbitrary and `b=K_i-13a`.  Integral translations are in the
kernel of `pi`, so the midpoint phase is `B0(pi(x_i))`, independently of
`a`.  The exact companion exhausts all `R=4826809` factorizations of each
macro and verifies this directly.  Thus no ordinary factorization has a
midpoint in the inherited delayed word.

Zero added phases cannot suffice.  One phase cannot serve both directions,
because `1/17 !=16/17`.  Adding the two phases in (8) does suffice at the
coarse base level.  This is the precise sense in which the ghost-midpoint
completion is minimal; it is not a uniqueness assertion about full physical
lifts.

## 4. Clock gluing forces fixed self-lines

The clock labels add a second, independent minimality statement.  If an event
with label `(a,b)` is followed by `(c,d)`, literal chronology requires `b=c`.
To subdivide the reciprocal transition

```text
(1,4) -> (4,1)
```

through one middle event, both gluings force that event to be `(4,4)`.
The reverse transition similarly forces `(1,1)`.  Hence the completed clock
cycle is uniquely

```text
(1,4) -> (4,4) -> (4,1) -> (1,1) -> (1,4).              (10)
```

The grade-zero objects are the reciprocal nonconstant edges, while the
grade-one ghosts are fixed by clock reversal.  The “fixed grading” is
therefore literal: a two-step subdivision of a reciprocal edge cycle must
pass through self-lines.  These diagonal labels are outside the nonconstant
clock class used to select the transverse packet cycle.

They nevertheless have honest partial physical lifts.  Among all
factorizations, the exact counts are

| macro | clock-diagonal lifts | present | rail | present and rail | also current and `D` roots |
|---|---:|---:|---:|---:|---:|
| `x0 -> x1` | 106080 | 27249 | 8160 | 2925 | 2925 |
| `x1 -> x0` | 106080 | 15988 | 8160 | 2479 | 2479 |

The first maximally local witnesses selected by the scan are

```text
x0 --A_1485215 D--> 41513423/L --A_4471840 D--> x1,
x1 --A_4459459 D--> 38392136/L --A_1903516 D--> x0.     (11)
```

Their midpoint clocks are `(4,4),(1,1)`, each has eleven active rails, the
matching present factor, and nonempty current and post-`D` root memberships.
Thus the ghosts are not rejected by a blanket absence of local geometry.

## 5. The exact missing factors

Every possible midpoint in the first direction has delayed phase `1/17`; in
the second it has `16/17`.  Both have semantic bit pattern

```text
guard safe?          false,
five unit safeties:  true,true,true,true,true,
c1 safe:             true,
target a dangerous:  false,
target b dangerous:  false.                              (12)
```

For the two lifted witnesses in (11), the inherited delayed-sector list is
empty.  Since delayed membership depends only on the forced phase, no other
ordinary factorization can repair it.  The two ghosts may carry rail,
present, and roots, but they are not objects of the inherited full packet
category.

The endpoints fail at the complementary semantic location.  At `4/17` and
`13/17` the guard and five units are safe, target `a` is dangerous, and target
`b` is safe, but `c1` is also dangerous at distance `1/17<1/14`.  Hence both
fail literal `Q_a` only at the unshifted source-`c1` complement.  More
decisively, neither physical endpoint in (1) belongs to any exclusive source
`E_j`.  Six ordinary dilations reach the deepest fork `Q_(3,{1,2})`, but not
along its required `E_3` source leg.

Consequently the completed diagram is

```text
off-diagonal packet endpoint with c1 debt
  -> diagonal ghost with delayed/guard/target debt
  -> off-diagonal packet endpoint with c1 debt.           (13)
```

It is a relative Segal factorization completion of a packet-support macro.
There is no functor back to the inherited packet category preserving the
delayed object, and no THM-2305 source-to-word cospan on the endpoints.
THM-2644 cannot consume endpoint equality in place of one nonnegative
transition and a lawful common middle fibre.

This does **not** prove that every conceivable direct two-step current is
impossible.  It proves that none is supplied by the inherited endpoint words,
the ordinary affine factorization, or the formal ghost completion.  A new
direct macro observable would have to explain why marginalizing (13) absorbs
the alternating `c1` versus guard/target debt while retaining common ancestry
and a target-active current.

## 6. Revised high-leverage target

The next useful object is no longer another recurrent endpoint pair.  It is
one of:

1. a common-ancestry mapping-cone current whose boundary is exactly the debt
   alternation in (13), with a proved map to a terminal target current; or
2. a re-rooting at an actual exclusive source, followed by reconstruction of
   the rail/present/root/unit packet at its prescribed clock.

The four-phase grading supplies the smallest test fixture for either route.
Any proposed macro cospan should first be evaluated on the two diagonal ghost
fibres in (11).  If it merely forgets them, it must prove a lawful
marginalization identity; calling (6) a composite is not enough.

## Reproduction

```bash
python3 04-computation/lrc14_relative_segal_macro_ghost_completion_probe_20260728.py
python3 -O 04-computation/lrc14_relative_segal_macro_ghost_completion_probe_20260728.py
```

Both executions byte-match

```text
05-knowledge/results/lrc14_relative_segal_macro_ghost_completion_probe_20260728.out.
```

The universe is all `2R=9653618` ordinary two-`D` factorizations of the two
macro arrows.  Positive controls are the two endpoint maps and the thousands
of ghost lifts retaining rail/present/root data; hostile controls are the
forced delayed phases, diagonal clock labels, endpoint semantic bits, and
missing exclusive-source legs.
