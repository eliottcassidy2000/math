# An inverse-branch source sheet adds two response directions to the Boolean square

**Status: FINITE-EXACT NONREDUNDANT SOURCE-ANCESTRY SIDECAR ON ONE `r=5`
HOST; LRC(14) remains OPEN.**  Retaining the high base-13 digit of the inverse
branch in the left `P_169` source packet refines the audited owner-visible
Boolean square.  The resulting `F_13 x V_4 x F_13` array has rank six versus
rank four after the branch marginal, and its pure three-way interaction has
rank six with all `432` possible triple modes nonzero.  This is finite
source-time ancestry, not an exact THM-2334 address, an arrival atom, a
temporal intertwiner, a physical current, a row exclusion, or LRC(14).

## Inheritance pass

The closest audited mechanism is the corrected common-owner Boolean square:
the unrestricted source support follows

```text
{0} -> {0,6} -> {6} -> {6,12} -> {12},
```

with measures `(1,12,2,12,1)/28`, while `OWNER` removes only `{6}` and leaves
four states.  MISTAKE-417 is the canonical hostile: complete formal Fourier
support can arise from a rank-one delta lift.  The least-used relevant sidecar
is an inverse-branch digit retained before source transfer.

The now independently audited root-difference refinement retains a different
coordinate, `s=u-q`: it is a colour of an ordered source-cut arc.  The
coordinate used here records which inverse source sheet supplied that arc.
They are distinct by construction, although the present probe marginalizes
`s` and therefore does not prove independence between the two refinements.

The concurrently arrived pointed-six candidate retains the absolute tail `u`
over the root-difference tensor and independently gains two state directions.
It names inverse ancestry as the next serious sidecar.  The present probe is a
disjoint realization of that next step after both `u` and `u-q` have been
marginalized.  Its rank six and the pointed tensor's rank six have different
flattenings and must not be identified by their common number.

## The retained coordinate

Write the inverse branch of the left `P_169 e` packet as

```text
n = 13b + r,     0 <= n < 169,
b = floor(n/13) in F_13.
```

The program splits `P_169 e` by `b` first, then applies the source `Q`
restriction and the `P_(13^5)` transfer.  It does not infer `b` from a
post-integrated table.  On all `91*13^2=15379` temporal source cells, the
direct arithmetic certificate identifies the high digit with the source
guard sheet.  Every sheet has exactly `91*13=1183` cells.

This identification is deliberately narrow.  The digit `b` is not thereby
the canonical current-leg owner branch `r_owner=a mod 13`, the deep label
`r_deep`, or the grouped exact address `C(a;X,m)`.  Equal cardinality and a
shared radix do not supply an intertwiner.

## Geometry before character inversion

After the literal `Q` restriction, branch profiles zero and twelve vanish and
all eleven interior branches survive:

```text
b support = {1,2,...,11}.
```

For every source root, summing the thirteen nonnegative branch profiles
recovers the audited unsplit root profile exactly.  Every surviving branch
meets every one of the four owner-visible Boolean states in one connected
component.  Thus the geometric support matrix is

```text
row b=0       0000
rows b=1..11  1111
row b=12      0000.
```

This says two useful things at once.  The branch coordinate is not a disguised
fifth Boolean state, and neither Boolean bit determines it.  It also warns
that support incidence alone has low complexity; the decisive test must occur
after coupling to the relation response.

Three literal delete-before-transfer guard controls pass.  Summing over `b`
recovers every entry of the audited Boolean-square gamma bank, including its
pinned digest, rather than merely reproducing a final scalar.

## The nonredundancy gate

Let

```text
T(b,state,t),
    b,t in F_13 and state in V_4,
```

be the exact inverse table for the complete endpoint relation bank
`(1,0,t)`.  Flattening `(b,state)` against `t` gives

```text
rank(Boolean square against t) = 4,
rank(branch x square against t) = 6,
rank(branch marginal against t) = 3.
```

The strict jump `4 -> 6` is the primary result: the branch sheet contributes
two response directions that disappear under branch marginalization.  This
is the rank gate that prevents a repeat of MISTAKE-417.

At fixed relation `(1,0,6)`, the `13 x 4` branch-state matrix has rank three
and exactly `44=11*4` nonzero entries, matching the geometric support rather
than a finite-field cancellation pattern.

## Three-way interaction

The full Walsh/character census by coordinate support is

```text
DC  branch  state  residue  branch-state  branch-residue  state-residue  triple
 1      12      3       12            36             144             36     432.
```

After subtracting every zero-, one-, and two-coordinate marginal, the exact
three-way interaction has flattening rank six and census

```text
DC  branch  state  residue  branch-state  branch-residue  state-residue  triple
 0       0      0        0             0               0              0     432.
```

Hence all `12*3*12=432` allowable triple modes fire.  Fourier fullness is a
secondary description here; it is interpreted only after geometric support,
exact marginal recovery, and the strict rank increase have passed.

## What this adds to the live concept board

The five-state Gray path describes source-support changes, the Boolean square
describes the four owner-visible cut states, root difference colours directed
cut arcs, a marked tail points one such arc, and `b` records a source inverse
sheet.  These are distinct levels of structure, not names for one tournament:

```text
Gray-path state -> owner state -> pointed/coloured cut arc -> inverse sheet.
```

The present result proves that the last level is visible to the endpoint
relation response even after arc colour is marginalized.  It does not prove a
recurrence along sheets, a ternary-tree transport, or a harmonic-series
encoding of arbitrary subsets.  Such claims need a second clock or scale and
an explicit transition preserving this rank-six interaction.

The cheapest next hostile is a joint `(b,u-q,state,t)` table.  It should test
whether `b` contributes rank after conditioning on the already-live
root-difference coordinate.  A different next step is to construct an
explicit map from `b` to the canonical current-leg `r_owner`; absent that map,
the two labels must remain separate.

## Connection contract and boundary

| field | exact answer |
|---|---|
| source | left `P_169 e` inverse branches in the audited THM-2471/2594 source service |
| target | branch sheet x owner-visible Boolean state x endpoint relation residue |
| map | retain `b=floor(n/13)` before `Q`, transfer, endpoint multiplication, and integration |
| preserved | source roots and profiles, literal guard order, common base, Boolean state, actual endpoint factors, relation phase |
| destroyed | low inverse digit, older ancestry, source arc difference, arrival atom, current-leg owner branch, deep label, chronology |
| positive gate | exact branch marginal plus response-rank jump `4 -> 6` |
| hostile boundary | branch support alone is only eleven identical four-state rows; Fourier fullness alone is insufficient |
| survivor | nonzero rank-six pure three-way interaction with `432/432` triple modes |

No exact `C(a;X,m)`, source-to-arrival transport, `U_clock` chronology,
uniform-row statement, physical current, grouped coefficient, row exclusion,
or LRC(14) conclusion follows.

## Reproduction

```text
python -B 04-computation/lrc_r5_ufull_owner_node_boolean_square_branch_sheet_sidecar_probe_20260816.py
python -B -O 04-computation/lrc_r5_ufull_owner_node_boolean_square_branch_sheet_sidecar_probe_20260816.py
```

The normal and optimized transcripts are byte-identical.  The semantic digest
is `c82d9aba055f2f5ccbea75e092dd7a45b9a9f2a4a1b91762f32ad5852cf9c552`.
