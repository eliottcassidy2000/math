# LRC(14): endpoint-589 literal exit and representation change

**Session status: THM-4320 PROVED RELATIVE + FINITE-EXACT; LRC(14) OPEN.**

## Portfolio

- **Anchor:** continue the 3,925-mask carrier from endpoint 590. The exact
  endpoint-589 replay exposed 20,036 failures, but a direct wall-mass audit
  closed every one without changing the carrier. The typed frontier moves to
  endpoint 588.
- **Niche:** understand the q=50 failure hypergraph rather than treating its
  size as opaque. It has two dominant hubs, a trivial label-automorphism
  group, and only 5,651 minimal transversals among 20,025 bodies.
- **Wildcard:** reconnect the hostile fixed-50 row with THM-4234/4242's petal
  atlas. The exact match at the old boundary `589` was signal: 18,694 carrier
  failures already belonged to inherited petal layers, while direct mass
  supplied the remaining 1,331 four-/five-petal cases and extended the full
  ray down one row.

## Mechanism recovered

The carrier stores sufficient low-rank responses. Its failure statement is

```text
no stored active rank-8/rank-9 mask is disjoint from this body,
```

not

```text
the body's literal safe mass is below 4/63.
```

On a pair-safe wall cell let `F` be the pool-failure mask. For body `B`, the
rank-at-most-nine subtotal

```text
L(B)=sum_{|F|<=9, F intersect B=empty} width(F)
```

is already a rigorous lower bound for the full literal mass. At endpoint 589
it is strictly above threshold on all carrier failures. This is why a large
response problem disappeared after restoring the coordinate that the carrier
quotient had discarded: the complement body itself.

## Connection audit

```text
source:       endpoint-589 carrier-failure ledger
target:       literal Haar mass of each failed labelled body
map:          B -> all pair-safe wall classes disjoint from B
preserved:    q, r, labelled body, exact cell widths, threshold normalization
destroyed:    reusable response rank, carrier multiplicity, deletion capacity
sidecar:      full high-rank wall classes and fixed-50 petal count
test:         verify 63 L(B)-4D>0 bodywise by independent wall reconstructions
```

The fixed-50 map `B -> |B intersect O|` preserves the labelled body only when
the body ledger remains attached. Petal count alone destroys margins and
carrier incidence. The gcd hostile `95` versus `190` likewise shows that
arithmetic residue type alone cannot replace labelled wall structure.

## Research move worth reusing

After any finite carrier failure, run the following escalating tests before
starting another exchange:

1. direct low-rank literal subtotal on the exact failure bodies;
2. full literal mass if the subtotal is inconclusive;
3. inherited chart/face classification on the remaining bodies;
4. only then a response-cover and deletion-capacity search.

Trigger: the carrier has a small exact failure ledger and the wall classes are
already available. Counterindication: the target needs a reusable carrier for
an infinite family rather than closure of a frozen row. Evidence now comes
from endpoint 589 and the earlier fixed-50 literal-head machinery; promotion
to a universal meta-pattern should wait for another independent endpoint.

## Live next tasks

1. Replay the unchanged carrier on the 66 endpoint-588 rows, but compute direct
   literal subtotals immediately for any failures.
2. Freeze the separate q=50 response diagnostic. Current exact work gives a
   nineteen-body incompatibility packing and an explicit finite cover; this
   measures representation cost but is not needed for THM-4320.
3. Seek a sufficient inequality predicting when the rank-at-most-nine subtotal
   is automatically positive from the wall-class rank distribution, avoiding
   bodywise enumeration.
4. Test whether dominant-hub fibres at endpoint 588 recur with a stable
   deletion/contraction rule or whether endpoint 589's trivial automorphism
   group is the generic obstruction to symmetry compression.

The main lesson is not that response methods failed. They exposed the exact
place where the representation ceased to match the target. Restoring literal
mass closed that layer; physical entry and LRC(14) remain untouched.
