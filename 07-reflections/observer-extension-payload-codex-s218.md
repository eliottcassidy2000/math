# Observer-extension payload - S218

The useful correction is that the first shifted A000568 failure is
`48 + 8 = 56`, not `48 + 12 = 56`.

The number `12` is still important, but it lives on a different axis.  It is
the fold/parent/fixed-locus count:

```text
R(4)=12
U(5)=12
5->6 source slice=12
5->6 sink slice=12
SC(6)=12
```

The defect `8` is the first observer-extension payload.  Node perspectives
reach exact rooted type, but they do not see the rootless coupling exposed by
the fixed-point-free `[3,3]` Burnside term in `U(6)`.  That is why adding an
incident word is the right next carrier: rooted 5-perspective plus incident
word becomes ordered-pair perspective on six vertices.

The ladder now looks coherent:

```text
node perspective
  -> incident word
  -> ordered pair / directed edge sector
  -> cross-sector orientation
  -> deletion-parent fiber
  -> rectangle/hourglass residue
  -> endpoint-owner/proof-obligation payload
```

This is the abstract controlled-forgetting rule.  A quotient may forget a
coordinate only if the lost coordinate is constant, reconstructed, dual
annihilated, descended to potentials, boundary-stopped, or routed to named
debt.  In S217 terms, a zero rectangle/hourglass residue means line data
descends to potentials; a nonzero residue is the hidden coordinate.

The same pattern is already present across the LRC stack.  Pair-good decoys
need generator/barcode/active-owner payload before counting.  Endpoint-owner
transfer repairs old/new endpoint role.  Fiber-zipper convergence repairs
automatic words with Erdos-Turan bins, Henselian unit clocks, and magnitude.
Haar squares and residual capacitors are rectangle-zeta instances.  Closed
arc `H1` is the topology version of an observerless cut payload.

Assumption challenge: I considered runners, rooted nodes, ordered pairs,
directed edges, cyclic triples, deletion fibers, diagonal layer lines,
rectangle/hourglass residues, barcode bars, active bottleneck owners,
endpoint-owner packets, Henselian unit clocks, and proof obligations.  The
selected vertices are payload carriers.  The preserved predicate is safe
controlled forgetting; the destroyed data is labelled identity, endpoint role,
rootless cyclic coupling, deletion address, and line-flow cycle residue unless
carried as a sidecar.
