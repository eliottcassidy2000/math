# k=8 Resolvent Sidecar Certificate

Anchors: HYP-3142, HYP-3141, HYP-3140, HYP-3139, HYP-3138, HYP-3137, HYP-3136, HYP-3135, HYP-3134, HYP-3133, HYP-3132,
HYP-3131, HYP-3129, HYP-3122, HYP-3119, HYP-3118, HYP-3113, HYP-3111,
HYP-3110, THM-577, T1207, LTI-268, LTT-166, OPEN-Q-108.

The useful reframe from this session is that "search niche unrelated work" no
longer means adding analogies.  The older Savitch/Bravais/De Moivre/Hensel
work has become a grammar for what a terminal k=8 certificate has to retain.
After the incoming GF and reflection-fold work, the role is sharper:
HYP-3137 names the generating-function payload, HYP-3138 supplies the
fold-adjoint repair table, HYP-3139 splits the reflection block into proof
pages, HYP-3136 reduces the multi-far floor to a finite packet chase,
HYP-3140 converts the `Rprime` scalar into a finite 14-sheet fiber-PGF
conditional first moment, HYP-3141 makes directed edges carry tip/tail
proof-information fields, incoming HYP-3135 says not to forget the middle De
Moivre/SPEC payload, and HYP-3142 is the exact bounded-core `U4` exit that
packet can call.

The new exact scout says: on the primitive bounded bank through `B=14`, every
k=8 row satisfies the exact 4-moment cap certificate `U4(E) <= cap_8`, and
the worst row is exactly `consec_8`.  At `B=13`, the scan covers `1716` rows;
at `B=14`, it covers `3431` rows.  In both cases `U4_over_cap_count=0`.

The striking part is not only the inequality.  The worst stalk has every
side-signal the older maps predicted should matter:

```text
E=(0,1,2,3,4,5,6,7)
U4=2633/7350
cap_8-U4=683/29400
nearest_root=1.488584
real_roots=0
Bravais peak=1/8
residue entropy=1
mirror defect=0
kappa4=-0.787150
```

This fuses the maps:

- HYP-3132 supplies the algebraic fold: `g(u+3)=u^4-5u^2+4`.
- HYP-3141/HYP-3140/HYP-3137 supply the edge/coefficient payload discipline:
  tournament edges carry tip/tail proof fields, `Rprime` becomes a fiber-PGF
  conditional first moment, and `U4` remains the k=8 terminal
  coefficient/cumulant exit.
- HYP-3135/HYP-3136 supply the surrounding packet theorem: keep signed SPEC,
  Lee-Yang, Q-floor, and edge-child fields until the bounded-core exit fires.
- HYP-3122 supplies the phi4 sign at the extremal stalk.
- HYP-3113 supplies the Bravais-flat warning: high coverage is reciprocal-flat.
- HYP-3118 supplies the repair-depth language for rows that miss one sidecar.
- HYP-3134 supplies the controlled-forgetting rule before edge child payload
  is dropped.

The next proof attempt should not chase another scalar.  It should prove the
moment-majorization statement

```text
U4(E) <= U4(consec_8)
```

for all primitive k=8 bounded-core rows, with non-flat Bravais spectra giving
strict slack and the flat exceptional stalk handled by the biquadratic
resolvent.  If an unbounded row evades that argument, the right failure label
is not "new obstruction"; it is a missing Hensel/CRT `2x7` reduction showing
how the row descends to the bounded flat stalk or to named finite resonance
debt.

Challenged assumption: the hard k=8 node does not need to be represented by
runners, roots, or tournament classes alone.  The live vertex set is proof
sidecars: exact `U4`, folded resolvent, Bravais shape, Savitch repair depth,
Lee-Yang root confinement, fiber-PGF sheet counts, A000568 global consistency,
and coordinate resurrection.  That is the object with the smallest current
missing-input vector.
