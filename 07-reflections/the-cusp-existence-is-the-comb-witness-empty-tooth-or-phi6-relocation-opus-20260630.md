# The cusp existence IS the Dirac-comb witness: at the cusp (odd core = Z₇) the apex measure vanishes (ρ=0) but D(q*) ≥ 1 holds at the witness denominator q* — the comb q*=14=n (the empty tooth, the 6 units, AP) or its relocation q*=Φ₆(n)=n²−n+1 (the killer fills the tooth, covering). So LRC(14) = off-cusp DOUBLET (measure, proved) + cusp COMB-WITNESS (existence) — and the cusp half is exactly my razor's-edge/comb/Eisenstein work

*opus-2026-06-30. Owner: work the cusp existence side. It is not a separate hard problem — it is the
EXISTENCE (witness) half, and the witness is the comb's empty tooth (or its Φ₆ relocation). The descent's
cusp and my Dirac-comb are the same object.*

## The cusp is the EXISTENCE side, not the measure side (pinned)
At a descent level with odd core `O_j mod 7 = Z₇` (the unique gap-0 core), the apex MEASURE vanishes
(`ρ_j = 0`); the doublet/measure argument is silent and correct (no contradiction). Existence is carried
by the WITNESS, computed:
| config | cusp? (`O₀ mod 7`) | witness `q*` | `D(q*)` (exists) | `M` |
|---|---|---|---|---|
| AP `{1..13}` | `Z₇` ✓ | **14 = n** | **6** | `1/14` |
| `{1..11,13,84}` | `Z₇` ✓ | 89 | 2 | `7/89` |
| `{1..12,182}` | proper | **183 = Φ₆(14)** | 2 | `14/183` |
> **MEASURE `lim_q D(q)/q → 0`** at the cusp (the lonely set is measure-zero — e.g. AP's lonely set is the
> 6 units `{k/14}`). **EXISTENCE `D(q*) ≥ 1`** holds at the witness denominator `q*`. The cusp needs ONE
> witness, not a positive density. "Existence direct from BOUNDED" (klein-S4): finite, decidable.

## The cusp witness IS the comb's empty tooth (and its Φ₆ relocation)
The witness denominators are exactly my comb:
> **AP: `q* = 14 = n`** — the comb `Ш₁₄` itself; the witnesses are the **6 empty-tooth units**
> `{1,3,5,9,11,13}/14` (the observer's gap). **Covering: the killer (mult-of-14) FILLS the observer's
> comb-tooth, so `q*` RELOCATES off 14** to a coprime denominator — the next Farey rational — and the
> minimal relocation is `q* = Φ₆(n) = n²−n+1 = 183` (the **Eisenstein-norm denominator**, `M = n/Φ₆(n)`).
So the cusp-existence witness is the **Dirac-comb empty tooth** (AP) or its **Φ₆/Eisenstein relocation**
(covering). The descent's cusp existence and my razor's-edge/comb/covering-min/Eisenstein reflections are
the SAME object — the killer filling the empty tooth, the witness jumping to the next Farey/Eisenstein
denominator. The cusp is not new; it is the comb, named in the descent.

## LRC(14) = two regimes, two tools (the full split)
> **OFF-CUSP** (proper core `O_j ⊊ Z₇`): `ρ_j ≥ g(O_j mod 7) ≥ 4cos²(3π/7)` — the **DOUBLET / MEASURE**
> side, **PROVED finite** (THM-590). The positive density gives `D ≥ 1` for free.
> **CUSP** (core `O_j = Z₇`): `ρ_j = 0`, the measure is silent — the **COMB-WITNESS / EXISTENCE** side:
> `D(q*) ≥ 1` at `q* = n` (the empty tooth, AP) or `q* = Φ₆(n)` (the relocation, covering).
The doublet carries the measure (bulk); the empty tooth carries the existence (cusp). Both are finite,
both are about the same `Z₇`/comb object — the doublet is its minimal *core*, the empty tooth its minimal
*gap*. (THM-588/HYP-3585: doublet ↔ 3-cycle ↔ the minimal relation; here doublet=measure-atom, empty
tooth=existence-atom.)

## What is proved vs open at the cusp
- **AP (the bare comb):** the empty-tooth witness EXISTS — the comb `Ш₁₄` has the observer's tooth as a
  width-`1/14` gap (the 6 units), `M=1/14` exactly. Proved (the construction).
- **Covering (the relocation):** the construction `{1,…,n−2,(n−1)n}` gives an explicit relocated witness
  `M = n/Φ₆(n) = 14/183 > 1/14` (the killer binds the next Farey rational). Proved for the construction.
- **OPEN (the global cusp existence):** that EVERY cusp covering config has `D(q*) ≥ 1` with `M ≥ 1/14`
  — i.e. the relocated gap never sinks below `1/n`. This is the covering-min `≥ 1/n` (the conjecture, the
  `Φ₆`/Eisenstein side). The cusp does not need a measure bound; it needs the relocated empty tooth to
  exist above `1/14`, which is the bounded-core/witness statement.

## Status
- **Pinned (opus):** the cusp = the EXISTENCE side (`D(q*) ≥ 1`, witness), not the measure side
  (`D/q → 0`); the cusp witness IS the comb empty tooth (`q*=n`, AP) or its Φ₆/Eisenstein relocation
  (`q*=Φ₆(n)`, covering). LRC(14) = off-cusp doublet (measure, proved) + cusp comb-witness (existence).
- **Unified:** the descent's cusp existence and my Dirac-comb/covering-min/Eisenstein work are the same
  object — the killer filling the observer's tooth, the witness relocating to the next Farey/Φ₆ rational.
- **Open (the cusp half):** global existence `D(q*) ≥ 1`, `M ≥ 1/14` for all cusp coverings = the
  covering-min `≥ 1/n` (the witness/bounded-core side, the Φ₆ relocation never below `1/n`). The off-cusp
  half (doublet measure) is proved.

Related: the per-level-vs-total / doublet reflection (this is its cusp/existence counterpart); klein
HYP-3597 (measure/existence split), HYP-3598 (Z₇ cusp), HYP-3580 (existence direct from bounded), THM-590
(doublet measure); my Dirac-comb, razor's-edge, covering-min-Φ₆-Eisenstein, two-pyramids reflections;
OPEN-Q-108.
