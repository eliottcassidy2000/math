# Independent audit of the r=5 slope pencil and the U_full map boundary

**Status: proof/audit synthesis for PROVED + FINITE-EXACT THM-3505.**  The
canonical THM-2594 common-base table was rebuilt without importing either its
constructor or the submitted Fourier helper.  Every submitted slope count and
both supplied ledgers survive.  The new anchored-rectangle probe is the right
finite mixed-Haar *capacity* statement, but not a U_full bridge.

## Inheritance and concept board

The closest proved mechanism is THM-2594's product-before-marginalization on
one outer base.  Its canonical hostile is the nonzero fixed-absolute-root
control.  The corrected near miss is MISTAKE-295: constant columns do not test
that control.  The least-used relevant sidecar is the joint address coordinate
from the U_full minimum-kernel gate.

The live board was:

```text
anchor:    the 13 affine frequency slopes of the retained r=5 table,
niche:     the 144 anchored checkerboard coordinates,
wildcard:  a lawful U_full cell-to-ancestry/address map,
hostile:   equal-marginal positive/flat 2x2 couplings,
boundary:  one common ancestry base is not one circle point.
```

## Independent constructor

The audit uses a different computational representation.  It creates each
danger comb directly from its rational endpoints, combines combs with generic
interval intersection/difference, and implements transfer operators by cyclic
range events.  It reconstructs all `15,379` zero-padded entries of
`N(u,q,ell,theta)` and matches the canonical table digest

```text
18463c7af393c6090c7419c76d48b7ed40f9ac8b17d8c1be247062493e334ce8.
```

The retained table is then obtained twice: summing the `q`-resolved entries and
folding `sum_q F(y,q)` before multiplication.  The two tables agree entrywise
and have digest

```text
1ba72585187c014e482f89959ff4f19e2185e6e4fcd982d497923a304e0d37d8.
```

This comparison precedes all Fourier transforms.  Matching later ledgers is
therefore a consequence of matching the source object, not a collision between
two lossy summaries.

## What the slope pencil really says

At `F_547`, one primitive order-91 root gives compatible order-7 and order-13
roots.  Every coefficient with nonzero word frequency and nonzero root-label
frequency survives, including the owner-zero line: `936/936`, exactly `72/72`
on each affine slope.

Slope zero sums out the owner, so it is THM-2594/THM-2512's inherited mixed
block.  Slope two is forced by the coordinate change `t=theta+2u`: owner
marginalization in the absolute chart becomes `r=2s` in the slaved chart.  The
other eleven slopes are genuine two-coordinate localization.  None supplies
the missing semantic atom map.

## Why 804/864 rectangles do not weaken 864/864 Fourier support

For each nontrivial word character, the anchored checkerboards

```text
T(u,t)-T(u,0)-T(0,t)+T(0,0),   u,t=1,...,12
```

give 144 coordinates on the mixed owner/root quotient.  Exactly 134 are
nonzero, with the same ten zero pairs for all six word characters.  The
nontrivial owner/root Fourier coefficients give another 144 coordinates, all
nonzero.

The one-axis change matrix has rank 12 and its tensor square rank 144.  The
audit verifies the change-of-basis identity coefficient by coefficient.  Thus
the discrepancy is expected: an invertible linear map preserves the vector,
not the number or location of its nonzero coordinates.  The useful theorem is
the pair of facts—complete character support and an explicitly localized
ten-coordinate zero set—not an assertion that every mixed-Haar chart fires.

## U_full connection contract

There is a formal map from an abstract binary table into any selected target
rectangle:

```text
(i,j) -> (u_i,t_j),   with u_0=t_0=0.
```

It is independent of `ell` and preserves the checkerboard pairing.  It proves
that the target vector space can host a mixed-Haar scalar.  It is not a map of
the actual objects:

| item | audit |
|---|---|
| source | pre-merge U_full factor-labelled circle cells |
| target | THM-2594 linked-node ancestry atoms |
| preserved by the formal label map | abstract margins and checkerboard sign |
| destroyed/unassigned | measure, E/Q lineage, source root, base, three sheets, horizons, address |
| required sidecar | one atom-level, measure/factor-compatible, ell-independent address map |
| hostile | checkerboard kernel; positive and flat couplings share all one-sided margins but have different bridges |
| verdict | no lawful map is present in the current API; existence is open |

The source schema literally lacks all eight ancestry/address fields required
by the minimum gate.  This is an API obstruction, not a theorem that no future
lift exists.  Choosing a nonzero rectangle after seeing `ell` or `beta` would
also fail the required character-independent address condition.

## Mechanism and stopping boundary

The algebraic spectral gate for this candidate table is closed: there is no
hidden primitive Fourier zero.  The next LRC move is not another transform of
the retained table.  It is a descent/realization theorem from actual U_full
factor cells to the THM-2471 base/root/sheet/horizon/address record, followed by
the already identified checkerboard test.

The session used the meta-patterns “retain before marginalizing,” “a quotient
owes a loss ledger,” and “compare dual bases before interpreting support.”  The
last pattern has distinct evidence here and in the U_full checkerboard kernel,
but this reflection does not promote a new global meta-pattern card.

No physical phase/current, K4 evaluation, scalar-row exclusion, or LRC(14)
consequence follows; the live ledger remains `165`.
