# Carriers, strict transforms, and operation-response compilers

**Research synthesis, 2026-08-03.  NON-CANONICAL.**  The theorem links below
are the truth sources.  This reflection records the current concept board,
tested reframes, cross-frontier maps, and the next decisive experiments.  A
reserved theorem is not evidence, and no item below proves `LRC(14)`, `JC(2)`,
`FC(3)`, or the open Q4 radius statement.

## 1. What changed mathematically

The most important recent advances are not a single reduction between the
open problems.  They are five sharper kinds of intermediate object.

1. **The direct LRC atlas moved, with its quotient correctly typed.**
   [THM-3230](../01-canon/theorems/THM-3230-projected-k3-z219-common-gcd-three-terminal-descent-and-cap218.md)
   closes all sixteen projected `z_1=219` rows, lowers the `k=3` cap to `218`,
   and leaves `119` rows at the next layer.  Every residual at `219` has
   `g=3,q=3`: the ambient quotient is the chain `{1,3}`, but residual support
   is saturated at its top.  THM-3245 proves its internal restorations form an
   idempotent band, not a `C3` action.  [THM-3242 (projected-k3 z217)](../01-canon/theorems/THM-3242-projected-k3-z217-exact-status-annihilation.md)
   also kills all `66` states on its eight rows, but out of order: open
   `z_1=218` still blocks any cap or ledger change.

2. **A carrier can now be costed before it is interpreted.**
   [THM-3228](../01-canon/theorems/THM-3228-four-jet-heisenberg-minimal-faithful-permutation-carrier-gate.md)
   proves that a permutation carrier seeing the four-jet commutator needs at
   least `p^2` points.  [THM-3234](../01-canon/theorems/THM-3234-singer-owner-compactification-and-pointed-heisenberg-carrier-gate.md)
   then proves that the numerical coincidence `168+1=13^2` has an exact
   abstract realization: a Singer cycle compactifies the 168-cycle to
   `F_13^2`, but the completion point must be mobile and the joint symmetry is
   the full `AGL_2(F_13)`.  A globally fixed head costs `170`, not `169`.

3. **A quotient wall can be divided out instead of merely declared zero.**
   [THM-3231](../01-canon/theorems/THM-3231-fraction-free-simple-pivot-wall-second-order-blowup-carry.md)
   proves universal coefficientwise divisibility by the square of a vanishing
   pivot and identifies the strict transform with a fourth osculating minor.
   This retains the normal direction erased by specialization.

4. **Root choice and branch placement are separable resources.**
   [THM-3227](../01-canon/theorems/THM-3227-selected-root-residue-contact-trie-primitive-carry-and-delayed-resplitting.md)
   and [THM-3229](../01-canon/theorems/THM-3229-hasse-pluecker-simple-root-contact-gcd-flag-and-degree-termination.md)
   organize selected and root-free contact data.  [THM-3232](../01-canon/theorems/THM-3232-root-free-contact-stratum-norm-and-discriminant-power.md)
   removes root ordering by an etale norm while preserving simultaneous
   nonvanishing.  It necessarily loses the additive branch label and prime
   carry.  Thus a root-free scalar is a gate, not a selector.

5. **Sequence formulas should be compiled from the operation, not guessed
   from terms.**  Order join, cyclic substitution, generic transitive
   blow-up, paired-owner Q4 blow-up, and a time-varying factorial transfer
   have different lawful response states.
   The recent tournament and factorial work gives exact formulas or linear/
   quadratic arithmetic evaluators in all five regimes, together with sharp
   reasons that the formulas cannot be compressed to the same recurrence
   class.

The active new theorem packages sharpen this picture further:

- **PROVED [THM-3233](../01-canon/theorems/THM-3233-fraction-free-exceptional-quotient-tail-renormalization.md)** iterates the THM-3231 exceptional-square division: on the normal
  divisor the entire later fraction-free tower is conjugate to a new tower
  generated by the reciprocal quotient tail `S^3(t/v)` and `1`.  Its universal
  extracted factors run on twice the Pell clock and equal actual row orders
  exactly when the normalized row is nonzero; the geometric/simple/double
  successor boundaries are exact.
- **PROVED [THM-3236](../01-canon/theorems/THM-3236-contact-spectrum-primitive-element-and-root-reconstruction-gate.md)** upgrades the contact norm to the full characteristic polynomial of
  the contact coefficient.  Its discriminant/Krylov determinant decides
  exactly when that coefficient is a primitive element of the root algebra;
  a second observer generically resplits collisions.  It still gives no root
  ordering or physical carrier.
- **PROVED [THM-3241](../01-canon/theorems/THM-3241-finite-field-contact-Singer-realization-and-order-gate.md)** realizes every prescribed nonzero irreducible finite-field contact
  coefficient and, over `F_169`, the exact THM-3234 Singer matrix.  Its
  same-norm field generator of order `24` proves that contact-algebra
  generation is strictly weaker than Singer transitivity; no physical LRC
  provenance is supplied.
- **PROVED [THM-3243](../01-canon/theorems/THM-3243-contact-deformation-blowup-equivariance-and-full-orbit-resolution.md)** identifies the exact blowup-equivariance boundary.  A one-point blowup
  lifts precisely the affine stabilizer of its centre; blowing up all `169`
  rational contact classes restores `AGL_2(F_13)`.  Its exceptional boundary
  is one `169`-point affine Heisenberg orbit plus one regular `2,197`-point
  orbit, and the latter is not the equal-cardinality THM-3240 address carrier.
- **PROVED [THM-3245](../01-canon/theorems/THM-3245-pointed-divisor-median-cubes-saturation-band-no-go-and-z219-supplier-support.md)** identifies pointed divisor intervals as median partial cubes but proves
  every natural internal restoration is a commuting idempotent, with trivial
  group image.  At `z_1=219` the apparent supplier graph is only a graph of
  sorted positions; its weighted automorphism group is trivial and the label
  map needed for a physical carrier has already been quotiented away.
- **PROVED [THM-3246](../01-canon/theorems/THM-3246-all-dilation-second-owner-seam-stabilization-and-sign-word.md)** gives exact quadratics for all `168` cell masses in one THM-3224 lane at
  every dilation.  Its twelve seam cells are the only negative second
  correctors, the other `156` are positive, and the Hodge sum is `1/24696`.
  This upgrades the Singer-line hostile to all dilations but supplies no row
  exclusion.
- **PROVED [THM-3235](../01-canon/theorems/THM-3235-transitive-tournament-blowup-saddle-and-hypergeometric-decimation.md)** gives a general substitution determinant, exact saddle/radius
  renormalization under uniform transitive blow-up, and hypergeometric
  decimation of the full coefficient sequence.  Its order-six observer twins
  refute compression by complete path-colour profile plus product radius.
- **PROVED [THM-3237](../01-canon/theorems/THM-3237-degree-nine-jacobian-infinity-wall-and-square-root-escape.md)** finds the first nonlinear `B_t=1+t x^9` term in the stated planar
  Jacobian source family that changes the infinity face.  At one exact wall,
  two leading resultant layers cancel and exactly two reciprocal roots obey a
  square-root escape law.
- **PROVED [THM-3240](../01-canon/theorems/THM-3240-exact-address-heisenberg-clutch-on-carrier-imbalance.md)** realizes the nonnormal minimal `H_13` stabilizer clutch on the
  exact THM-2763 carrier-imbalance address quotient.  It is an address action,
  not yet a coefficient-current action.
- **PROVED [THM-3247](../01-canon/theorems/THM-3247-heisenberg-central-fourier-decomposition-and-canonical-current-cyclicity.md)** decomposes the standard affine Heisenberg permutation module into
  thirteen scalar lines and twelve charged `13`-dimensional packets.  Every
  canonical endpoint current hits all `25` blocks and is cyclic of rank `169`
  in both certified fields for every nonzero direction.  This closes the
  coefficient-carrier rank debt, not physical ancestry or target naturality.
- **PROVED [THM-3250](../01-canon/theorems/THM-3250-charged-heisenberg-blowup-address-intertwiner-and-pointed-multiplicity-gate.md)** gives an explicit unitary-normalizable Fourier intertwiner between every
  nonzero central sector of the exact-address and nonvertical-flag carriers.
  Their entire representation mismatch is neutral.  In each charged sector a
  pointed current fixes the `GL_13` multiplicity frame exactly when its
  `13`-by-`13` coefficient determinant is nonzero.  Independent audits accept
  the signs, spectra, commutant, determinant and scope boundaries.
- **PROVED [THM-3252](../01-canon/theorems/THM-3252-singer-compactified-owner-hodge-word-universal-charged-cyclicity.md)** zero-sum compactifies THM-3246's owner-Hodge word into a `13`-by-`13`
  matrix that is nonsingular in all `8,064` Singer cyclic gauges.  A rational
  signed central contrast combines its twelve cyclic blocks into one packet
  spanning all `2,028` charged dimensions.  This closes the exact owner-word
  multiplicity gate, not positivity, endpoint provenance, target covariance,
  ancestry, or an LRC row.
- **PROVED [THM-3238](../01-canon/theorems/THM-3238-complete-physical-product-gamma-bank-unique-reset-stitch.md)** glues twenty-two lawful coarsening-upset rows into a positive cocircuit
  whose unique zero is the reset on the complete `4,319`-state physical bank.
  It closes one support-`(1,3)`/bank-`I2` face, not a local deletion flow or
  the other `229` faces.
- **PROVED [THM-3244](../01-canon/theorems/THM-3244-unique-reset-exposure-deletion-graph-nonmorse-boundary.md)** proves that the exposed `H`-reset has `32` nonreset one-pole local
  maxima, while lawful rows 2 and 10 cover every nonreset state by a
  reset-distance-monotone one-pole ascent.  Among the 22 stitched rows there
  are 31 covering pairs and no singleton cover; two chart-label switches are
  sharply necessary.  No fixed positive blend of rows 2 and 10 removes the
  switching: two rational trap intervals cover every blend ratio.  Separately,
  radius `10` is the sharp `H`-unique-sink scale and two hubs route the bank in
  at most two jumps.  The audit types these as a state-dependent selector and
  folded preorder, not an orthant or an arbitrary discrete-Morse conclusion.
- **PROVED [THM-3248](../01-canon/theorems/THM-3248-q4-paired-owner-stirling-compiler.md)** factors the full Q4 walk resolvent through two owner pairs and gives
  an exact all-depth finite-difference contraction, a positive one-sum
  Hamiltonian formula, and fixed-depth `O_d(M(r)+r log r)` unit-cost
  evaluation.  Its observer group is not `Aut(Q4)`, and the radius/non-P
  frontiers remain open.
- **PROVED [THM-3239](../01-canon/theorems/THM-3239-universal-sixth-resonant-prs-strict-transform-wall-and-pell-atlas.md)** computes the sixth universal resonant PRS strict transform: after
  the inherited square is removed, one primitive irreducible degree-28 wall
  remains, the Pell content reaches `70`, and the normalization gauge reaches
  `239`.  Its parity makes the new wall nonzero at every integer, but the
  all-depth primitive-factor pattern is not claimed.

All statements remain governed by their literal theorem statuses; this
reflection does not itself promote them.

## 2. Live concept board

The board below is deliberately typed.  Similar cardinalities or equations
do not identify the objects.

| concept | representation | invariant now retained | operation being tested | information still missing |
|---|---|---|---|---|
| LRC owner/divisor shadows | cyclic word of length `168` plus pointed `Div(g)` intervals | gcd-carry Hodge split; universal charged cyclicity after zero-sum completion; median and label-loss boundaries | dilation, restoration, Singer compactification | positivity, endpoint provenance, labelled supplier map, physical entry, cellwise safety |
| minimal four-jet carrier | `H_13/K` on `169` points; full flags `E=V disjoint_union H_13` | full `13+156` response; exceptional module has exact generator width two | Heisenberg action, current orbit, Singer/blowup transport | target-preserving physical intertwiner and common ancestry |
| carrier address | `G_delta` of size `13^3`, or `G_full` of size `13^4` | target axis, carrier imbalance; charged intertwiner; signed owner frame spans all `2,028` charged dimensions | typed section, quotient gauge, exact `H_13` clutch | physical coefficient frame, positivity, neutral data, factor colours, ancestry |
| factorial contact/reset flag | finite etale contact algebra plus one `4,319`-state physical bank | contact spectrum/order; exposed reset; lawful two-row one-pole selector; sharp radius-10 routing | Hasse contact, upset stitching, chart switching, Rips/edit scale, PRS step | other faces, scalar gauge beyond the row-(2,10) cone, cross-face flow, selected branch and global root choice |
| exceptional normal coordinate | pivot or infinity parameter before specialization | strict-transform coefficient after leading cancellations | blow-up, reciprocal Newton polygon | physical/Keller chart entry and global continuation |
| tournament quotient operator | multivariate determinant `D_Q` and walk series `W_Q` | Neumann domain, saddle, Q4 paired-owner numerator | substitution, order join, transitive blow-up | generic minimal response-equivalent sidecar |
| sequence response state | fixed/moving jet, paired Stirling rows, or bounded transfer | exact coefficient or recurrence law | finite difference, decimation, continuant convolution | bit complexity, converse closure, generic seed computation |

Every new idea was compared with all seven concepts.  Several comparisons
survived as exact maps or compilers; others survived only as mechanism
analogies; the tempting object identifications failed hostile tests.

## 3. Exact connection contracts

### 3.1 Cyclic owner orbit to Singer carrier

```text
source:      the abstract 168-cycle in THM-3224;
map:         j -> alpha^j in F_169^*, then adjoin 0;
target:      the 169-point affine plane;
preserved:   cyclic shift and the factorization 168=14*12;
destroyed:   interval order, phase endpoints, q-values, ancestry and sign;
new invoice: completion point mobile; joint group AGL_2(F_13);
sidecar:     an owner-to-address/current map preserving the LRC predicate;
hostiles:    fixed-head lower bound 170 and the refuted sign-line reframe.
```

This is a genuine common carrier but not a physical carrier.  THM-3246 proves
for every dilation in one exact lane that the twelve
negative second-corrector owners are the boundary cells
`0,...,5,162,...,167`, occupying twelve distinct residues modulo `14`.
Under every Singer gauge, a punctured vector line meets them in at most one
point.  Adding zero therefore does not turn the negative class into a line.

THM-3252 shows that this sign-line failure is not a charged-rank failure.
After adjoining the unique zero-sum completion, the owner word is a
nonsingular `13`-by-`13` coefficient matrix in every one of the `8,064`
Singer cyclic gauges.  Its signed rational central contrast spans the entire
`2,028`-dimensional charged address sector.  The preserved coordinate is the
multiplicity frame; the destroyed predicates are positivity, endpoint origin,
target semantics and ancestry.

THM-3241 now supplies an exact algebraic source for the same target:
`f=S`, `g=S+S^2(1+10x)` over `F_13`, with `S=x^2-2`, has first-live contact
`c_2=1+2x`, and multiplication by `c_2` is literally the THM-3234 matrix.
This preserves contact depth and exact multiplicative order but destroys all
LRC provenance.  The hostile `c_2^49=7x` has the same primitive norm and still
generates `F_169`, yet has order `24`; the root-free order-resultant tests are
therefore an indispensable sidecar rather than a consequence of THM-3236.

THM-3243 makes the resolution boundary equally exact.  Blowing up only the
delayed-contact origin carries the Singer lift because Singer fixes that
centre, but no nontrivial Heisenberg translation self-lifts there.  Blowing
up the complete `169`-point rational centre orbit restores full affine
equivariance and produces

```text
vertical flags:      one H_13 orbit of size 169;
nonvertical flags:   one regular H_13 orbit of size 2,197.
```

The second number equals `|G_delta|` but the objects are not equivariantly
interchangeable: the flag orbit has trivial stabilizer, whereas `G_delta` is
thirteen `169`-point orbits with stabilizer order `13`.  Thus resolving centre
drift supplies a new carrier and a sharp obstruction, not the missing address
identification.

There is also an exact representation-width consequence of proved
THM-3243+3247, recorded here without a separate theorem promotion.  Over a
splitting field of characteristic different from `13`, the exceptional
permutation module is

```text
K[E] ~= K[V] direct_sum K[H_13].
```

The regular-torsor identification in this display requires a nonvertical base
flag.  The `13` scalar characters selected by `K[V]` occur twice in `K[E]`,
the other `156` scalar characters occur once, and each charged
`13`-dimensional irreducible occurs `14` times.  The selected-scalar and
charged multiplicities exceed the corresponding cyclic-module bounds `1`
and `13`, so `K[E]` is not `H_13`-cyclic.  It needs exactly two generators:
one point delta generates `K[V]` and another generates the regular summand.
In THM-3247's characteristic-zero and certified specializations, its canonical
current may replace the first delta.  THM-3244's lawful reset selector also has
width two, but the types differ: module width two means simultaneous linear
generators, whereas the reset state chooses a lawful row and two states require
two chart-label switches.  The coarse two-hub atlas is different again.  No map
between these structures is claimed.

### 3.2 Carrier imbalance to the nonnormal Heisenberg clutch

Retain THM-2763's imbalance quotient

```text
G_delta={(r,delta): r.W+delta=0}/(L_13,0).
```

Using the typed section `e_0` and a chosen THM-2350 target axis, write one
fibre as `(s,delta) in F_13^2`.  The proved address action is

```text
(x,y,c):(s,delta) -> (s+x, delta+c-y*s).
```

Its stabilizer at `(0,0)` is the required noncentral line
`{(0,y,0)}`; its core is trivial and its center is visible.  This repairs the
specific defect of the old additive `F_13^2` prefix grid, whose regular
pullback kills the center.

```text
source:      one target coordinate plus carrier imbalance in G_delta;
target:      13 disjoint faithful 169-point H_13 orbits;
preserved:   quotient constraint, chosen target axis, central imbalance shift;
destroyed:   factor provenance after marginalization and integral carry;
extension:   G_full has 169 such orbits after common harmonic is restored;
sidecar:     factor-coloured extended coefficient current;
hostile:     projection to the old target quotient kills y and the center.
```

This is stronger than the cardinality observation and weaker than LRC
covariance.  THM-3247 now puts a proved coefficient on the isomorphic
endpoint-plane carrier: after a frame `(q,u)` with `det(q,u)=1`, pull back

```text
C_(q,u)(a,delta)
  =P_((delta+1)q+a*u) Q_(delta*q+a*u).                 (A)
```

The central Fourier decomposition is

```text
13 center-blind scalar lines + 12 charged packets of dimension 13,
```

and `(A)` hits every packet, so its `H_13` orbit spans all `169` states.  The
old coarse projection is precisely the center-neutral sector: every
equivariant map from a center-trivial old current to a charged packet is zero.

The abstract map is nevertheless not target-preserving.  It identifies the
address target coordinate `a` with the endpoint determinant `det(q,R)`, while
the old endpoint target difference is the fixed vector `q`.  A fixed-`q`
fibre therefore cannot intertwine the old target under the transverse
generator.  The next sidecar is a frame bundle varying `q`, together with
ancestry and exact-address Bezout/physical-section compatibility.

THM-3243 rules out one tempting shortcut to that sidecar.  The nonvertical
exceptional flags and `G_delta` both have `2,197` points, but their Heisenberg
orbit/stabilizer spectra differ.  Any bridge must therefore be a typed
representation-level map with a multiplicity/pointing coordinate, not an
equivariant bijection of the underlying finite sets.

THM-3250 now constructs exactly the charged linear bridge.  For
every `kappa!=0`, its two-stage Fourier map is

```text
T_kappa(E_(s,t)^kappa)=F_(s-kappa^(-1)t,t)^kappa,
```

an `H_p`-intertwiner that becomes unitary after normalization.  The neutral
spectra remain different: `G_delta` has `p` copies of only the characters
`chi_(a,0)`, whereas the regular flag orbit has every `chi_(a,b)` once.  A
charged block is `pi_kappa tensor K^p`; its frame ambiguity is `GL_p`, and a
pointed coefficient matrix `A` is cyclic exactly when `det(A)!=0`.

THM-3252 supplies that determinant for one precise cross-thread object: the
zero-sum compactification of THM-3246's signed owner-Hodge word.  It is
nonsingular in all Singer cyclic gauges, and one rational signed packet is
cyclic on all twelve charged blocks simultaneously.  Thus gauge-uniform
charged rank is closed for this word; converting it into a positive,
factor-coloured endpoint current is the exact remaining type change.

This closes the representation-level bridge, but does not supply a physical
clutch.  Its charged-space unitarity supplies no positive point-mass
kernel, and no Singer/AGL equivariance or canonical source/target current has
been shown to pass the determinant gate.  Neutral data, target semantics,
ancestry and the all-`91`-unit mask remain outside it.

### 3.3 Tournament operation to sequence compiler

There are now four sharply different tournament regimes.

| operation | lawful state | exact computational form | recurrence boundary |
|---|---|---|---|
| repeated order join | fixed positive-colour jet `Q_A(1),...,Q_A(k)` | fixed polynomial-exponential formula; triangular-order C-finite tail | loses SCC order; only fixed extreme depth |
| cyclic substitution | complete moving finite-difference jet plus `W_Q` | `O(N^2)` arithmetic and `O(N)` storage at fixed output depth | factorial-cube growth rules out C-finite; endpoint range grows |
| uniform transitive blow-up | base determinant/saddle and the subsequence at `mr` | exact variable substitution, radius scaling, hypergeometric decimation | inherits the base radius wall; does not compute the seed operator |
| equal Q4 transitive blow-up | paired variables `Y,Z,C` plus the full walk numerator | one Stirling sum for `H`; all-depth finite differences; fixed-depth quasi-linear arithmetic | moving width; no P-recursiveness or radius conclusion |

For `P^[m]=P[T_m,...,T_m]`, the exact laws are

```text
D_(P^[m])(X)=D_P(Y),
Y_i=product_j(1+X_ij)-1,

c_(i,j)=c_i/m,
R_(P^[m])=R_P^m/m^(bm),

a_(P^[m],d)(r)
 =(((mr)!)/(r!)^m)^b a_(P,d)(mr).
```

The exponential coordinate `X=e^x-1` is the reason the block operation
becomes addition: `Y_i=e^(sum_j x_ij)-1`.  This is an operation-level closed
form, not a fitted recurrence.

The order-six observer twins show exactly why a smaller scalar summary is
unsafe.  They share the complete path-colour profile, the polynomial
`t^6+16t^4+28t^2`, Perron balance, and product radius `(log(2)/2)^6`, yet their
uniform transitive-lift Hamiltonian counts already differ at block size two.
Their determinant symmetry groups have orders `48` and `6`.  The consumer
needs multivariate cycle placement, represented by `W_Q` or a proved
response-equivalent state.

For the Q4 lane, THM-3248 supplies that state in one special quotient:

```text
Y=(1+X1)(1+X4)-1,  Z=(1+X2)(1+X3)-1,  C=X2X3,
D=1-CY,            N=Y+Z+YZ+2CY.
```

The denominator alone gives `1` at block size one, while the true Hamiltonian
count is `5`; the numerator is load-bearing.  The paired observer symmetry is
the Klein four group on `{1,4}|{2,3}`, whereas the labelled tournament
automorphism group is trivial.  This is controlled forgetting for this
consumer, not a generic tournament quotient.

### 3.4 Factorial transfer to efficient P-recursive evaluation

[THM-3186](../01-canon/theorems/THM-3186-full-exterior-continuant-path-convolution-and-cancellation-wall.md)
provides the complementary sequence compiler.  The state dimension is fixed,
but the transfer coefficients vary polynomially with time.  A normalized
continuant satisfies an order-two recurrence and first-order differential
OGF; the selected visibility satisfies a cleared order-three recurrence.
Both are computable in linear arithmetic time for fixed parameters.

The signed exit-time convolution is load bearing.  Two nonzero unit paths can
cancel exactly, so a graph of possible paths or local Smith data cannot be
collapsed to Boolean reachability.  This is the factorial analogue of the
tournament observer twins: state compression must be judged by the future
consumer, not by agreement on one scalar readout.

### 3.5 Pointed divisor shadow to a lawful external carrier

THM-3245 makes the projected LRC quotient exact without turning it into a
group action.  For `g=gcd(z,L)` and `f=L/g`, the map

```text
D |-> q=D/f
```

identifies the pointed interval `[f,L]` with `Div(g)`, preserving meet, join,
valuation `l1` distance and graph medians.  The natural internal operations

```text
J_a(q)=lcm(a,q)
```

are commuting idempotents; every map from their band to a group is trivial.
External `S3` actions exist on an abstract Boolean divisor cube, but they need
a new carrier map and do not inherit LRC coefficients, owners or ancestry.

The `z_1=219` sorted residual checkpoint sharpens the information invoice:

```text
preserved:   top saturation, sorted valuation positions and their weights;
destroyed:   supplier labels, intersections and restoration order;
hostile:     the unweighted triangle-plus-leaf has C2 symmetry, but its
             weighted automorphism group is trivial;
needed map:  labelled states with external bijections S,R preserving the
             physical predicate, coefficient/phase, owner and ancestry.
```

At the next open layer, common `g=2` on all `119` rows is ambient atlas
metadata only.  THM-3251 is a reserved empty namespace, not a descent result.
Thus the exact next task remains the direct `z_1=218` screen; a modular-action
story cannot replace it.

## 4. A mechanism bridge: exceptional squares and escaping pairs

The factorial PRS wall and the planar Jacobian infinity wall are not the same
object.  They do, however, instantiate the same local procedure.

1. Keep the transverse parameter before specializing to the wall.
2. Compute enough leading layers to determine their common exceptional
   multiplicity.
3. Divide by the exceptional factor.
4. Read the first nonzero strict-transform coefficient.
5. Use a reciprocal coordinate when the disappearing degree represents roots
   at infinity.

For THM-3231, two layers cancel universally and division by the pivot square
exposes a fourth osculating Toeplitz minor.  THM-3233's proved normal-cone
tower suggests how to continue after that division: factor the common series
and iterate on the surviving quotient tail.  In the THM-3237 theorem family,
the top two resultant coefficients vanish at `t=2/Gamma`, the next coefficient
is a nonzero multiple of

```text
kappa_inf=4*v*v14-3*v15^2,
```

and the reciprocal initial form is quadratic.  Consequently two roots, not
one, escape with `w^2` proportional to the transverse parameter.

```text
transferable mechanism: exceptional multiplicity -> strict transform;
not transferred:         PRS row, Keller condition, root labels, or theorem;
shared invariant:        first nonzero normal coefficient after division;
cheapest hostile:        force the next coefficient to vanish as well;
next sidecar:             branchwise cofactor unit / selected reciprocal root.
```

This bridge changes the Jacobian search order.  Before expanding an entire
resultant, compute only its top reciprocal jet and look for the first degree
of `B` that can enter that jet.  In the stated family, degrees at most eight
cannot change the face and degree nine is the first live boundary term.

## 5. Bold reframes and their verdicts

| reframe | cheapest decisive test | verdict | survivor |
|---|---|---|---|
| `168+1=169`, so the LRC owner orbit is the four-jet carrier | Singer plus one transvection; pointed degree bound | **partly true** abstractly | common 169-set, but full affine mixing and mobile completion point |
| every residual has `g=3,q=3`, so the LRC layer carries a `C3` symmetry | inspect occupied quotient support and internal restorations | **REFUTED** | the top of ambient chain `{1,3}` is saturated; restorations form an idempotent band |
| the sorted-position triangle is a supplier `S3` carrier | restore weights and inspect the checkpoint's label map | **REFUTED** | weighted symmetry is trivial and sorting has discarded supplier labels |
| twelve negative owners plus zero form an affine line | exhaust Singer gauges / residues mod `14` | **REFUTED FOR ALL DILATIONS** | THM-3246 fixes the `156+ / 12-` word and nonlinearity; THM-3252 shows its zero-sum completion is nevertheless charged-cyclic in all gauges |
| the old additive target grid should carry `H_13` | inspect stabilizer and central kernel | **REFUTED**, then repaired | carrier imbalance supplies the nonnormal stabilizer clutch |
| all charged central modes should already imply a full carrier | test the thirteen center-neutral transverse characters | **REFUTED as an implication, then completed** | THM-3247 supplies all thirteen scalar blocks and rank `169` |
| full coefficient-carrier rank should imply a physical LRC current | track sign, endpoint target, ancestry and Bezout section through the frame map | **REFUTED** | THM-3247 and THM-3252 close endpoint and signed-owner rank; frame-bundle physical naturality is the exact remaining invoice |
| blowing up the delayed-contact point should carry the whole affine action | apply the universal lifting criterion to a translation | **REFUTED**, then repaired globally | one centre keeps its stabilizer; the full rational centre orbit restores `AGL_2` and splits into `169+2,197` Heisenberg strata |
| the `2,197` nonvertical flags are the `2,197` address carrier | compare Heisenberg stabilizer/orbit spectra | **REFUTED as sets; repaired after charge localization** | THM-3250 gives explicit charged-sector Fourier isomorphisms; neutral spectra still differ |
| a root-free trace should detect first contact | split examples with cancelling branches | **REFUTED** | etale norm preserves simultaneous nonvanishing |
| a primitive norm or full contact-field generator should be Singer | compare exact multiplicative order | **REFUTED by THM-3241 hostile** | retain the prime-divisor resultant order gates |
| a vanishing PRS row has no further information | divide by the wall parameter | **REFUTED** | nonzero square-normalized osculating row |
| affine `B` exhausts the planar Jacobian source family | degree filtration of top resultant coefficients | **REFUTED as a search boundary** | nonlinear degree nine changes the infinity face |
| path-colour profile plus radius identifies tournament continuation | order-six exhaustive pair and transitive lifts | **REFUTED** | retain determinant/walk placement |
| the Q4 denominator/owner pair computes its blowup sequence | compare denominator-only and full walk diagonals at `r=1` | **REFUTED** (`1` versus `5`) | retain `N=Y+Z+YZ+2CY`; paired Stirling contraction survives |
| a depth-nine reset dual should extend unchanged | evaluate the first complete depth-ten layer | **REFUTED**, then stitched | 19 positive depth-ten hostiles; THM-3238's eight overlap rows repair the complete bank |
| a globally exposed reset supplies an `H`-increasing one-pole deletion flow | enumerate exact one-pole maxima | **REFUTED, THEN REPAIRED WITH A SELECTOR** | THM-3244 proves 32 `H`-local maxima; rows 2/10 give a state-dependent atlas, no fixed positive blend of them works, and radius-10 routing repairs the single-height route coarsely |
| every efficient sequence formula should be C-finite | compare fixed join jet, moving cyclic jet, and time-varying transfer | **REFUTED** | operation-specific C-finite, P-recursive, decimation, or moving-jet forms |

The recurring lesson is not “keep all information.”  It is “name the next
consumer, then retain the smallest state proved sufficient for that
consumer.”

## 6. Frontier-specific next tests

### LRC(14): make the cyclic coefficient current physical and target-preserving

The abstract group computation and coefficient rank are now complete:
THM-3247 proves the canonical endpoint current fills the entire `169`-state
module.  THM-3252 separately proves that the exact signed owner-Hodge word
passes every charged multiplicity determinant in all `8,064` Singer gauges
and yields a `2,028`-dimensional signed packet.  The decisive test is therefore
physical naturality before marginalization, not another abstract rank scout.

1. Replace the fixed-`q` chart by the frame bundle `(q,u)` with
   `det(q,u)=1`, so the endpoint target `q` transforms rather than remaining
   frozen while the address target changes.
2. Lift `(A)` to a factor-coloured `A_tilde(a,t,k,l)` on `G_full`, keeping
   source/target factor labels and common ancestry.
3. Audit the three `H_13` generators at the atomic-factor level, including
   exact-address Bezout-section changes, the all-`91`-unit mask and target
   survival.
4. Use THM-2782/2806 and the fixed cell-90 carry values as hostiles: central
   marginalization or a residue-blind section must fail visibly.
5. Use THM-3252's owner matrix as the positive rank control, then form the
   twelve THM-3250 matrices `A_kappa(s,t)` of the factor-coloured physical
   current.  The exact gate remains `det(A_kappa)!=0` in every nonzero sector,
   but failure must now be attributed to the physical lift rather than the
   owner word.  THM-3253 is only a **RESERVED / UNPROVED EMPTY STUB** for the
   positive owner-mass Newton version of this test; it supplies no result yet.
6. If those gates pass, solve the pointed equations `A_kappa U_kappa=B_kappa`
   against a target-side current and audit positivity, neutral data, target
   and ancestry.  Only after physical frame covariance survives should the
   Singer conjugates be tested; Singer does not preserve the polarization.
7. Zero-extend a certified THM-3247 current to the vertical exceptional flags,
   add the one Singer generator on all `2,366` flags, and compute its exact
   affine orbit rank.  In parallel compare Singer-transported `J_q` with the
   current rebuilt in the transformed `(q,u)` frame.  This tests coefficient
   covariance; target and ancestry remain separate obligations.

This route does not replace the direct LRC obligations: finite heads, other
cells, arbitrary `k<=1`, the rung, physical entry, and the remaining reflected
ledger all stay open.  On the projected `k=3` lane the exact next finite task
is the `119`-row layer `z_1=218`, with current ledger `373411`; `z_1=217` is
already preclosed for the subsequent contiguous descent.  THM-3245 proves the
common `g=2` there is only ambient metadata; THM-3251 is reserved, so neither
changes the cap nor substitutes for the direct screen.  A supplier-action
side lane must rerun the labelled stage rather than infer roles from sorting.

### Planar Jacobian: turn an infinity bifurcation into a Keller gate

The degree-nine wall is a local source-family result.  Its most valuable next
test is whether the square-root pair survives the Keller equations.

1. Impose the full Jacobian/cofactor constraints on the `B_t` deformation
   rather than treating `P` as an unconstrained source polynomial.
2. Compute the reciprocal Newton polygon and branchwise cofactor units at
   `t=2/Gamma`.
3. Determine whether the escaping pair gives a genuine odd infinity inertia
   component or is cancelled/identified by the second coordinate.
4. Repeat with the first live `C_0` and nonlinear `E_0` terms to learn whether
   degree nine is a stable face threshold or only a slice artifact.

If the pair survives with unit cofactors, it supplies exactly the sort of odd
component demanded by the current `S4` Keller lane.  Without that chart entry,
it is not evidence for or against `JC(2)`.

### Factorial conjecture in three variables: stitch, do not rescan

The exact recurrence-row atlas now reaches `r=2498`; the first unaudited row remains
`r=2499,d=2501`.  THM-3238 has now glued the complete `4,319`-state physical
bank for one support/bank face, while THM-3222 supplies `230` local collars
and THM-3231 preserves the normal direction at a simple PRS wall.  Audited
THM-3244 gives both the exact failure of the single-height one-pole route and
two repairs on this bank: a lawful state-dependent rows-(2,10) selector at
one-pole scale, and a sharp coarse radius-10/two-hub `H` route.

The highest-value tests are:

1. transport the twenty-two-row overlap cocircuit to the other `229` faces,
   first testing whether the eight nonprincipal transition rows have a
   face-independent construction;
2. transport THM-3244's 31 covering row pairs to the other faces, carrying the
   direction masks and switch-depth invariant as sidecars; test whether one
   pair is face-stable, retain its state-dependent selector because every fixed
   positive rows-(2,10) blend is obstructed, and test the full 22-row cone only
   if the next consumer truly needs one scalar gauge;
3. use the strict-transform row at a resonance as the transition state across
   a reset wall, and audit whether the proved twice-Pell extracted-factor clock and
   quotient-tail tower remain compatible with the physical pole labels;
4. use the THM-3232 norm only as a nonvanishing gate, retaining a selected
   branch/carry sidecar whenever the recurrence consumes additive data;
5. continue `r=2499,d=2501` in parallel, because finite closure
   and structural stitching are independent evidence lanes; and
6. use THM-3241's exact separation of the three `p=13` gates:
   irreducibility, contact-algebra generation, and multiplicative order `168`.
   Its order-24 hostile shows why norm or squarefree spectrum cannot replace
   the order-resultant tests.  Separately, compare two deformation arcs with
   the same THM-3243 exceptional tangent but different second normal lifts;
   disagreement proves that projective direction is not the next contact jet;
   and
7. treat the proved degree-28 sixth PRS wall as a fixed-offset arithmetic
   atlas, not as an all-depth selector: its integer nonvanishing and Pell
   content still need a lawful stitch to the full exterior/physical state.

### Tournaments: classify operators, not only invariants

1. Enumerate determinant-owner groups `Gamma_Q` and continuation twins at
   order six and seven.  Ask which multivariate cycle placements split every
   response class.
2. Treat uniform transitive blow-up as a solved renormalization operation.
   It inherits the Q4 radius; it cannot prove its transcendence.
3. Use THM-3248's paired variables to attack the Q4 product radius through its
   two-log arithmetic, while retaining the walk numerator; the compiler alone
   implies no fixed recurrence.
4. Classify which strong quotients admit a bounded paired-owner
   factorization of both `D_Q` and `N_Q`.  The generic response-equivalent
   compression problem remains open.
5. Keep `H>=disc` on its intrinsic strong-core lane.  None of the carrier or
   sequence analogies changes that inequality.

### Efficient sequence computation: choose the compiler first

Given a new sequence arising from an operation, ask in this order:

1. Does the operation multiply a polynomial transform?  Use fixed evaluations
   and finite differences; fixed extreme jets may be C-finite.
2. Does a block substitution linearize under `X=e^x-1`?  Use block sums,
   saddle renormalization, and exact hypergeometric decimation.
3. Does the full walk numerator share a bounded owner factorization with the
   denominator?  Use paired finite-difference/Stirling rows, but keep the
   numerator and certify state-width growth.
4. Can the quotient revisit a factor?  Expect a moving endpoint jet; seek a
   banded kernel and an `O(N^2)` evaluator rather than a fixed recurrence.
5. Is there a bounded time-varying transfer?  Eliminate the state to a
   polynomial-coefficient recurrence and retain signed path sums.
6. Before fitting anything, pre-register the next unused term and run a
   same-summary/different-continuation hostile.

This is a practical closed-form hierarchy.  “Closed form” may mean a fixed
polynomial-exponential sum, a D-finite differential equation, a
hypergeometric decimation, or a moving finite sum with proved complexity.
Demanding one syntax for all these regimes destroys useful structure.

## 7. Portfolio after this synthesis

```text
ANCHOR:   LRC(14) factor-coloured address current whose twelve charged
          multiplicity matrices pass the determinant and physical sidecars.
NICHE:    paired-owner factorizations beyond Q4 and the minimal full-walk
          response state for tournament substitution.
WILDCARD: reciprocal strict-transform walls linking planar Jacobian infinity
          escape to factorial reset transition cocircuits.
```

The wildcard has already paid: it found the degree-nine infinity threshold
and the paired square-root escape.  The niche has paid: it produced an exact
blow-up/decimation law, the Q4 Stirling compiler, and sharp numerator/observer
hostiles.  The anchor has paid algebraically: the current is cyclic, and
THM-3250 supplies the charged linear bridge.  It stays the anchor
because positivity, neutral data and a target-preserving physical map are
still missing.

The next session should begin from these three precise objects, not from the
numerical coincidences that suggested them:

```text
LRC:       audited THM-3250 plus twelve physical address-current determinants;
JC:        a Keller-constrained reciprocal two-root wall;
FC(3):     transport of the reset cocircuit plus an audited coarse routing atlas;
tournament:paired-owner classification beyond the special Q4 operator;
sequence:  compiler complexity with explicit response-width and bit costs.
```

## 8. Procedural beach residue: precise niche problems worth retaining

The searches that did not become the three promoted theorems still produced
specific, cheaply testable objects.  They are **NON-CANONICAL SCOUTS** until
separately packaged.

1. **SCC Euler product with order loss.**  For order join, the candidate
   identity `1+W_(R▷S)=(1+W_R)(1+W_S)` turns fixed path-cover depth into a
   rank-at-most-`d+1` convolution kernel.  It forgets SCC order, so it is a
   compiler sidecar, not a tournament reconstruction theorem.  The decisive
   next test is a same-factor-multiset/different-SCC-order continuation pair.
2. **Duality blindness.**  The determinant and scalar walk observer satisfy
   the scouted symmetry `D_(Q^op)=D_Q` and `W_(Q^op)=W_Q`; endpoint-resolved
   resolvents distinguish a source-over-cycle from its opposite.  This is a
   clean example of a one-coordinate sidecar restoring destroyed direction.
3. **Mixed transitive sections.**  Blowups
   `T_m[T_(r+1)^s,T_r^(m-s)]=T_(mr+s)` realize every residue section.
   All `m` response lanes reconstruct the sequence, while one decimation lane
   cannot.  The niche question is an iff theorem for P-recursiveness under the
   complete section vector, with one-lane hostiles kept explicit.
4. **Contact/address carrier bridge.**  After a basis, target-axis and Bezout
   section are declared, THM-3241's contact deformation plane and a fixed
   THM-3240 address fibre have the identical standard `H_13` action.  The
   contact Singer transports to `(a,delta)->(a+4delta,2a+delta)`, but does not
   normalize the fixed center line; joint closure costs the full affine group.
   THM-3243 realizes that closure only after blowing up all rational centres:
   its `2,197` nonvertical flags form one regular orbit, not the thirteen
   affine orbits of the address carrier.  THM-3250 nevertheless
   identifies every charged sector by an explicit Fourier map and reduces a
   pointed current to a `13`-by-`13` determinant gate.  THM-3252 clears that
   gate for the zero-sum signed owner-Hodge word in all `8,064` cyclic gauges
   and combines the blocks into one `2,028`-dimensional signed packet.  Neutral
   characters, positivity, endpoint provenance, Singer covariance and physical
   ancestry remain missing.
   Contact zero is delayed contact, not empty physical support.
5. **Direction scaling is a full mode bank.**  The dual-field all-direction
   scout found `28,392/28,392` nonzero two-dimensional modes per field, so
   translations alone make every `J_q` cyclic.  But for `q->2q`, every one of
   the `169` physical-frequency ratios is distinct for every `q`; no direction
   admits a constant or affine-character gauge.  All `2,366` complete
   twelve-step generator-cycle products telescope to one.  Propagation exists,
   but its response state is the full Fourier bank rather than a scalar
   covariance law.
6. **Exposed reset versus local flow.**  THM-3238 proves a global singleton.
   Hostile-audited THM-3244 finds `32` one-pole `H`-local maxima and a long
   merge-tree obstruction, refuting a deletion/Morse reading.  The full lawful
   bank nevertheless has 31 covering row pairs; rows 2/10 route every state at
   one-pole scale, with switch histogram `716/3,600/2`, while two sharp trap
   intervals rule out every fixed positive blend of that pair.  A separate
   radius-`10` two-hub atlas repairs the single-height route coarsely.  The next
   task is cross-face transport of the selector and direction sidecar, plus a
   wider 22-row gauge test—not another search for `H`-monotonicity on this bank.

These scouts all instantiate the same procedure: compile the operation,
measure response-state width, exhibit the smallest controlled forgetting,
and invoice exactly the coordinate needed by the next consumer.
