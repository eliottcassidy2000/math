# The certificate is a ladder and the relaxation smears: per-set finiteness beats universal averaging, again

klein-2026-07-01-S88 (HYP-3841/3842/3843)

## One day, one lesson, three costumes

Yesterday the swarm learned "a moment can average too" (HYP-3822): the global moment LP
cannot see the atom. Today the same lesson wore two new costumes and produced its
positive counterpart.

**The negative costume (HYP-3842):** relax the covering adversary to a fractional speed
profile and the arc x radius LP -- radius-coupled, overlap-cut, any W -- has value zero
at every radius. Not because the geometry is wrong but because FRACTIONAL SMEARING IS
AVERAGING: thirteen units of speed-mass spread over two hundred speeds imitate a uniform
measure, and a uniform measure covers everything from r = 1/26 on. Integrality -- the fact
that a runner is one speed, concentrating its whole budget on v fractions -- IS the
covering floor's binding structure. Every universal relaxation that forgets it dies the
same death: Fourier (transform-averaging), global moments (congestion-averaging), now
LP relaxations (profile-smearing). The sequence is pleasingly exhaustive.

**The positive costume (HYP-3841):** keep the set INTEGRAL and the certificate FINITE.
The lonely distribution function is piecewise linear; between anchor and target it can
only underperform its tangent by the exposed concave defect K. So three numbers --
anchor value, anchor slope, defect budget -- certify M >= 1/14 from below the critical
radius, which is exactly where covering sets (whose shallow witnesses are all dead --
they contain a multiple of 14) can be anchored. One anchor certifies the generic
covering set; the deep-well family, whose far element throws the one big concave kink,
needs two rungs. Eleven of eleven. And the defect concentrates precisely where the
covering-min analysis already located the danger zone -- the instrument rediscovers the
deep well from pure kink data.

**The identity costume (HYP-3843):** on the final window (1/15, 1/14] the two tight sets
are not merely tied in slope -- they are THE SAME FUNCTION. The census's last mile is
single-valued. The curvature question ("does the tie break at second order?") has the
sharpest possible answer: it breaks at no order inside the window; it breaks AT the
window's edge, r* = 1/15, the q+q' = 15 Farey layer. Meanwhile the mean loneliness
E separates the pair at n = 14 AND n = 20 (GW wins both times). Two invariants, exactly
complementary blindness -- and the pair (c, E) keeps looking like a complete invariant
of the tight locus.

## The shape of the endgame this suggests

The working pattern across the whole frontier is now unmistakable: every universal/
averaged instrument returns zero; every per-instance finite certificate works and its
failures localize to a named finite family. The peel (opus HYP-3900) closes j <= 6
outliers with a scale-free constant; the ladder closes covering sets from two anchors;
the unit-residue rigidity pins tight sets to residue data; the census closes what
remains. The proof of LRC(14), when it assembles, will not be an inequality that holds
everywhere -- it will be a FINITE ATLAS of certificates, each chart carrying three or
four numbers, with the deep well and the tight pair as the only charts needing bespoke
data. The lonely envelope is the atlas's index: who carries the minimum, radius by
radius; and its last chart, we now know, contains a single function.

## Swarm note

This session also exercised the collision protocol at speed: opus ceded 3834/3835
mid-day and self-renumbered to a 3900+ block; kps took 3950+; mac-mini claimed the
owner's directive as HYP-3850(a)-(e); I re-scoped to the disjoint instruments (sieve,
LP, final window) within an hour of the overlap appearing. Per-machine blocks + honest
stubs + first-committer cession worked with zero arbitration. The swarm's namespace
discipline is becoming as load-bearing as its mathematics.
