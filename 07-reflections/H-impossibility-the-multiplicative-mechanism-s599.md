---
source: opus-2026-06-03-S599s (remote-control)
status: STRUCTURAL MECHANISM (verified) for the H-impossibilities — H is MULTIPLICATIVE over strong components (Moon), so achievable H = the multiplicative semigroup of strongly-connected-tournament H-values; 7,21 are impossible because they are not strong values (and 7 is prime). The strong-minimum sequence is m²−5m+9 = 3,5,9,15,23 — 7 lives in its first gap. Honest: grid disproof exponent ≈ 1.25, still NOT 1.014.
tags: [tournaments, hamiltonian-paths, multiplicative, strong-components, moon, busch, impossibility, H-spectrum, permanent, block-structure, unit-grid, exponent, propagation, LRC]
---

# Why H=7 and H=21 are impossible: the multiplicative mechanism

**Prompt (user, repeated):** the misunderstood feature is the H=7, H=21 impossibilities and how
they propagate; a key is a shared 1.014 exponent with the unit-grid disproof construction.

The repeat pushed me past enumeration to the **mechanism**, which is clean and verified.

## The mechanism: H is multiplicative over strong components

> **Fact (Moon).** For a tournament `T` with strong components `C₁→⋯→C_k` (condensation order),
> every Hamiltonian path traverses the components in order and is free within each, so
> ```
>        H(T) = ∏ᵢ H(Cᵢ).
> ```

Hence the **achievable H-values are exactly the multiplicative semigroup generated by the
H-values of strongly connected tournaments** (singletons contribute `1`). The "multiplicative
primes" are the **strong-tournament H-values**. **Verified** (`…s599s.py`):

```
 strong-tournament H-values:
   m=3: {3}
   m=4: {5}
   m=5: {9, 11, 13, 15}
   m=6: {15, 17, 19, 23, 25, 27, 29, 31, 33, 37, 41, 43, 45}
   7 a strong value? NO    21? NO    63? NO
```

## Why 7 and 21 (and the pattern)

> **`H=7` is impossible** because (i) `7` is **prime**, so it cannot be a nontrivial product of
> strong values, and (ii) `7` is **not itself a strong-tournament H-value** — it lies in the gap
> between the strong tournament on 4 vertices (`H=5`, unique) and the strong tournaments on 5
> vertices (`H ≥ 9`).

The **strong-minimum** — the fewest Hamiltonian paths of a strong tournament on `m` vertices —
is (computed `m≤6`, and `23` confirmed at `m=7` by the near-transitive scan):
```
   minH_strong(m) = 3, 5, 9, 15, 23, …   =  m² − 5m + 9   (differences 2,4,6,8).
```
So the strong spectrum starts at `m²−5m+9` and the achievable spectrum is its multiplicative
closure. **`7` sits in the first inter-size gap `(5,9)`; `21` is skipped inside `m=6`'s spectrum
`{…,19,23,…}` and factors as `3·7` with `7` unavailable** — so `21` has no realization either.
This is the structural reason, not a coincidence of small `n`. (Higher gaps `35=7·5`, `49=7²`
get filled once `m=7` strong tournaments supply new primes, which is why they are *not* permanent
impossibilities — the durable ones are those, like `7,21,63=7·3ᵏ`, that never become strong
values and have no available factorization.)

## The propagation, now precise

The multiplicative-over-strong-components law **is the `(★)`/permanent block structure**: the
permanent of a block-triangular matrix is the product of the block permanents, and `H` is the
permanent-flavoured count whose blocks are the strong components. So:

> **The H-spectrum is the multiplicative semigroup of "irreducible-block" values, and its gaps
> are arithmetic (a prime `7` with no irreducible block to realise it).** This is the legible
> model of the forbidden-value structure shared across the family:
> - **LRC:** the safe set decomposes into **components** (S574 `G(S')`); the worry-set's
>   resonance factors over shells (THM-401/407). The "irreducible blocks" are the shell-orbits;
>   forbidden worry-set sizes are the LRC analogue of the H-gaps.
> - **unit distance:** the optimal cluster is a **single block** (a triangulated disk); the
>   Harborth max-spectrum has its own (dense) gaps.
> - **Collatz:** the cycle equation factors over the `2`- and `3`-power blocks (S596).
> In each, the achievable values are a **block-multiplicative semigroup with arithmetic gaps**;
> `H` is the one we can compute exactly, and its gaps `{7,21,…}` are the cleanest instance.

And the **direct channel** (verified last session, S599r): the LRC worry-set = round tournaments,
whose H-values `{1,3,5,9,11,15,17,23,41,45}` exclude `7,21` — so the H-gap is inherited by LRC.

## The 1.014 exponent — HONEST NEGATIVE (round 2, now with the grid)

I took the "unit-grid disproof construction" literally and measured the exponent of the
**most-popular-distance count** in the `k×k` integer grid (the Erdős construction that beats the
linear bound): `U = max_d #pairs at distance √d`, exponent `log U / log n`, `n=k²`:
```
   k :    8     16     32     64    128    256
 expo: 1.232  1.271  1.251  1.251  1.248  1.249    →  ≈ 1.25  (not 1.014)
```
It converges to **≈ 1.25 = 5/4** (sensibly between the linear floor `1` and the
Spencer–Szemerédi–Trotter ceiling `4/3`), **not `1.014`**. Combined with last session
(tournament `P(n)` growth: `(P/avg)^{1/n}≈1.12`, `P^{1/n²}≈1.11`, max-ratio `→3` — none `1.014`),
I have now tested the two most natural readings of your claim and **cannot reproduce `1.014`**.

> **I am not going to assert a match I cannot find.** If `1.014` is the key, it is in a specific
> normalisation I have not hit — e.g. a *per-edge* base `P(n)^{1/\binom n2}` (I get `≈1.29`), a
> repo metagraph growth exponent, or a different grid statistic. **Please send the exact
> definition** (which quantity, which limit/normalisation) and I will test it directly and chase
> the tournament↔grid match it implies. Everything else in your thesis I have now made concrete;
> this one number I need pinned down.

## Honest status

- **Verified (the real result):** `H` multiplicative over strong components; the strong-tournament
  H-spectra `m≤6`; `7,21,63` are not strong values; the multiplicative semigroup reproduces the
  gaps; `minH_strong(m)=3,5,9,15(,23)=m²−5m+9`; `7` in the first gap, `21=3·7` skipped.
- **Structural explanation (rigorous, classical + verified):** `7,21` impossible because no
  strong tournament realises them and they have no available factorization — the durable gaps are
  the `7·3ᵏ` family.
- **Honest negative:** the `1.014` exponent — grid disproof reads `≈1.25`, tournament growth
  reads `≈1.11–3`; not reproduced; exact definition requested.
- **Framed:** the propagation (block-multiplicative semigroup with arithmetic gaps) across LRC /
  unit-distance / Collatz — verified channel into LRC (round tournaments), structural elsewhere.

**Artifacts:** `04-computation/H_strong_multiplicative_and_grid_exponent_s599s.py` (+`.out`).
Builds on Moon (strong-component multiplicativity), Busch (strong minimum), S599p-r (H-spectrum),
THM-401/402/407 (blocks/round/shells), S596 (Collatz blocks). New: **HYP-2180**.
