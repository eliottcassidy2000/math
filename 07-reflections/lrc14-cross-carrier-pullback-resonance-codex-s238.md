# LRC14 Cross-Carrier Pullback Resonance - S238

The useful new angle is not another decisive analogy.  It is a portfolio audit.

I encoded `22` carrier pullbacks from the CPI/HYP stack and scored them against
`9` remaining proof obligations.  The core alphabet is duodecimal:

```text
status, route, exact_scale, topology, owner, period_deck,
analytic_certificate, automaton_partial_cube, crt_padic,
observer_cut, hodge_cycle, formal_exit
```

The finite result is sharply anti-scalar: the first portfolio that covers all
obligation axes has size `9`.  No small universal bundle appears.  Individual
obligations often have compact two- or three-carrier covers, but those covers
are different.  That suggests the proof should be glued from local portfolios
through a formal exit ledger rather than squeezed through one quotient.

Two proof angles came out.

First, a blindness-pair theorem.  Every carrier that forgets a load-bearing
coordinate must be paired with the carrier that restores it.  `mu^2/phi` is
the clean example: it is useful as a squarefree capacity meter, but it destroys
prime-power, owner, period, and route data, so it must be paired with
Ramanujan primitive decks or CRT/p-adic trees before it touches the q=23
petal/covering residual.  The same pattern applies to automata, observer cuts,
owner strips, and geometry-regime guards.

Second, a resonance-portfolio theorem.  Work obligation by obligation.  The
top recurring triples combine safe-stalk/barcode geometry, observer or
rectangle/hourglass cut payloads, and exact Farey scale.  That links the old
A000568 perspective defect, S217 rectangle residues, Moser/fibbinary partial
cubes, and multi-scale tournament spectra through the same controlled-
forgetting shape: local owner geometry plus observer extension plus binding
scale.

Incoming-mainline signal: the verified Lean moment-dual step in
`07-reflections/lrc14-lean-moment-dual-audit-codex-20260626.md` should be read
as a concrete `formal_exit` row for this ledger.  It preserves the miss-count
predicate and the p0-to-moment bridge, but it destroys endpoint owner and exact
lonely-time location until a finite rational dual certificate and event bridge
restore enough payload for Part A.  This matches the blindness-pair rule more
than it contradicts it.

Assumption challenge: I considered runners, gaps, route labels, proof
obligations, CPI carrier rows, hidden coordinates, endpoint-owner strips,
primitive decks, analytic clocks, automaton states, partial-cube Theta classes,
CRT roots, observer cuts, rectangle/hourglass residues, Hodge cochains,
median-center pages, state-lift sectors, and formal exits.  The chosen
tournament vertices were proof-carrier pullbacks.  This preserves payload
coverage and destroyed-coordinate accountability, but destroys actual HYP-2963
packet identity until the next emitter writes concrete packet rows.

Tournament Analysis is transitive under the payload-retention gauge:

```text
score_hist = {0:1, 1:1, ..., 21:1}
directed_3cycles = 0
scc_sizes = 22 singleton SCCs
hamiltonian_path_count = 1
```

The next concrete job is to emit actual HYP-2963 packet rows with
`carrier_pullback_row_id`, `core_incident_word`, `preserved_lrc_predicate`,
`destroyed_coordinate`, `required_sidecar`, `blindness_pair_id`,
`resonance_portfolio_id`, `status_mixing_result`, `route_mixing_result`, and
`legal_exit_status`.  Then test whether the listed portfolios actually make
the residual coarse fibers status-pure and route-pure.
