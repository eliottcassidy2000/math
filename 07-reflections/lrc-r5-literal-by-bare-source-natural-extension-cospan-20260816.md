# The BY endpoint has a lawful depth-six pullback, but only as a cross-word support cospan

**Status: FINITE-EXACT VERIFIED IN NORMAL AND OPTIMIZED MODES; independent
typing audit pending.**  The literal BY endpoint variable can be placed on
the THM-2471 ancestry stalk without imposing `v=y`.  The canonical map is the
already-proved bare-source node

```text
v=Y_(q,e')(y)=(y+q+13e')/13^6.
```

At the fixed endpoint address `(alpha,beta)=(1,0)`, inserting the literal
`U_full` BY support at this node before transfer gives a nonzero tensor on
the retained `(ell,c)` chart and all thirteen guard shifts.  This is a lawful
measurable **support** pullback.  It is not yet a same-speed-word LRC packet,
does not insert the BY character, leaves AX external, and proves no current,
row exclusion, or LRC(14) result.

## 1. Inheritance and correction ledger

The relevant canon distinguishes three progressively stronger statements.

1. THM-2471 gives a finite Boolean stalk on one outer base, with linked nodes
   `w_u`, `X_(u,a)`, `Z_(u,a,b)`, and `Y_(q,e')`.  MISTAKE-293 requires saying
   “one ancestry fibre,” not “one circle point.”  Its bare endpoint factor is
   already evaluated at `Y_(q,e')` before the sheet sum.
2. THM-2555 classifies the natural extension exactly: a depth-`L` endpoint is
   `(z+n)/13^L`, and the sheet address is indispensable whenever the pulled
   observable is not future-base measurable.  This rules out a bare `v=y`
   replacement and identifies the coordinate that must be retained.
3. THM-2538 proves that separately integrated endpoint banks determine only
   margins.  A temporal/source coupling must be multiplied on a common base
   before integration.  MISTAKE-281 and MISTAKE-283 impose the same rule on
   Fourier factors and on objects sharing only row or word labels.

The other listed ancestors sharpen the scope rather than supplying another
map.  THM-2449 requires its complete lawful source/deep/word factor list at
the typed clock.  THM-2512 transposes the signed ANOVA interaction of such a
table but creates no Boolean current.  THM-2594 realizes a nonzero auxiliary
linked-node contraction and, after MISTAKE-295, does not attribute the signal
uniquely to affine slaving.  THM-3479 forms `phase*AX*BY` only after separate
endpoint integrations.  THM-3514 and THM-3515 retain abstract regular
`F_13` torsors and complete endpoint spectra, but explicitly have no support
predicate on the THM-2471 stalk.  MISTAKE-313 finally requires comparing the
entire factor constructor: equal labels or output hashes cannot replace a
missing clock or guard.

## 2. The exact map is `v=Y`, not `v=y`

At the `r=5` window, `d=13^5`.  THM-2471's bare-source inverse branch is

```text
w_q=(y+q)/13,
Y_(q,e')=(w_q+e')/d
        =(y+q+13e')/13^6,       q in F_13, e' in Z/13^5 Z.   (1)
```

Thus

```text
T^6 Y_(q,e')=y,
n_R=q+13e' in Z/13^6 Z,                                  (2)
```

and `(q,e') -> n_R` is the complete depth-six branch address.  For each
fixed branch `(q,e')`, equation (1) is a natural-extension section; summing
the branches is exactly the Perron sheet average already present in the
THM-2471 Boolean expansion.  No new choice of torsor origin is made.

The endpoint-leg assignment is forced by the inherited formulas.  In the
THM-3479 endpoint engine, AX is built from the marked intersection
`E(x) 1_Q(T^2x)`, whereas BY is built from `E(v)` alone.  In THM-2471's
Boolean expansion the marked/current leg is exactly
`1_Q(X_(u,a))1_E(Z_(u,a,b))`, while the bare leg is `1_E(Y_(q,e'))`.
Consequently the type-preserving dictionary is

```text
AX marked endpoint  <-> (Z_(u,a,b),X_(u,a)),
BY bare endpoint    <-> Y_(q,e').                         (2a)
```

Putting BY at `Z`, `X`, or the outer base `y` would change its endpoint role,
not merely choose a different coordinate chart.

Let `E_src,nu` be the canonical `r=5` bare-source event restricted to its
39-atom label `nu`, and let `E_BY(alpha,beta),nu` be the literal half-open
`U_full` BY endpoint support with only the central guard removed, restricted
to the same geometric atom.  The inserted right-leg predicate is

```text
1_(E_src,nu)(Y_(q,e'))
1_(E_BY(alpha,beta),nu)(Y_(q,e')).                     (3)
```

Equation (3) is formed before `P_(13^5)`, root extraction, multiplication by
the source-refined current, seven-cell integration, and the common-offset
marginal.  It is therefore a genuine common-stalk support product.  The
central guard is restored afterwards by its exact atomwise value
`safe(nu,tau)`; the left guard is retained simultaneously on the left atom.

The connection contract is:

| field | exact content |
|---|---|
| source | THM-2471 outer ancestry fibre `(y,q,e')` with the canonical bare-source node `Y_(q,e')` |
| target | literal no-central-guard `U_full` BY endpoint support at address `(alpha,beta)` and atom `nu` |
| map | pull the endpoint predicate back by `(y,q,e')->Y_(q,e')`, then perform the inherited sheet sum |
| preserved | source endpoint event and atom, BY endpoint atom, `(alpha,beta)`, every guard shift `tau`, `ell`, common offset `c`, and the depth-six branch sum |
| destroyed | the individual `e'` label after transfer, BY character phase, endpoint order/sign, and the distinction between sheets having equal marginals |
| required next sidecar | retain `n_R=q+13e'` through the BY character transfer and pair it with the marked AX branch and a same-speed-word theorem |

## 3. Exact common-grid certificate

The source and endpoint half-open interval engines have grids

```text
D_src=297836897838480,
D_BY =483730250419703196.
```

Their least common grid is

```text
D=744944585646342921840
 =2501183 D_src
 =1540 D_BY.                                           (4)
```

Replacing the BY observer by the identity on each source endpoint atom and
running the generalized depth-six transfer reconstructs the pinned source
tensor entrywise, multiplied by the grid scale `2501183`.  This is stronger
than matching a total mass or a hash: all `39*39*7*13=138411` coordinates
agree.

Across all `169` `(alpha,beta)` shifts, the audit intersects `7,107,008`
literal target intervals.  The exact endpoint-level diagonal census is

```text
3244/6591 atom-address intersections nonempty;
112 atom-address pairs have both marginals positive but empty diagonal.  (5)
```

The second line is the concrete transportation hostile promised by
THM-2538: positive separate endpoint masses do not imply same-point support.
The complete address ledger has digest

```text
d450c50c66242664c2c13076c4dbd775793d3a191ce9a143218880574edfecad.
```

## 4. The selected one-leg support tensor

At `(alpha,beta)=(1,0)`, the identity and diagonal tensors have exact ledgers

```text
identity:  support 5150/138411,
           mass 32130005012548873716037676925000;

v=Y:       support 4885/138411,
           mass 16609023891511992906074761046460.       (6)
```

The natural diagonal digest is

```text
196e4388b03e28b43b0291e9a4206af9621596905ea98922108e328193f93bb0.
```

Restoring both atomwise guards for every `tau` gives all
`13*7*13=1183` retained `(tau,ell,c)` coordinates, with digest

```text
0c96ca52529bb2c9373f42939d7cb20eb8d14ca75a1c30ebfe2b230613cf8954.
```

After Fourier transformation in `tau`, every one of the thirteen tables has
shape

```text
(total,DC,F_7 axis,F_13 axis,mixed)=(91,1,6,12,72).     (7)
```

Equation (7) is only a finite-field spectrum of the support-weighted tensor.
It does not insert the BY endpoint character and is not a THM-2512 input.

## 5. Hostiles identify what the map contributes

Two exact hostiles are decisive.

1. **Fiat `v=y`.**  Evaluating the same endpoint support directly at the
   outer base rather than at `Y_(q,e')` leaves only `668/138411` supported
   tensor entries and mass
   `76498321376065665451323508560`.  It disagrees with the lawful pullback in
   `1131/1183` guarded coordinates.
2. **Delete the right guard.**  Keeping the natural-extension map but dropping
   only the right guard changes `1030/1183` coordinates.

Nevertheless both hostile banks still have shape `(91,1,6,12,72)` in every
`tau`-Fourier fibre.  This independently repeats the lesson of the one-leg
Fubini audit: complete spectral support is compatible with a wrong point map
or a missing guard.  The map and factor list, not the spectrum, certify the
typing.

## 6. Why this is not yet the physical relation current

The pullback in (1) closes the variable-level question but exposes the next
two type debts.

First, the two speed words are

```text
W_src  =(1,14,27,40,53,66,13,2197,742586),
W_full =(1,183,27,131,53,313,13,2197,742586).          (8)
```

They agree in six roles and differ exactly in roles `1,3,5`.  A natural
extension acts on a fixed physical word; it does not replace those three
runner factors.  Therefore (3) is a lawful measurable **cross-word
observer** on the source stalk, not yet proof that the `U_full` BY factor is
the bare endpoint factor of the canonical `r=5` row.  The minimal semantic
repair is either:

```text
an explicit same-tuple/word transporter preserving all nine endpoint
factors, guards, addresses, and the selected row,

or a reconstruction of the endpoint factor directly for W_src.          (9)
```

Second, support is weaker than the BY functional.  The next exact right-leg
object must retain

```text
n_R=q+13e' in Z/13^6 Z
```

through the cyclotomic BY character at `Y_(q,e')`.  The marked AX leg then
has its own lawful THM-2471 node

```text
Z_(u,a,b)=(y+u+13a+13^6 b)/13^8,
X_(u,a)=T^2 Z_(u,a,b),                                  (10)
```

so its minimal address is

```text
n_L=u+13a+13^6 b in Z/13^8 Z.
```

The cheapest complete cospan is consequently

```text
(y,u,q,a,b,e'; ell,c; alpha,beta,tau,t)
  -> (Z_(u,a,b), X_(u,a), Y_(q,e'))                     (11)
```

with both endpoint characters and the grouped exact relation address applied
before any branch, atom, or base marginal.  Equation (11), plus (9), is the
minimal missing coordinate package.  Chronological arrival and a physical
current would still need to be proved after that static construction.

There is therefore no THM-2449 lawful response table, generic THM-2512
bridge, grouped coefficient `C(a;X,m)`, physical current, scalar-row
exclusion, or LRC(14) conclusion here.  The LRC(14) ledger remains `165`.

## 7. Reproduction

Run

```text
python -B 04-computation/lrc_r5_literal_by_bare_source_natural_extension_support_probe_20260816.py
python -B -O 04-computation/lrc_r5_literal_by_bare_source_natural_extension_support_probe_20260816.py
```

Both executions reproduce

```text
05-knowledge/results/lrc_r5_literal_by_bare_source_natural_extension_support_probe_20260816.out
```

byte-for-byte.  The pinned semantic SHA-256 is

```text
cce1850a9b287f4b612c6724bfc63441febc78b2223b19d6d72e6989b2590500.
```
