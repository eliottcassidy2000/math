---
source: codex-2026-07-25-lrc-word-current-parity
status: >
  PROVED + VERIFIED-EXACT; THE CANONICAL XOR/PARITY ATLAS IS
  INDEPENDENTLY HOSTILE-AUDITED.  The exact terminal-word handoff is a
  row-wise group-algebra spectral-overlap problem for the TOTAL full-set
  and TOTAL word derivative currents.  A common source/pulled-target comb
  pair contributes only in one XOR parity, and mismatched separately
  assigned Leibniz terms cancel.  A literal-clock u=2 interval model gives
  complementary physical spectra in both rows, disproving any universal
  incidence argument based only on inclusion, grade, residue permutation,
  or a unit-coloured frequency difference.  The hostile is a sharp local
  word control, not a canonical THM-2305 Boolean stratum or an LRC
  counterexample.  No scalar profile is excluded and LRC(14) remains open.
depends_on:
  - THM-2304-deepest-boundary-cyclotomic-current-separation
  - THM-2305-canonical-blocker-word-handoff-hypergraph
related:
  - THM-2302-same-label-expiration-dichotomy-and-pure-terminal-shell-no-go
  - THM-2312-sparse-root-bispectrum-positive-word-current
  - THM-2323-primitive-fixed-colour-cross-correlation-and-same-gauge-word-alignment
  - THM-2337-expiration-word-residue-invisibility-and-first-bockstein-sidecar
  - THM-2344-correlation-inverse-rigidity-and-aligned-tooth-twist-hostile
  - THM-2356-finite-field-chirp-gram-tomography-and-bockstein-pairing
  - lrc14-fixed-colour-long-row-capture-and-gauge-thickness-opus-20260725
  - lrc14-planar-chirp-quotient-resolution-ladder-opus-20260725
  - lrc14-two-frequency-orientation-current-leakage-opus-20260725
  - lrc14-literal-handoff-affine-unit-bispectrum-opus-20260724
script: 04-computation/lrc14_word_current_parity_and_u2_hostile_probe.py
output: 05-knowledge/results/lrc14_word_current_parity_and_u2_hostile_probe.out
script_sha256: cf85b6e734e48372363e23189899c4a0a0336930ed5b29dd879bcfe31b1d867b
output_sha256: b4cc8c2fe5af84a67aff309084f84dcff85925274ef50b75ffdedbc4d57b4251
hash_basis: LF-normalized working-tree bytes
---

# Terminal-word incidence is a cyclic zero-divisor problem

> **CURRENT SCOPE UPDATE.**  Promoted THM-2349 (`4389031a2`) supplies a
> positive literal word and marked primitive-`91` deepest edge on every
> repeated `(1,1,c)` row.  Consequently the current/inverse-character
> boundary discussed here now applies to all `165` rows; repeated-row
> marked incidence is no longer a separate obligation.

> **QUADRATIC PHASE-RETRIEVAL UPDATE.**  Promoted THM-2355
> (`d6a7cb80c`) repairs the phase sidecar exactly.  Singleton component
> energies together with all complete pair-union energies decide whether
> a grouped current vanishes.  More economically, singleton magnitudes and
> lawful `p>=3` pair-twist energies along a spanning tree recover every
> relative complex phase.  The singleton calibration is load-bearing:
> `(-1,2,-1)` and `(-2,1,-2)` have the same two path-edge twist responses
> but different totals.  Whole target-twist energy alone remembers only
> residue autocorrelation, and a real full-support `C_13` CAZAC shows that
> full residue support does not repair the loss.  Thus the short-row target
> below is now precise: realize lawful calibrated pair probes on the
> terminal-component graph, or intertwine root and target phases inside an
> acute cone.  More unsigned energy or bare support cannot suffice.

> **LOCAL SHARPENING.** The audited packet
> `lrc14-quadratic-phase-tree-minimal-calibration-opus-20260725` shows that
> a connected twist graph needs only one singleton calibration, and a
> connected nonbipartite graph needs none.  One global cyclic twist bank
> suffices when its unique-difference graph is connected; in `C_13` this
> holds for every two-support, every non-AP three-support, and `364/715`
> four-supports, but never at support at least five.  The separate audited
> packet `lrc14-owner-side-acute-current-multiplier-seven-opus-20260725`
> proves that every nonempty oriented/addressed scalar owner-side cell has
> nonzero current through multiplier seven.  Hence in the semi-hard
> primitive cases `4,5,6`, vanishing requires exact balance of both owner
> sides.  The
> cheapest scalar target is now a private one-sided deepest address; the
> broader target is a lawful difference-Sidon tree.

> **PLANAR-CHIRP SCOPE REPAIR.** PROVED THM-2356 (promoted at
> `59c933aae`) passes independent sign, normalization, diagonal, graph,
> and `F_169` audits. More importantly, PROVED THM-2337 already supplies its
> coefficient-level input: after choosing identifications of the target
> and first-jet planes with `F_169`, the joint array partitions into the
> resulting `169` planar graphs
> `z=q^2/2+c`, and linear target/jet characters restrict to quadratic
> chirps. Their intensities are exact derived quadratic functionals, not
> independently supplied physical pair probes. The new audited
> quotient-resolution packet
> `lrc14-planar-chirp-quotient-resolution-ladder-opus-20260725` proves that
> one projective chirp line gives exact `13`-edge derivative-line sums,
> while all `14` directions recover every off-diagonal edge. Cross-graph
> polarization further reduces the hostile to a common target atom.
> Same-frequency graph polarization cannot distinguish that atom at
> `q=0` from one at `q!=0`, whereas cross-frequency interference can.
> Failure of every one-sparse nonzero-target landing is exactly the formal
> vertical tensor `A(q,z)=delta_0(q)B(z)`.  The Parseval detector
> `D_c=169^-1 sum_b|F_c(b)-F_c(0)|^2` is positive exactly when graph `c`
> has a nonzero-target coefficient.  The sharp debt is therefore one
> lawful graph-channel pair twist/phase ratio, one aligned singleton
> energy, or one phase-sensitive cross-frequency target anchor, not
> construction of a coefficient-level planar grouping.

> **FOLLOW-UP LONG/SHORT SPLIT.** The fixed-colour thickness packet
> `lrc14-fixed-colour-long-row-capture-and-gauge-thickness-opus-20260725`
> now proves that, under the explicit total post-transfer comb
> hypotheses, every row with `u>=J_QJ_E` has the required overlap.  Its
> shifted `u=2` hostile satisfies THM-2323 and XOR alignment but still
> misses the prescribed diagonal.  Thus this note's zero-divisor audit
> is now confined to short rows; post-summation idempotents cannot repair
> them.

## 1. The corrected physical object

The previous boundary-current analysis retained exact owner, clock,
orientation, relative address, and prime-to-thirteen residue information.
The remaining handoff was phrased too strongly when it asked for a whole
word-current unit.  The actual question is only whether the full-set and
word currents meet at one physical character.

Fix the prime-to-thirteen residue order `u` and the two physical frequency
rows

```text
n_(0,k)=A'+k13^b,

n_(1,k)=A+k13^b,                     0<=k<u.        (1)
```

Let `E` be the full source set and let `E_Q` be one exact pulled terminal
word.  Use the complete derivative currents

```text
f_j(k)=sum_(x in Jump(E)) Delta E(x) exp(-2pi i n_(j,k)x),

w_j(k)=sum_(x in Jump(E_Q)) Delta E_Q(x)
                         exp(-2pi i n_(j,k)x).       (2)
```

The sums in (2) include every coincident and target-only pulled-word bank.
They are not one arbitrarily assigned provenance summand.

Over a characteristic-zero splitting field, there are unique elements

```text
F_j,W_j in K[X]/(X^u-1)                             (3)
```

with

```text
F_j(zeta_u^(-k))=f_j(k),

W_j(zeta_u^(-k))=w_j(k).                            (4)
```

Fourier evaluation identifies the quotient in (3) with the product of
its `u` character fields.  Therefore the exact handoff criterion is

```text
F_0W_0!=0  or  F_1W_1!=0

iff

some physical row and residue index carry both currents.             (5)
```

Put `N=X^u-1`.  Since `N` is squarefree,

```text
F_jW_j=0 mod N

iff

lcm(gcd(F_j,N),gcd(W_j,N))=N.                       (6)
```

Thus

```text
Res(W_j,N)!=0                                      (7)
```

is a sufficient unit test, but it is stronger than necessary.  A unit
claim for one bank summand is weaker in a different direction: other
word-bank currents can cancel it before the total `W_j` in (2) is formed.

## 2. The local XOR theorem

Let a source and its `K`-clock pulled copy contain the same comb bank
`c`.  At a common endpoint write

```text
x_0=(14r+epsilon)/(14c),             epsilon in {+1,-1}.   (8)
```

Encode safe by `0` and danger by `1`.  If `q_alpha` is the source bit and
`q_delta` the pulled target bit, then on a punctured neighborhood of
`x_0`,

```text
q_alpha(x) q_delta(T^Kx)
 =
 q_alpha(x)      if delta=K+alpha mod 2,

 0               otherwise.                         (9)
```

Indeed, for `x=x_0+t`,

```text
q_alpha(x)=1_[(-1)^alpha epsilon t>0],

q_delta(T^Kx)=1_[(-1)^(K+delta) epsilon t>0],       (10)
```

because

```text
13^K=(-1)^K mod 14.                                (11)
```

The same orientation statement holds for the guard bank because
`13=-1 mod 7`.  Strict/open endpoint conventions change only the value
at `x_0`, not the one-sided or distributional jump.

Equation (9) is already true before multiplication by either physical
row phase.  Every shifted-frequency address map `pi_k` merely permutes
the endpoint labels, so the parity filter commutes with the complete
tomography bank.

## 3. Why separate provenance gives the wrong answer

The parity law belongs to the **combined source/pulled-target collision
packet**, not to separately named right-Leibniz terms.

For a safe source put

```text
sigma=(-1)^(K+delta),

S_+=(1+epsilon)/2,             S_-=(1-epsilon)/2,

q_+=(1+sigma epsilon)/2,

Delta S=epsilon,              Delta q=sigma epsilon.    (12)
```

The two right-sided pieces need not vanish separately:

```text
q_+ Delta S=(epsilon+sigma)/2,

S_- Delta q=(sigma epsilon-sigma)/2.                (13)
```

Only their sum has the XOR behavior:

```text
Delta(Sq)=epsilon(1+sigma)/2.                        (14)
```

In a mismatched branch, a nonzero source term can be cancelled exactly
by the pulled-target term.  Grouping the common bank pair first is
therefore load-bearing.

Also,

```text
boundary(D_c) subset boundary(D_(13^Kc)).           (15)
```

There are

```text
2c common boundaries,

2c(13^K-1) target-only boundaries.                  (16)
```

The second population has no source partner and must remain in the total
word current `W_j`.

## 4. Canonical THM-2305 parity atlas

Apply (9) to the positive terminal words of THM-2305.  The following are
the possible source-boundary supports of the paired collision packet;
other masks can still kill them.

For even `K`:

```text
pure {a}:
  guard, five unit banks, and the absent blocker b;

pure {b}:
  guard, five unit banks, and the absent blocker a;

fork {a,b}:
  guard and five unit banks only.                   (17)
```

For odd `K`:

```text
pure {a}:
  source owner j and target blocker a;

pure {b}:
  source owner j and target blocker b;

fork {a,b}:
  all three blocker banks.                          (18)
```

Repeated copies of the same bank at clocks `K_l` survive only if every
bit satisfies its own XOR equation

```text
delta_l=K_l+alpha mod 2.                            (19)
```

Different banks can mask or collide, but cannot resurrect a mismatched
common pair.

The fork split in (17)--(18) is structurally useful.  It says exactly
which boundary family must carry a future conductor-separation argument;
it does not assert that one of those possible supports has a nonzero
total current.

## 5. A minimal two-character hostile

The complementarity in (6) is not cosmetic.  Take

```text
lambda=1,                   b=3,

K=lambda+1=2,

c_*=2*13^5=742586,

m=170,                      gcd(m,91)=1,

A'=13,

A=A'+m c_*=126239633.                             (20)
```

For `u=2`, the two rows (1) use

```text
k=0:
  13, 126239633;

k=1:
  2210, 126241830=57123*2210.                      (21)
```

Let `I` be a centered interval of length `3/4420`, let `J` be the
concentric subinterval of length `1/2210`, and put

```text
E=I disjoint-union (I+1/2),

Q=T^2J.                                             (22)
```

Because

```text
|I|<1/169
```

and the nearest wrong half-shifted preimage is at distance `1/338`, the
literal pullback is exact:

```text
E intersect T^(-2)Q=J.                             (23)
```

At `k=0`, both frequencies in (21) are odd.  The half-period pair in
`E` cancels, while the `J` coefficient is nonzero:

```text
E_hat=0,                         J_hat!=0.           (24)
```

At `k=1`, both frequencies are integer multiples of `2210`, so

```text
J_hat=0.                                             (25)
```

Their products with `|I|=3/4420` are half-integers, so

```text
E_hat!=0.                                            (26)
```

All frequencies are nonzero, hence the same zero pattern holds for the
derivative currents.  In both physical rows,

```text
F_j proportional to 1-X,

W_j proportional to 1+X,

F_jW_j=1-X^2=0 mod X^2-1.                           (27)
```

The target length is

```text
|Q|=169/2210=13/170<1/7,                            (28)
```

and at center zero it lies inside one `D_1` danger arc.  Thus the model
retains a literal prescribed clock, a one-target-danger local word, the
grade-one rows, the unit-coloured difference, and the `u=2` residue swap.

Its boundary is equally important:

```text
it is not a complete THM-2305 Boolean stratum;

it is not a global zero-safe scalar cover;

it is not an LRC(14) counterexample.                 (29)
```

It proves that inclusion, grade, a unit-coloured pair, and address
tomography do not by themselves force (5).

## 6. What THM-2323 already supplies, and what remains

The right replacement for universal word-unitness is factorwise spectral
overlap before all banks are summed.  This factorwise step is no longer
an open proposal: THM-2323 proves fixed primitive-colour
cross-correlation and same-gauge bare/word alignment under its
one-arc/comb hypotheses, with a finite endpoint-Vandermonde lift.

Let `G` be one finite endpoint-address group, let `c(g)` be the complete
endpoint current of an XOR-aligned paired bank, and let `r(g)` be the
remaining zero-one word mask.  Parseval gives

```text
sum_(chi in Ghat)
  c_hat(chi) conjugate((rc)_hat(chi))

 =|G| sum_(g in G) r(g)|c(g)|^2.                   (30)
```

If the aligned masked packet contains an endpoint, the right side is
positive.  Hence some character sees both the full factor and its masked
word factor.  This is the elementary core of the stronger fixed-colour
mechanism already canonized in THM-2323.

The new XOR atlas and the `u=2` hostile locate the strictly later
obligation: the aligned factor sits inside a **sum** of pulled-bank
currents, and its common character may be cancelled in the total
`W_j`.  Three obligations therefore remain:

```text
1. land the common character in a unit-coloured shell;

2. retain the target-only banks from (16);

3. use conductor separation or an exact-order idempotent to prevent
   cancellation by every other pulled bank.                         (31)
```

The strengthened THM-2344 exposes an orthogonal downstream boundary:
even an arbitrary nonconstant transported word on one active axis stays
on a single inverse-character line, and every one of its `169` twists
can have the same full amplitude.  Thus neither THM-2323, generic word
nonconstancy, nor target-side reflection positivity solves the
total-current cancellation problem.  A successful twist needs two
independently translated base coordinates or cross-axis word/base
mixing **and** a coordinate which separates collision multiplicity.
The audited pending THM-2353 handoff shows that the first condition
alone can still leave all `169` deep twists constant: two distinct left
atoms occupy one target residue while the right side sees only their
aggregate.  A nonvertical Bockstein/ancestry or acute phase-cone
difference sidecar is therefore load-bearing.

THM-2352 adds that no fixed finite prefix complex can certify the
eventual absence of such collision taxes: the indexed `q`-adic
abscissa spectrum is dense at every positive level in every finite
cylinder.  Uniform descent therefore also needs an infinite-depth end
marker or radix clock.

The next finite audit should therefore be:

```text
for each canonical word and physical row,
sum the TOTAL current first;

factor gcd(F_j,N) and gcd(W_j,N);

test (6) in each exact-order idempotent;

trace any complementary factor back to the paired-bank atlas
(17)--(18).                                          (32)
```

THM-2323 eliminates complementarity inside one aligned fixed-colour
factor and, on a physical block of length `J_QJ_E`, now closes the total
row outright.  On the remaining short rows, conductor separation must
act **before summation** and prove that a private factor cannot be
cancelled in the whole `W_j`; only then can a genuinely two-coordinate,
collision-separated target twist face the inverse-character obstruction
of THM-2344 and the collision-multiplicity obstruction of the pending
THM-2353 packet.

## 7. Information ledger

```text
source:
  THM-2305's exact word, THM-2304's derivative currents, and the affine
  shifted-frequency bank;

target:
  two total-current elements in the semisimple cyclic group algebra;

preserved:
  physical row, residue index, exact clock, word, owner, bank parity,
  target-only boundaries, and unit-coloured frequency difference;

destroyed by a single resultant:
  which factor of X^u-1 carries the full current;

destroyed by separate provenance:
  cancellation at common boundaries and cancellation among pulled banks;

strongest positive survivor:
  the exact lcm criterion (6), canonical XOR atlas (17)--(18), and
  factorwise Parseval overlap (30);

strongest hostile:
  the literal-clock complementary pair (27);

next decisive target:
  canonical total-current gcd/lcm enumeration followed by exact-order
  conductor separation on one XOR-aligned paired bank.               (33)
```

No LRC profile decrement follows yet.

## 8. Exact reproduction

Run

```text
C:\Users\Eliott\.cache\codex-runtimes\codex-primary-runtime\
dependencies\python\python.exe
  04-computation/lrc14_word_current_parity_and_u2_hostile_probe.py
```

and repeat with `-O`.  The companion exhausts the safe/danger XOR truth
table, checks the repaired Leibniz sum, verifies all arithmetic in
(20)--(28), checks the literal-pullback clearances, and multiplies the two
group-algebra factors modulo `X^2-1`.

Stored transcript:

```text
05-knowledge/results/lrc14_word_current_parity_and_u2_hostile_probe.out
```

Normal, optimized, and stored transcripts match.  LF-byte SHA-256:

```text
script  cf85b6e734e48372363e23189899c4a0a0336930ed5b29dd879bcfe31b1d867b
output  b4cc8c2fe5af84a67aff309084f84dcff85925274ef50b75ffdedbc4d57b4251
```
