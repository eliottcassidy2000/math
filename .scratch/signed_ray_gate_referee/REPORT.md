# THM-2984 new-claims referee (2026-07-30)

Status: **mathematical audit passed; canonical ordinary/optimized replay,
direct Lean elaboration, and dedicated target build passed; root import added,
with the full root build blocked by an unrelated false rational-side lemma**.

Audited claims:

- gcd-stratum invariance and the partial-totient capacity formula;
- exact transporter obstruction and equality-boundary stabilizer coset;
- `Stab(B_d)=U(d)` for `2<=d<=14` and `{+-1}` for `d>=15`;
- every additive-order `2..7` pair gives universal unit escape;
- `beta(d)<=ceil(d/7)<=d/R` and independence of `B_d` in `G(d,7)`;
- the sharp arbitrary-translation capacity `kappa(d)=ceil(d/7)`, the `d=28`
  separation from centered `beta`, and the exact finite affine transporter;
- the exact affine pair criterion, identification of its two-element
  nonfaces with `E(G(d,7))`, and the flag/non-flag boundary `d=8/9`;
- the fixed-unit cyclic-gap formula
  `#{a:uS subset C_a}=sum_i max(0,Delta_i-(d-kappa))` for nonempty residue
  sets, with singleton gap `d` and duplicates discarded;
- the all-cardinality diagonal-unit/positive-composition normal form, and its
  prime projective-chain and six-ordering triple-ratio specialization;
- the `d=43`, `{1,2,15}` minimal nonface and non-flag transport complex;
- stabilizer-coset/projective-unit compression and the cone apex `0`.

No counterexample was found.  The proofs are uniform; the canonical script
`04-computation/lrc14_projected_k3_signed_ray_gate_thm2984.py` is a hostile
finite audit rather than a theorem dependency.  Run both modes from the
repository root:

```bash
PYTHONHASHSEED=0 python3 04-computation/lrc14_projected_k3_signed_ray_gate_thm2984.py
PYTHONHASHSEED=0 python3 -O 04-computation/lrc14_projected_k3_signed_ray_gate_thm2984.py
```

Ordinary and optimized modes are byte-identical to the canonical transcript
`05-knowledge/results/lrc14_projected_k3_signed_ray_gate_thm2984.out`.  It has
26 lines, 1072 bytes, output SHA-256
`9e33756dd352042ba6a4571a0af1af626e70e0991e94624c0dfe18711a229dbc`,
and semantic SHA-256
`756ae7ed291fc438648ed76f57b07eae1cde4a1879b184f9564b6ca8adc94f48`.
The canonical source hash is
`291818c42929dabf3b261ca91604d5f0fdfa16a210faf51e19bcac71a38a1b41`.

Two rejected pre-canonical transcripts preserve the audit lineage.  The older
`check_thm2984_new_claims.pre-repair.out` had semantic SHA-256
`d6e2a94dbd5d51a1132be8ea6ba77d9e693d30f118a42214ef52cd4b366a10fb`;
its optimized run exposed that Python `-O` erased load-bearing `assert`
statements.  A later full ordinary run was rejected before freezing because a
finite-gap table locally shadowed the top-level formula-row variable, causing
the advertised modulus count to print `6`.  Renaming that local variable to
`unit_rows` repaired the profile.  Neither transcript reached canon, so no
`MISTAKES.md` entry is needed.

The pre-promotion audit also found that the equality-coset loop checks the
first `min(8,phi(d))` units at each modulus, whereas its initial comment said
"every unit preimage."  The mathematics is uniform, but the finite evidence
must advertise its actual sampled universe.  The code comment, semantic
profile and canonical transcript now state that scope exactly.

Exact corrections made during review:

1. At `|S|=|B_d|`, the transporter is **equal** to
   `Stab(B_d)u_0`, not merely contained in it.
2. `E(G(d,7))` lies in the **nonedge** graph of the transport complex's
   one-skeleton; it is not nested in the compatibility graph.  The inclusion
   is strict at `d=15`, `S={1,2}`.
3. The face poset retains only nonemptiness of transporter masks.  The masks
   themselves are strictly richer and are required after restriction to
   `U_active`.
4. `K_d` is the cone `{0}*K_d^x`, hence contractible.  Its nonzero decone is
   the meaningful topology/flag object.
5. A Python verifier intended for ordinary/optimized replay must not place
   load-bearing checks in `assert`; `-O` removes them.  All checks now pass
   through the explicit `require` function.
6. A finite control over selected representatives must not be described as
   exhaustive over all representatives.  The equality-coset control is
   explicitly the first `min(8,phi(d))` units for each `2<=d<=500`.

Additional uniform observation: `d=43` is the first non-flag modulus.  For
`b=floor((d-1)/14)<=1` this is immediate.  For `b=2`, deconed facets are
`{+-a,+-2a}`.  When `d` is even the two antipodal classes occupy different gcd
strata, so every clique lies in its cross-edge facet.  When `d` is odd the
projective compatibility graph consists of cycles generated by `[2]`; a
triangle would force `8=+-1 (mod d)`, hence `d|7` or `d|9`, impossible for
`29<=d<=42`.  The displayed `d=43` triple supplies the first failure.

For the affine complex, translation starts have an exact gap statistic.  The
complement of a `kappa`-block is a consecutive block of `d-kappa` residues.
If `Delta_i` is a positive cyclic step gap of the distinct residues of `uS`,
then its `Delta_i-1` unoccupied interior residues contain exactly
`max(0,Delta_i-(d-kappa))` such complement blocks.  Every avoiding complement
lies in one unique gap, proving the sum formula and the existence threshold
`max_i Delta_i>=d-kappa+1`.  The empty set is outside this formulation (it has
all `d` starts); a singleton has one full-turn gap `d` and therefore exactly
`kappa` starts.  The final full transcript records `2,044` affine
subset-equivalence checks, `9,237` cyclic-gap formula checks, and `2,225,000`
all-active-subset checks, including both integer endpoint directions.  These
and the `(13j)--(13l)` affine pair/flag controls pass in both Python modes.

The composition normal form also survives all boundary checks.  A singleton
uses the unique empty composition; a set larger than `kappa` has no positive
composition of the required length; and for pairs the one-coordinate law is
exactly the affine pair criterion.  At a prime modulus, equality of the two
projective vectors yields one common nonzero scalar, hence one common unit
across every adjacent difference.  For triples, the six orderings need not
give six distinct ratios; if `r` is one ordered ratio their multiset is drawn
from
`{r,1/r,-1-r,-1/(1+r),-r/(1+r),-(1+r)/r}`.  At `kappa=2` the positive ratio
set is empty, correctly making every triple a nonface.  The Farey language
should mean the modular image of the short positive composition triangle,
not an injective reduction of rational fractions.  Ordered prime
configurations more precisely occupy the type-`A` braid-arrangement
complement inside projective space: every consecutive subsum of the
difference chain is nonzero.  A targeted independent replay passed `13,587`
all-cardinality cases through `d=18` (including one above facet capacity) and
`2,947` exhaustive prime-triple cases for `p=5,7,11,13,17,23`.

## Complete affine flag classification (audited canonical candidate)

The composition and pair normal forms give a complete classification:

```text
K_d^tr is flag
  iff d is in {2,3,4,5,6,7,8,10,12,14,15}.
```

Equivalently it is nonflag for `d=9,11,13` and for every `d>=16`.
This is a uniform proof, not a finite-range conjecture.

Let `R=max{r:r|d,1<=r<=7}`.  By THM-2979,
`alpha(G(d,7))=d/R`, while `(13k)` says that independent sets of this graph
are cliques in the one-skeleton of `K_d^tr`.  Every face has at most
`kappa=ceil(d/7)` vertices.  Therefore

```text
d/R>kappa  implies K_d^tr is nonflag.                   (F1)
```

Writing `d=Rq`, equality is equivalent to
`(7-R)q<7`.  Keeping the condition that `R` is the largest divisor at most
seven gives exactly

```text
d/R=kappa
iff d is a multiple of 7, or
    d in {2,3,4,5,6,8,10,12,15,18,24,30,36}.           (F2)
```

This arithmetic was checked exactly through `d=5000`; `(F2)` itself follows
directly from the displayed inequality.

For `d<=7`, `kappa=1` and the complex is discrete.  For
`d=8,10,12,14`, `kappa=2`, and every compatibility edge has odd difference,
so the one-skeleton is bipartite; a complex with two-point facets and no
triangle is flag.  At `d=15`, compatibility means unit difference.  A clique
has at most three vertices because its residues modulo three are distinct.
After translating and multiplying by a unit, a triangle is `{0,1,t}` with
both `t` and `t-1` units.  CRT gives only `t in {2,8,14}`; these are the three
orderings of a unit-step three-term progression, hence facets.  Thus `d=15`
is flag.  At each of `d=9,11,13`, the set `{0,1,2}` is a three-clique while
`kappa=2`, so it is a nonface.  An all-subset audit over every `2<=d<=15`
checked `65,532` subsets and found exactly those three small witnesses.

Four isolated equality moduli and the two exceptional multiples of seven use
the following exact cyclic-gap table.  The listed `u` are the positive
representatives of `U(d)/{+-1}`.  Each entry is
`u:(positive cyclic gap word; maximum)` for `uS`; the negative representative
reverses the cyclic word and has the same maximum.  In every row all pair
orders in `S` exceed seven, while every displayed maximum is strictly below
the face threshold `d-kappa+1` from `(13n)`.

| `d` | `kappa` | clique `S` | threshold | exact `+/-u` gap rows |
|---:|---:|---|---:|---|
| 18 | 3 | `{0,1,5}` | 16 | `1:(1,4,13;13)`, `5:(5,2,11;11)`, `7:(7,10,1;10)` |
| 21 | 3 | `{0,1,5}` | 19 | `1:(1,4,16;16)`, `2:(2,8,11;11)`, `4:(4,16,1;16)`, `5:(4,1,16;16)`, `8:(8,11,2;11)`, `10:(8,2,11;11)` |
| 24 | 4 | `{0,1,10}` | 21 | `1:(1,9,14;14)`, `5:(2,3,19;19)`, `7:(7,15,2;15)`, `11:(11,3,10;11)` |
| 30 | 5 | `{0,1,8}` | 26 | `1:(1,7,22;22)`, `7:(7,19,4;19)`, `11:(11,17,2;17)`, `13:(13,1,16;16)` |
| 36 | 6 | `{0,1,11}` | 31 | `1:(1,10,25;25)`, `5:(5,14,17;17)`, `7:(5,2,29;29)`, `11:(11,2,23;23)`, `13:(13,22,1;22)`, `17:(7,10,19;19)` |
| 42 | 6 | `{0,1,10}` | 37 | `1:(1,9,32;32)`, `5:(5,3,34;34)`, `11:(11,15,16;16)`, `13:(4,9,29;29)`, `17:(2,15,25;25)`, `19:(19,3,20;20)` |

The table covers `d=18,24,30,36`, the `d=21` start of the multiple-seven
family, and its one exceptional construction at `d=42`.  The sign-paired
audit checked all `58` units, not merely the displayed representatives.

It remains to cover `d=7m` uniformly.  For `m>=4`, `m!=6`, define

```text
S_m=({0,1,...,m-1} minus {1}) union {m+1}.              (F3)
```

This has `m=kappa` vertices and is a one-skeleton clique.  Differences among
the old interval points lie between `1` and `m-1` and therefore have order
greater than seven.  The new differences are
`{2,3,...,m-1,m+1}`.  The first group has the same property, while

```text
ord_(7m)(m+1)=7m/gcd(m+1,7m)
              =7m if 7 does not divide m+1, and m otherwise,             (F4)
```

which is greater than seven in the stated range; `m=6` is exactly the failed
case.

The clique `(F3)` is not a facet.  Let `N_q(S)` count unordered pairs at
circular difference `+/-q`.  Directly,

```text
N_1(S_m)=m-3,                 N_2(S_m)=m-2.             (F5)
```

If `S_m={A+jv:0<=j<m}` for a unit `v`, an index separation `h` contributes
`m-h` pairs whenever `hv=+/-q`.  For fixed `q` there is at most one such
`1<=h<m`: two equal signs would force `d|(h-h')`, and opposite signs would
force `d|(h+h')`, impossible because `h+h'<=2m-2<7m`.  Thus `(F5)` forces

```text
3v=epsilon_1,                 2v=2 epsilon_2 mod 7m,
epsilon_1,epsilon_2 in {+1,-1}.                         (F6)
```

After multiplication by `2` and `3`, `(F6)` would make `7m` divide
`2 epsilon_1-6 epsilon_2`, one of `+/-4,+/-8`, impossible for `m>=4`.
So `(F3)` is a clique nonface.  The two excluded values are exactly the table
witnesses `d=21` (`m=3`) and `d=42` (`m=6`).  A symbolic hostile replay checked
all old and new orders, `(F4)`, `(F5)`, and all four sign contradictions for
every `4<=m<=1000`, `m!=6` (`996` moduli).

Together `(F1)--(F6)` prove the classification.  The exhaustive and symbolic
controls pass under ordinary and optimized Python with byte-identical output.

The root-imported canonical Lean module
`04-computation/lean/TournamentH7/TournamentH7/LRCSignedRayGate.lean`
combines the ray theorem with the analytic triangle lemma, generic cardinality
and invariant-stratum gates, exact bad-band cardinality, count-threshold
sharpness, strict translated-capacity arithmetic, all seven fixed-`R`
ceiling-equality laws, pair-count index extraction, and the signed congruence
elimination.  Direct elaboration passes.  Its first combined audit caught and
repaired three inherited scratch proof-script defects: two impossible sign
branches left implicit for `aesop`, and one omitted forwarding of the
`hsafe` hypothesis.  The repaired module has SHA-256
`6f3ab284fc1936ebc0b565bf812fcdc38e6940d17e93d607b802f80d6261bc91`.
Every `#print axioms` report is contained in
`{propext, Classical.choice, Quot.sound}`; no declaration uses `sorryAx` or
`native_decide`.  The root import has been added.  The full project build
currently fails in the unrelated tracked module
`LRCCoherentBlockerChronology.lean:130`: there `c,k,delta` are rational, and
`k<c`, `delta=1` does not imply `0<=c-k-1`.  The neighboring faithful address
lemma uses integers.  This blocker is not in the signed-ray dependency chain.
