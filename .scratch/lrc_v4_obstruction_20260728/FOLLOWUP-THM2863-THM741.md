# THM-2863 / THM-741 follow-up: exact composition audit

Status: **SYNTHESIS OF PROVED/FINITE-EXACT INPUTS; NO NEW ROW CLAIM.**
Read from `origin/main` at `f7ec46f16f`.  No canonical file was edited.

## Verdict

The two incoming results advance LRC(14) in different senses.

1. **THM-2863 is a sharp algebraic interface, not a row closure.**  Four
   consecutive 91-unit multiplier probes split the oriented endpoint scalar
   into its fixed and faithful character-three lines.  The charged line is
   canonically isomorphic, after a displayed normalization, to the
   character-three projection of THM-2851's carry derivative.  It pays the
   representation and endpoint-orientation invoice, but not simultaneous
   full-current survival, a lawful signed physical projection, E3 transport,
   or a deterministic carry section.
2. **The new THM-741 addendum is direct LRC progress.**  It closes the final
   six label-one flood roots and hence all `21/21` bodies

   ```text
   H union {a,b},     H={8,...,14},  1<=a<b<=7,
   ```

   uniformly over the remaining four speeds.  Equivalently, every
   thirteen-speed family containing `H` and at least two low labels is
   lonely.  This is one 21-body sub-stratum of the 2,002-body near-AP
   theorem.  Global THM-741 remains `CLAIMED`, not proved.

## What THM-2863 actually pays

For `Y=38,64,90,116`, the frequency-cleared endpoint numerators have the
exact two-node form

```text
C_m=alpha_L lambda_L^m-alpha_R lambda_R^m,
ord(lambda_L)=182,       ord(lambda_R)=14.
```

Every shifted Hankel minor is nonzero.  The signed weights, not merely the
unordered nodes, give

```text
Gal-orbit(C_m) = fixed line direct-sum character-3 line,
Pi_3(C_m)=13 L_m.
```

For THM-2851's oriented derivative

```text
d h=449(delta_1-delta_0),
```

THM-2863 proves the normalized equivariant map

```text
Pi_3(d h) -> Pi_3(C_m),
iota_m=13L_m/[449(omega^(-3)-1)].
```

This is dimension one.  It necessarily forgets the chosen point of a
thirteen-state torsor and cannot distinguish the split and nonsplit
169-state extension laws.

## Composition with the THM-2859 germ

There is a real but incomplete triangle:

```text
THM-2859 physical endpoint-address T^8 edge
              ?  \
              ?   \ one-sided endpoint character
              ?    \
THM-2851 ancestry derivative --Pi_3,iota_m--> THM-2863 endpoint line.
```

The bottom arrow is proved by THM-2863.  The upper object is the finite-exact
THM-2859 germ: the `+66h` step `2 -> 68` translates both endpoint masks by
`T^8=(0,8)` on twelve long-horn labels while preserving ancestry, all native
factors, carrier, and semantic value.

The missing vertical arrow is load-bearing.  THM-2859 proves that the
endpoint-address edge is not an allocation-q arrow; q7 has empty endpoint
mask, q0/q11 are not translates, and the physical source/target endpoint
phases are inverse.  Projecting to one character does not repair this typed
fibre-product failure.

Conditionally, if a lawful identification of physical endpoint `T^8` with
the ancestry carry were supplied, then

```text
Pi_3(449(delta_8-delta_0))
 =449(omega^(-24)-1)
 =449(omega^2-1) !=0,
```

so a unique normalized one-dimensional intertwiner would again exist.
This conditional calculation is not that identification.

Even a successful character-three clutch would leave two independent
THM-2859 invoices:

- the full Witt relation `L^13=T` on `C169`, including q/basepoint data;
- the missing degree-`(1,0)` base object in the punctured `V4` action.

A scalar character twist decorates existing arrows and cannot create either
the missing object or its empty-to-`delta_0` physical fibre.

## Why the THM-741 closure does not compose

The flood addendum works on literal good-set carriers.  With horizon
`W=2500`, it partitions four future speeds into:

- at least two tails, closed by the two largest exact root coverages plus
  two covariance caps;
- exactly one tail, reduced to `1,6,1,4,6,3` dangerous head triples and
  closed on their exact residual carriers;
- no tails, reduced to finite candidate banks and checked by independent
  nested subtraction and direct comb union.

This has no supplied map to the q/endpoint/current groupoid of THM-2859.
It therefore does not fill the Witt or `V4` obstruction.  Its rigorous
interaction is only counterexample pruning: the complete H-core stratum
need no longer be considered by any global LRC(14) search.

There is, however, a useful methodological agreement.  THM-741 succeeds
only after retaining the residual carrier `G_(b,S)`, just as THM-2863
retains signed Prony weights and THM-2859 retains the missing typed object.
Root-coverage sums, unordered nodes, and scalar grade sums are all lossy
shadows; none is a substitute for the object on which the next operation
must act.

## Best next decisive experiment

Run a **typed T8--Prony fibre-product audit** before attempting another
abstract clutch.

1. Use the twelve labelled THM-2859 step-`2 -> 68` occurrences (one physical
   edge) and the four THM-2863 multiplier probes `Y=38,64,90,116`.
2. At both ends of every probe retain, simultaneously:

   ```text
   physical interval/root; q allocation; E3 and all native factors;
   source/target carrier; both endpoint masks; literal ancestry labels.
   ```

3. First compute the literal fibre product with the q3/q11 probe rows.  If
   it is empty, report the first lost coordinate and a minimal witness; do
   not compute a scalar Prony shadow.
4. If it is nonempty, frequency-clear the four coefficients on that one
   full current, verify the Prony determinant, split `L_m,R_m`, and compare
   the physical `T^8` difference with `Pi_3(delta_8-delta_0)` separately on
   source and target orientations.
5. Require a noncancelling signed output that remains E3/q co-supported.

This is decisive because success constructs the missing vertical arrow in
the triangle, while failure localizes it to an exact q/E3/current predicate.
It still would not prove LRC(14), but it directly tests the strongest new
algebraic mechanism against the strongest current physical germ.

For the independent bounded-speed lane, the next scalable computation is
the one-H-hole shell

```text
(H minus {h}) union three low labels,
```

using the same finite-head/tail residual-carrier partition.  That is direct
counterexample pruning, but it is orthogonal to the T8--Prony composition
test.
