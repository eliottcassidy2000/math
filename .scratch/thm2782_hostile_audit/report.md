# THM-2782 hostile audit: semantic arm survives; opposite wings do not meet

**Verdict: PROMOTE WITH A SHARP ADDENDUM; REVISE THE ADDRESS/CHAMBER
REPORT BEFORE USING IT AS CANON.**

The incoming semantic-arm companion is reproducible and its scoped positive
claim survives.  The proposed all-567 wing result is also exact, but it is a
complementary **opposite-wing common-atom no-go**, not a replacement proof of
the reserved semantic-arm attachment.  The selected D cell is not
intrinsically factor-pure; it is pure only in the declared ordered
first-failure partition.

No tracked file was edited.

## 1. Incoming semantic companion

The tracked companion

```text
04-computation/lrc14_semantic_arm_right_wing_central_digit_thm2782.py
05-knowledge/results/lrc14_semantic_arm_right_wing_central_digit_thm2782.out
```

passes ordinary and optimized Python byte-for-byte against stored output.
It has no `assert` AST nodes.  Its LF/raw SHA-256 values are

```text
script  7fbc6bb1ec303ded98eaad6e5d8205eb3d247258ada32b6f9904fc439ebb11fb
output  13f570d63f212808171cecdb4d8f9aa41884fbdc7ed571dbfe27122b412fadc4
```

The script constructs literal positive intersections of the right cofiber
with semantic/carry cylinders.  Thus the one-sided statement

```text
right-wing atom + E3 -> Q_(3,{1,2}) semantic/carry labels
```

is physical, not merely a Fourier product.  Its endpoint `Z` discussion is
properly coordinate-only: it proves the same formula after the `mod 169`
section, while the full-address descent, `Y` cospan, target-label action, and
root-deck intertwiner fail or remain absent.

Two wording safeguards should be explicit in canon:

1. the three printed arms are **three distinct positive right-wing atoms**
   with the same semantic/carry labels and successive central-coordinate
   addresses, not one common physical atom;
2. agreement of the address-step formula with endpoint central `Z` is not a
   physical endpoint allocation or action.

For self-contained evidence pin the three imports:

```text
lrc14_fully_marked_root_zero_target_profile_thm2749.py
  d67c852c52f88feaadb2fcaa0a9a07a212f2e47018040b455855df886200595e
lrc14_root_zero_clutch_mayer_vietoris_wing_shear_thm2751.py
  25cbed38026d61891173c687006250a69fe38aea56d67439406bd8bb60fa2552
lrc14_relative_present_semantic_lift_probe_20260728.py
  f16754bd38ae0dfa0d7d91cc404b4447dbf359635101aa7b4223363f8064352f
```

## 2. Independent all-567 object typing

The independent control does not import either proposed discovery companion.
For every one of the `81` lawful `(s,t)` labels and every one of the seven
physical clocks, it aligns the target carrier with the source chart and
constructs

```text
A = source one-sided carrier,
B = pullback(target one-sided carrier),
M = A intersect B,
L = A minus B,
R = B minus A.
```

It verifies the canonical physical constructors equal these set-theoretic
objects in all `567/567` cells.  Hence

```text
L intersect R = empty
```

in all `567/567` cells.  Exactly `193/567` left wings and `193/567` right
wings are nonempty.  The natural `e=1` sidecar agrees literally with the
one-sided family in all `81/81` endpoint labels; its gain `11` remains an
inherited result of the separate canonical sidecar, not a result of the
all-567 set identity.

Once `A` and `B` are physically typed, the disjointness is structural:
opposite exclusive pieces cannot furnish a common `(1,1)` allocation atom.
This blocks only the **wing/wing** realization of THM-2772's marked `K4`.
It does not block a right-wing/semantic atom, a one-sided/common lift, or any
other atom-dependent allocation.

## 3. The D correction

For `D=(s,t,e)=(0,10,3)`, the ordered first-failure partition

```text
shifted E3, shifted clock, shifted q1, shifted q2,
shifted c2, shifted c3
```

does put the complete source and target wing masses into its first bin:

```text
source first-failure masses
  (2188067024426754240,0,0,0,0,0)
target first-failure masses
  (1458711349617836160,0,0,0,0,0).
```

The raw coefficients and geometry reproduce exactly:

```text
source 2853968755527296447040,
target 5707937511054592894080 = 2 source;
piece counts 3/2;
weighted masses 2188067024426754240/1458711349617836160;
not a chart translate.
```

But the full simultaneous failure incidences are

```text
source
  (2188067024426754240,0,729355674808918080,0,
   1458711349617836160,0)
target
  (1458711349617836160,0,0,0,1458711349617836160,0).
```

Thus source atoms also fail `q1` and `c2`, and every target-wing atom also
fails `c2`.  “E3-pure” is valid only as shorthand for **E3-first in this
ordered disjointization**.  The report's claims that D removes factor
ambiguity or supplies a common E3 factor ancestry must be deleted.  A lawful
factor sidecar must retain the complete failure subset, not only the first
failed factor.

## 4. Extended address and gauge

The fixed triangle is typed through THM-2763/2772:

```text
G_full = V x F_13 x F_13,
E = V x V -> V, (L,R) |-> L-R,
P = G_full x_V E = {(q,k,l,R)}, |P|=13^6.
```

The audit checks the representative gauge on every one of the `2,197` dual
cells of both banks.  The endpoint present set transforms by `-T/13` under
`ell -> ell+W`, while the carrier harmonics transform by
`(a,b)->(a+1,b-1)`.  This gauge translation must not be confused with the
adjacent physical chart shift `7T/13^6`.

All `2,197/2,197` left and right gauge checks pass.  Holding the carriers
fixed fails already at

```text
left  ((0,0),0): 136208899724200714 -> 0,
right ((0,0),0): 177701812651845879 -> 0.
```

The fixed virtual sector

```text
(q,Delta,k,l)=((1,0),1,0,0)
```

has `13/13` nonzero endpoint edges and is nonzero in both certified
exact-order specializations:

```text
64970359711488738 mod 352341050142921841,
578986423856957328 mod 956354278959359281.
```

Either nonzero image certifies characteristic-zero nonvanishing.  This is a
coefficient in the independently formed abstract pullback bank.  Because
`L` and `R` have no common physical atom, it is exactly THM-2772's virtual
mixed face, not a physical determinant current.

## 5. Exact canonical addendum wording

Recommended theorem subsection:

> **Opposite-wing common-atom boundary (FINITE-EXACT).**  In each of the 81
> lawful full-target labels and seven physical clock sheets, align the target
> one-sided rail-eight carrier with the source chart.  If the aligned
> one-sided supports are `A` and `B`, the common carrier is `A∩B` and the two
> cofibres are `A\B` and `B\A`.  Consequently their physical intersection is
> empty in all 567 cells (although 193 left and 193 right wings are
> individually nonempty).  Therefore the two opposite wings cannot realize
> one common-ancestry `(1,1)` vertex of the THM-2772 allocation `K4`.  This
> does not obstruct the positive right-wing/semantic atoms constructed
> above, nor a future atom-dependent one-sided/common lift.
>
> At `(s,t,e)=(0,10,3)` the ordered first-failure partition assigns both wing
> coefficients first to shifted `E3` and the target/source coefficient ratio
> is exactly `2`; however simultaneous failure incidence also records `q1`
> and `c2` on the source and `c2` on the target.  Hence no intrinsic
> single-factor ancestry is inferred.  Separately allocating the two
> endpoint banks gives a nonzero characteristic-zero virtual sector at
> `((1,0),1,0,0)`, but not a physical common-atom current.

Recommended canonical companions:

```text
04-computation/lrc14_all81_opposite_wing_common_atom_boundary_thm2782.py
05-knowledge/results/lrc14_all81_opposite_wing_common_atom_boundary_thm2782.out

04-computation/lrc14_gain2_d_virtual_determinant_sector_thm2782.py
05-knowledge/results/lrc14_gain2_d_virtual_determinant_sector_thm2782.out
```

The first should add the simultaneous-failure census and remove the
unsupported “D removes factor ambiguity” conclusion.  The second should say
`virtual` in both its title and scope and should exhaust the representative
gauge, not sample four cells.

## 6. THM-2785 and remaining sidecar

Reserved THM-2785 is not duplicated.  Its target concerns THM-2672's
thirteen carry-rebase facets, the type-`A12` minuscule orbit, and a physical
deep-frequency `2*13^5` character tested against THM-2749.  The present
result ranges full-target `(s,t,e)` cells and proves only that the two
opposite wing cofibres do not meet.

The clean relation is a negative gate: THM-2785 may cite the promoted
opposite-wing boundary to exclude a wing/wing construction, but it still
must build and type its own carry-component/semantic atom.  It is a
dependency only if THM-2785 uses that exclusion; otherwise `related` is
correct.

The smallest open positive sidecar is one ancestry atom `xi` retaining

```text
clock and semantic word/owner;
external (s,t,e) and predecessor carry;
P0(xi),P1(xi),Q0(xi),Q1(xi);
complete simultaneous factor-failure/allocation signatures;
the exact G_full representative (r,k,l);
one endpoint pair (L,R), q and Delta;
endpoint displacements s_xi,t_xi and all four varying q-sectors;
the physical root-deck intertwiner and Cech invoice.
```

## 7. Audit evidence

```text
.scratch/thm2782_hostile_audit/audit.py
  45c2c185eb001b32ad634aaa2da48ffda6c47fec79550138cc2175376fd8e06d
.scratch/thm2782_hostile_audit/audit.out
  1aedb2675ed916c9c17c8a84db74b7769fe0a9811f31c8b0d6aca1d196b4c5ac
```

The audit has no `assert` AST nodes.  Ordinary and optimized Python both
byte-match the stored output exactly.
