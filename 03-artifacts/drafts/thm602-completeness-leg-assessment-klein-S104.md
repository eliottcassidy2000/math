# The THM-602 completeness leg: where exhaustion is proved, where it is finite-unrun, and where it is genuinely open

klein-2026-07-02-S104 (HYP-4009; owner flag: "censuses certify the families they contain,
but nothing yet proves those families exhaust the covering universe").

## What IS total (proved, no gap)

1. **The trichotomy** (THM-602 Part A) is exhaustive BY CONSTRUCTION: it classifies by
   rank of the height-<=7 resonance lattice (zero / intermediate / maximal) -- every
   (F, I) instance falls in exactly one case. No exhaustion gap here.
2. **The recursion** (Part D) is well-founded: mu = (j, corank) drops lexicographically;
   every branch terminates at a case-1 leaf or j <= 6 unconditional. Total.
3. **The gap-cut entry**: every covering 13-set either has all ratios <= Lambda or a
   multiplicative gap (true by definition); with MSS (arXiv:2411.06903, cited policy)
   even the bounded branch is finite-in-principle at 91^12.

## Where the gap actually lives: the LEAF CERTIFICATION

A case-1 leaf is certified by the quintic/Part-C bounds whose error terms depend on
(i) the finite combinatorial type (j <= 13, heights <= 7, d <= 5 -- FINITE), and
(ii) continuous parameters (drifts, window length, phases). The censuses certify finite
parameter families. Exhaustion = two named residues:

**O1 (the middle band, quantitative).** The proved caps (spread <= 30 by THM-529;
heights/speeds by MSS ~ 10^23; Lambda-cut ~ 10^4) exceed the censused ranges (spread <=
16; W* <= 513; heights <= 120 probed). Closing = the F3-sharpened rate (mac-mini's
claimed O(1/N), which shrinks the band to ~10^3 = decidable) OR the wide-but-finite
sweep (MSS guarantees finiteness; astronomically impractical without F3). STATUS:
quantitative, closable, NOT structurally open -- but the ONLY item whose closure
currently rests on an unlanded claim.

**O2 (the deviation table, finite-unrun).** Part C's per-pattern remainders range over
patterns of height <= 7 x d <= 5 -- a FINITE table (the S100 subtable shows exact zeros
at (1,7),(2,7),(1,14)). The "finitely supported deviation" statement needs the WHOLE
table evaluated exactly, once. STATUS: a census-shaped computation, not open mathematics.

**O3 (monotonicity glue).** The finite tables dominate the continuous leaf space only
via parameter-monotonicity of the error bounds (kps HYP-3950 proved minima monotone in
r; the drift/window monotonicity used by Part C is stated explicitly there). STATUS:
transcription of Part B/C's explicit bounds; no new mathematics.

## Verdict

The covering universe IS exhausted by [trichotomy total] + [recursion well-founded] +
[leaf types finite]; what is NOT yet proved end-to-end is the leaf certification over
the continuous/band parameters, which splits into O1 (rests on F3 -- the single
genuinely load-bearing unlanded claim in the whole program), O2 (one finite table run),
O3 (transcription). RECOMMENDATION: (a) elevate F3 to the ledger's unique Tier-0
mathematical risk and land it (or its fallback bound with the wide sweep priced);
(b) run the O2 table this week; (c) fold O3 into the THM-602 transcription. If F3
falls, the honest statement becomes: LRC(14)'s proof is complete modulo one
astronomically-large-but-finite computation -- true, citable, and unsatisfying; F3 is
therefore where the remaining mathematical effort belongs.
