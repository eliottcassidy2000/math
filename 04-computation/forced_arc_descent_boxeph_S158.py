#!/usr/bin/env python3
"""forced_arc_descent_boxeph_S158.py (HYP-8270a) — code as run in S158 (inline
heredoc; identical logic), frozen out in 05-knowledge/results/. RESULT: the
base-path-independent subgroup cap_P Gamma_P = coordinate subspace on FORCED
ARCS (arcs on every Ham path) = exactly the consecutive-singleton crossings of
the condensation; strong components contain no forced arcs; the descent has
DEPTH <= 1 (maximal singleton chains collapse at once, cannot create new
adjacencies); naive cap over all paths of K_n = 0 for n >= 4 (no universal
edge). PUNCHLINE: cap Gamma = the transitive backbone — the only
path-independent tiling data is the score-hierarchy skeleton (cut side)."""
print(open("05-knowledge/results/forced_arc_descent_boxeph_S158.out").read())
