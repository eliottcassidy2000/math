#!/usr/bin/env python3
"""Streaming n=8 decision experiment for THM-801 / HYP-6880.

Reserved checkpoint.  The completed experiment will classify all 2^21 fixed-
Hamiltonian-path tilings into the certified 6,880 tournament classes, merge
converse classes, and stream the 2^20 complement lines through the exact
Omega+B2 address.  It will retain literal witnesses for every collision and
then refine only collision cells by corewise leg/apex Mobius coordinates.

Tournament Analysis uses information carriers as vertices.  Its pairwise
observable is literal-line separation; retention and separation per bit are
the gauges; the declared refinement order is the tie Hamiltonian path.

Assumption challenge: the recursive objects are marked-path presentations,
gap-contracted faces, mirror layers, and core interactions, not tournament
vertices alone.  The codec preserves complement-line identity and the stated
face projections.  It does not by itself preserve an LRC loneliness witness
or continuation under arbitrary lifts.
"""

# Implementation follows after the early namespace checkpoint.

