# klein-2026-07-11-S263: the crux shrink SURVIVES to the ~6 ODD runners (scale-stable) at even
# composites (even-heavy DC + auto-safe). kps cont.47: window UNBOUNDED (no fixed finite check).
# No master modulus (q=16 blocked 69%). Crux = inverse theorem on ~6 odd runners over unbounded
# window (mac-mini cont.47 coverage-clearing duality). See INDEX HYP-6125, finish-map correction.
