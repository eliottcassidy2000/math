# LRC(14) — bleeding-edge synthesis (death-star-2026-07-09-S1)

**Supersedes** `LRC14-STATUS-2026-07-08.md` (klein-S194 audit, updated through klein-S195/S203).
Everything there remains correct; this records the 07-09 burst, which **changed the shape of the
endgame**: hlink discharged, the drift/pinch obstructions unified and desingularized, the covering
reframe (the tight AP is out of scope), M(AP) = 1/14 fully formalized, and the residual reduced to
**one analytic node with a precise residual class** plus **one Lean wiring obligation**.

**One-line state:** LRC(14) is sorry-free-conditional in Lean on exactly `hfloor` + `hpartA`
(`LRCFourteenSkeleton.lrc14_from_witness_floor` / `LRC14Assembly.lrc14_endgame`); the analytic
content of both has been reduced to the **Kronecker/equidistribution realization on genuinely
multi-scale shapes (speeds-ratio > 13)**, with every single-scale corner now closed by explicit
witnesses. No hard analytic wall is known to remain; the residual is a composition problem.

---

## 1. The proof DAG (with Lean anchors)

```
LRC14Statement  (∀ v : Fin 13 → ℤ, v ≠ 0 → ∃ t, Lonely 14 v t)     [LRCFourteenSkeleton]
├─ Link 1  q-witness sieve: counterexample ⟹ covering-saturated 13-set
│          PROVED (counterexample_needs_all_divisors, sieve_one_div, lrc14_no_multiple_of_14)
├─ Link 2  non-covering ⟹ LRC(≤13) citation (owner directive)      PROVED (LRC13Citation)
│          ★ the TIGHT AP {1..13} lives HERE: no multiple of 14 ⟹ witness τ = 1/14, equality —
│            M(AP) = 1/14 EXACT is now fully formalized (kps-S110/S111 LRCAPTight.mreach_AP_eq)
└─ Covering case  ⟸  Mreach v ≥ 1/14                               [lonely_of_Mreach_ge]
     ├─ hfloor: witnessMP ≤ witnessG2(shapeOf v)   — the density floor
     │    ├─ k ≤ 7 pigeonhole: PROVED (skeleton)
     │    ├─ k = 8..13 pure-cluster diam ≤ 75: PROVED UNCONDITIONAL in Lean
     │    │    FareyRoof (THM-637) → RoofBridge → AP20/AP30/AP44/AP76 (exact rational
     │    │    2314528732/40290957525) → TailDiameter.hlarge_floor_of_diam_le (m_P = 14249/252252)
     │    │    → GoodSetBridge.slowμ_goodSet_toReal_ge_mP_diam75
     │    ├─ diam ≥ 76 / P ≠ ∅ intersected legs: closed in exact arithmetic
     │    │    (THM-661/663 + LEM-005/006 + kps-S60 ledger), NOT yet transcribed to Lean
     │    └─ ⚠ the identification witnessG2 := slowμ(goodSet ∩ G_P) is a BRIDGE NOTE, not a
     │       theorem — witnessG2 is still `opaque` in the skeleton (see §4, obligation W)
     └─ hpartA / hembed = THM-527 Part A — the REALIZATION            ★ THE NODE ★
          ├─ criterion-C core: nearInt(v_i·τ) = nearInt(frac(Vmax·τ) − frac(e_i·τ)),
          │    e_i = Vmax − v_i   [triple convergence: klein-S204 LRCCriterionC,
          │    kps-S105 LRCSlowFast, opus-S176 LRCHembedIdentity — all kernel-pure]
          ├─ hlink (free-gap extraction): DISCHARGED (opus-S175 LRCHlinkExtract +
          │    klein-S204 LRCGoodPeriodFreeGap, two independent kernel-pure proofs)
          ├─ ruler points are NEVER lonely: minReach v (j/Vmax) = 0 ∀j — the witness is
          │    forced NON-LOCAL (klein-S207 LRCRulerPoints, kernel-pure); do not attempt
          │    local witnesses on the Vmax-ruler
          ├─ continuum bridge: measure→point desingularization + drift-free observer
          │    (kps-S112 LRCSmoothBridge, 7 thms) — sole remaining hypothesis = hrefl,
          │    the Kronecker realization
          ├─ single-scale corners: ALL CLOSED (see §2)
          └─ residual: genuinely MULTI-SCALE shapes (speeds-ratio > 13) — see §3
```

## 2. What closed on 07-09 (the burst)

1. **hlink** — the good-period ⟹ free-residue-gap extraction, discharged twice independently
   (opus-S175, klein-S204 via the `0 ∈ E ⟹ cyc.last = Vmax` wrapping simplification).
2. **The drift and the grid-pinch are ONE finite-Vmax wall, and it desingularizes at the
   continuum** (kps-S107/S108/S112 + opus-S177 + klein-S207). Sharp indicator: jump
   discontinuities ⟹ O(1/V) grid error + measure-zero pinches invisible to every grid
   (MISTAKE-130). Smooth surrogate `W = Σ(gap−1/7)₊`: corners only ⟹ O(1/V²) aliasing
   (kps-S108; sharp constant TV(W′)/(12V²) in flight as **THM-665**, monad HYP-5707), and
   `∫W > 0 ⟹ ∃x, W(x) > 0` needs no grid at all (`exists_pos_of_integral_pos`).
3. **Ruler points are never lonely** (klein-S207): the observer runner IS Vmax, so it sits on
   the origin at every τ = j/Vmax. mac-mini-S64's hembed counterexample is thereby FORCED, not
   accidental; the fast phase must live in [1/14, 13/14]; drift ≥ e_i/(14·Vmax) is unavoidable;
   the 1/7-object's witness is non-local. No repair of THM-663 needed.
4. **The covering reframe** (klein-S206 + mac-mini-S64, HYP-5690): the tight AP — the cluster
   that broke five routes (MISTAKES 127–130 + the kissing certificate) — is **non-covering**
   (no multiple of 14) and is dispatched by the sieve with the exact witness τ = 1/14. Three of
   the four route-breaking clusters are out of scope. On the covering branch: every one of the
   966 primitive covering 13-subsets of [1,18] has a strict good period (min margin 1.2353) and
   exact min M = **1/12 > 1/14** (strict cushion 1/84); the equality locus is entirely
   non-covering. **The proof shape is forced**: [exact τ = 1/q on non-covering, equality
   allowed — no margin argument can ever work there] + [strict margin on covering].
5. **M(AP) = 1/14 exact, both halves formalized** (kps-S110/S111): ≥ via nearInt(i/14) ≥ 1/14;
   ≤ via Mathlib's Dirichlet with n = 13. The LRC(14) equality extremal is a Lean theorem.
6. **Single-scale corners of the realization all closed** (death-star-S1, HYP-5710, this doc's
   contribution): for pure clusters — co-offsets e = Vmax − v ∈ [0, D], P = ∅ — **kps-S28's
   `spread13_lonely` subsumes every realization mechanism**: 13D ≤ 12·Vmax puts all speeds in a
   ratio-≤13 band ⟹ lonely at the explicit witness t = 1/(2Vmax − D). Packaged kernel-pure in
   `LRCPureClusterCorner.lean` (`mreach_ge_of_pure_cluster`, hpartA-shaped, no good-period
   input). klein-S205's `Mreach_ge_of_driftGap` (j = 1, a = D/V, g = 1 − D/V) independently
   closes the same corner at the worse constant Vmax ≳ 2.76·D. Consequence: **the realization
   node's residual class is exactly speeds-ratio > 13** — a scale gap in the speed spectrum.
7. **Extremal understanding anchored classically** (opus-S179/S180/S181): the AP is the unique
   tight extremal BECAUSE it is Freiman's unique minimal-sumset set (|S+S| = 2n−1); additive
   energy is necessary-not-sufficient — the tight locus is the 1-dimensional COHERENT relation
   lattice (dilates c·{1..13}), which is why LEM-012/013 correctly key on longest-AP.

## 3. The one remaining analytic node, precisely

**hrefl / the Kronecker realization** (kps-S112's named hypothesis; = hpartA = hembed after
criterion-C): from a positive-measure good event of the co-offset cluster, produce a real τ
whose fast phase sits in the cluster gap **while the slow part P stays safe**.

The residual class after the 07-09 burst:
- speeds-ratio ≤ 13 (no scale gap): CLOSED — `spread13_lonely` / `LRCPureClusterCorner`.
- coarse multi-scale (≤ 12 effective clusters after scale grouping): CLOSED —
  `LRCCoarseReduction.lonely14_of_coarse_le12` (conditional only on the LRC(≤13) citation).
- **OPEN: single-chain multi-scale covering shapes** — a fast cluster L at scale Vmax plus a
  slow part P, with the total ratio > 13 but no reduction-grade scale gap. The witness must
  make the fast phase clear the cluster teeth (positive-measure event, floor PROVED) at a τ
  where P is simultaneously safe (the G_P event). The sweep that places the fast phase moves
  the P-phases by O(p/Vmax) each — NOT small for mid-band p — so the composition is the genuine
  content. This is a **composition problem** (measure + Lipschitz bookkeeping), not a wall.

**In flight / landed on this node during 2026-07-09 (five concurrent sessions):**
- monad-explorer HYP-5707 / **THM-665**: |R_grid| ≤ TV(W′)/(12V²) sharp aliasing + exact TV
  ledger — the a-priori grid-existence half.
- monad-explorer HYP-5717 / **THM-666**: the clamp port of the FULL measure to the V-ruler at
  1/V² + the explicit per-shape P∪L sufficient criterion
  μ_L(θ′) + meas_grid(G_P) − 1 − TV/(12V²) − 2Σ_P v_p/V > 0 ⟹ Mreach ≥ 1/14.
- boxeph HYP-5708 / **LEM-014: LANDED** — the P-SEPARATED composed realization, wide regime,
  exact-verified INCLUDING P ≠ ∅ (|P| = 3, 5; k = 10 margin +0.038, k = 8 +0.069); the
  V/spread ≈ 2.7 boundary is intrinsic (= THM-665's V₀). Lean transcription
  (`LRCComposedRealization.lean`) is the named follow-up; boxeph→death-star handoff: compose
  LEM-014 with the HYP-5710 delineation (the pure-cluster base case + ratio > 13 residual).
- kps-S113: **`LRCDensityFloorCert.lean`** (sorry-free) — the GRID-FREE a-priori cert of
  ∫W > 0 (`exists_pos_of_measure_support_pos`, `mreach_ge_of_good_measure_pos`), honoring
  klein-S201 (grid routes fail at resonant rulers; the density floor is the certificate).
- mac-mini HYP-5720 / **THM-668** (renumbered): realization slack ≥ c₀/(v_i+v_j) on covering
  clusters; live-ruler stats (the AP has exactly ONE live ruler q = 14; covering sets min 2,
  mean 11; witness q ∈ (Vmax, 2Vmax] in 89.6% of cases).
- ID collisions RESOLVED same-day: THM-666 → monad (mac-mini ceded, took THM-668);
  LEM-014 → boxeph (claim-first 15:57:15; opus-S183's Schur-triple lemma renumbers → LEM-015).

## 4. The remaining Lean obligations (mechanical, but real)

- **(W) the witnessG2 de-opaquing** (opus-S180: "the one substantive remaining Lean
  obligation"): replace `opaque witnessG2` (LRCFourteenSkeleton:204) with the concrete
  `slowμ(goodSet(cluster s) ∩ G_P s)` and let the cert stack (AP76 → TailDiameter →
  GoodSetBridge, D3 floor cert) actually discharge hfloor legs instead of feeding a
  documented-but-unproved identification. Heavy refactor of a 640-line hub file — schedule it
  in a LOW-CONCURRENCY window (today's 5-session burst is exactly the wrong moment; two
  checkpoint rebase conflicts occurred within one hour).
- **(T) diam ≥ 76 / P ≠ ∅ floor transcription**: closed in exact arithmetic (LEM-005/006 +
  kps-S60 intersected ledger), not yet Lean.
- **(F) the finite windows**: exact-M(S) check Vmax ≤ 1001 (kps-S30) and the residual
  mid-band window after THM-665 shrinks it — native_decide territory once the criterion of
  HYP-5717 fixes the exact window shape.
- Two court cases await only coordinator close (good-period-smallV: substance agreed;
  convergent-not-covering-min: both parties recommend GRANT).

## 5. Method notes the burst re-confirmed (for the next session's hygiene)

- **Existence is a MAX, not a mean** (MISTAKE-129): never certify good periods by averages at
  resonant rulers; always test the tight AP at its own ruler — and now, first check whether
  your cluster is even covering (klein-S206) and whether its ratio is ≤ 13 (HYP-5710).
- **Verify-first beats build-first** (MISTAKE-131 avoided twice today): before formalizing a
  realization lemma, grep for `spread13_lonely`, `Mreach_ge_of_driftGap`,
  `scale_separation_phase`, `lonely14_of_coarse_le12` — the single-scale toolbox is complete.
- **Work on the CLOSED/non-strict good set** (opus-S177): the pinches live on the boundary;
  every open-set/grid-sampling route dies there.
- Claim stubs + immediate checkpoints kept five concurrent sessions collision-free today
  except one benign timestamp conflict and the THM-666 number collision.

*Sources: 00-navigation/SESSION-LOG.md entries 2026-07-09 (kps-S99..S112, klein-S201..S207,
opus-S172..S181, mac-mini-S64/S65, monad-S1/S2, boxeph-S1, death-star-S1); Lean tree
04-computation/lean/TournamentH7/TournamentH7/; hypotheses INDEX HYP-5690..5720.*
