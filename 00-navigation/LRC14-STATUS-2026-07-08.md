# LRC(14) — honest proof status (klein-2026-07-08-S194 audit)

**Verdict: NOT yet a complete rigorous proof, but reduced to a short, explicit list of finite /
mechanical items — no hard analytic wall remains.** Both former "hard" obstructions (the density-floor
AP-minimality lemma; the large-spread Weyl estimate) have been BYPASSED by elementary/finite routes.

`LRC(14) = ` [non-covering] `+` [covering]. The chain (THM-663):

| # | Link | Status |
|---|------|--------|
| 1 | q-witness sieve (THM-369): counterexample ⟹ covering-saturated 13-set or ≤13 instance | **PROVED, Lean-checked** ✓ |
| 2 | Non-covering ⟹ LRC(≤13) | **SETTLED** (owner directive) ✓ |
| 3 | Reformulation (THM-527): global witness (`x∈G_P`, maxgap>1/7) ⟹ `M(S)≥1/14` | **PROVED** (w0→∞ limit; finite bridge = item 6) |
| 4 | Density floor `μ_{1/7}(E) ≥ bar_k`, k=8..13 | **k=8 proved; k=9,10,11 a-priori; k=12,13 a-priori modulo box-bound writeup** (see below) |
| 5 | `ρ*_{1/7}(P,E) ≥ m_P > 0` via THM-530 (Bonferroni + item 4) | **PROVED given item 4** ✓ |
| 6 | THM-527-A finite-`Vmax` glue: a good ruler period exists | **bounded-spread PROVED; large-spread PROVED (LEM-010) modulo a bounded finite check** (see below) |
| 7 | Lean transcription of 1–6 | **in progress** (THM-369, D3 floor cert, dilation-invariance done; not complete) |

## The genuinely-remaining items (all finite or mechanical)

- **(R1) The THM-527-A bounded finite check** `{Vmax ≤ 3^{k−1}, spread ≥ 6·Vmax/7}` (≤ 531441 at k=13).
  LEM-010 closes everything OUTSIDE this box elementarily (j=1 if spread<6Vmax/7; Dirichlet j*≤3^{k−1}
  if Vmax>3^{k−1}). **So THM-527-A is CLOSED modulo this finite check** (only the *size* of the check is
  the issue). Cleanest shrink: prove `j* = O(k)` ⟹ check becomes `Vmax ≤ O(k)`, trivial.
  **PROGRESS (klein-S195):** the worst-case `j*` (≈k) is achieved ONLY by (near-)exact APs of step
  ≈Vmax/k; **generic hard clusters open a gap at tiny `j` (`j*≤6`)**.
  **BREAKTHROUGH (klein-S196/S197, LEM-012): the structured branch is now ELEMENTARY.** Split on the
  longest AP `L(E)`: **`L ≥ k−5`** ⟹ `j* ≤ ⌈7(L−1)/(L−k+6)⌉` by **Dirichlet + gap-split** (cluster the
  L-AP into a `<(L−k+6)/7` arc; complement gap `>(k−L+1)/7` split by `m=k−L≤5` strays into `≤m+1` pieces,
  largest `>1/7`); **`L = k−6`** ⟹ `j* ≤ 7⌈49(L−1)/3⌉` by the **×7 collapse** (bad ⟹ 7 clumps forced onto
  a V/7-grid ⟹ `7j` maps the grid to one super-point ⟹ gap). **So `L ≥ k−6` is ELEMENTARY** (no
  Weyl/resonance; recovers mac-mini's exact-AP bound at `L=k`; mechanisms verified 100%). `k=8` has no
  `L≤k−6` sets ⟹ **k=8 fully closed by LEM-012**. Remaining: **`L ≤ k−7`** (deeply dissociated / Sidon-like,
  only `k≥9`, `L≤6` at k=13) ⟹ `j*≤5` verified, a-priori via the LEM-011 𝒲̂-smallness (Sidon-like = minimal
  additive energy = fewest resonances = smallest correction — the natural 𝒲̂ regime). The dichotomy is
  clean: **structured (long-AP) ⟹ elementary clustering; Sidon-like ⟹ 𝒲̂ few-resonances.** Combined
  `j*≤686` ⟹ with kps-S30's exact `M(S)` check (`Vmax≤1001`), THM-527-A closes once the Sidon-like `j*≤B`
  is written.
  **CORRECTION (klein-S201, MISTAKE-129): the good-period leg owns only `V ≥ Q+1` (`Q=⌈7(L−1)/(L−k+6)⌉`),
  not `V > max E`.** LEM-012's Dirichlet `j` (`‖jd/V‖<1/Q`) collapses to the excluded trivial period
  `j≡0 (mod V)` when `V ≤ Q`. The **small-ruler corner `V ≤ Q` (the extremal tight AP `{0..k−1}` at `V=k`,
  = the `M=1/14` instance) has NO good period** — it is DENSITY-FLOOR territory (`μ_good({0..12})=0.44 ≥
  bar_13`), not good-period. So the dichotomy is a `2×2`: {near-AP, dissociated} × {`V≥Q+1`, `V≤Q`}; the
  good-period leg (LEM-012/LEM-013, MAX-based) owns the three large-ruler+dissociated cells, the density
  floor owns near-AP∩small-ruler. This ALSO refutes opus-S170's smooth grid-MEAN route as the near-AP
  closure (`E_x[maxgap]>1/7` but `E_grid=1/13<1/7` for the tight AP at `V=13`; disc `0.134`, not `0.006`).
  Existence is a MAX, never a mean/count. Files: `lrc14_smooth_route_and_LEM012_smallV_klein_S201`,
  CASE-good-period-smallV-no-good-period.
  **LEAN ASSEMBLY (klein-S203): the good-period leg is now wired end-to-end.** `LRCGoodPeriodReach.lean`
  (sorry-free, kernel-pure) proves `HasGoodPeriod E Vmax ⟹ 1/14 ≤ Mreach v` modulo exactly two named links:
  `hlink` (free-gap extraction — finite list plumbing, `List.Sorted` API shifted) + `hembed` (= **THM-527
  Part A**, the `Vmax`-ruler embedding). Chain: kps-S99 dispatch (tiling LEM-012∪LEM-013) → klein
  `LRCGoodPeriodMaxgap` (decidable `HasGoodPeriod` + native_decide certs) → kps-S31 `GapReach` (gap→`nearInt`
  clearance) → `Mreach_ge_of_witness`. **REFRAME: Part A (`hpartA`, opaque `witnessG2`) is the ONE shared
  open node for BOTH the good-period route AND the density-floor route** — finishing the proof = formalizing
  Part A, not the good-period branches (LEM-012 proved, LEM-013 verified + 2×2-covered) nor the finite
  free-gap. Reflection: `the-good-period-leg-is-assembled-part-A-ruler-embedding-is-the-shared-blocker-klein-S203`.

- **(R2) Density floor k=12,13 tail — DONE, a-priori (klein-S195).** k=12,13 tail closes via the
  box bound (kps-S89 template, LEM-011 exact moments): **[box bound d≥30: min D3 ≥ bar] + [finite check
  d≤29: min D3 = 0.374/0.326 ≥ bar 0.199/0.056, margins +0.175/+0.269]**. Crossover d₀=30 (vs k=11's 62;
  bigger margins). Fully a-priori, no numerical certification. With k=8 (THM-651), k=11 (kps-S89 box),
  k=9,10 (B_4 exhaustive+tail), **all six density-floor legs are now a-priori.** File:
  `lrc14_k1213_boxbound_klein_S195`.

- **(R3) The `G_P`/P-coupling — RESOLVED (reduces to R1 at k=13; klein-S195).** The discrete
  good-period existence needs `x=j/Vmax ∈ G_P` AND cluster maxgap>1/7 (THM-530). Resolution: **fold `P`
  into the co-offset set** — use all 13 co-offsets `E_full = {Vmax−v : v∈S}`. Then a gap>1/7 in the
  full-13 phases is a witness avoiding EVERY runner (P included). `P`'s co-offsets `Vmax−p ≈ Vmax` have
  phases near 0 (they cluster with `e_0`, harmless). VERIFIED: the all-13-co-offset good period exists
  at tiny `j` (`≤2`, 0 fails over sampled covering sets with `P≠∅`, `k=8..12`, `Vmax≤1500`). So R3 is
  subsumed by R1 (the `k=13` case), confirming mac-mini's "only `j*=O(k)` remains." File:
  `lrc14_R3_gp_coupling_klein_S195`.

- **(R4) Lean transcription** of the full chain (mac-mini: `good_period_j1_wraparound` sorry-free;
  opus: `muGood_dilate`; kps: D3-floor cert + exhaustive slices).

## What is NOT missing (both hard walls bypassed)

- The density-floor **"AP minimizes μ" lemma** — bypassed by [exact exhaustive over compact shapes] +
  [longest-AP tail with a decorrelation rate, now exact via LEM-011 / box-bounded by kps-S89]. No
  uniform extremal lemma needed.
- The large-spread **Weyl equidistribution estimate** — bypassed by LEM-010's elementary Dirichlet
  pigeonhole. THM-664 (Weyl) + LEM-011 (exact `𝒲̂`) give the conceptual unification + abundance, not a
  proof-critical gap.

## One-line

**Updated (klein-S195): R2 DONE (k=12,13 tail a-priori, box bound), R3 RESOLVED (folds into R1 at
k=13). The ENTIRE LRC(14) now rests on a SINGLE open math item: (R1) general `j*=O(k)`, which reduces
to AP-extremality (the AP maximizes the first-good-period; generic hard clusters have `j*≤6`).** Until
then THM-527-A holds modulo the bounded finite check `Vmax ≤ 3^{12}` (LEM-010(ii) unconditional). Plus
(R4) Lean. No open hard analysis. Source: klein-S195, building on mac-mini (LEM-010, AP case, Lean),
opus (S161/S162, muGood_dilate), kps (S89/S90 box + exhaustive).
