# LRC(14) — honest proof status (klein-2026-07-08-S194 audit)

**Verdict: NOT yet a complete rigorous proof, but reduced to a short, explicit list of finite /
mechanical items — no hard analytic wall remains.** Both former "hard" obstructions (the density-floor
AP-minimality lemma; the large-spread Weyl estimate) have been BYPASSED by elementary/finite routes.

`LRC(14) = ` [non-covering] `+` [covering]. The chain (THM-663):

| # | Link | Status |
|---|------|--------|
| 1 | q-witness sieve (THM-369): counterexample ⟹ covering-saturated 13-set or ≤13 instance | **PROVED, Lean-checked** ✓ |
| 2 | Non-covering ⟹ LRC(≤13) | **SETTLED** (owner directive) ✓ |
| 3 | Reformulation (THM-527): global witness (`x∈G_P`, maxgap>1/7) ⟹ `M(S)≥1/14` | **PROVED** (w0→∞ limit; finite bridge = item 6) |
| 4 | Density floor `μ_{1/7}(E) ≥ bar_k`, k=8..13 | **k=8 proved; k=9,10,11 a-priori; k=12,13 a-priori modulo box-bound writeup** (see below) |
| 5 | `ρ*_{1/7}(P,E) ≥ m_P > 0` via THM-530 (Bonferroni + item 4) | **PROVED given item 4** ✓ |
| 6 | THM-527-A finite-`Vmax` glue: a good ruler period exists | **bounded-spread PROVED; large-spread PROVED (LEM-010) modulo a bounded finite check** (see below) |
| 7 | Lean transcription of 1–6 | **in progress** (THM-369, D3 floor cert, dilation-invariance done; not complete) |

## The genuinely-remaining items (all finite or mechanical)

- **(R1) The THM-527-A bounded finite check** `{Vmax ≤ 3^{k−1}, spread ≥ 6·Vmax/7}` (≤ 531441 at k=13).
  LEM-010 closes everything OUTSIDE this box elementarily (j=1 if spread<6Vmax/7; Dirichlet j*≤3^{k−1}
  if Vmax>3^{k−1}). Inside it, only `Vmax ≤ 1001` is verified (kps-S30). **This is the single largest
  remaining item.** Cleanest closure: prove mac-mini's conjecture `j* = O(k)` (empirically `j*≤7` for
  k≤13, incl. adversarial APs) ⟹ the check shrinks to `Vmax ≤ O(k)`, THM-527-A **fully elementary**.
  A simultaneous-Diophantine / three-distance problem — OPEN but strongly supported.

- **(R2) Density floor k=12,13 tail, fully a-priori.** k=8 (THM-651) unconditional; k=11 tail closed
  a-priori by kps-S89's box bound (d₀=62 + finite check) — opus-S161/S162 "k=11 tail math complete."
  k=9,10 via B_4 exhaustive+tail. k=12,13 tail rests on the decorrelation rate; **LEM-011 (klein-S194)
  now gives the exact `𝒲̂`**, so the constants are a-priori — but the clean box-bound writeup (à la
  kps-S89) for k=12,13 is not yet done. Mechanical.

- **(R3) The `G_P` (small-part) coupling in the finite-`Vmax` EXISTENCE route.** LEM-010/THM-664
  produce a period where the **cluster** phases leave a gap >1/7; the full witness also needs the
  observer to avoid the small part `P` (`|P|=13−k`; the `x∈G_P` condition). For k=13, `P=∅` — vacuous.
  For k<13: **likely resolvable, should be written.** (a) In LEM-010(i) at `j=1`, the P-runners have
  SMALL speeds (`≤13`), so their phases `v_p/Vmax ≤ 13/Vmax` cluster near `0` — INSIDE the cluster's
  span `[0, spread/Vmax]` — so the wraparound gap `1−spread/Vmax` is empty of `P` too, giving a FULL
  witness. (b) In LEM-010(ii) the Dirichlet pigeonhole can be run on **all 13** phases (`j ↦
  (⌊3·frac(v_i j/Vmax)⌋)_i`), still `j*≤3^{12}`, so the `1/3`-gap avoids every runner. So the natural
  reading carries `P`. Flagged as a writeup item, analysis says it holds.

- **(R4) Lean transcription** of the full chain.

## What is NOT missing (both hard walls bypassed)

- The density-floor **"AP minimizes μ" lemma** — bypassed by [exact exhaustive over compact shapes] +
  [longest-AP tail with a decorrelation rate, now exact via LEM-011 / box-bounded by kps-S89]. No
  uniform extremal lemma needed.
- The large-spread **Weyl equidistribution estimate** — bypassed by LEM-010's elementary Dirichlet
  pigeonhole. THM-664 (Weyl) + LEM-011 (exact `𝒲̂`) give the conceptual unification + abundance, not a
  proof-critical gap.

## One-line

LRC(14) is **proved modulo (R1) a bounded finite check `Vmax ≤ 3^{12}` (or the `j*=O(k)` bound that
removes it), (R2) a mechanical k=12,13 tail writeup, (R3) confirming the `G_P` coupling in the
elementary existence route, and (R4) Lean.** No open hard analysis. Source: klein-S194, reading
THM-663/527/527-A, LEM-010/011, THM-664, and the density-floor legs.
