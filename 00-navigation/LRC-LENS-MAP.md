# LRC14 Comprehensive Lens Map

**Source:** codex-2026-06-26-S206.
**Status:** synthesis and coordination map, not a proof.
**Purpose:** collect the main lenses through which the repo has viewed LRC14,
state what each lens preserves and destroys, and expose the common object they
all seem to be pointing at.

## Central Readout

The current evidence does not point to one decisive scalar, one universal
runner tournament, or one sequence shadow.  It points to a labelled packet
sheaf with controlled forgetting.

The live proof object has at least four layers:

1. **Status and boundary layer:** strict-open versus AP/GW endpoint-boundary
   equality; closed arc-Cech H1; open topes; safe-component stalks.
2. **Route and family layer:** Q-WITNESS, AP/GW, C27/petal, K33/THM-572,
   covering/boundary-moment, analytic/Haar/Fejer/Ramanujan exits.
3. **Local owner, period, and topology layer:** endpoint owners, active
   bottleneck owners, barcode bars, normal-fan supports, primitive safe decks,
   residual capacitors, drop/add squares, exact-M zeta.
4. **Certificate layer:** dual annihilation, cut/cochain/zeta repair,
   familywise descent, Henselian unit rules, Fejer/Toeplitz interval
   certificates, Ramanujan exact-period projectors, formal exact arithmetic.

Every LRC quotient seen so far is safe only if the forgotten coordinate is
fiber-constant, reconstructed, dual-annihilated, descended familywise,
stopped at AP/GW boundary topology, or routed to a named residual sector.

```text
LRC-safe quotient
  = fiber-constant
  or reconstructed
  or dual-annihilated
  or family-descended
  or boundary-stopped
  or named residual.
```

The shortest current slogan is:

```text
zero-open
  = owner-essential AP/GW boundary H1
  or named F7/THM-572/harmonic residual debt;

positive-open residual
  = first tooth + primitive deck + capacitor/zeta/owner cut.
```

## Lens Families

| Lens family | What it preserves | What it destroys or hides | Required sidecar / repair | What it points at |
|---|---|---|---|---|
| Labelled packet / reduction lens | theorem route, exact packet identity, AP/GW boundary atoms, C27/K33/covering family | local topology and certificate economy if scalarized | route label, exact `M`, qdiv, packet family, residual sector | packet sheaf base |
| Tournament / metagraph / observer lens | pairwise comparator, wall-crossing relation, marked source/phase order | thresholds, endpoints, scale, family route if vertices are raw runners | marked vertices, retained observable, tie Hamiltonian path, lost-coordinate ledger | relation walk over packet base |
| Exact circle / event / topology lens | boundary/open status, closed arc-Cech H1, open topes, owner-essential cycles | route labels and arithmetic period if topology is used alone | closed/open arc beta, owner-deletion persistence, cocircuit facets | AP/GW boundary H1 versus open topology |
| Endpoint-owner / normal-fan / barcode lens | local bottleneck owners, safe bars, active supports, stalk shape | global route and period labels | endpoint-owner strip, barcode shape, normal-fan support, largest-stalk key | owner-strip descent |
| Arithmetic / Farey / period lens | exact scale, qdiv, Farey excess, primitive periods, Henselian unit roots | topology and owner identity when reduced to numeric buckets | magnitude cocycle, primitive safe deck, Hensel unit/zero-root debt | status gate then route scheduler |
| Harmonic / analytic / certificate lens | dual certificates, capacity meters, squarefree blindness, smoothing clocks | packet family, prime-power route, owner geometry if raw estimates are used | Fejer packet key, Ramanujan projector, analytic blindness report | packet-keyed certificate backend |
| Automata / sequence / lacunary lens | Moser/fibbinary/gap language, carry state, finite transition data, power-lift guards | exact safe intervals, AP/GW equality, theorem route | exact `M`, endpoint/topology sidecars, automaton shadow class | telemetry sidecar, not proof quotient |
| Pair / decoy / local switch lens | false switches, blocker teeth, pair shells, Haar square coordinates | row identity and route class if decoys are counted only | generator class, blocker deck, barcode/normal-fan relation, zeta class | first nonzero local cut |
| Residual / state-lift / F7 lens | final named proof debt after legal reductions | everything if residual is anonymous | forbidden sector definition, state-lift signature, harmonic residual name | terminal debt boundary |
| Formal / computational lens | exact rational checkability, reproducible ledgers, cached packet fields | conceptual compression if outputs are not theorem-targeted | schema, source hash, theorem route, sidecar key | auditable packet manifest |
| External analogy lens | possible carrier grammar from other subjects | LRC predicate unless translated into packet fields | preserved predicate, destroyed coordinate, sidecar demand | hidden-lift grammar only |

## Lens Connection Graph

The map is not a line.  It is a directed repair graph whose center is
`controlled_forgetting_packet_sheaf`.

```text
automatic / sequence shadow
  -> magnitude cocycle
  -> barcode / stalk / normal fan
  -> route scheduler

coarse ET + Henselian unit gate
  -> status convergence
  -> residual route-mixed fibers
  -> first tooth / primitive deck
  -> AP-tail q13 puncture / fixed-point clock
  -> owner-strip filtration pages
  -> capacitor cut
  -> zeta square / endpoint-owner strip

closed arc topology
  -> owner-essential AP/GW H1
  -> zero-open theorem target

analytic clocks
  -> capacity + blindness report
  -> q=23 squarefree residual
  -> Haar zeta / owner strip

pair-good decoys
  -> blocker generator teeth
  -> barcode / normal fan relation
  -> owner-strip descent

external analogies
  -> packet-field translation test
  -> controlled-forgetting law
```

If two lenses disagree, the map asks which coordinate each one has forgotten:
status, route, owner, topology, period, exact scale, dual certificate, or named
residual handoff.

## Detailed Matrix

| Lens handle | Strongest current evidence | Preserved predicate | Destroyed coordinate | Next pull |
|---|---|---|---|---|
| HYP-2963 labelled packet bank | `21913` exact packets with named route census | route and exact packet family | sidecar compression | add lens-family tags and hidden sidecar fields |
| HYP-3024 coarse ET + unit rule | `0` mixed boundary/open fibers, `15` route-mixed fibers | status | exact route | prove status convergence before route purity |
| HYP-3030 status-topology gate | AP/GW are the only boundary rows in mixed residue-terminal status collisions | zero-open topology | route scheduling | prove AP/GW boundary H1 theorem |
| HYP-3034 arc-boundary path lift | AP/GW closed-H1 reps killed by any owner deletion | owner-essential boundary | arithmetic period | add closed-H1 owner support sidecar |
| HYP-3035 residual tooth atlas | `15` coarse residual fibers split by first topology/stalk teeth | open residual localizer | global route | prove owner-strip descent lemmas |
| HYP-3036 primitive-period scheduler | `q<=13` primitive decks split residual Q-witness from covering | direct witness route | q=14 boundary layer | cached full-bank primitive deck |
| HYP-3041 AP-tail puncture atlas | AP-core one-tail rows split by `q=13` puncture or reciprocal fixed point | AP-tail family route | `m mod 13` clock | search two-tail analogues before Fejer/THM-572 |
| HYP-3042 owner-strip filtration | post-status pages from primitive deck to Haar zeta to endpoint-owner strip current | residual necessary condition | first surviving page | cached full-bank filtration ledger |
| HYP-3037 residual capacitors | two residual pairs cut by topology or exact scale | finite mixed-route cut | family proof | family capacitor audit |
| HYP-3038 q=23 square | exact-M zeta and endpoint-owner strips expose petal/covering diagonal | square interaction | scalar magnitude height | more drop/add square audits |
| HYP-3021/HYP-3022 pair-good decoys | generator teeth and barcode/normal-fan overlap explain decoys | local blocker generation | raw count meaning | classify by generator grammar |
| HYP-3015/HYP-3018 barcode/normal fan | bars and active bottleneck supports localize positive rows | local owner geometry | theorem route | prove stalk/barcode descent |
| HYP-3023 automatic zipper | automatic words mix routes; exact magnitude cocycle is route-pure in bank | sequence shadow telemetry | exact scale | family magnitude-cocycle lemma |
| HYP-3009/HYP-3012 Moser/fibbinary/lacunary | automatic languages mix all residues and need packet labels | carry/gap state | boundary/open status | keep as sidecars only |
| HYP-3032 analytic bridge | `mu^2/phi` is a capacity meter with blindness report | analytic capacity | prime-power/family route | packet-keyed analytic zipper |
| HYP-2995/HYP-2997 cocycle atlases | forgotten coordinates become cochains or named residuals | obstruction class | row identity | implement `omega_Q` manifest |
| Carrier pullback index CPI-* | `90` cross-surface carrier rows | source-to-LRC translation | proof if uninstantiated | instantiate rows with status/route tests |
| Incoming Pincherle/operator forum cue | operator and distribution analogies for hidden lifts | analogy grammar | LRC predicate | translate only as sidecar/certificate law |

## Pair-Good Decoys And Active Owners

Pair-good decoys should no longer be counted as a threat population.  They are
generated by local modular blocker teeth:

```text
14 * min(c*p mod q, q - c*p mod q) < q.
```

That says the generator class is the object.  The count is secondary.  The
barcode/normal-fan work then says where the generated false switch sits:
outside a strict bar, inside a zero-bar AP/GW boundary row, overlapping an
active bottleneck support, or disjoint from it.  The active bottleneck owner is
therefore the local owner of the decoy's repair obligation.  This connects the
pair-good lens directly to safe-component stalk descent and residual first
teeth:

```text
pair-good generator
  -> blocker deck
  -> barcode / normal-fan incidence
  -> active owner strip
  -> first residual tooth.
```

So the proof should not try to upper-bound pair-good decoys first.  It should
classify their generator grammar and show each grammar routes to a known owner
strip, boundary atom, or named residual.

## Sequence And Automaton Shadows

Moser-de Bruijn, fibbinary/Zeckendorf, Ostrowski-Hadamard lacunary support,
Fermat-Catalan power guards, Hurwitz doubling states, and Mahler-style
distribution analogies all behave the same way in this map.  They are useful
telemetry fields, but unsafe quotient fields.  They preserve carry/gap state
and sometimes expose no-lift or growth restrictions; they do not preserve
AP/GW boundary status or theorem route unless exact magnitude, topology, owner,
period, and packet family sidecars remain attached.

This is the most important warning from the automaton lens:

```text
automatic language class
  != proof carrier
unless exact scale + topology + owner + route handoff are present.
```

The AP-tail puncture atlas is the current cleanest example of why this matters.
The coarse largest-stalk lens remembered the mod-14 owner strip but forgot the
`m mod 13` clock.  Reattaching either the `q=13` puncture bit or the reciprocal
fixed-point tail certificate made the two coarse residual fibers route-pure.
That is not a new proof sector; it is a controlled-forgetting repair.

The owner-strip filtration pushes that lesson into a necessary-condition
ladder for residuals: after status is protected, a surviving packet should
pass through primitive safe deck, AP-tail clock, Haar/drop-add zeta, and
endpoint-owner strip current before it is allowed to become named F7/THM-572
debt.  This turns "owner strip" into a page of the proof complex rather than
only a local label.

## External Analogies

The newer operator/distribution cue from the shared forum post belongs in the
external analogy lens.  Pincherle derivatives, Meissel-Mertens constants in
arithmetic progressions, and Mahler power-sequence distribution language may
suggest operator-side hidden-lift grammar, but none of them is LRC evidence
until translated into a retained packet predicate and a destroyed-coordinate
ledger.

The same guardrail applies to perfect numbers, unit-distance graphs,
irreducibility/convolution, triangular/Faulhaber lanes, Goldbach/Fibonacci
representation economies, and Poincare/worldline frames.  They are valuable
only after they declare:

1. which LRC predicate survives;
2. which coordinate is destroyed;
3. which sidecar reconstructs or discharges it;
4. where failures route.

## Tournament Analysis

Assumption challenge: vertices need not be runners.  For this map, considered
vertices include runners, gaps, fixed circle sections, section boundaries,
wall-crossing events, residues, cover arcs, Fourier modes, Haar rectangles,
matroid topes/cocircuits, proof obligations, sidecar fields, and lens
families.

The chosen tournament vertices are lens families / proof obligations.  The
pairwise observable is:

```text
(boundary/open retention,
 route schedulability,
 owner/topology retention,
 period/scale retention,
 dual-certificate strength,
 named residual handoff,
 compression,
 formal checkability).
```

Switch rule: `A -> B` if `A` preserves more of the LRC predicate with fewer
unnamed destroyed coordinates.  Ties follow the Hamiltonian path:

```text
packet_sheaf
  > boundary_topology
  > owner_stalk_barcode
  > period_arithmetic
  > harmonic_certificate
  > local_switch_decoy
  > automaton_sequence
  > external_analogy
  > raw_scalar.
```

This tournament preserves boundary/open status, route schedulability, and
named residual handoff.  It destroys row enumeration, runner identity, and raw
scalar ranking.  That is intentional: the map is a proof-obligation scheduler,
not a certificate.

## Agent Work Hooks

When adding a new LRC lens, add these fields to the relevant note, script
methodology, or packet ledger:

```text
lens_family
preserved_lrc_predicate
destroyed_coordinate
required_sidecar
handoff_target
status_mixing_result
route_mixing_result
tournament_vertex_choice
challenged_assumption
```

When instantiating a carrier, run at least the status and route checks:

```text
raw_lens_key
  -> status fibers
  -> route fibers
  -> first leaking coordinate
  -> sidecar repair
  -> named theorem target.
```

The current best theorem target is the **lens unification principle**:

> For every primitive LRC14 packet, any quotient produced by a lens in this
> map is legal only if it preserves boundary/open status and theorem-route
> schedulability directly, or exposes a named sidecar whose first nonzero
> coordinate routes to AP/GW boundary H1, a Q-witness primitive deck, an
> AP-tail puncture/fixed-point clock, an owner/stalk/barcode descent, an
> owner-strip filtration page, a capacitor/zeta cut, a packet-family
> certificate, or F7/THM-572/harmonic residual debt.
