---
id: THM-3737
title: "Russell-cylinder exceptional-quartic Jacobian image hyperplane"
status: >
  PROVED + VERIFIED-EXACT + INDEPENDENTLY HOSTILE-AUDITED.  For the
  exceptional-quartic restriction algebra S of THM-3703, the actual-target
  linearized Jacobian image is exactly one retained-value hyperplane in
  K[x].  Its cokernel is the single functional Lambda, and its kernel has an
  explicit free K[Z]-basis with Apéry degrees (0,19,2,...,17).  A nonzero
  150-square determinant in a good split reduction certifies the required
  characteristic-zero minor; modular failure is not used as evidence.
  Consequently every later stable equation has one exact scalar solvability
  gate.  This does not construct a coherent all-order series, global pair,
  Keller map, or JC(2) counterexample.
source: jc_zero_debt_lift / exact positive-minor saturation, 2026-08-23
audit: >
  PASS AFTER THREE TYPING REPAIRS -- an independent replay matched the
  pinned normal, optimized, and stored transcript bytes.  Separate mod-137
  elimination recovered the selected rank-150 determinant 73, full rank
  150, and hostile cutoff rank 69.  The audit independently checked the
  retained functional, free K[Z]-kernel basis and codimension, positive-minor
  direction, filtered-to-global saturation, Taylor factorials, and the
  stagewise (n+1) scalar gate.  It repaired only the distinction between
  restrictions and image polynomials, the representative-level typing of
  normal sidecars, and the explicit gamma_0 inputs to L_0; no mathematical
  conclusion changed.
depends_on:
  - THM-3683-russell-cylinder-sixth-debt-quartic-on-the-zero-fourth-parabola
  - THM-3687-russell-cylinder-exceptional-quartic-actual-j0-lift
  - THM-3703-russell-cylinder-exceptional-quartic-sagbi-module
related:
  - THM-3677-russell-cylinder-degree-eight-fourth-debt-parabola
  - THM-3688-russell-cylinder-exceptional-quartic-actual-j1-j2-lift
  - THM-3629-russell-cylinder-positive-global-pair-to-jc2-counterexample
script: 04-computation/jc2_russell_cylinder_exceptional_quartic_jacobian_image_hyperplane_thm3737.py
output: 05-knowledge/results/jc2_russell_cylinder_exceptional_quartic_jacobian_image_hyperplane_thm3737.out
script_sha256: 4bbae46df140fbcda30b747f6356538a74fea34b928ed396e481e196fd0303c9
output_sha256: e97bb6b47f147b5dac5fff8ceb4deab87e8cd85a902d4bb8a1397c1ccecbd996
hash_basis: raw LF bytes
---

# THM-3737 -- the actual Jacobian image has one scalar cokernel

**PROVED + VERIFIED-EXACT + INDEPENDENTLY HOSTILE-AUDITED.**  The
restriction-ring obstruction left implicit by the raw coupled solves has a
global one-functional description.  At every stable order, actual-target
solvability is equivalent to one retained-value identity.

All rings below have characteristic zero.

## 1. Field, restriction algebra, and image module

Retain the irreducible quartic field and exceptional fold of THM-3683,

```text
K=Q[alpha]/(72783360 alpha^4-77822208 alpha^3-28419741 alpha^2
                                      +7849770 alpha-1276420).
```

THM-3703 gives the actual-target restriction algebra

```text
S=K[B,C,E] = direct_sum_(r=0)^17 K[Z]p_r subset K[x],
deg Z=18.                                                   (1)
```

Here `Z` is monic, and the displayed summands are a free module.  Define the
linearized Jacobian operator and its image

```text
L_0(F,G)=-E'F+C'G,
I=L_0(S^2)=C'S+E'S subset K[x].                            (2)
```

The sign in the second equality is immaterial because `S` is a vector
space.  THM-3687 supplies actual `F_1,G_1 in S` satisfying

```text
C'G_1-E'F_1=1.                                            (3)
```

Thus `S subset I`.  It follows from `(1)--(3)` that `I`, as a `K[Z]`-module,
is generated by the following `54` explicitly typed image polynomials:

```text
p_r, C'p_r, E'p_r                 (0<=r<18).              (4)
```

Here `p_r in S` is an actual restriction and lies in `I` because `S subset
I`, while

```text
C'p_r=L_0(0,p_r),                 E'p_r=L_0(-p_r,0).
```

The latter two are images of actual restrictions; they need not themselves
belong to `S`.  This distinction is load-bearing because `I` is a module of
source polynomials, not a submodule of the restriction algebra.

The object in `(2)` is a `K[Z]`-module, not an ideal of `K[x]`; no ideal
closure is used below.

## 2. The retained functional contains the image

At the retained points `x=-1,0,1`, every element of `S` has one common
value, because `(B,C,E)=(0,0,-3)` at all three points.  Exact differentiation
gives

```text
(C'(-1),C'(0),C'(1))=(3,3,3),
(E'(-1),E'(0),E'(1))=(-9,4,9).                           (5)
```

Define

```text
Lambda(P)=5P(-1)/18-P(0)+13P(1)/18.                      (6)
```

Both weighted rows in `(5)` vanish under `(6)`.  Hence

```text
Lambda(C's)=Lambda(E's)=0                for every s in S,
I subset ker Lambda.                                      (7)
```

This is a genuine codimension-one containment: `Lambda(1)=0` but
`Lambda(x)=4/9`.  Moreover `Z(-1)=Z(0)=Z(1)=0`, so

```text
Lambda(h(Z)P)=h(0)Lambda(P)              (h in K[Z]).     (8)
```

In particular `ker Lambda` is itself `K[Z]`-stable.

## 3. An explicit basis of the retained hyperplane

Since `Z` is monic of degree `18`, ordinary division gives

```text
K[x]=direct_sum_(r=0)^17 K[Z]x^r.                        (9)
```

For positive powers, direct evaluation in `(6)` gives

```text
Lambda(x^r)=4/9  if r is odd,
Lambda(x^r)=1    if r is even.                           (10)
```

Equations `(8)--(10)` give the following free `K[Z]`-basis in residue order:

```text
1,
xZ,
x^r-x             (r=3,5,...,17),
x^r-(9/4)x        (r=2,4,...,16).                        (11)
```

Indeed, subtracting the appropriate multiple of `x` kills the constant
`Z`-coefficient in every residue `r>=2`; the remaining coefficient of `x`
must be divisible by `Z`, and is therefore generated by `xZ`.  The Apéry
degrees of `(11)` are

```text
(0,19,2,3,...,17),                                      (12)
```

so the codimension is

```text
dim_K K[x]/ker Lambda
  =sum_(r=0)^17 (a_r-r)/18=1.                            (13)
```

In particular the degree-at-most-`150` part of `ker Lambda` has dimension
`150` inside the `151`-dimensional ambient polynomial space.

## 4. Positive-minor saturation over the quartic field

It remains to prove that every generator in `(11)` actually belongs to the
image module `I`.  The companion builds the `54` generators `(4)` exactly
over `K`, takes every `Z`-shift of degree at most `150`, and obtains `165`
columns in `K[x]_(<=150)`.

Use the split good reduction

```text
p=137,                         alpha=44 mod 137.          (14)
```

The leading coefficient of the quartic and every denominator in the chosen
matrix are units modulo `137`, and the defining quartic vanishes at `44`.
Thus entrywise evaluation gives a legitimate reduction of the exact
`K`-matrix.  For the first `150` columns and coefficient rows `0,...,149`,
the frozen square has

```text
det =73 mod 137 !=0.                                     (15)
```

The exact square hash is

```text
49756c9bd390c102cd181c9dc3c573eaf33602ab14033d1801e0419b6cee5c06. (16)
```

The logical direction in `(15)` is only the sound positive-minor direction:
if the exact determinant in `K` were zero, every good reduction would be
zero.  Therefore the selected exact determinant is nonzero, and the `150`
selected image polynomials are `K`-linearly independent.  They all lie in
`ker Lambda cap K[x]_(<=150)` by `(7)`, whose dimension is exactly `150` by
Section 3.  Hence

```text
I cap K[x]_(<=150)=ker Lambda cap K[x]_(<=150).           (17)
```

All `18` generators in `(11)` have degree at most `19`, so `(17)` places
every one of them in `I`.  Both modules are `K[Z]`-stable, and `(11)`
generates the whole right side.  Combining this with `(7)` proves the global,
cutoff-free identity

```text
boxed: L_0(S^2)=I=ker Lambda.                             (18)
```

As a hostile insufficient-cutoff control, the same deterministic grammar at
cutoff `100` supplies only `69` columns in an ambient space of dimension
`101`; their good reduction has rank `69`, leaving ambient codimension `32`.
Thus that truncation cannot possibly supply the `100` dimensions of the
filtered kernel.  This control is not used to infer any characteristic-zero
rank failure.

Because `(18)` is an identity over the irreducible field `K`, it remains true
under each of its four complex embeddings.  The result is therefore uniform
over all four exceptional roots, not a statement about one numerical root.

## 5. The all-order stagewise scalar gate

In the established stable-expansion notation, fix actual target
representatives for every coefficient and define the typed normal sidecar

```text
gamma_k(H)=gamma(partial_q^k H).
```

This notation is attached to the chosen actual representative `H`; normal
differentiation need not descend to an operation on its restriction class in
`S`.  The pulled source coefficients of two actual target series are then

```text
a_m=sum_(0<=k<=m/2) gamma_k(F_(m-2k))/k!,
b_m=sum_(0<=k<=m/2) gamma_k(G_(m-2k))/k!.               (19)
```

Their Jacobian coefficient is

```text
[t^n]J=sum_(i+j=n+1)(j a_i'b_j-i a_i b_j').             (20)
```

After all earlier target coefficients have been fixed, the new pair
`F_(n+1),G_(n+1)` occurs in `(20)` only through

```text
(n+1)L_0(gamma_0(F_(n+1)),gamma_0(G_(n+1))).              (21)
```

Consequently, if `D_n` denotes the already determined debt and `c_n` the
desired coefficient, an actual next target coefficient exists exactly when

```text
Lambda(c_n-D_n)=0.                                      (22)
```

For the Keller normalization `c_0=1` and `c_n=0` for `n>=1`, every later
step therefore has one scalar gate `Lambda(D_n)=0`.  THM-3677 and THM-3683's
second-, fourth-, and sixth-debt identities are now seen as calculations of
this exact cokernel coordinate, while THM-3688 proves one compatible actual
lift through `J_2`.

Equation `(22)` is stagewise sufficiency only.  It does not say that the
scalar gates vanish at every order, that one can choose a coherent infinite
series with controlled degrees, or that such a series algebraizes.

## 6. Reproduction and strict boundary

From the repository root:

```bash
python3 -B 04-computation/jc2_russell_cylinder_exceptional_quartic_jacobian_image_hyperplane_thm3737.py
python3 -O -B 04-computation/jc2_russell_cylinder_exceptional_quartic_jacobian_image_hyperplane_thm3737.py
```

Normal and optimized runs must byte-match the stored LF transcript.  The
script contains no Python `assert` statements.  It checks the field relation,
all retained evaluations, the exact containment, the explicit basis, every
good-reduction denominator, the determinant `(15)`, and the hostile cutoff.

This theorem proves neither the later scalar identities nor an all-order
solution, positive global pair, noninjective Keller map, or counterexample to
the planar Jacobian conjecture.  **QED.**
