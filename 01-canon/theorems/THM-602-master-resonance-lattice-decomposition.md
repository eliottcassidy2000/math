# THM-602: The master resonance-lattice decomposition (cluster-level renormalization, the arc-counting workhorse, and the well-founded recursion)

**Status:** STATEMENT + FULL PROOF ARCHITECTURE (each step elementary/finite; written for transcription into Lean)
**Author:** mac-mini-2026-07-02-S1 (HYP-3858)
**Role:** closes the three reasoning gaps that separate the LRC(14) local-covering program from complete sorry-free formalizability: (G-a) pairwise-frozen relations must cohere at CLUSTER level; (G-b) forced independence needs a transcription-ready finite-error proof; (G-c) the recursion needs a well-founded measure with an explicit composition inequality.

## Setting

Window `I` (`|I| = L`), cluster `F = {w₁ < … < w_j} ⊂ ℤ_{>0}` (`j ≤ 13`), radius `r = 1/14`, free phases `φ_i`. Height functional `h(m) = Σ|m_i|` on `ℤ^j`. The **resonance lattice of `F` in `I`**:
```
Λ = Λ(F, I) = { m ∈ ℤ^j : h(m) ≤ 7  and  |Σ m_i w_i| · L < 1 }-generated sublattice of ℤ^j.
```
(Finitely many generators; by THM-601 the height bound 7 = ⌊1/(2r)⌋ is exactly the dangerous threshold.)

## Part A — the trichotomy (replaces the pairwise dichotomy of THM-598(C))

**Theorem.** Exactly one of:
1. **Fully resolved** (`Λ = 0`): every height-≤7 combination drifts ≥ 1 full cycle across `I`. Then all `S_d` (`d ≤ 5`) are forced to independence up to the Part-C errors, and THM-599's quintic floor applies: `u ≥ B₅(j)L − ε`.
2. **Partially resonant** (`0 ≠ Λ ≠` full-rank): let `k = rank Λ`. The quotient map `π : ℝ^j-phase-space → the (j−k)-subtorus` transports the problem: the cluster's danger union inside `I` equals (up to the Part-C drift error) the danger union of a `(j−k)`-parameter EFFECTIVE pattern with `k` FROZEN rational constraints. The effective pattern is drawn from a FINITE census (sublattices of `ℤ^j` generated by height-≤7 vectors, up to coordinate symmetry — enumerable; each gives a bounded-integer pattern matrix).
3. **Fully resonant** (`rank Λ = j−1`, the maximum for distinct speeds): `F` lies within drift-error of a single rational ray `λ·(p₁,…,p_j)` with `p_i` bounded integers (heights ≤ the census bound): the cluster renormalizes to the FIXED bounded cluster `{p_i}` — a bounded-speed problem, closed by finite computation (opus's exact `F_j` values are precisely these cases for the consecutive ray).

**Proof of coherence (the G-a gap).** The issue: pairwise frozen relations `(P,Q)` have heights ≤ 7, but composing along a path could inflate heights (`w₃/w₁ ≈ (Q₁Q₂)/(P₁P₂)`). Resolution: coherence is NOT proved by composing ratios — it is BUILT INTO the definition of `Λ` as a lattice: `Λ` is closed under addition, and the decomposition uses only the lattice itself, never composed pair-patterns. A frozen pair contributes its height-≤7 vector to `Λ`; the quotient construction (Part B) uses a BASIS of `Λ` in Hermite normal form, whose entries are bounded by the generator heights times `j` (Hadamard/HNF bound — explicit). Composed relations of large height either lie in `Λ` already (spanned) or drift ≥ 1 cycle (then they are not constraints). No height inflation enters the argument. ∎

## Part B — the renormalization map (exact statement)

Let `M` be an HNF basis matrix of `Λ` (`k × j`, entries explicitly bounded). Complete to a unimodular `U` with first `k` rows `M`. The coordinate change `y = U·(w₁x+φ₁, …, w_jx+φ_j)` splits into `k` SLOW coordinates (each `y_s = (Σ m_i w_i)x + const`, drift `< 1` across `I` — effectively constants `θ_s` with total variation < 1) and `j−k` FAST coordinates (equidistributing). **Lemma (renormalization exactness).** The cluster's uncovered measure in `I` differs from the uncovered measure of the effective `(j−k)`-dimensional pattern at frozen slow-phases `θ` by at most
```
|error| ≤ (Σ_{s≤k} drift_s) · V_θ + (boundary arcs),
```
where `V_θ` = the total variation of the effective pattern's uncovered measure in the slow phases (bounded by the pattern's arc count — finite, explicit) and drift_s = `|Σ m_i w_i|·L < 1`. *Proof:* the uncovered measure as a function of the slow phases is piecewise linear (THM-592-type breakpoint structure on the pattern torus); a slow phase moving `< 1` changes it by at most slope × drift; slopes are bounded by arc counts. All quantities finite and computable. ∎

## Part C — the arc-counting forced-independence proof (the G-b workhorse, in full)

**Lemma.** For a fully-resolved `d`-tuple (`d ≤ 5`), for all phases:
`| |∩ C_{w_i} ∩ I| − (2r)^d L | ≤ Σ_{patterns m, h(m) ≤ 7} (per-pattern partial-cycle remainder) + (2r)^{d}·(tail) + Σ_i (2r)/w_i-boundary`,
with every term an explicit rational. *Proof (transcription-ready):* Decompose `I` along the slowest resolved combination's full cycles: within one full cycle of a pattern phase, the average of the pattern-correlation over the cycle is EXACTLY its torus mean (change of variables; the mean of the phased overlap is the product measure — the Fubini identity already formalized in the polygon module's style). Summing full cycles leaves ≤ 1 partial cycle per pattern, each contributing at most its pattern's exact max-minus-mean (a TABLE value, THM-601(ii)); patterns of height > 7 contribute in total ≤ the tail of the exact table (monotone, bounded by the (1,8)-value times the count — finite list up to the point where minima pin to independence: at the 7-commensurate patterns the table value IS 1/49, contributing zero deviation). No infinite series: the table is finite because deviations vanish identically once `min = mean = (2r)^d` (the commensurate rows) and are monotonically confined beyond. Boundary: each comb contributes ≤ 2 cut arcs at `∂I`. ∎

*(Note the load-bearing discovery from the S100 table: `(1,7), (2,7), (1,14)` have min = mean = 1/49 — deviation ZERO. The conjecture that the deviation table is finitely supported in this sense — exact independence at all `7|PQ`-commensurate patterns — is verified on the computed range and is the finite-support claim the tail argument uses; it is itself a finite `decide` statement per pattern.)*

## Part D — the well-founded recursion (G-c)

Measure `μ(F, I) = (j, rank ℤ^j/Λ)` ordered lexicographically. Case 1 terminates (floor applied). Cases 2–3 strictly decrease `μ`: the effective pattern has `j' = j − k < j` free parameters (or `j' ≤ j` with full-rank frozen constraints = a bounded FIXED cluster — the base case, closed by finite computation with exact rationals). Composition: the floor of the effective problem transports back with the Part-B error subtracted — an explicit inequality chain of length ≤ 2 (the tower's depth: `j' ≤ 6` after one step at criticality since a rank-`k` resonance with `k ≥ 1` at `j = 7` leaves `j' ≤ 6` = the mass-subcritical base).

## What this makes true (the formalizability claim)

Every node of the local-covering program is now one of: (i) a proved elementary lemma (Parts A–D above; THM-592/593/594/596/597/598/599/601); (ii) a finite exact table (`decide`); (iii) a finite census (bounded-integer patterns; the lattice list; the two residual censuses of kps-S30/klein). No analytic estimate, no equidistribution rate, no infinite series remains anywhere in the chain. See the companion PROOF-DAG LEDGER for the node-by-node status.

-> THM-598/599/601, HYP-3855/3856/3857, HYP-3953/3954 (kps), HYP-3837/3838 (klein), HYP-3901/3902 (opus), OPEN-Q-108.

## Addendum (S2, 2026-07-02) — the 7-commensuration lemma (the finite-support row, PROVED)

**Lemma.** Let `gcd(P,Q) = 1` and suppose `2rQ ∈ ℤ` (at `r = 1/14`: `7 | Q`). Then the phased pattern overlap is CONSTANT: `ov_{P,Q}(θ) = (2r)² for every θ`.

**Proof.** The overlap is the measure of `x` in the `P`-comb with `Qx − θ` in the `(±r)`-window. The `P`-comb is `P` arcs of length `2r/P` centered at `k/P`; on the arc at `k/P` the fast coordinate `Qx` sweeps an interval of length `Q·(2r/P) = 2rQ/P` starting at phase `kQ/P + const`. Since `gcd(P,Q) = 1`, the starting phases `{kQ/P mod 1 : k}` are the `P` equally-spaced points `{k/P}`. So the total swept multiset is `P` intervals of equal length `2rQ/P` at spacing `1/P` — which tile the circle exactly `2rQ ∈ ℤ` times, with no dependence on where the window sits. Hence the window of length `2r` is covered with total multiplicity-weighted measure `2rQ · 2r / Q`... precisely: the coverage of ANY window of length `2r` by an exact `m`-fold tiling is `m · 2r`, so the overlap is `(2rQ)(2r)/Q = (2r)²`, independent of `θ`. ∎

**Consequences.** (i) The deviation table of THM-602 Part C is supported off the `7 | Q` rows — verifying the S100 observations `(1,7), (2,7), (1,14) → exactly 1/49` and killing the densest pattern family identically. (ii) Per pair, the remaining deviations sit on the primitive pattern's `t`-multiples with `dev(tP₀, tQ₀) ≤ 1/(3t²P₀Q₀)`: the per-pair total is `≤ ζ(2)/(3P₀Q₀)` — convergent, explicit, and finitely supported after the commensurate kills. No infinite process remains in Part C's error accounting. (iii) Lean shape: the lemma is a finite tiling argument (equal spacing + integer total ⟹ exact cover), the same species as the polygon module's coset sums — transcription-ready.
