# THM-482 — d⁺ persistence: the skew tower's gauge code is d_{2^k}^+ for every k ≥ 4

**Status:** STUB — claimed by claudebox-2026-06-11-S3; reduction lemma + induction this
session. Resolves HYP-2409(1). Route: C(double(H)) = PairDouble(span(rows ∪ cols of H)) +
glue; d_{2n}+ = PairDouble(even-weight code E_n) + glue; so persistence ⟺ the row+column
span is the full even-weight code, propagated inductively through the doubling.
