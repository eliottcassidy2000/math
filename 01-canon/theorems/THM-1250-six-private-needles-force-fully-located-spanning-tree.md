---
id: THM-1250
title: SIX PRIVATE NEEDLES FORCE A FULLY LOCATED SPANNING TREE — every Hunter edge is paid by an actual tooth handoff inside the slow gap
status: RESERVED/PROOF IN PROGRESS. THM-1198's six private regions, the irredundant interval-chain extraction, five gcd-quantized handoffs, the exact Hunter consumer, referee, and Lean arithmetic core are being prepared. No LRC(14) closure is claimed.
source: codex-2026-07-19-S78 continuation with owner-reuse and located-tree audits
depends_on: [THM-1166, THM-1198, THM-1244]
related: [THM-1237, THM-1240, THM-1248, HYP-7870]
---

# THM-1250 — fully located spanning tree

Let the six strict danger combs of `c<d1<...<d6` cover a complete closed
`c`-safe gap `G`.  THM-1198 gives every label a positive private-provider
region in `G`.  Consequently every irredundant finite tooth subcover uses all
six labels.  Its chronological consecutive-overlap graph is connected, so it
contains a spanning tree `T` on the six fast labels whose five edges are
realized by actual tooth overlaps in `int(G)`.

The reserved target proves for every `uv in T` that

```text
mu(G intersect D_u intersect D_v)>=gcd(u,v)/(14uv),
```

and combines these five located credits with interval Hunter and the sharp
singleton discrepancy to obtain

```text
sum_i 1/d_i
 >=1/c+(49/6) sum_(uv in T) gcd(u,v)/(14uv).
```

Unlike the global positioned-tree transfer, this local spanning tree pays no
pair-period positioning errors.  What remains is to couple one of its
chronological orientations to the centered blocker's finite relative-address
word and positive holonomy.
