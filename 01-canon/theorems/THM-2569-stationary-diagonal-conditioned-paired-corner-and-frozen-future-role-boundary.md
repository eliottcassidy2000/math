---
id: THM-2569
title: "Stationary-diagonal conditioned paired corner and frozen-future-role boundary"
status: >
  PROVED + VERIFIED-EXACT + INDEPENDENTLY AUDITED.  Condition THM-2563's arbitrary positive old-head
  weight on THM-2565's same-root stationary return.  On every live row this
  produces one positive common-ancestry packet containing the old
  target-informed head, a genuinely later target-active k_a occurrence with
  the same physical root, a translated deepest probe, and both lawful
  moving-endpoint blocker--graft dipoles.  If rho is the original head mass,
  the conditioned partial-bare table has mass at least 63 rho^2/26 and a
  primitive-deep/nonzero moving-dipole coefficient with normalized floor
  7 rho^2/913952, or 9 rho^2/913952 when both graft roles are ordinary.
  Nevertheless the stationary Hall diagonal is only a multiplicative
  spectator under endpoint completion.  The old k_a-danger and moving
  k_a-safe masks are orthogonal on every common lawful twist, so THM-2568
  annihilates their full-X recombination in every coarse target character,
  even root-stratum by root-stratum.  The equality of old/future physical
  digits h=b supplies neither the left relation residue eta.u nor a fixed X,
  normal, or jet.  Thus positive common support is proved, but no THM-2334
  current, scalar-row exclusion, or LRC(14) conclusion follows.
source: lrc-semantic-frontier-2026-07-27-stationary-paired-corner
depends_on:
  - THM-2452-indicator-idempotent-aggregate-endpoint-restoration
  - THM-2563-paired-dipole-deep-target-corner-and-partial-bare-boundary
  - THM-2565-target-active-self-return-and-future-root-overlap-audit
  - THM-2568-full-x-transition-annihilation-and-refined-pair-drift-boundary
related:
  - THM-2334-relation-residue-current-and-character-twist-pushforward
  - THM-2383-polarized-complete-subcube-gram-tomography
  - THM-2538-anchored-transverse-gain-and-common-ancestry-arrival-boundary
  - THM-2545-word-stratified-hall-arrival-criterion-and-owner-word-transportation-hostile
script: 04-computation/lrc14_stationary_diagonal_paired_corner_thm2569.py
output: 05-knowledge/results/lrc14_stationary_diagonal_paired_corner_thm2569.out
script_sha256: ea3f5e41d4ce7793d150d2d8f3d9fcb848b88616182ec0d489a359d2fd295581
output_sha256: bb80f789ef09624e6013a49bfdef21ae083b7798a3e3b5403c8db7457fa77a26
hash_basis: working-tree bytes (LF)
---

# THM-2569 -- the future diagonal and paired corner share support, not action

**PROVED + VERIFIED-EXACT + INDEPENDENTLY AUDITED.**

THM-2565 closes the semantic future-root overlap for THM-2559's
target-informed head.  THM-2563 proves its paired-dipole corner for **every**
positive subpacket of that head.  They therefore compose without another
mixing or selection loss.

The composition is useful but sharp:

```text
same-root future target event + paired moving endpoint
  -> one positive common-ancestry partial-bare packet;

complete the old danger / moving safe endpoint and sum all X
  -> zero in every coarse target character.                    (1)
```

The first line repairs a real common-support debt.  The second proves that
support alone cannot supply the missing endpoint action.

## 1. Condition on the stationary Hall diagonal

Fix one live row, choose a positive THM-2559 slope stratum, and retain
THM-2565 notation

```text
A=T_delta,                     rho=integral_T A>0,

A_h=A 1_(floor(13x)=h),        p_h=integral_T A_h.             (2)
```

Choose `N` beyond the THM-2565 mixing and chronology thresholds and put

```text
w_(N,h)(x)=A_h(x)A_h(T^N x),

w_N(x)=sum_(h in F_13)w_(N,h)(x),

rho_N=integral_T w_N(x)dx.                                   (3)
```

The summands in (3) are disjoint, so `w_N` is a rational Boolean step
function.  THM-2565 gives

```text
rho_N>=rho^2/26>0.                                           (4)
```

Every point of `w_N` retains all the old-head facts

```text
c_3 safe,        a safe,        j dangerous,        k_a dangerous,
                                                                    (5)
```

and also a genuinely later occurrence satisfying

```text
d_(L_a)(k_a T^N x)=1,

floor(13x)=floor(13T^N x).                                  (6)
```

The future role in (6) is categorically target-active by THM-2461, with
`k_a` first in the freely ordered five-role bank.  The fixed terminal word
is retained only as the source-sibling stratum label exactly as in THM-2565;
neither head emits that word.

## 2. The fully paired partial-bare table on the same packet

Apply THM-2563 with its weight `w` replaced by `w_N`.  With

```text
eta=e_a-e_(k_a),              ell=e_(c_3)-e_(k_b),
```

the resulting nonnegative table is

```text
R^N_(r,s,t)
 =integral_T w_N(x)
    d_1(c_3 x-r/13)

    u_1(a x-s/13)u_(L_a)(k_a x+s/13)
    u_1(c_3 x-t/13)u_(L_b)(k_b x+t/13) dx.             (7)
```

Every positive cell of (7) still contains the same-root future event (6),
because that event is part of the fixed weight `w_N`.  THM-2563 applies
verbatim and gives total mass

```text
sum_(r,s,t)R^N_(r,s,t)
 >=63rho_N
 >=63rho^2/26.                                             (8)
```

For the normalized full three-variable transform, some nonzero deepest
colour and nonzero moving `eta`-dipole colour satisfy

```text
Re Rhat^N(A,B,0)
 >=7rho_N/35152
 >=7rho^2/913952,                         one guard,          (9)

Re Rhat^N(A,B,0)
 >=9rho_N/35152
 >=9rho^2/913952,                         both ordinary.     (10)
```

After the unnormalised `t`-marginal, the corresponding bounds are

```text
7rho^2/70304,                  or                  9rho^2/70304.
                                                                    (11)
```

The displayed deepest multiplier remains coprime to `91`.  Equations
(7)--(11) prove more than a tensor product of separately integrated facts:
the semantic future diagonal and the paired corner occur inside the same
integral on one physical circle ancestry.

## 3. Why the Hall diagonal is a full-X spectator

The coefficient in (9) is still a **moving-endpoint** colour.  To turn it
into a THM-2334 target character, one must complete the fixed old endpoint,
co-shift both endpoint copies lawfully, and recombine every physical
frequency `X`.

Refine the two endpoints to complete Boolean masks.  On every cell retained
by (7), the old endpoint has `k_a` dangerous while the repaired moving
endpoint has `k_a` safe.  Hence their complete labels `tau,nu` differ.  For
each common target twist `s`, use THM-2568 notation

```text
L_s=P_s L_s,                 R_s=(1-P_s)R_s.                (12)
```

The stationary Hall packet can be inserted into `L_s` as an additional
multiplier, or resolved first by the common physical root `h`.  Neither
operation changes

```text
w_(N,h)(x) P_s(x)(1-P_s(x))=0.                              (13)
```

Endpoint Parseval therefore gives, for every fixed deepest frequency offset
`m`, root stratum `h`, and target twist `s`,

```text
sum_X Lhat_(s,h)(X) conjugate(Rhat_s(X+m))=0.               (14)
```

Taking the target DFT proves zero in every coarse target character.  In the
independently twisted pair table, the same statement is THM-2568's line-sum
law

```text
sum_(a+b=q) Ahat_h(a,b)=0
                      for every h,q in F_13.                 (15)
```

Summing (15) over the positive Hall diagonal does not change zero.  Thus the
new semantic sidecar survives pointwise as support while the completed
coarse target current annihilates.

This is not caused by freezing the future field.  Even if a common covariant
translate of that field were supplied, it would remain a multiplier of
`P_s(1-P_s)` in (13).  An escape would need a genuinely **relative** endpoint
sidecar before the diagonal pushforward, not another common factor.

## 4. Three coordinates printed in the same alphabet

The apparent shortcut identifies three different `F_13` coordinates:

| coordinate | actual definition | information retained |
|---|---|---|
| `h` | `floor(13x)` | old physical inverse-branch digit |
| `b` | `floor(13T^N x)` | immediate future physical digit |
| `eta.u` | left endpoint relation residue | target charge before the pair-to-coarse line sum |

THM-2565 proves `h=b` on (3).  It constructs no map from either digit to
`eta.u`, and therefore proves neither `eta.u=0` nor a value of the difference
`eta.(u-v)`.  Identifying them because all lie in `F_13` would repeat the
typed-anchor error in MISTAKE-260.

Likewise, the physical Fourier frequency `X` in (14) is a summation index of
endpoint coefficients, not a pointwise finite label on `w_N`.  A stationary
Boolean refinement of (3) cannot select an `X` without an additional spectral
construction.  The Hall diagonal supplies no normal/jet weight and no
oriented endpoint reference.

## 5. Sharp tensor hostile

The separation already has a finite exact model.  On the root square take

```text
D(h,b)=1_(h=b),                                             (16)
```

a perfect positive semantic Hall diagonal.  On the independently twisted
endpoint square take THM-2568's complementary-mask table

```text
C(s,t)=1_(s!=t).                                           (17)
```

The tensor `D tensor C` has positive root-diagonal mass and positive refined
endpoint mass.  Nevertheless `C(s,s)=0`, so every pair-to-coarse line sum is
zero, separately for each `h=b`.  Thus even a perfect semantic diagonal does
not supply a target reference.

This is an abstract finite-group hostile, not a scalar-cover row.  Its role is
to prove that the stopping boundary is structural rather than a weakness of
the quantitative floors in (8)--(11).

## 6. Exact gain and remaining obligation

The new composition ledger is

```text
old target-informed head
  + stationary same-root future k_a event
  + deep probe and both moving blocker--graft dipoles
  -> positive common-ancestry partial-bare coefficient;      PROVED

same packet
  + complete danger-to-safe endpoint
  + full-X pair-to-coarse pushforward
  -> zero in every target character.                         PROVED (18)
```

Consequently the next object must stop before the annihilating sum.  It must
construct one of THM-2568's escape sidecars—one lawful fixed `X`, a physical
nonconstant normal/jet weight, or an oriented reference—and retain it on the
same packet as (6) before the pair-to-coarse pushforward.  Merely multiplying
by more owner, word, root, or common chronology factors cannot change (13).

The theorem does not prove THM-2537 equation (56), make the head inherit a
terminal word, or produce a THM-2334 current.  No row is excluded and LRC(14)
remains open.

## 7. Exact companion

Run

```bash
python3 04-computation/lrc14_stationary_diagonal_paired_corner_thm2569.py
python3 -O 04-computation/lrc14_stationary_diagonal_paired_corner_thm2569.py
```

Both executions byte-match

```text
05-knowledge/results/lrc14_stationary_diagonal_paired_corner_thm2569.out.
```

The dependency-free referee checks the inherited rational floors, all
`212,992` stationary-weight Boolean orthogonality cells, all `2,197`
pair-to-coarse character sums, and the perfect-Hall-diagonal tensor hostile.
The live-row common-support theorem is the exact composition of THM-2563 and
THM-2565; the full-X no-go is THM-2568 applied root-stratum by root-stratum.

The independent common-endpoint audit checked the composition quantifiers,
Boolean disjointness of the root pieces, every propagated floor, and the
root-stratumwise application of the full-`X` annihilation.  Normal and
optimized runs, stored output, byte hashes, and bytecode compilation all
passed; no mathematical defect was found.

**QED.**
