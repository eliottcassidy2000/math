# THM-1835: the abstract single-character theorem, the 0-unstable atom stratification (two corrections to THM-1830-kp), and the complement-reversal parity lemma

**Status:** DELIVERED. (I) PROVED at working grade (the I0-reduction is an
identity, verified both worlds; the abstract theorem rides the named
axioms). (II) VERIFIED corrections (court case CASE-thm1830-atom-range
filed). (III) PROVED (two-line lemma) + verified. HYP-8630.
**Author:** boxeph-2026-07-20-S192
**Owner targets:** the functional-agnostic both-signs single-character
nonvanishing; n >= 13 two 3-cycle atoms; blue parity as SC/complement lore.

## I. The abstract both-signs single-character theorem

SETTING: a character-graded commutative algebra, L a functional with
(A1) CHARACTER HOMOGENEITY: L kills every nonzero total charge;
P = aX + b + cY, X in charge d, Y in charge -d, b central (single
character, both signs iff ac != 0).

**I0-REDUCTION LEMMA (identity; verified exactly m <= 15 in BOTH proved
worlds — E with factorial weights, CT with flat weights):**
  L(P^m) = sum_i m!/(i!i!(m-2i)!) (ac)^i L((XY)^i b^{m-2i}),
equivalently sum_m L(P^m) t^m/m! = L0(e^{tb} I0(2t sqrt(ac·XY))) — the
S174 clean form, now functional-agnostic. The whole single-character
nullcone question lives in the CENTRAL data (L0, XY, b).

**THEOREM (agnostic form).** Assume additionally
(A2) CENTRAL EMP: on the central subalgebra generated by (XY, b), the
     family L0(R_t^m) = 0 for all m forces the deformation trivial (the
     exponential-moment-problem rigidity).
Then all L(P^m) = 0 with both signs forces ac · (XY-mass) = 0: the
single-character both-signs nullcone is EMPTY.
Proof: the reduction lemma turns the moment family into the central
exponential family with deformation parameter ac; (A2) kills it. QED.
INSTANCES: E-side (A2) = THM-1510/1615 (EMP, proved) => the Gaussian case;
CT-side XY = 1, (A2) = the TNC single-pair case (THM-1605, proved) => the
toral case. NEW REACH: any twisted functional with FINITE character
support (the mod-q/spread shape of TTNC instances, THM-1830-boxeph)
reduces to finitely many coupled central families — the named route for
the twisted world.

## II. The 0-unstable atom stratification — with TWO corrections to kp THM-1830

The criterion (kp THM-1825): unstable <=> some char_A-root of multiplicity
> n/2. For reducible T: char_A = prod over SCCs; 0-multiplicity =
#singletons + sum of the ATOMS' OWN 0-multiplicities.

**CORRECTION 1 (verified):** kp's "for 7 <= n <= 12 the ONLY form is one
3-cycle atom" FAILS from n = 9: the witness (5 singletons) + (strong-4
atom) at n = 9 has char_A = x^5(x^4 - 2x - 1): 0-mult 5 > 4.5, unstable,
non-transitive, not of the 3-cycle form. Timeline of ZERO-FREE-atom
strata (sum|atoms| < n/2): C3 at n>=7; C4 at n>=9; C5 at n>=11;
{C3,C3} AND strong-6 at n>=13; {C3,C4} and 7-atoms at n>=15.

**CORRECTION 2 (verified; also corrects this file's own naive timeline):**
the zero-freeness hypothesis FAILS from atom size 6: 720/22320 labeled
strong 6-tournaments and 14/353 strong 7-classes have eigenvalue 0
(sizes 3,4,5 are zero-free: 544/544 strong-5 nonsingular). Hence
SINGULAR-ATOM strata enter EARLIER than the naive count: e.g. n = 11 with
(5 singletons) + (singular strong-6): 0-mult = 5 + 1 = 6 > 5.5 — unstable
with sum|atoms| > n/2. The correct law:
  **unstable-via-0 <=> #singletons + sum(atom 0-mults) > n/2,**
and the zero-free timeline above is exactly the singular-atom-free part.
kp's n = 7, 8 uniqueness SURVIVES (4-atoms need n >= 9; singular atoms
need size >= 6, impossible below n = 9 with the count).
Court case: 02-court/active/CASE-thm1830-atom-range.md.

**The owner's n >= 13 two-atom stratum:** slots N = n - 4; classes =
C(N,2) (36 at n = 13); char x^{n-6}(x^3-1)^2.

## III. The complement-reversal parity lemma (the blue 1/0 law as lore)

**LEMMA.** For a stratum of reducible classes given by placing marked
atoms in the slots of the SCC linear order: the complement functor
reverses the SCC order and complements each atom. Hence a class is
SELF-COMPLEMENTARY (BLUE) iff its slot multiset is reversal-symmetric
with complement-self-paired atoms. COROLLARIES: one-SC-atom stratum
(N slots): blue count = 1 iff N odd, 0 iff N even — kp's law (N = n-2:
1 at odd n, 0 at even n) is the middle-slot existence bit, the exact
class-level shadow of the repo's SC/complement-parity lore (zero-halo
alternation, grid-sym fractions). Two-C3 stratum: blue = floor(N/2)
(= 4 at n = 13; symmetric {3,7} verified SC, asymmetric {1,2} verified
not).

## Files
04-computation/single_character_atoms_parity_boxeph_S192.py + frozen .out.
