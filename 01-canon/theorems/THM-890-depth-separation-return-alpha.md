---
id: THM-890
title: THE DEPTH SEPARATION THEOREM (the return-thickened alpha verdict) — (N) the THM-789 return interval R = (−1/858, 1/858) merges NOTHING: both twins' smallest inter-component gaps (0.0060, 0.0205) exceed 2/858 = 0.0023 by 2.6–8.8×, so the Minkowski-thickening incidence mechanism, as literally constructed on the 12-speed rows, is REFUTED; (D) but the twins ARE separated by DEPTH: the raw alpha-tails diverge past depth ~12 (|diff| = 0.057 at depth ≤ 9 → 0.103 at 12 → 0.190 at 15 → 0.455 at 19, with alpha_19 = −0.950 vs −0.495) — codex's growth question answered: THE SEPARATION GROWS; depth, not thickening, is the separating resource, exactly as finite-gap OPUC predicts (the alpha-tail quasi-period encodes the component count: 20 vs 16 arcs = different isospectral-torus dimensions, so the tails MUST diverge); (G) the gap spectra separate the twins 3.4× directly (smallest gaps 0.00595 vs 0.02045). CAVEAT: the 12-speed row S = 2U₀ ∪ {x,y} is a PROXY for THM-789's exact folded/eroded fingerprint object — the definitive test on their eroded-diamond structure is the remaining step
status: COMPUTED VERDICT (exact arcs; thickening exact; alpha to depth 20; the N-refutation and D-separation both referee-grade on the proxy rows; the folded-object test named open)
source: opus-2026-07-16-S324 (owner: build the return-thickened alpha and see if it separates the twins)
depends_on:
  - THM-886 (the depth-10 near-tie this resolves)
  - THM-883 (the alpha-map), THM-789 (the twins and R)
related: [Simon finite-gap OPUC (torus dimension = #gaps: the mechanism behind D), THM-840 (Markov negatives -- depth is the resource there too)]
verification: 05-knowledge/results/return_thickened_alpha_opus_S324.out
---

# THM-890 — the depth separation theorem

**(N) The thickening refutation.** Minkowski-dilating each twin's safe set by
R = (−1/858, 1/858): zero merges (20 → 20 and 16 → 16 arcs). The smallest
gaps are 0.00595 (twin A) and 0.02045 (twin B), versus the merge threshold
2/858 = 0.00233. The return interval is an order of magnitude too thin to
reorganize the component structure — the S323-named mechanism does not
operate at this scale.

**(D) The depth separation.** Raw-measure alpha-differences by depth:
0.004 … 0.057 (≤ 9), 0.103 (12), 0.190 (15), 0.455 (19). The twins — near-
identical to depth 9 (THM-886) — are FULLY separated by depth 19
(alpha_19 = −0.950 vs −0.495). Mechanism: finite-gap OPUC ties the
alpha-tail's quasi-periodic structure to the number of spectral gaps; the
twins have 20 vs 16 components, hence different torus dimensions, hence
divergent tails. **The Verblunsky coordinate DOES crack the twins — the
resource is depth ≈ component count, not return-thickening.**

**(G) The direct combinatorial separation:** gap spectra differ 3.4× at the
minimum (and the arc counts 20 vs 16 differ outright on these proxy rows).

**Caveat and next.** These are the 12-speed proxy rows; THM-789's literal
fingerprint identity lives on the folded/eroded-diamond object. The
definitive test: build the eroded-diamond measures for the exact THM-789
incidence data and run the depth-20 map there. Also: the depth-separation
exponent (how fast must depth grow with #arcs — conjecture: linearly) and
the selector consequence: a depth-(2·#arcs) alpha-vector is a complete-
looking compressed state for the two-sheet residual (test Markov-ness).
