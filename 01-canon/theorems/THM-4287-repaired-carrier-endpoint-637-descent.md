---
id: THM-4287
title: "Endpoint-637 prefix-carrier closure and two-index signature-fibre surgery"
status: >
  PROVED RELATIVE TO THM-4283 + FINITE-EXACT + DETACHED LITERAL-WALL
  AUDITS PASS. The inherited 8,996-mask carrier already closes all three
  current endpoint-637 rows; the 9,006-mask repair first fails on the complete
  endpoint-636 layer, with 101 bodies on two rows. An exact minimum-two
  delete-two/add-two deck closes the 33-row signature ideal generated by
  {275,345}. Their typed union removes 35 rows and leaves 22,647, maximum
  endpoint 636. No physical entry or LRC(14) follows.
source: root/endpoint637-carrier-descent/2026-08-29
depends_on:
  - THM-4283-endpoint-644-carrier-response-and-signature-fibre-surgery
related:
  - THM-4286-signature-response-nonfactorization-and-two-deck-surgeries
  - THM-4282-inactive-signature-deck-surgery-endpoint-663
  - THM-4281-rectangle-common-joint-deck-endpoint-670-bridge
artifact_root: 05-knowledge/results/lrc14_repaired_carrier_endpoint637_descent_thm4287
artifact_manifest: 05-knowledge/results/lrc14_repaired_carrier_endpoint637_descent_thm4287/SHA256SUMS
artifact_manifest_sha256: e5cb5fd9f2584de6977b5753a02c2c35faff319f6ecad309961715a379b86265
primary_scripts:
  - 04-computation/lrc14_repaired_carrier_endpoint637_descent_thm4287/repaired9006_descending_scan.cpp
  - 04-computation/lrc14_repaired_carrier_endpoint637_descent_thm4287/detached_endpoint637_literal_audit.cpp
  - 04-computation/lrc14_repaired_carrier_endpoint637_descent_thm4287/signature275_345_fibre_surgery.cpp
  - 04-computation/lrc14_repaired_carrier_endpoint637_descent_thm4287/detached_signature275_345_literal_audit.cpp
  - 04-computation/lrc14_repaired_carrier_endpoint637_descent_thm4287/proof_graph_consequence.py
  - 04-computation/lrc14_repaired_carrier_endpoint637_descent_thm4287/proof_graph_consequence_independent.rb
primary_script_sha256:
  - 98bcc7d5af47d944274ba9f2f48efadc7feced2ae986837b5743a66055a764fe
  - 368ecb0fe1446a9c54405d5f2a62bf5eda70a15cb6b01bb8551c47515bba419d
  - 3dfcf7f366e969b1812445d0108c634420afb732f692760f02f41fb99837670b
  - 46e77c572dc02152506aa3150559d0dd1aa8d749e0a9b016fd096b49f76b8217
  - e272fa126ff9e9a2cd2894283fb4c19e32691749dfcb8c00096c8578c9b71ae0
  - 794aefa9570adcd7f6bafc2b3c868a0ef209c0b922553462a4d276195ea137ba
primary_output_sha256:
  - 0aa71a2f1724dabacd0ac72b146d3782dd3460dd7b93fef2e46cf6e31abac7b9
  - 3d8e4ace76e33bec7108fc418484494832f55e8083c8d9a1efa780a1b626ecce
  - d7ec5e042cec65a9fcd7f31f1aadd30dfdab8991b4966dc6226aa247fd7f0a1d
  - e824d2c4c9a9102daa5058150f79907911e4ec389202edf43c74864f24219141
  - 961782fa2572559d8b950ab9e77140ba4b56ce1294183b8e5dd9dcc8ea91da23
  - 961782fa2572559d8b950ab9e77140ba4b56ce1294183b8e5dd9dcc8ea91da23
hash_basis: raw LF bytes
audit: >
  PASS / ACCEPT, subject to the frozen top-level SHA256SUMS. The source-pinned
  joint-exposure scan and a detached literal-wall implementation agree on the
  endpoint-637 closure, complete endpoint-636 boundary, and byte-identical
  101-row failure ledger. The signature surgery checks the complete common
  activity universe, exact response quotient, rebuilt body cover, and every
  claimed activity cell; an import-free literal implementation independently
  replays the body cover and all 13,893 activity cells. O0, O3, NDEBUG, and
  UBSan controls agree where stated. Exact Python and independently written
  Ruby proof-graph consumers agree byte-for-byte.
---

# THM-4287 -- endpoint-637 prefix-carrier closure and two-index signature-fibre surgery

**PROVED RELATIVE TO THM-4283 + FINITE-EXACT + DETACHED LITERAL-WALL
AUDITS PASS. LRC(14) REMAINS OPEN.**

## 1. Statement

Retain THM-4283's labelled thirty-speed pool, threshold `1/14`, ordered
421-mask joint deck `E`, final 8,996-mask carrier `C`, repaired 9,006-mask
carrier `C++`, and exact residual `U_4283`:

```text
|U_4283|=22,682,                 FNV=f7563445f15efebf,
SHA256=7d0044bc4c32f08b9d09dca420171a05666d26e03f38fbc48a9baa1fcb027102,
max endpoint=637,
top={(100,637),(294,637),(520,637)}.                    (1)
```

There are two complementary certificate conclusions.

1. The old carrier `C` already closes all three rows in the top layer of
   `(1)`. Consequently its nested extensions also close them. The repaired
   carrier `C++` does not close the next complete layer: at endpoint 636 it
   has exactly 101 failures, supported on `(100,636)` and `(256,636)`.
2. The endpoint-637 row `(520,637)` has inactive joint signature
   `{275,345}`. Deleting those two joint masks and appending the exact
   minimum-two family

   ```text
   02229124,1c030206                                      (2)
   ```

   gives a 421-mask body-covering deck common to the complete current
   signature ideal

   ```text
   H={p in U_4283:I_E(p) subset {275,345}},
   |H|=33, FNV=f11425c1e1b17094.                         (3)
   ```

Let `K` be the three-row carrier layer in `(1)`. The two certificate nodes
have exact overlap

```text
K intersect H={(520,637)}.                              (4)
```

Thus their typed union removes `3+33-1=35` rows from `U_4283`. The exact
remaining residual is

```text
count=22,647,
FNV=df5374d4aca67677,
SHA256=14f9be0d9472bc573e582ec6f4cb92c7def6f583f6afaf0b747f2a9713330317,
maximum endpoint=636,
top={(100,636),(256,636),(294,636),
     (338,636),(372,636),(384,636)}.                     (5)
```

The carrier and rebuilt common deck are distinct proof nodes. Their typed
union in `(4)` is not one globally common deck.

## 2. Exact carrier attribution at endpoint 637

The three nested carrier identities are

```text
|C|=8,996,       FNV=fd899660f14b311c,
|C+|=8,997,      FNV=8e1860a25d0fcf87,
|C++|=9,006,     FNV=fdc1c57ae4dc1bb6.                 (6)
```

The exact current endpoint-637 layer has pair FNV `f4b48b16a28826ad`.
Joint-exposure scanning gives:

| pair | active masks in `C++` | exposed bodies | positive hit range under `C++` | failures under `C` | failures under `C++` |
|---|---:|---:|---:|---:|---:|
| `(100,637)` | 5,656 | 2,114 | `2..305` | 0 | 0 |
| `(294,637)` | 7,653 | 52 | `17..283` | 0 | 0 |
| `(520,637)` | 7,633 | 6 | `49..195` | 0 | 0 |

The positive hit range is over exposed bodies that are hit by active
nonjoint carrier masks. Since its lower endpoint is positive in all three
rows, every exposed obligation is covered. The direct `C` replay is
load-bearing attribution: none of the ten masks appended after `C` causes
the endpoint-637 closure.

This is a genuine nonmonotone carrier boundary. THM-4283 showed that `C+`
still has 40 failures at `(256,638)`; after repairing that layer, the older
prefix `C` already succeeds at every current row one endpoint lower. No
endpoint monotonicity was assumed or inferred.

## 3. Complete endpoint-636 hostile layer

The repaired carrier scan completes all six endpoint-636 rows before
stopping. The layer has FNV `b3a1869a95dec7aa`:

| pair | exposed bodies | failures under `C` | failures under `C++` | `C++` failure FNV |
|---|---:|---:|---:|---:|
| `(100,636)` | 31,675 | 70 | 64 | `d611500ea833ff83` |
| `(256,636)` | 14,062 | 68 | 37 | `ee7792a8a2fd51c9` |
| `(294,636)` | 34 | 0 | 0 | `cbf29ce484222325` |
| `(338,636)` | 8 | 0 | 0 | `cbf29ce484222325` |
| `(372,636)` | 24 | 0 | 0 | `cbf29ce484222325` |
| `(384,636)` | 259 | 0 | 0 | `cbf29ce484222325` |

Thus the repair suffix removes six old failures at `(100,636)` and 31 at
`(256,636)`, but it does not close either row. The complete labelled failure
ledger has 101 pair-body rows and SHA-256
`e5da728e0a40076141e43450983dd4bb0f6d9c5aa281f95becb5dbafc555e242`.
This failure means only that this sufficient carrier certificate is absent.
It is not evidence that either pair is dangerous for the physical LRC target.

## 4. The `{275,345}` signature ideal

For the ordered joint deck, retain THM-4282's notation

```text
I_E(p)={i in {0,...,420}:E_i is inactive at p}.         (7)
```

The exact target signature and deleted masks are

```text
I_E(520,637)={275,345},
E_275=1aa28002,  E_345=18868880.                       (8)
```

Intersecting `(7)` with the current residual gives the ideal `(3)`. Its 33
rows split into 14 with signature `{275}`, 16 with signature `{345}`, and
three with signature `{275,345}`. This is the complete ideal inside
`U_4283`, not a sample or a target-local fibre.

By THM-4282's exact surgery lemma, deleting `(8)` exposes exactly those
bodies whose old joint response is contained in the deleted set. Exhaustive
enumeration gives six obligations:

```text
004c50c9,03306409,054c6408,
204c50c1,23505009,245c0281,              FNV=5098163ca56aa069. (9)
```

Four obligations respond only to index 275 and two only to index 345; none
responds to both deleted masks.

## 5. Exact common-activity response quotient

For a common deck on all of `H`, activity is intersected before response
optimization. Complete enumeration of all rank-eight masks gives

```text
common-active masks          2,027,392
common-active FNV             43a535bbda51fab8
response classes                             17
response incidences                     135,082
full responders                                 0.       (10)
```

The absence of a full responder proves that one appended mask cannot cover
all six obligations. The two response patterns of `(2)` are `0x24` and
`0x1b`; their union is `0x3f`, so they cover every obligation. Hence the
common-activity replacement minimum is exactly two.

After retaining the other 419 joint masks and appending `(2)`, the rebuilt
deck has

```text
size=421,                       FNV=9781de64dff60cee.    (11)
```

An exhaustive replay over all `C(30,9)=14,307,150` labelled bodies finds
zero failures. Direct signed-margin evaluation of all `33*421=13,893`
claimed activity cells finds zero equalities and every margin positive.
Therefore `(11)` is a body-covering deck common to every row in `H`.

The mechanism realizes the sidecar isolated by THM-4286: the inactive
signature selects the deletion ideal and its six obligations, while the full
33-row activity intersection selects lawful appended masks. Signature
equality alone would not prove the common-deck claim.

## 6. Exact proof graph and validity gates

The two typed closure ledgers have

```text
|K|=3,                         FNV(K)=f4b48b16a28826ad,
|H|=33,                        FNV(H)=f11425c1e1b17094,
|K intersect H|=1,             FNV=3cefea79822890f4,
|K union H|=35,                FNV=0b5d8d9c28d7325d.   (12)
```

Exact Python and independently written Ruby set consumers verify `(12)`,
subtract it from the pinned residual `(1)`, and emit byte-identical copies
of `(5)`.

The primary carrier path imports the source-pinned joint-exposure machinery.
A detached program instead rebuilds literal walls and activity, exhausts all
`9*C(30,9)=128,764,350` body instances across endpoints 637 and 636, finds
zero activity equalities, and emits the same 101-row failure ledger. It also
independently verifies that the 8,996-mask prefix closes endpoint 637.

The primary surgery path reads the complete inherited signature atlas,
intersects the full activity universe, solves the exact six-bit response
quotient, and rescans the rebuilt deck. A second source imports no project
code: it reconstructs literal walls for all 33 pairs, exhausts the rebuilt
body cover, tests all 13,893 activity cells, and again finds zero failures
and zero equalities. O0, O3, NDEBUG, and UBSan controls agree as recorded in
the artifact packet.

No symmetry quotient or unlabelled identification is used: pairs remain
ordered with `q<r`, masks and bodies retain all thirty labels, and every
negative result inherits the exact THM-4283 residual filter.

## 7. Scope and non-consequences

- The carrier conclusion is a finite sufficient-certificate statement. Its
  endpoint-636 failures do not assert failure of physical entry.
- The signature deck and carrier are separate consumers. Their set union is
  lawful only at the proof-graph level.
- The result neither supplies an exclusive owner nor proves semantic arrival
  or termination.
- No extrapolation beyond the frozen pool, residual, or endpoint layers is
  made. In particular, no uniform finite carrier is claimed.
- LRC(14) remains open.
