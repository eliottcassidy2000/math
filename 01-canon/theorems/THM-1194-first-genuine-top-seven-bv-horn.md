---
id: THM-1194
title: The first genuine top-seven BV horn at v6=6 and 78<=v7<=90
status: RESERVED / EXACT CERTIFICATE RUNNING.  The intended theorem combines the explicit [1,6]-core safe interval [11/84,13/84] with phase-specific THM-1182 BV needles.  No row beyond the completed certificate is claimed here.
source: codex-2026-07-18-S77 live top-seven target adjustment
depends_on: [THM-1176, THM-1182]
---

# THM-1194 -- first genuine top-seven BV horn

This identifier is reserved while the exact rational certificate runs.

If an ordered thirteen-speed counterexample has

```text
78<=v_7<=90,                 v_7>=13v_6,
```

then distinctness forces `v_6=6` and `{v_1,...,v_6}={1,...,6}`.  Every one
of these six speeds is safe at radius `1/14` throughout

```text
I=[11/84,13/84].
```

For each carrier `a` in this range, the running certificate uses the complete
`a`-slow gap `G_k(a)` contained in `I`: `k=11` for `78<=a<=84` and `k=12`
for `85<=a<=90`.  A proved phase-specific BV needle for those thirteen rows
would contradict coverage of `I` by the top seven speeds.  Until every row
and the independent replay pass, this file is only an honest namespace claim.
