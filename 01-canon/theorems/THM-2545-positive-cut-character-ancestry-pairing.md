---
id: THM-2545
title: "The positive cut-character ancestry pairing: the 42-cut bank rides the owner first-collision cospan"
status: >
  PROVED CANDIDATE + VERIFIED-EXACT (all decisions exact in Q(zeta_91)
  via Phi_91 arithmetic; normal and -O runs byte-identical); independent
  hostile audit REQUESTED.  Executes THM-2507 Section 7's designated
  "highest-leverage next test" with the POSITIVE outcome the trio's
  closing sections priced: this is the first physical cut-character
  current.  SCOPE: uses the one licensed typed-sheet bridge (THM-2478
  (21a)-(21b), k = alpha); does NOT transplant the punctured-stalk
  packet to the live 165 rows (the remaining named gap), does NOT
  remove a scalar row; LRC(14) remains OPEN.
source: opus-2026-07-27 (test named by THM-2506/2507/2508 closing
  sections; stalk data THM-2471; graft THM-2478)
depends_on:
  - THM-2508-affine-cut-bundle-covariance-and-carry-permutation
  - THM-2506-punctured-stalk-primitive-module-saturation-and-thirteen-primary-pushforward-no-go
  - THM-2471-owner-first-collision-weighted-root-service-and-temporal-atom-boundary
  - THM-2478 (grafted future-owner handoff)
related:
  - THM-2507 (Section 7 names this test)
  - THM-2542 (Cech holonomy; audited SURVIVES-WITH-REPAIRS this session)
script: 04-computation/lrc14_cut_bundle_ancestry_pairing_opus_20260727.py
output: 05-knowledge/results/lrc14_cut_bundle_ancestry_pairing_opus_20260727.out
---

# THM-2545 -- the 42-cut bank pairs with the ancestry cospan

## Statement

Instantiate THM-2508's cut bundle on THM-2471's canon-realized
first-collision Boolean ancestry stalk (4 strata, 5 Boolean sheets, 7
atoms; `I_r = 24/4225`, service `24/25`), grafted forward by THM-2478,
through the one licensed typed-sheet bridge (finite character-label
alignment `k = alpha`, THM-2478 (21a)-(21b)). Then for EVERY
`(tau, a, beta) in F_13^* x F_7^* x F_7^*`, the pairing

```text
P_{tau,a}(beta) = sum_{alpha in F_13^*} Chat(-alpha) Psi_{tau,a}(alpha, beta)
```

is a NONZERO element of `Q(zeta_91)` -- all `432` placements, in both
leg conventions (`864` total), every cut character `beta != 0` working.
Explicit witness at `(1,1,1)` (dual leg):
`100 P = 312 zeta_91^39 - 312 zeta_91^52`.

Retention:
- **target charge**: the `alpha = 0` target root is auto-excluded by
  `K_{0,beta} = 0`; the signed `-I_r` ledger anchors the dual leg;
  full-stalk target neutrality holds at the first lawful delay
  `L = K+1` and sharply fails at `L = K` (both verified);
- **owner type**: the source-refined matrix stays the single owner loop
  `M_{O,O} = 169 I_r` before and after the graft;
- **sidecars**: `kappa` and the cut label survive as RETAINED
  equivariant sidecars -- the exact kappa-dressing identity
  `P_H = sum_alpha zeta^{-alpha H} term_alpha` holds for all `H`, and
  both torsor collapses (average over `H in F_13`, over `C in F_7`)
  vanish identically, exactly the invariant-readout death THM-2508
  (20)-(21) mandates; `a mod d_0` is trivially retained (`d_0 = 1`);
  the ancestry sheet `b` breaks only under REBASING past the deep
  valuation (mod-13 hostile replayed at `L = 6 > lambda = 5`), which
  the forward graft avoids.

## Septimal transport boundary (typed-sheet finding)

The stalk's root index `u` (temporal collision fibre) and the bundle's
`h` (static quotient-stalk row) are different typed sheets; no septimal
identification exists. The graft multiplier `13^L mod 7` reaches only
the parity subgroup `{1, 6} subset F_7^*`: the cut torsor is NOT
transported -- the cut phase rides the static leg with parity pinned.
Any future transplant theorem must either live with the pinned parity
or supply a new septimal transport.

## Controls

Positive: the full `5,184`-mode THM-2508 nonvanishing and the
factorization `Psi = K_{alpha tau, beta} dtilde(alpha, -beta a)`
reproduced exactly mod `Phi_91` (not a spot check). Hostile: `864/864`
`beta = 0` vanishings; `504/504` invariant linear readouts zero; the
`beta = 0` pairing identically zero; both torsor-averaged pairings
identically zero. Covariance (THM-2508 (19)) verified under all `91`
CRT translations and one full gauge `(A,H,B,C) = (2,3,2,4)`.

## Canon note (for the THM-2471 owner)

THM-2471 eq (15) states `Chat = 13 J` while eqs (8)+(13) give
`J = Chat/169`; the ledger eq (10) (`J(0) = I_r`, sum `-I_r`) forces
`J = Chat_unnorm/169`. A one-line normalization repair in THM-2471 is
requested; this file anchors on the ledger.

## Frontier effect

Per THM-2506/2507/2508's own framing: the positive outcome makes this
the first physical cut-character current; the remaining gap on the
mixed 91-bundle frontier REDUCES to the branch-transplant obligation
(moving the high-septimal packet's laws to the live 165 low-septimal
rows), with `kappa` and the cut label as retained equivariant sidecars.
