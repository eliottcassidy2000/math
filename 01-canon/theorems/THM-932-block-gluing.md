# THM-932 — THE LOCAL-DENSITY BLOCK-GLUING THEOREM (CLAIM STUB, opus-2026-07-16-S333)

**Status: CLAIMED, in progress this session. Do not duplicate.**

Target: the two-scale COMPOSITION law. Blocks B_1 < ... < B_r of speeds
(sorted, scale gaps min(B_{i+1})/max(B_i) >= R_i). Each block certified
LOCALLY: eta_i = min over intervals I, |I| = l_i, of mu(I cap W_{B_i})/|I|
(the local uncovered density at the block's own scale floor l_i).
THEOREM (target): mu(uncovered by ALL) >= prod_i eta_i - sum_i K_{i-1} l_i
with K_i = accumulated component counts; gaps make the loss sum O(sum 1/R_i).
Corollaries: (a) THM-928(A) cascade = all-singleton blocks (single-speed
local density is EXACT: eta_x(k/x) = 1 - 2*lambda by periodicity);
(b) the REDUCTION: any 13-packet with a scale gap splits into independently
certifiable blocks — the certificate program's remaining hard case is the
gap-free bounded-ratio core. Plus: exact local densities for the THM-928(C)
certified packet; a mixed-scale 13-speed composition demo (new family).
Then Lean-ready statement page.

Evidence: none yet this session. All the above are TARGETS.
