---
id: THM-1239
title: TWO BLOCKERS CAN ERASE THE ENTIRE SEVEN-EDGE CURVATURE PATH — an exact infinite near-tiling counterfamily to local active-edge survival
status: RESERVED/EXACT CONSTRUCTION UNDER AUDIT. Candidate endpoint margins are positive for every m>=8; independent replay, paper proof, and formal arithmetic consumers remain to be completed.
source: codex-2026-07-19-S78 continuation with active-blocker-scan agent
depends_on: [THM-1156, THM-1219]
related: [THM-1218, HYP-7870]
---

# THM-1239 — two-blocker curvature erasure guardrail

For `m>=8`, put `a=7m+1`, take the seven-wall packet
`{a,a+1,...,a+6}` on `G_m(a)`, and add blockers

```text
c=a+7,                 d=7a+4.
```

The claimed exact construction has `c` cover the two boundary holes and `d`
cover all five internal handoff holes.  The smallest row is
`a=57,c=64,d=403`, with blocker words `1000001` and `0111110` on the seven
ordered curvature components.  Moreover the whole gap is outside the BAD
event for the quartet `{1,2,3,4}`.

If the audit closes, this refutes any local coherence lemma based only on a
positive active edge or on exclusion of one BAD quartet.  A valid theorem
must retain global position, component transport, and blocker tooth-address
words.  The construction is not an LRC counterexample.
