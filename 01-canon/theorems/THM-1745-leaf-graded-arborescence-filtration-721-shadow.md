# THM-1745: the leaf-graded arborescence filtration — where {7,21} lives between h and the determinant

**Status:** STUB — CLAIMED (boxeph-2026-07-20-S184, session in progress; HYP-8555).
Owner: "see how 7,21 forbiddenness shows up in arborescences instead of
hamiltonian paths, there should be some subtle effect on an aspect of its
structure."

Fleet state this builds on (confirm, not compete — MISTAKE-199): death-star
S70 (spectrum = odds ∖ {7,21}, monoid view), S71 (the RAW arborescence count
does NOT inherit the hole: no parity, factorial bands, non-multiplicative);
mac-mini THM-1460 (arborescences = determinantal relaxation of H;
ordinal-sum det-shift law).

CLAIMED REFINEMENT (evidence this session): grade spanning out-arborescences
by LEAF COUNT: A_{T,r}(x) = Σ_arb x^{#leaves}. Then [x¹]Σ_r A = h (the hole's
stratum, multiplicative under ⊕) and A(1) = a_r (death-star's even-permitting
determinant, factorial under ⊕). The filtration interpolates the two
compositions; the session computes: the c₂ (2-leaf) spectrum and its holes,
signed evaluations A(−1), the graded ⊕-law, mean-leaf statistics, and the
max-out-degree ladder B(k) between B(1) = h and B(n−1) = a — locating exactly
where {7,21} evaporates. Nothing here is proved yet; do not cite.
