# THM-406 — The covering-depth master object: factorial moments are overlap volumes, and the "five principles of fundamentality" are the spectral theorem

**Status:** PROVED (M1, M3 rigorous; M2 a rigorous corollary). Verified exactly at n=4,5.
**Source:** opus-2026-06-03-S599c (remote-control). Makes rigorous the master-object frame of
S599/S599b (HYP-2153, HYP-2154).
**Convention:** `X = ℝ/ℤ` with Lebesgue measure `μ`; speeds `v_1,…,v_n ∈ ℤ_{>0}`; gap
`δ ∈ (0,½)`; danger arcs `D_i = {t : ‖v_i t‖ < δ}` (so `μ(D_i)=2δ`); **depth**
`N(t)=Σ_i 1_{D_i}(t)`; **covering-depth distribution** `p_k = μ(N^{-1}(k))`; **overlap sums**
`S_j = Σ_{I⊆[n],|I|=j} μ(⋂_{i∈I} D_i)` with `S_0=1`. (LRC gap `δ=1/(n+1)`.)

---

## M1 (factorial moments = overlap volumes) — PROVED

> **Theorem M1.** For every `0 ≤ j ≤ n`,
> ```
>   E_μ[ C(N, j) ] = S_j           (binomial-moment form)
>   E_μ[ N(N−1)⋯(N−j+1) ] = j! · S_j   (factorial-moment form).
> ```

**Proof.** Pointwise, `C(N(t),j)` counts the `j`-subsets `I⊆[n]` with `t∈⋂_{i∈I}D_i`:
```
  C(N(t),j) = Σ_{|I|=j} ∏_{i∈I} 1_{D_i}(t) = Σ_{|I|=j} 1_{⋂_{i∈I} D_i}(t).
```
Integrate against `μ` and use linearity: `E[C(N,j)] = Σ_{|I|=j} μ(⋂_{i∈I}D_i) = S_j`. The
factorial form is `j!·C(N,j)`. ∎

**Corollaries.**
- **(a) The first moment is config-free.** `E[N] = S_1 = Σ_i μ(D_i) = 2nδ`. *Every* speed set
  has the same mean depth `2nδ → 2`; the first moment carries no information (it is the S550
  measure bound).
- **(b) Loneliness is an alternating sum of overlap volumes (inclusion–exclusion).**
  Since `Σ_{j≥0}(−1)^j C(k,j) = (1−1)^k = 𝟙[k=0]`,
  ```
        p_0 = μ(N=0) = Σ_{j=0}^{n} (−1)^j S_j .
  ```
  The lonely measure is *exactly* the inclusion–exclusion alternating sum of the multi-arc
  overlap volumes.
- **(c) Covariance / Poisson reference.** A `Poisson(λ)` law has `E[C(N,j)] = λ^j/j!`, i.e.
  reference overlaps `S_j^{Pois} = (2nδ)^j / j!`. The config's departure from Poisson is the
  excess sequence `S_j − (2nδ)^j/j!`. At `j=2`: `Σ_{i<j} Cov(1_{D_i},1_{D_j}) = S_2 −
  \binom{n}{2}(2δ)^2`.

**Verified exactly** (`lrc_depth_rigor_moments_s599c.py`, n=4,5): `S_j` from `{p_k}` equals
`S_j` from direct arc-intersection for `j=1,2,3`; `Σ(−1)^j S_j = p_0` exactly (e.g. AP n=4:
`p_0=0`; generic `(1,4,6,9)`: `p_0=1/9`); `S_1=2nδ` for all.

---

## M2 (the collapse is a genuinely higher-order cancellation) — corollary, PROVED + verified

> **Theorem M2.** The worry-set `{p_0=0}` is *not* determined by any finite truncation of the
> moment sequence. Specifically `S_1` is config-free and `S_2` is generically **sub-Poisson**
> (`Σ Cov < 0`) and does **not** separate the collapse family from generic configs;
> `p_0 = Σ_{j=0}^n(−1)^j S_j = 0` is an **all-orders** cancellation.

**Evidence (exact, n=4, δ=1/5, all share `S_1=8/5`, `λ²=64/25`):**

| config | `S_2` | `2S_2−λ² = ΣN(N−1)−λ²` | `p_0` |
|---|---|---|---|
| AP `(1,2,3,4)` | `14/15` | `−0.693` | `0` |
| chain `(1,3,4,7)` | `37/42` | `−0.798` | `0` |
| generic `(1,4,6,9)` | `13/15` | `−0.827` | `1/9` |

All three are sub-Poisson (negative covariance — danger events *repel*), and the collapse
configs are **not** the ones with largest `|S_2|` excess; the generic config here has the
*more* negative covariance yet `p_0>0`. So the vanishing of `p_0` is invisible at orders
`≤2`: it requires the full alternating sum `Σ(−1)^j S_j` to cancel, every `S_j (j≥2)`
participating.

**Rigorous reading of the Vitali wall (S551o).** Bonferroni's inequalities give only
*one-sided* bounds from truncations: `Σ_{j=0}^{2r}(−1)^jS_j ≥ p_0 ≥ Σ_{j=0}^{2r+1}(−1)^jS_j`.
Hence **no finite-moment (energy/measure) functional certifies the sign of `p_0` near the
floor** — the positivity of loneliness is the *full* alternating series, not any finite
energy bound. This is precisely "measure is blind on the residual," now as a statement about
Bonferroni truncation depth: the worry-set lives strictly **beyond every finite moment
order**. ∎

---

## M3 (the master object IS a spectral measure; the five principles ARE the spectral theorem) — PROVED

Let `H = L²(X,μ)`, `M_N : f ↦ N·f` the multiplication operator by the (bounded,
integer-valued, self-adjoint) depth `N`, and `Ω = 1` the constant function. For any bounded
Borel `g`, `g(M_N)` is multiplication by `g∘N`, so the **spectral measure of `M_N` relative
to `Ω`** is
```
   E_Ω(A) = ⟨Ω, 𝟙_A(M_N) Ω⟩ = μ(N ∈ A) = Σ_{k∈A} p_k .
```

> **Theorem M3.** The covering-depth distribution `{p_k}` is exactly the spectral measure of
> the depth operator `M_N` at the cyclic vector `Ω=1`. Consequently the five informally-stated
> "principles of fundamentality" (S599b) are the five standard faces of the **spectral
> theorem** for the abelian von Neumann algebra `𝒜 = {g(M_N)}'' generated by `N`:

| principle (S599b) | spectral-theorem content |
|---|---|
| **P1 complete** (minimal sufficient statistic) | the measure class `[{p_k}]` + multiplicity is a **complete unitary invariant** of `(𝒜, Ω)` (multiplicity-free here); `Ω` cyclic ⇒ `{p_k}` determines the GNS representation |
| **P2 diagonalizing** | `M_N ≅` multiplication by `id` on `L²(\{p_k\})` — the **spectral diagonalization** |
| **P3 maximal forgetting** | `σ(N)` is the **minimal** σ-algebra for which `N` is measurable; `{p_k}=N_*μ` is the canonical pushforward |
| **P4 variational** | `min/max supp{p_k}` are the **Rayleigh–Ritz / min–max** extrema of `⟨·,M_N·⟩` |
| **P5 natural** | the **Borel functional calculus** `g ↦ g(M_N)` is functorial in `g` and equivariant under unitary conjugation |

**Proof.** The displayed identity `E_Ω(A)=Σ_{k∈A}p_k` is immediate from the functional
calculus for a multiplication operator. Each row is then a textbook corollary of the spectral
theorem for a bounded self-adjoint operator with a cyclic (here: cyclic-for-the-generated-
abelian-algebra) vector: unitary equivalence to multiplication-by-coordinate on `L²` of the
spectral measure (P2), with the measure class the complete invariant (P1), the spectral
measure the pushforward along the minimal generating σ-algebra (P3), spectral edges given by
min–max (P4), and the natural Borel calculus (P5). ∎

> **Slogan, now a theorem.** *A master object is the spectral measure of the problem's
> defining observable at its canonical cyclic vector; "fundamentality = decomposition under
> its own symmetry" is the spectral theorem for the abelian algebra that observable
> generates.*

---

## Consequences for LRC

- LRC `⟺` for every speed set, `Σ_{j=0}^{n}(−1)^j S_j` (the alternating overlap sum) is the
  measure of a set whose *support* is nonempty at `δ=1/(n+1)` — a statement purely about the
  overlap-volume sequence `{S_j}`, i.e. the additive/resonance data (THM-401, S577).
- The worry-set `{p_0=0}` is the **all-orders cancellation locus**; by M2 no finite-energy
  bound reaches it — the rigorous Vitali wall. The closing instrument must control the full
  `{S_j}` (e.g. via the additive-chain structure, S599, or a Baker bound on the resonant
  overlaps, S595/S596), not a truncated moment.
- The Poisson reference `S_j^{Pois}=(2nδ)^j/j!` is the **idealized free baseline**; the true
  object is sub-Poisson (M2), so `p_0 < e^{−2nδ}` generically — loneliness is *typical* but
  by a smaller margin than independence predicts (refines HYP-2154's heuristic).

**Artifacts:** `04-computation/lrc_depth_rigor_moments_s599c.py` (+`.out`). Builds on S599
(master object), S599b (Poisson baseline), S550 (first moment), S551o (Vitali wall),
THM-401 (`2n−1` resonance), S577 (3-term overlaps). New theorem; companion reflection
`lrc-coimage-fundamentality-made-rigorous-s599.md` (HYP-2155).
