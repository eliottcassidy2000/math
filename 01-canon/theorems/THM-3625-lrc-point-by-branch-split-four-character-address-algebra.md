---
id: THM-3625
title: "LRC point-by-branch split four-character address algebra"
status: >
  RESERVED / PROVISIONAL PROOF CANDIDATE + FINITE-EXACT + VERIFIED-EXACT /
  PENDING INDEPENDENT HOSTILE AUDIT.  The thirteen point-by-branch address
  operators above THM-3615 are invertible and span a commutative, unital,
  product-closed four-dimensional algebra isomorphic to F_p^4.  Its action on
  the six-point module has character multiplicities 2,1,1,2.  The inherited
  four-space P6sharp intersect Q6sharp is the sum of the doubled character
  spaces, while K2sharp takes one line in each.  Hence no address-algebra
  element can have K2sharp as kernel or image.  No chronology,
  characteristic-zero transfer, row exclusion, or LRC(14) result is claimed.
source: kps-s189 / THM-3615 destroyed-address-sidecar continuation, 2026-08-21
audit: >
  PENDING -- author exact reconstruction passed the parent pins, all 13
  address ranks, 169 commutators, product closure, Fourier and reversal
  controls, cyclic minimal polynomial, split-character decomposition,
  inherited-flag intersections, multiplicity obstruction, and semantic
  digest.  Independent hostile reconstruction remains required.
depends_on:
  - THM-3615-lrc-pointed-root-difference-lift-flag
related:
  - THM-3602-lrc-centered-a4-flag-inside-pointed-six-carrier
  - THM-3593-lrc-common-a4-anova-graph-flag
script: 04-computation/lrc_point_by_branch_four_character_address_algebra_thm3625.py
output: 05-knowledge/results/lrc_point_by_branch_four_character_address_algebra_thm3625.out
script_sha256: b32b985e2817f135dcc6d19f8f675d3d174e7f1b2a81bd14848d5424fabaf6ad
output_sha256: 8ef35eb1d7dd2f00c67db0171483ac1a9da8851eb21afb88bbdef5976a49b325
semantic_sha256: 89d69b3eb2dff06f7ebc236d90be73555c397b39542a155e79786370884c06df
hash_basis: LF-normalized bytes for files; canonical JSON for semantic ledger
---

# THM-3625 -- LRC point-by-branch split four-character address algebra

**RESERVED / PROVISIONAL PROOF CANDIDATE + FINITE-EXACT + VERIFIED-EXACT /
PENDING INDEPENDENT HOSTILE AUDIT.**  This restores the branch address that
THM-3615 deliberately summed away.  It identifies both the structure gained
and the exact multiplicity information still missing.

## 1. Point-by-branch operators

Work over the pinned field

```text
F=F_p,                  p=755373809845391722745761,      (1)
```

and use the six point states in the inherited order

```text
(0,0),(1,0),(1,6),(3,6),(3,12),(2,12).                 (2)
```

Let `mathcal T(x,r,s,t)` be the clean-room four-way tensor of THM-3615, with
branch digit `r`, root difference `s`, and relation `t`, all in `F_13`.  Put

```text
g_(x,r)(s,t)=mathcal T(x,r,s,t),
g_x(s,t)=sum_(r in F_13)g_(x,r)(s,t).                  (3)
```

The six `g_x` are independent and span `P6sharp`.  Every branch-resolved row
lies in that same space.  Therefore there is a unique matrix `A_r in M_6(F)`
such that

```text
g_(x,r)=sum_y A_r[x,y]g_y.                             (4)
```

Equivalently, `A_r` is the matrix in the ordered frame `(g_x)` of the linear
operator `g_x |-> g_(x,r)`.  Exact reconstruction gives

```text
rank A_r=6                         for every r in F_13,
sum_r A_r=I_6.                                           (5)
```

Thus every address is invertible even though their sum is the identity.

## 2. The address algebra is split of dimension four

Let

```text
mathcal A=span_F{A_r:r in F_13} subset End_F(P6sharp).  (6)
```

The complete exact multiplication table is compressed by the identities

```text
dim_F mathcal A=4,
[A_r,A_s]=0                         for all 169 pairs,
span_F{A_r,A_rA_s:r,s in F_13}=mathcal A.               (7)
```

Because `(5)` puts the identity in `(6)`, equations `(7)` make `mathcal A` a
commutative unital four-dimensional algebra.

It is already generated by `A_3`.  The four powers

```text
I,A_3,A_3^2,A_3^3                                      (8)
```

are independent, and the next power satisfies the minimal polynomial

```text
m(T)=T^4-222116990570652248514761 T^3
        +157450021680728426647686 T^2
        -141894481903583399851990 T
        +64677205047035279561501.                       (9)
```

Over `F`, `(9)` has the four distinct roots

```text
lambda_0=189500145028362740071369,
lambda_1=380076640875614845827929,
lambda_2=565142856996729319560976,
lambda_3=598144967360728788546009.                     (10)
```

The Chinese remainder theorem now gives the exact algebra identification

```text
mathcal A=F[A_3] isomorphic to product_(i=0)^3 F.       (11)
```

This is split semisimplicity over the one pinned finite field, not a claimed
characteristic-zero lift.

## 3. Four characters and the multiplicity debt

Let

```text
E_i=ker(A_3-lambda_i I).                                (12)
```

The square-free polynomial `(9)` gives the direct decomposition

```text
P6sharp=E_0 direct_sum E_1 direct_sum E_2 direct_sum E_3,
(dim E_0,dim E_1,dim E_2,dim E_3)=(2,1,1,2).           (13)
```

Every element of `mathcal A` acts by one scalar on each `E_i`.  In coefficient
coordinates, the inherited spaces from THM-3615 are `mathcal A`-invariant and
satisfy

```text
P6sharp intersect Q6sharp=E_0 direct_sum E_3,          (14)

dim(K2sharp intersect E_i)=(1,0,0,1).                  (15)
```

Since `K2sharp` is invariant, `(15)` says it is the direct sum of one line in
each doubled character space.  This precisely separates what the branch
address algebra can and cannot see.

For `a in mathcal A`, write its four character values as `a_i`.  Then

```text
ker a=direct_sum_(a_i=0) E_i,
im a =direct_sum_(a_i!=0) E_i.                          (16)
```

Every kernel, image, and idempotent image therefore takes all or none of each
`E_i`.  Equations `(13),(15)` prove

```text
K2sharp is neither ker a nor im a for any a in mathcal A. (17)
```

The address algebra recovers the four-space `(14)` but cannot select the two
distinguished lines inside its two multiplicity-two blocks.  Any completion
must add an observable that breaks at least one of those multiplicities; more
polynomials in the branch addresses cannot do so.

## 4. Fourier and reversal controls

Let `zeta` be the pinned primitive thirteenth root

```text
zeta=266737884585332483769981 in F,                    (18)
```

and define the branch Fourier operators

```text
Ahat_k=sum_(r in F_13) zeta^(kr)A_r.                   (19)
```

Every one of the thirteen `Ahat_k` is invertible, and their span is again
`mathcal A`.  Thus ordinary branch Fourier support does not split the doubled
characters.

In the pinned point-label frame, all thirteen matrices are symmetric.  If `J`
reverses the order `(2)`, the only affine branch covariance found by the exact
atlas is

```text
J A_r J=A_(-r-1).                                       (20)
```

The version with a transpose is the same identity because the matrices are
symmetric.  Symmetry itself is a pinned-frame fact.  Commutativity, algebra
dimension, split type, character multiplicities, and `(14)--(17)` are
preserved under simultaneous change of basis.

The canonical exact digests are

```text
addresses:
20afa4a3d65e92f7c343710f7e1b9ac163c55fa93ee3d4aa54fde76b347645e0,
Fourier operators:
d66be14669f799d2ff227f32ad804c62e002e9faee7e74cb23d53811e8c507e6,
K2sharp coefficient plane:
065ca6f0d61df266a5e55aab3d50e03f35ddc37a156373eabf6ec6c04cc8015f,
P6sharp intersect Q6sharp coefficient four-space:
8d2da40cf11f96dd7edfb042ac9d7c87b20eed8596cc3b3de673979e43020936. (21)
```

## 5. Scope and reproduction

Reproduce with

```bash
python3 04-computation/lrc_point_by_branch_four_character_address_algebra_thm3625.py
python3 -O 04-computation/lrc_point_by_branch_four_character_address_algebra_thm3625.py
```

The companion reconstructs the branch-resolved tensor rather than accepting
the matrices as input.  It pins the promoted THM-3615 theorem, script, and
output; verifies `(5)--(21)`; and reports a canonical semantic digest.

This candidate is static over one finite field.  It does not identify the
address operators with chronological evolution, transport a current, exclude
a row, lift to characteristic zero, or prove LRC(14).  Its concrete frontier
is narrower and useful: the missing LRC observable must act non-scalarly on a
doubled character block, so another commutative branch-address statistic is
provably insufficient.

Independent hostile audit remains the promotion gate.
