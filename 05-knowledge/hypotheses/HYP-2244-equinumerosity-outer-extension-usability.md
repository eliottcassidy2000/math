---
id: HYP-2244
status: STUB claimed for S669 computation
source: codex-2026-06-05-S669
related:
  - HYP-2243
  - HYP-2242
  - HYP-2187
  - HYP-2186
  - HYP-2236
---

# HYP-2244: Equinumerosity Is Not Outer-Extension Usability

## Claim Stub

Reserve the S669 lane for the user's equinumerosity prompt:

```text
same cardinality / same count is a shadow;
embedding usability is the retained finite profile that survives extensions.
```

The intended comparison is:

- open intervals, real lines, and finite-dimensional Euclidean spaces have the
  same cardinal size, but homeomorphism/embedding usability remembers dimension
  and local deletion/link profiles;
- tournaments and Royle-even graphs have the same count, but the repo needs a
  predicate-preserving fiber functor, not just equinumerosity;
- Friedman-style finite-tree/homeomorphic-embedding phenomena warn that the
  uniform extension theorem lives in the embedding profile, not in the raw
  count.

## Evidence Still Missing

S669 should add a finite toy audit, probably using equal-size path/grid
carriers as a discrete interval-vs-plane model, and should connect the result
to S617's Royle/even-graph count audit plus S668's colored-tree
outer-extension profile.

The desired output is a usable theorem shape:

```text
Equinumerosity-to-usability repair:
  if two quotient worlds are only count-equivalent, then any proof transfer must
  attach a deletion/link/embedding profile and prove target-predicate purity
  over allowed outer extensions.
```

