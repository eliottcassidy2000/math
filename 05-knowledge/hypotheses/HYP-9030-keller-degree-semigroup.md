---
id: HYP-9030
title: "The Keller degree semigroup: JC>=3 counterexamples as a graded monoid over the sporadic core"
status: >
  REFUTED / SUPERSEDED by THM-3438.  The proved monoid law and the
  subfamily {3^k} survive, but weighted lifts realize every generic degree
  n>=3 already in dimension three.  Thus KDeg(m)={1,3,4,5,...} for m>=3,
  the global G1 question is positive, and every degree >=3 contains an
  S_n-monodromy weighted atom.  Numerically factorable grades also contain
  composites, beginning at 9; the proposed atom set {1,3} was false (1 is
  the unit).  The ternary F-iterate tree remains a valid subfamily calculation.
source: kind-pasteur-2026-07-27-S135
related:
  - THM-3438-weighted-lift-keller-degree-spectrum
  - THM-3440-weighted-lift-cyclic-infinity-torsor-and-7x13-character-grid
  - THM-2465-g1-exclusion-package-for-degree-four-twojet-keller
  - THM-1335-trisection-modulus-master-identities-trace-polynomiality
  - HYP-9029-strong-interval-tiling-law
  - HYP-9027-twojet-disc-jelonek-odd-exponent-law
script: 04-computation/keller_degree_semigroup_kps_S135.py
output: 05-knowledge/results/keller_degree_semigroup_kps_S135.out
---

# HYP-9030 -- the counterexample family is a degree-graded monoid

> **REFUTATION (THM-3438 / MISTAKE-396).**  This file is retained for the
> monoid mechanism and the ternary branch calculation.  Its completeness and
> atom-spectrum conjectures are false: an explicit quartic 2-jet Keller map is
> the minimal witness, and one weighted-lift seed realizes every degree `>=3`.

## 1. The verified base (companion, all exact)

The wild map `F` (`u = 1+xy`, `det J_F = -2`) has its real triple
collision at `(-1/4, 0, 0)`, and the trisection branch anatomy is

```text
4T^3 - 3T -+ 1 = (T -+ 1)(2T +- 1)^2:
each branch fiber = the mu_3 FIXED point + a folded conjugate PAIR
("1 + 2"), and -1/4 = -e_3{1, -1/2, -1/2} = -prod_j cos(2 pi j/3)
```

-- klein-S324's mechanism independently re-verified: the collision
value is the constant-coefficient story of `T_3(W) = 1`, not
numerology. (klein's further LRC-floor echo remains flagged there,
unadjudicated here.)

## 2. The family, composed (the JC >= 3 infinite family)

Chain rule: `det J_{F o F} = 4` (companion-checked); in general
every composition of Keller maps is Keller, generic fiber
cardinality is multiplicative for dominant maps (classical), and
stable extension `F (+) id` preserves both. Hence, writing
`KDeg(n)` for the set of generic-fiber degrees of Keller
endomorphisms of `C^n`:

```text
KDeg(n) is a multiplicative monoid, and {3^k : k >= 0} subset
KDeg(n) for every n >= 3.                                       (1)
```

The JC >= 3 counterexample "infinite family" is therefore not a
list but a GRADED MONOID over the sporadic core: tame conjugates
and elementary extensions move within a degree grade (THM-2465's
W1, W2 are degree-3 members in new shapes, including 2-jet);
composition multiplies grades; the collision anatomy of `F^k` over
the branch tower is governed by k-fold iterates of the 1+2 branch
split -- a ternary orbit tree (SPECULATIVE link to the repo's other
ternary grammar, THM-2450's C3-towers: flagged, untested).

## 3. The repaired reframe (the H-spectrum grammar, one universe over)

HYP-9029 reduced the H-spectrum to "the multiplicative semigroup
generated by strong atoms, with {7, 21} the non-atom-non-product
gaps". The same grammar here:

```text
KDeg(3) = {1} union {n:n>=3}.  Degree one is the unit.  Every grade
n>=3 contains an S_n-monodromy weighted atom.  A grade contains a
composite iff n=ab with a,b>=3, and every such grade is mixed.  The
grades forcing every map to be atomic are exactly odd primes, 4, 8,
and twice an odd prime.                                      (2)
```

The original Keller-world "7" analogy failed at its target: degree four
exists and is itself forced atomic.  The useful survivor is the grammar,
not the predicted spectrum: numerical factorization supplies a necessary
block pattern, while intermediate fields and monodromy decide whether a
particular map actually decomposes.  THM-3438 proves this distinction sharply:
`F_9` is an `S_9` atom while `F_3 o F_3` is an imprimitive composite in the
same first mixed grade.  The proposed Rédei parity lens is also
globally false as an even-degree exclusion; conjugation only gives
`#real preimages = d (mod 2)` at real regular values, which is perfectly
compatible with THM-3438's even-degree maps.  The z-affine order-`{1,3}`
conjecture remains a strictly smaller architectural question.

## 4. The Berlekamp-Massey sharpening (recorded verdict)

The first-untested-point law (S135 card) is, for linear-recurrence
data, exactly the BM rank-lock: an order-r recurrence is guaranteed
determined by `2r` terms (fib r=2 at 4; tri r=3 at 6;
companion-checked), so a k-point fit certifies linear complexity at
most `floor(k/2)` and generically dies at the rank jump `k+1`;
MISTAKE-198's "2r+2" = guarantee + test + spare. **Weak form
CONFIRMED. Strong form (interpolation rank = nullcone rank) tested
and RELEASED** -- they coincide only on spectrum-sequences.

## 5. Cheapest decisive tests

(i) Classify decomposition of arbitrary maps in the mixed grades; degree alone
cannot distinguish the primitive `F_n` from compositions.  (ii) Compute the
Jelonek sets and boundary inertia of the weighted atoms, beginning with the
quartic and the degree-five `S_5` non-radical member.  (iii) Continue the
ternary-tree collision census of `F o F`; it remains a finite exact model of
one imprimitive submonoid even though it no longer controls KDeg.
