---
id: HYP-9032
title: "The transplant trichotomy: re-homing the 91-stalk laws on the live rows"
status: >
  OPEN synthesis with a complete typed decision tree; grounded
  claim-by-claim in PROVED canon (each input cited); two decisive
  single-row computations in flight this session (drift bypass,
  replica dichotomy). Nothing here removes a row; LRC(14) OPEN.
source: opus-2026-07-27 (from three-reader map of THM-2435/2436,
  THM-2365/2461/2466/2471/2478, THM-2512/2542/2544/2349)
related:
  - THM-2506 (Section 6 names the seam)
  - THM-2512 (the lawful-interaction host, 2/3 inputs proved)
  - THM-2542 (the clock-boundary host, semantically empty so far)
  - THM-2547 (parity-pinned transport)
  - HYP-9031 (the D5 dictionary; this is its LRC-side H^2 program)
---

# HYP-9032 -- the transplant trichotomy

> **RESOLUTION UPDATE (same session, 2026-07-27):** both in-flight
> computations returned the NONDEGENERATE branch (THM-2550, independently
> audited with the MISTAKE-283 scope repair): (A) owner-loop drift POSITIVE on the canonical typed row
> (both packet and bare tensors; exact rational drift energies; the
> escalation ladder dies at rung one -- no circulant hostile), and
> (B) NON-REPLICA (both additive defects nonzero, all 91/91 entries;
> all 5,184 cut modes fire at every large lawful clock; denominator
> floor supplied). Prediction P1 CONFIRMED. The live obligations are
> now: the ancestry/Boolean realization of the ignited host, and
> transporting the drift instance through THM-2368 (37) toward
> covering rows. The two computations share row/word labels but not a
> clock, packet, marked triangle, or common-root service. Sections below
> retain the pre-resolution analysis.
>
> **SECOND RESOLUTION (2026-07-28, THM-2575/2577):** prediction P2 is
> CONFIRMED by construction -- the base-only bridge pairing yields the
> first MISTAKE-281-clean 13x7 live-branch host W(k,ell) homed on the
> OWNER CLOCK, whose centred array satisfies (T1)+(T2)+(T3); and the
> word-depth collision law r = nu_13(deepest in-word blocker) opens
> the r = 5 unique-intertwiner window at sigma = {b}.  THM-2577 strengthens
> the finite clock census to every positive-return clock on both words
> containing `b`.  The existing host is on the distinct `{a}`, `r=3`
> packet, so the u-slaved THM-2512 bridge is a live **next construction**,
> not yet a proved contraction on one common word fibre.
>
> **THIRD RESOLUTION (2026-07-28, THM-2581 CANDIDATE):** that word-fibre
> mismatch is removed at the verified-exact candidate level.  Rebuilding the
> common-base array directly on the lawful `{b}`, `K=2`, `r=5` packet gives a
> centred owner-clock host satisfying (T1)--(T3), nonzero in all `91` cells,
> of maximal centred rank `6`, with all `1,638` cyclotomic `2 x 2` minors
> nonzero.  Its central reflection difference is a nonzero odd root profile
> with all twelve nontrivial colours; the separate `{a}`, `r=3` replay has
> that profile identically zero and exactly `90` zero minors.  Moreover the
> affine deep-root sidecar is literally `theta=t-2u=floor(2y)`, an endogenous
> half-circle label.  Independent hostile audit is still pending.  The next
> construction is now the **joint theta/deep-root contraction** on this same
> positive tensor, not another host: target activity, temporal endpoint
> identity, and scalar-row exclusion remain open.

## 1. Why the packet cannot be re-run (PROVED anatomy)

THM-2435/2436's punctured 91-stalk needs a fibre-constant deep blocker
(`nu_7(C_j) > M`): on a generic `7^(M+1)`-fibre such a blocker is
constant (removes the fibre or cuts a vertical column), which is what
creates the `F_7` column structure and the parent floor `(4+k)/7`. On
the live low-septimal rows (`nu_7(c_3) <= M`, forced by THM-2436 (42))
EVERY blocker occupies exactly `N/7` preimages per fibre -- averaged
out: no forced gap set, no parent floor, no vertical columns. **The
C_7 coordinate cannot be re-found on a live row; it must be re-homed.**

## 2. The three-input abstraction

The whole trio (all-72-colour saturation THM-2506, 11-of-12 tomography
THM-2507, 5,184-mode cut bundle THM-2508) consumes exactly three
inputs about a `13 x 7` array `d`:

```text
(T1) row-zero:        sum_{r in F_7} d(h,r) = 0 for every h;
(T2) dichotomy:       d h-independent  =>  d = 0;
(T3) magnitude floor: integrality or a denominator/l1 bound.
```

A transplant theorem = a PHYSICAL live-row array satisfying (T1)-(T3)
whose nonvanishing is target-active. The candidates and their exact
status:

| host | (T1) | (T2) | (T3) | nonvanishing | physical/target |
|---|---|---|---|---|---|
| `d_A` (THM-2512 lawful-interaction defect) | PROVED (4) | PROVED strongly (5) | OPEN (no denominator bound stated) | OPEN (replica branch alive via THM-2456 hostile) | OPEN (signed, not one Boolean event; not on the ancestry sheet) |
| `D_k` (THM-2535/2542 clock boundary) | PROVED (12) | all primitive modes nonzero (13) | integral by construction | PROVED | REFUTED so far (equal-mass hostile: full saturation + nonzero holonomy + EMPTY target-active edge set) |
| `B(q)` reshape (THM-2334 (46)-(49)) | OPEN | OPEN | OPEN | OPEN | the right target object; THM-2544 (the A-vs-B kernel tool) is an empty stub |

## 3. The native F_7 indices of a live row (and the transport pins)

A live row natively carries: the root character `kappa mod 13`
(everywhere, THM-2349 (5)); the 91-unit multiplier `m mod 7` (NOT
persistent -- THM-2452 recombination reselects `X` and `m`); the
septimal shift `s mod 7`; and the owner clock / visible source row
`ell in F_7` (THM-2449/2539 -- the ONLY native full-F_7 index).
Thirteen-adic clocks transport only the parity subgroup
(`13^L mod 7 in {1,6}`: THM-2547; THM-2334 (45)): **the full order-6
cut torsor cannot be moved by clock dilation.** Prediction (P2): the
eventual transplant re-homes `C_7` on the owner clock `ell`, not on
`m mod 7` and not on the transported cut label.

## 4. The decision tree (two computations in flight)

- **(A) Drift bypass** (THM-2365 (19b) on the canonical typed row):
  - `S(s,t)` nonconstant => `D_(H_E) > 0`: first drift-positive
    instance; the chain THM-2365 (31) + THM-2466 (32) + THM-2471 (48)
    is then all-PROVED on this row's packet -- the seam is bypassed
    there without any transplant. (Scope: typed row, not a cover.)
  - constant => escalate to the `13^3` masses; exact circulant law
    `H_E = G_E(r-t)` would be the sharp zero object (THM-2367's
    hostile shape realized on the canonical row).
- **(B) Replica dichotomy** (THM-2512 (26)-(28) on the same row):
  - `d_(M_rho) != 0` or `d_(C_rho) != 0` => all 5,184 primitive cut
    coefficients fire at every large lawful clock in the class;
    (T1)-(T3) hold on a live-branch array (the computed denominator
    supplies (T3)); residual = the ancestry/Boolean realization.
  - both zero => the canonical typed row sits on the replica branch:
    typed no-go for host `d_A`; the transplant burden moves entirely
    to the bidegree-(1,1) semantic 2-cell of THM-2542 (invoice
    `sum_k c_k = -7a`).

Open tension (P1, falsifiable): THM-2541 proves the same row's twist
bank has FULL target support (all 169 `A(q) != 0`). No implication
between full `A`-support and either dichotomy is known; if both (A)
and (B) come out degenerate on a full-support row, that conjunction
is itself a new exact object (and would sharpen THM-2544's mission).

## 5. Typed obstacle ledger (all PROVED, carried from the map)

(1) parity pin `{1,6}`; (2) ancestry-sheet rebase boundary
(`L > lambda` breaks `b`); (3) 13-primary invisibility (row-zero
kills every affine pushforward to a 13-group and every gauge-invariant
linear readout -- the `F_7` sidecar must be CARRIED, never summed);
(4) signed-not-Boolean (ANOVA centring); (5) the missing
bidegree-(1,1) cell (THM-2542 (25)).
