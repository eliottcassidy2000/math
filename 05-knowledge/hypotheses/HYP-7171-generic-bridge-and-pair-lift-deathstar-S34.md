# HYP-7171 — The generic-law bridge and the pair lift (death-star-2026-07-16-S34)

**Status:** CLAIMED — in progress (S34). Verify the build report in the session log
before consuming.

**Owner directive:** "connect kps's generic law to DenseCoreDissociatedB5Supply and
run the pair lift."

**Claim A (bridge, module `LRCDenseCoreRelationTrap.lean`):** the kernel-checkable
content of kps THM-934's mechanism on the chain-dense core: in `ChainDenseCore`
(ratios ≥ 3 at every pair index above the dense pair j),
(A1) every top-unit relation with below-mass ≤ 2·w(top−1) — the 3-AP/dilate shapes —
is impossible with top element strictly above the dense pair;
(A2) every unit-coefficient (±1) vanishing relation has top element ≤ j+3
(geometric ladder sum: tail ≤ w(t)/2 + 12·w(j+1) < w(t) for t ≥ j+4).
Adapter: `denseCoreDissociatedB5Supply_of_trapped` — the B5 supplier may assume the
relation-trap structure; the analytic singular-series bridge stays the named separate
obligation (codex-S28's honest note).

**Claim B (pair lift, module `LRCPairLiftDichotomy.lean`):** insert an S20 BLevel
PAIR at the last dense position and re-run the THM-937 split search through
`cite_blockchain_lonely`: the dense core shrinks from {ratio-<3 pair} to the
COMPRESSED-TRIPLE normal form {w(j+1) < 3·w(j) ∧ (w(j+2) < 21·w(j+1) ∨ pair-entry
failure (13−j)·w(j) < 13·(j+1)·w(j−1))}; dense cores at j = 11 (pair at the top)
close outright. Transition constants: single→single 3, single→pair 26/7,
pair→single 21, pair→pair 26.
