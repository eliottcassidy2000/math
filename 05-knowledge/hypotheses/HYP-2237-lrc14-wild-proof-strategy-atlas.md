# HYP-2237: LRC14 wild proof strategy atlas

**Status:** OPEN / STUB CLAIMED
**Session:** codex-2026-06-05-S661

This hypothesis reserves a focused atlas for a long LRC `n=14` strategy session.
The working guess is deliberately modest: the missing proof step is not another
raw exact search over speed rows, but a predicate-preserving side-channel theorem
that reattaches the information lost by the `Res_27`, odd-wall, pair-sum, and
scalar support quotients.

Evidence still missing at claim time:

- a broad repo search across the older LRC, tournament, unit-distance, Kakeya,
  deck-reconstruction, union-closed, pincer, rigidity, and automaton threads;
- a ranked strategy atlas with Tournament Analysis over proof routes;
- a concrete next experiment list for the `n=14` no-leak/carry-owner seam.

This stub exists only to reserve `HYP-2237` for that synthesis.
