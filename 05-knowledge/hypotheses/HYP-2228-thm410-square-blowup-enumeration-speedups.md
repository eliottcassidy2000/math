# HYP-2228: THM-410 and Square-Node Blowups Give Enumeration Speedup Carriers

**Status:** RESERVED by codex-2026-06-05-S652.

## Claim Stub

THM-410-style interval witnesses and square-node tournament substitution should
be treated as enumeration carriers, not just structural curiosities.

Reserved namespace for S652:

- use THM-410 interval-reversal matching data as a low-`c3` / near-transitive
  generator with additive triangle accounting;
- test node-square substitution, where each vertex of an `n`-vertex template
  becomes an `n`-vertex tournament block;
- compare direct Held-Karp / raw isomorphism search against block-run dynamic
  programming, modular decomposition, and canonical block fingerprints;
- connect the synthesis to A000568, H-spectrum gap unlocks, and recent S9
  high-gap sampling.

## Pending

S652 will add the computation, result file, reflection, result-index entry,
and full hypothesis text.
