# Independent audit: complete 9240 and 11088 word banks and zero cuts

**Verdict: PASS.** The two declared clocks close in the stated actual
connected-complement scope. The primary whole-word, complete native-bank,
and zero-component conclusions require no mathematical repair. This audit
imports or executes no mathematical producer.

## 1. Exact targets and independent path

The frozen primary targets are

* continuing13_20260908_lrc_zero_clock_probe_certificate.json,
  SHA256 a56f8d8bdee84f642d7f837a6c9e6398008eadbc0318b0778d6d0e435c0b0201;
* continuing13_20260908_lrc_zero_cuts_certificate.json,
  SHA256 23ae81ba6cb39bc14aecf2d93fc3fcf6d4e3c587b5240db35ea1d8f3992551f1.

The independent C++ consumer descends from the already audited continuing12
native consumer. It constructs the strict atlas from its multiplicative
definition, builds danger intersections from raw spatial walls, computes
all-phase capacities with signed events, and uses Kruskal for minimum
trees. Its seven-multiset enumeration has no partial-profile or partial-MST
pruning. Every completed word is tested against all 126 inherited proper
profiles. The separate Python consumer checks exact pins, entire arrays,
word-bank digests, owner trees, every recorded native zero ratio, every
positional component, and the complete exceptional cuts.

This explicitly fills the primary's optional unpruned-control gap at
domain sizes 21 and 20. It does not infer source completeness from the
primary's partial-completion pruning.

## 2. Complete finite universes and results

| Clock | Domain size | Entire unpruned multiset universe | Valid full-profile words | Minimum M-E |
| --- | ---: | ---: | ---: | ---: |
| 9240 | 21 | 888030 | 22469 | 108 |
| 11088 | 20 | 657800 | 18780 | 156 |

All 1545830 multisets are visited, and the complete 41249 valid-word
arrays match their frozen digests. Every native state satisfies 7d|t, so
E is identically zero on both entire domains. Both complete survivor lists
are empty, not merely empty under a selected tree or a partial word search.

The first geometry pass reconstructs all 441 pair-table entries, including
repeated sheets and impossible pairs, and agrees with every primary
minimum. It makes 38852 exact profile evaluations after its independently
proved separate-interval lower bound. A *second* scan then computes the
capacity of every compatible native ratio with no incumbent shortcut:

| Clock | Entire compatible-ratio bank | Exact zero ratios |
| --- | ---: | ---: |
| 9240 | 53195 | 2235 |
| 11088 | 48641 | 1849 |

Thus all 101836 compatible native ratios are paid, and all 4084 zero ratios
match the primary set with their directed margin roles. The second scan
also independently recovers every pair minimum. For each zero ratio it
rechecks every wall and chamber using direct open-interval integer counts,
and separately checks the entire literal quotient grid at an independently
found minimizing phase. The stored phase need not equal the producer's
chosen minimizing phase; the complete zero relation and directed roles do.
Each separate physical arm (ep,eq) has exactly the declared margins.
No simultaneous physical realization is inferred from these witnesses.

## 3. Full positional cuts and analytic consequence

Using the independently recovered zero relation, Python reconstructs the
graph on all seven positions of every valid word. All 41249 such graphs
are disconnected. Their complete component-size histograms and isolated
vertex counts agree with the primary. Exactly four words lack an isolated
vertex:

* At 9240: (6,8,8,8,12,15,15), with components {6,12}, {8,8,8}, {15,15}.
* At 11088: (6,8,8,8,9,9,36), (6,8,8,9,9,16,36),
  and (6,8,9,9,16,16,36).

For every exception the audit retains the actual positional component
partition and checks the declared full-cut crossing minimum against all
crossing pairs. It does not collapse repeated labels or replace full cuts
by vertex isolation. The whole-alphabet component sidecars are also
checked; they cannot substitute for the complete seven-position word.

The analytic consumer is sound. Under an unsafe translated grid, the seven
danger cardinality bounds sum to exactly the clock because E=0. Covering
the whole grid therefore forces pairwise disjoint danger sets. Every actual
strict edge must have a native zero ratio at this clock. An actual connected
strict graph would consequently be a connected spanning subgraph of the
possible zero graph. Its proved disconnectedness is a contradiction.
The argument never treats a possible edge as an actual edge and does not
require common phases or lifted prime depths once disconnectedness is paid.

## 4. Exact frontier and physical scope

The complete predecessor is the continuing12 depth-wedge array, whose
certificate SHA256 is
c56ea56a87c98cae1a16efe186696d4424942a9325d4c4189c5ce2bedbe3abea and
whose semantic array hash is
66f903430fd804074e6c8adb330aca4f6bb6b8f97d955e014a73309c39d17a8d.
Only 9240 and 11088 are removed. The entire resulting array is exactly
7618 clocks with maximum 11935 and semantic SHA256

    89a3e7545cc77467c5f85fbe0bdaab1f71071cff65184bbc1212eb86c9f07177.

The physical scope remains primitive rows of thirteen distinct positive
speeds, a selected six-label subset with one of these gcd clocks, and the
complementary seven labels' **actual strict-atlas graph connected**. All
126 proper profiles are retained. The result gives weak clearance and the
inherited selected-six lift. It introduces no speed-height bound, physical
unit speed, connectivity of the selected six labels, or unit-gcd premise
on the complement. General LRC(14) and disconnected-complement entry remain
open. This is not a clock-monotonicity theorem.

## 5. Reproduction and freeze

The normal run passes 203759 always-active native gates and 45415
always-active Python gates, for 249174 total. The optimized run compiles
with -O3 -DNDEBUG and repeats all gates; the normal native build uses -O2.
Normal and optimized raw LF output bytes agree. The full independent native
source and repo-relocatable Python runner are frozen with this report.

After relocation run

    python 04-computation/continuing13_20260908_lrc_independent_audit.py
    python -O 04-computation/continuing13_20260908_lrc_independent_audit.py

Outside the repository, --root and --producer select the frozen inherited
and target packet directories, while --work-dir selects disposable compiled
output. The checker pins and reads target certificates but never imports
or executes the producer. Its mathematical scope and exact finite universe
are fixed above. No maintained repository file was changed by this audit.
