# Independent audit of connected-decoder gcd descent

**Verdict: PASS.** The complete outgoing-edge proof, finite necessary clock
universe, and three sharp safe entry controls are accepted without repair.
The primary is `overnight12_20260906_lrc_decoder_descent.md`. Root read the
incoming joint-shadow theorem, the full primary proof and its verifier.
The separate verifier below imports no producer code and reconstructs the
finite controls through a multiplicative atlas and subset-gcd dynamic program.

## 1. The exact implication and its scope

For an actual boundary edge u--v leaving S, put d=gcd(S). Since d divides u,
at each prime the valuation of d/gcd(d,v) is at most the valuation of
u/gcd(u,v). This proves divisibility, not merely an inequality. Connectedness
supplies a boundary edge for every nonempty proper S, including disconnected
S. Repeated growth to seven vertices therefore multiplies the endpoint gcd
by factors at most355. It never asserts that an arbitrary added vertex has
that property.

The incoming joint-shadow theorem is already PROVED and independently audited.
Its seven-subset cap90 applies to physical coordinates of a primitive strict
failure. Thus the bounds31,950,11,342,250,4,026,498,750 for physical subset
sizes6,5,4 follow. Dividing by the core scale t converts them to primitive
shape bounds. The condition t<=2 applies to a hypothetical strict failure,
not to all safe decoder entries.

The exact edge multiplier divides an oriented atlas coefficient. Independent
multiplicative generation of admissible sums gives all5,855 ratios, whose
oriented coefficient set is exactly1,...,355. Multiplication by the incoming
42 seven-body gcd values therefore gives exactly6,121 necessary six-body
clocks, with maximum31,950. This list is a necessary arithmetic universe;
it is not asserted that each value occurs in an unsafe actual row.

The distinction from the unrestricted seven-tail sieve is correct. Here an
actual connected eleven-component supplies the missing outgoing edge. No
extension of the unrestricted theorem is inferred. Likewise a small gcd of
five labels does not supply the two-core-coordinate representation required
by the support-three crossing compiler. The report retains that unresolved
support and height obligation explicitly.

## 2. Full validity of the sharp controls

For k=4,5,6 the displayed base90 constructions have eleven distinct physical
core speeds and gcd2. The primitive shape contains a unit. The outside pair
g,3g, with g=2Q max(core)+1, is coprime to that scale. All three complete
rows are positive, primitive, distinct, and satisfy sum<=Q^2.

The declared internal edges really belong to the all-scale decoder atlas;
in particular1:355 uses356=4*89 and the base90-to12 edge uses2:15 with
sum17. The independent verifier reconstructs every edge and finds precisely
the claimed11+2 components, then computes rank11 by rational elimination of
their literal weighted relation rows.

Dominance excludes every bounded crossing. With one outside label and at
most two core labels, a nonzero outside contribution has magnitude at least
g>2Q max(core). With two outside labels their partial sum is an integer
multiple of g; a nonzero sum cannot be balanced by one bounded core term,
and a zero sum cannot be balanced by a nonzero core term. All internal
relations lie in their componentwise orthogonal spaces, already spanned by
the connected decoder rows. Hence W=V_dec, not merely rank of a selected
family. The actual-entry assertion is nonvacuous.

The controls pass every current inherited profile at subset sizes7 through12,
not just the scalar caps. A subset-gcd dynamic program checks all12,285
body/complement signatures across the three full rows. The maxima are
90,6,6,3,2,1. Every smaller core subset also satisfies its bound, and the
chosen top-k subset attains equality. Thus the scalar descent envelope is
sharp under the stated graph and profile constraints.

Every full-row speed is nonzero modulo7, so x=1/7 has clearance at least1/7.
This verifies the claimed strict safety. The examples cannot be described
as residual unsafe candidates, and the primary does not do so. Its repair
lineage is also accurate: the earlier base90 control with four odd-scale
small coordinates passed scalar caps but failed a full c90 profile.

## 3. Independent reproduction

```
python -B 04-computation/overnight12_20260906_lrc_decoder_descent_audit.py
python -B -O 04-computation/overnight12_20260906_lrc_decoder_descent_audit.py
```

The verifier passes61,138 always-active gates. It uses a sieve and bounded
products to generate the atlas, rather than factoring each candidate sum;
a bitmask recurrence computes all subset gcds; union-find and rational
elimination independently check the actual decoder graph and rank. Every
boundary edge of every nonempty proper core subset is checked, including
disconnected subsets, with exact loss divisibility. A separate prime-valuation
bank checks the elementary local inequality. The incoming JSON is pinned by
its original SHA256 before use and lives beside the source after filing.

The finite bank verifies normalization and the sharp examples. The universal
claim follows from the primewise divisibility proof and the explicitly cited
incoming hierarchy. No full LRC closure or five-to-three support reduction
is asserted.

**Filing:** root integrated these independently audited artifacts in the twelfth
checkpoint. Reproduction commands are relative to the repository root.
