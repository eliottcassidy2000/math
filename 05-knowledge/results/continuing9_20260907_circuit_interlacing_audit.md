# Independent referee: circuit words with one or two native interlacers

**Status: PASS — complete analytic review and independent exact controls.**
The C-only neighborhood theorem, all 27 explicit ternary witnesses, and the four strict full-model controls are accepted. No exclusion of an additional full-model word follows. The producer's mathematical claims require no correction.

Primary: [continuing9_20260907_interlacer_circuits.md](continuing9_20260907_interlacer_circuits.md). This referee reads the proof and frozen coefficient certificate but never imports or executes the producer. The previously audited [fixed-moment theorem](continuing9_20260907_fixed_moment_circuits_audit.md) supplies the exact coefficient chart. All computation in this referee uses standard-library rational arithmetic.

## 1. Native map, retained predicates, and the hostile

The ordinary coefficients of the five positive beta roots are `(1,13,55,x,y,z)`. Thus their sum is 13 and square sum is `13^2-2*55=59`. The native polynomials are exactly

```
B(v) = v^5 - 13v^4 + 55v^3 - xv^2 + yv - z,
C(v) = v^4 - 12v^3 + 45v^2 - (2/3)xv + (3/7)y,
D(v) = v^4 - 11v^3 + 36v^2 - (5/12)xv + (1/7)y.
```

For `h_k=e_k/binom(5,k)`, set `R_k=h_k^2/(h_(k-1)h_(k+1))` and the circuit quotient `C_k=R_k/R_(k-1)`. Direct reconstruction gives

```
R_1 = 338/275,
C_2 = 831875/(8788x),
C_3 = 13x^3/(166375y),
C_4 = 44y^3/(x^3z).
```

The unsubscripted interlacer C and the circuit quotients are different objects. The fixed-moment chart is an invertible continuous positive-coefficient chart near the all-one circuit vector. It preserves the two moments and the quotients; it does not preserve either interlacer merely by fixing those moments.

At distinct real B roots `b_1<...<b_5`, the residues `A(b_i)/B'(b_i)` are the weights of `A/B`. Since both native numerators are monic of degree four, positivity of every residue is equivalent to strict interlacing: the signs of A at the five roots must be `+,-,+,-,+`, giving one numerator root in each intervening interval and exhausting its degree. These roots are positive when the B roots are positive.

The Hankel factorization in the primary follows by partial fractions: its five-by-five matrix is `V diag(weights) V^T`, with the invertible square Vandermonde V. Its positive definiteness is therefore equivalent to positivity of the five residues. This argument uses the actual shared B denominator and actual C or D numerator. Free moments or unrelated real-rooted polynomials would not suffice.

## 2. The neighborhood theorem is valid

The center is exactly

```
x = 831875/8788,
y = 3460080078125/52206766144,
z = 791547381622314453125/52414354446428935168.
```

The referee independently constructs it from `q=275/338`, `mu=13/5`, and `e_k=binom(5,k) mu^k q^binom(k,2)`. Its ratios are `(338/275,...,338/275)`, so its three circuit quotients equal one exactly.

Separate rational Sturm isolation proves that B has five positive simple roots and C has four positive simple roots strictly alternating with them. Exact interval evaluation at the smallest isolated B root proves `D(b_1)<0`. Since `B'(b_1)>0`, this is a strictly negative D/B residue. It is an independent geometric rejection of D, without accepting a numerical approximation or relying on a Hankel determinant sign.

The frozen C leading minors are all strictly positive; the D leading-minor signs are `+,+,+,-,-`. The referee reconstructs every fraction, including the displayed negative four-by-four determinant. The ordinary D matrix through moment degree four is positive definite while its matrix through degree six is not. This is a precise low-order observer failure; no minimality among all shifted or localizing tests is asserted.

Positive simple B roots, strict C interlacing, and the strict negative D principal minor are open properties of the coefficients. Pulling back a sufficiently small common neighborhood through the coefficient chart gives an open neighborhood of `(1,1,1)` in circuit space with fixed moments, strict C interlacing and failed D interlacing throughout. Small independent positive, negative or zero changes of the three circuit coordinates realize every ternary word. Exact ties in a circuit do not force root multiplicity.

This proves an existential open neighborhood, not any prescribed numerical radius. The main theorem's geometric radius `1/2048` does not by itself certify C interlacing on that whole cube.

## 3. Exact finite witnesses and their limits

Every saved C-only word is regenerated by the ratio recursion, with target quotients `1+sigma_j/2048` and the exact scale `mu=13/5`. The complete universe is `{-1,0,1}^3`, with each of its 27 words appearing once. Each point has independently verified positive simple B roots and strict C interlacing. At every one of them D is negative at the smallest B root; the negative four-by-four D principal minor agrees.

These are 27 separately certified points. The audit does not infer a whole C-admissible cube from these points. The center is repeated as the zero-word record; there are 32 certificate records in total when the center and four full-model controls are counted.

The four full-model points are reconstructed as exact fractions from their terminating-decimal coordinates. Independent Sturm intervals show both four-root numerators strictly alternating with the five B roots. Literal circuit ratios verify exactly `+++`, `++-`, `+-+`, and `-++`. All their native C and D leading minors are strictly positive.

The complete classification of full-model circuit words remains **OPEN**. No absence of a word, in particular one containing two negative signs, is proved by the four controls or by the parent's numerical search. These are coefficient-model realizations. Even strict simultaneous C/D interlacing does not supply an integer Laurent support, an original phase equation, the inverse carry, or a response-sign conclusion.

## 4. Independent verification and reproducibility

The verifier uses fraction-valued polynomial division and Sturm sequences to isolate the roots of B, C, and, for full-model controls, D. It verifies actual root ordering rather than deriving interlacing from the producer's Hankel calculation. Interval Horner evaluation proves the native residue signs. No floating-point root is used.

All nine moments of each numerator are reconstructed by rational coefficient convolution, with a separate multiplication identity checked through degree eight. All five leading determinants are then computed by symmetric rational LDL elimination, not a symbolic determinant engine. Across the 32 records, all **576 moments and 320 leading determinants** match the frozen certificate. Coefficients and circuit values are independently recovered using cumulative-ratio recursion rather than the producer's closed exponent product.

The certificate and the inherited coefficient certificate are byte-pinned. The verifier supports adjacent files and the filed results-directory fallback. Reproduce with

```
python3 -B continuing9_20260907_circuit_interlacing_audit.py
python3 -B -O continuing9_20260907_circuit_interlacing_audit.py
```

The producer's input lookup needed one routine filing-layout clarification: prefer its inherited coefficient certificate beside the source, with the results directory as fallback. That does not alter a mathematical coefficient, certificate record or output. The parent owns final promotion and filing.

## 5. Connection board

- **Anchor / full model:** strict simultaneous interlacing is retained only in the four certified full-model controls. Further word exclusions require an analytic constraint using D; numerical omissions do not provide one.
- **Niche / fixed moments and circuit coordinates:** the all-one chart remains locally free even after adding C, so those constraints cannot restrict circuit sign words.
- **Wildcard / finite moment observation:** the center passes the ordinary D moment test through degree four but fails at degree six, while its negative native residue identifies the missing sign directly.

The usable source-to-target transfer is the exact coefficient chart followed by native residue evaluation. The destroyed predicate in the tempting broader transfer is D interlacing. The smallest decisive retained object here is the sign of D at the smallest B root, or its certified negative degree-six Hankel determinant. The next unresolved question is an actual full-model circuit restriction with that D information retained.

## Frozen audit record

Both runs pass **1,459 always-active exact gates**, with byte-identical actual LF stdout.

- Referee source SHA256: `2e06bcb4009ef5cac851d005135528538d252c2d015ab95b112bc136e206f4bf`.
- Normal and optimized stdout SHA256: `e3291feb54b1de7e157deaf2a3cc4764b946c317f40a5b3e74ffcdbfd7150839`.
- Audited producer source SHA256: `a2d27b5925c3992d23089405bf41911936eac97c31b346a80664129b8fd3c369`.
- Producer stdout SHA256: `0b3cc97abe03805dd757b21b2e88b364ae83c820bcd16e51a065ecffcd0b3841`.
- Producer certificate SHA256: `dfbae81c3078b21042cd4a33cc0717860caec3d9b858fdca461b136225a84657`.
- Reviewed producer prose SHA256 before promotion: `37dd6585f7fa90aae7a1eae167f16ca9a7c2a677c9b3fa7211ef616d7d59ade6`.
