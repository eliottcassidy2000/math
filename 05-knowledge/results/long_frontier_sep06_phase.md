# An actual fifth-clock phase word beyond the scalar decoder gates

**Status: PROVED family by elementary inequalities and FINITE-EXACT constants;
INDEPENDENTLY AUDITED.** This note gives 384061 distinct admissible integer
scales of a fixed unitless 5+8 actual decoder entry, each with an explicit
physical time of clearance strictly greater than 1/6. Every member passes the
inherited hereditary profile filters and fails the forward native,
cross-divisor endpoint, dual native, and whole-arc scalar gates specified
below. This is a safe family, not a hypothetical counterexample or a general
closure of decoder entries. LRC(14) remains OPEN.

The number 384061 counts scales. It is not a coefficient-height threshold or
a census of all entries up to a height. The physical box remains the fixed
box with Q=91^6 throughout.

## 1. Inheritance and the retained coordinate

The actual phase-packet instrument is already proved in
[the incoming phase-bank note](continuing1_20260906_lrc_phase_bank.md),
with its [independent audit](continuing1_20260906_phase_bank_audit.md).
Its seventh-clock corollary already explains the safe time of
[our preceding residual entry](open_frontier_sep06_decoder_residual.md).
The new content here is the fixed larger shape, the exact scale interval,
and a four-case fifth-clock word retaining the signed phase offset. We do
not claim a new general packet theorem.

The live board has six objects: actual decoder components, separated prime
supports, full signed coefficient boxes, hereditary cofactor profiles,
translated half-grids, and phase words modulo a small clock. The closest
proved mechanism is the half-grid lift of a component-safe rational phase.
The preceding residual entry is the canonical hostile to forcing scalar
closure from equality and profiles. Its seventh-clock repair is a near miss
for the present family: the larger maximum now exceeds every allowed scale,
so that sufficient repair no longer applies. The least-used sidecar is the
actual signed displacement of a selected packet point. The hostile in
Section 4 shows why its sign and size must be retained.

The source-to-target map is a fifth-clock point k/5 together with an actual
smaller-component half-grid lift x=k/5+delta/(10t). It preserves the physical
relation tx=1/2 modulo 1 and hence the smaller component's clearance 1/2.
Replacing the signed offsets for individual larger speeds by their maximum
absolute Lipschitz error destroys the useful residue information. The needed
sidecar is the list of eight residues ku modulo 5, with their affine offsets.
The cheapest decisive test was the nearest-lift hostile in Section 4.

Keep the full **THM-3818** domain from
`01-canon/theorems/THM-3818-scaled-inert-cubeclass-support-two-pair-packet.md`:
thirteen distinct positive primitive physical speeds, sum at most Q^2, the
actual all-scale decoder atlas, and the full span W of support-at-most-three
integer relations of coefficient height at most Q. The equality consumer is
[the complete two-component decoder](overnight14_20260906_lrc_general_decoder.md).
The hereditary profiles are the audited
[joint-shadow profiles](lrc14_joint_shadow_empty_core_next_sep06.md).
The new reverse-orientation comparison is
[the incoming dual-pair theorem](continuing1_20260906_lrc_dual_pair.md), with
[its audit](continuing1_20260906_lrc_audit.md).

## 2. The exact family and complete entry

Put

    Q=91^6=567869252041,
    q=(337,343,349,355), C=product(q)=14321146945,
    V={C} union {(356-q_i)C/q_i:i=1,...,4}
     =(40341259,287243635,542783995,807423715,14321146945),
    P=2*3*11*17*23*29=748374,
    U={P/p:p in {2,3,11,17,23,29}} union {P,9P}
     =(25806,32538,44022,68034,249458,374187,748374,6735366),
    K=max(U)=9P=6735366,
    D_min=min gcd(v,w)=115591.

For every integer scale t satisfying

    L0=Q/115591 < t < L1=9P,       gcd(t,5P)=1,       (1)

take the actual physical row n(t)=tV union U. Equivalently,
4912747<=t<=6735365 with the indicated coprimality condition.
Both normalized shapes are primitive and unitless. Every V label is odd;
the prime factors occurring in V and U are disjoint. In particular
gcd(tv,u)=1 for every cross pair, by (1). Thus n(t) is primitive.

The five-star edges have reduced sum 356=4*89. A spanning tree on the six
cofactor labels uses prime-index pairs

    (2,3),(3,17),(17,29),(2,23),(23,11),

with reduced sums 5,20,46,25,34. The additional links P/3--P and P--9P have
reduced sums 4 and 10. Every listed sum has only primes congruent to 2
modulo 3, each to exponent at most two, and is at most 356: these are actual
atlas edges. On the other hand,

    L0 min(V)=349Q >355K,

so every physical cross ratio exceeds 355. No cross atlas edge is possible.
The actual graph has exactly the stated two components. The source also
checks the entire graph directly on the finite controls.

The full physical box and distinctness hold throughout (1), because

    L1 sum(V)+sum(U)<Q^2,
    t min(V)>355 max(U),

and both shapes have distinct coordinates. No independent coordinate
rescaling or replacement by an abstract graph is used.

The two opposing mixed-relation obstructions are

    t min gcd(V pairs)>Q,
    t min(V)>349Q>262Q,
    max_{u<w in U}(u+w)/gcd(u,w)=262.                 (2)

For two smaller labels tv,tw and one larger label u, let D=gcd(v,w).
Cross-coprimality implies gcd(tD,u)=1, so the nonzero distinguished
coefficient of u is a multiple of tD>Q. This excludes all 80 supports of
that orientation. For two larger labels u,w and one smaller label tv, put
D=gcd(u,w). Here gcd(D,tv)=1. After changing the overall sign, any relation
with nonzero distinguished coefficient must read

    jtv=r(u/D)+s(w/D),    j>=1,    |r|,|s|<=Q.

The right side has absolute value at most 262Q, while the left side exceeds
349Q. This excludes all 140 opposite supports, including every higher
multiple of the primitive distinguished coefficient. A redundant pair
coefficient may be zero, so mixed supports of size two are included.

The actual edge relations have height at most 355<Q and span the two
internal weighted kernels. Every bounded support-at-most-three relation
stays inside one of those kernels by (2). Consequently **W=V_dec** for
every scale in (1). This establishes actual equality entry, not only a
sufficient graph pattern or absence of one chosen mixed orientation.

## 3. Every inherited profile passes

A body meeting both components has gcd one, because every cross pair is
coprime. A pure V body has at most five labels. The only nontrivial body
gcds of sizes 7 through 12 therefore arise by deleting one cofactor P/p
from U. The remaining seven U labels have gcd p. Deleting P or 9P instead
leaves gcd one. All six tails in the nontrivial case are coprime to p.

Thus the complete nontrivial profile list is

    (c; gcd(c,tail_i))=(p;1,1,1,1,1,1),
    p in {2,3,11,17,23,29}.                          (3)

All six belong to the frozen joint-shadow bank. Directly, the scalar
capacity inequality is 6 ceil(p/7)>=p and the forced pair intersection is
max(0,2 ceil(p/7)-p)=0. A prime clock has no further nontrivial divisor cut;
adjoining a tail makes its gcd one. The special clock-32 and clock-96
exclusions do not apply. The actual subset gcd maxima for body sizes 12
down through 7 are exactly (1,1,1,1,1,29), throughout the family.

These are necessary relaxed profiles for unsafe rows. Passing them does
not assert unsafety. Section 4 explicitly proves the opposite here.

## 4. The actual phase word and the failed nearest lift

Write r=t modulo 5 and choose

| r | k | delta | physical phase x_t |
|---|---|---|---|
| 1 | 2 | 1 | 2/5+1/(10t) |
| 2 | 1 | 1 | 1/5+1/(10t) |
| 3 | 2 | 3 | 2/5+3/(10t) |
| 4 | 3 | 1 | 3/5+1/(10t) |

Since t is odd, each row of this table satisfies
2tk+delta=5 modulo 10. Therefore tx_t=1/2 modulo 1. All V labels are odd,
so ||tv x_t||=1/2 for every smaller physical label. This is the actual
physical affine lift; no normalization changes the clearance target.

For u in U set a_u=(ku modulo 5)/5. The exact phase modulo its fixed
integer branch is

    a_u+delta*u/(10t).

Across the entire continuous interval L0<t<L1 it lies between

    ell_u=a_u+delta*u/(10L1),
    h_u  =a_u+delta*u/(10L0).                        (4)

The 32 rational endpoint computations, one for each pair (r,u), give
0<ell_u<=h_u<1. Thus there is no hidden integer crossing in (4), and

    ||u x_t|| >= min(ell_u,1-h_u).

The minimum over the eight labels in each residue class is

| r | exact uniform lower bound from (4) |
|---|---|
| 1 | 637017991/3311190974 |
| 2 | 1529494468/8277977435 |
| 3 | 2932887917/16555954870 |
| 4 | 1613561814/8277977435 |

Consequently every row in (1) has the explicit bound

    min_i ||n_i(t) x_t|| >= 2932887917/16555954870 >1/6.    (5)

This is a uniform strict improvement over 1/14 for this family. It is not
claimed to be the optimum margin, even for a single family member.

The nearest fifth-clock lift is insufficient. At the actual admissible
scale t=4912753, which has residue 3, the choice k=4,delta=1 gives

    x_near=7860405/9825506,
    min_i ||n_i(t)x_near||=309014/4912753 <1/14.

The larger maximum 9P is the offending label. The farther lift prescribed
by the table instead gives

    x_t=3930203/9825506,
    min_i ||n_i(t)x_t||=1740589/9825506 >1/6.

The failed implication is that minimizing the displacement from a rational
packet point optimizes the whole translated phase. The strongest survivor
is the inherited sufficiency of the phase-packet instrument. Its repaired
application retains a different base point and the actual signed offsets.
This hostile concerns a selected time, not the safety of the row.

## 5. Precisely which scalar consumers still fail

All comparisons retain the true larger maximum K=9P. In every forward
native test the cancellation divisor is gcd(gcd(u,w),tv)=1. For a reduced
larger pair a=u/D,b=w/D, its exact central signed-box radius is

    R=Q(a+b)-(a-1)(b-1).

Every one of the 140 scores from
[the native cross-divisor theorem](open_frontier_sep06_decoder.md) fails:

    max_{u<w,v} 5R/(63Kv)
       =154916434855/3564749706111 <1.               (6)

Indeed R<=262Q, with equality at the primitive pair 1:261, and the maximum
uses min(V). For all 35 maximum-endpoint comparisons the coefficient
condition 30D<=Q holds, but the width condition fails:

    min lcm(gcd(u,K),v)=1041046529754 >5Q/63.

The smallest endpoint gcd is 25806. This also lies beyond the stronger
5+8 cutoff 26 in the incoming
[leaf-cofactor theorem](second_20260906_entry.md); the earlier generic
endpoint constants are not treated as the current frontier.

The incoming dual-pair theorem uses two smaller primitive labels A,B,
D=gcd(A,B), and an outside larger label. Its cancellation divisor is
gcd(tD,u)=1 here. Every one of its 80 native width conditions fails:

    max_{A<B,u} 5Q/(63KD)
       =1182568205/20428365078 <1.                   (7)

Thus the additional orientation does not settle this family through that
scalar comparison. Its sufficient theorem is unaffected.

The two universal whole-arc grid conditions also fail throughout (1):

    5t<63K,            8g<42 max(V),    g=1.         (8)

The incoming seventh-clock sufficient repair requires max(U)<t, whereas
every scale here is smaller than max(U). The incoming 23-bank safe-family
theorem has a 6+7 split and its specific A23 residue hypothesis; this actual
5+8 family has neither that split nor all smaller residues in A23. This
is an absent hypothesis, not a refutation of the bank. The larger shape
has no unit, and the actual graph is not 11+2. No claim is made that every
other known safety mechanism fails; (5) is itself a safety mechanism.

## 6. Finite exact universe, count, and reproduction

[Source](../../04-computation/long_frontier_sep06_phase.py),
[output](long_frontier_sep06_phase.out). The proof for all scales is (1)--(5),
with fixed integer constants and 32 rational affine endpoint comparisons.
The source does not scan all 384061 scales. Inclusion-exclusion over the
seven primes dividing 5P counts exactly 384061 eligible integers. A second
count uses 64 divisor conditions separately in each nonzero residue class
modulo 5, obtaining

    r=1:96015, r=2:96015, r=3:96014, r=4:96017.

The finite physical-control universe is the first and last eligible scale
in each of the four residue classes:

    (4912751,6735361), (4912747,6735347),
    (4912753,6735353), (4912759,6735359).

For each of these eight rows the source checks every graph edge, all 220
mixed supports with both zero-allowed pair coefficients, all 4095 body
subsets of sizes 7 through 12 against the pinned full profile bank, and
the literal thirteen-speed physical clearance. The signed-box congruence
solver and the separate coefficient/support inequalities both exclude the
mixed supports. The exact scalar score comparisons cover all 140 forward,
80 dual, and 35 endpoint selections as scale-independent calculations.
The actual nearest-lift hostile and its repair are retained separately.
No floating point arithmetic or broad height census supplies a proof input.

```
python3 -B 04-computation/long_frontier_sep06_phase.py
python3 -B -O 04-computation/long_frontier_sep06_phase.py
```

The inherited profile JSON is pinned to SHA256
`935f3f687b6d7c89cc099e536f536238fd753bcc4c1747906d213cef387ca93f`.
Normal and optimized outputs are byte-identical, with 38683 explicit gates.

* Source SHA256: `51b8acf006ea19cac1804d06758b24045b0cff0f4b4598c459e032250bbcbe43`.
* Output SHA256: `c3a77805e6b9d240c93df8c0904295dcfb81c5f77a5244a5d0697363fb6c1191`.
* Semantic digest: `a8c0f182e22fe82240717515e7e6fa32eab9d79cf67a316365af9610523c97a7`.

The [independent referee](long_frontier_sep06_phase_audit.md) checked the
full proof and source, recomputed the four scale counts and all 32 affine
bounds, checked both complete relation orientations, hereditary profiles,
every named consumer's scope, and the actual hostile and repaired phase.
Normal, optimized, and frozen outputs and both raw hashes were independently
replayed: PASS. Source and output are frozen.

The next structural question is whether small actual phase words can be
selected from entry arithmetic for a broader component class. The present
result supplies one complete family and a concrete warning against replacing
that selection problem by the closest lift or a worst-case gap statistic.
