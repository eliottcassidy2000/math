# Continuing synthesis: identical unit futures, hidden arms, and exact repair

**CURRENT CHECKPOINT, 2026-09-08. Status: PROVED scoped results,
FINITE-EXACT certificates and independent audits.** On fixed W2, identical
pointed Weyl modules generated by the unit coexist with arbitrarily many
ambient torsion arms. Two more LRC clocks close, the globally submersive
quadratic rational-mate class is exhausted, and matching completes a finite-direction
repair regime. General LRC(14), planar JC and extremal no-three-in-line
remain **OPEN**. No external priority claim is made.

The [previous synthesis](continuing11_20260908_synthesis.md) supplies the
unit-order-two example, coefficient-rank mechanism, three composite clocks,
and third-direction repair charge. This session starts from23f9506e8;
no new incoming commit was present at its two initial review points.
The [manifest](continuing12_20260908_manifest.json) pins proofs, certificates,
independent replays and inherited artifacts.

The final packet has49 pinned artifacts and12 independent/primary engines.
The parent replayed **202,200 always-active exact gates in each of normal
and optimized Python**, with identical raw LF transcripts and11 unchanged
regenerated certificates. All909 inherited artifact pins remain intact.
The unbounded statements use analytic proofs and independent referees;
finite controls retain their explicit universes and hostile examples.

## 1. Portfolio and the question that changed

The anchor was zero-credit connected-complement entry for LRC. The niche
was another exact global quadratic pencil. The wildcard was the actual
incidence hidden by three-direction grid repair. Six concepts stayed live:
original atoms; the complete native graph; unsaturated versus clipped
valuations; both physical surface charts; labelled principal coefficients;
and the distinction between a generated module and its ambient module.

The inherited mechanisms were the full126-profile forest consumer,
prescribed-clock valuation paths, complete exact quadratic pencils,
the canonical component connection, and original-cell repair duality.
Hostiles were locally compatible but jointly unrealizable valuation paths,
source submersions with a boundary critical point, scalar unit order
losing coefficient rank, and a three-direction incidence triangle losing
flow integrality. The corrected near miss was to infer ambient geometry
after restoring only every operation on the distinguished unit. The
underused sidecars were the middle speed's full prime depth, a different
global linear function with nonconstant boundary value, and three-way
cell incidences instead of their pairwise clique.

The root's higher-degree construction overtook the original niche. It
shows that even the complete unit-generated module can miss arbitrarily
much ambient torsion. This changes the next test: more derivatives of
that same unit cannot recover the omitted components.

## 2. All unit Weyl relations stay fixed while ambient torsion grows

Fix nonzero complex b,d,h and any integer n>=3 on the SAME W2. Put

    u=x-h, z=u-2h+u^3t, W=u z^2,
    F=u z(b+dW^n).

**PROVED.** F is a global submersion and has an explicit rational mate.
Its source-t degree is2n+1. Its zero fibre in the original source has
n+2 reduced disjoint irreducible components; every other source fibre
is irreducible. The complete source torsion therefore has n+1 arms,
but the distinguished unit has exact order SIX and generates just TWO
of those arms under the full canonical Weyl algebra.

The [primary construction](continuing12_20260908_fixed_unit_hidden_arms.md)
and [independent referee](continuing12_20260908_hidden_arms_audit.md)
give the complete proof. The actual mate is

    S(W)=sum_(m=0)^5 binom(5,m)d^m b^(5-m)W^(nm)/(nm+3),
    G=S(W)/(u^3(b+dW^n)^6).

The identity (W^3S)'=W^2(b+dW^n)^5 proves J(F,G)=1 directly and
F^6G is polynomial. The original source derivative on u=0 is-2bh.
On the added boundary the tangential derivative is-b. Elsewhere valid
(u,y=uz) coordinates prove submersion. These checks cover all points.

The zero components are u=0, z=0 and W=rho for the n roots
rho^n=-b/d. The nonzero source fibre over c is exactly
C[W,W^-1,(b+dW^n)^-1], via
u=c^2/[W(b+dW^n)^2], z=W(b+dW^n)/c. This pays all fibres and the
rational constant field C(F), rather than only a chosen dense chart.

In the SAME parameter g=F, the complete principal parts are

    E_u: -8b^2h^3/(3g^3)-2bh/g^2,
    E_z: 0,
    E_rho: b^5rho^3 c_n/g^6,
    c_n=120 n^5/product_(j=0)^5(3+jn) !=0.

The regular z component stays present before quotienting by the common
diagonal. These coefficients span exactly two independent directions.
After a constant basis choice the unit is always

    theta=A/g^6+B(g^-3+lambda*g^-2),
    lambda=3/(4bh^2).

At fixed b,h, this is the SAME pointed Weyl module for every n and d.
The primary gives its exact left ideal using g^6 and four explicit PBW
relations; g^2 theta and g^5 theta form a polynomial derivative basis
with determinant-1. Thus equality includes EVERY relation among every
finite sequence of multiplication and canonical differentiation, not
merely matching scalar order or finitely many jets. Meanwhile the
ambient quotient by this generated module has n-1 additional arms.

This is a strict strengthening of the previous same-order/different-future
example. Here even all legal futures of the unit agree. Additional
response classes or the actual fibre components are necessary to see
the ambient growth. The statement concerns the original ring C[x,t];
it does not replace it by O(W2), and these rational mates have poles.

There is a second unbounded invariant invisible to this same unit module.
The [independently refereed pair-degree corollary](continuing12_20260908_pair_degree.md)
proves

    [C(x,t):C(F,G)]=5n+3.

Indeed C(x,t)=C(g)(W), and g^6G=H_n(W) is a polynomial of exact degree
5n+3 with leading coefficient d^5/(5n+3). Every other rational mate
with a nonzero constant Jacobian is lambda*G+R(g), so it generates the
SAME embedded pair field. This degree cannot be reduced by choosing a
different rational mate. The earlier all-m power-map example had pair
degree equal to unit order; the present family separates them while
keeping every unit Weyl relation fixed. These are rational pairs with
poles, not polynomial Keller maps.

## 3. The global quadratic rational-mate class is now exhausted

The [whole-pencil classification](continuing12_20260908_quadratic_pencils.md)
and [independent audit](continuing12_20260908_quadratic_pencils_audit.md)
retain the full global coefficient boxes and both charts.

* The pencil u^3 span{1,u} forces a source-critical line.
* The pencil u^4 span{1,u^2} has two exhaustive branches: one is
  source-critical, and the other has a boundary critical point or an
  entirely critical boundary. Neither has a global submersion.
* The pencil u^7 span{1,u} has exactly the global submersions

      z=u-2h+u^3t,
      F=s+u z[(au+d)z+k],
      d*k*h*(k-2dh)!=0, a and s arbitrary.

Every member of the last family has a rational mate, exact source unit
order THREE and exactly two full torsion arms. Its unit generates both
except on the two admissible complex branches

    k/(dh)=(15 plus-or-minus sqrt(-15))/10,
    a=3(2dh-k)/(4h^2),

where it generates one. There is no real alignment locus. An exact
PBW compiler obtains the full unit annihilator from the constant
relations between its three principal-coefficient vectors. The all-degree
operator proof preserves both admitted source gcd jumps and a separate
component pole drop; neither is confused with the alignment locus.

The [last two-pencil proof](continuing12_20260908_last_quadratic_pencils.md)
and [independent referee](continuing12_20260908_last_quadratic_pencils_audit.md)
finish the independent table. The pencil span{1,x} is source-submersive
but has an entirely critical added boundary. The elliptic pencil
u^5 span{1,u^3} has exactly the global submersions

    F=s+x(1+x^2t)[d(1+x^2t)+p x], d*p!=0.

With w=1+x^2t, g=F-s and v=w/x, the exact rational mate is

    G=v/(3g)=1/[3x^2(dw+px)], D_F v=3g.

Every nonzero completed fibre has genus ONE. The full field satisfies
x^3=g/[v(dv+p)]; it is a cubic extension of C(g)(v), not a rational
parametrization. Geometric integrality pays the rational constant field
C(g). Its complete source principal parts are

    (d/(3g^2)+p/(3dg), 0, -p/(3dg)).

The two coefficient directions are independent. Thus the unit generates
both full torsion arms, with exact pointed Weyl module D/Dg^2. The
earlier genus-zero unit-order-two example has this SAME pointed module.
All unit operator relations therefore fail to determine generic-fibre
genus as well as the ambient arm count in section2.

The remaining dependent-pencil obligation closes by a different, simpler
operation. If Delta=P^2-4NQ=cN, factor N=K R^2 with K squarefree. Divisibility
forces P=2K R S, whence F+c/4=K(Rt+S)^2. The nonempty curve Rt+S=0 is
critical in the original source. The [dependent-pencil closure](continuing12_20260908_dependent_pencil_closure.md)
therefore excludes every such genuine quadratic even before globality.
The [independent audit's separate proof](continuing12_20260908_last_quadratic_pencils_audit.md)
also checks a direct square-completion proof on N!=0.

**PROVED COMPLETE CLASSIFICATION.** Combining this exclusion with the
inherited exact-pencil rational-mate criterion leaves precisely THREE
families of globally submersive functions on fixed W2 of original source
t-degree exactly TWO admitting a rational mate:

| Surviving family | Exact unit order | Full torsion arms | Generic genus |
|---|---|---|---|
| Fifth-order pencil, continuing11 | 2 | 2 | 0 |
| Seventh-order pencil, above | 3 | 2 | 0 |
| Elliptic pencil, above | 2 | 2 | 1 |

The cited family proofs give all parameter conditions and unit-cyclicity
walls. The exact unit-order spectrum in this entire class is {2,3}.
The higher-degree construction in section2 escapes its two-arm ceiling.
This classification concerns rational mates with poles. General source
degree, global regular mates and planar JC remain outside its conclusion.

## 4. A full middle valuation closes the zero-credit LRC wedge

**PROVED + FINITE-EXACT.** The declared clock probe tests6930, then7560
and stops at the first complete residual. It covers42,550 full-profile
word/clock evaluations. Clock6930 closes with minimum margin178.
Clock7560 has exactly six zero-credit survivors; the later declared
clocks9240 and11088 were not tested.

The [complete probe](continuing12_20260908_lrc_zero_clock_probe.md),
[native depth proof](continuing12_20260908_lrc_depth_wedge.md), and
[independent unpruned/native audit](continuing12_20260908_lrc_independent_audit.md)
close all six7560 survivors. Every connected zero-edge graph on each
survivor forces the same4--24--18 path through its UNIQUE24 label.
Each survivor has total overlap budget E=0. Hypothetical full unsafety
therefore forces every actual strict edge to have zero overlap; actual
connectivity then supplies this zero-edge graph and its forced path.
The complete native zero banks force incompatible depths:

| Edge | Complete oriented zero ratios | Forced v2 of the middle speed |
|---|---|---|
| 4--24 | 307/48 | 6 |
| 18--24 | 309/8,303/16,219/128,201/152 | 4,5,8 |

The endpoint margins4 and18 have unsaturated2-adic depths2 and1 at
clock7560. Their true valuations are therefore fixed. The middle's
clipped margin24 alone hides its true depth, which the two actual ratios
recover incompatibly. This rules out the path before any endpoint
overlap calculation. The quotient grids1890 and1260 are recomputed;
no clock7200 ratio weight is reused.

The independent audit enumerates1,731,324 unpruned multisets, all681
compatible native ratios in the two banks, and all30 connected
zero-subgraphs across the six residual words. The mechanism is a genuine
joint realization obstruction. Separate pair compatibility and a
possible zero-edge graph had discarded the missing middle depth.

The physical scope is primitive rows of thirteen distinct positive
speeds, a selected six-label A with one of these gcd clocks, and the
complementary seven labels' ACTUAL strict-atlas graph connected. All126
proper profiles are retained. There is no height bound, physical unit
speed, connectivity of A or unit-gcd assumption on the complement.

The necessary array now contains **7,620 clocks, maximum11,935**, SHA256
`66f903430fd804074e6c8adb330aca4f6bb6b8f97d955e014a73309c39d17a8d`.
Only6930 and7560 were removed here. General and disconnected-complement
entry remain open.

## 5. Graph matching completes the sparse-incidence repair regime

The [finite-direction theorem](continuing12_20260908_no3_matching_completion.md)
and [independent audit](continuing12_20260908_no3_matching_audit.md)
replace the failed extension of bipartite flow by the correct integral
graph problem. The vertices are ORIGINAL overfull lines; a shared
original cell is an edge only when it touches exactly two such lines.

For any finite direction set, if every cell touches at most two
overfull lines, adjoin every zero-incidence cell for free. The remaining
retained cells form an exact capacity-two graph b-matching, with unit
cell edges and private leaves. When all overfull
lines are triples, write Gamma for their intersection graph and v for
its vertex count. Then

    exact deletion tau=v-maximum_matching(Gamma),
    fractional deletion tau_LP=v-fractional_matching(Gamma),
    Boolean line-selection bound beta=independence_number(Gamma).

These hold for the whole declared regime, not only triangles. A new
two-regular6-by-6 board with rows (03,12,04,14,35,25) has Gamma=C5 and

    beta=2 < tau_LP=5/2 < tau=3,
    previous two-direction cost=2, safe third-direction increment=0.

The matching theorem sees the missing deletion. No smaller two-regular
square board has a five-cycle component in this regime. The n=6 discovery
search was bounded, not exhaustive. Separated copies give unbounded
additive gaps on finite integer boards with empty rows in their bounding
squares; no saturated-board or probabilistic improvement is inferred.

There is an exact extension to ALL finite boards and finite direction
sets. Let H be the original cells touching at least three overfull
lines and C those touching one or two. For each retained subset R of H,
reduce each line's capacity to2-|R intersect line| and solve the remaining
integral graph b-matching. If mu(R) is that optimum, then

    tau=|H|+|C|-max_valid_R (|R|+mu(R)).

This uses at most2^|H| graph calls, with no claim that |H| is small. A
concurrent-triple cell must remain one three-way action: a real n=5
board needs one deletion while its naive pairwise triangle predicts two.
Conversely a triangle with a pendant vertex has a perfect matching and
no gap. Both concurrency and attachments are necessary coordinates.

The safe-original-cell charge also composes over arbitrary ordered
direction blocks on disjoint original strata. It remains an adaptive
original-cell-loss lower bound. This session supplies structural exact
reductions, with no new random-board mean coefficient or extremal claim.

## 6. What the connections now require

All three lanes distinguish a correct observable from a complete state
for a different question. The unit's pointed Weyl module really is
complete for every future operation on that unit, yet incomplete for
ambient geometry. The clipped clock margins are necessary local data,
yet incomplete for a joint labelled path. The Boolean deletion dual
is valid, yet needs nonbipartite matching and genuine hyperedge actions
to recover exact integral repair. Each failure has an actual witness
and a paid replacement, rather than a general analogy.

Next decisive tests:

1. Inspect a response class outside D theta or a labelled fibre invariant
   if the goal involves the hidden arms. Repeating unit derivatives on
   the section2 family cannot distinguish its ambient ranks.
2. Test source degree three for a global submersion with a rational mate
   and unit order one. The complete genuine-quadratic spectrum {2,3}
   closes that search in degree two; higher degree needs its own proof.
3. For another zero-budget clock, recover native ratio-induced middle
   depths at an unsaturated endpoint before attempting a more expensive
   common-phase overlap. This is not a clock-monotonicity principle.
4. For grid repair, bound or exploit the actual exceptional-cell set H.
   The exact branching formula supplies no small-|H| theorem by itself.
   Count odd-cycle components with their attachments, not arbitrary cycles.
