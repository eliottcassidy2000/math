# Independent audit of fifth collision payment and retained target forms

**PASS: analytic all-degree argument and independent exact controls.** The
three results of [the source/target report](overnight7_20260906_jc_fifth_module.md)
are accepted in their explicit categories. A lawful polynomial source shear
can pay the fifth collision coefficient; the full filtered target-Hamiltonian
retained image cannot; every order-ten-or-later graph change leaves the
existing order-eight obstruction to a nonzero constant source density intact.
None constructs a Keller pair or settles planar JC(2).

## Analytic dependency and quantifier audit

The source construction uses the actual polynomial plane definition of
[THM-3612 / russell-cylinder-even-fold-nongraph-collision-jet-rigidity](../../01-canon/theorems/THM-3612-russell-cylinder-even-fold-nongraph-collision-jet-rigidity.md).
The graph equation and its quotient `K[x,w]` require no parity condition on
Q. The other theorem statements in that older file have their own parity
hypotheses; those are not silently generalized here. A triangular shear has
an explicit polynomial inverse and determinant one. Characteristic zero
supplies its relative source Hamiltonian by integrating h(x). This is not
an assertion that that Hamiltonian descends to the target ring.

The inherited collision coefficient and its clock are checked against
[THM-4424 / russell-constant-normal-debt-discriminant-contact-correspondence](../../01-canon/theorems/THM-4424-russell-constant-normal-debt-discriminant-contact-correspondence.md).
Subtracting its known fifth-order repair gives the affine compatibility
condition `L(h)=8chi5=-18kappa/5`; the rank-five linear system acts on the
first changed coefficient, with all lower forcing fixed. The constant
pencil parameter is s, while the source fold has s=t^2. Thus its fifth
coefficient is t^10 and its equal collision images are only asserted modulo
t^12 for the displayed truncated polynomial source.

For an arbitrary actual polynomial F in the Russell cylinder ring, regraphing
the Hamiltonian variation at fixed x gives exactly `-partial_x A_F`.
The target space permits all lower-order cancellations in F; it is not the
smaller span of individually prefix-vanishing monomials. Subtracting a
polynomial in the stable coordinate w removes the x-constant prefix without
changing the normal response. This justifies the normalized premise
`A_F=t^10 a(x)+O(t^11)` for the upper-bound proof.

At the smooth surface triple, the vanishing base restriction first kills
the gradient and then the entire Hessian. The latter step needs all three
tangent directions, not just the dimension-two tangent span; their quadratic
evaluation determinant is 63180. With the inherited branch collision
congruence modulo t^10, the zero constant Hessian improves the gradient
congruence to modulo t^11. The formal tangent relation has a unit minor,
so its coefficients can be chosen with constant vector `(5,-18,13)`.
Taking the first nonzero pullback coefficient then proves `L(a')=0`.
The same Taylor argument kills the differences of a's three values.

These are all-degree local arguments applied to every global polynomial
F; no truncation rank is used as their proof. The polynomial generators
`w^10 C`, `w^10(E+3)`, and `w^10` attain the two derivative directions and
the common value independently. The full retained primitive first-jet image
therefore has dimension three, and the normal-value image is exactly ker L.
The normal space is K-linear, not asserted to be a K[x]-module. Multiplying
a valid element by x would contradict the retained image in general.

The filtered two-form lemma is valid for arbitrary regular coefficient
germs: a map difference O(t^N) remains of that order under x differentiation
and drops at most one under t differentiation. Thus at N=10 all J0 through
J8, with all retained x-jets, remain identical to the original map. The
complete arbitrary-target-form relation in
[THM-4046 / exceptional-quartic-j7-lift-and-j8-obstruction](../../01-canon/theorems/THM-4046-exceptional-quartic-j7-lift-and-j8-obstruction.md)
continues to give `c*kappa=0` for a proposed nonzero constant source density c.
The auditor read the complete 45-row retained packet specification, including
the three arbitrary two-form slots. Its original 495-column census was not
rerun; the new proof transports that proved relation unchanged. Formal unit
densities, arbitrary source reparametrizations and earlier source jets remain
outside this exclusion.

## Independent exact controls

The independent script imports no producer or repository mathematical code.
It rebuilds Q and the actual compiler symbolically, differentiates the full
polynomials to recover the three tangent rows, checks the target equation,
the complete Hessian determinant, the source shear determinant and the
compiler two-form coefficient. It verifies kappa is nonzero at all roots of
the exceptional quartic by a polynomial gcd, and checks the exact chi5 and
first visible density-period identities.

A distinct dense-array Taylor implementation starts from the globally
expanded compiler and translates x using binomial coefficients. It then
rebuilds the full 140-column weighted local primitive packet at two fresh
exceptional embeddings `(p,alpha)=(467,169),(487,313)`. Incremental
column-space elimination, rather than the producer's row reduction, gives
ranks 105,107,108 for prefix constraints, normal outputs, and the full
value/derivative packet respectively. The L-row and both branch-value
differences are in the prefix row span, while the three actual global
polynomial witnesses attain all allowed retained dimensions.

The modular packet is explicitly a finite local relaxation of global
prefix vanishing. It is a hostile control of the analytic proof, not a
characteristic-zero rank upper bound. The source also preserves the sharp
`t^9 x` hostile: it changes J8 by `36(-1,0,1)` with Lambda value16, but its
collision period is nonzero. No claim treats that map as a collision-preserving
escape. This is the next constraint to test.

The primary was replayed normally and optimized: **226 gates PASS**, equal
outputs. Only its final printed scope sentence was clarified to mean arbitrary
target two-forms with nonzero constant *source* density. The mathematics did
not change. The independent script passes **47 gates**, also byte-identical
normally and optimized.

```text
python -B 04-computation/overnight7_20260906_jc_fifth_module.py
python -B -O 04-computation/overnight7_20260906_jc_fifth_module.py
python -B 04-computation/overnight7_20260906_jc_independent_audit.py
python -B -O 04-computation/overnight7_20260906_jc_independent_audit.py

primary source 0a478c1e94ff0b9ad69de2727801f6d0413f7df4a1275aa0a75f0885a4f5cdc1
primary output c73b297befb08788e6d5ad12472df9477d93f7cac8161bd25c56d6964ea63b5f
audit source b070b1515c762cc47444ed4844477657ca605509461cdbbb9c8aa4e483651f11
audit output fd4f9f81bb1423d97611ca2b13c8c4c8dafaf8bf4cb616b5735cf4634b3fb925
```
