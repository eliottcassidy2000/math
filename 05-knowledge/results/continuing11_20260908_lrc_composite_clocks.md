# Three composite clocks close through forced positive connections

**Status: PROVED in the inherited connected-complement scope +
FINITE-EXACT; independently audited.** The complete declared clocks 10080, 10800 and
11520 close in the connected-complement entry reduction. This removes three
clocks and leaves 7,622 necessary clocks, maximum 11,935. LRC(14) and
disconnected-complement entry remain **OPEN**.

## 1. Inheritance, bounded experiment and exact scope

The immediate supplier is the audited
[minimum-tree consumer](continuing8_20260906_lrc_minimum_tree.md), with its
complete 126 proper-subset profile conditions and margin-conditioned strict
ratio atlas. Its grid capacity and forest mechanisms retain **THM-2060**,
`01-canon/theorems/THM-2060-crt-tail-coset-saturation.md`, and **THM-2064**,
`01-canon/theorems/THM-2064-multitail-sheet-capacity-and-dyadic-seam.md`.
The strict atlas is **THM-3818**,
`01-canon/theorems/THM-3818-scaled-inert-cubeclass-support-two-pair-packet.md`.

The source necessary array is the full audited
[continuing10 upper-twenty array](continuing10_20260908_lrc_upper_twenty.md),
which already includes the separately proved clock7200 closure. It has
7,625 elements, maximum 11,935, and canonical JSON SHA256

    51c00db52c18732ec6128e4d9e1ed197813ab107bfd3dec7dc7ace9c5c3c67c7.

The experiment declared the ordered candidate list `[10080,10800,11520]`,
checked membership in this array, and imposed the stopping rule: stop after
the first complete minimum-tree residual, or after all three clocks. All
three belong to the array and close, so no residual was reached and no
fourth clock was tested. The selection stresses composite divisor domains
and zero-credit topology; it is not a numerical extrapolation from the
previous largest-twenty census.

The physical scope is unchanged: a primitive row of thirteen distinct
positive integer speeds, a selected six-label set A, complementary set B
of seven labels whose actual strict-atlas graph is connected, and
`t=gcd(A)`. Under hypothetical weak LRC(14) failure the inherited proper
profiles and clock reduction apply, including `gcd(B)<=90` and its
coprimality with t. No height box, physical unit speed, connectivity of A,
or decoder-span equality is imposed.

The closest hostile is the realizable zero-credit tree at clock7200,
whose omitted endpoint overlap required native path composition. The
corrected near miss is to transfer its ratio weights to a changed quotient
clock, or prune a partial minimum tree: neither is used here. The
least-used sidecar restored in this experiment is the cut forced by the
full positional graph after all zero-credit edges have been retained.

The live concept board is: complete divisor domains; all 126 profiles;
strict endpoints and common translation; possible versus actual graph;
zero-edge connected components; and the marginal ceiling cost.

## 2. Complete finite consumer

For each clock t, restrict the inherited 42-state sheet alphabet to
divisors of t. Enumerate every sorted seven-multiset of this domain,
keeping repeated values as distinct positional labels. Require total gcd
one and, for every nonempty proper subset I,

    (c, sort(gcd(c,d_j): j outside I)) in P_(7-|I|),
    c=gcd(d_i: i in I).

These are the complete 126 inherited conditions. Enumeration uses only
the already proved necessary partial-completion profile pruning; it
never prunes on the weight of a partial minimum tree. In particular,
adding a vertex is not assumed to increase the minimum spanning-tree
weight. The independent audit must check domain completeness separately;
the producer's optional unpruned small-domain control does not activate
for these three larger alphabets.

For every positional margin pair a,b, let e=gcd(a,b), n=t/e. Enumerate
all 5,855 strict coprime atlas ratios p<q satisfying

    {e gcd(n,p),e gcd(n,q)}={a,b}.

The pair weight is the minimum, over these ratios and every real grid
translation, of e times the number of points of the translated n-grid
in the literal open intersection `||px||<1/14`, `||qx||<1/14`.
The exact endpoint sweep retains walls, open chambers and coincident
endpoint defects. A separate component count is only a sound lower
bound for skipping candidates that cannot improve an incumbent minimum.

Let M_t(d) be the minimum positional spanning-tree cost on the possible
edge graph, and

    E_t(d)=sum_i d_i ceil(t/(7d_i))-t.

The inherited theorem excludes a disconnected possible graph as incapable
of containing the actual connected graph. Otherwise it supplies at least
`M_t(d)-E_t(d)` weak-safe grid points. The minimum is the correct direction
because an actual connected graph need not contain every possible edge.
Individual minimizing ratios or phases are not claimed to coexist.

## 3. Exact results and the topology behind them

| Clock | Divisor states | Full valid words | Minimum margin | E at owner | M at owner |
|---:|---:|---:|---:|---:|---:|
| 10080 | 24 | 62,905 | 156 | 0 | 156 |
| 10800 | 27 | 79,399 | 48 | 108 | 156 |
| 11520 | 25 | 70,258 | 96 | 84 | 180 |

There are 212,562 complete word/clock evaluations and no residual word.
The minimum owners, with positional repetitions retained, are respectively

    10080: (6,8,8,9,9,32,36),
    10800: (9,10,16,20,27,36,48),
    11520: (9,9,10,20,32,36,48).

Each owner has a minimum tree with five zero-credit edges and one positive
edge. Its full zero-edge graph therefore has exactly two components: the
five tree edges already leave at most two, and a connection between them
would contradict the strictly positive minimum cost. In terms of labelled
margin multisets, those cuts are

    10080: {6,9,9,36} | {8,8,32},
    10800: {9,16,27,36,48} | {10,20},
    11520: {9,9,32,36,48} | {10,20}.

Every actual connected graph on the owner positions must cross its cut.
The cheapest compatible bridge gives the entire minimum-tree credit:

| Clock | Bridge margins | Minimizing primitive ratio | e | Actual quotient clock | Minimum overlap |
|---:|---|---|---:|---:|---:|
| 10080 | 8,36 | 2:9 | 4 | 2520 | 156 |
| 10800 | 16,20 | 35:284 | 4 | 2700 | 156 |
| 11520 | 20,36 | 27:305 | 4 | 2880 | 180 |

These are compatible ratio minimizers, not a claim that all selected
tree ratios form a jointly realizable physical row. The graph cut itself
does not require that converse. At 11520 the independent interval-component
lower bound for the minimizing bridge is 164; retaining its actual
interval placement raises the all-translation minimum to 180.

This explains the negative preliminary signal. The global smallest
component-bound pair weight is zero at every selected clock. Consequently
six times that weight cannot dominate even the coarse marginal excess
cap; it gives margins 0, -540 and -450. The complete positional graph and
profiles recover a mandatory expensive connection. At 10080 there is the
additional exact resonance that every allowed d divides 10080/7, so E is
identically zero on the full domain. The actual minimum tree still had
to be checked; an E=0 bound alone does not rule out a zero-credit graph.

## 4. Physical consequence, retained array and stopping reason

For a physical hypothetical failure with one of these three clocks, the
CITED proper-six supplier produces a safe phase for A/t. Its t lifts
preserve the six clearances. The inherited coprime sheet map places B/gcd(B)
on a translated t-grid, where the relevant positive minimum-tree credit
supplies a common weak-safe lift. This contradicts the hypothetical
failure in the stated connected-complement scope.

Delete exactly 10080,10800,11520 from the pinned source array. The new
necessary set has 7,622 clocks, maximum 11,935, with canonical JSON hash

    832714c179ab4f425a76172c41d7d8eecf05dc15adfdd3c5e08bc7eb5a13e48b.

The method stops because the declared three-clock budget is exhausted;
no incomplete word bank, timeout, or mathematical residual has been
treated as a closure. No additional clocks are removed by proximity,
divisibility analogy, or monotonicity in t. The next native-topology
experiment requires a newly declared surviving clock with a complete
residual bank; these three clocks supply none.

## 5. Reproduction

Run `04-computation/continuing11_20260908_lrc_composite_clocks.py` normally
and with `-O`. Before installation, run the standalone source with the
repository as working directory; its certificate is then written beside
the source. It reads pinned profile and array suppliers but imports no
mathematical producer. NumPy is used only for bounded exact integer
arrays. Literal wall/chamber controls and Kruskal versus Prim owner
checks are retained.

The two modes pass 92,032 always-active exact gates. The certificate
contains all three complete domains and their word hashes, all compatible
pair minima, every owner and its tree, all empty residual lists, and the
complete resulting clock array. Raw LF output and regenerated certificate
bytes must agree across modes. The independent full-domain audit is a
separate promotion gate.

## Accepted independent audits

- [lrc composite clocks audit](continuing11_20260908_lrc_composite_clocks_audit.md).

The parent accepted these reviews without mathematical repair. Source, output
and certificate bytes remain frozen; this filing changes status and routing only.
