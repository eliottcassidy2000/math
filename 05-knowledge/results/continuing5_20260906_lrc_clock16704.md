# Clock16,704: one located survivor and its exact owner repair

**Status: FINITE-EXACT bounded stopping result, with the inherited conditional grid transfer; INDEPENDENTLY AUDITED.** The complete current candidate universe at this one clock has988 entries. Only the first107 located profiles were evaluated, stopping at the first survivor. Its exact forced-word maximum then removes it. The remaining881 candidates were not tested by the new located or conditioned methods, so there is **no whole-clock elimination claim**.

The subsequent [complete one-clock theorem](continuing5_20260906_lrc_clock16704_complete.md)
now excludes all cases at16704. This report retains the intermediate
mechanism and its declared finite universe; it is not the current scale bound.

## 1. Inheritance and the two coordinates

`third_20260906_grid_refined.md` is the independently audited current necessary-clock reduction:8,202 clocks, maximum16,704. Its supplier `third_20260906_grid_full_words.md` gives M(16704)=188 as an exact maximum over full projected seven-state words. It is not a census of actual physical rows. The small-sheet-gcd theorem supplies some actual atlas edge with e<=6, not every edge or a particular representative.

Our `continuing5_20260906_lrc_extremal_location.md` eliminates the displayed edge e4,p3,q308 by raising credit172 to336 through joint interval locations, even though its exact conditional word cost remains188. This note asks the bounded next question at the same clock, retaining every other eligible pair. It stops as soon as the common-translate coordinate alone no longer improves the sufficient decision.

Incoming `long_frontier2_sep06_lrc.md`, fetched at origin/main2d3c53942 and read without merging the dirty worktree, independently develops the same common-translate overlap mechanism: two adaptive pairs close84 grid scales and81 actual entries with zero individual interval floors. That concurrent overlap mechanism is credited. The distinct target here is the complete current filter at clock16,704 and the interaction of phase positions with a conditioned full-word objective.

The board is: actual pair candidates; forced sheet margins; a shared interval translate; a shared full-word owner; and the actual physical transfer. The corrected near miss is to call the first surviving quotient candidate an actual failing row, or to attach unrelated maximizers to an edge.

## 2. Complete old-filter universe, with no hidden symmetry quotient

Use the exact5855 strict coprime pairs1<=p<q with p+q<=356, each prime in p+q congruent2 modulo3 and each exponent at most2. At t=16704, retain every e in{1,2,3,4,6}, the divisors at most6. There are29,275 base candidates.

For each pair, retain its complete clipped open interval lengths. With n=t/e and lengths ell_i, the inherited separate credit is

`C_sep=e sum_i(ceil(n ell_i)-1)`.

The forced sheet margins are

`d_p=e gcd(n,p), d_q=e gcd(n,q)`.

The paired marginal upper cost is exactly the incoming one: allowed states are the18 divisors of t among the42 inherited level-six gcd values; their capacities are the counts of entries at least d in(90,30,9,4,2,1,1). Reserve the two distinguished margins, respecting a repeated margin's capacity, and take the five largest remaining residue costs `d*(-t/d mod7)`. Divide their sum and the reserved costs by7 to obtain E_pair.

The retained test is exactly

`C_sep<=min(188,E_pair)`,

with valid forced margins. The separate aggregate filters of the incoming implementation are omitted as runtime shortcuts, not weakened mathematically: aggregate lower credits are bounded above by C_sep, so a candidate passing the displayed test automatically passes those shortcuts.

The complete counts are:

-28,060 rejected by C_sep>188;
-227 rejected by forced-margin availability or capacity;
-zero further rejections by E_pair after those tests;
-988 retained candidates.

The full sorted candidate array is retained in the transcript, in lexicographic order(e,p,q). This order is explicitly ours; it is not claimed to be the incoming search order. Its entries are `[e,p,q,d_p,d_q,C_sep,E_pair]`, and compact-JSON SHA256 is

`8610d17932b51afbaaeec4699f5a21e2362441f58b9c28235c48182106d45bc1`.

The data dependencies are pinned: the full hereditary profile JSON, and the independently audited complete `[t,M(t)]` maximum table. Rechecking an attaining owner is not substituted for that table's optimization proof.

## 3. First common-translate survivor

For each candidate in this order compute the exact located credit

`C_loc=e min_alpha #(X_alpha intersect D_p intersect D_q)`,

where X_alpha is the translated n-grid and both danger sets are open. Literal integer interval intersections retain positions. Their projected endpoint residues give the complete discontinuity partition; every wall and every open cell is checked exactly. The first106 candidates have C_loc>min(188,E_pair). At zero-based index106 the first survivor is

```
e=4, p=23, q=323, n=4176,
forced margins=(4,4),
C_sep=180, E_pair=207, M=188,
C_loc=180.
```

The pair sum346=2*173 is in the strict atlas. Its49 components, in units1/104006, have lengths46 repeated43 times and38,24,10 each repeated twice. There are98 distinct projected endpoint walls. The exact normalized count has

```
minimum45 at alpha=0,
maximum92 at alpha=75651/104006.
```

Thus the separate interval minima **are simultaneously attained** here. The location refinement is sharp at the old credit180; it cannot beat188 for this pair. The minimum and maximum, including open endpoints, are independently checked by literal danger predicates on all4176 grid points at all196 walls/cells. All107 evaluated profiles are retained in the transcript; their compact-JSON SHA256 is

`dd18e64c39a2dd0aeaa10ece48199ef87691fa8ef1892b124e16a57cba851013`.

This is a genuine hostile to claiming a location gain for every retained pair. It is not a physical counterexample or a full conditioned-word survivor.

## 4. Exact forced-word cost removes that survivor

The next test is sharply bounded: fix two entries4,4 and enumerate every five-entry multiset from the18 allowed divisors of16704. There are exactly

`binom(18+5-1,5)=26334`

completions. Each is tested against all126 full projected proper-subset words and the global gcd-one requirement. This enumeration has no branch-and-bound pruning and imports no producer implementation. It has2422 valid completions and exact maximum

```
M_(4,4)(16704)=134,
owner=(4,4,3,9,36,58,64).
```

The source independently checks every full word of this owner and its literal ceiling cost. Conversely, exhausting all26,334 completions establishes the upper bound; the owner alone establishes only attainment. Repeated states are allowed, and the two distinguished4 entries are retained, so neither labeling nor multiplicity is lost.

The separate-capacity bag attains E_pair207 at `(4,4,72,58,64,36,29)`, which is not a full word. Already the singleton first4 has complement word `(1,2,4,4,4,4)`, absent from level6 at core4. The unconditioned maximum188 can be attained by a different allowed word, but does not establish an owner with the forced4,4. The exact conditional maximum resolves that ambiguity.

Since C_loc=C_sep=180>134, the inherited grid inequality leaves at least46 weak-safe lifts for any actual row with this selected pair and the retained full profiles. In this case conditioning alone suffices; the location step does not improve the numerical credit. No complete actual row with these parameters is asserted or needed for this conditional exclusion.

## 5. The crossed boundary and the remaining work

The two selected edges diagnose independent losses:

|Edge at t16704,e4|Separate credit|Located credit|Exact conditioned full-word cost|Needed repair|
|---|---:|---:|---:|---|
|(3,308)|172|336|188|Common interval translate|
|(23,323)|180|180|134|Common full-word owner|

Neither coordinate subsumes the other. The correct next sufficient test is the combined inequality

`C_loc(t,e,p,q)>M_(d_p,d_q)(t)`.

Both sides retain the same selected pair. A future one-clock proof may alternate cheap exclusions on the two sides. This note stops after evaluating107 of988 candidates and resolving the first located survivor by exact word conditioning. It does not scan candidates107 through987, reduce the8,202-clock set, or infer physical realizability from word membership. The largest necessary clock16,704 is unchanged by this bounded certificate alone.

The actual consumer is inherited: a six-body safe phase lifts to a translated t-grid; the selected pair's common gcd reduces it to an n-grid with multiplicity e; the total danger excess is bounded by the compatible full-word objective. Positive credit minus that objective produces weak safety. This remains a sufficient method. Strict physical clearance is not inferred from weak endpoints.

## 6. Reproduction and frozen pins

```
python ../../04-computation/continuing5_20260906_lrc_clock16704.py
python -O ../../04-computation/continuing5_20260906_lrc_clock16704.py
```

Both runs pass10,944 always-active exact gates with byte-identical actual LF stdout. Adjacent inherited data are preferred on filing, with the outside worktree fallback. The transcript retains the complete988 candidate array and the107 evaluated located profiles, rather than only their totals.

- Source SHA256:`2d12351426bdf67c0801bdc46b16c50e35f6f6600b49e8e5323d4cc975bc2d3a`.
- Output SHA256:`6d6acf82d798074bd5fac208f79c6b91bf7b9df9f425f1204d9e56324f5cac76`.
- Full profile JSON:`935f3f687b6d7c89cc099e536f536238fd753bcc4c1747906d213cef387ca93f`.
- Complete maximum-table semantics:`ca6b6f562db1fc3632f8b7570b89a16020a981ae8aa130be200dc1bdcb4264ca`.

The exploratory `_probe.py` is not a filing artifact; all claims above are reproduced by the frozen primary. No repository or Git mutation was used, and no prior frozen artifact was changed.

Independent [proof and exact referee](continuing5_20260906_lrc_clock16704_audit.md) passes.

The [raw-byte checkpoint manifest](continuing5_20260906_manifest.json) pins
the filed source, report and identical normal/optimized transcript. Any
candidate-report hashes above identify the pre-promotion bytes.
