# Level eleven, an exact dyadic zero law, and the meaning of gluing

**PROVED from explicitly CITED modular-form inputs; FINITE-EXACT checks.
Short solo session, 2026-09-22. Collatz OPEN; no independent-agent audit.**

Inheritance: the incoming [lead reflection, section4](collatz_mod6_20260922_lead_reflection.md)
already gives a8=0 and the dyadic zero pattern. Its trigonometric prefactor
had an extra sqrt2; corrected this session. Closest mechanism is the Hecke
recurrence; hostile is treating a periodic coefficient clock as a Collatz
orbit. The least-used sidecar is the odd part of the coefficient index.
Anchor: dyadic coefficients; niche: the two signed gluings; wildcard: radix
operators and tournament diagonal counts. Board: eta product, recurrence
matrix, odd-part multiplicativity, signed map, directed relation type.

## The modular link is substantive

CITED: f(tau)=eta(tau)^2 eta(11tau)^2 is the normalized weight-two level-11
eigenform attached to X_0(11), with a model
`E: y²+y=x³-x²-10x-20`. Its q product is
`q product_(n>=1)(1-q^n)^2(1-q^(11n))^2`.
Primary sources: [Dummigan, 2001](https://intlpress.com/site/pub/files/_fulltext/journals/mrl/2001/0008/0004/MRL-2001-0008-0004-a008.pdf),
[Elkies's course](https://people.math.harvard.edu/~elkies/M263.21/index.html),
and [McMurdy's eta-product construction](https://math.uni.lu/wiese/CMF/resources/McMurdyTalk.pdf).
For good primes p, a_p=p+1-#E(F_p); a_(mn)=a_m*a_n if gcd(m,n)=1;
a_(p^(r+1))=a_p*a_(p^r)-p*a_(p^(r-1)). These classical eigenform inputs
are cited, not inferred from our finite computation. The point-count link
is an actual modularity correspondence. No map from the separate
odd-square absorption witness11 to this curve's conductor has been established.

## Extension: a complete dyadic factor and a forced zero set

Write b_r=a_(2^r). Since b0=1,b1=-2,
`b_(r+2)=-2b_(r+1)-2b_r`. Its matrix M=[-2,-2;1,0] satisfies M^4=-4I.
Equivalently, as formal power series,

```
sum_(r>=0)b_r t^r = 1/(1+2t+2t²)
                  = (1-2t+2t²)/(1+4t^4).
```

Therefore, for every j>=0,

```
(b_(4j),b_(4j+1),b_(4j+2),b_(4j+3))=(-4)^j*(1,-2,2,0).
```

This proves the inherited zero pattern and adds the exact block scaling.
For n=2^r*m with m odd, multiplicativity gives the full factorization
`a_n=b_r*a_m`; hence

```
a_n=0 iff [r=3 mod4] or [a_m=0].
```

In particular the forced dyadic zero set has natural density
`sum_(j>=0)2^(-4j-4)=1/15`. Existence of this density follows by finite
truncation; the remaining tail is contained among multiples of2^(4J+3).
This is not the density of ALL coefficient zeros: a19=a29=0 supply other
zeros. It is not a Collatz orbit-density claim.

The corrected trigonometric form is
`b_r=2^(r/2)*sin(3pi(r+1)/4)/sin(3pi/4)`.
The characteristic roots are -1+i and -1-i. After dividing by sqrt2 they
have order8; their ratio has order4. Thus a precise eighth-root phenomenon
exists, while the projective recurrence has order4, not3.

For any integer N>=8 the FORMAL product
`G_N=q product(1-q^n)^2(1-q^(Nn))^2` has the same coefficient of q8, zero.
The second product first alters degree N+1. At N11 the first change is
q12, by -2. So the early zero does not single out eleven. We do not claim
G_N is a normalized modular form for every N: the eta prefactor would be
q^((N+1)/12), which equals q precisely at11.

## What the two gluings actually do

Let oddcore retain sign, U_b(n)=oddcore(3n+b) on odd nonzero n where defined.
Negation conjugates U_b to U_-b without reversing arrows. The known
positive U_-1 cycles have representatives1,5,17 but lengths1,2,7:
`(1)`, `(5,7)`, `(17,25,37,55,41,61,91)`. Their negatives are U_+1 cycles.
Thus (1,5,17) is a list of cycle minima, not a three-cycle. Completeness
of this cycle list remains OPEN.

The odd glued map oddcore(3n+sign(n)) is two reflected copies of positive
U_+1. The opposite gluing oddcore(3n-sign(n)) is two reflected copies of
positive U_-1 and has six known cycles. The number line has not changed;
the piecewise rule has. Six selected representatives have36 ordered pairs,
but the six actual known odd cycles have20 vertices, not6.

The supplied groups {1,3,6,8,10},{2,7,9},{4,5,11,12} are the components
of the square-sum graph Q12, recovered in the incoming
[square-filter lane](collatz_mod6_20260922_w6_summand_closure_square_filter.md).
They are not preserved by the minus map: 5 maps to7 across components.
There can still be a new encoding, but the displayed membership is not
already a cycle invariant.

## Radix and diagonal statements that survive exactly

In base B, multiplying by10_B sends x to Bx; multiplying by11_B sends
x to Bx+x. Appending the digit1 is instead x->Bx+1. The full plus
Collatz numerator is (x<<1)+x+1 in binary. This makes the shift-and-add
structure precise; neither the decimal prime11 nor a conductor is
invariant under replacing its digit string by11 in another base.

Six representatives have6²=36 ordered pairs including loops, or18 ordered
pairs from each chosen triple. This is a full relation matrix. A tournament
has only binom(6,2)=15 arcs and no loops. Converting six loops into edges
to a new vertex gives the valid count15+6=21=binom(7,2); orientations need
six specified bits. It does not give36 tournament arcs. Also6*3=18, not16.
Neither count derives the Euler product6/pi².

The previously corrected fifth-power identity and odd-square absorption
calculation remain intact. Their count/spacing mechanisms do not acquire
modularity from this shared numeral. The substantive modular result here
is the exact coefficient factorization and its forced density1/15.

Reproduce: `python 04-computation/experiments/level11_short_20260922.py`.
The companion tests the product to degree512, dyadic recurrence to r100,
every dyadic/odd factorization through512, selected good-prime point counts,
the signed cycles, and the pre-level coefficient independence. Explicit
exceptions retain all checks under -O. No Lean or independent-agent claim.
