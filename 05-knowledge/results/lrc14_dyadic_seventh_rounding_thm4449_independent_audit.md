# THM-4449 independent audit: sharp dyadic owner cuts

**Status: ACCEPT / VERIFIED-EXACT; LRC(14) OPEN.**

Two clean-room programs independently audit the theorem's finite obligations.
They use Python's standard library only, import no primary module, and retain
all checks under optimized Python.

The seventh-rounding audit constructs strict quotient walls on exact rational
lattices. It checks the formula on all `2,350` coprime odd ratios through
denominator `151`, the `4/77` 3-unit cap, five-ray alphabet, complete energy
compatibility table, owner identity, scale localization, dyadic transfers,
and hostile lift masks. It confirms energy `124/847` at `(1,11,121)` and the
physical/energy split `108/847+16/847`.

The sharp-cap audit is independent at the representation level. It lifts each
endpoint to the integer denominator `lcm(14pqt,14c)`, proves every exposure
margin positive and cutoff nonintegral, and checks all `1,704` full-odd and
`2,801` odd-3-unit primitive presentations left by the analytic BV reduction.
It reproduces

```text
full odd: 214/1449, equality (1,9,23);
odd 3-unit: 72/539, equality (1,7,11).
```

It also checks the general pair cap `4/63` at `(1,9)`, the 3-unit cap `4/77`
at `(1,11)`, exclusion of the `pq=33` 3-unit boundary, and both physical and
quotient endpoint-coordinate controls. Common dilations in each equality
classification are restricted to those preserving the stated domain.

The separate height-199 program checks all `47,905` triples from the `67`
positive odd 3-units through `199`. Its former finite maximum `72/539` is now
a hostile/positive control for the proved all-height result, not the proof.

Every command and raw-LF hash is listed in the result note. Normal and
optimized runs are line-for-line identical to their frozen outputs and end in
`PASS`. The computations discharge finite residue and exposure boxes; the
Fourier/Bernoulli identities, BV cutoff, owner expansion, and topological
equal-mass gate remain analytic parts of the proof.
