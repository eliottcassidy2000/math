# Independent audit: the explicit V4 surface and changed carriers

**Status: PASS / PROVED in the stated scope.** Root independently read
the complete [producer proof](planar_jc48_sep06_dg_surface.md) and
[source](../../04-computation/planar_jc48_sep06_dg_surface.py), including
the general shear extension added after the first 39-gate version.
The audited final source has 62 always-active gates. No actual Keller
envelope is classified or constructed.

## Geometry and differential audit

The two charts really cover the complement of the graph, including
the second projective coordinate at infinity. Their transitions give
the boundary A1 and its affine-plane complement without omitting a point.
Very ampleness of O(2,1) proves affineness directly. The divisor quotient
Z²/Z(2,1), primitive boundary generator, canonical value 2[D], and both
Euler decompositions agree. No inference from a Weil class to smoothness
is needed: smoothness already follows from the charts.

Independently differentiating t=-r²-r⁴b gives
dx wedge dt=r² dr wedge db and
x dt=(-2-4r²b)dr-r³db. Their coefficients are regular, and differentiating
the latter yields the former. This is a global exact form with zeros,
so it does not trivialize the canonical line bundle.

The three carrier substitutions have boundary values (-1,0,0), whereas
x²(1+x²t)=-b separates boundary points. Thus every pair from B contracts
D. This is a sufficient obstruction under the explicit hypothesis that
**both** coordinates lie in B. The nodal parametrization is an exact
hostile to deducing that hypothesis from the single defect equation.
It is correctly identified as boundary data only.

## Universal shear audit

Expanding -(r^-1+delta)²+(r^-1+delta)^4 r²(1+r²b)
independently gives the displayed b-prime formula. If f(0)=0,
delta=f(-r²-r⁴b) is divisible by r², so b-prime is regular and equals b
on D. The denominator of r-prime is a unit on all of D; the named open
set together with U0 covers W. Gluing with the source map and its
inverse for -f is valid. The same cover with the group parameter retained
establishes a regular additive action, not just pointwise extensions.

If f(0)=c is nonzero, r b-prime restricts to 2c on D. No other term can
cancel that pole. Since b is a global function this excludes even a
morphism extension. On the old collapsed component M, the second term
of the transformed p has pole order 4 deg(f)+4, strictly greater than
2 deg(f)+3. Hence every nonzero f fails even one-way old-carrier descent.
All admissible shears fix D pointwise, so the algebra generated by the
entire orbit of B still has constant restriction. This universal assertion
uses restriction as a ring homomorphism; it is not inferred from the
three sample shears.

## Evidence, attribution, and limits

The primary Flenner–Kaliman–Zaidenberg introduction was read directly.
Its classical V4 terminology is correctly credited; the isomorphism
theorem is not being applied to hypothetical envelopes. The independent
analytic checks above cover the all-degree and all-orbit claims that
finite examples cannot establish. Normal and optimized source replay
both match the frozen output exactly.

* Source SHA256: `f41184dbb2e0f6cc4159747e3149e2ccdb8d7af0c011aaff6ddb1024ff6fcb57`.
* Output SHA256: `d28d37bf908fbd196b679117eb4a43f2ec7b37b76f375ca540d822bb09150641`.

The result supplies a positive surface control, a genuine changed-carrier
operation, and a boundary-collapse obstruction for its entire orbit
algebra. It supplies no degree-six map, inertia, or actual source entry.
