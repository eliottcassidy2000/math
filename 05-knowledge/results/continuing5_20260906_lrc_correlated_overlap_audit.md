# Independent audit of the located-overlap improvement

**Verdict: PASS, with no mathematical repair.** The bounded967..1042 class
has the stated actual decoder entry and complete inherited profiles. Every
separate normalized pair-interval lower count vanishes throughout that class.
At t=968 the located107/167 intersection has exact translated-grid minimum15
and maximum23, so the inherited union correction guarantees at least10 safe
points. The literal row is already safe, including at denominator7. No
unresolved-entry or universal LRC closure claim is accepted or inferred.

The full primary proof and source were read. The independent source imports
neither the producer nor its transcript. It uses a product-generated strict
atlas, integer weighted rank, complement-mask profile enumeration, center-pair
intersection geometry and a different complete phase-wall partition.

## 1. Actual entry and full-profile quantifiers

All cofactor products, pairwise coprimalities and disjoint prime supports
check exactly. The complete strict-atlas graphs are the declared connected
six-star and seven-tree. Their actual bounded weighted edge rows have rank11.
The physical row is primitive, distinct and in the Q^2 sum box.

The all-parameter crossing proof is sound in both orientations. For two
tV labels and one U label, the latter's nonzero coefficient is divisible
by t times the V pair gcd, exceeding Q from t=967 onward. For a U pair
D*p,D*q and outside t*v, coprimality forces its coefficient to be kD;
division gives |k|t*v<=Q(p+q), contradicted uniformly by
t*min V>388Q. All105 and126 mixed supports are covered, including zero
inner coefficients and therefore mixed supports of size two.

Internal weighted edge rows span the complete internal kernels, so the two
inclusions W_(Q,3)=V_dec follow after crossing exclusion. This conclusion
uses the actual graph and height bounds, not just graph rank. Every mixed
large subset has gcd1; the only unmixed subset of size>=7 is the entire
primitive U. The all-one complement words belong to the pinned full bank.
Thus the whole declared class retains those profiles, and all4095 were
independently reconstructed at t=968.

The exact main-row checks reproduce sum2075281984219247, forward minimum
858481264080>Q and reverse margin55157421217140/55083317447977>1.

## 2. Zero separate counts and exact located minimum

The independent engine enumerates the literal pairs of danger-arc centers
for all21 coprime group pairs. Every interval length is at most1/(7q), with
q>=149. Hence t*ell<=1042/1043<1 throughout the class, making
ceil(t*ell)-1=0. This universal bound and all finite geometry checks agree.
For107/167 there are39 components with total length2553/125083.

To verify the located count by a separate route, the referee does not use
the producer's39 intervals or78 endpoint partition. For p=107,q=167 it
constructs **all548 individual-danger walls**

    alpha = t*(k +/- 1/14)/v mod1,
    v in{p,q}, 0<=k<v.

These contain every possible change of either literal danger indicator on
the t-grid. Counts are constant on the548 complementary open cells. Direct
integer modular evaluation at every wall and one exact rational midpoint
of every cell gives minimum15 and maximum23. Strict danger endpoints are
included as separate cases. Exactly78 of these walls affect the intersection
count, independently recovering the producer's smaller partition. The
minimum15 is also attained at alpha=0.

This overpartition is a complete all-real-translate proof, not sampling.
It establishes the strict separation between the exact minimum of the sum,
15, and the sum of the39 separate minima,0. Relative interval positions are
the missing coordinate.

## 3. Physical consumer and scope firewall

The selected cofactor D=H/(107*167) is coprime to968, so multiplication by D
permutes the grid and passes to another translate of the primitive clock.
All seven U labels are also coprime to968. Each individual danger therefore
contains at most ceil(968/7)=139 points. Subtracting the selected pair overlap
is a valid pointwise multiplicity correction, so the union has at most
7*139-15=958 points, leaving at least10 weak-safe points on every lifted grid.
No unsupported sum of several pair credits is used.

The supplied V quarter-phase has clearance1/4. Its968 physical lifts retain
that core clearance. Independent full-row modular checks give exactly335
strict-safe lifts; the first is13/3872 with clearance885/3872. Since7 does
not divide3872, a distance exactly1/14 is impossible at such a rational
phase, so the endpoint typing is correct. The referee separately verifies
that the first physical denominator is7, with all six nonzero numerators
safe and clearance1/7 at1/7.

The all-parameter claims are entry/profiles/zero separate credits on967..1042.
The located uniform15 count and335 literal lifts are asserted only at968.
The simple denominator7 safety remains visible. Thus the result is a precise
method obstruction and repair for an already-safe row, not a residual unsafe
class or a new universal LRC theorem.

## 4. Reproduction and frozen identities

The [independent source](../../04-computation/continuing5_20260906_lrc_correlated_overlap_audit.py)
and [output](continuing5_20260906_lrc_correlated_overlap_audit.out) pass
**5881 always-active exact gates** in normal and optimized modes. Both
captured outputs are byte-identical actual LF without newline normalization.

From the source directory:

    python continuing5_20260906_lrc_correlated_overlap_audit.py --profiles PATH
    python -O continuing5_20260906_lrc_correlated_overlap_audit.py --profiles PATH

The default profile path is source-relative when filed in04-computation,
with a current-worktree fallback outside the repo. The entire raw profile
file is pinned; full word membership is checked, not replaced by a scalar cap.

- Producer source: `94415dca6e8445de037f8748c46a62f0b8b793d3331a6b1a386b6ce7b9507f82`.
- Producer output: `f896aad46c791f8370a9b6aa802a6f7dd9cc2a7edfd7384488bd9bbe4f00156d`.
- Producer report read: `d1fbf320852140018ea70c9a35ba35d42e97841d3ef67b99068fee2daa8c0b09`.
- Profiles: `935f3f687b6d7c89cc099e536f536238fd753bcc4c1747906d213cef387ca93f`.
- Audit source: `67eb9537e60f58d177dbac08f05e9ef2e82d59b641d594e9ca3a44449b2e790d`.
- Audit output: `49e31a451458d658ba9c8a3e455051f7e7ac8dd8c54bba66255a95445a9cfeed`.

No producer, maintained file or Git state was changed by this referee.

Filed checkpoint provenance: the [raw-byte manifest](continuing5_20260906_manifest.json)
pins the final report, source and output. Reviewed candidate-report hashes
above refer to the pre-promotion bytes. Source-location defaults and local
links were made portable where necessary; all emitted outputs were replayed
as raw bytes. The independent audit supplies the stated promotion basis.
