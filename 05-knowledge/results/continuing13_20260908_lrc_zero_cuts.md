# Complete zero-edge cuts close both declared clocks before depth lifting

**Status: PROVED analytic consumer + FINITE-EXACT complete native banks
and positional cuts; INDEPENDENTLY AUDITED.** Every full-profile
seven-word at9240 or11088 has a disconnected possible zero-edge graph.
This explains both clock closures. Exactly four words lack an isolated
zero-graph vertex; their full component cuts are retained below.

## 1. Inheritance, operation and missing coordinate

The complete domain and forest supplier are in
`continuing13_20260908_lrc_zero_clock_probe.md`, including the exact
126-profile and inherited-array pins. The closest structural predecessor
is `continuing12_20260908_lrc_depth_wedge.md`: at7560 the residual
zero graph could be connected, but its forced actual4--24--18 wedge
required two incompatible absolute2-adic depths at the common middle.

Here the zero graph already disconnects before any lifted depth is
assigned. The source is an actual connected strict graph with its seven
positional margins. The map replaces each actual edge by membership in
the complete native zero bank at that exact clock. The preserved
predicate under an unsafe zero-budget grid is that every actual edge
must remain in this zero graph. Individual ratios, phases and absolute
depths are forgotten in the graph, so disconnectedness is sufficient;
connectedness would leave those sidecars unpaid. We do not infer an
actual edge from a merely possible zero edge.

The live concept board is: complete126 profiles; positional label budget;
zero-edge components; actual connected-graph cuts; clipped versus absolute
prime depths. The prior7200 realizable zero-tree hostile still forbids
treating connected possible graphs as exclusions. The four words below
are the new exact hostiles to replacing full cuts with vertex isolation.
The whole-alphabet zero graph also contains a nontrivial gcd1 component
at each clock, so alphabet-level connectivity cannot replace the seven
actual label positions and their inherited profiles.

## 2. Zero-budget cut criterion

Let t be a clock with actual seven-tail margins d_i=gcd(t,u_i), and
suppose 7d_i divides t for every i. If a translated t-grid has no
weak-safe point, its seven open danger sets cover the grid. Their
individual cardinality upper bounds sum to exactly t. They must
therefore attain those bounds and be pairwise disjoint.

For each pair of positional margins, put an edge in the possible zero
graph precisely when at least one native compatible strict ratio has
zero all-phase intersection minimum on its actual quotient grid. Every
actual strict edge under the unsafe hypothesis must lie in this graph:
a positive uniform intersection would contradict pairwise disjointness.

Consequently, if the possible zero graph is disconnected, no actual
connected strict graph can coexist with an unsafe grid. Equivalently,
an actual connected graph must cross any zero-component cut; every
compatible crossing edge has positive uniform capacity. If there is no
compatible crossing at all, actual connectivity is already impossible.
This argument uses the entire cut and does not force a chosen possible
edge to be actual.

The criterion is general with its stated hypotheses. This packet verifies
them only for the two declared clocks and the complete word universes
in the pinned probe. It is not a clock-size or scale monotonicity rule.

## 3. Complete unpruned native zero banks

For actual edge speeds Dp,Dq with coprime p,q, let
e=gcd(t,D)=gcd(d_i,d_j). Multiplication by D maps the t-grid to a
translated quotient grid of clock t/e with multiplicity e. Compatibility
is exactly

    {e*gcd(t/e,p),e*gcd(t/e,q)}={d_i,d_j}.

The companion enumerates the entire5855-ratio strict atlas for every
relevant e. It computes capacity for **every** compatible ratio, with no
incumbent or lower-bound pruning, and recovers:

| Clock | Compatible native ratios | Zero ratios | Possible margin pairs |
|---|---:|---:|---:|
|9240|53195|2235|231|
|11088|48641|1849|210|

These are the actual quotient clocks, not reused7200 weights. Every
pair minimum agrees with the separate full consumer. Every zero ratio
is additionally checked at every literal wall and chamber using exact
integer interval counts. The certificate retains all4084 zero ratios,
their margin roles and an exact zero phase on the quotient grid.

Each recorded zero arm has a separate actual realization (ep,eq) with
the prescribed margins. These witnesses do not assert simultaneous
realizability or a common original phase. That stronger information is
unnecessary because no entire admissible word has a connected zero
graph at all.

## 4. Full positional cuts, including the four exceptions to isolation

The verifier constructs the zero graph on all seven labelled positions
for each of the41249 complete profile words. Its complete component-size
histograms are stored in the certificate. The compact classification is:

| Clock | Complete words | Words with a zero-isolated vertex | Words without one |
|---|---:|---:|---:|
|9240|22469|22468|1|
|11088|18780|18777|3|

At9240 the unique exception is

    (6,8,8,8,12,15,15).

Its zero components have margin multisets

    {6,12}, {8,8,8}, {15,15}.

Every compatible edge crossing the {6,12} component has native credit
at least108. Thus an actual connected graph must either have such a
positive crossing or be impossible in the prescribed atlas.

At11088 the three exceptions are

    (6,8,8,8,9,9,36),
    (6,8,8,9,9,16,36),
    (6,8,9,9,16,16,36).

Each has exactly two zero components: the common component {6,9,9,36}
and respectively {8,8,8}, {8,8,16}, or {8,16,16}. Every compatible
crossing has native credit at least172. These are genuine nontrivial
cuts, with no isolated vertex to use. Therefore the shortcut “every
valid word has an isolated zero vertex” is false, while the full
disconnected-zero-graph criterion survives for every word.

Repeated equal margins remain distinct positional vertices, including
zero edges between equal margins where native ratios allow them. This
prevents collapsing the three8 labels or two15 labels and accidentally
changing the connectedness question.

For comparison, the whole-alphabet zero graph at9240 has component
{5,6,8,10,12,15,20,24,30,40,60}, which has gcd1. At11088 its analogous
component is {6,8,9,12,16,18,24,36,48,72}, also of gcd1. Both also have
the common11-divisible component {11,22,33,44,66,88}. The full126
profiles and exact positional word, not just componentwise gcd, are
load-bearing in the complete conclusion.

## 5. Consequence, controls and paid stopping point

The cut criterion and the complete word bank give a weak-safe lift for
the complementary seven labels whenever their actual strict graph is
connected and the selected-six gcd clock is9240 or11088. Together with
the inherited proper-six phase, this excludes both clocks from the
specified connected-complement failure array. It does not settle general
LRC(14) or assume connectivity on the selected six labels.

The final necessary array has7618 entries, maximum11935, SHA256

    89a3e7545cc77467c5f85fbe0bdaab1f71071cff65184bbc1212eb86c9f07177.

There is no complete residual requiring a new method. The earlier7560
depth sidecar was not reused with changed weights or forced into this
different topology. The present bounded task stops after its two declared
clocks. The four explicit non-isolation words are retained as the failure
boundary of the simpler local criterion, and full native zero cuts are
the strongest necessary repair established here.

The independent zero-bank verifier imports no producer. It passes193399
always-active gates in both normal and -O modes, with identical raw-LF
stdout and certificate bytes. It pins the complete probe certificate,
checks every compatible native ratio, every zero wall and chamber, all
441 pair minima, all positional words and selected cuts, the four exact
exceptions, and the entire final-array pin.

    source 999e1f9ab9a78b93a4958b99d3f98a8b6c3a6dea069e723814a32576edf1148b
    output d5bf4214fe2b628e7df8e6424be5b647017dc9fe386235037f7a189c602e0eec
    cert   23ae81ba6cb39bc14aecf2d93fc3fcf6d4e3c587b5240db35ea1d8f3992551f1

After relocation:

    python -B 04-computation/continuing13_20260908_lrc_zero_cuts.py
    python -B -O 04-computation/continuing13_20260908_lrc_zero_cuts.py

Before installation, run with the repository as current directory and
the frozen probe certificate beside this source. The independent audit
of full profile-domain completeness and native geometry remains separate
from this primary computation and is required before promotion.

## Independent acceptance

The [independent audit](continuing13_20260908_lrc_independent_audit.md) accepts
this scoped theorem without mathematical repair. Its 249174 exact
gates pass in normal and optimized modes with identical raw LF transcripts
and certificates where applicable. The parent repeated those checks after
filing the frozen sources under the repository reproduction paths.
