# Independent audit of rooted gcd products and automatic decoder entry

**ACCEPTED: PROVED arithmetic lemma and conditional actual-entry classes;
FINITE-EXACT literal controls.** This audits the
[rooted-pair theorem](third_20260906_decoder.md).

The prime-valuation proof retains the common gcd factor and works for every
root, with repeated labels allowed. In the primitive case, equality in the
minimum-factor inequality would make all rooted gcds equal; their collective
gcd is one. Thus a nonunit root gives the strict boundary needed to exclude
178 in the three-label case. The leaf-cofactor supplier bounds a primitive
minimum, and the new product mechanism lawfully extracts a selected pair
containing that minimum. It does not require that pair to be an actual edge.
The inherited actual-entry internal-height bound supplies the signed-box
radius; the whole U maximum pays both target containment and phase-grid
spacing. The argument uses only necessary failure scale caps and correctly
returns weak safety in their early-exit branch. No upper bound on t or
coprime star numerators is smuggled into the proof.

The [independent source](../../04-computation/third_20260906_decoder_root_audit.py)
imports no producer code. It exhausts prime-valuation vectors over 0,...,3
of lengths2,...,8 and checks 310,156 distinct root-value states. It recomputes
the exact cofactor floors, strict-power cutoffs and both automatic gates.
For the actual unitless boundary control, it factors the literal reduced
edge sums using SymPy, computes the rank of the entire integer decoder
matrix, excludes all231 mixed supports directly by the displayed necessary
coefficient divisibility or amplitude inequality, and checks all4,095
inherited complement profiles. The equality of relation spaces follows
because all remaining supports lie within the two weighted kernels already
spanned by the actual edges. A literal rational phase checks the final
physical consequence. The missing-common-scale example and sharp three-label
shape are hostile and endpoint controls.

[The frozen output](third_20260906_decoder_root_audit.out) records **314,516
always-active gates**. Normal and optimized outputs agree byte for byte.

```bash
python3 -B 04-computation/third_20260906_decoder_root_audit.py
python3 -B -O 04-computation/third_20260906_decoder_root_audit.py
```

Raw LF SHA256:

```text
source 21f796b2eb1f220d616dda28d3fac71a292c657b6ab782d104df4a6a7441d5cc
output 0bcb0e1d3cbc662fbc9a539f6f329cf5a1053992d62eab9f1e8898c96a5aa804
```

This is an automatic all-shape entry subclass, not an exhaustive census,
a claim of first safety for its explicit control, or a proof of LRC(14).
