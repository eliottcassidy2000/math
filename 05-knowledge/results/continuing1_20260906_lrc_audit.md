# Independent audit: no-unit pair-gcd decoder entry

**Status: PASS; PROVED relative to the named proved and CITED suppliers.**
This audit accepts the complete statement and boundaries in
[the pair-gcd theorem](continuing1_20260906_lrc_dual_pair.md). LRC(14) is
still **OPEN**. The statement concerns actual equality entries in the
specified physical box, not arbitrary partitions of thirteen speeds.

Root independently derived the main implication before reading the final
proof. For an internal pair D(p,q) and outside physical label gu, the
minimal outside coefficient is c=tD/gcd(tD,gu), and the normalized target
is x=gu/gcd(tD,gu). The signed-box radius is
R=Q(p+q)-(p-1)(q-1)>Qq, with q<=Q essential. Under the theorem's two
inequalities and g<=G_b, x<=G_b maxU<=Qq<R. If c<=Q there is a literal
bounded mixed relation. Hence actual equality forces t>Q/D. The larger
component's full safe arc has length a/[7(b+1)maxU], so t times that length
is strictly greater than one. Coprime scales turn the preserved smaller
component phase into a complete translated t-grid. This proves strict
safety in this branch, including equality in the displayed product gate.

For g>G_b the audited joint-gcd supplier only excludes strict failure;
the theorem correctly exports weak safety. The native criterion retains
the full physical gcd and is valid without this failure branch. Neither
a unit nor an upper bound on the whole smaller-component scale is used.

The automatic table follows by growing a selected pair inside its own
connected primitive component. At most a-2 boundary edges divide its gcd
down to one, each by a factor at most355. This proves D<=355^(a-2), but
does not permit growing an entire six-component to seven vertices. The
four nonunit maximum cutoffs are independently recomputed as
13,520,696,477;62,323,312;257,485;1,007. The six-component crude cutoff is
three and is correctly recognized as vacuous for seven distinct labels.
The coprime-star identity is a valid primewise gcd identity; it yields the
separate maximum1359 class and the displayed sharper maximum10261 class.

The [independent source](../../04-computation/continuing1_20260906_lrc_audit.py)
imports no producer. It constructs admissible sums multiplicatively from
primes2mod3 with exponents0,1,2, recovering all5855 actual atlas ratios.
This independent generator caught the provisional omission of the
all-primes condition; the frozen producer is repaired. Its positive rows
survive the correct graph, while the discarded residual candidate did not.

For the high-minimum six-star and nonunit U=(2,3,4,6,18,486,9234), the
audit independently rebuilds the actual6+7 graph, every internal height,
all231 mixed supports in both orientations, all subset-gcd maxima of
sizes7..12, the native box, pair gcd5929017, and the precise global-maximum
cutoff10261. The phase141506215/707531074 has strict clearance
70752184/353765537. This is a consequence on all thirteen physical labels.
The example's scale1768827685 also disproves the invalid whole-component
31950 cap, even while all the genuine scalar subset caps hold.

The source additionally enumerates677 complete signed boxes and4600
physical gcd normalizations. Its inverse congruence uses the other pair
coefficient from the producer's main crossing path. Both normal and
optimized executions agree with the [frozen output](continuing1_20260906_lrc_audit.out)
and pass10841 always-active checks:

    python -B 04-computation/continuing1_20260906_lrc_audit.py
    python -B -O 04-computation/continuing1_20260906_lrc_audit.py

Root read the full proof, including the actual safe entry that fails the
new sufficient condition, and replayed the producer separately. This audit
does not claim an independent census of every possible physical row or a
new proof of the cited lower-dimensional LRC supplier.
