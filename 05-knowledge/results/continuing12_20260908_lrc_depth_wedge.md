# The unclipped middle depth closes the entire clock7560 residual

**Status: PROVED ANALYTICALLY with complete FINITE-EXACT native banks;
INDEPENDENTLY AUDITED.** Clock7560 closes in the inherited
connected-complement entry reduction. Together with the separate6930
minimum-tree closure, the necessary set has7620 clocks, maximum11935.
LRC(14) and disconnected-complement entry remain OPEN.

## 1. Inheritance and the structural mechanism

The actual grid/forest inequality and full126 profile consumer are inherited
from [continuing8 minimum tree](continuing8_20260906_lrc_minimum_tree.md),
retaining **THM-2060**,
`01-canon/theorems/THM-2060-crt-tail-coset-saturation.md`, and **THM-2064**,
`01-canon/theorems/THM-2064-multitail-sheet-capacity-and-dyadic-seam.md`.
The native ratio and prescribed-clock valuation mechanism is **THM-4106**,
`01-canon/theorems/THM-4106-lrc-pair-owner-reciprocity-and-valuation-tree-decoder.md`,
as audited in [continuing9 ratio tree](continuing9_20260907_lrc_ratio_tree.md).
The strict5855-ratio atlas is **THM-3818**,
`01-canon/theorems/THM-3818-scaled-inert-cubeclass-support-two-pair-packet.md`.
This is an application of those mechanisms, not a new general LRC theorem.

The closest hostile is the saturation example with proposed margins
(1,2,1) at clock2 and locally compatible ratios1:4 and2:3. Their joint
primitive realization is (1,4,6), whose actual margins are (1,2,2).
Local compatibility at a saturated vertex does not retain its absolute
prime depth. The corrected near miss would reuse the7200 zero banks or
endpoint weights at7560; both effective clocks are recomputed below.

The operation here restores that missing depth before attempting an
endpoint overlap. Unlike the prior7200 hostile, these zero-credit
arms cannot coexist at all. The source is an actual labelled path;
the target is its middle speed's integer valuation. The map preserves
the directed ratio and the unsaturated endpoint valuation, loses nothing
about that prime, and contradicts two disjoint requirements on the same
actual middle label.

## 2. A criterion for the zero-budget clock family

Let t be a clock, u_1,...,u_7 actual positive integer tails, and
`d_i=gcd(t,u_i)`. Suppose `7d_i|t` for all i, so E_t(d)=0. This holds
for the inherited failure alphabet at every clock divisible by7, since
all42 permitted sheet states are coprime to7.

If a translated t-grid has no weak-safe point, its seven open danger
sets cover the grid. Their separate cardinality bounds sum to t, so
they must be pairwise disjoint. In particular each actual strict-atlas
edge must have zero uniform intersection capacity; an edge with a
positive all-phase minimum would already give a safe point by the
forest inequality.

For margins a,b define the complete oriented zero bank to consist of
the actual ratios u_a/u_b among all compatible strict-atlas ratios whose
intersection minimum on the actual quotient grid is zero. Fix a prime
p with `p^K || t` and an endpoint margin a with `v_p(a)<K`. That
unsaturated endpoint forces `v_p(u_a)=v_p(a)`. Each ratio r=u_a/u_b
in its zero bank therefore forces

    v_p(u_b)=v_p(a)-v_p(r).                          (1)

If a zero-edge graph forces a path a--b--c with both endpoints
unsaturated at p, and the two complete sets of middle depths supplied
by (1) are disjoint, no actual zero-credit path can realize it.
Together with zero budget and actual graph connectivity this excludes
that full word. This is a sufficient criterion for arbitrary clocks
with these stated properties; it is not a monotonicity assertion about
clock size or a criterion using clipped margins alone.

## 3. Every residual word forces the same actual wedge

The pinned [first-residual probe](continuing12_20260908_lrc_zero_clock_probe.md)
exhausts all37209 full-profile words at7560. All except the following six
already have strictly positive minimum-tree credit:

    (4,4,4,9,9,18,24), (4,4,4,9,18,24,27),
    (4,4,8,9,9,18,24), (4,4,8,9,18,24,27),
    (4,8,8,9,9,18,24), (4,8,8,9,18,24,27).

Each has a unique24 label and a unique18 label, and at least one4 label.
The complete possible zero-edge graph has these two decisive properties:

* Every4 label has the unique24 label as its only possible zero neighbor.
* The only zero edge crossing between the positions with margins in
  {9,18,27} and all other positions is18--24.

If the actual strict graph is connected and all its edges have zero
capacity, it must contain both a4--24 edge and the18--24 bridge. They
meet at the same unique24 label. This quantifies over every actual
connected graph, not just a selected Kruskal tree. Repeated4,8 or9
entries remain distinct positional vertices throughout.

## 4. Complete native banks at the changed quotient clocks

For one actual edge write its speeds as D(p,q) with p,q coprime.
Then e=gcd(t,D)=gcd(a,b), and multiplication by D maps the t-grid
to a translated t/e-grid with multiplicity e. A ratio is compatible
precisely when `{e gcd(t/e,p),e gcd(t/e,q)}={a,b}`. This is the
inherited sheet map; its all-phase capacity retains the actual quotient.

For4--24, e=4 and the quotient clock is1890. Among all454 compatible
strict ratios,453 have positive uniform intersection capacity. The
**entire zero bank** is

    u_4/u_24=307/48.                                (2)

For18--24, e=6 and the quotient clock is1260. Among all227 compatible
strict ratios,223 have positive uniform capacity. The entire zero bank is

    u_18/u_24 in {309/8,303/16,219/128,201/152}.       (3)

Every listed arm is individually realizable with the requested margins
and an actual zero-capacity translated grid. The retained controls are:

| Margin pair | Separate actual speed pair | Zero phase on quotient grid |
|---|---|---|
|4,24|1228,192|153/307|
|18,24|1854,48|51/103|
|18,24|1818,96|50/101|
|18,24|1314,768|36/73|
|18,24|1206,912|33/67|

These phases are on the effective1890 or1260 grid after the sheet map;
they are not asserted to be a shared original phase. Their purpose is
to show the obstruction is genuinely joint: neither individual zero
bank is empty or incorrectly declared impossible.

The finite verifier enumerates all5855 strict ratios. It computes exact
capacity for every compatible ratio and separately evaluates every wall
and chamber literally for all five zero cases. It does not infer a zero
bank from one minimizing representative or from a short floating sample.

## 5. The same middle would need two different absolute depths

The clock has v_2(7560)=3. The endpoints have unsaturated depths

    v_2(u_4)=2, v_2(u_18)=1.

The first arm (2) has denominator48 of depth4 and odd numerator, so it
forces `v_2(u_24)=2+4=6`. The four arms (3) have denominator depths
3,4,7,3 and odd numerators. They force respectively

    v_2(u_24)=4,5,8,4.

Thus the two complete possible depth sets are

    {6} and {4,5,8},

which are disjoint. Although all these values truncate to the same
24-margin depth3, they cannot describe the same actual middle speed.
This contradicts the forced actual wedge and closes all six words.

The contradiction does not depend on untested endpoint geometry or a
joint realization inferred from separate arms. Restoring the native
depth shows that the local zero choices have no joint realization.
As a separate native corollary, any actual4--24--18 strict path at
this clock has a positive-capacity arm. On a seven-tail zero-budget
grid that arm supplies at least4 weak-safe points, because its sheet
multiplicity is either4 or6. The connected-word consumer only needs
the existence of a safe lift, and does not assume the path is present
outside its zero-edge case.

## 6. Entry consequence, exact array and stopping reason

In the inherited physical scope, suppose a primitive thirteen-speed row
has a six-label subset A with gcd(A)=7560 and connected actual strict
graph on the complement B. Under hypothetical failure the full126
profiles and the complete37209-word bank apply. The prior minimum-tree
consumer or the six-word contradiction above then supplies a weak-safe
complementary lift of the CITED proper-six safe phase. This contradicts
failure. No connectivity on A, speed-height box, decoder equality or
physical unit speed is imposed.

The probe first removes only6930 from the pinned7622-clock array.
This theorem removes only7560 from that resulting7621-clock array.
The final necessary set therefore has7620 clocks, maximum11935, with
canonical JSON SHA256

    66f903430fd804074e6c8adb330aca4f6bb6b8f97d955e014a73309c39d17a8d.

The initial census stopped at its first complete residual as declared.
That same residual was then pursued to a structural contradiction;
the two later declared clocks9240 and11088 remain untested. There is
no reason to continue sampling nearby clocks within this subtask.
The reusable next criterion is the compatibility of lifted prime depths
on mandatory zero-edge paths, keeping the complete profile word and
the real quotient clocks. No unexamined clock is deleted by that criterion
without its hypotheses being paid.

Run `04-computation/continuing12_20260908_lrc_depth_wedge.py` normally
and with `-O`. Before installation, run it with the repository as the
working directory and the frozen probe certificate beside the source.
It imports no producer and verifies the full residual pin, positional
topology, both entire native banks, literal zero controls, depth
incompatibility, and the complete old/new array difference. Both modes
pass735 always-active exact gates with identical raw LF output and
certificate regeneration. Independent audits of the complete profile
census and the native proof remain separate promotion gates.

## Independent acceptance

The [independent audit](continuing12_20260908_lrc_independent_audit.md) accepts
this scoped result and its analytic proof, with 91203 exact gates per
normal and optimized pass. Its producer-independent controls retain the
declared positive and hostile boundaries. The parent replays both modes
against frozen raw LF outputs and certificates.
