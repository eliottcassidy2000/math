# Independent audit: two actual meridians for the next infinity curve

**Status: INDEPENDENT ANALYTIC + EXACT SOURCE AUDIT PASS.**
The full theorem in [the primary note](planar_jc48_sep07_next_braid.md)
is accepted: the actual affine complement is generated by two positive
curve meridians, and the inherited whole-support Keller realization is
excluded. The stronger all-degree retained-sheet consumer is also accepted
with the now-explicit irreducibility hypothesis. No source, witness, or
mathematical correction remains.

## 1. The certificate belongs to the actual curve

I read the full source and proof, together with the earlier geometry
audit. The actual pair is

```
U=t⁴+t³+t², V=16t⁶+24t⁵-19t³-19t².
```

Using ordinary symbolic elimination separately from the verifier, I
reconstructed Res_t(U-u,V-v) and obtained exactly its literal monic
quartic F(u,v). I also independently reconstructed

```
disc_v F=-u⁵(256u²+11u+12)
          (1183744u³+1870400u²+974463u+168070)².
```

The inherited birational normalization and degree-four u-projection
therefore give the correct minimal equation, rather than an extra
component of a resultant. The new proof does not need to identify which
critical value is enclosed by a path: any certified regular closed path
gives the necessary group relation.

I separately checked, with Fraction arithmetic on the raw witness, that
every successive u-point lies on the stated six edges, in the stated
order and with strictly increasing affine parameter on each edge.
All six vertices occur, and the two totals are5875 and9012 segments.
Thus the declared paths, as well as their closed-loop property, have
been checked without relying on the numerical proposal routine.

## 2. Root isolation, matching, and actual braid transport

The rational verifier uses only Fraction arithmetic in its default
mode. The Gaussian lower bound max(|Re z|,|Im z|) is at most |z|,
and the upper bound |Re z|+|Im z| is at least |z|. Consequently the
strict displayed Taylor inequality is a valid Rouché domination of all
nonlinear and base-motion terms by B01 w. It isolates exactly one root
in each fixed disk throughout a segment. The four disks are disjoint:
each radius is at most1/32 of the relevant pair's lower separation,
so the sum of two radii is strictly smaller than their actual separation.
Monicity and degree four ensure that these disks contain all roots.

The endpoint displacement and the second endpoint isolation gate are
both needed. The new centre moves by less than half the old radius;
its actual root lies within min(old radius,new radius)/64. It therefore
belongs to both successive disks. This identifies the continuation of
each labelled root and is stronger than a numerical nearest-neighbour
match. Initial isolation, a common labelled base centre tuple, and exact
final unordered closure identify the two loops in the same configuration
space. Inside each convex disk, the straight homotopy from the actual
root strand to the polygonal strand cannot meet another strand. These
homotopies agree at junctions, and the endpoint isotopy is common to
both paths.

The projection multiplier1+i/4 preserves orientation. The source checks
nonzero endpoint real differences, distinct rational crossing times,
adjacency and nonzero imaginary separation. The initially-left strand
below the right one gives the positive counterclockwise half-twist.
Chronological cancellation only removes adjacent inverse letters; it
does not change the braid. The resulting literal words are

```
W+=[3,2,1,-2,-3], W-=[-3,2,3].
```

The meridian convention is the below-stem convention of the previously
audited actual braid proof. Its geometric half-twist sends the old left
meridian to the final right one and the old right meridian to
x_right^-1 x_left x_right. Contravariant transport of representation
coordinates consequently gives the chronological Hurwitz action
H+(a,b)=(aba^-1,a), H-(a,b)=(b,b^-1ab). The orientation and direction
of this action are consistent; no unlabelled conjugacy statement is
being substituted for marked fixedness.

## 3. Why the two words are relations in the whole affine complement

The monic quartic supplies a continuous global outside-root section;
over a compact region containing both loop fillings one can choose one
constant vertical basepoint above a uniform Cauchy root bound. Each
section lift then bounds a disk in the actual affine complement.
Over the regular base, punctured-fibre generators and section lifts
generate the bundle fundamental group. Restoring the finitely many
critical fibres kills the section lifts, and general position lets any
loop in the full complement avoid those fibres. Therefore the fibre F4
surjects onto the actual affine fundamental group.

Transport around either loop must hence fix each of the four meridian
elements exactly. This is the load-bearing bridge from configuration
paths to actual group relations; a conclusion only up to simultaneous
conjugacy would not suffice. No complete list of critical loops or full
van Kampen presentation is required for these necessary relations.

I independently multiplied both words in an arbitrary group. For a
tuple (a,b,c,d), the two results are

```
W+ : (a b c d c^-1 b^-1 a^-1, b, c, c^-1 b^-1 a b c),
W- : (a, b d b^-1, b d^-1 c d b^-1, b).
```

The first is fixed iff a=b c d c^-1 b^-1, and the second iff b=d.
Thus all four meridians lie in the subgroup generated by the actual
positive meridians c,d. This proves the two-meridian generation claim
without using a finite permutation census.

## 4. Actual consumer, sharp controls, and scope

Two single three-cycles are intransitive on six labels: intersecting
supports cover at most five labels, and disjoint supports preserve two
triples. The latter case has no common fixed point, so the proof correctly
uses intransitivity. The raw finite inventory retains that hostile:
the order-nine rows have zero common fixed labels.

I also checked the all-degree inequality. Replace each nontrivial cycle
of a generator by a spanning tree on that cycle. The resulting graph
has exactly the generated group's orbits as connected components.
A generator moving at most delta labels contributes at most delta-1
edges when delta>=1; an identity contributes zero. Transitivity on d>1
labels implies r(delta-1)>=d-1. Two cycles meeting in one label attain
the abstract bound at d=2delta-1.

For an irreducible whole nonproperness curve, the generic retained
sheet count a is the same at both positive-meridian generators. These
actual sheets are fixed; the moved-label count is at most delta=d-a.
If a>=d/2, two generators give 2(delta-1)<=d-2, contradicting the
connected-graph lower bound d-1. This does not assume that deleted fixed
sheets are absent or that each meridian is one cycle. The inherited
ordinary-node ledger with at least two nodes supplies d<=2a for the
stated one-odd-cusp-plus-nodes domain. The actual curve has that domain.

The theorem excludes this whole-support Keller realization. It does
not calculate the full affine group, classify all curves with a cusp,
or invalidate the independently proved infinity-link A6 representation.
That representation survives at the boundary and cannot extend to the
actual affine complement with the required marked transitive action.

## 5. Full rational replay and frozen pins

I replayed the default verifier normally and under python -O. Both
119191-gate outputs are byte-identical to the frozen1147-byte output.
This rechecks all14887 rational tubes, endpoint matches and crossings,
the40³ raw fixed-first-cycle universe, the independent two-generator
parametrization and all stated positive/hostile controls. No floating
arithmetic or scout mode is used in either replay.

```
python3 -B 04-computation/planar_jc48_sep07_next_braid.py
python3 -B -O 04-computation/planar_jc48_sep07_next_braid.py
```

```
source d5e74a1eb6f8e7d9bad37bb723ccf144f335d6a51d5c77170b097f8be10e8efc
output 0d33e3e7ec3d4bc181455fd2267b23a9740ca55be188e1c9679a4a9b1bfe87a4
compressed witness e623253a5980eebe2a882680a6f79ba21703adb324319d4d909594beb2cfc330
raw witness c2bc0600d55c05b566e231a1af284aa4b825b7d666b5ede0775db92fe49f11c8
```

Sizes are14547 source bytes,1147 output bytes,1102982 compressed witness
bytes and4205356 raw JSON bytes. All pins match the primary freeze.
The clarified irreducibility condition is present in the final primary
text. This independent audit is frozen for parent integration.
