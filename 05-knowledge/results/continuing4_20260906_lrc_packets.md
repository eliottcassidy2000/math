# Actual small-scale packet obstructions and the interval information they omit

**Status: PROVED elementary construction + FINITE-EXACT + INDEPENDENTLY AUDITED.** There are actual primitive boxed 6+7 decoder equality entries, with every retained hereditary profile, at scales 1369,1373,1583. Their first physical safe denominators are respectively 31,29,37. All three lie inside the newly reduced scale domain, but **the entire displayed class is already safe by the inherited individual-interval grid count**. This is a precise stopping object and information-loss certificate, not a new unresolved LRC class or a universal closure.

[Independent proof and exact audit](continuing4_20260906_lrc_packets_audit.md) passes.

## 1. Inheritance and concept board

The baseline was a5b2011be, with a clean synchronized worktree. Read the current startup, research protocol, LRC frontier and active guardrails, targeted mistakes, continuing3 synthesis, and both incoming grid/decoder proofs and their audits.

The closest proved mechanisms are [THM-3818 / actual inert pair decoder](../../01-canon/theorems/THM-3818-scaled-inert-cubeclass-support-two-pair-packet.md), the [incoming grid ceiling](third_20260906_grid.md), and [rooted gcd decoder closure](third_20260906_decoder.md). These leave a hypothetical unsafe balanced entry with t<=97096 and max U>=29. The earlier [actual packet obstruction](continuing3_20260906_lrc_packet_obstruction.md) has t>=412164, so it is outside this smaller domain.

The canonical hostile is that earlier pair of scales with identical primitive shapes and full profiles but different denominator29 compatibility. The corrected near misses are (i) using graph rank without excluding all crossing supports; (ii) treating gcd profiles as a phase condition; and (iii) replacing the sum of clipped interval counts by a bound using only total overlap length and component count. The least-used sidecar here is the list of **individual overlap interval lengths before summing their rounding errors**.

The concept board is: cofactor prime support; strict actual decoder topology; bounded crossing coefficients; inherited complement words; located modular phase packets; and unpooled translated-grid counts. No tournament representation is imposed. The new object is an embedding of packet failure inside the reduced scale domain, rather than a new CRT theorem or grid-ceiling refinement.

## 2. One complete actual-entry class

Set Q=91^6=567869252041 and

```text
P=179*191*251*257*263=580028043449,
V={P} union {83P/q: q in(179,191,251,257,263)}
 =(183050675309,187324231931,191802102017,
   252054071237,268951550873,580028043449).

q=(81,101,125,128,161,209,221),
H=product(q)=973398810384000,
U={H/q_i}
 =(4404519504000,4657410576000,6045955344000,
   7604678206125,7787190483072,9637611984000,12017269264000).
```

**Claim.** Every row tV union U with

```text
1334<=t<=97096,       gcd(t,H)=1                       (1)
```

is an actual primitive, distinct, positive thirteen-speed row in the Q^2 sum box, with exactly two actual graph components V and U, weighted decoder rank11, and W_(Q,3)=V_dec. Every subset of sizes7 through12 has gcd1, and all4095 full inherited complement profiles pass. Both primitive shapes are unitless, min V>3Q/28, and max U>28. V is all odd; U has mixed parity.

**Proof.** The five leaf denominators are pairwise coprime and coprime to83; the seven q_i are pairwise coprime. Thus both shapes are primitive. Their prime supports are disjoint. The strict actual atlas retains only reduced sums at most356 with every prime factor congruent to2 modulo3 and exponent at most two. Direct factorization gives a connected five-edge star on V and a connected eight-edge graph on U. This is a complete graph check, not a selected-edge assertion.

The smallest V pair gcd is D_min=712259437. On any support with two physical V labels and one U label, the latter's smallest nonzero coefficient is t*gcd(v_i,v_j), because t and every V prime are coprime to H. It exceeds Q throughout (1). This excludes all105 supports, including the cases where one of the other coefficients vanishes.

For two U labels H/p,H/q and an outside t*v, put D=H/(pq). Their reduced coordinates are q,p and gcd(D,t*v)=1. Any nonzero outside coefficient is kD for some nonzero integer k. A bounded crossing would therefore require

```text
|k|*t*v = |r*q+s*p| <= Q(p+q).
```

But t*min V>Q*(221+209) for every t>=1334. This excludes all126 reverse supports, including every possible outside multiple, rather than only its first value. The exact sufficient entry threshold is

```text
max(floor(Q/D_min)+1, floor(430Q/min V)+1)=1334.
```

No bounded crossing remains. All remaining support-three relations lie within the two weighted component kernels, which the actual connected edge rows span. Consequently W_(Q,3)=V_dec and its dimension is11. The companion also checks the literal integer edge rows and computes rational matrix rank independently of component counts.

The disjoint component prime supports and gcd(t,H)=1 make every mixed subset gcd1. The only size-at-least7 subset confined to one component is the whole U, whose gcd is1. Therefore all4095 large subsets have gcd1 and all complement words are all ones. The source verifies that these exact words belong to the current full profile bank; scalar gcd bounds are not substituted for that membership. Finally 97096 sum V+sum U<Q^2. Disjoint support also prevents a physical equality t*v=u, so the thirteen labels are distinct. This proves the whole bounded class.

## 3. The located packet changes inside this class

For a positive row A let B_d(A)={j mod d: min_(v in A)||vj/d||>=1/14}. The exact inherited CRT operation is

```text
B_d(tV union U)={j in B_d(U): t*j mod d in B_d(V)}.     (2)
```

This elementary identity retains the scale residue. It is not a new gluing theorem; compare [THM-2059 / CRT dot-product gluing](../../01-canon/theorems/THM-2059-crt-fiber-product-phase-packet.md) and the earlier packet report. Every d=2,...,28 divides at least one U label, as the companion's complete divisor certificate shows. Hence every numerator fails for U, and for every physical row containing it, at those denominators.

The first U packet is

```text
B29(U)=(1,2,4,5,24,25,27,28),
B29(V)=(3,7,8,9,11,13,14,15,16,18,20,21,22,26).
```

The number of common phases in (2), indexed by t mod29 from0 through28, is

```text
(0,0,2,4,6,2,0,4,8,8,6,8,0,6,2,2,6,0,8,6,8,8,4,0,2,6,4,2,0)
```

The zero residues are 0,1,6,12,17,23,28. In particular the following literal physical controls pass every entry and profile check:

| t | Physical sum | First safe denominator | One phase | Exact clearance |
|---:|---:|---:|---:|---:|
|1369|2329090049184301|31|4/31|4/31|
|1373|2335742891883565|29|2/29|3/29|
|1583|2685017133594925|37|1/37|5/37|

Both primitive packets at29 and31 are nonempty, and all three t are units at those moduli. Thus at1583 neither of the first two useful U clocks,29 and31, joins; this is a phase incompatibility rather than an empty component packet or a noninvertible scale. The full first-denominator statements check every numerator at every smaller denominator, not only prime denominators.

These are counterexamples to forcing a denominator<=28 or compatibility at the first useful prescribed U clock **even after retaining actual equality, all current profiles and t<97097**. They do not refute adaptive methods allowed to leave a fixed small denominator bank, or an arbitrary uniform larger denominator bound.

## 4. The precise stopping boundary: the same class is already grid-safe

For a translated t-grid, the inherited marginal bound gives at most ceil(t/7) dangerous points per U label because gcd(t,u)=1. Its seven-tail excess is E=7ceil(t/7)-t, with0<=E<=6. Take the U pair with reduced coordinates p=81,q=101. Its open danger intersection has25 circle components. Their exact lengths are

```text
ell_0=1/(7q),
ell_(+k)=ell_(-k)=min(2p,p+q-14k)/(14pq), 1<=k<=12.
```

The source reconstructs these lengths independently by intersecting the literal rational danger arcs and rejoining the origin component. Their sum is389/19089.

The pooled lower bound t*sum ell-25 gives55316/19089<E=3 at1369, and56872/19089<E=6 at1373. Across **all21 U pairs**, these are the only positive pooled credits; even their total fails. Rounding the total overlap length before subtracting25 likewise does not force positivity. Consequently no forest built from those pooled credits repairs this particular loss.

The inherited interval count can instead be applied before summing:

```text
C(t)=sum_j(ceil(t*ell_j)-1).                           (3)
```

It is15 at1369 and1373, already exceeding E. More strongly C(1334)=13, and every term is nondecreasing in t. Therefore C(t)>=13>6 for every t>=1334. The individual overlap intervals guarantee at least C(t)-E safe grid points uniformly over the translated phase. This is exactly the unpooled source of the incoming grid proof, not a newly invented overlap inequality.

V's half-phase is safe with clearance1/2 since it is all odd. Its physical lifts are (2j+1)/(2t); (3) gives U safety on one of these lifts. Since7 does not divide t in (1), no such lift has a coordinate at distance exactly1/14, so the full row is strictly safe. Literal enumeration gives462,458,530 safe half-phase lifts at t=1369,1373,1583 respectively.

This is the stopping reason. Cofactor regrouping retains the actual entry and all profiles while changing the modular packet, but it does not defeat the richer translated-grid supplier. A failed pooled cost is not a failure of its unpooled interval source. The next genuinely different target must also retain the individual clipped interval count or the actual located grid, rather than declare these rows unresolved.

## 5. Connection contract and reproduction

The source object is a pair of primitive cofactor components with disjoint prime supports. Scaling preserves internal actual topology and makes full crossing exclusion uniform; it destroys phase location unless t modulo the selected clock is saved. The exact sidecar is (2). The further map from clipped overlap arcs to total length and component count preserves a coarse sufficient lower bound but discards separate rounding gains. The controls above test both losses on the same actual boxed rows.

The [standalone source](../../04-computation/continuing4_20260906_lrc_packets.py) uses only the Python standard library and imports no producer. It checks all231 crossing supports and4095 profiles for each of three rows, literal weighted ranks and physical phases, complete denominator exclusions and CRT joins, and an independent exact arc-intersection reconstruction. Its positive controls are the actual safe phases and translated half-phase grids; its hostiles are the empty first/second packet joins and failed pooled overlap estimates. The containing class follows from the displayed all-parameter proof, not a scale census.

```text
python -B 04-computation/continuing4_20260906_lrc_packets.py --profiles 04-computation/overnight12_20260906_lrc_decoder_descent_inherited_profiles.json
python -B -O 04-computation/continuing4_20260906_lrc_packets.py --profiles 04-computation/overnight12_20260906_lrc_decoder_descent_inherited_profiles.json
```

Normal and optimized runs pass13,338 always-active gates with identical actual LF output bytes. No post-normalization was applied. Pins:

```text
source 22db0faeddf433aed8d1daeb664976813e4ed8a0b5a0fec57fdeaac027a23b08
output 46c71d72aefab9d3e120c58317cc616c8ad37c1db3b7de9e2335c887a8fae151
profiles 935f3f687b6d7c89cc099e536f536238fd753bcc4c1747906d213cef387ca93f
```

No theorem ID, maintained file, repository change, or Git mutation was made. LRC(14) and general entry remain OPEN.
