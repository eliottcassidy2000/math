# Actual equality entries do not force small phase packets or their first compatibility

**Status: PROVED elementary construction + FINITE-EXACT complete controls; full independent proof/source audit passed.** The two displayed rows are safe and already fall under the inherited larger-unit theorem. This note proves a stopping boundary for a proposed packet-forcing route, not a new general gluing theorem or LRC closure. No canonical ID is claimed.

An explicit bounded class of actual decoder entries with split6+7 can have a normalized six-component whose first1/14-safe rational denominator is29. All hereditary joint-shadow profiles can still be retained. For two such rows with the same primitive shapes and profiles, the exact29-packet compatibility is positive for one scale and zero for the other. The complete physical first denominators are respectively29 and31.

The opposite primitive component has a denominator2 packet. Thus this does **not** refute a theorem that adaptively selects either component, nor any unproved bound larger than the displayed tested limits.

## 1. Inheritance and the precise failed implication

**THM-2059**, `01-canon/theorems/THM-2059-crt-fiber-product-phase-packet.md`, already gives the exact CRT histogram join. Its nonzero dot product certifies a selected grid; zero rejects only that grid. **THM-3381**, `THM-3381-reflected-residue-affine-phase-transport-and-frozen-tree-stability.md`, explains why an unlocated arithmetic perturbation can destroy a phase certificate. **THM-4049**, `THM-4049-lrc14-d2-two-phase-residue-firewall.md`, and **THM-4098**, `THM-4098-weight-seven-antipodal-scale-escape-and-missing-parity-rows.md`, already supply multiple-phase and periodic safe-net transport. None asserts that the actual decoder forces the required packet or alignment.

The canonical corrected near miss is **THM-762**, `THM-762-small-denominator-signed-pair-deck.md`, together with **THM-1100**, `THM-1100-extended-rational-point-sieve.md`: primitive normalization does not remove coordinatewise denominator loading. THM-1100 explicitly credits the earlier **THM-566** obstruction. The general small-denominator no-go is therefore inherited. The present addition is its distributed zero-owner realization inside the complete actual rank11 decoder and all current hereditary profiles.

The entry mechanism is the full two-component criterion in `overnight14_20260906_lrc_general_decoder.md`, under **THM-3818**, `THM-3818-scaled-inert-cubeclass-support-two-pair-packet.md`. Current profiles are from `lrc14_joint_shadow_empty_core_next_sep06.md/.json`. The companion `continuing1_20260906_lrc_phase_bank.md` closes an explicit residue class but does not force that class. Exact searches for the new coordinates and scale found no earlier copy; this is an internal duplicate check, not external priority.

Concept board: distributed divisor owners; actual weighted component kernels; hereditary body gcds; primitive phase packets; located scale compatibility. Divisor owners control the first packet but are invisible to all large-body gcds in this construction. Compatibility depends on a located scale residue even after every other displayed object is fixed.

## 2. Distributed covering with a strict-atlas six-component

Set

    Q=91^6=567869252041,
    q=(16,25,27,161,187,247),
    P=product(q)=lcm(1,...,28)=80313433200,
    V=sorted(P/q_i)
     =(325155600,429483600,498841200,
       2974571600,3212537328,5019589575).

The q_i are pairwise coprime, so gcd(V)=1. The shape is unitless and has both parities. For every d<=28, at least one q_i is coprime to d: d has at most three distinct prime factors, while the six disjoint q_i groups would require at least six. Since d divides P, it then divides P/q_i. Consequently at every rational phase k/d with2<=d<=28, at least one V speed is exactly at the observer. This blocks every numerator, including nonunits; there is no missing coprimality inference.

The strict inert atlas uses reduced sum at most356 with every prime factor2 mod3 and exponent at most2. Its complete edges on the displayed zero-based V order are

    (0,3),(0,5),(1,3),(1,4),(2,3),(4,5).

Their reduced sums are274,263,214,212,188,41, respectively; direct factorization verifies every edge and the graph is connected. The complete safe denominator29 packet is

    A={5,6,7,9,10,11,12,17,18,19,20,22,23,24} mod29.   (1)

It is nonempty, so29 is exactly the least safe rational denominator of V.

## 3. Two complete actual-entry rows with all profiles retained

Take

    U=(1,43,67,79,97,109,127),
    t0=412164, t1=412183,
    n_i=t_i V union U.                                (2)

Both U and the physical rows are primitive. The complete U atlas edges are

    (0,1),(0,2),(0,5),(1,2),(1,6),(2,4),(3,5),(5,6),

with reduced sums44,68,110,110,170,164,188,236. This graph is connected. Every physical cross pair is coprime, and every cross ratio exceeds355, so the actual graph is exactly6+7. Internal scaling leaves the reduced sums unchanged. The first physical sum is5135637177376615; both sums are strictly below Q^2. Their actual edge spans have dimension13-2=11.

Rank alone is not entry. Put

    dV=min gcd(v,w)=1738800,
    sU=max (u+w)/gcd(u,w)=236.

For either scale, all cross gcds gcd(t_i v,u) are1. For two smaller labels tv,tw and one larger label u, a nonzero outside coefficient must be a multiple of t gcd(v,w)>Q. At t0 the minimum is

    t0 dV=716670763200>Q.                              (3)

For two larger labels u,w and one smaller label tv, clearing D=gcd(u,w) forces target magnitude at least tv, while the remaining signed sum after normalization is at most Q(u+w)/D. At t0 the minimum target/bound ratio is

    t0 min V/(Q sU)=4786336882800/4786326552917>1.       (4)

This excludes all105 and126 supports in the two orientations. Zero coefficients on a redundant label include support-two crossings. The same inequalities remain strict at t1>t0, whose cross-coprimality is separately checked. Thus every height-at-most-Q, support-at-most-three relation stays internal. Actual atlas edges of height at most355<Q span both internal weighted kernels, proving **W=V_dec** for both rows.

Any body of size at least7 either is all seven U labels, with gcd1, or meets both components and contains a coprime cross pair. Hence all4095 bodies of sizes7 through12 have gcd1. Their complementary gcd profiles are precisely the all-one signature at each tail count1,...,6. The current joint-shadow note explicitly retains those signatures, and the source independently checks them against the complete pinned JSON and checks every actual body in both rows. This verifies more than the scalar caps.

This construction illustrates a real information loss: a pure six-component is below every hereditary body size in that profile hierarchy. Large internal cofactor gcds and complete small-clock zero ownership coexist with gcd1 on every inspected larger body.

The same proof gives the entire bounded scale class

    412164 <= t <= 25880486141010,
    gcd(t,305613336829)=1,                              (5a)

where305613336829=product(U). Every such row tV union U is primitive, has the same actual6+7 graph, satisfies W=V_dec and the physical sum bound, and retains every hereditary profile. Indeed cross-coprimality follows from the fixed disjoint prime supports and the displayed gcd; both mixed-relation inequalities increase with t. The lower endpoint is the exact integer threshold for the stronger of these two uniform inequalities. The upper endpoint is exactly floor((Q^2-sum U)/sum V). Cross-component ratios are already greater than355 at the lower endpoint and increase. This is a bounded class within the actual box, not an unbounded-height family. The larger-unit theorem already closes every member.

## 4. Positive and zero compatibility with the same component packets

The U denominator29 packet is

    B={5,9,10,12,14,15,17,19,20,24}.                   (5)

At physical phase k/29, the two conditions are exactly tk mod29 in A and k in B. This is the simple equal-modulus specialization of inherited CRT packet compatibility, not a newly claimed join theorem.

For t0 mod29=16, the compatible packet is

    {5,12,17,24}.                                     (6)

The denominator-loading argument still blocks every physical denominator2 through28, so the complete physical first denominator is29. At x=5/29 its exact clearance is3/29>1/14. A separate lifted phase166010/332021 has clearance7/29.

For t1 mod29=6, the two nonempty primitive packets have **zero** compatibility. The complete overlap count as the multiplier runs0,...,28 is

    (0,8,8,2,8,2,0,8,6,4,2,6,2,4,10,10,4,2,6,2,4,6,8,0,2,8,2,8,8).

Thus only the nonzero multipliers6 and23 fail at this clock. Denominator30 is also blocked by a divisible coordinate. Direct exact enumeration gives the first physical denominator31, with packet{3,28}; x=3/31 has clearance3/31. The two scales differ by19 but have identical primitive shapes, internal atlas, actual partition, rank, equality entry, and full inherited profile signatures. Those data do not determine the positive/zero status of this selected first primitive clock. The missing coordinate is the scale residue in the packet compatibility relation.

The inherited larger-unit theorem `overnight15_20260906_lrc_larger_unit.md` already proves both rows safe: U contains1 and min V=325155600<=floor(3Q/28). The new result is therefore an exact actual-domain stopping object, not another independent closure of a previously open safety class.

## 5. Strongest survivor and scope of the stop

The failed implications are now precise:

1. Actual bounded6+7 equality and all retained profiles do not force a safe rational denominator at most28 in **each** primitive component or in the full physical row.
2. Even at the first useful V denominator, the same entry and profile data do not force positive compatibility of the two nonempty component packets.

The strongest survivors are the exact CRT join, sufficient circular-gap/interval gates, and phase-aware transport. A zero join only rejects that clock. U has the denominator2 phase1/2, so an adaptive method that chooses either component is not refuted. No all-denominator obstruction or unbounded-height claim is made: the actual Q^2 box and atlas component-height bounds remain retained.

Connection contract: source is a primitive cofactor cover and exact component packets; target is a complete actual physical row; the scale map preserves internal graph and primitive shape but changes the multiplier in the CRT join. Passing only packet cardinalities, component ranks, or hereditary gcd profiles destroys that located multiplier. The sidecar is the exact residue-labelled overlap; the two scales in(2) are the cheapest decisive test. A larger generalized compiler without a new compatibility-forcing argument would restate THM-2059, so this bounded lane stops here.

## 6. Exact companion and audit status

The standard-library source uses no producer imports. It checks every strict atlas edge, both complete mixed support orientations, all4095 body profiles for each row, all low-denominator numerators, both first physical packets, and the complete multiplier profile. It accepts --repo for the inherited JSON; canonical LF input bytes are pinned, accounting explicitly for Windows CRLF checkout conversion. Output is captured as actual LF bytes without postprocessing.

    python -B 04-computation/continuing3_20260906_lrc_packet_obstruction.py --repo .
    python -B -O 04-computation/continuing3_20260906_lrc_packet_obstruction.py --repo .

Normal and optimized outputs are identical and pass12684 always-active exact gates.

* Source SHA256: `66eacc9647b765c392fb080ed41a031fd50a307b6d58b49629d2cdf342832080`.
* Output SHA256: `b3e340d2c5731d9e9b0bf48b78ff8f8b84db887cf1b5c375e129a364a63ee0f4`.
* Inherited JSON canonical-LF SHA256: `935f3f687b6d7c89cc099e536f536238fd753bcc4c1747906d213cef387ca93f`.

An initial bounded arithmetic review was followed by the
[full independent audit](continuing3_20260906_lrc_packet_obstruction_audit.md).
It accepts both rows and the complete bounded scale class, reconstructing
weighted rank, all mixed supports and profiles, and both exact first clocks
by distinct arithmetic paths. Its 8842 normal/optimized gates and the
primary 12684 gates reproduce byte-identical frozen outputs. This supersedes
the preliminary review's coverage, without changing the mathematical claim.
