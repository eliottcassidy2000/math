# Independent referee: the minimum connecting-tree LRC consumer

**Status: PROVED analytic audit and FINITE-EXACT complete independent replay; PASS.**
No mathematical repair is requested. This accepts the producer's full
connected-seven-complement consequence, and does not claim LRC(14) is closed.

The audited producer is
`continuing8_20260906_lrc_minimum_tree.md/.py/_certificate.json`.
Its original candidate report, source, and complete certificate are pinned below.
The parent owns any subsequent status promotion and repository filing.

## 1. The graph direction is correct

Write the actual seven speeds as `u_i`, put `d_i=gcd(t,u_i)`, and let
`X_alpha={(alpha+j)/t mod1:0<=j<t}`. Danger means the strict predicate
`||u_i x||<1/14`. An actual edge has speeds `D p,D q` with primitive strict
atlas ratio `p<q`. The identities

```
e=gcd(d_i,d_j)=gcd(t,D),  t=en,  D=eD',  gcd(D',n)=1,
{d_i,d_j}={e gcd(n,p),e gcd(n,q)}
```

are exact. The multiplication map by `D` takes the actual grid to a translated
`n`-grid, with multiplicity `e`; its remaining cofactor is a permutation.
Thus the minimum over all compatible primitive ratios and all translations
is a lower bound for the actual edge at every actual phase. The producer
does not replace a conditional minimum by a favorable sample.

For any spanning tree of the actual connected graph, and at each grid point,
the number of induced tree edges on the dangerous labels is at most their
multiplicity excess. Summing gives

```
safe count >= t - sum_i |D_i intersect X_alpha|
                  + sum_actual_tree |D_i intersect D_j intersect X_alpha|
           >= MST_w(d) - E_t(d),
E_t(d)=sum_i d_i ceil(t/(7d_i))-t.
```

The **minimum** tree on the graph of all possible compatible edges is at most
the cost of every actual spanning tree. This is the crucial direction.
Independent edge minimizers need not coexist in a physical row, and their
minimizing phases need not coincide: the lower bounds already hold separately
for every phase, so they can be summed on any one actual tree. A maximum
tree over merely possible edges would be invalid. The audit includes a
zero-weight actual path with positive-weight nonactual star edges as a
literal graph-level hostile to that replacement.

Seven positional vertices are retained, including repeated sheet margins.
If the possible-edge graph is disconnected, an actual connected graph cannot
realize the word. The finite sentinel `10^10` implements this case safely:
all finite weights are at most `t<=14868`, and marginal excess is much smaller
than the sentinel. It does not assert a finite physical overlap of that size.

This is the inherited induced-forest inequality, with a new correctly typed
minimum over unknown actual graph data. Relevant proved routes are
`third_20260906_grid.md`, THM-2060
`01-canon/theorems/THM-2060-crt-tail-coset-saturation.md`, THM-2064
`01-canon/theorems/THM-2064-multitail-sheet-capacity-and-dyadic-seam.md`, and
the strict ratio atlas of THM-3818
`01-canon/theorems/THM-3818-scaled-inert-cubeclass-support-two-pair-packet.md`.
The general transfer is already explicit in
`third_20260906_grid_refined.md`, general connected-complement corollary.

## 2. Independent geometry and strict boundaries

The referee never imports or executes a mathematical producer. Its native
companion builds the atlas by multiplying inert primes with exponents zero,
one, or two, then lists primitive positive pairs whose sum is at most356.
This independently gives all5,855 pairs.

For a pair `p,q`, set `L=14pq`. The referee collects **all raw spatial walls**
of both danger predicates, evaluates exact integer midpoint membership in
every resulting cell, and joins the true cells, including the origin wrap.
This differs from the producer's merge of two sorted interval lists.

For each unwrapped open component `(a/L,b/L)` its count at phase
`alpha=phase2/(2L)` is reconstructed directly as

```
ceil((2nb-phase2)/(2L)) - floor((2na-phase2)/(2L)) - 1.
```

The independent event engine starts from the literal counts at phase0 and
immediately to its right. At any positive projected wall, ending intervals
are removed **before** the wall is tested; starting intervals are added
only afterward. Coincident starts and ends therefore retain isolated holes.
This is a signed floor-count sweep, not the producer's base-plus-projected-arc
and puncture implementation. Every chamber and every wall is represented.

The separate open-interval floor is used only to skip a candidate whose
proved lower bound cannot improve the current minimum. The audit checks
each final attainer's strict-atlas membership, two margins, separate floor,
and exact located count. It also checks all-wall direct ceil/floor controls
and original native-grid danger predicates, including equal-frequency
geometry controls and the zero count at `(p,q,n)=(1,355,2485)`.
Every finite table attainer at the worst upper clock12672 and the hostile
clock7200 is additionally checked on its literal minimizing grid.

Open endpoints are essential. The excluded danger sets are strict, so the
result produces weak clearance `>=1/14`. No strict physical clearance or
monotonicity in the clock is inferred.

## 3. Complete independent finite universe

The referee pins the raw inherited profile JSON and the complete continuing7
necessary array. It derives its own target list as **every** member at least
12000, then appends7200 solely as a hostile. Each clock's alphabet is derived
by divisibility from the full42-state alphabet, without consulting a producer
domain cache.

For every distinct domain the native program enumerates the entire unpruned
sorted seven-multiset universe. At each complete word it requires total gcd1
and directly tests all126 nonempty proper positional subsets against the
appropriate complete inherited profile table. It uses no partial-word
projection pruning. Repeated values remain distinct positions in these tests.
Every lexicographically ordered complete word-list hash agrees with the
producer certificate, including the extra7200 domain.

For every clock it reconstructs every possible margin-pair minimum over the
full compatible atlas universe. It evaluates all its independently generated
words using **Kruskal on21 positional edges**, rather than the producer's
vectorized Prim algorithm. It matches every finite pair-table column,
impossible-pair flag, compatible-candidate count, exact-profile evaluation
count, minimum owner, six positional owner-tree edges, and full survivor list.

The complete replay has:

| Quantity | Count |
|---|---:|
| Selected upper clocks |549|
| Upper clocks plus7200 |550|
| Distinct divisor domains including7200 |123|
| Unpruned seven-multisets |40,189,061|
| Complete admissible domain words |1,159,828|
| Word/clock evaluations |1,609,124|
| Complete margin-pair table entries |28,455|
| Compatible atlas candidates |21,313,204|
| Exact located-profile evaluations after safe pruning |4,958,975|

All549 selected upper clocks have a strictly positive minimum-tree margin.
The minimum is93 at clock12672, attained by
`d=(2,8,9,9,36,48,64)`, with excess108 and tree credit201.
Literal deletion from the pinned necessary array leaves exactly7,646 clocks,
maximum11,995. Its canonical JSON hash is

```
8ffc6d14b3883cf7e02c3ab02ddca5339d909a8051411def9096dee83b0aaed7
```

At7200 the complete76,814-word domain has exactly15 survivors. The worst is
`(4,9,9,16,18,24,32)`, with excess74 and minimum-tree credit0.
This is a surviving **compatible-cost relaxation**, not an actual unsafe
thirteen-speed row. It precisely marks the information discarded by
independent ratio minimization; no joint valuation or common physical
realization of the minimizing edges is asserted.

## 4. The actual consumer and promotion basis

For a primitive row of thirteen distinct positive speeds, choose any six
labels `A`, with seven-label complement `B`. Put `t=gcd(A)`, `g=gcd(B)`.
Primitiveness gives `gcd(t,g)=1`; the strict graph on `B/g` is the same graph
of primitive pair ratios as on `B`. Under hypothetical weak-safety failure,
the inherited profile supplier and continuing7 scale reduction apply.
Multiplication by `g` permutes the `t` lifts of a supplied six-body safe phase.
The positive lower bound above gives a lift weak-safe for all seven tails,
while those six original clearances are preserved.

Thus the accepted consequence requires a connected actual strict graph on
the complementary seven labels. It requires neither decoder-span equality,
a speed-height box, nor connectedness of the selected six-label graph.
The proper-six safe-phase input is the **CITED** supplier already used in the
inherited grid theorem; this referee does not strengthen that supplier.

Promotion is justified by the analytic proof, the exact pinned inherited
inputs, and the full independent finite reconstruction. There is no sampled
clock argument and no claim that a retained clock is itself a counterexample.
The general LRC(14) problem remains open.

## 5. Reproduction and frozen lineage

Keep the `.py` and `.cpp` companions adjacent. The Python wrapper uses only
the standard library and a C++17 compiler; it compiles with `-O2` normally
and `-O3 -DNDEBUG` under Python `-O`. All gates remain active in both modes.
Native scratch word lists and inputs are placed under the system temporary
directory, keeping repository files unchanged. Filed defaults resolve both
inherited inputs and the producer certificate from the repository layout;
the outside defaults resolve the original research directories. Explicit
`--root`, `--producer`, and `--work-dir` overrides are available.

```
python continuing8_20260906_lrc_minimum_tree_audit.py
python -O continuing8_20260906_lrc_minimum_tree_audit.py
```

The `--scout` development option compares the historical exploratory records;
it is not the frozen final reproduction, which requires the pinned complete
producer certificate.

Producer pins before audit promotion:

```
source      28217cfdb76a45c27db0b3ce7e8976141447fd9d2beff4fe9faba82709b198bd
report      ac5529a0c7dae2f43f1666304d21494963b5e65541b7fb827450ca292add18b1
certificate 580a7c930103aab3bea867ad463a90b0e0208323a90ee95a685ff811a761582d
```

Final independent replay: **6,758,545 active gates per mode**, comprising
6,749,775 native gates and8,770 Python input/completeness gates. Normal and
optimized outputs are byte-identical,752 bytes, with no carriage returns.
Both source files are also actual LF. Frozen pins:

```
Python 8b7ae371492f42a62fb3f8fb8fba8bcd904b46dc9c0191ff6c4d90d3663cf719
C++    31a4f7160b1bf171c43a279b44072767440583901468b7c821b98ea44aab0daf
output 54a76256b0b664bbe9f58a483dd4205ffe115fd560bb98b3cd6d26cd3db07a17
```

The two source files plus this report, the normal output, and the identical
optimized output suffice for filing. Native prepared inputs, executables,
progress files, and forty-megabyte word-list scratch banks are reproducible
temporary artifacts and are not proof dependencies or additional files to
file. No mathematical producer or exploratory cache is executed in final mode.
