# Independent referee: three clock domains and their full residue tables

**Verdict: PASS, analytically and by a complete independent exact computation.** No producer correction is requested. The accepted claims are the three whole-clock exclusions14886,14880,14874; every seven-residue conditional cost table on their specified divisor domains; and the selected-edge exclusion(e,p,q)=(6,1,355) at every t>=14868. The last claim does not exclude all edges at14868 or at another untested clock. LRC14 remains OPEN.

This review concerns `continuing7_20260906_lrc_residue_domains.md` and its frozen source/output/certificate. It accepts the inherited proper-six supplier as **CITED**, the full projected profile tables and global residue maxima as proved exact inputs, and does not infer actual unsafe rows from maximizing arithmetic words.

## 1. Independent proof review

The inherited42 states are all units modulo7 and have least common multipleLambda=5388292800. Therefore D(t), the allowed states dividing t, is exactly the set dividing gcd(t,Lambda). The complete positional profile predicate and the global gcd condition defining a seven-state word depend only on this domain. At a fixed residue tau the ceiling identity

    7 E_tau(d) = sum_i d_i ((-tau d_i^(-1)) mod7)

is exact: it follows separately from each integer ceiling, with seven copies of t cancelling. The divisors are units modulo7, and seven congruent summands make the right side divisible by7. Repeated states remain separate positions. Thus both unrestricted and pair-conditioned maxima depend only on the domain and the residue; a repeated forced pair requires two copies. Domain inclusion is monotone at the same residue, since it only enlarges the set of admissible words. None of these statements supplies a converse physical realization.

The generic identity and the global residue cap270 are already explicit in `third_20260906_grid_full_words.md`, Section3. Their provenance is accurately credited. The new reusable finite information is the complete restriction to domains18,480,6, including every conditional type, every null entry, and an attaining owner for each nonempty type. The same-residue hostile is valid: the domain6 cap at residue5 is15, while the domain480 word(1,3,4,6,32,48,60) has cost95. A residue-only transport would discard a necessary coordinate.

Each actual supplied edge has reduced coprime ratio p<q from the full strict atlas and e=gcd(t,D)<=6. Writing D=e h and n=t/e gives gcd(h,n)=1. Consequently its two margins are exactly e gcd(n,p),e gcd(n,q), with gcd equal to e; the unit h cannot be silently removed from the physical phase, but it does only permute the reduced grid and change its common translate. The source considers all e<=6 dividing the clock, including e5 at14880, and all5855 ratios. No preferred edge is substituted for that universe.

For a connected open overlap component of lengthell, every translated n-grid contains at leastceil(n ell)-1 points. Summing this valid lower bound is the separate credit. The complete enumeration shows that every noncritical edge exceeds even the global cost at its clock; the stronger conditional-first accounting preserves which full word can own each forced pair. The three critical cases have normalized n2481,2480,2479 and exact credit300. At14880 the q margin is30, rather than6, because gcd(2480,355)=5. The report retains that distinction and does not pretend conditioning is necessary for these particular three exclusions: their global maxima44,95,20 are already below300.

The inherited missing-interval proof applies uniformly to q355,n2485-s,1<=s<=7: here R25 and s*351<=2457<2485. Its closed absence intervals are strictly separated, so exactly one tooth can be absent, giving minimum50 among51 teeth. Endpoint inclusion is essential; the inherited actual-atlas hostile q43,s7 has minimum5 at the touching phase, rather than6.

At exact resonance n2485 each tooth has normalized length1, and all its endpoint phases coincide. The pair count really is zero atalpha1/2. It is correctly excluded by the different coordinate6 gcd(2485,355)=2130, which is absent from the entire allowed state set. This is a necessary-profile exclusion of a purported supplied edge, not a spurious positive geometric count. Above resonance, n>=2486 makes every one of the51 separate tooth counts at least1, hence credit at least306. The inherited global bound270 closes this whole selected-edge range. The proof covers all t>=14868 for which e6 could be supplied; when6 does not divide t such a sheet gcd cannot occur.

Finally the physical transfer preserves its inherited quantifiers. In a primitive thirteen-speed row, any chosen six-label set A has t=gcd(A), while its seven-label complement B has g=gcd(B), and gcd(t,g)=1. Under hypothetical failure the complete hereditary profiles hold. Connectedness of the complement's strict graph supplies some tested edge. A proper-six safe phase has t lifts preserving A safety, and multiplication by g permutes the translated t-grid in the U=B/g clock. At a point of danger multiplicitym, one selected pair indicator is at most(m-1)_+, so creditC and total marginal count at mostt+E leave at leastC-E weak-safe points. All required surpluses are positive. This proves the stated weak full-row safety implication without decoder-span equality, a height box, or connectivity of A.

## 2. Independent complete computation

The referee imports no producer implementation. It reads only pinned data and compares outputs after reconstructing the mathematical objects.

- **Words:** recursive multiplicity-vector enumeration, followed by a bitmask subset-gcd recurrence, replaces the producer's positional subset-tuple checks. Every proper nonempty mask is tested, including multiplicities. All347016 multisets are covered; the18108 accepted words and their literal ceiling costs match entry by entry.
- **Residues:** for each domain, choose seven actual multiples of its lcm, one in each residue modulo7, and compute native ceilings at those clocks. This avoids the producer's inverse-mod-seven weight formula. All seven complete conditional tables, including nulls and lexicographic attaining owners, agree exactly. These are arithmetic cost controls, not additional whole-clock safety scans.
- **Ratios and geometry:** enumerate admissible prime-factor sums first and then all coprime splits. For each ratio, sort every raw danger boundary and use the literal midpoint predicate to reconstruct the open overlap arrangement. This differs from the producer's two-pointer interval-intersection and closed numerator formula. Every entry in the complete81970 edge/sheet candidate array agrees, including margins, separate credit, conditional maximum and decision.
- **Location:** use an enter/exit event sweep, with every wall and open-cell value independently counted by binary searches of the actual ordered rational grid inside the reconstructed intervals. This uses no component ceiling/floor point-count formula and no producer modular grid predicate. All stored phase/count pairs agree. Separate exact geometric controls cover every deficit1,...,7, exact resonance, and the strict/equality hostiles.
- **Lifts and inheritance:** construct the complete residue multiplicities of the physical unit map and compare the geometric counts. Read the full previously audited8198-clock set, verify its semantic pin, and delete exactly the three declared clocks. The new8195-element array agrees in full and has maximum14868; no adjacency assumption is used.

The independent complete domain results are:

| Clock / domain | Multisets | Valid words | Maxima for residues0,...,6 |
|---|---:|---:|---|
|14886 /18|792|221|0,33,30,12,44,27,24|
|14880 /480|346104|17826|0,94,88,68,116,95,92|
|14874 /6|120|61|0,6,12,12,14,15,20|

The complete candidate decisions are respectively23419/0/1,29572/5557/1,21571/1848/1, in the order separate/infeasible/located. Each normalized near-resonance profile has minimum50, maximum51 and102 walls. At exact resonance the profile instead has one wall, minimum0 and maximum51. Those different numbers are retained as the hostile to extending the strict location lemma through equality.

## 3. Frozen inputs and reproduction

Producer pins checked by the referee:

- Source:`1afc8c3438c9db2ad6bad53b771d84a9b6e46d4f6464722aa4ce240d73084e1b`.
- Output:`f7554bdb450c2f6a042c0caef9270eba4832934682d72b1c8cfaaff5db223858`.
- Certificate:`4764d3def5d24702465f702180bad715b14402e9c45ddcd72ae1968e676a8086`.
- Complete inherited profiles:`935f3f687b6d7c89cc099e536f536238fd753bcc4c1747906d213cef387ca93f`.
- Old clock-array semantics:`c146d73c149c448e744e138aa3bb6f8c286748be81353b8a6605adc47a129117`.
- New clock-array semantics:`c67f5e98eff3fe406a4416057c6063095290330a50f039e731ced0fc2ca4657a`.

Both `python continuing7_20260906_lrc_residue_domains_audit.py` and `python -O continuing7_20260906_lrc_residue_domains_audit.py` pass **27,535 always-active independent gates**, with byte-identical raw LF output. The source searches adjacent inputs first, then the sibling results directory in the filed layout, with outside-worktree input fallbacks.

- Audit source SHA256:`453c49cb2086af157e5c92ea0b739d1564ecec0ab955f1409851b1183151e456`.
- Audit output SHA256:`f997a2fec5d35e60008627be30403c5ed87f45f2dc81604ffb7a773db087a148`.

The analytic and finite conclusions pass at the declared scopes. Full LRC14 closure, whole-clock14868 closure, sharpness for actual physical failures, and an unrestricted new gluing theorem are not conclusions of this audit.
