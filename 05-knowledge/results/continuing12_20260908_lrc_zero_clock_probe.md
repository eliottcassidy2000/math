# Divisible-by-seven clocks: one closure and the first complete zero-credit residual

**Status: FINITE-EXACT under the inherited proved minimum-tree consumer;
INDEPENDENTLY AUDITED.** The declared experiment closes clock6930 and
stops at the complete six-word residual for clock7560. That residual is
closed by the separate [depth-wedge theorem](continuing12_20260908_lrc_depth_wedge.md).
The raw minimum-tree certificate remains unchanged as its input provenance.

The immediate proved suppliers are
[continuing11 composite clocks](continuing11_20260908_lrc_composite_clocks.md)
and the full consumer in
[continuing8 minimum tree](continuing8_20260906_lrc_minimum_tree.md).
Their physical scope is retained: a primitive thirteen-speed row, a
selected six-label gcd clock t, and a connected actual strict-atlas graph
on the complementary seven labels. The full126 inherited proper-subset
profiles and all42 sheet states remain necessary conditions under failure.
LRC(14) and disconnected-complement entry remain OPEN.

The starting array has7622 clocks, maximum11935, canonical JSON SHA256

    832714c179ab4f425a76172c41d7d8eecf05dc15adfdd3c5e08bc7eb5a13e48b.

The predeclared ordered list is `[6930,7560,9240,11088]`. All four
belong to that array. The stopping rule is to stop after the first
complete minimum-tree residual, or after all four. The first two were
tested, and the experiment stopped at7560; 9240 and11088 were not tested.

This is a structural choice of clocks: all are divisible by7, while the
inherited sheet alphabet is coprime to7. Hence every allowed margin d
divides t/7 and

    E_t(d_1,...,d_7)=sum_i d_i ceil(t/(7d_i))-t=0.

At such clocks the actual overlap consumer has no positive marginal
excess to overcome. Under a hypothetical unsafe translated grid, every
actual pair intersection must vanish. This turns the subsequent problem
into zero-edge topology and compatibility, rather than a small numerical
improvement to a positive budget.

Every sorted seven-multiset in the complete divisor alphabet is tested
against total gcd one and all126 proper-subset profiles. The producer
uses only inherited necessary partial profile-completion pruning, never
partial minimum-tree pruning. It reconstructs all5855 strict coprime
ratios, computes their compatible pair weights with the actual quotient
clock, and takes positional minimum trees. Walls, open chambers and
coincident endpoint defects are retained in the exact phase sweep.

| Clock | Divisor states | Full valid words | Minimum M-E | Residuals |
|---:|---:|---:|---:|---:|
|6930|16|5341|178|0|
|7560|23|37209|0|6|

There are42550 complete word/clock evaluations. The worst6930 owner is

    (2,5,6,9,18,22,30), E=0, M=178.

Its displayed minimum tree has four zero edges and positive connections
of88 and90. Thus a bound using only the smallest edge weight, which is
zero, would miss the closure.

The exact7560 residual bank is

    (4,4,4,9,9,18,24), (4,4,4,9,18,24,27),
    (4,4,8,9,9,18,24), (4,4,8,9,18,24,27),
    (4,8,8,9,9,18,24), (4,8,8,9,18,24,27).

All six have E=M=0. They are projected survivors, not actual unsafe
rows. Their common forced graph structure is the next theorem's input.
No claimed realization of an independently minimized ratio tree is used.

The closest hostile is the actual zero-credit tree at7200, whose omitted
endpoint mattered. The corrected near miss is a graph on the whole
divisor alphabet: the earlier10080 zero-edge graph has a gcd-one
connected component, even though no admissible seven-label word survives.
Consequently the number of available labels and their inherited profiles
must remain part of the graph criterion. Replacing that seven-label
problem by full-alphabet connectivity is unsound.

The live board is: the zero-budget clock family; full126 profiles;
strict zero-credit edges; mandatory actual cuts; clipped sheet margins;
and the untruncated valuation restored by a native ratio.

The raw probe removes only6930, giving7621 necessary clocks with the
same maximum11935 and hash

    567d76d5b776e805a0606298399e7ecedc14657c6910f03321ca7e64e6c4c092.

The separate depth-wedge certificate then removes7560. Keeping both
arrays avoids silently reinterpreting a minimum-tree residual as a
minimum-tree closure.

Run `04-computation/continuing12_20260908_lrc_zero_clock_probe.py`
normally and with `-O`. Before installation, use the repository as the
working directory. The standalone producer imports no mathematical
producer. It pins its full profile and necessary-array inputs and records
every pair table, whole valid word bank, domain hash, minimum owner and
residual list. Its optional small-domain unpruned control is not activated
by these larger domains; the independent audit must separately verify
completeness. Both modes pass38113 always-active exact gates with matching
raw LF output and regenerated certificate bytes.

## Independent acceptance

The [independent audit](continuing12_20260908_lrc_independent_audit.md) accepts
this scoped result and its analytic proof, with 91203 exact gates per
normal and optimized pass. Its producer-independent controls retain the
declared positive and hostile boundaries. The parent replays both modes
against frozen raw LF outputs and certificates.
