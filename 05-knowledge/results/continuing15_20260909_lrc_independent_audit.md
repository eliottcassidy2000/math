# Independent referee: complete mandatory-wedge survey and its physical boundary

**Status: INDEPENDENTLY ACCEPTED, analytically and by a complete finite replay.**
The frozen [primary report](continuing15_20260909_lrc_mandatory_wedges.md)
requires no mathematical repair. Its three clock exclusions are exactly
**5880, 6804, and 7056**. The inherited necessary array now has **6,886**
entries, maximum **11,934**; its zero-budget part has **353** entries,
maximum **6,552**. General LRC(14) remains OPEN.

The entry is a primitive row of thirteen distinct positive speeds, a selected
six-label subset with gcd clock t, and an **actual connected strict graph on
the complementary seven labels**. No connectivity of the selected six is
assumed. These are exclusions in this inherited entry scope. Remaining clocks
and remaining ratio products are necessary candidates, not unsafe examples.

## 1. Target, inheritance, and independence

The reviewed primary report SHA256 is

    0fb26e30697f69c4ce7346a0e21da8bf1ff15cd6aea4b7a706ad532e9c1bd7a8.

The executable pins that report and all four other frozen primary artifacts.
The primary's pending-referee header records its preacceptance state; this
referee supplies the acceptance without changing the frozen primary bytes.

The mathematical suppliers are the complete native chamber-zero and slot
classification in [continuing14](continuing14_20260908_lrc_zero_classification.md)
and its [independent referee](continuing14_20260908_lrc_independent_audit.md),
together with THM-4106 /
`01-canon/theorems/THM-4106-lrc-pair-owner-reciprocity-and-valuation-tree-decoder.md`.
The audited native geometry and full-profile mechanism are inherited;
the bridge consumer, complete bounded survey, composite endpoint banks, and
5376 physical control are the present claim.

The exact continuing14 supplier certificate is pinned at
`863d33accf19a5515b3ecd2048aac35d77f30f4e004371c73f5760d345bbb240`;
its separate referee certificate is pinned at
`0d2572099bc7d314ee8fc50ffac32ba1789f4361586a9ed174726430a7631f4f`.
The full inherited profiles have raw SHA256
`935f3f687b6d7c89cc099e536f536238fd753bcc4c1747906d213cef387ca93f`.
The input array is all 6,889 entries with semantic SHA256
`b603ca0186c1ebaa2318782348a7446a16f529543147eb53062f1ba360e8e57b`.

The referee imports or executes **no mathematical producer**. It independently
regenerates every requested whole word bank from the pinned profiles; the
full ratio-count table is an explicitly inherited, independently audited
supplier. Every native bank consumed by the new arithmetic is regenerated
without a positive-bound shortcut. Its C++ geometry is a self-contained
adaptation of the prior independent referee, not the current primary.

The canonical 7200 synchronized zero tree still prevents replacing native
zero edges by coverage. The continuing12 depth wedge prevents replacing a
common actual speed by separately compatible arms. The present forced-bridge
and off-atlas endpoint coordinates pay those two distinct losses, while the
new 5376 physical tree exposes the remaining coverage loss.

## 2. Analytic audit of the consumer

All 356 clocks under review are divisible by seven. Every allowed margin d
is prime to seven and divides t, hence 7d divides t. Each individual danger
set on the translated t-grid has at most t/7 points. A seven-tail unsafe
cover therefore forces all seven marginal counts to attain t/7 and every
pair intersection to vanish. At an individual strict endpoint a marginal
loses d points. This proves the native chamber restriction; it does not
replace open dangers by closed ones.

The possible zero graph has seven **position vertices**, including distinct
positions with the same margin. Each actual strict edge of a hypothetical
unsafe row belongs to this graph. By the entry hypothesis the actual graph
is connected. Deleting any bridge of the possible graph disconnects every
spanning subgraph that omits that bridge. Consequently every possible-graph
bridge is an actual edge. Two incident bridges really do force one actual
three-speed wedge. No such implication holds for an arbitrary nonbridge.
The independent census finds bridges by literal edge deletion and reachability,
and separately verifies the primary's retained position owners.

For margins (a,b,c), put g=gcd(a,b,c), T=t/g, and
(A,B,C)=(a/g,b/g,c/g). Let r=u_a/u_b and s=u_c/u_b range over the
complete oriented native banks. Propagating (r,1,s) gives a primitive positive
integer triple v. Every realizing triple has the form H v, so

    gcd(t,H)=gcd(gcd(t,Hv_1),gcd(t,Hv_2),gcd(t,Hv_3))=g.

Writing H=gH' gives gcd(T,H')=1. Hence the required margins occur if and only if

    (gcd(T,v_1),gcd(T,v_2),gcd(T,v_3))=(A,B,C).

Necessity follows from the displayed coprimality; sufficiency uses H=g.
This is a complete local scale criterion for the three labelled integers,
including all clipped prime depths and arbitrary common triple gcd. It is
not a global seven-speed realization theorem. Coincident primitive entries
are separately rejected because the original speeds must be distinct.

If the local margins and distinctness pass, the endpoint ratio is exactly
r/s. For its coprime numerator and denominator p,q, the actual pair sheet is
e=gcd(a,c), with actual quotient n=t/e. Multiplication by the actual common
speed factor maps the original grid onto this quotient grid with multiplicity
e. A positive **all-phase** minimum m for the p,q intersection therefore
forces at least em overlapping original points, contradicting the pairwise
disjointness already proved. This argument applies when p:q is outside the
strict atlas. The composite pair does not need to be an actual strict edge.

Dividing t,a,b,c by g preserves both arm quotient clocks and oriented margin
roles exactly. Reusing a normalized signature therefore preserves this
predicate. It is not a comparison with a nearby clock or an asymptotic scale.
Conversely a zero minimum is retained as a residual; it need not share a
phase with either arm minimum, or with any other pair in a seven-speed row.

## 3. Complete independent finite path

The referee reconstructs the complete 126 proper-subset profile test and
enumerates seven-multisets without partial-word pruning. Each of the 121
accepted domain banks agrees byte for byte with the pinned inherited bank.
It rebuilds full position graphs and the distinct-speed slot filter, then
uses edge deletion to recover both entire mandatory-wedge catalogues.

| Recomputed universe | Count |
|---|---:|
| Full inherited zero-budget clocks | 356 |
| Distinct complete divisor alphabets | 121 |
| Unpruned seven-multisets | 7,105,596 |
| Accepted distinct-domain words | 291,811 |
| Complete word-clock evaluations | 353,571 |
| Surviving clock-labelled words | 47,568 |
| Singleton-eligible words | 1,533 |
| Singleton clock-labelled / normalized signatures | 94 / 53 |
| Bounded clock-labelled / normalized signatures | 561 / 304 |
| Bounded-eligible words / clocks | 5,424 / 77 |

The strict atlas is rebuilt multiplicatively from its permitted inert prime
powers, giving all 5,855 coprime unordered ratios. For each requested arm,
the referee constructs pair intersections from raw individual walls and
exact midpoint tests. This differs from the primary interval intersection
algorithm. A signed event sweep retains strict-wall and open-chamber minima
separately. Seventy-two ratio/clock controls compare the sweep against direct
evaluation at **every** wall and chamber and then count a minimizing actual
grid literally.

All 91 requested oriented arm banks are rebuilt from **57,438** compatible
native atlas pairs, without skips. Every one of their 2,346 oriented zero
fractions is compared, not merely their counts. Every chamber-zero pair has
literal attained marginals and zero intersection at its recorded common
quotient phase. The 64 evaluated signatures contain all **3,055** Cartesian
products. Their 2,408 distinct admissible composite endpoint capacities are
computed independently, including pairs outside the atlas, and both reported
minimizers are checked by literal quotient-grid enumeration. All signed
interval products fit the checked 64-bit range; speed multiplication uses
128-bit integers.

| Exact product outcome | Count |
|---|---:|
| Positive all-phase endpoint minimum | 2,158 |
| Incompatible common primitive depths | 630 |
| Duplicate speed values | 9 |
| Retained arithmetic residual | 258 |

All complete product records match the primary: both input fractions,
primitive triples, actual margins, endpoint fractions, actual quotient clocks,
and **both** capacities. There are no chamber-only composite positives in
this declared arithmetic universe. Exactly 37 normalized signatures close.

In particular the seven 7056 endpoint products have quotient3528 minima
**(all-phase55, chamber56)**. The original sheet is2, so the uniform forced
overlap is110. Keeping the two minima distinct pays strict walls. Both
products at (5880;4,20,5) fail common depths. At the first bounded continuation
clock6804, all171 products at (2268;4,6,9) have positive all-phase minima;
the other signature's43 residuals are retained.

## 4. The declared stop and full physical hostile

The entire singleton arithmetic bank has 53 keys and 1,980 products; 33 keys
close. Its complete clock exclusions are5880 and7056. Independently deriving
newly wholly bounded-eligible clocks yields precisely the declared descending
order **[6804,5376,5124]**. All bounded signatures at6804 are evaluated and
that clock closes. At5376 one word survives, **(3,3,6,8,12,16,16)**, so the
policy stops. The5124 key is absent from the evaluated arithmetic union;
its cheap topological eligibility is not an arithmetic computation.

Applying every one of the37 proved signatures throughout the complete
eligible topology catalogue removes exactly1,119 original words. The referee
compares every remaining clock bank hash and every retained first owner,
checks every removed original word and its correctly oriented proved wedge,
and confirms that only5880,6804,7056 have no word left. No partial deletion
is promoted to a clock exclusion.

The5376 physical control is independently checked on all5,376 original grid
points at phase1/2. Its seven speeds are distinct and primitive with the
reported gcd margins. All21 actual reduced pair ratios are computed. The
entire actual strict graph is exactly the six claimed edges and is a
connected tree. Every actual strict edge has zero intersection at this
**single common phase**, and every marginal has768 points. Nevertheless
exactly **1,600 grid points are safe**. All seven complete danger-set hashes
and all omitted non-atlas pair intersections match the primary.

This is a seven-tail safe-grid hostile, not a thirteen-speed failure, an
unsafe cover, a selected-six phase realization, or an exclusion of5376.
It refutes the sufficiency of synchronized native-tree edges for coverage.
The complete topology census, the64-key arithmetic bank, and this one physical
phase are three different finite scopes and remain distinguished.

## 5. Reproduction and frozen verification

The [Python referee](../../04-computation/continuing15_20260909_lrc_independent_audit.py)
compiles its self-contained
[C++ companion](../../04-computation/continuing15_20260909_lrc_independent_audit.cpp)
in a private temporary directory. It imports no producer and regenerates
all finite outputs. Once filed, run from the repository root:

    python 04-computation/continuing15_20260909_lrc_independent_audit.py
    python -O 04-computation/continuing15_20260909_lrc_independent_audit.py

Outside the repository the same source accepts explicit `--root` and
`--producer` directories. The certificate is written beside an outside source,
or to `05-knowledge/results` when the source is filed in `04-computation`.
The normal build uses native `-O2`; Python `-O` uses native `-O3 -DNDEBUG`.
Every gate raises explicitly in both modes. Both modes pass **179,124 gates**:
101,285 Python gates and 77,839 native gates. Their stdout is raw LF and is
byte-identical; their full certificate bytes are also identical. The frozen
[transcript](continuing15_20260909_lrc_independent_audit.out) and
[certificate](continuing15_20260909_lrc_independent_audit_certificate.json)
record exact gate totals and byte-identical normal/optimized replay results.

The final necessary array semantic SHA256 is

    acbcbd2e63f01572e3af4789b058ce71a557bcc6b8317be8af8b21b1ff45f2cd.

The new proved mechanism is a sufficient actual-wedge obstruction. Neither
the arithmetic decoder nor the entire possible zero graph is claimed to
characterize realizable or unsafe original rows.
