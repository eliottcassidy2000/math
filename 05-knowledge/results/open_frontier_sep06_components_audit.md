# Independent audit of the full component-prefix envelope

**Status: INDEPENDENT ANALYTIC AND SOURCE AUDIT — PASS.**
Referee: `three_ray_geometry`. Target:
[open_frontier_sep06_components.md](open_frontier_sep06_components.md).
No target-source edits, theorem reservation or Git operations.

I read the complete target proof and source, and recovered the proved
[inert Pareto envelope](creative_20260906_inert_pareto.md) to verify its
exact arithmetic hypotheses, strict infinite-tail mass bound and literal
component-width bound. The target correctly treats these as dependencies,
not as conclusions of its separate 46-pair head.

The analytic checks passed:

- For `q>67`, the width and total mass give every prefix bound by ten
  equal lengths `2/469`. The complete head and its five attained vectors
  therefore establish the full all-height envelope.
- Compact containment in open intervals forces every sorted prefix
  inequality to be strict. Selected closed components may share an open
  carrier, but their grouped total still leaves positive slack. Isolated
  points do not join components; indices beyond the number of positive
  components retain strictness when the total length is positive.
- The physical pullback needs the stated odd common scale. The replicated
  prefix formula is the linear interpolation of adjacent original prefix
  sums, so it preserves weak majorization. The inclusive gate is a valid
  sufficient contradiction to strict failure, with no converse asserted.
- The hinge representation gives square-energy domination after
  multiplication by two and integration. Compact/open packing gives the
  strict version used by the inclusive energy gate. The sharp primitive
  carrier ceiling is attained by `(7,13)`.
- The six-arc reflected control proves a gain in the declared compact-set
  domain. The recovered eleven-body is correctly kept separate and is
  already covered by the prior joint criterion; no new actual-body family
  or LRC(14) closure is inferred.

The source checks also passed. I inspected the primitive/inert filter,
both independent component constructors, strict endpoints, complete finite
prefix comparisons, maximal-vector test, dilation arithmetic, compact
packing assignments, literal body geometry and arithmetic hostiles. The
normal and optimized runs independently reproduced the saved output byte
for byte: **1,659 explicit gates**, including 1,296 packing assignments.

```text
source SHA256 132b21448bb1d8f4fb12e9f009b834f3b9eb6a49e1901399e79db41d16bb486b
output SHA256 5f04e3d6d846d30df99d96763471b7a12815ff5f4e95780255daadd0c560e710
semantic SHA256 40fbb1b572251f3362c24adb2344cd472ff534ff64bf6f593a0219e4380be89b
```

I inspected the optional body-template generator and its stated count
`528+61+10010=10599`, but did not independently rerun that optional search.
Its no-gain report is not a dependency of the analytic envelope or entry
theorems. No mathematical correction was required.
