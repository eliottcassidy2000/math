# The two remaining declared composite clocks close in the complete consumer

**Status: PROVED inherited consumer + FINITE-EXACT complete selected domains;
INDEPENDENTLY AUDITED.** The predeclared clocks9240 and11088 both close
in the actual connected-complement entry reduction. The necessary array
decreases from7620 to7618 clocks, with maximum11935 unchanged. General
LRC(14) and disconnected-complement entry remain OPEN.

## 1. Inheritance and exact universe

This continues the declared clock order in
`continuing12_20260908_lrc_zero_clock_probe.md` and its audited
`continuing12_20260908_lrc_depth_wedge.md`. That session stopped at7560,
then closed its complete six-word residual by restoring the absolute
2-adic depth at a forced common middle vertex. Its two later declared
clocks were not tested there. The present ordered universe is exactly

    [9240,11088].

The inherited array has7620 entries, maximum11935, semantic SHA256

    66f903430fd804074e6c8adb330aca4f6bb6b8f97d955e014a73309c39d17a8d.

The supplier is
`continuing12_20260908_lrc_depth_wedge_certificate.json`; no other clock
is added to this task or deleted by analogy. The complete126 profile
supplier is
`04-computation/overnight12_20260906_lrc_decoder_descent_inherited_profiles.json`,
raw SHA256

    935f3f687b6d7c89cc099e536f536238fd753bcc4c1747906d213cef387ca93f.

The inherited minimum-tree theorem is
`continuing8_20260906_lrc_minimum_tree.md`, with
THM-2060 / `crt-tail-coset-saturation` and
THM-2064 / `multitail-sheet-capacity-and-dyadic-seam`. The native strict
ratio atlas is THM-3818 /
`scaled-inert-cubeclass-support-two-pair-packet`: all5855 coprime p<q,
p+q<=356, with every prime factor of p+q equal to2 modulo3 and of
exponent at most two. The associated theorem files are under
`01-canon/theorems/` with those exact IDs and slugs.

The hostile is the old7200 actual zero-credit tree: possible-edge
statistics did not eliminate that native object, and its endpoint
geometry mattered. The corrected near miss would reuse its quotient
weights at new clocks. Here every weight is recomputed with the actual
quotient t/gcd(d_i,d_j). The least-used sidecar is the zero-component
cut, which becomes decisive before a depth or endpoint obstruction is
needed. The companion `continuing13_20260908_lrc_zero_cuts.md` retains
all four hostiles to an isolated-vertex-only shortcut.

## 2. Complete profile domains and minimum-tree consumption

For each actual tail label write d_i=gcd(t,u_i). The domain consists of
all nondecreasing seven-tuples from the complete permitted sheet alphabet
dividing t, with total gcd1 and every one of the126 inherited nonempty
proper-subset profile conditions. Repeated margins remain separate
positional vertices. No subset of those profile tests is substituted.

The source uses only the inherited sound partial-profile-completion
pruning while constructing complete words. It does not use partial-MST
pruning. Its optional unpruned Python control applies only to alphabets
of at most nine states and therefore does not activate at the present
21- and20-state domains. An independent unpruned census is an explicit
referee obligation, rather than an unperformed check claimed here.

For margins a,b put e=gcd(a,b). Every native compatible strict ratio
satisfies

    {e*gcd(t/e,p),e*gcd(t/e,q)}={a,b}.

The pair weight is the minimum, over the complete compatible atlas, of
e times the literal all-phase intersection minimum on the quotient grid
of clock t/e. The geometry retains strict walls, open chambers and the
circle wrap. Exact lower bounds may skip an expensive capacity evaluation
only when they cannot improve the incumbent minimum; all compatible
ratios are still considered. The separate zero-cut verifier reevaluates
every compatible ratio without this optimization and agrees on every
pair minimum.

Both clocks are divisible by7, and every permitted margin d divides
t/7. Thus the exact marginal excess E=sum d*ceil(t/(7d))-t is zero
throughout each whole domain. The coarse uniform edge bound is also
zero, so that cheap bound alone cannot close either clock.

The complete possible-edge **minimum** spanning-tree consumer gives:

| Clock | States | Complete words | Compatible native ratios | Minimum M-E | Residuals |
|---|---:|---:|---:|---:|---:|
|9240|21|22469|53195|108|0|
|11088|20|18780|48641|156|0|

The minimum owner at9240 is (3,5,8,8,12,24,30), with zero budget and
tree credit108. Its displayed Kruskal tree has five zero edges and one
3--12 edge of credit108. The minimum owner at11088 is
(2,8,8,9,9,36,48), with zero budget and tree credit156; its displayed
tree has five zero edges and one2--8 edge of credit156. These are
complete-domain extrema of a sufficient statistic, not claims that the
selected possible trees or minimizing ratios are jointly realizable.

Any actual connected strict graph contains a spanning tree. Its actual
edge credits dominate the corresponding possible-edge minima, so its
tree credit dominates the complete possible-graph minimum M. The
inherited forest inequality gives a weak-safe grid point because M>E.
If a possible graph is disconnected, that instead contradicts the
assumed actual connectivity. The implementation's large sentinel for an
absent compatible edge is not a physical intersection count.

## 3. Exact entry consequence and stopping state

Suppose a primitive thirteen-speed row has a six-label subset A with
gcd(A) equal to9240 or11088, and the actual strict graph on the seven
complementary labels is connected. Under hypothetical failure the
inherited full profiles put its margin word in the appropriate complete
domain above. A weak-safe proper-six phase lifts along the clock grid,
and the forest consumer supplies a lift also weak-safe for all seven
tails. This contradicts failure in the inherited scope.

Only9240 and11088 are deleted. The final array has7618 clocks and
maximum11935, semantic SHA256

    89a3e7545cc77467c5f85fbe0bdaab1f71071cff65184bbc1212eb86c9f07177.

There is no complete residual in either declared clock. Consequently no
native-depth, endpoint or joint-phase search is required; the bounded
task stops after the second declared clock. The zero-cut companion
explains the mechanism on every word and retains the failure boundary
of a tempting simpler isolation claim. No larger unexamined clock family
is asserted to close.

## 4. Frozen reproduction

The source passes39029 always-active exact gates in each normal and -O
mode. Raw-LF stdout and certificate bytes are identical. It stores the
complete word banks, all pair minima and native minimizing witnesses,
both owner trees, and the entire old-to-new array result.

    source 4850d8984350f22988c4bc1d25a7df958c88d8297f99efeb247fb90baf4b55e9
    output 84b25e47a2ec8c7cd1f4ceb77149da6a3da10b8bba1b7375a8cb43c82f709312
    cert   a56f8d8bdee84f642d7f837a6c9e6398008eadbc0318b0778d6d0e435c0b0201

After relocation:

    python -B 04-computation/continuing13_20260908_lrc_zero_clock_probe.py
    python -B -O 04-computation/continuing13_20260908_lrc_zero_clock_probe.py

Outside the repository, run with the repository as the current directory.
The source then writes its certificate beside itself; after installation
it writes to05-knowledge/results. Independent census and native audits
remain the publication promotion gate.

## Independent acceptance

The [independent audit](continuing13_20260908_lrc_independent_audit.md) accepts
this scoped theorem without mathematical repair. Its 249174 exact
gates pass in normal and optimized modes with identical raw LF transcripts
and certificates where applicable. The parent repeated those checks after
filing the frozen sources under the repository reproduction paths.
