# Compatible words and located overlaps exclude the entire clock16,704

**Status: PROVED ANALYTICALLY + FINITE-EXACT complete one-clock certificate; INDEPENDENTLY AUDITED.** Every candidate supplied at clock16,704 is excluded by combining its exact compatible-word cost with its located pair overlap. Deleting this one clock from the independently audited necessary set leaves **8,201 clocks, maximum14,904**. No other clock is newly tested. LRC(14) remains OPEN.

The physical conclusion retains the stronger general connected-complement scope. Let n be a primitive row of thirteen distinct positive integer speeds, choose any six-label subset A, and let B be its seven-label complement. Put t=gcd(A),g=gcd(B),V=A/t,U=B/g. Suppose the graph on U is connected when an edge means that its primitive positive ratio belongs to the strict5855-pair atlas. Then the row is weakly safe whenever t lies outside the new necessary set. In particular,

`gcd(A)>14904 implies a phase with clearance at least1/14`.

No decoder-span equality, physical height box, or connectivity of the chosen six labels is required. This is weak safety, not a claim of strict clearance on every supplied grid lift.

## 1. Inheritance and the two repairs that must coexist

The proved inputs are:

- `third_20260906_grid.md` and its audit: exact marginal danger ceilings and translated-grid overlap transfer. Its proper six-component LRC phase supplier remains **CITED**.
- `third_20260906_decoder_profile_graph.md`: full inherited projected words plus connectedness supply some actual edge with sheet gcd e<=6. It does not prescribe the edge or bound every edge.
- `third_20260906_grid_full_words.md` and `third_20260906_grid_refined.md`: the exact projected full-word domain and the audited necessary set of8,202 clocks, maximum16,704.
- THM-3818, `01-canon/theorems/THM-3818-scaled-inert-cubeclass-support-two-pair-packet.md`: the strict actual ratio atlas. Each prime in a reduced sum is2 modulo3 with exponent at most2, and the sum is at most356.
- The complete inherited profile JSON from `overnight12_20260906_lrc_gcd_semigroup.md`, pinned below, with its proved/cited supplier dependencies unchanged.

The immediate mechanism checkpoints are `continuing5_20260906_lrc_extremal_location.md` and `continuing5_20260906_lrc_clock16704.md`. At the displayed edge(3,308), separate credit172 improves to located credit336, beating the exact compatible cost188. At edge(23,323), location gives no gain180->180, but enforcing the same full-word owner reduces the cost188->134. Neither repair subsumes the other.

Concurrent incoming `long_frontier2_sep06_lrc.md`, read at origin/main2d3c53942, independently proves an adaptive two-pair located-overlap supplier and81 actual zero-individual-credit entries. The elementary common-translate mechanism is shared and is not claimed as a new general algorithm here. The distinct consumer is the complete one-clock combination of common phase and common compatible owner across every eligible atlas edge.

The board is: full words; forced margins; actual atlas edges; one common interval translate; and the lifted physical phase. The corrected near misses are independently maximizing costs before attaching an edge, or independently minimizing each interval count. The least-used sidecar is the pair-conditioned owner together with the common translate of that same pair.

## 2. Complete conditional costs at one clock

Fix t=16704. Let F_t be the incoming full-word domain of seven-entry state multisets d. Each d_i divides t and lies in the42 allowed core values. Their gcd is1. For every proper nonempty k-subset I of positions,

`(gcd(d_i:i in I), sort(gcd(gcd(d_i:i in I),d_j):j outside I))`

must belong to inherited profile level7-k. Repeated states are allowed. These projected words are necessary under hypothetical failure; they do not assert every actual physical profile or realize arbitrary seven-component ratios.

At this clock the allowed divisors are exactly

`1,2,3,4,6,8,9,12,16,18,24,29,32,36,48,58,64,72`.

There are binom(24,7)=346104 sorted seven-state multisets. The source enumerates every one without branch pruning, rejects global gcd or literal full-word failures, and obtains exactly19073 valid multisets. For each valid word it computes the native ceiling excess

`E(d)=sum_i d_i ceil(t/(7d_i))-t`.

For every unordered pair of entries present in the word, update its conditional maximum. A repeated margin is included only if two copies actually occur. Thus one complete enumeration computes every

`M_(a,b)(t)=max{E(d):d in F_t contains a,b with multiplicity}`.

All171 unordered margin types are recorded. Exactly161 have an owner; the remaining ten are infeasible. Every recorded maximum has a literal attaining full word, but attainment is not substituted for the exhaustive upper bound. The global maximum is188, independently recovered rather than imported from the older optimizer. The sorted valid-word sequence has compact-JSON SHA256

`e585b7c1aa6f5bfd55abc2d3caaf2b245789a9ace580d8abd46a629d0fa49eef`.

The complete table is retained in the JSON. It includes M_(4,4)=134 and M_(12,16)=188, preserving both crossed controls.

## 3. Complete edge universe and exact located supplier

There are5855 strict coprime atlas pairs(p,q), p<q. Every possible supplied e<=6 dividing t is retained:

`e in{1,2,3,4,6}`.

Hence there are29275 base candidates. For n=t/e, the two forced margins are

`d_p=e gcd(n,p), d_q=e gcd(n,q)`.

These are not freely chosen states. If the actual U pair is(Dp,Dq), e=gcd(t,D), then D/e is coprime to n, proving the displayed margins and the t-grid-to-n-grid map with multiplicity e.

For the open pair danger intersection I_(p,q), the exact component lengths have common denominator14pq and numerators2p once, and min(2p,p+q-14k) twice for1<=k<(p+q)/14. The containment clip is retained. The separate credit is

`C_sep=e sum_components(ceil(n*length)-1)`.

For a placed component(a,b), an exact translated n-grid count is

`ceil(n b-alpha)-floor(n a-alpha)-1`.

The located credit is therefore

`C_loc=e min_(alpha mod1) sum_components[ceil(n b-alpha)-floor(n a-alpha)-1]`.

This is at least C_sep. Literal integer arc intersections determine the positions. The only discontinuities are the endpoint residues n*a,n*b modulo1; every wall and every intervening open cell is evaluated with strict open-danger endpoints. This is a complete proof for all real translates, not numerical sampling.

The actual pair's common factor sends any translated t-grid onto a translated n-grid with multiplicity e. Its coprime unit part only changes the translate. Consequently C_loc is a uniform physical pair-overlap lower bound, irrespective of the unknown common factor beyond its sheet gcd.

## 4. Every candidate is excluded

The incoming filters are reconstructed independently, without importing their producer:

- 28060 base candidates have C_sep>188 and already close against the global full-word maximum;
- 227 have invalid forced margins; the complete conditional table confirms those margins occur in no full word;
- the old additional relaxed paired-cost test excludes no further candidate at this clock;
- 988 remain, exactly the pinned crossed-checkpoint candidate array.

For each remaining candidate use the exact upper cost M_(d_p,d_q), not the minimum of two independently maximized relaxations. Of the988:

- **835** satisfy C_sep>M_(d_p,d_q);
- the other **153** satisfy C_loc>M_(d_p,d_q).

There is no survivor. Every decision and every located profile is retained. The least surplus among these988 decisions is4. The least surplus among the153 located decisions is18, at

```
e=6, p=132, q=221, n=2784,
forced margins=(72,6), conditional maximum180,
C_sep=174, normalized located minimum33,
C_loc=198, surplus18.
```

This pair has51 open components and86 distinct projected walls; its maximum normalized count is68. The source independently checks every wall and every open cell by literal modular danger predicates at every grid point for this least-surplus control and for both crossed controls(3,308),(23,323). The complete153-profile proof uses the exact discontinuity partition for every candidate; the three literal paths are independent validation, not a substitute for that completeness.

The transcript and JSON account for every one of the29275 base candidates. No maximizing pair, numerical envelope minimizer, or first candidate is substituted for the full atlas.

## 5. Physical transfer and the general connected-complement corollary

First consider any primitive seven-shape U whose sheet word d_i=gcd(t,u_i) lies in F_t and whose strict atlas graph is connected. The inherited small-edge certificate supplies some actual edge with e<=6. Its ratio and forced margins are in the complete universe above. If a formal candidate is excluded as infeasible, it cannot be that actual edge. Otherwise its overlap credit C strictly exceeds its compatible total ceiling excess.

Each individual danger count is at most d_i ceil(t/(7d_i)). At a grid point with m dangerous labels the overcount of the sum relative to the union is(m-1)_+, which is at least the selected pair indicator. Thus the number of weak-safe points on every translated t-grid is at least

`C-E(d)>0`.

This proves the profile-conditioned grid supplier at t16704. No sum of unrelated pair credits is taken.

Now let n=A union B be the primitive thirteen-speed row in the opening statement. If it had no weak-safe phase, the inherited joint-shadow profiles apply globally. With t=gcd(A),g=gcd(B), primitivity gives gcd(t,g)=gcd(V)=gcd(U)=1. The projected sheet word belongs to F_t, and the connected U graph supplies the small edge. The **CITED** proper six-component LRC theorem supplies a V-safe phase eta. Its physical lifts(eta+j)/t preserve that clearance on A. In the U clock, multiplication by g permutes the t-grid because gcd(g,t)=1. The profile-conditioned supplier then gives a lift weak-safe for every label of B, contradicting hypothetical failure.

This argument uses neither W_(Q,3)=V_dec, a height bound, nor connectedness of A. It preserves the general connected-seven-complement scope already proved in `third_20260906_grid_refined.md`. In the original actual balanced decoder domain it specializes immediately, leaving every other entry obligation and g<=90 necessary bound unchanged.

## 6. Updating only the necessary set, and stopping

The complete old set S_refined is read from the independently audited output and pinned by compact-JSON SHA256

`4f29481d984ead40d0144556ce1c45dce210e30b964bb65835a7904ca6353e59`.

It has8202 members and maximum16704. Remove exactly the newly excluded clock:

`S_new=S_refined minus{16704}`.

The source verifies that S_new has **8201 members and maximum14904**; its full array is retained in the JSON, with SHA256

`ddbe0e091d36d54c8f6a7c8ea631bbf363d799b9adaa2ec4fe4cf56250d11a76`.

Under hypothetical failure in the declared connected-complement setting, t must lie in S_new. In particular any such chosen six-label gcd exceeding14904 is now weakly safe. This is not a finite enumeration of all physical shapes, a proof that the remaining clocks are realizable failures, or a universal LRC(14) solution.

The earlier prefix and crossed-boundary artifacts remain frozen as mechanism lineage. This complete certificate supersedes only their deliberate stopping scope for this one clock. No other clock was newly scanned, and no extension to the remaining8201 is asserted.

## 7. Reproduction and pins

```
python ../../04-computation/continuing5_20260906_lrc_clock16704_complete.py
python -O ../../04-computation/continuing5_20260906_lrc_clock16704_complete.py
```

Both runs pass30532 always-active exact gates, with byte-identical actual LF stdout and JSON. The capture rejects carriage returns instead of normalizing them. Filed layout reads adjacent profile data and the sibling results directory; the outside-worktree fallback is retained.

The JSON contains the full171-type conditional table, all owners, all988 candidates and decisions, all153 necessary located profiles, native controls, and the full new necessary clock array. The bounded complete full-word enumeration, not merely its owners, proves every maximum.

- Source SHA256:`2c1e624743c6357257558ed8b8f291667577aad02e36a567b71f4d3ee1eec05f`.
- Output SHA256:`33820ee3775baaedb0b8516b1a837b184b6ffdd279266614e479079f215e49fa`.
- JSON SHA256:`8ab0b9fe8791707ac780f32030103eeec71de6830613619c8333672563a15345`.
- Inherited full profile JSON:`935f3f687b6d7c89cc099e536f536238fd753bcc4c1747906d213cef387ca93f`.
- Current988-candidate semantic pin:`8610d17932b51afbaaeec4699f5a21e2362441f58b9c28235c48182106d45bc1`.

No producer implementation is imported. No repository or Git mutation was performed by this producer. The independent full-clock audit passed before status promotion; parent owns integration and the maintained frontier.

Independent [proof and exact referee](continuing5_20260906_lrc_clock16704_complete_audit.md) passes.

The [raw-byte checkpoint manifest](continuing5_20260906_manifest.json) pins
the filed source, report and identical normal/optimized transcript. Any
candidate-report hashes above identify the pre-promotion bytes.
