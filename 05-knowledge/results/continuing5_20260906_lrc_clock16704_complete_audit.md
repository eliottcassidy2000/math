# Independent referee: complete exclusion of clock 16,704

**Status: PASS.** I accept the analytic transfer and complete finite certificate in `continuing5_20260906_lrc_clock16704_complete.md`, with no mathematical repair requested. The conclusion is that the inherited necessary clock set loses 16,704 and now has **8,201 members, maximum 14,904**. The scope includes a primitive thirteen-speed row with any chosen six-label subset whose seven-label complement has a connected strict ratio graph. The proper six-label phase input remains **CITED**. LRC(14) remains open.

The independent source imports no producer implementation. It reconstructs the complete word and edge universes and compares every conditional maximum, every decision, and every located profile against the frozen certificate. Normal and optimized runs pass **8,493 always-active exact gates**, with byte-identical raw LF output.

## 1. Inheritance and the precise object

The input domain is the one in `third_20260906_grid_full_words.md` and its independent audit: seven sheet states dividing t, lying in the inherited 42-value domain, with gcd one and all 126 proper nonempty projected complement words valid. At t=16704 the allowed divisors are

    1,2,3,4,6,8,9,12,16,18,24,29,32,36,48,58,64,72.

Sorting quotients only the permutation of seven positions. Repeated values remain repeated positions in every subset test and in every forced-pair condition. This matters because distinct physical speeds can have equal sheet gcds.

The operative map takes a full valid word to its ceiling excess together with the two distinguished states occurring in it. It retains a compatible owner for the selected edge. The older marginal bag and unconditional maximum retain neither this joint attachment nor all full profiles. Separately, the exact located count retains the common phase of every interval in the selected pair's danger intersection. Both coordinates are required: the frozen (3,308) and (23,323) controls exhibit opposite losses.

The inherited finite suppliers remain load-bearing. I pin the full profile JSON, the complete old maximum-table semantics, and the full 8,202-clock set. I independently recover the maximum at this clock, rather than relying on its old attaining example. I do not re-audit other clocks or infer physical realizability from a projected word.

## 2. Complete word optimization, independently reproduced

My source enumerates all binom(24,7)=346104 nondecreasing seven-state multisets without pruning. A subset-gcd bitmask recurrence computes every selected-subset gcd and tests its full complementary profile. Exactly **19,073** words survive, with global maximum ceiling excess **188**. The entire sorted valid-word sequence agrees with the producer's semantic digest; the source additionally retains a digest of the words together with their independently computed excesses.

For every valid word I visit unordered pairs of distinct positions, retaining repeated-value pairs only when two occurrences exist. This produces exactly **161 realized types among 171 possible unordered state pairs**. All 171 maxima or infeasibility entries are compared individually with the certificate. Every recorded owner is checked again for full profile validity, forced multiplicity, and literal ceiling cost. Every maximum lies below both the independent marginal relaxation and 188.

The exhaustive census proves the upper bounds; owners prove attainment. The ten absent types have no valid full word. In particular,

    M_(4,4)=134,  M_(12,16)=188,  M_(6,72)=180.

Nothing in this optimization requires an owner to realize actual speeds. That is harmless: the finite domain contains every actual word in a hypothetical failure, so maximizing over it gives the needed upper bound.

## 3. Every candidate and every common translate

I independently enumerate the strict THM-3818 atlas: all 5,855 coprime positive ratios p<q whose sum is at most 356 and has only primes congruent to 2 modulo 3, each with exponent at most two. All five divisors e<=6 of 16704 are retained. Thus all **29,275** base candidates are accounted for.

Each pair's geometry is reconstructed by sorting its raw rational danger boundaries and applying the literal midpoint predicate. This avoids the producer's clipped component-length formula. A five-slot bounded knapsack in native ceiling costs independently reconstructs the old paired relaxation, rather than importing the producer's sorted residue-cost bag. These paths reproduce all 988 retained candidate rows exactly, with 28,060 separate-cost exclusions and 227 unavailable forced margins.

The conditional table excludes **835** of the 988 rows using their separate component counts alone. For the other **153**, my source computes the complete common-translate profile by an enter/exit event sweep from the independently reconstructed geometry. At a wall, departing points are removed before evaluating the strict open-danger count; arriving points are added only in the next open cell. This handles coincident events as well as isolated endpoints. A literal grid predicate initializes each sweep and independently checks both published extremum owners. All 153 minima, maxima, active-wall counts, conditional costs and positive surpluses agree individually with the certificate.

Every resulting decision is compared entry by entry, and every one closes. The least located surplus is **18**, at

    (e,p,q)=(6,132,221), n=2784, margins=(72,6),
    conditional maximum180, separate credit174,
    normalized minimum33, located credit198,
    normalized maximum68, active walls86.

For this worst located row I also use the strictly larger partition containing every projected raw boundary of either predicate, including inactive boundaries. Literal evaluation at all **464 walls and 464 open cells**, on every one of the 2,784 grid points, gives the same minimum 33 and maximum 68. Thus the event proof is checked by an independent complete native path at its tightest located control.

The earlier two controls remain exact: (3,308) has conditioned cost188, separate credit172 and located credit336; (23,323) has conditioned cost134 and separate/located credit180. All translations use one common phase. No sum of unrelated pair credits or separate extremum owners appears in the successful inequality.

## 4. Analytic transfer and scope review

For an actual pair (Dp,Dq), let e=gcd(t,D), n=t/e and D=e k. Then gcd(k,n)=1. Multiplication by D sends any translated t-grid to a translated n-grid with multiplicity e; multiplication by the unit k permutes its residues. The exact forced states are e gcd(n,p) and e gcd(n,q). The verifier includes 27 full t-grid phase/unit controls, including phase zero, to check this map and the weak-endpoint convention literally.

The selected pair indicator is pointwise at most the total danger multiplicity excess (m-1)_+. Consequently any uniform pair credit C and any compatible word d give at least C-E(d) weak-safe grid points. Each candidate in the complete universe has C>M_(d_p,d_q)>=E(d), so every possible supplied edge gives a positive count. A formal pair with no compatible word cannot be an actual edge of the hypothetical row. This is a sufficient physical safety certificate, not an equivalence with failure.

The connected-complement extension is valid. Given a primitive thirteen-speed row and a chosen six-label subset A, put t=gcd(A), g=gcd(B), V=A/t and U=B/g. Primitivity gives gcd(t,g)=gcd(V)=gcd(U)=1. Under hypothetical failure, the global inherited profiles put its sheet word in the declared finite domain. The proof in `third_20260906_decoder_profile_graph.md` supplies an edge with e<=6 using only that word and connectedness of the seven-label strict atlas graph: otherwise its state-gcd graph at threshold seven would be connected, contrary to the inherited complete certificate. No decoder-span equality, height bound, or connectivity of A enters this argument.

The CITED proper six-label lonely-runner phase gives a V-safe phase. Its t physical lifts preserve A-safety. Since gcd(t,g)=1, their U-clock images form a translated t-grid, to which the preceding positive count applies. This proves weak safety. The strict open danger sets are exactly what makes boundary points weak-safe; no strict-clearance conclusion is inferred.

For t outside the older necessary set, the inherited theorem already applies with this same connected-complement scope. The only new clock evaluated here is t=16704. The exact old array has 8,202 entries and only this clock above 14,904. Removing it gives the entire independently compared new array of 8,201 entries, with maximum14,904. Remaining clocks are necessary possibilities, not asserted realizable failures.

## 5. Reproduction and frozen pins

Run the adjacent `continuing5_20260906_lrc_clock16704_complete_audit.py` with ordinary Python and with `python -O`. Both outputs are retained as `.out` and `_optimized.out`. Dependencies prefer adjacent source/data files and the sibling results directory, with the outside worktree fallback retained. The exploratory `_audit_probe.py` is not a filing artifact.

- Audit source SHA256: `ce93254002fca0d26cfe7526bdec73fdde0e4ecc2958bb97ac07e1e38b047931`.
- Audit output SHA256: `b93e62d76658562a7cc1a273680547871ea9a56699cbd909e848615edd90a3f3`.
- Producer source: `2c1e624743c6357257558ed8b8f291667577aad02e36a567b71f4d3ee1eec05f`.
- Producer output: `33820ee3775baaedb0b8516b1a837b184b6ffdd279266614e479079f215e49fa`.
- Producer JSON: `8ab0b9fe8791707ac780f32030103eeec71de6830613619c8333672563a15345`.
- Audited prepromotion report: `44475b6ebb54e24cb30ce07e5afa0a80b1647066c0f31547af518513c379ed8d`.
- Inherited profile JSON: `935f3f687b6d7c89cc099e536f536238fd753bcc4c1747906d213cef387ca93f`.
- Sorted valid-word semantics: `e585b7c1aa6f5bfd55abc2d3caaf2b245789a9ace580d8abd46a629d0fa49eef`.
- Old clock-array semantics: `4f29481d984ead40d0144556ce1c45dce210e30b964bb65835a7904ca6353e59`.
- New clock-array semantics: `ddbe0e091d36d54c8f6a7c8ea631bbf363d799b9adaa2ec4fe4cf56250d11a76`.

This referee created only new outside-worktree artifacts and changed no repository or Git state. The parent owns status promotion and integration.

The [raw-byte checkpoint manifest](continuing5_20260906_manifest.json) pins
the filed source, report and identical normal/optimized transcript. Any
candidate-report hashes above identify the pre-promotion bytes.
