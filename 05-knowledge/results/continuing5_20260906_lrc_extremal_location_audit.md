# Independent audit of the displayed clock-16704 located pair

**Verdict: PASS, exact one-pair count and conditional weak-safety transfer.**
The displayed selected pair and word no longer obstruct the located-overlap
test. This audit does not eliminate every candidate at clock16704, reconstruct
an actual seven-speed realization of the word, or rerun the inherited
full-word maximum optimization.

The producer is `continuing5_20260906_lrc_extremal_location.md/.py/.out`.
This referee imports no producer or repository implementation. It constructs
a larger, independent native-predicate phase partition and counts the actual
grid points with integer modular inequalities. The mathematical proof was
reviewed separately from the finite arithmetic.

## 1. The word and selected-edge domain

The input is

    t=16704, e=4, n=t/e=4176, (p,q)=(3,308),
    d=(12,16,72,58,64,9,9).

The pair is coprime, its sum311 is prime and congruent2 modulo3, and the
strict atlas height limit356 holds. Every word entry divides t and their
joint gcd is one. The forced margins are exactly

    e gcd(n,p)=12, e gcd(n,q)=16.

The referee independently recomputes every one of the126 nonempty proper
subset complement profiles against the pinned full profile bank. This checks
the same finite word domain as the producer. It does not assign word
positions to actual speeds or verify additional physical profiles of an
unprovided row.

The literal marginal ceiling costs are

    (2388,2400,2448,2436,2432,2394,2394),

whose sum minus16704 is188. This verifies the attained cost of the displayed
owner. It is not a proof that no other allowed word has larger cost. The
separate incoming optimization in `third_20260906_grid_full_words.md` supplies
M(16704)=188 when the producer invokes that stronger inherited statement.

## 2. Independent exact geometry and all-translate certificate

Write I={x mod1: ||3x||<1/14 and ||308x||<1/14}. Instead of intersecting
preconstructed interval lists, the referee sweeps every raw rational boundary
of both danger predicates:

    (14k+sign)/(14v) modulo1,
    v in {3,308}, 0<=k<v, sign in {-1,+1}.

There are622 distinct spatial boundaries. The literal predicate on each
intervening cell reconstructs45 open circle components, represented with an
unwrapped final cell where necessary. Exactly43 lengths are1/2156 and two
are1/4312. Their total measure is1/49. The open-interval independent lower
counts on the4176-grid are therefore43 ones and two zeros, totaling43 and
giving the earlier physical credit172 after multiplication by e.

For the uniform located count, the referee uses all residues of n times
those raw spatial boundaries. This gives156 phase walls, rather than the
producer's90 walls belonging to the intersection itself. These form a
complete partition for every individual literal danger indicator at every
grid point. For each wall and each intervening open cell, the code visits all
4176 points and evaluates both strict inequalities using integer arithmetic.

More precisely, for alpha=A/B and the j-th grid point, set N=A+Bj and
D=4176B. Danger for speed v is exactly

    14 min(vN mod D, D-(vN mod D)) < D.

There is no interval-floor counting formula in this path. The count is
constant on each open native phase cell because no point can enter or leave
either danger set without crossing one of the enumerated walls. Wall values
are evaluated independently with the same strict predicate, so no endpoint
convention is inferred from a neighboring open cell.

The resulting complete histograms are:

| Count | Native walls | Native open cells |
|---:|---:|---:|
|84|32|16|
|85|80|72|
|86|34|53|
|87|10|15|

Both columns total156. Comparing the actual grid-point bitsets across each
wall shows that exactly90 are active for the intersection, precisely the
90 projected component endpoints. A further check counts membership in the
independently reconstructed spatial cells at five named translates.

Thus the minimum over all real translates is84 and the maximum87. The
quoted translates53/539 and74/77 attain these values. These are exact
uniform extrema from a complete finite partition, not sampled estimates.

## 3. Lifting and the actual consequence

Let an actual selected pair be (Dp,Dq) and suppose gcd(t,D)=e. Writing
D=e k gives gcd(k,n)=1. Multiplication by D maps the t-grid with translate
alpha to the n-grid with translate k alpha, each point occurring e times.
Multiplication by an additional physical common factor g also permutes the
t-grid whenever gcd(g,t)=1. Consequently the selected pair contributes at
least4*84=336 simultaneous dangerous incidences for every translate.

The referee checks this identity directly on the full16704-grid for four
different k values and four translates, using only the literal predicates.
All16 full-grid counts equal e times their reduced-grid counts. These are
controls of the general multiplication proof, not its only justification.

At a grid point with r dangerous labels, the selected pair indicator is at
most (r-1)_+. Therefore the union of seven danger sets has size at most the
sum of their marginal counts minus336. When their total ceiling excess is
at most188, at least336-188=148 grid points are weak-safe for all seven
labels. Lifting a supplied six-body safe phase then produces weak-safe
physical phases for the complete row, by the inherited translated-grid
consumer. The proper six-body phase supplier remains an inherited cited
dependency; this audit does not establish a new lower-dimensional LRC result.

The displayed word has exactly that excess. Relative to the independently
proved M(16704)=188 bound, the same selected edge also closes any other
allowed word with excess at most188. None of this proves that every eligible
edge at clock16704 has sufficient located credit. The report correctly leaves
that separate finite edge exhaustion undone.

The endpoint convention is essential: danger is strict, hence its complement
has clearance at least1/14. The argument proves weak safety and does not
promote it to strict clearance. The distinction between a compatible finite
word and a complete physical speed realization is maintained throughout.

## 4. What was actually repaired

For this retained primitive pair, all45 interval locations were already
determined. The earlier sufficient credit independently minimized each
interval count and discarded their common translate. A minimum of their sum
is at least the sum of their minima, with strict inequality here:84>43.
This restores correlated endpoint rounding. It does not recover the actual
seven-label ratio pattern, all seven-mask intersections, or the subset of
translates supplied by a particular six-body phase set.

The previous refined scale set remains a sound necessary set. Eliminating
one of its displayed compatible pair-and-word controls is an improvement
to a retained candidate, not a contradiction of that earlier result and not
a whole-clock elimination.

## 5. Reproduction and frozen identities

Run the independent source directly or after filing it in04-computation:

    python continuing5_20260906_lrc_extremal_location_audit.py
    python -O continuing5_20260906_lrc_extremal_location_audit.py

It uses only the Python standard library. It pins the producer source/output
and inherited profile JSON, but never imports their implementations. The
adjacent filed dependency layout is supported, with the current outside
worktree as a fallback for the existing inherited JSON.

Both normal and optimized modes pass **425 always-active exact gates** and
produce byte-identical LF output. The only producer change requested was
portable dependency lookup; its mathematical computation and output remain
unchanged.

- Referee source SHA256: `89ba1a53f567062c1eef09767dbbacdc75d116ded5a51e1e09e7063dd0125be1`.
- Referee output SHA256: `c403adc2aacda0b1c3c741f3d6a7cdba936704affceb36a766c2066f3a646356`.
- Producer source pin: `6526dd4306c31861792c5e938b37e498675cb1f39f9a05b8158d2f0940d44659`.
- Producer output pin: `954c713447f9ff4ba162f0318195d12b5a983c6e646ca54821864a661a625ed8`.
- Inherited profile pin: `935f3f687b6d7c89cc099e536f536238fd753bcc4c1747906d213cef387ca93f`.

All referee artifacts are outside the repository. No producer, navigation or
Git state was edited by this referee.

The [raw-byte checkpoint manifest](continuing5_20260906_manifest.json) pins
the filed source, report and identical normal/optimized transcript. Any
candidate-report hashes above identify the pre-promotion bytes.
