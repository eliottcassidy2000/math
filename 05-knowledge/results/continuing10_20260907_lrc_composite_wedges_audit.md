# Independent referee: complete native composite wedges

**Verdict: PASS.** The analytic full-row safety arguments and every declared
finite universe in `continuing10_20260907_lrc_composite_wedges.md` are accepted,
relative to the stated proved proper-profile and CITED lower-runner suppliers.
This audit imports or executes no producer. It does not claim that the two
wedge patterns are forced in every connected complement, that all fifteen
projected words are excluded, or that LRC(14) is solved.

## 1. Actual thirteen-speed consumer

The hypotheses are a primitive row of thirteen distinct positive integers,
a selected six-label subset A of gcd t=7200, and the specified actual pattern
in its seven-label complement. They do not include decoder equality, a speed
box, physical speed one, or complement connectivity.

Under hypothetical failure of weak 1/14 safety, the inherited global
proper-subset profile supplier applies to A together with each nonempty
proper subset I of the seven tails. The gcd of this body is
gcd(t,u_i:i in I)=gcd(d_i:i in I), where d_i=gcd(t,u_i). Its remaining
tail sheet gcds are gcd(gcd(d_i:i in I),d_j). This is exactly the condition
in `third_20260906_grid_full_words.md`, Section 1, for all 126 positional
subsets, including positions whose values repeat. Global primitivity gives
gcd(d_1,...,d_7)=1. Consequently the producer's full-profile maxima are valid
upper bounds on the actual marginal danger excess. They are not assertions
that every maximizing arithmetic word is a physical unsafe row.

For an actual forest F on the seven labels and a dangerous set S at one
grid point, the number of induced edges is at most max(|S|-1,0). Summation
therefore gives

    safe >= 7200 - sum_i danger_i + sum_ij in F intersection_ij.

The bound danger_i <= d_i ceil(7200/(7d_i)) is valid also on strict endpoint
walls. Any forest of uniform pair credits exceeding the conditional excess
therefore gives a safe lift for every translation. The proper-six supplier
gives a phase safe for A/7200; all 7200 lifts preserve it. This proves the
actual thirteen-row conclusion, rather than only a favorable tail statistic.

For a pair D(p,q), write e=gcd(t,D), n=t/e. The factor D/e is a unit modulo
n; its multiplication permutes grid labels and every label occurs e times.
The pointwise pair count is e*C(n,p,q,(D/e)alpha), with the phase multiplier
retained. Taking the minimum over all translations gives the exact uniform
credit e*min C. For a primitive triple R with actual multiplier h,
gcd(t,h)=gcd(d_i,d_j,d_k), so this clock gcd determines all its pair sheet
gcds. There is no discarded scale branch in using representative scale 2 for
the first two wedges, or 6 for the named third family.

## 2. Independent exact profile reconstruction

The referee partitions [0,14pq] at every endpoint of either original danger
family and tests the rational midpoint of each cell by literal modular
inequalities. This reconstructs the joint open components without the
producer's two-pointer intersection algorithm. Joining the two origin
pieces produces exactly the saved interval semantic hashes.

There is a second independent route to every phase count. For intervals
(a,b)/L, let A be the sorted multiset of na mod L, B the sorted multiset of
nb mod L, and M=sum(floor(nb/L)-floor(na/L)). At a phase beta in [0,1),

    count(beta) = M - #{B <= L beta} + #{A < L beta}.

Thus at a wall w the count is M-#(B<=w)+#(A<w), and immediately after it
is M-#(B<=w)+#(A<=w). This is an order-statistic formula, not a recursive
event sweep. It handles coincident endpoints and the wall at zero directly.
It independently recovers every one of the 2,149,609 saved walls and every
intervening chamber across all 1,154 profiles, as complete semantic hashes.
The declared minimum and maximum of each profile are separately checked by
literal enumeration of its native grid at the saved attaining phases.

The test n=7,p=q=1 has zero strict overlap at beta=1/2, whereas changing
the predicate to closed danger gives two points. It is correctly retained
as an endpoint test, not treated as an eligible distinct atlas edge.

## 3. Complete finite universes

An independent smallest-prime-factor sieve reconstructs all 5,855 strict
atlas pairs. For each conditional word, dynamic subset gcds check all 126
proper positional masks. The three complete four-free multiset universes
each have binomial(29,4)=23,751 candidates. They reproduce exactly:

| Forced margins | Admissible multisets | Maximum excess |
|---|---:|---:|
| 4,24,18 | 1033 | 108 |
| 16,24,18 | 949 | 116 |
| 24,18,30 | 162 | 107 |

The complete directed arm sets have sizes 316,316,231. The referee uses
integer path products (uz,vz,wv), divided by their collective gcd, instead
of rational propagation to recover every low-credit primitive triple.
Both complete 316*231=72,996 products are checked. The first wedge has
72,939 arm-sum successes and 57 residual products; the second has 72,782
and 214. Every residual has the actual required margins at scale 2. The
largest-two-of-three forest credits have minima 114 and 142, respectively,
strictly exceeding 108 and 116. The complete all-zero subcases contain
5 and 30 products. No global compatibility filter removes a local choice.

For (3476,21093,445) at scale gcd 6, the independently reconstructed pair
credits are exactly 0,96,66. The endpoint credit 96 does not pay the
conditional bound 107, while its same-triple forest credit 162 does. This
repairs the pair-only near miss with an actual forest; it is not a sum of
unrelated minima on invented carriers.

The retained hostile word (1,9,16,18,32,60,72) satisfies every proper profile
and has excess 164. Thus deleting the middle margin 24 loses the 116 bound.
It is a quotient-level obstruction, not an unsafe physical row.

## 4. Selected-bank and nonvacuity scope

The independent homogeneous tree equations recover all 29 inherited
orientations, including rejected ones. Exactly twenty are distinct primitive
realizations with the prescribed clock margins, on ten projected words.
Every actual pair ratio is checked; every pair with reduced sum at most
10,000 is evaluated. Each accepted row has one certified pair already
exceeding its own excess. The saved forests are checked for acyclicity and
actual edge weights, and a separate Prim calculation recovers their maximum
weight on the evaluated graph. No optimum over unevaluated large ratios is
inferred. Every selected row's structural wedge or named-family tag is
checked on its actual labels.

Literal thirteen-row enumeration recovers the safe counts 2541,2504,2476
at six-body phase 1/7. All three controls are primitive, distinct, have no
speed one, and retain all 126 profiles. Their entire strict complement graph
has just the two displayed arms and four isolated vertices. The inherited
seven-tail hostile has all six atlas intersections zero at the same phase
and still has 2020 safe points. These verify the proof's nonvacuity and the
distinction between an actual overlap forest and an unknown-graph minimum.

The full-bank scope remains twenty selected realizations. Neither this
referee nor the producer concludes that every realization on the same ten
words closes, or that all fifteen old projected words have disappeared.

## 5. Reproduction and frozen inputs

Run the standalone referee normally and with optimization:

    python 04-computation/continuing10_20260907_lrc_composite_wedges_audit.py
    python -O 04-computation/continuing10_20260907_lrc_composite_wedges_audit.py

Optional `--certificate`, `--profiles`, and `--inherited` arguments select
the explicitly pinned inputs. Defaults support the outside audit directory
and the repository's 04-computation/05-knowledge/results layout. Source
stdout explicitly uses LF; raw captures are compared without normalization.

Pinned producer source:
1d2f52c100802ab1d3b330c920a3f494eca662b7a51a7c5fedc6f1fe9a287cb5.
Pinned producer certificate:
890d00d44f0195765d62fe1d40b59ad102311f34a84a42a5fddc229d037209e9.
Inherited full profile and selected-bank hashes are checked by the source.

Both normal and optimized runs pass **12,660 always-active exact gates**.
Their raw outputs are byte-identical and contain no carriage returns.

    audit source af7e34a7a0251033788a67e7e47dd9d9ee380f3509e8a8ca0e4b9de5b96ddfcc
    audit output 81e6545bd21d3c0305fccb1573ca99a32cca96a1077ae3fb112f5f0e5d763712

Parent owns promotion and filing. No repair to the producer was required.
