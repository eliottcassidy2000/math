# Independent audit of ranked inert-tail component capacities

**PASS: the analytic theorem and its finite head are accepted.** This audits
Sections1--4 of
`05-knowledge/results/continuing1_20260906_profiles.md`, read
together with its standalone source and the proved inherited
`05-knowledge/results/creative_20260906_inert_pareto.md`.
No repair is requested. The result is an all-height sufficient body
certificate and an abstract strict improvement. A new physical eleven-body
family or universal body lower bound is not established.

The independent verifier is
`04-computation/continuing1_20260906_lrc_profiles_audit.py`.
It imports neither producer nor inherited mathematical code. It constructs
all strict half-lift danger sets directly from the two physical phases and
evaluates every open atom and its endpoints, rather than reusing the
producer's opposite-parity interval-intersection formula.

## Analytic audit

For a compact finite-component G contained in a proper open finite-component
carrier C, each chosen positive closed G-component lies in one open
C-component. Several chosen components may share the same carrier component;
an injection is neither asserted nor required. Within each used open
component the finite selected compact union leaves positive length near
both ends. Its total length is strictly smaller than that carrier length.
The k selected largest G-components use at most k carrier components, whose
sum is at most the k-largest carrier sum. This proves strict top-k
inequalities at every positive rank. For ranks beyond the last positive
G-component, monotonicity of the carrier top sums completes the proof.
Isolated points remain part of the actual containment question but cannot
be certified through a zero length sum. Inclusive equality is therefore a
valid escape condition, and touching open danger pieces must retain their
safe boundary point.

The equal-splitting formula for D_g is exact at k=gr+s:
`T_k(D_g(v))=T_r(v)+(s/g)v_(r+1)`. The right side linearly interpolates the
two adjacent original partial sums. This proves preservation of weak
majorization. Splitting also cannot increase the sum of a fixed number of
largest entries, so D_g(v) is weakly majorized by v. These statements use
integer g>=1 and nonnegative lengths with zero padding; no endpoint
placement is preserved by them.

The inherited inert-tail theorem supplies the global mass cap20/469,
uniquely attained at(1,67), and component bound2/(7q). For q>67 the mass is
strictly below20/469 and each component strictly below2/469. Thus the entire
list is weakly majorized by ten entries2/469: use the entry bound up to rank
ten and the total bound thereafter. Only the complete q<=67 head remains.
The independent head below verifies precisely the five displayed maximal
profiles at every rank. Distinct profiles with increasing mass and
decreasing maximum cannot weakly majorize each other. This proves the
all-height frontier relative to the inherited mass/width theorem; it is
not an extrapolation from the numerical tail.

For the physical consumer, a candidate body clock y has exactly the two
physical half-lifts y/2 and(y+1)/2. With odd common scale g, multiplication
by g permutes their tail phases relative to the half-lifts of gy modulo
one. Therefore the strict spoilage set is exactly the inverse image of
C_(p,q) under multiplication by g, and its component lengths are D_g of
the primitive profile. A uniformly failed row puts the compact body safe
set in that proper open carrier. The corresponding positive top-sum
inequality contradicts the strict containment result. The proof is valid
for any finite nonempty H satisfying the displayed certificate; an
eleven-body is required only for the thirteen-nonzero-speed application.
It neither asserts decoder entry nor erases coincidences in the physical
speed union.

If H contains an even speed, both reflection fixed points0 and1/2 are
dangerous. Every positive connected component is paired with a different
reflected component, so lengths occur in equal pairs. For a paired sorted
source, partial sums are linear between consecutive even ranks. The
(19,25) target has even breakpoints2,4,6; width controls rank2, mass controls
rank6, and rank4 is the only new condition. The(5,53) target has breakpoints
6 and8, controlled respectively by width and total mass. Uniform target
blocks add no condition. Interpolating the odd ranks proves the exact
mass/width/top-four reduction, with equality included. The even-speed
hypothesis is used for this reduction, not for the full ranked theorem.

The abstract paired list(1/100 repeated four times,1/2000 repeated twice)
has the claimed strict gain. Its mass and maximum both miss the(19,25)
profile, but its rank-four sum1/25 exceeds120/3325. Its other four targets
are paid by mass or width. Disjoint reflection-paired closed arcs can
realize the lengths; nothing in the proof realizes them as G_H for an
eleven-speed H. Keeping that distinction is essential.

## Independent finite controls and limits

The verifier builds the eligible sums multiplicatively from primes2mod3
with exponents at most two. Coprimality and odd3-unit filters leave exactly
46 pairs with p<q<=67. For each pair it partitions the y-circle at every
possible strict-danger endpoint and directly tests

    for both j=0,1, some w in {p,q} has ||w(y+j)/2||<1/14.

Consecutive dangerous atoms are merged only if their shared point is also
strictly dangerous. It recovers the complete decreasing length list,
all five maximal profiles, and every domination relation. For each of the
46 pairs, separate literal computations at common scales3 and5 give the
exact split lists D_g and their weak-majorization relations. Thus the
physical odd-scale normalization is tested directly, not just by invoking
the abstract D_g formula.

A fresh alphabet of seven rational lengths, all multisets of four entries,
and paired duplication give210 independent source lists. Every rank is
compared with the reduced mass/width/top-four disjunction for all five
targets. Separate controls retain many-to-one containment, strict/open
equality, and isolated-only geometry. The verifier passes5,643 always-active
gates in normal and optimized Python, with identical LF bytes.

The large2,496,144-body scout was **not independently replayed** in this
bounded audit. Its negative numerical claim remains producer FINITE-EXACT,
not part of this independent numerical coverage. It is not needed for the
analytic theorem; the producer correctly treats it as a stopping reason
and does not infer all-height physical redundancy. No larger body census
was performed here.

```text
python -B 04-computation/continuing1_20260906_lrc_profiles_audit.py
python -B -O 04-computation/continuing1_20260906_lrc_profiles_audit.py
```

The promotion basis is an independently read all-parameter proof, a fresh
literal physical construction of the complete finite head and its odd-scale
copies, and an independently checked breakpoint reduction. It supports the
stated ranked-capacity theorem, not a new physical eleven-body closure.
