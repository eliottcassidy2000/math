# A profile-admissible actual entry defeats every cross-divisor closure test

**Status: FINITE-EXACT actual entry + elementary obstruction proof;
INDEPENDENTLY AUDITED.** The row below is safe. It refutes the proposed implication
that actual decoder equality and all inherited hereditary profiles force one
of the sufficient tests in
[the cross-divisor theorem](open_frontier_sep06_decoder.md). It does not
contradict that theorem, classify the remaining entries, or challenge LRC(14).

Both normalized components are unitless, the actual graph has split 5+8,
and every mixed support orientation is retained. Even the two elementary
whole-arc grid sufficient conditions fail. An actual favorable grid alignment
nevertheless supplies an explicit strictly safe time.

## 1. Construction and inherited objects

Keep the full **THM-3818** domain from
`01-canon/theorems/THM-3818-scaled-inert-cubeclass-support-two-pair-packet.md`:
`Q=91^6=567869252041`, thirteen distinct positive primitive physical speeds,
sum at most `Q^2`, the actual all-scale decoder atlas, and the full span W of
support-at-most-three integer relations of coefficient height at most Q.
The equality criterion is inherited from
[the complete two-component decoder](overnight14_20260906_lrc_general_decoder.md).
All hereditary profiles are from
[the joint-shadow result](lrc14_joint_shadow_empty_core_next_sep06.md).

The live board is actual components, prime-support separation, squarefree
cofactor profiles, bounded-support coefficient scales, exact signed radii,
and actual phase-grid alignment. The canonical near miss was the preceding
billion-divisor stress, already excluded by inherited profiles. Separating
the prime supports repairs that defect while keeping large internal pair
gcds. The underused sidecar is the actual grid translate, discarded by the
universal safe-arc length test.

For `q=(337,343,349,355)` put `C=product(q)` and take the five-star

    V={C} union {(356-q_i)C/q_i:i=1,...,4}
     =(40341259,287243635,542783995,807423715,14321146945).

Put `P=2*3*11*17*23*29=748374` and

    U={P/p:p in {2,3,11,17,23,29}} union {P,3P}
     =(25806,32538,44022,68034,249458,374187,748374,2245122).

Set `t=4912751`, `g=1`, and `n=tV union U`. Exact integer checks give

    gcd(V)=gcd(U)=gcd(n)=1,
    gcd(t,P)=1,
    gcd(v,u)=1 for every v in V and u in U,
    sum(n)=78598806272076840 < Q^2.

The two primitive components have no unit. Each five-star edge has reduced
sum 356. A spanning tree on the six cofactor labels uses prime-index pairs
`(2,3),(3,17),(17,29),(2,23),(23,11)`, whose reduced sums are respectively
5,20,46,25,34. The links `P/3 -- P -- 3P` have reduced sum 4. All are actual
atlas edges. Every cross-component physical ratio exceeds 355, so no cross
edge is possible; the source also computes the entire actual graph directly.
Thus the graph is exactly the declared 5+8 split.

The component sizes exclude the inherited 11+2 theorem's actual graph
domain. The absence of a unit in the larger shape excludes the hypothesis
of the fifteenth larger-unit theorem. No assertion is made that this safe
individual row is outside every other known sufficient safety mechanism.

Incoming concurrent work at `origin/main` commit `2763ed0df4` was read
before finalizing this scope. The
[leaf-cofactor theorem](second_20260906_entry.md) improves the generic
nonunit endpoint cutoffs to 698294,4854,26 for 3+10,4+9,5+8, respectively;
the earlier generic table is therefore not the current numerical frontier.
The present 5+8 endpoint gcd minimum is 25806 and still misses that stronger
cutoff. The independent
[balanced-entry obstruction](second_20260906_decoder.md) already gives
profile-admissible 6+7 entries with all maximum-endpoint native tests failing.
The present witness is a distinct 5+8 unitless realization: it additionally
checks every arbitrary larger pair and fails both universal whole-arc grid
tests. These are complementary exact obstructions, not competing claims
that the general forcing question was previously untouched.

## 2. Complete equality entry with a narrow coefficient gate

The smaller primitive pair gcd and larger primitive pair-sum extrema are

    d_V=min_{v<w in V} gcd(v,w)=115591,
    L_U=max_{u<w in U} (u+w)/gcd(u,w)=88.              (1)

The scale was chosen so that

    t d_V=567869800841 > Q,
    t min(V)=198186560493509 > 88Q=49972494179608.      (2)

These inequalities exclude every bounded mixed relation by different
mechanisms in the two orientations.

For two smaller labels `tv,tw` and one larger label u, coprimality across
the components gives

    gcd(t gcd(v,w),u)=1.

The distinguished coefficient of u is therefore a nonzero multiple of
`t gcd(v,w)>Q`. This excludes all 80 supports of that orientation.

For two larger labels u,w and one smaller label tv, put `D=gcd(u,w)`.
Here `gcd(D,tv)=1`. Any relation has distinguished coefficient `jD`, and
after dividing by D it reads

    jtv = r(u/D)+s(w/D),    j>=1,    |r|,|s|<=Q.

Its right side has absolute value at most `Q L_U`, while its left side
exceeds that quantity by (2). This excludes all 140 opposite supports.
Zero coefficients on a redundant label are allowed, so support-two crossings
are included. A support-three crossing of a two-component partition always
has one of these two orientations.

The actual decoder edge span is the direct sum of the two internal weighted
kernels, and its edge heights are at most 355<Q. Every bounded relation now
stays inside a single component. It follows that **W=V_dec**. This proof
does not infer full entry from a selected orientation or a sampled rank.
The source separately runs all 220 exact native signed-box tests.

The reusable construction criterion exposed by this example is simple:
when `g=1`, all cross pairs are coprime, and t is coprime to every larger
coordinate, it suffices for equality that

    t min gcd(V pairs)>Q,
    t min(V)>Q max normalized-sum(U pairs).

Together with the actual graph and box, this can give equality long before
the universal phase-grid length threshold. It is an entry construction,
not an LRC closure theorem.

## 3. Every inherited profile passes

Any body subset meeting both components has gcd one, by the literal
cross-coprimality. A pure V subset has at most five labels. The only
nontrivial body gcds of sizes 7 through 12 consequently arise from seven
of the eight U labels. Deleting `P/p` leaves gcd p; deleting P or 3P leaves
gcd one. In the nontrivial case the six tails consist of the deleted
cofactor and the five smaller physical labels, all coprime to p.

Hence the complete nontrivial profile list is precisely

    (c; gcd(c,tail_i)) = (p;1,1,1,1,1,1),
    p in {2,3,11,17,23,29}.                          (3)

All six are retained in the frozen joint-shadow profile bank. For a direct
check, the uniform effective order is p and
`6 ceil(p/7)>=p`, while the forced pair intersection is
`max(0,2 ceil(p/7)-p)=0`. The prime clock has no other nontrivial divisor
cut, and adjoining any tail makes the body gcd one. None of the special
clock-32 or clock-96 exclusions applies.

The source checks every actual body subset of sizes 7 through 12 against
the complete inherited JSON, with no reclassification or weakened filter.
There are no rejections. The gcd maxima for sizes 12 down to 7 are

    (1,1,1,1,1,29).

Passing these necessary relaxed profiles does not assert actual unsafety.

## 4. The sufficient tests all fail, and an actual phase succeeds

For every larger pair u,w and smaller label v, the cancellation divisor
in the preceding theorem is

    delta=gcd(g gcd(u,w),tv)=1.

For endpoint pairs `w=K=max U`, each `u<K` divides P and hence divides K.
Thus `D=gcd(u,K)=u`, and `gcd(D,v)=1`. The coefficient comparison
`30D<=Q` passes throughout, but the minimum endpoint lcm is

    min_{u<K,v in V} lcm(gcd(u,K),v)
        =1041046529754 > 5Q/63.                     (4)

Every endpoint cross-divisor width test fails.

The exact native radius refinement, including all non-endpoint larger
pairs, also fails. Its largest dimensionless score is

    max [5 delta R/(63 K v)]
        =4730272820/108022718367 < 1.                (5)

Equality in this maximum occurs at `(u,w,v)=(25806,2245122,40341259)`.
Indeed every reduced larger pair sum is at most 88, so R<=88Q. The displayed
pair has primitive ratio 1:87 and hence R=88Q, and it uses min(V).
The reverse orientation has all 80 distinguished coefficient gates greater
than Q by Section 2. Thus neither direction can supply a native certificate
of the preceding type.

Even the direct whole-arc grid tests, using the actual scales rather than
the forced lower bound, fail in both directions:

    5t < 63 max(U),          8g < 42 max(V).          (6)

Nevertheless the rational physical time

    x=1403643/9825502

has exact minimum clearance

    min_i ||n_i x||=696962/4912751 > 1/14.            (7)

All V labels are odd and `tx=1/2 modulo 1`, so the smaller component has
clearance 1/2. Every U label is coprime to 7, and

    |x-1/7|=1/(14t).

Consequently even the elementary estimate
`||ux||>=1/7-u/(14t)` is strictly above 1/14 here, since max(U)<t. This
actual grid alignment is favorable despite failure of the universal arc
length tests. The source also evaluates (7) against every physical speed.

## 5. First failed implication and next surviving question

The failed implication is: actual component connectivity, full bounded
equality entry, and all hereditary profiles force a useful endpoint gcd or
the present native radius/gluing comparison. They do not. Prime separation
makes all mixed gcd information trivial while preserving large within-pair
gcds; the exact distinguished coefficient still keeps the components apart.

The strongest survivor is the proved sufficiency of each cross-divisor and
native criterion. Its proof is unchanged. The missing coordinate exposed
by this hostile is the actual position of the translated phase grid. A
larger safe component interval, a phase packet, or an arithmetic alignment
certificate can settle rows that every uniform length estimate misses.

This is a stopping object for the proposed purely hereditary forcing route.
It does not show that no stronger arithmetic invariant can close the branch.
The cheapest next test is to retain an actual component-safe packet and its
translated grid residues for this fixed row, rather than enlarge a generic
height census or replace necessary profiles by unsafe examples.

## 6. Exact verification

[Source](../../04-computation/open_frontier_sep06_decoder_residual.py),
[output](open_frontier_sep06_decoder_residual.out). The universe is this one
declared actual row: all graph edges, all 220 mixed supports, all 140 forward
native scores and 35 endpoint scores, every inherited body profile, and the
literal physical safe phase. No large row census or floating computation
supplies a proof input.

```
python3 -B 04-computation/open_frontier_sep06_decoder_residual.py
python3 -B -O 04-computation/open_frontier_sep06_decoder_residual.py
```

The inherited profile JSON is pinned to SHA256
`935f3f687b6d7c89cc099e536f536238fd753bcc4c1747906d213cef387ca93f`.
Normal and optimized outputs are byte-identical, with 5141 explicit gates.

* Source SHA256: `0bd89477d6cb63a94e8455f1bc90cc7af99ece5176111d8ecc622bde6b3064b2`.
* Output SHA256: `917151c281ab517daa60b9884b1431fce0f500297c6b10682a083261e16af6d1`.
* Semantic digest: `85aa8f055fe3858412e68fb9fed52ad696ad7d40b96729c2dfd8f8a337c6cf80`.

Root independently audited the complete proof and source, including both
support orientations, every profile type, all native score failures, both
universal grid failures, and the actual 7-clock alignment: PASS. Root also
replayed normal, optimized and frozen outputs and verified all pins.
