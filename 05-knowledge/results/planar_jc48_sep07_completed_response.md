# A completed Hamiltonian really supplies the earlier weight-22 response

**Status: PROVED FINITE-ROW + FINITE-EXACT; independent audit PASS.**
September 7, 2026. The image below is complete for every polynomial
universal carrier whose induced coordinate correction preserves the
prefix through row nine. It contains a nonzero replacement of the high
row-fifteen payer, but not the exact earlier compensated packet. It is
the finite restriction of an actual completed source automorphism.
No later Keller equations, polynomial termination, or chart entry follow.

## 1. Inheritance and the actual image question

The finite supplier is the incoming independently audited
[complete valuation-eleven response](planar_jc_long_20260906_memory_earlier.md).
It classifies all source changes of valuation at least eleven through
row fifteen, with coordinate changes beginning at row ten, the actual
depth conditions and the original coefficient caps. Its seven conditions
give a weight-22 family `G_m x A^9` for a fixed high payer, with separate
ten-dimensional coordinate fibres. The actual low background depends on
`Xi=xi10`, an affine function of the boundary parameter.

The all-order supplier is the incoming
[completed Hamiltonian theorem](planar_jc_long_20260906_hamiltonian.md),
using the universal affine-source carrier proved in
[the original Hamiltonian note](planar_jc48_sep06_hamiltonian.md).
The [maximal depth-carrier theorem](planar_jc_long_20260906_depth_carrier.md)
separates preservation of depth alone from the smaller simultaneous
source/depth carrier. Here that smaller carrier is retained throughout.

The canonical hostile is the exact old
[weight-22 packet](planar_jc48_sep06_weight14.md): it has a finite
Hamiltonian realization, but that displayed generator is not a universal
source carrier. The corrected near miss is to infer membership in the
completed-carrier image from finite solvability alone. The unused sidecar
is the whole induced coordinate correction, including its leading row.
The five concepts are source valuation, actual depth, completed flow,
the full response image, and the amount of prefix that must change.
These incoming routes explicitly left this image test open; no literature
priority claim is made.

Use the characteristic-zero source chart and signs

```text
p=t(1+x^2t), y=xtp, u=x^2t, Delta=p^3-y^2=t^3(1+x^2t)^2,
A0=k[p,y], P_d=sum_(a+b<=d) x^a u^b A0,
P(A,C)=C^2-A^3+(3/4)A+1/4,
delta={-,S}, {f,g}=f_x g_t-f_t g_x,
S=p^2 Delta R(p,y).                                    (1)
```

An irrelevant scalar may be added to `S`. The symbol `Delta` here is the
cusp polynomial, not the numerical background parameter `896/15`.
The actual background rows zero through five are those of the incoming
valuation-eleven theorem, with arbitrary `Xi`; higher rows can be arbitrary.
Their source through row five is

```text
H5=-3p+(8/3)p^2-(1376/135)p^3+(896/15)p^4-(32/5)y^2
   -(731648/2025)p^5+(512/75)py^2,                     (2)
P(A,C)=-u/2+H5 mod t^6.
```

Although `A5,C5` genuinely vary with `Xi`, (2) does not. The verifier
derives (2) from those literal rows without specializing `Xi`.

## 2. A complete finite reduction, including its kernel

Define `nu(p^a y^b)=a+2b`. This is the actual source `t`-valuation of
a nonzero polynomial: at the least valuation `n`, its leading coefficient
is `r_n(x)=sum_(a+2b=n) R_ab x^b`, and distinct `b` cannot cancel.
For a nonzero `R` of least valuation `n`,

```text
S=t^(n+5)r_n(x)+higher t powers,
delta A=((n+5)/2)x r_n(x)t^(n+4)+higher t powers.        (3)
```

Here `A0=1+x^2/4`, so the displayed coefficient is nonzero. Consequently
preserving both coordinate prefixes through row nine forces exactly
`nu(R)>=6`; this is not an assumed monomial cutoff. Conversely that
condition gives both corrections valuation at least ten.

Terms of `R` of valuation at least twelve give coordinate corrections of
valuation at least sixteen and are invisible at the entire present
horizon. It is therefore necessary and sufficient to test the **thirty**
monomials

```text
p^(n-2b)y^b,       6<=n<=11, 0<=b<=floor(n/2).          (4)
```

The source correction starts one row later. Indeed `G0=0` and
`G1=-(x^2+6)/2`, so its leading coefficient is

```text
[t^(n+5)] delta G=((x^2/2)+3)r_n'(x)-(n+5)x r_n(x).    (5)
```

The coefficient of the top degree `b+1` is
`(b/2-n-5)[x^b]r_n`, which is nonzero since `b<=floor(n/2)`.
Thus (5) is injective. Exactly the six valuation-eleven columns are
invisible in source rows through fifteen; they remain visible in the
coordinate fibre. For `R=p^(11-2b)y^b`, `0<=b<=5`, they give

```text
delta A=8x^(b+1)t^15 mod t^16,
delta C=-(12x^b+6x^(b+2))t^15 mod t^16.               (6)
```

All higher carrier tails are invisible in both coordinates. This gives
the complete support bound and the precise six-dimensional coordinate
kernel, rather than conflating it with the source image.

Only the background through row five can enter the coordinate corrections:
an omitted row of valuation six bracketed with a carrier of valuation
eleven has valuation at least sixteen. The source response likewise
depends only on (2). Its exact polynomial operator is

```text
A_R=(2Delta+3p^3)R+pDelta R_p,
B_R=p(-2yR+Delta R_y),
Psi_H(R)=-(p^3y/2)(10R+2pR_p+3yR_y)
          +Delta(A_R H_y-B_R H_p).                    (7)
```

At this horizon one may use `H=H5`. This formula is separately compared
with literal `(x,t)` Hamiltonian differentiation of all background rows.

## 3. Complete image inside the incoming seven-equation family

Let `T` range over the sixteen monomials of valuation at least eleven,
valuation at most fifteen and weight `wt(p)=2,wt(y)=3` at most twenty-two,
with the single additional high column `k p^3y^6`.
Use the incoming seven-equation coefficient names and put

```text
alpha=[p^9y]T, beta=[p^6y^3]T, gamma=[p^3y^5]T,
rho=[p^2y^6]T, k=[p^3y^6]T,
v=[p^5y^4]T, w=[py^6]T.
```

**Complete image theorem.** The induced corrections of some carrier (1)
obey all the original source, bracket, prefix, cap and projected-depth
equations with source `T` if and only if `T` satisfies the incoming seven
conditions and these additional five independent conditions:

```text
[p^2y^5]T=0,                  [y^7]T=0,
[p^5y^3]T=-(5/6)gamma,
v=-(3916119864752 rho+35134702155 k)/1552077744720,
w=45(957062888 rho+31275315 k)/206943699296.             (8)
```

Thus `alpha,beta,gamma,rho,k` are free. The complete source image has
dimension five, with six-dimensional coordinate fibres after quotienting
out carrier tails of valuation at least twelve. In particular the high
coefficient `k` is unconstrained: a completed-carrier replacement exists.
For the actual boundary, prescribing `k=-j(s)` gives a source family
`G_m x A^4` inside the incoming `G_m x A^9` family, with carrier coordinate
fibres `A^6` inside the full `A^10` fibres. This is a finite response
classification; it is not a dimension statement for infinite solutions.

**Certificate and sufficiency.** For each of the thirty columns (4), the
producer retains `pi_15{A,S}` and `pi_15{C,S}`. Every column satisfies
every bracket coefficient below order fifteen, both degree caps, and
all **91** rows of the complete actual depth annihilator inherited from
the audited valuation-eleven system. The literal chain derivative of
`P(A,C)` agrees with (7) in every row. Thus no replacement coordinate
correction is silently chosen after testing the source image.

Equating that source to `T` gives a **constant** rational `51 x 47`
matrix on thirty carrier and seventeen source coefficients. Its exact
rank is 36. The source relation space has rank twelve. The verifier
constructs the entire eleven-column kernel, checks its identity minor,
and substitutes every column back into all original source rows. The
source projection has the five displayed identity coordinates; its kernel
is exactly (6). It separately verifies the incoming seven conditions.
These exact rational identities prove necessity and sufficiency over
every characteristic-zero field and every `Xi`, without a generic-rank
specialization or a denominator in the background parameter. The frozen
output contains the full five source columns and their thirty-coordinate
carrier lifts; these determine every coefficient of (8) and its inverse.

## 4. A short polynomial supplier and a sharp prefix obstruction

The following remainder has six terms, total `(p,y)` degree seven,
weight seventeen, and valuations between six and ten:

```text
Rstar=y^3(12015/15962432+(28467/3990608)p
          +(2499/498826)p^2-(4806/1247065)p^3
          +(639368/11223585)p^4)
      +(532468/6235325)y^5.                            (9)
```

With `Sstar=p^2 Delta Rstar`, its response is exactly, modulo `t^16`,

```text
Tstar=(108135/15962432)p^7y^2-(12015/1680256)p^3y^4
      +(208143/3990608)p^8y^2-(741987/7981216)p^4y^4
      -(196239/997652)p^5y^4+(180225/15962432)py^6
      +(346905/3990608)p^2y^6-p^3y^6.                 (10)
```

All seven low terms have weight at most twenty-two. Taking scalar time
`j(s)` cancels the inherited high coefficient `+j(s)p^3y^6` on the whole
actual boundary. At its zeros the map is the identity; no parameter
is divided by `j(s)` or by a function of `Xi`.

The additional earlier motion is necessary, not merely a feature of
(9). For every source in (8), the two valuation-six coefficients of `R`
at `p^4y` and `y^3` are

```text
[p^4y]R=-729(3990608 rho+346905 k)/517359248240,
[y^3]R=3(957062888 rho+31275315 k)/206943699296.         (11)
```

Their coefficient matrix in `(rho,k)` has determinant
`1751787/413887398592 != 0`. Preserving coordinate row ten would force
both to vanish by (3), hence `rho=k=0`. Therefore every nonzero high
replacement in this entire filter necessarily changes row ten. The
previous transport, which preserved a longer prefix, cannot be retained.

In fact the precise old normalized packet

```text
-(27945/235202)p^5y^4+(39123/470404)py^6
 +(52578/117601)p^2y^6-p^3y^6
```

satisfies all incoming seven equations but fails (8). Thus no universal
carrier with the allowed prefix produces that exact source packet, even
after varying its induced coordinate fibre. Its old finite realization
remains valid. The repaired statement is the different earlier-memory
replacement (9)–(10), rather than promoting its old generator.

## 5. Why this is an actual finite restriction of the completed flow

For every carrier admitted by (3), `S` has valuation at least eleven.
The literal derivation raises `t`-valuation by at least ten on every
polynomial and on `k[x][[t]]`. Consequently the scalar-time exponential
of the incoming completed theorem satisfies

```text
E_a(A)=A+a delta A mod t^16,
E_a(C)=C+a delta C mod t^16,
E_a(-u/2+H)=-u/2+H+a Psi_H(R) mod t^16.               (12)
```

All second and later iterates are beyond the horizon. Equivalently,
the nonlinear terms of the literal finite coordinate change have source
valuation at least twenty and bracket valuation at least nineteen.
Thus the entire source/bracket/depth response is preserved for arbitrary
scalar time, not only to first order in that scalar.

The actual map is the known continuous automorphism of the `Delta`-adic
completion of `B_2=k[x,u,p,y]`, restricted through the known continuous
map to the literal source completion and then to row fifteen. Finite
depth representatives of the inherited coordinates give the same rows,
regardless of their higher choices. This retains the completed affine
source form and regular source volume as supplied by that theorem. It
does not identify the `Delta`-adic completion with the logarithmic Laurent
completion, and does not turn the finite background into a completed
Keller solution. The later source coefficient produced by the flow need
not be polynomial, and no termination or rational-time assertion is made.

## 6. Reproduction and stopping boundary

The connection now has an actual source, target and map: a complete
filtered finite solution is carried to another by the restriction of
`E_a`, using the full induced coordinate columns. It preserves the entire
finite compatibility and completed source structure; it changes the prefix
starting at row ten and forgets no coordinate needed at this horizon.
The lost information is later compatibility and polynomial termination.
The strongest survivor is the proved completed operation; the cheapest
hostile to a stronger old-packet claim is (8), and to a longer fixed-prefix
claim is the nonzero determinant (11).

The next useful test is later compatibility for this named completed
orbit, with the actual later background retained. Merely enlarging the
finite source matrix or knowing a constant response quotient would not
pay that obligation. The torsion connection for a fixed smooth polynomial
is a separate object; this result supplies no identification with it.

The [standalone source](../../04-computation/planar_jc48_sep07_completed_response.py)
and [frozen output](planar_jc48_sep07_completed_response.out) use no
repository mathematical imports. The complete universe, rational module
kernel, literal and cusp-coordinate operator paths, every original row,
and positive/hostile controls are explicit.

```sh
python3 -B 04-computation/planar_jc48_sep07_completed_response.py
python3 -B -O 04-computation/planar_jc48_sep07_completed_response.py
```

Normal and optimized runs pass **4,025 always-active exact gates** and
agree byte for byte with the frozen output. The complete source/output
are frozen with these SHA256 pins:

```text
source 669567f67d3febc2c05089b4215a656d7bbfb1315527df319019b3b1c71d0b47
output 2427ecb11e54c8a9e386e13c061f7914a0616af5867f8907f5049751885d73da
semantic 51059577fc7fd0f0269d6f79f6144b16c7f3cd9e8145271479c8b0535ceabfdb
```

The [independent analytic/source audit](planar_jc48_sep07_completed_response_audit.md) passes. Its separate Fraction reconstruction confirms the full image and supplier with twenty additional always-active gates. No theorem identifier is allocated.
