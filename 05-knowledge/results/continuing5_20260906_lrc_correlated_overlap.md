# An actual balanced entry with zero separate interval credits and positive correlated credit

**Status:** PROVED elementary bounded entry class, with FINITE-EXACT located-count certificate; INDEPENDENTLY AUDITED. This is a method obstruction for an already-safe row, not a new unresolved LRC(14) entry or a closure of LRC(14).

## Inheritance and precise claim

The actual graph and box are those of THM-3818, `01-canon/theorems/THM-3818-scaled-inert-cubeclass-support-two-pair-packet.md`. The final grid implication is inherited from THM-2060, `THM-2060-crt-tail-coset-saturation.md`, and THM-2064, `THM-2064-multitail-sheet-capacity-and-dyadic-seam.md`. The clipped pair geometry retains the containment correction in THM-739, `THM-739-pairwise-coprime-bad-overlap-exact-bernoulli-closed-form.md`. The closest controls are `third_20260906_grid.md` and `continuing4_20260906_lrc_packets.md`: pooling lengths already failed there, but separate interval lower counts still closed the displayed class. Here **every separate interval lower count is zero**, and the interval locations recover a positive uniform count.

The concept board is: actual weighted components; bounded mixed relations; full hereditary profiles; clipped interval lengths; relative interval positions; a literal physical phase. No graph-rank surrogate is used for actual entry, and no scalar gcd surrogate is used for the profile bank.

Put Q=91^6=567869252041. Let P=722214566370 be the product of 198,215,251,257,263, and let V consist of P and the five numbers 83P/q:

```
V=(227923228170,233244393030,238819956210,
   278808413994,302746510145,722214566370).
```

Let H=3102310371501629 be the product of the seven pairwise coprime groups
`(107,149,167,173,179,191,197)`, and let U consist of H/q:

```
U=(15747768383257,16242462678019,17331342857551,
   17932429893073,18576708811387,20820874976521,28993554873847).
```

For every integer 967<=t<=1042 with gcd(t,H)=1, the physical row tV union U is primitive, distinct, boxed by sum<Q^2, has exactly the two actual atlas components of sizes6 and7, and satisfies W_(Q,3)=V_dec of rank11. It retains every inherited full profile. Every individual open interval in every one of the21 cofactor pair-danger intersections has translated t-grid lower count zero. At t=968, retaining the locations of the39 intervals of the pair107/167 gives uniform count at least15. The seven-danger excess is5, so the inherited overlap inequality guarantees at least10 safe points in every grid lifted from a V-safe phase.

## Actual entry, with all quantifiers

The strict atlas accepts a reduced pair sum at most356 only if **each** prime divisor is2 mod3 and each exponent is at most2. The six shape has exactly five star edges: its leaf sums with83 are281,298,334,340,346. The seven shape has exactly the six edges corresponding to group pairs
`(107,167),(107,191),(149,191),(149,197),(167,173),(167,179)`.
Both graphs are connected trees. All component prime supports are disjoint. Both shapes are primitive and contain no unit; V has mixed parity, U is odd. Also min(V)>3Q/28 and max(U)>28, so the old uniform minimum and small-maximum hypotheses do not describe this object.

The least gcd of a V pair is886860810. A mixed relation on two tV labels and one U label with nonzero U coefficient requires that coefficient to be divisible by t*gcd(V_i,V_j), because gcd(t*prod(V),prod(U))=1. This exceeds Q for t>=967. Conversely, for U_i=H/p,U_j=H/q, let D=H/(pq). A mixed relation with outside label tv has outside coefficient kD, k nonzero, and reduces to

`k tv + r q + s p=0`.

Thus it is impossible with |r|,|s|<=Q if t*min(V)>Q*max(p+q)=388Q. The exact joint sufficient threshold is

`max(floor(Q/886860810)+1, floor(388Q/min(V))+1)=967`.

This covers all105 and126 mixed supports, including zero internal coefficients. It also excludes any cross atlas edge. Every remaining support-at-most-three relation lies in an internal weighted kernel. Connected actual edge rows span those two kernels, proving W_(Q,3)=V_dec, not merely its rank. The maximum-scale physical sum is<Q^2.

Every subset of size7 through12 has gcd1: a mixed subset contains numbers with disjoint component prime supports, while the only unmixed such subset is the whole primitive seven shape. Its complete complement word is therefore all ones. The verifier checks that each such full word is actually allowed by the inherited JSON, and independently checks all4095 subsets of the t968 row. Bank: `04-computation/overnight12_20260906_lrc_decoder_descent_inherited_profiles.json`, SHA256 `935f3f687b6d7c89cc099e536f536238fd753bcc4c1747906d213cef387ca93f`.

At t968 the complete graph has11 edges and literal rational weighted rank11. The forward minimum coefficient is858481264080>Q; the reverse minimum signed-amplitude ratio is55157421217140/55083317447977>1. Physical sum is2075281984219247.

## Why every separate interval credit vanishes

For coprime p<q the open danger intersection has one origin component of length1/(7q) and, for each integer1<=k<(p+q)/14, two components of lengths

`min(2p,p+q-14k)/(14pq)`.

The minimum possible count in an open circle interval of length ell on a translated t-grid is ceil(t ell)-1; endpoints are excluded. Every displayed component has ell<=1/(7q). Every pair of distinct groups has q>=149, hence t ell<=1042/1043<1 throughout the class. Thus all of these lower counts are zero. The complete21-pair geometry is replayed independently as intersections of literal open arcs, including the containment clip and the wrap component.

This refutes the proposed supplier “an actual balanced retained entry in the reduced scale domain always has a positive separate pair-interval credit.” It does not refute safety or all grid methods.

## Exact repair: correlated rounding on a located union

For p=107,q=167, the literal circle intersection consists of39 components with total length2553/125083. Represent its wrap component as an open interval with negative lower endpoint, and all other components inside(0,1). For X_alpha={(alpha+j)/968 mod1}, its exact count is

`C(alpha)=sum_intervals [ceil(968*hi-alpha)-floor(968*lo-alpha)-1]`.

The only discontinuities occur at the78 distinct values968*lo or968*hi modulo1. Counts are constant on every intervening open cell. Evaluating all78 walls and all78 cell midpoints, with strict endpoints, gives

`min_alpha C(alpha)=15, max_alpha C(alpha)=23`.

The minimum is attained at alpha=0. The verifier also counts the literal two danger predicates at all968 grid points for each of these156 cases; it does not use the interval representation for that check. This finite wall partition is a complete proof for all real translates, not sampling.

For the actual U pair, multiplication by D=H/(107*167) sends any translated968-grid bijectively to another translated968-grid, since gcd(D,968)=1. Each individual U danger set contains at most ceil(968/7)=139 points. For danger multiplicity m, the pointwise excess `(m-1)_+` is at least the indicator of the selected pair overlap. Consequently the union of seven danger sets has size at most7*139-15=958, leaving at least10 safe points on every lifted grid.

V has clearance1/4 at phase1/4. Its968 physical lifts have phases(4j+1)/3872, and335 of them are strictly safe for the full row. The first is13/3872 with clearance885/3872. Strictness at these rational lifts also follows because a distance exactly1/14 would require7 to divide their denominator, which it does not. Separately, every physical speed is a7-unit, so1/7 has clearance1/7 and the complete first physical denominator bank is `(1,2,3,4,5,6)` at7. This simpler safety is explicitly retained; the example is not claimed to defeat all other suppliers.

## Connection contract and stopping boundary

Source: a complete actual entry. Target: a translated-grid safety certificate. Map: primitive cofactor pair, then its full located danger intersection. Preserved predicate: the exact count on every grid translate, using the coprime cofactor clock. Lost by the failed map: relative interval locations and correlated endpoint rounding. Needed sidecar: the78 endpoint residues, or their exact count profile. Cheapest decisive test: the39-component107/167 pair on the968-grid. Strongest survivor: generic CRT/overlap gluing remains valid; separate interval minima are only a sufficient relaxation.

No universal small-denominator or adaptive-packet forcing statement is inferred. The located profile is finite exact, not an all-t lower bound. The entire967..1042 class has actual entry and zero individual credits, while the uniform15 count and335 literal lifts are asserted only for t968. This bounded positive/hostile milestone is frozen without a further scan.

## Reproduction and pins

```
python ../../04-computation/continuing5_20260906_lrc_correlated_overlap.py
python -O ../../04-computation/continuing5_20260906_lrc_correlated_overlap.py
```

Both runs have4629 always-active gates and byte-identical actual-LF output. Source SHA256 `94415dca6e8445de037f8748c46a62f0b8b793d3331a6b1a386b6ce7b9507f82`; output SHA256 `f896aad46c791f8370a9b6aa802a6f7dd9cc2a7edfd7384488bd9bbe4f00156d`. No repo or Git mutation. The new finite count construction is an elementary inherited-method refinement, with an independent PASS audit; no scarce theorem ID is claimed.

Independent [proof and exact referee](continuing5_20260906_lrc_correlated_overlap_audit.md) passes.

Filed checkpoint provenance: the [raw-byte manifest](continuing5_20260906_manifest.json)
pins the final report, source and output. Reviewed candidate-report hashes
above refer to the pre-promotion bytes. Source-location defaults and local
links were made portable where necessary; all emitted outputs were replayed
as raw bytes. The independent audit supplies the stated promotion basis.
