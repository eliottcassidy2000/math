# Codex Comment: Boundary-Moment Adjunction As The Missing Picture

- Created: 2026-06-24T09:24:00Z
- Agent: codex

## Session Meat

I searched the current mainline around HYP-2950/2951/2952, then back through
HYP-2886 exact-period packets, HYP-2704 survival seven-packet quotient,
HYP-2840/gK8 decorrelation, and HYP-2666 two-gate boundary currency.
The now-completed HYP-2950 gauntlet found no counterexample in its named,
single-swap, two-swap, shell-alias, lcm-tail, or impostor banks; it also showed
that shell labels and raw tournament shadows are too coarse unless exact
M/Farey and Haar/Baire packet addresses stay attached.

The missing connection looks like this:

```text
adaptive exact-period packet boundary
  -> signed missed-depth boundary quotient
  -> gK8/L_y moment dual
```

The AP/GW packet census explains the `qdiv=14` boundary kernel.  The gK8/L_y
route explains why far or covering residuals should have positive moment
slack.  The exact-period atlas is the missing middle: it should convert a
minimal packet cover into the signed boundary object that the moment dual can
see.

The proof-shaped claim:

```text
ker(boundary-to-moment map) among zero-open packet boundaries
  = AP/GW orbit + named K33/state-lift debts,
    unless the gauntlet finds a genuinely new kernel class.
```

Then:

```text
qdiv < 14  -> q-witness wins.
qdiv = 14  -> AP/GW boundary-source-core census wins.
qdiv > 14  -> AP/GW kernel is impossible, so gK8/L_y positivity or K33 lift wins.
```

This reframes the summit as one bridge lemma rather than a pile of analogies.

## Random Repo Niche

HYP-2704 was the old clue.  It says the seven-sector survival currency is not a
raw sign pattern but a Stokes-like boundary quotient:

```text
C = p1+p2+p3+p4 - 4p6 = G1 - G5 - 4G6.
```

That is exactly the kind of object an exact-period packet cover should map to.
The packet atlas supplies the boundary faces; the gK8/L_y dual supplies the
positive functional.  AP/GW are the null boundary orbit.

## Connections

- HYP-2951: AP and GW are the only one-swap boundary-only rows in the tested
  AP neighborhood; two-swaps are positive-open.
- HYP-2952: apex-pressure tournaments are a necessary front filter but too
  coarse; all named low frontier rows remain transitive.
- HYP-2886: fixed denominator bases fail; adaptive exact-period packets are
  the right discrete atlas.
- HYP-2704: missed-depth currency is already a boundary quotient.
- HYP-2840 and THM-534: the far branch is gK8/L_y moment positivity plus
  decorrelation.
- HYP-2666: shell gates must be paid before scalar p1/moment tax.

Concrete next computation: add a HYP-2950 gauntlet column that records, for
each candidate row, its exact-period boundary packet, missed-depth image,
gK8/L_y slack, and kernel label `AP/GW | petal | K33 | wide-positive | unknown`.
