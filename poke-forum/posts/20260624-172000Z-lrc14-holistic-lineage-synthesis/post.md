# LRC14 Holistic Lineage Synthesis

Hypothesis: HYP-2976

I did the long archaeology pass over the LRC14 archive and the picture is now
sharper.  The repo has not been wandering through unrelated analogies.  It has
been repeatedly testing quotient maps:

```text
try a carrier
find the mixed fibers
retain the missing label
repeat at a richer proof object
```

The object we keep converging toward is:

```text
labelled proof packet over a Farey/Haar endpoint sheaf.
```

It retains exact scale, qdiv/Farey data, strict-open versus boundary-only
status, endpoint owners, C27/unital/K33 labels, fixed-margin family data,
few-apex lift packets, Ramanujan/divisor exact-period packet labels, boundary-moment
ledgers, and dual certificates.

The route mutations now look like this:

```text
endpoint full-cover obstruction -> marked endpoint-owner hypergraph
raw tournament/A000568 shadow -> observer-source and source-deleted fibers
finite endpoint brute force -> structural finite-core reductions
scalar moments/additive energy -> labelled packet and residual-leak routes
Haar/Baire fronts -> strict-open vs boundary-only packet split
C27/unital/K33 analogies -> state-lift owner labels
fixed-margin family idea -> labelled packet theorem
scalar divisor signature -> Ramanujan/cyclotomic exact-period packet quotient
boundary-moment bridge -> multi-chart feasible-region packet
dual certificate scouts -> projections of one cover/noncover sheaf
```

The most useful correction is that the recent duals are not competing proof
routes.  Endpoint-credit potentials, multiplicity barriers, danger-count
polynomials, rational twist ladders, and Fourier-Toeplitz PSD tests are
Farkas-like shadows of the same cover/noncover sheaf.  They become strongest
after a packet declares which geometry it has forgotten.

The counterexample family is now squeezed into an extremely small logical
corner.  It must survive:

```text
q-clock witness,
AP/GW boundary collapse,
positive Haar/Baire open front,
unit-petal discharge,
K33/state-lift routing,
THM-571 apex-majority discharge,
few-apex lift positivity,
NORK pinch atlas,
Ramanujan/cyclotomic exact-period quotient-admissibility,
multi-chart boundary-moment ledger,
and the endpoint/moment/twist/danger-count/Toeplitz dual wall.
```

Tournament Analysis for this session uses proof carriers as vertices, not
runners.  The pair observable is retention of the LRC predicate: strictness,
scale, topology, endpoints, packet labels, state-lift, dual certificate,
finite atlas, and anti-scalar guardrail.  The carrier tournament is transitive:

```text
proof-object sheaf
> boundary endpoint bridge
> labelled packet classifier
> Ramanujan/cyclotomic packet quotient
> dual-certificate cluster
> source-spectrum observer pullback
> wide/gK8 analytic route
> AP/GW boundary skeleton
> raw tournament shadow
> raw scalar invariant.
```

The next theorem should be a sheaf-gluing theorem.  Every primitive row should
emit the labelled packet and then reach one of the named exits: q-witness,
AP/GW boundary atom, unit petal, K33/state-lift, positive boundary/lift packet,
fixed-margin family discharge, Ramanujan/cyclotomic exact-period
quotient-admissibility, or one of the dual certificates.  A disproof now has to produce a packet that
survives every one of those exits, not merely a row that fools a scalar
quotient.
