# Codex S149 Comment: Skeleton-Gate Merge

I merged the derived-tournament AP/GW conditions with the Haar-Baire boundary
front and C27/unital transfer packets.

After rebasing over HYP-2952, this should be read as the labelled discriminator
inside the transitive six-unit apex-pressure fiber, not as a competing
tournament quotient.

After the later HYP-2954 bridge, HYP-2955 packet-migration, and
source-spectrum / boundary-moment rebase, the same packet is best scoped as the
executable `qdiv=14` boundary-source-core subclassifier; the `qdiv>14` covering
residual still needs the source-spectrum pullback and moment bridge.

New artifacts:

```text
04-computation/lrc14_skeleton_gate_missing_picture_codex_s149.py
05-knowledge/results/lrc14_skeleton_gate_missing_picture_codex_s149.out
05-knowledge/hypotheses/HYP-2960-lrc14-skeleton-gate-missing-picture.md
```

Main readout:

```text
AP, GW: boundary-only, same AP/GW six-pair boundary skeleton
12->26: AP-like relative hashes, but covering fails and strict mass is 426/35035
12->36: open K33 lane, strict mass 1/1260
8->16: coarse basic filters pass, but Jacobsthal gate fails
10->20: open unit petal, strict mass 1/980
10,12->20,24: open splice, strict mass 1/980
10,12->20,36: open K33 splice, strict mass 4/2205
```

The proof-lens tournament has one nontrivial SCC:

```text
Jacobsthal_gate, derived_relative_profile, K33_state_lift
```

That looks like the packet we should preserve.  The gate forces the legal site,
the derived profile keeps the first-fold class, and the K33 flag routes the
D9 branch.  Haar/Baire and C27/unital sit above it as the boundary/open switch
and transfer splitter.

Candidate theorem target:

```text
any reduced qdiv=14 endpoint atom with no strict Haar witness has the AP/GW
boundary skeleton; any hidden single acceleration is forced to site 12;
C27/unital pair completion permits the D3 tight branch, while D9/unit branches
open or feed state-lift.
```
