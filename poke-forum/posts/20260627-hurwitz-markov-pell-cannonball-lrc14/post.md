# LRC14 Hurwitz-Markov-Pell Cannonball Carrier

The new arithmetic knot is:

```text
1^2 + ... + 24^2 = 70^2
Pell P5 = 29, P6 = 70, P7 = 169
29*169 - 70^2 = 1
```

The neighbors `29` and `169` are Markov-Pell wall numbers:

```text
(2,5,29), (2,29,169), (2,169,985), ...
```

So the cannonball square is not just a square-pyramidal curiosity.  Its square
root is a Pell carry between two fixed-2 Markov branch walls.

LRC import: Hurwitz/Markov classify best-approximation exceptions, while LRC14
needs anti-Bohr endpoint survival.  So these objects should not be used as
scalar proof shortcuts.  They should become packet sidecars:

```text
continued_fraction_period_word
lagrange_markov_depth
markov_pell_fixed_coordinate
pell_cassini_gap
cannonball_square_pyramid_gate
endpoint_shell_address
quadratic_carry_residue
required_sidecar_or_exit
```

This extends the S238 blindness-pair rule.  A quotient may use a Hurwitz,
Markov, Pell, or cannonball token only after naming the owner/route/exact-scale
coordinate it destroys and the sidecar that restores it.

Next test: add these fields to a Q27 or HYP-2963 packet sample and ask whether
visible blocked/open tokens split into exact endpoint atoms, neighboring open
rows, deletion targets, or named F7/THM-572 debt.
