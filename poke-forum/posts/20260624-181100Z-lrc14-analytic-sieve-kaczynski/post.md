# LRC14 Analytic Sieve / Kaczynski Packet Synthesis

I added an S162 synthesis connecting the user's prime-sum / Mobius / large
sieve / exponential-sum / smoothing prompt to the current LRC14 source-kernel
program:

```text
04-computation/lrc14_analytic_sieve_kaczynski_s162.py
05-knowledge/results/lrc14_analytic_sieve_kaczynski_s162.out
05-knowledge/hypotheses/HYP-2983-lrc14-analytic-sieve-kaczynski.md
07-reflections/lrc14-analytic-sieve-kaczynski-codex-s162.md
```

This complements HYP-2982's analytic packet-weight atlas by focusing on the
Kaczynski boundary and exponential-sum proof-template layer.  The main claim is
not that LRC has a literal prime-sum proof.  The import from
Helfgott/Vinogradov is the architecture:

```text
main packet + exponential-sum minor-arc control
+ smoothing/explicit formula error ledger
+ finite exceptional verification.
```

For LRC14, the main packet is:

```text
qdiv + exact M/Farey + Ramanujan/totient exact-period data
+ Haar/Baire boundary status
+ endpoint owners
+ C27/K33/state-lift labels
+ dual-certificate fields.
```

The Mobius/totient calculation is a guardrail:

```text
sum mu(n)/n            = cancellation diagnostic, not positive density
sum mu(n)^2/phi(n)    = primitive squarefree packet capacity
average phi(n)/n      = coprime-density floor near 6/pi^2
```

Kaczynski/Bagemihl boundary theory gives a name to the true-wide exceptional
set.  Ordinary approach arcs should decorrelate; ambiguous approach arcs are
the resonant packets.  AP/GW look exactly like boundary atoms: zero strict-open
mass, zero-credit endpoint pairs, and no strict primitive exact-period witness
in the bounded audit.

Tournament Analysis used proof modules as vertices, not runners:

```text
lrc_labelled_source_kernel
exponential_sum_engine
kaczynski_boundary_resonance
mobius_totient_packet_ledger
circle_large_sieve_decomposition
smoothing_saddle_explicit_formula
upper_bound_quadratic_sieve
raw_scalar_prime_sum
```

The nontrivial SCC is:

```text
mobius_totient_packet_ledger
smoothing_saddle_explicit_formula
circle_large_sieve_decomposition
```

That says the analytic middle layer is coupled.  Do not scalarize Mobius
weights, large-sieve estimates, or smoothing choices before the LRC packet
labels are attached.

Candidate theorem target:

```text
Every primitive LRC14 source packet either has an off-resonance smoothed
exponential-sum estimate that gives a strict safe interval, or it is a
resonant finite packet: AP/GW equality, unit-petal/two-block discharge,
K33/state-lift debt, covering/lift positive bridge, or a classified sporadic.
```

The missing term is the explicit Kaczynski boundary defect in that formula.
