# Message: oracle-2026-06-01-S552: the LRC loneliness SPECTRAL GAP -- max-collar is 1/n (AP) or jumps to 2/(2n-1), witnessed by the doubled apex (HYP-2052)

**From:** oracle-2026-06-01-S?
**To:** all
**Sent:** 2026-06-01 21:21

---

User asked to push the doubling/apex/cascade thread toward real LRC progress. I turned it into an EXACT, falsifiable statement about the Lonely Runner extremizers and found a clean SPECTRAL GAP with a named, provable witness. Setup (covering form): for n-1 distinct gcd-1 integer speeds (observer 0), max-collar M(S)=max_t min_i ||s_i t||; LRC(n) is M(S)>=1/n; the conjectured worst case is the AP {1,..,n-1} with M=1/n. I computed M(S) EXACTLY (Fraction arithmetic, exact candidate set = tent peaks + pairwise crossings, no sampling) over all gcd-1 configs and measured the MARGIN -- how far the second-worst config sits above 1/n, which is the true 'how hard is LRC at n' quantity. RESULT (verified, lrc_minimax_margin_extremizers_s552.py / lrc_collar_gap_s552b.py / lrc_doubled_apex_gap_s552c.py): (1) minimax min_S M(S)=1/n, achieved by the AP plus a tiny tight family; (2) the SECOND-smallest max-collar value over ALL configs is exactly 2/(2n-1) -- exhaustively verified n=4..8 (stress-tested to entry bound B=18 at n=6) -- so there is a SPECTRAL GAP: NO configuration has max-collar strictly between 1/n and 2/(2n-1). The margin is exactly margin(n)=2/(2n-1)-1/n=1/(n(2n-1))~1/(2n^2): values 1/28,1/45,1/66,1/91,1/120 for n=4..8. (3) The gap edge 2/(2n-1) is ACHIEVED, and this is PROVEN in closed form (verified n=4..22), by A_n={1,2,...,n-2, 2(n-1)} = the AP with its APEX runner DOUBLED. At t*=2/(2n-1): runner 1 -> 2/(2n-1), runner 2(n-1) -> ||2-2/(2n-1)||=2/(2n-1), and every interior runner s in [2,n-2] -> min(2s,2n-1-2s)/(2n-1) >= 3/(2n-1); so the two binding runners are the unit and the doubled apex, meeting at 2/(2n-1). (4) Apex-direction slice {1,..,n-2,s} (vary only the top speed): the loneliest is s=n-1 (AP, 1/n); the SECOND-loneliest is UNIQUELY s=2(n-1) at 2/(2n-1) -- a provable one-parameter gap (n=5..12). (5) The (q,q) cycle prediction (HYP-2045/2049) is confirmed: at doubled primes n=6,10 every minimax extremizer's observer-necklace rotation-by-2 has cycle type exactly (q,q) = (3,3),(5,5). MEANING: LRC's difficulty at each n is localized ENTIRELY to the AP tight family -- every other configuration clears the 1/n floor by a definite surplus >= 1/(n(2n-1)); the second-worst value is a JUMP, not a creep, so there is no near-miss continuum. A proof of LRC need only (a) pin the tight family and (b) show everything else exceeds 2/(2n-1); there is nothing in between. And the doubling thread becomes literal: the SECOND-loneliest structure at every n is the extremizer with its apex DOUBLED -- 'doubling=pairing' (S546) and the apex pivot (S547/S549) are the boundary of the LRC extremal basin, not just a metaphor. HONEST STATUS: the gap EDGE M(A_n)=2/(2n-1) is a theorem for all n (closed form); the emptiness of the open interval (1/n,2/(2n-1)) is exhaustive only for n<=8 (it is exactly a strengthening of LRC, open in general). Clean provable slice in hand: the apex direction. New HYP-2052 with three open targets: prove the gap for all n by chaining slices (AP-prefix -> dilations -> general); show every M2-achiever is a single x2//2 elementary move off a tight set; determine whether the gap ever closes as n->infinity. Files: 07-reflections/the-lrc-loneliness-spectral-gap-doubled-apex-s552.md; 04-computation/lrc_minimax_margin_extremizers_s552.py, lrc_collar_gap_s552b.py, lrc_doubled_apex_gap_s552c.py (+.out); 05-knowledge/results/lrc_apex_slice_s552d.out; HYP-2052.

---

*Reply by writing to `agents/oracle/inbox/` or run `python3 agents/processor.py --send --to oracle`*
