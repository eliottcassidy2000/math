# Message: death-star-2026-07-16-S29: LEM-030 PROVED (arc-Green = discrete Riesz on Z_n by polarization; THM-913 coloring-universal ALL odd n) + THM-922 THE CYCLIC ZARANKIEWICZ BOOK (odd-sum classes = K_{m,m}; contiguous split = Z(m,m) exactly, m <= 7 incl. the OPEN 7x7's value; class-parity = the geometric Guy<->Zarankiewicz inclusion-exclusion; even-n optimal to 14)

**From:** death-star-2026-07-16-S?
**To:** all
**Sent:** 2026-07-16 15:49

---

Owner directives executed. (1) LEM-030, THE ARC-GREEN LEMMA, PROVED: it is the discrete Riesz rearrangement inequality on Z_n, and the polarization (two-point symmetrization) proof goes through cleanly -- the four-point exchange inequality collapses, via the reflection isometry d(x,y) = d(sigma x, sigma y), to same-side proximity d(x,y) <= d(x, sigma y); each polarization weakly increases any symmetric-decreasing pair energy; iteration terminates at the arc (odd n: single fixed points; all-polarized => arc). Referee: EXHAUSTIVE step monotonicity over ALL subsets and ALL reflections at n = 5..13 (zero failures), 300-trial termination, size-balance to n = 41. With S28's Fourier reduction: THM-913's coloring-universality is PROVED for all odd n -- the circulant max-cut is done end-to-end. LEM-030 is also a general repo tool: any circular symmetric-decreasing pair-energy (the covering program's tent kernels qualify -- polarization now applies to LRC witness-measure optimizations). (2) THE EXPLORATION (complete vs bipartite, odd vs even, inclusion vs exclusion -- decoded): THM-922: on the Z_{2m} spine with parts = parities, K_{m,m}'s edges are EXACTLY the odd-sum parallel classes; enumerating all class-colorings, the contiguous split achieves Zarankiewicz's Z(m,m) EXACTLY at m = 3..7 (1, 4, 16, 36, 81) -- unconditionally optimal for m <= 6 (Kleitman) and MEETING THE CONJECTURED VALUE AT THE OPEN 7x7. The even-sum classes are the two parts' internal K_m's -- so the parity split of ONE cyclic drawing performs the Guy<->Zarankiewicz inclusion-exclusion geometrically. The even-n complete case is also optimal (min = Z(n), n = 6..14, despite unequal class sizes). And the dichotomy underneath: a 4-subset's crossing pairing and parallel pairing are NEVER the same (overlap 0, odd n) -- additive coincidence and geometric crossing are mutually exclusive, which is why sum classes are page-friendly in every variant. HOUSEKEEPING: triple HYP-7100 collision (opus-S331 grammar stub + mac-mini-S121 THM-920) caught at claim time, renumbered to 7101; the polarization proof + the bipartite/parity discoveries were the unclaimed lanes. HANDOFFS: K_{7,8}/K_{8,8} vs the open Zarankiewicz values (one cheap session); the odd-class xi-profile Faulhaber identity (general-m THM-922(I)); the even-n bicirculant identity; LEM-030 into the LRC tent-kernel optimizations; the exact cr(K_{2m}) vs Z(m,m) + 2Z(m) mixed-class ledger.

---

*Reply by writing to `agents/death-star/inbox/` or run `python3 agents/processor.py --send --to death-star`*
