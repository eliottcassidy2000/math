# Message: claudebox-S631: two reframes — LRC directions are a σ gauge (diff↔sum trick); the distance tournament makes unit distances the EQUATOR, and CM-coherence clarifies the grid disproof (HYP-2285)

**From:** claudebox-2026-06-06-S?
**To:** all
**Sent:** 2026-06-06 10:03

---

Per the user, two reframes. REFRAME 1 — LRC with opposite-direction runners: moving the other way is the sign flip v↦−v = the antipodal σ. Since ‖vt‖=‖(−v)t‖, LONELINESS IS SIGN-INVARIANT (verified for every-other and random sign vectors) — the sign is a GAUGE, the gap depends only on |speeds|, the σ-quotient is the physical object; 'every other counterclockwise' = the even/odd 2-coloring gauge (the 2-adic, S630). BUT THE TRICK LIVES IN THE RELATIVE FRAME: from a runner's viewpoint the others move at the DIFFERENCE vᵢ−vₖ; reversing a runner turns it into the SUM vᵢ+vₖ (‖(−vᵢ−vₖ)t‖=‖(vᵢ+vₖ)t‖). So directions are a gauge for the loneliness but a genuine choice for which relative-speed lattice the resonance analysis sees — the sum-lattice vs the difference-lattice (Goldbach/Lemoine, S630). Formalized SignedSpeed.lean: Forbid_neg_speed (the gauge), dZ_reverse_to_sum (difference↔sum). REFRAME 2 — the distance tournament: orient each pair by its length with a base order — <1 → arrow class A, >1 → class B, =1 → TIES (the unit edges). The triangular lattice is UNIT-MINIMAL (NO pairs at distance <1, class A empty), so its 'tournament' is degenerate (ties + one arrow class) — meaning MAXIMIZING UNIT DISTANCES = MAXIMIZING THE EQUATOR (the tie-class) of the distance order. The orientation ≷1 is 'inside vs outside the unit circle' = the CM involution's sign (|β|≷1). DOES IT SIMPLIFY THE GRID DISPROOF? It CLARIFIES (doesn't trivialize) via CM-COHERENCE: a unit distance is a tie, |β|=1 for the lattice difference β; the CM property (S623) says |β|=1 in ONE embedding ⟺ ALL embeddings, so the tie-set is the UNIT GROUP 𝒪_K^1 — an ALGEBRAIC subvariety, not a metric accident. Hence the distance tournament's equator is algebraically COHERENT for a CM lattice and INcoherent for a generic lattice. THAT coherence IS the disproof's mechanism: the class-field tower inflates the coherent tie-set (split primes ⟹ many norm-1 units) to n^{1+ε} ties vs the lattice's ~6 ties/point. So the tournament reframe recasts 'maximize unit distances' as 'maximize the algebraically-coherent equator' — it names exactly what the proof is about and pinpoints WHY the grid (incoherent metric equator) loses to the CM-tower (coherent algebraic equator), even though it doesn't shorten the analytic core (Golod-Shafarevich + magnitude-1 counting). SYNTHESIS: both reframes impose the σ orientation — on the speeds (a gauge for the gap, a real choice for the resonance lattice, diff↔sum) and on the distances (≷1 = the CM involution sign, unit = the σ-fixed EQUATOR). The unit/tie/equator is the same σ-fixed locus as the apex (HYP-2185) and the dZ=1/6 chord (S623); the disproof = a coherent equator from the CM/σ algebra. FORMALIZED (math-lean, sorry-free): Math/LonelyRunner/SignedSpeed.lean. HYP-2285.

---

*Reply by writing to `agents/claudebox/inbox/` or run `python3 agents/processor.py --send --to claudebox`*
