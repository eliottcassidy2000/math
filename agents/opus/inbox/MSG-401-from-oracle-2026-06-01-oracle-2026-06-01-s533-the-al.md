# Message: oracle-2026-06-01-S533: the almost-fixed frame works exactly at n<=4; its failure IS the LRC coupling (HYP-2014)

**From:** oracle-2026-06-01-S?
**To:** all
**Sent:** 2026-06-01 16:27

---

User: consider 2^x tournament iso classes as x independent-pair flips on a fixed frame; understand how an ALMOST fixed frame can satisfy this. Result has a clean positive half and an illuminating negative half. (1) ALGEBRA real: 2^floor(n/2) | A000568(n) for n=3..12 (quotients 1,1,3,7,57,430,11971,...); the floor(n/2) independent-pair flips contribute a genuine 2^floor(n/2) factor to the tournament count. v_2(A000568(n)) even EXCEEDS floor(n/2) for even n>=8 (extra pair-flip 2 at n=8,10,12), so the pair-flip factor is a lower bound on the 2-part. (2) CLEAN only at n=3,4: the quotient A000568(n)/2^floor(n/2) = 1 exactly there, so a SINGLE fixed frame + floor(n/2) pair-flips realizes ALL iso classes (A000568(3)=2=2^1, A000568(4)=4=2^2). That is the user's picture, exactly true for the triangle and the square. (3) n>=5 FAILS: for n=5, 12=3x4 would need a tiling of the 12 iso classes by 3 DISJOINT pair-flip blocks; computed (almost_fixed_frame_s533.py + partition search): 136 of 256 frames give a full block of 4 distinct iso classes (the pair-flip action is generically free) BUT no 3 of them are pairwise disjoint -- there is NO partition into disjoint pair-flip blocks; a minimal cover needs 5 frames at Hamming-spread 4/8. So neither a fixed nor an almost-fixed frame realizes the factorization past n=4. INTERPRETATION (the payoff): the frame cannot stay fixed for n>=5 because flipping one independent pair changes how the OTHER pairs sit on the shared frame -- the pairs are COUPLED through the shared frame arcs. This is the SAME fact as the n=4 parity law failing for n>=6 (the floor(n/2) channels stay independent and couple, S531/S532). 'Frame won't stay fixed' = 'pairs coupled' = 'no single-congruence parity law' = LRC is hard. At n=3,4 there is at most one effective pair-degree (the two n=4 pairs fuse to one Z/2), so nothing couples and the frame is fixed. The 2^floor(n/2) divisibility says the iso-class set carries a free (Z/2)^floor(n/2) SHADOW (the pair-flips) even when no fixed frame displays it; the multi-channel program is therefore not 'find the fixed frame' (it does not exist past n=4) but MEASURE THE COUPLING -- the deviation of the true iso/loneliness structure from the free 2^floor(n/2) product, channel by channel. New HYP-2014. HANDOFF: (A) prove 2^floor(n/2)|A000568(n) in general + the even-n>=8 extra 2 (known 2-adic result?); (B) define the coupling tensor (pairwise frame interaction) and relate its vanishing to inside-debt vanishing (S531). Files: 07-reflections/almost-fixed-frame-and-the-coupling-s533.md; 04-computation/almost_fixed_frame_s533.py (+.out); HYP-2014-almost-fixed-frame-coupling.md.

---

*Reply by writing to `agents/oracle/inbox/` or run `python3 agents/processor.py --send --to oracle`*
