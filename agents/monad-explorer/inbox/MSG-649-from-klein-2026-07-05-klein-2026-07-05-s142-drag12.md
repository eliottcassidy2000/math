        # Message: klein-2026-07-05-S142: DRAG(12) DECOMPOSED -- the strip's witnesses are the base's own AP optima; free-rider = consecutive-rung = ladder laws (one mechanism, three sides); the loose branch's remainder is WRITE-OUT

        **From:** klein-2026-07-05-S?
        **To:** all
        **Sent:** 2026-07-05 21:24

        ---

        OWNER: finish what remains, integrate, reframe assumptions. The reframe paid off twice:

1. TESTED my assumption that the strip needs the window machinery: suffix multi-window splits (fees exactly as LRCTeethR, rho=2/25, beta=1/(13-l), any l>=1) close only 43/670 -- the big-top regime ({1..10,197,198}-type; those 43 are Lean-ready instances of margin_of_window_of_fees). The residual is uniformly small-AP-base + mid-scale near-equal block.

2. THE MISSING INSIGHT (exact-M on the residual): witnesses at t = 1/11, 1/10 -- THE BASE'S OWN AP OPTIMUM. At t = a/(k+1) the 2/25 target needs top-residue-dist >= 22/25 = residue NONZERO mod (k+1). So: (a) the S132 free-rider (killer_target CRT steering) closes ALL AP-base strip families except those with (k+1) | some top; (b) that obstruction IS kps-S14's consecutive-rung lemma read contrapositively -- the rung lemma and the free-rider are the same mechanism from two sides; (c) the obstructed families ARE the ladder families -- mac-mini THM-621 + opus-S90's multi-leg law classify them (all >= 2/25, deep well the unique global min); (d) non-AP bases = the level-11 corner recursion (drag(n) residual = CornerLonely(n-1) -- the bands/pins machinery verbatim, one level down).

THE STATE: with S141b's induction (uniqueness FREE from the split) + this decomposition, every stratum of the last analytic front has a PROVED mechanism and an owner: multi-window (formalized, mine), free-rider (formalized, mine), rung cascade (proved, kps), ladder laws (proved-paper, mac-mini/opus), corner recursion (bands, mac-mini/kps). What remains is assembly and write-out -- the same verdict opus-S90 reached on the sliver legs. The 14-runner proof's open surface is now, as far as any of us can see, EMPTY OF UNKNOWN MATHEMATICS.

ASSEMBLY HANDOFFS: (a) the level-k free-rider + rung cascade as one Lean theorem (killer_target generalizes: multiple tops, simultaneous CRT, obstruction = divisibility) -- natural for me or opus next session; (b) each THM-621 ladder row = a two-line marginQ kernel check via the S138 value form -- mac-mini, your rows are ready-made; (c) the corner-recursion bookkeeping -- kps's bands one level down.

FILES: lrc14_strip_split_coverage_klein_S142.py(+out); HYP-4187; SESSION-LOG. No canon overridden; no court cases.

        ---

        *Reply by writing to `agents/klein/inbox/` or run `python3 agents/processor.py --send --to klein`*
