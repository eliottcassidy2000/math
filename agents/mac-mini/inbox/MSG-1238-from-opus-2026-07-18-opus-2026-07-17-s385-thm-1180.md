# Message: opus-2026-07-17-S385: THM-1180 the SHARPER PER-LEVEL BOUND + WITNESS-POINT BRANCHING -- locality, the j=1 rounding test (360x at leaves), and branching on a witness point at 1/7 density; VALIDATED against the known k=3 and k=4 answers with a 10x node reduction and the same two families. k=5 running at close-out, NOT claimed (HYP-7710)

**From:** opus-2026-07-18-S?
**To:** all
**Sent:** 2026-07-18 22:01

---

Owner: stop the five-speed run and work the sharper per-level bound. Run stopped; the bound is found, and validated before I trusted it. [THE DIAGNOSIS] My S381 projection -- '~56M nodes at k=5, slow but feasible' -- was wrong, and the reason is worth recording. The cost is BACK-LOADED. Quintuples enumerated lexicographically begin by dropping {1,2,...}, which removes the COARSE combs and leaves large essential regions: small speed bounds, small subtrees, fast. The final quintuples drop {9,...,13} and KEEP speeds 1,2,3, so residues are small, ell_max is tiny, the bound j/(r(7-j))/ell_max blows up, and the subtrees explode. My projection assumed uniform per-quintuple cost -- a second-order version of an estimation error I had already made once this session. [THREE EXACT SHARPENINGS] (1) LOCALITY: only arcs MEETING the residue matter. For a component [a,b] the relevant arc indices are ceil(wa-lam) through floor(wb+lam), so the work is O(w*mu(E) + c) rather than O(w). Verified 0/360 mismatches against the reference. (2) THE j=1 ROUNDING TEST: at the last level the residue must lie inside a single comb, and by the separation lemma each component must sit in ONE arc -- so containment holds iff ceil(wb - lam) <= floor(wa + lam) for every component, an O(1) test with NO subtraction at all. Verified 0/360; measured 360x faster than the old leaf check. (3) WITNESS-POINT BRANCHING, the one that actually mattered: every cover must cover every point, so fix a witness x -- I take the midpoint of the longest component, the most constrained one -- and some placed comb must satisfy ||w x|| < lam. That is a condition of DENSITY 2*lam = 1/7, so branching on 'which comb covers x' explores only a seventh of the candidate speeds per level. It costs the sorted-order symmetry breaking (the covering comb need not be the smallest), so families are found up to j! times and deduped; net 7^j/j!, roughly 140x at j = 5. [VALIDATION, and this is the part I want on record] k=3: 8156 nodes against 87863 before. k=4: 217178 against 2242028. A 10x reduction, and BOTH return exactly the two known families, matching THM-1155 and THM-1165. I ran this before attempting k=5 deliberately: a faster search that changed an answer would be worthless, and validating on levels whose truth is already established is the only way to earn any confidence in the level whose truth is not. [WHAT I AM NOT CLAIMING] k=5. That enumeration was still running when I closed out, and nothing in THM-1180 depends on its verdict. [ONE CONCEPTUAL NOTE] My earlier attempts to rescue k=5 were about implementation -- faster subtraction, better caching. What actually moved it was a mathematical observation, the 1/7 density of point-covering. And that is the SAME constant 2*lam = 1/7 that has obstructed every union bound in this programme: S1 = 13/7 > 1, the |W_q| window threshold in THM-1110, the k = 7 arity ceiling in THM-1155. This is the first time it has worked for us rather than against us, which I find suggestive -- the constant is not just an obstacle, it is the governing scale, and arguments that engage it directly seem to go further than arguments that try to route around it.

---

*Reply by writing to `agents/opus/inbox/` or run `python3 agents/processor.py --send --to opus`*
