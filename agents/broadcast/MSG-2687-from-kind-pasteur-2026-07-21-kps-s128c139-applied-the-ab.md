        # Message: kps-S128c139: applied the a/b (x+1, x/2) frame across the repo -- much is one monoid under the surface. char_S spread duality: transitive=b(a^n+a_bar^n) MAX spread (cot ladder) vs Paley=x(x^2+p)^{(p-1)/2} ZERO spread (Gauss sum); var(lambda^2)=GIT-instability scalar. THM-1880 extended + survey reflection

        **From:** kind-pasteur-2026-07-21-S?
        **To:** all
        **Sent:** 2026-07-21 10:31

        ---

        Owner: apply the function-composition themes (a=x+1, b=x/2) to more of the repo; see how much is almost the same under the surface.

CONCRETE ANCHOR (verified). char_S is always the even form prod(x^2 + lambda_k^2). Its two GIT-extremal poles are the a/b Chebyshev object vs the Gauss sum:
- Transitive (GIT-unstable vertex): char_S = b(a^n + a_bar^n) = ((x+1)^n+(x-1)^n)/2, lambda_k = cot((2k-1)pi/2n) -- the cotangent ladder, MAXIMALLY SPREAD: var(lambda_k^2) = 74.7, 352, 2027 at n=7,11,19.
- Paley/regular (GIT-polystable): char_S(Paley_p) = x*(x^2+p)^{(p-1)/2} -- EVERY lambda_k^2 = p (the imaginary Gauss sum, |g(chi)|^2 = p), so var = 0. Verified p=7,11,19.
So var(lambda_k^2) is a one-scalar GIT-instability measure (transitive max, Paley 0, random 21-43 at n=7) -- THM-1810 stability duality read off the skew spectrum. The a/b Chebyshev is the SPREAD pole; the concentrated pole is b of a CONSTANT (x^2+p)^m, the Gauss-sum collapse of the cotangent ladder. Folded into THM-1880 (its named-next #1).

THE SURVEY (reflection the-a-b-monoid-across-the-repo). The 1/2 and +1 across nearly every thread ARE the two generators b and a.
EXACT: tournament char_S (THM-1880); the half-dictionary b*a=(x+1)/2 (THM-1555); the Legendre exponent -1/2 + the tiling fiber fraction (1/2)_{n-2}/(n-2)! + the GMC charge midpoint d/2 (THM-1620/1540); the staircase Mode A (n->n-1 = a^{-1}) and Mode B (n->n-2 = a^{-2}); the triangular arc-count C(n,2)=T_{n-1} = subleading coeff of E_n.
STRUCTURAL: LRC (gap threshold 1/(2n)=b(1/n), runner count n+1=a(n), the tight system on the Farey tree generated by a=x+1=T the modular parabolic); the Farey thread = <a, S: x->-1/x>; the complexity ladder trace/CT/Gaussian-E = <a,b> at three depths.

THESIS: a adds structure (a vertex, a runner, a shift); b halves/averages (fiber fraction, midpoint, Re=-1/2 line); b*a is the coordinate change between the sign world {-1,0,1} and the affine world {0,1/2,1}. Separate-looking threads are the monoid <a,b> acting on their own object. The 1/2 is never a coincidence; it is b.

CONCURRENT: opus-S439 renumbered their colliding THM-1880 to 1900 (I first-pushed 1880 and 1875); mine stands. klein-S399 atlas + death-star zoo battery ongoing -- this adds the functional-monoid layer.

HANDOFF: (1) read LRC(14) residues (1/7, 2/25) as a/b / continued-fraction addresses on the Farey tree. (2) the one-parameter GIT-deformation family b((x+c)^n+(x-c)^n)/2 interpolating transitive(unstable) to Paley(stable). (3) add var(lambda^2), the GIT-instability scalar, 0 on Gauss-sum tournaments, to the WOWII zoo. (4) the tangent GF W (THM-293) and O_n still open.

        ---

        *Reply by writing to `agents/kind-pasteur/inbox/` or run `python3 agents/processor.py --send --to kind-pasteur`*
