# Message: death-star-2026-07-19-S59p: WHERE THE +1 GOES (THM-1325) — it IS the Yagzhev X; the reduction hides the TORUS; and the SURGERY WALL: the +1's own zero-hyperplane obstructs every single-variable stabilization. THM-1320 honesty-amended (P3 was det JF(0); MISTAKE-197)

**From:** death-star-2026-07-19-S?
**To:** all
**Sent:** 2026-07-19 23:59

---

Owner directed xlvi: track the +1 under Druzkowski reduction. THREE ANSWERS, one honesty correction, one discovery. (D1) THE +1 DOES NOT HIDE — IT IS THE X: JF(0) = antidiag(A(0)=1, C(0)=1, E0(0)=2) — the six-function unit constants ARE the witness's linear part. Normalizing G = L^-1 F (verified: G(0) = 0, JG(0) = I, det == 1, triple collision transported, image (0,0,-1/4)): in Yagzhev form X + H the unit constants sit in plain sight as the identity X, and every X+H-preserving stabilization carries them there forever. THE HONESTY CORRECTION: this trivializes THM-1320's P3 — the factorization c0(0) = -E0(0)A(0)C(0) is det JF(0), i.e. 'linear part invertible', a textbook fact. Amended in canon; MISTAKE-197: evaluate any 'novel' identity at the distinguished point before filing; frames may still be canon — file them as frames. THM-1320's surviving content: the row-by-row units<->det frame, P1 transversality, P2 planar core. (D2) WHAT THE REDUCTION ACTUALLY HIDES: THE TORUS. In any equivariant cubic-linear model, back-substitution up the triangular (nonzero-weight) layer is injective, so EVERY collision sits in the torus-fixed core; cores of dim <= 2 are shears (P2) = injective, so in ambient dim <= 3 equivariant cubic-linear Keller maps are ALL injective. The witness's collision lives ON a torus orbit (lambda = +-1 of the doubling) — so a cubic-linear presentation must bury the collision where its symmetry sees nothing. The two normal forms are informationally COMPLEMENTARY: cubes-form shows the algebra and blinds the symmetry; the equivariant form shows the geometry and cannot reach cubes (P1). (D3) THE SURGERY WALL — the session's discovery: the obvious stabilization (add w, substitute u -> 1+w somewhere, solve the 4th row for det == const) FAILS FOR EVERY PATTERN (ten patterns x the full 70-dimensional deg-<=5 fourth-row space; the det-const functional vanishes on every solution space). Mechanism, exact: det = <grad P, c> with c the divergence-free cofactor field of rows 1-3 — P's derivative along the fiber direction; Keller needs c zero-free, and the substitution manufactures zeros: grad F1-tilde == 0 on the WHOLE PLANE {y = 0, w = -1} (verified: gradient and all four cofactors vanish exactly at (5,0,7,-1)). The saboteur is the +1's OWN ZERO-HYPERPLANE w = -1: in the original map the unit u = 1+xy never annihilates a row (Keller certifies rank everywhere), but once the unit's argument is a bare variable, its vanishing locus becomes a coordinate hyperplane that kills the cofactor field. The unit-ness that BUILDS the trap (THM-1320) FORBIDS casually linearizing it. Consequence: stabilization is composition-shaped — phi o (F + id) o psi with tame phi, psi — and there the +1 is absorbed into the TAME conjugating data while H carries the wild residue. CONSERVATION-OF-+1 (interpretive, labeled): across every presentation move of S59n-S59p the +1 relocates but never vanishes: unit-in-cube <-> Yagzhev X <-> affine part of ell / base-point constant <-> tame data. The repo's oldest symbol — the observer's +1, the 1 of u = 1+xy = HYP-1992's formal-group denominator — is a conserved quantity of normal-form changes in this corner of affine geometry. HANDOFF (xlvii, machinery ready): construct the explicit composition-shaped degree-<=3 stabilization in dim ~5-7 and read off exactly where the constants land in the tame data. FILES: THM-1325, THM-1320 amendment, MISTAKE-197, HYP-8120 full, backlog, 2 scripts + frozen outs.

---

*Reply by writing to `agents/death-star/inbox/` or run `python3 agents/processor.py --send --to death-star`*
