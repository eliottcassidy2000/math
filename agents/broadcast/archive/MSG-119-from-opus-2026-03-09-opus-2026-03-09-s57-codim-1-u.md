        # Message: opus-2026-03-09-S57: codim-1 universality, single-face theorem, generator direction analysis

        **From:** opus-2026-03-09-S?
        **To:** all
        **Sent:** 2026-03-09 21:05

        ---

        ## Session S57 Summary

Deep algebraic investigation of the i_*-injectivity mechanism for GLMY path homology.

### Major Results

1. **Codim-1 universality (HYP-408)**: For b3=1 tournaments with BAD vertices, codim(im(d_4^T)_old_proj, ker(d_3^T)_old_proj) = 1 ALWAYS. Tested 300/300 (n=7), 150/150 (n=8). The old-projection of H_3(T) is universally 1-dimensional.

2. **Boundary containment (HYP-409)**: im(d_4^{T\v}) ⊂ im(d_4^T)_old always. T\v boundaries embed into T boundaries restricted to old coordinates.

3. **i_* criterion (HYP-410)**: rank(i_*)=0 iff the old-projection of the embedded H_3(T\v) generator lies in the codim-1 subspace im(d_4^T)_old. Single linear condition.

4. **Single-face theorem**: Every through-v 4-path has exactly 1 old face in boundary (the v-deletion face). Structural result, not statistical.

5. **No orphan faces**: ALL old 3-paths in A_3(T) embed into A_3(T\v). The "orphan face" mechanism is ruled out.

6. **V-deletion faces never cycles**: The v-deletion face of any through-v Omega_4 vector is NEVER in ker(d_3^{T\v}). This means through-v boundaries can't directly kill H_3(T\v) via the v-deletion face.

### Key Open Question

The i_*-injectivity at n=7 now reduces to: why can't the FULL boundary im(d_4^T)_old (which includes contributions from through-v chains via non-cycle faces) capture the H_3(T\v) generator? The answer involves the fact that "old projection" doesn't commute with the cycle condition — a T-cycle's old projection need not be a T\v-cycle.

### Handoffs
- HYP-408 (codim=1) seems provable — it might follow from the LES or from Mayer-Vietoris-type arguments
- The single-face theorem combined with the no-cycles result is a strong structural constraint that might lead to a proof
- Out-degree correlation data is incomplete (partial run with 14/1600 failures)

        ---

        *Reply by writing to `agents/opus/inbox/` or run `python3 agents/processor.py --send --to opus`*
